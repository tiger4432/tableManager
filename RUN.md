# 지금 돌리면 되는 것

> 🔵 **17:48 갱신.** 아래 ⓞ 가 «제일 먼저»입니다. 나머지는 그다음입니다.
> 🆕 이번 pull 의 체인 로그 변화는 «부팅 로그에서 볼 것» 절의 `[ChainRule]` 줄에 있습니다.

## ⏱️ 바쁘시면 «이 셋»만 — 나머지는 «보고 나서» 찾아 읽는 자리입니다
```
① ⓞ 마이그레이션 (아래 «0.» 절)   -> 안 돌리면 쓰기가 전부 멈춥니다
② 재기동                          -> 저장소 루트에서 `run_app.bat`  (서버만: `run_app.bat --server-only`)
③ 「0.」 절의 순서 셋
```
📌 아래 ①~⑪ 은 «할 일»이 아니라 **「화면·로그에서 «본 것»의 뜻」** 입니다. 무언가 이상해 보일 때 그 번호를 찾으십시오.
   ⚠️ 그중 «할 일»이 있는 것은 ①(마이그레이션)과 ④(선언 둘 옮기기) 뿐입니다.

---

## 🆕 19:29 — 인리치의 «파생행» 반쪽도 이제 다른 것들과 «같은 맵퍼»로 돕니다

```
무엇이 바뀌었나  합성된 규칙 `enrichment_dedup:<이름>` 이 `mapper_module`/`mapper_function`(import 경로)
                대신 `mapper: "declared:enrich"` 를 답니다. «돌리는 몸통은 그대로»입니다
🔴 선언은 «한 칸도» 안 바뀝니다   enrichment_rules.json · 통합 `derive.decide` 둘 다 그대로 쓰십시오
                                 (파생행·오토컨펌·참조뷰 셋 다, SQL 칸 포함)
```
**확인 명령 — 붙여 넣으면 됩니다** (저장소 루트에서):
```
server\..\  <- 아래 한 줄을 server 폴더에서
python -c "import mapper_sdk;mapper_sdk.discover();from chain import dynamic_mappers as d;print(sorted(d.TEMPLATES))"
```
```
이 답이 나오면 맞습니다
  ['declared:decide', 'declared:enrich', 'declared:join', 'declared:virtual_join']
🔴 'declared:enrich' 가 «없으면»  -> 그 프로세스는 옛 코드입니다. 재기동하십시오
⚠️ 넷보다 «많으면»               -> 운영자 맵퍼가 같은 이름을 썼다는 뜻입니다 — 이름을 바꾸십시오
```
**부팅 로그에서 볼 줄**: `[ChainRule] enrichment_dedup:<이름> … mapper=declared:enrich`
**급하면 끄는 스위치**: 해당 enrichment 규칙의 `"enabled": false` — 종전과 같습니다

---

## 🧪 «직접 걸어서» 확인하는 법 — 규칙 하나를 선언하고 끝까지 따라가기

> 이번 라운드는 「규칙이 어떤 종류든 «한 길»로 돈다」입니다. 그것을 «직접» 재는 걸음입니다.
> 각 단계가 «무엇을 증명하는지» 옆에 적었습니다. 이 박스 컬럼 이름은 한 개도 안 씁니다.

### 두 줄 요약 (이게 안 되면 이 라운드는 실패입니다)
```
① 선언 파일에 규칙 한 줄: 「어느 표가 바뀌면(trigger_table) · 무엇을 돌릴지(mapper) · 이름(name)」
② 재기동하면 부팅 로그가 그 이름을 읽고, 대기열에 뜨고, 그 표에 행을 하나 넣으면 로그에 «한 줄»이 남는다
```
필수는 «셋»뿐입니다 — `name` · `trigger_table` · 돌릴 것. 나머지는 전부 선택입니다.

### 0) 화면으로 하실 거면 — Chain 탭 › 「규칙 등록」 › **+ 규칙 추가**
```
새 규칙 폼이 이렇게 나오면 «맞습니다»:
   name(필수) · derive(필수) -> [ join | decide | mapper ] 고르개 · into(필수) …
⚠️ 「trigger_table」 이 보이고 「derive」 가 «없으면» 옛 폼입니다 — 아래 함정을 보십시오
```
✅ **그 함정은 09-17 13:41 에 닫혔습니다.** 「기존 규칙을 한 번 열어 본 뒤 추가하면 옛 폼이 나온다」가
   이 자리에 적혀 있었습니다. 이제 «어느 순서로 눌러도» 새 폼이 나오고, 「취소」를 누르면 «보고 있던
   규칙»이 값째 돌아옵니다. 새로고침 먼저 하실 필요 없습니다.
🔴 **다만 화면이 안 바뀌면 «브라우저가 옛 것을 들고» 있는 것입니다** — Ctrl+Shift+R 로 한 번
   강제 새로고침하십시오. 총괄이 09-17 에 바로 이것 때문에 「아직 안 고쳐졌다」고 잘못 봤습니다.
   ⚠️ 그리고 «기존» 규칙을 열 때 옛 폼이 나오는 것은 «정상»입니다 — 그 규칙이 옛 문법으로 적혀 있으면
      그 문법대로 열어야 값이 안 보이는 채로 저장되지 않습니다(판정 514).

### 0-ter) 그 «평면» 규칙을 통합으로 «옮기고 되돌리는» 문이 열렸습니다 (14:58 착지, 서버 몫)
```
무엇이 생겼나   서버 라우트 하나. POST /admin/chain/rules/grammar
                body { "name": <규칙 이름>, "to": "unified"|"flat",
                       "base": <그 규칙을 열 때 받은 sha256 지문>, "dry_run": true|false }
🔴 `base` 는 «실제 저장»(dry_run:false)에 «필수»입니다 — 없으면 이름 대어 거절됩니다
   (「연 뒤에 파일이 바뀌었는지 가릴 수 없다」). 화면은 이 값을 자동으로 싣습니다
화면 버튼       «생겼습니다» (15:57 기준). Chain 탭 › 규칙 등록 › 규칙을 «고르면»
                「통합으로」 / 「평면으로」 버튼이 나타납니다
⚠️ 15:52 까지는 누르면 «항상 거절»됐습니다 — 확인창까지는 뜨고 «쓰기만» 안 됐습니다.
   고쳐졌습니다. 대신 규칙을 «열지 않고» 누르면 「지문 없음 — 다시 열고 눌러 주십시오」가 뜨는데,
   그건 «정상»입니다 — 열어야 「연 뒤에 파일이 바뀜나」를 가릴 수 있습니다
```
```
이 문이 지키는 것
   dry_run:true   「이렇게 바뀝니다」만 보여 줍니다. 파일을 «한 바이트도» 안 건드립니다
   되돌리기       to:"flat" 으로 «같은 문»을 반대로. 스냅샷도 이력도 필요 없습니다 —
                 왕복이 «항등»이라 되돌린 규칙이 «칸 하나까지» 원래와 같습니다
   한 규칙만      나머지 규칙의 직렬화가 «글자 그대로» 같습니다. 시험이 그걸 잽니다
   다시 도는 것   답에 reruns.rows 가 «0» 이면 「세었고 0」입니다.
                 🔴 그 칸이 «없으면» 「안 세었다」입니다 — 0 으로 읽지 마십시오
```

### 0-quinquies) 🔴 «10 초» 확인 하나 — 「통합으로」의 «쓰기»는 제가 «안 눌러 봤습니다» (16:2x)

```
왜 안 눌렀나   그 쓰기가 닿는 것이 server/config/chain_rules.json — «소유자 파일»입니다.
              제가 확인창을 넘기고 쓰는 것은 안 하기로 했습니다 (기록자는 하나입니다)
제가 «잰» 것   요청에 지문이 «실려 나갑니다» — 화면에서 요청 몸을 직접 찍었습니다:
              {"name":…,"to":"unified","base":"sha256:…","dry_run":true}
              그리고 지문이 «없으면» 아예 «안 보내고» 이름 대어 말합니다
제가 «안 잰» 것 「예를 누르면 쓰기가 «착지»하나」
```

**눌러 보시는 법 (10 초):**
```
① Chain 탭 › 「규칙 등록」 › 규칙 하나를 «고릅니다»  -> 「통합으로」 버튼이 뜹니다
② 누르고, 확인창에 «예»
③ 됐으면   그 규칙이 통합 문법으로 «바뀌어» 다시 그려집니다
   안 됐으면 «거절 문구»가 이름을 대고 뜹니다  <- 그 «문구 그대로» 알려 주십시오
④ 되돌리기  같은 자리에서 「평면으로」 — 왕복이 «항등»이라 칸 하나까지 원래대로 돌아옵니다
             (오늘 소유자 선언 10/10 으로 왕복 항등을 확인해 뒀습니다)
```
⚠️ 혹시 몰라 «백업»을 떠 뒀습니다:
```
C:/Users/kk980/Developments/.assy_watch/chain_rules.before_574_press.json
   (지금 파일과 sha256 동일: b2dcfb466177f8ed…  = 제 손으로 한 바이트도 안 바꿨다는 뜻입니다)
```

### 0-quater) 처음 저장하면 선언 파일이 «한 번» 정리됩니다 — 놀라실 자리입니다 (15:06)
```
무엇이 보이나   규칙 «하나»를 저장했는데 파일의 여러 줄이 바뀝니다 (예: 174 줄 -> 212 줄)
고장 아닙니다   제품은 «한 형식»으로만 씁니다(배열을 줄마다 펼침). 손으로 한 줄에 써 두신 것이
              그 형식으로 «한 번» 정리되는 것입니다
그 뒤로는      저장해도 줄이 «안 흔들립니다». 같은 파일을 다시 써도 바뀌는 줄 «0» 입니다(실측)
🔴 내용은 어느 쪽에서도 «안 바뀝니다»  통합으로 저장했다가 되돌리면 규칙이 «칸 하나까지» 원래와 같습니다
   (총괄이 이 박스 선언 «열 개»로 직접 확인: 잃은 칸 0 · 값 다른 칸 0 · 다른 규칙 0)
⚠️ 그래서 «첫 저장 전»에 그 파일을 한 벌 복사해 두시면 마음이 편합니다. 되돌리기는 화면으로도 됩니다
```

### 0-bis) 화면에 «평면 N» 이 새로 보일 겁니다 — 그 수의 뜻 (14:44 착지)
```
「평면 9」   아직 «옛 문법»으로 적힌 규칙이 아홉 개라는 뜻입니다. 고장이 아닙니다
왜 보이나   옛 문법에서는 조인·오토컨펌을 «고르개로 고를 수 없습니다». 그래서 몇 개가 남았는지
           화면이 말합니다 — 안 말하면 이관이 끝났는지 물어보셔야 합니다
줄어드는 때  규칙 하나를 통합 문법으로 «저장»할 때마다 하나씩 줄어듭니다. 0 이 되면 끝난 것입니다
🔴 제품이 «알아서» 안 바꿉니다  기동해도 · 열어봐도 안 바뀝니다. 누르셔야 바뀝니다.
   선언이 바뀌면 체인과 원장이 «다시 돌기» 때문에, 언제 돌지는 쓰는 분이 정하십니다
⏳ 아직 «바꾸는 버튼»은 없습니다 — 짓는 중입니다. 오늘은 «수만» 보입니다
```
```
「문법 모름」  규칙을 열었는데 이 말이 나오면, 제품이 그 규칙을 «어느 문법인지 못 읽은» 것입니다
             -> 폼을 «추측해서» 안 그립니다. 원문 편집기로 여십시오. 그게 맞는 동작입니다
```

### 1) 규칙을 적습니다 — `<설정 디렉터리>/chain_rules.json`
```jsonc
{ "name": "확인용",  "trigger_table": "<바뀌는 표>",  "target_table": "<채울 표>",
  "mapper": "<맵퍼 이름>" }          // 또는 "mapper_module" + "mapper_function"
```
📌 «내장 규칙»(조인·판정 등)을 쓰실 거면 `mapper` 에 그 이름을 적습니다. **같은 칸입니다** —
   이번 라운드가 없앤 것이 바로 「내장은 다른 칸·다른 길」이었습니다.

### 2) 재기동 → 부팅 로그
```
[ChainRules] set(N): 확인용[출처,방식] | ...
                           ^^^^   ^^^^
증명  «방식» 칸이 그 규칙의 «실제 종류»를 말합니다.
      🔴 어제까지는 조인이든 판정이든 «전부 mapper» 로 나왔습니다. 조인을 걸고 `join` 으로 찾으면 0 건이었습니다
```
❌ **이름이 안 보이면** — 바로 아래 거절 줄을 보십시오. 이번 라운드부터 제품이 «이름을 대고» 거절합니다:
```
· 돌릴 것을 못 찾음        -> 「규칙 '확인용' 이 <이름> 을 대는데 그것을 불러올 수 없습니다 (…)」
· 어디서도 안 도는 규칙    -> 「규칙 '확인용' 을 «집는 경로가 없습니다»」
🔴 어제까지 둘 다 «조용»했습니다. 선언은 받아들여지고 아무 일도 안 일어났습니다
```

### 3) 대기열 화면 — `GET /admin/chain/queue` (관리자 토큰)
```
증명  종류와 무관하게 «뜹니다». 2026-09-15 에 「체인 대기열에서 안 뜨고 돌고 있었네」 하신 그 자리입니다
      아직 안 돌았으면 「아직 평가 안 됨」, 한 번이라도 돌면 «마지막 결과»가 뜹니다
```

### 4) 그 표에 행을 «하나» 넣습니다 → 체인 로그
```
[ChainRule] rule=확인용 kind=<무엇이 돌았나> target=<표> rows_in=1 rows_out=N written=… refusal=… error=… elapsed=…
증명 ①  낱말이 «하나»입니다. `ChainRule` 한 번만 찾으면 «전부» 나옵니다
        🔴 어제까지 내장은 `[ChainBuiltin]`, 파일 맵퍼는 `[mapper@…]` 로 갈려 반쪽만 나왔습니다
증명 ②  `rows_in`/`rows_out` 이 «같은 함수»로 세어집니다 — 종류가 다른 두 규칙의 수를 견줄 수 있습니다
증명 ③  규칙이 «터져도» 이 줄이 남습니다(`error=`). 줄이 없으면 「안 돌았다」가 «참»입니다
        🔴 어제까지 터지면 아무 줄도 없어서, 못 찾은 것과 안 돈 것이 같아 보였습니다
```

### 5) 일부러 틀려 보십시오 — «조용하지 않은지»가 이번 라운드의 핵심입니다
```
· 돌릴 것의 이름에 오타       -> 부팅에서 «이름 대고» 거절 (어제까지: 파일 맵퍼는 파이썬 오류 스택)
· 아무도 안 집는 규칙로 선언   -> 부팅에서 «이름 대고» 거절 (어제까지: 조용히 안 돎 + 「아직 평가 안 됨」)
```

### ⚠️ 아직 «안 고친» 것 — 보시면 이상하지만 알고 있는 것입니다
```
· 규칙 목록 화면이 「Batch mapper / Row mapper」라고 씁니다 -> «맵퍼가 두 종류»라는 뜻이 아닙니다.
  같은 맵퍼가 «몇 행씩 받나»입니다. 낱말이 틀렸고 큐에 있습니다
· 한 «행씩» 처리하는 규칙이 맵 메타데이터·일괄 작업을 제안하면 «조용히» 버려집니다 (큐에 있음)
```

## 🔴🔴 이번 pull 에서 «달라지는 것» — 무엇이 보이면 무슨 뜻인가

### ① 마이그레이션이 «재기동 전»에 필요합니다 (아래 ⓞ)
안 돌리면 쓰기가 «전부» 멈춥니다. 상세는 ⓞ.

### ② 화면이 「못 읽었을 때」 더 이상 «0» 이라고 말하지 않습니다
```
전   거절당한 탭이 「0개」·「없습니다」를 그려서 «없어서 0» 과 «못 읽어서 0» 이 같은 그림이었습니다
후   거절이면 «수를 안 그리고» 「… 조회 실패 (HTTP 401). 수를 그리지 않습니다」라고 «이름»을 댑니다
     꺼진 버튼도 «자기 자리»에서 왜 꺼졌는지 말합니다
=> 숫자가 «사라져» 보이면 고장이 아니라 「못 읽었다」는 뜻입니다. 사유가 옆에 있습니다
```

### ③ 🔴🔴 「읽기 시점 조인」 은퇴 — 이제 «두 파일 다» 거절합니다. 그리고 컬럼이 «사라집니다»
```
사유   소유자 판정(09-16): 운영은 조인 컬럼을 «표에 써서» 씁니다 (into.table)
거절 ㉠  chain_rules.json 의 `into: { read: true }`
        -> 「읽기 시점 조인(into.read)은 은퇴했습니다 — …」
        할 일: `into: { table: … }` 로 바꾸십시오
거절 ㉡  🆕 virtual_join_rules.json 의 `materialize` 가 «false 이거나 없는» 선언 «전부»
        -> 「'materialize' is false, which declared a READ-TIME join … Declare the join in
           chain_rules.json instead - derive: {kind: "join"} with on and take …」
        할 일 «둘 중 하나»:
          · chain_rules.json 으로 옮긴다 (권장 — 값이 «표에» 써집니다)
          · 그 선언에 `materialize: true` 와 `max_rewrite_rows: <수>` 를 적는다 (쓰기 조인으로)
✅ 안 거절되는 것   `materialize: true` 인 선언은 «쓰기 조인»이라 그대로 «돕니다». 한 글자도 안 바뀝니다
🔵 ⚰️ **[해소됨] 위 ㉡ 의 수리가 «원장 setup 번들» 안에서도 됩니다** (판정 481·482 착지)
   몇 시간 전 이 자리에 「번들 안 조인은 그 수리를 못 한다」가 적혀 있었습니다. 참이었습니다 —
   빼면 로더가 거절하고 넣으면 번들이 «필드 자체»를 거절했습니다. 이제 아닙니다:
     · 번들 검증기가 `materialize: true` 를 «요구»합니다 (거절 문장은 위 ㉡ 과 «같은 문장»입니다)
     · ⚠️ **가상 조인은 «작성 화면에서 못 만듭니다»** — 화면이 이름 대어 거절합니다(`unauthorable_kind`,
       「cannot be created or removed on this screen」). 만들 수 있는 것은 술어·엔티티·소스 셋뿐입니다
       -> 가상 조인은 «파일에 적습니다**: `virtual_join_rules.json` 또는 원장 setup 번들.
          두 칸을 같이 적으십시오 — `materialize: true` · `max_rewrite_rows: <수>`
       ⚰️ 이 자리에 제가 «두 번» 틀리게 적었습니다(「이미 켜진 채 태어납니다」 · 「꺼진 채 태어납니다」).
          둘 다 «폼이 만든다»를 전제했는데 폼은 이 절을 아예 «안 만듭니다**. 셋째 줄이 오늘의 참입니다
     · 가이드(§6)의 «복붙용 JSON 블록»도 이제 그 두 칸을 들고 있습니다
   => 번들이든 `virtual_join_rules.json` 이든 «할 일이 같습니다»: 위 「할 일 둘 중 하나」 그대로
   ⚰️ 이 자리에 「고치려 하지 마시고 그 줄만 알려 주십시오 — 제품 수정이 필요합니다」가 있었습니다.
      그 제품 수정이 끝났습니다(481·482·484) — 이제 **직접 고치시면 됩니다.**
      번들이든 `virtual_join_rules.json` 이든 할 일은 위 「할 일 둘 중 하나」 그대로입니다 — 어느 쪽인지 가를 이유가 없습니다
```

### ③-bis 🔴 그래서 «화면에서 사라지는 것»이 있습니다 — 고장이 아닙니다
```
그리드      조인이 붙여 주던 컬럼이 행 페이로드에 «없습니다»
CSV 추출    그 컬럼이 머리글에 «없습니다». (created_at/updated_at 은 그대로 마지막입니다)
/schema    `virtual_columns` · `join_resolved_columns` 두 키는 «남아 있고 항상 []» 입니다
           (키를 지우면 화면이 깨지므로 지우지 않았습니다 — 키 제거는 클라와 같이 하는 별건입니다)
검색·필터   그 이름은 이제 «모르는 컬럼»입니다
정렬        `?order_by=<그 이름>` 은 «422 로 이름 대어 거절»됩니다 — 조용히 row_id 순서로 떨어지지 «않습니다»
쓰기        그 이름을 쓰면 «조용히 떨어집니다»(미선언 컬럼 드롭). 전에는 400 으로 거절했습니다
=> 값을 계속 보시려면 그 조인을 chain_rules.json 으로 옮기십시오. 그러면 값이 «표에» 들어가고
   그 뒤로는 평범한 컬럼이라 검색·정렬·필터·추출이 전부 됩니다
```

### ③-ter ⚠️ 제품이 만든 UNIQUE 인덱스가 «같이 걷힙니다» — 되돌릴 수 있습니다
```
왜      인덱스는 «그것을 요구하는 조인이 사는 동안»만 삽니다(S-248). 위 ㉡ 로 거절된 선언은
        더 이상 아무 키도 요구하지 않으므로, 제품이 자기가 만든 `uq_vjoin_…` 를 걷어냅니다
로그    [VirtualJoin] 인덱스 uq_vjoin_… (<표>) 를 «제품이» 걷어냈습니다 — … 다음: 없음.
        그 조인을 다시 켜면 제품이 다시 세웁니다.
⚠️ 걷힌 «동안»은 그 키의 중복이 «들어올 수 있습니다». 그 조인을 `materialize: true` 로 다시
   세우시면 제품이 인덱스를 다시 만들고, 만들다 중복 오류가 나면 «데이터에 실제로 중복이 있다»는
   뜻입니다(그때는 데이터를 먼저 정리하셔야 합니다)
✅ 운영자 이름으로 손수 만드신 인덱스는 «절대» 안 걷힙니다 — 접두 `uq_vjoin_` 인 것만입니다
```

### ③-quater 설정 리로드가 이제 search_columns 를 «항상» 판정합니다
```
전   엔진이 없으면 「가상 절반을 못 본다」고 보고 «아무 말도 안 했습니다»
후   검색 가능한 컬럼 = «선언된 컬럼» 이므로 연결이 필요 없습니다. 매 리로드마다 판정합니다
보이는 것   [Config] '<표>' declares search_columns that nothing can search: [...] …
할 일      그 이름들을 column_types 에 넣거나 search_columns 에서 빼십시오.
          (리로드는 «안 멈춥니다» — 오타 하나로 서버를 내리지 않습니다)
```

### ④ 🔴 dt_map 파생의 조인 둘은 이제 «체인 선언»에서 찾습니다
```
전   dt_log_confirmed_attribution · dt_log_frame_attribution 의 짝을
     virtual_join_rules.json 에서 찾았습니다
후   chain_rules.json 의 `derive: { kind: "join" }` 에서 찾습니다 (위 ③ 의 귀결)
보이는 것   없으면 「... is absent from the chain declaration」이 «이름을 대고» 뜹니다.
          조용히 무시하는 것이 아니라 그 자리에서 거절합니다
할 일      그 두 선언을 위 ③ 의 모양으로 옮기십시오. 조인 키는 `on`, 가져올 컬럼은 `take`
⚠️ 파생을 안 쓰시면 아무것도 안 보입니다. 그게 정상입니다
```

### ⑤ 합성 실패가 이제 «어느 쪽»인지 말합니다 — 재기동 뒤 로그에서 볼 줄
```
정상이면 이 줄은 «안 뜹니다». 뜨면 그것만으로 무엇이 멈췄는지 알 수 있습니다:
  [ChainRules] the virtual join half of synthesis failed, so materialised join rules are NOT running: ...
  [ChainRules] the enrichment half of synthesis failed, so dedup and auto-confirm rules are NOT running: ...
전   한 쪽이 죽으면 «둘 다» 죽었고, 로그는 「두 파일」을 묶어 말했습니다
    -> «사라진 규칙이 무엇인지» 알 수 없었습니다
할 일  그 줄이 뜨면 «그 반쪽만» 고치십시오. 다른 반쪽은 도는 중입니다
```

### ⑥ 조인 거절 문구가 «꺼져 있다»와 «없다»를 가릅니다
```
꺼두었을 때   ... is declared but no rule stands for it. The loader said: <이름>: enabled=false ...
없을 때      ... is absent from the chain declaration. Next: declare it there as
               `derive: {kind: "join"}` with `on` ... and `take` ...
전          둘 다 「absent」 한 줄이었습니다 -> 꺼 둔 운영자가 「안 썼다」는 말을 들었습니다
읽는 법     「enabled=false」 가 보이면 «선언은 있습니다» — 켜면 됩니다
```

### ⑦ 🔴 «설정 해소» 화면이 죽은 반쪽을 이름 대고, 그동안 「정상」이라고 안 합니다
```
전   합성의 한 쪽이 죽으면 그 규칙들이 화면에서 «그냥 안 보였습니다».
     그리고 그 표들에 「… 규칙이 없습니다. 파생이 필요 없는 표라면 이것이 정상입니다」가 붙었습니다
     -> «죽은 것»이 «원래 없는 것»과 같은 그림이었고, 화면이 그것을 「정상」이라고 «불렀습니다»
후   거절 목록에 이 줄이 섭니다:
       제품의 합성 중 «virtual join» 반쪽이 실패했습니다 — 표에 쓰는 조인 규칙이 돌지 않습니다. (…)
       제품의 합성 중 «enrichment» 반쪽이 실패했습니다 — 중복 제거·자동 확정 규칙이 돌지 않습니다. (…)
     그리고 표 줄의 끝이 바뀝니다:
       … 규칙이 없습니다. 합성의 반쪽이 실패했으므로 이 판단은 «불완전»입니다 — 위의 거절 항목을 먼저 보십시오.
할 일  ① 거절 목록의 «반쪽 이름»을 먼저 보십시오 — 무엇이 안 도는지 그 줄이 말합니다
      ② 표 줄의 「불완전」은 «그 표가 잘못됐다»는 뜻이 «아닙니다». 반쪽이 없어서 판단을 못 한다는 뜻입니다
      ③ 반쪽을 고치고 화면을 다시 여십시오. 「정상입니다」가 돌아오면 그때가 진짜 정상입니다
⚠️ 둘 다 정상이면 이 줄들은 «안 뜹니다». 항상 켜진 경고가 아닙니다
```

### ⑧ 🆕 체인 그래프의 머리 줄이 「0」과 「못 읽음」과 「거절됨」을 «가릅니다»
```
전   네 선언 중 하나를 «못 읽으면» 그 수가 «0» 으로 나갔습니다 — 「선언이 없다」와 같은 픽셀
후   ① 읽었고 없으면      그대로 0. 그 0 은 답입니다
     ② 못 읽었으면        그 이름이 머리 줄에서 «사라집니다». 수를 안 그립니다
                        사유는 응답의 `unread` 에 있고, 원장 쪽은 지금처럼 화면에 문장으로 뜹니다
     ③ 거절됐으면        `<이름>_refused N` 이 «더 붙습니다» — 선언은 있고 제품이 안 받은 것입니다
읽는 법   수가 «안 보이면» 고장이 아니라 「못 읽었다」입니다
         `..._refused` 가 보이면 «파일을 고쳐야» 합니다(「선언이 없다」의 정반대입니다)
⚠️ 위 ③(읽기 시점 조인 은퇴)을 pull 하시면 `virtual_joins_refused` 가 «보일 수 있습니다» —
   그건 이 그림이 고장 난 것이 아니라, 은퇴한 선언이 몇 개인지 «처음으로» 보이는 것입니다
```

### ⑨ 거절 문장 둘이 «더 정확해집니다»
```
체인 바인딩   전: 「'T' declares neither a single-column 'map_key_columns' nor ...」
             후: 유도가 «거절»된 경우에는 그 거절 사유가 문장에 실립니다
             -> 전에는 «이미 적어 두신» map_key_columns 를 적으라는 말을 들으셨습니다
맵 기준 좌표   전: 바인딩을 못 읽으면 「기준 맵 '…'에 좌표가 없습니다」 (조회 조건 «없이»)
             후: 「키 바인딩을 읽지 못해 무엇을 조회할지 정하지 못했습니다 — <사유>」
                 코드는 `binding_unresolved`
             -> 「조회 조건」 절은 «키가 잘못 쪼개진 것»을 찾는 열쇠인데, 하필 못 읽었을 때만 빠졌습니다
```

### ⑩ 🆕 어드민 「최근 거래」 목록이 «다음 장»을 받을 수 있습니다 — 그리고 «빠지던 그룹»이 안 빠집니다

```
전   `/audit_logs/recent` 이 `next_cursor` 를 «내주면서 받지는 않았습니다».
     그리고 그 커서는 «스캔 위치»(청크 바닥)라, 「페이지가 찼다」와 「청크가 끝났다」 사이의
     거래 그룹이 «1 장에도 2 장에도 없었습니다». 오류는 «안 났습니다»
후   같은 라우트가 `?cursor=` 를 «받습니다». 커서는 이제 «마지막으로 내놓은 그룹»의 위치이고,
     다음 장은 이미 내놓은 그룹을 건너뜁니다 -> 두 장이 «겹침 0 · 빠짐 0»
     못 읽는 커서는 «400» 입니다 (조용히 1 장으로 되돌아가지 «않습니다»)
```
**확인하실 때** (서버가 떠 있으면 한 줄, 읽기만 합니다):
```bash
curl -s "http://localhost:8000/audit_logs/recent?limit_groups=5" | python -c "import sys,json; b=json.load(sys.stdin); print('truncated=',b['truncated'],'next=',b['next_cursor']); print([g['transaction_id'] for g in b['groups']])"
```
```
`truncated: true` 이고 `next_cursor` 가 있으면   -> 그 값을 `&cursor=<그 값>` 으로 다시 부르면 «다음 5 그룹»입니다
`truncated: false` · `next_cursor: null`        -> 이게 이력의 «끝»입니다. 정상입니다
⚠️ 화면은 «아직 그대로»입니다 — 타임라인 패널에 「더 보기」 버튼이 «없습니다».
   서버 절반만 선 것이고, 버튼은 클라 레인의 «별건»입니다. 지금은 위 curl 로만 보입니다
```

### ⑪ 🆕 개발 벤치가 이제 제품이 만드는 맵퍼(`declared:…`)도 «돌립니다» (09-17 — 어제 적은 것의 «정정»)

```
전   벤치가 builtin:… 을 «이름 대어 거절»했습니다 (「등록된 종류이지 파일 맵퍼가 아니다」)
후   «돌아갑니다». 맵퍼가 전부 «페이로드»를 받게 되면서 거절할 이유가 없어졌습니다 (판정 562)
```
⚠️ 이 자리에 «반대로» 적혀 있었습니다 — 거절이 «새로 생겼다»고 썼는데 실제로는 «없어졌습니다».
   그리고 「그런 규칙은 소급 화면에서」라는 안내도 이제 «불필요»합니다. 벤치에서 됩니다.
🔴 다만 벤치가 보여 주는 것과 «운영이 하는 것»이 «세 군데» 다릅니다 (판정 591·593):
```
입력 거르기 · 답 거르기 · 그리고 «팬아웃»
   운영은 is_batch 가 아니면 «한 행씩» 부르는데 벤치는 «항상 전체 리스트»를 건넵니다
=> 비-배치 맵퍼를 만드실 때 벤치가 보여 주는 «모양»을 그대로 믿지 마십시오.
   다음 라운드에서 벤치를 좌석에 태워 맞춥니다
```
### ⏪ 되돌려야 할 때
```
🔴 이 절의 ①~③ 은 «각 항목마다 따로»입니다 — 이번 pull 에 라운드가 여럿 들어 있습니다
① ③(읽기 시점 조인 은퇴)은 «커밋 하나»입니다. 해시는 이렇게 찾으십시오:
   git log --oneline -1 -- :/server/chain/legacy_materialized_join.py
   git revert <그 해시>
   -> 되돌리면 조인 컬럼이 화면에 «돌아오고», 제품이 걷었던 uq_vjoin_ 인덱스는
      «자동으로 돌아오지 않습니다». 그건 선언이 다시 요구하는 순간 제품이 세웁니다
② ⑩(최근 거래 다음 장)도 «커밋 하나»입니다:
   git log --oneline -1 -- :/server/admin/audit_cache.py
   git revert <그 해시>
   -> 되돌리면 `?cursor=` 가 «무시»되고(파라미터가 사라집니다) 빠지던 그룹이 다시 빠집니다.
      화면은 어느 쪽이든 «똑같습니다» — 아직 그 버튼이 없기 때문입니다

② 인덱스 좌석 이동(그 앞 라운드)은 «커밋 둘»입니다 — 하나만 되돌리면 트리가 깨집니다
   git revert 21abf176 7ecc6a1f
```

---
> 이 파일은 **푸시할 때마다 갱신**됩니다. 지금 main 에 있는 것 기준입니다.
> 🔴 «아래 파이썬 명령들»은 `server/` 에서 돌립니다 (위 git·psql 은 «아닙니다» — 각자 적어 뒀습니다). 운영은 그 환경의 `python`, 박스는 `C:\Users\kk980\anaconda3\envs\assy_manager\python.exe`.

---

## 0. 지금 순서 «셋» (2026-09-16 10:5x 기준 — 이 순서대로 하시면 됩니다)

---

### 🔴🔴 ⓞ 재기동 «전»에 «먼저» — 마이그레이션 하나 (2026-09-16 22:2x 추가)

**pull 하신 코드는 `cell_sources` 에 컬럼 하나를 «씁니다». 그 컬럼이 없는 데이터베이스에서는
모든 쓰기가 죽습니다** — `create_all` 은 «이미 있는 표»에 컬럼을 절대 안 더합니다.

```bash
# 🔴 «저장소 루트»에서 (server/ 에서 돌리면 경로가 틀립니다)
psql "$DATABASE_URL" -f server/migrations/add_cell_source_origin_row.sql
```

```
안 돌리고 재기동하면   (psycopg2.errors.UndefinedColumn) column cell_sources.origin_row_id does not exist
                    -> 인제션·체인·그리드 편집이 «전부» 멈춥니다
비용                ADD COLUMN ... NULL 은 PostgreSQL 11+ 에서 «메타데이터만» 바꿉니다 — 표를 다시 안 씁니다.
                    행이 3천만이든 0이든 같습니다. 백필 «없습니다»
인덱스              CONCURRENTLY 라 «잠그지 않습니다». 중간에 끊기면 무효 인덱스가 남으니 그때만 확인:
                    SELECT c.relname FROM pg_index i JOIN pg_class c ON c.oid=i.indexrelid
                    WHERE NOT i.indisvalid AND c.relname='idx_sources_by_origin';
새 DB               아무것도 안 하셔도 됩니다
```
**무엇이 좋아지나:** 체인이 «다른 표의 행을 읽어» 채운 칸이, 그 «읽힌 행이 지워지면» 같이 사라집니다.
지금까지는 남아 있었고, 남은 값은 «측정한 값»과 화면에서 구별되지 않았습니다.

### 🔵 재기동 뒤 로그에서 볼 줄 (이번 라운드)

```
[ChainRetract] table=<표> deleted_rows=N groups=G cells_withdrawn=C protected_skipped=P
```
지워진 행이 «먹이던» 칸 C 개를 거뒀다는 뜻입니다. `cells_withdrawn=0` 은 정상입니다 —
그 행이 아무 칸도 안 먹였거나, 도장이 생기기 «전»에 쓰인 칸이라는 뜻입니다(소급 없음).


---

### ① pull + 재기동 — 🔴 **사흘 끌던 인제션 멈춤이 여기서 풀립니다**

```bash
git pull
run_app.bat
```
돌던 창은 «먼저» 닫으십시오(Ctrl+C). 서버만 띄우실 때는 `run_app.bat --server-only` 입니다.
`run_app.bat` 은 저장소에 들어 있고 conda 환경을 «스스로» 찾습니다 — 파이썬 경로를 적으실 필요가 없습니다.

그 산출물이 안 들어간 이유는 **선인출 진단기**였습니다. 청크가 느릴 때(기본 1.0 초 초과) 실행 계획을 한 번 찍어 주는 자리인데, 그것이 **이미 닫힌 세션의 객체를 다시 읽으려다** 던졌고 그 예외가 **파일을 통째로** 데려갔습니다. 그래서 ⓐ 그 산출물 하나만, ⓑ 매번 같은 자리에서, ⓒ `__force__` 로도 안 됐습니다 — 방아쇠가 파일이 아니라 «걸린 시간»이었기 때문입니다.

pull 하시면 그 자리가 ① 세션을 안 쓰고 아이디를 읽고 ② 그래도 무엇이 터지면 **계획만 건너뛰고 청크는 그대로** 갑니다.

```
[Ingest] <표> chunk N: ... · INSIDE THE WRITE · prefetch 10.xxx s ...   <- 계속 나옵니다(느린 건 그대로)
[Ingest] <표> prefetch 10.xxx s over 1.0 s - PLAN (once per file) | target: ... | cell_sources: ...
```
🔵 **두 번째 줄이 이제 파일당 한 번 뜹니다.** 그 줄의 `target:` / `cell_sources:` 에 `Seq Scan` 이 보이면 인덱스를 안 타는 것이고, `Index Scan` 이 보이면 인덱스는 타는데 양이 많은 것입니다 — 그 한 줄만 주시면 다음 수리가 갈립니다.

> ⛔ **급할 때 끄는 스위치:** 설정 폴더의 `ingestion_settings.json`(그 설치의 config 경로) 에 `"slow_prefetch_explain_seconds": 9999` 를 넣으시면 됩니다. 계획 줄만 사라지고 인제션은 그대로입니다.

### ② 「가림 N」 세기 — 제가 기다리는 수입니다 (읽기만, `--apply` 없음)

```bash
python scripts/count_absent_null_layers.py
```

마지막 줄의 «가림» 숫자 하나만 주시면 됩니다. 0 이 아니면 그만큼 조인 값이 파일의 빈 층에 가려져 «안 보이는» 중입니다 — 지우기는 되돌릴 수 있게 별도로 올리겠습니다. 뜻은 §1-bis 표에.

### ③ 조인이 «안 선다»고 느껴질 때만 — §1 로

지금은 조인이 서 있어야 정상입니다. 안 서면 §1 의 한 줄이 이유를 셋 중 하나로 가릅니다. 평소엔 안 돌리셔도 됩니다.

### ④ 「savepoint 명령은 트랜잭션 블럭에서만」이 보이면 — 끄는 스위치가 있습니다

로그(체인·인제션 워커)에 아래 낱말 중 하나가 보이면 같은 것입니다:

```
savepoint 명령은 트랜잭션 블럭에서만 사용할 수 있음   ·   SAVEPOINT   ·   25P01
no_active_sql_transaction
```

**무엇인가:** 큰 적재 뒤에 도는 «통계 다시 재기(ANALYZE)» 좌석이 연결 하나를 빌려 쓰고 **되돌려 놓지 않은 채** 반납합니다. 그 연결을 다음에 받은 작업이 이 문장을 뱉습니다. **적재된 행은 무사합니다** — 그 뒤에 그 연결을 쓴 «다른» 작업이 실패합니다.

> ⛔ **끄는 법:** 설정 폴더 `ingestion_settings.json` 에 `"analyze_after_rows": 0`.
> 그 좌석이 아예 안 돕니다. 대가는 큰 적재 직후 «페이지 질의가 한동안 느린 것»이고, 수리가 착지하면 되돌리시면 됩니다(기본값으로 돌리려면 그 줄을 지우십시오).

🔴 **순서가 증거입니다.** 같은 표에 대해 이 줄이 «먼저» 나오고 뒤에 위 문장이 나오면 이 사슬이 맞습니다:

```
[<표>] statistics re-analysed after N row(s) ...      (성공해도 오염은 남습니다)
[<표>] could not re-analyse after N row(s): ...       (실패해도 마찬가지)
```

⚠️ 이것은 **사흘 장애가 풀리면서 드러난 것**입니다 — 그 적재가 끝까지 가게 되자 이 좌석이 처음으로 돌았습니다. 수리는 진행 중(S-272)이고, 착지하면 이 절을 지우겠습니다.

### ⑤ 「체인이 도는가」를 한 줄로 — 🔴 «할 일이 없다»와 «멈췄다»를 가릅니다

```bash
curl -s http://127.0.0.1:8080/health
```
(포트는 그 설치의 API 포트. 브라우저로 열어도 됩니다.)

그 답에서 **수 둘만** 보십시오 — `checks.workers.chain.age_seconds` 와 `checks.outbox.pending`:

| 대기(pending) | 체인 박동(age) | 뜻 | 조치 |
|---|---|---|---|
| 0 | 몇 초 이내 | ✅ 정상 — 할 일이 없는 것 | 없음 |
| 0 | 낡음/없음 | 🔴 **루프가 죽었다** — 큐가 비어 있어 «정상처럼» 보입니다 | 재기동 |
| > 0 | 몇 초 이내 | 🔴 루프는 도는데 **소비를 못 한다** | 아래 ⑤-bis |
| > 0 | 낡음/없음 | 🔴 멈췄고 쌓이는 중 | 재기동 |

🔴 **두 번째 줄이 제일 위험합니다** — 대기가 0 이라 화면상 아무 문제도 없어 보이는데 체인은 죽어 있습니다. 「인제션은 잘 되는데 체인이 아예 안 돈다」가 이 모양입니다.

⚠️ **맨 위의 `status` 단어는 «보지 마십시오».** 일꾼이 전부 멀쩡해도 감독자 파일이 낡으면 `unhealthy` 로 나옵니다(알려진 결함, 큐 S-273). **수 둘**이 답입니다.

### ⑤-bis 쌓인 것이 «무엇인지» 볼 때만

```bash
python scripts/outbox_triage.py --count
```

읽기만 합니다(`--apply` 없이는 아무것도 안 바꿉니다). 표가 «표 이름 · 모양 · 사유»로 나옵니다 — 그 세 낱말을 주시면 제가 다음을 가릅니다.

⚠️ **§6(PG 시험)은 코드를 고친 뒤에만** 돌리시면 됩니다 — 운영에서 정기로 돌릴 것이 아닙니다.
---

## 1. 조인이 왜 안 서는지 «한 줄로» 가른다

```bash
python scripts/check_one_row_one_fact.py --db
```

읽기만 합니다. 답이 셋 중 하나이고 **수리가 전부 다릅니다**:

| 답 | 뜻 | 고칠 곳 |
|---|---|---|
| 키가 «신원보다 좁은» 조인 | 유일 인덱스가 «영원히» 못 섭니다 | 🔴 **선언** — 조인 키를 신원까지 넓히기 |
| 같은 키 행이 있음 (진짜 중복) | 사본이거나 다른 사실이거나 | 아래 2번으로 가름 |
| 키가 «비어 있는» 행 | 중복이 아니라 **부재** | 키를 채우거나 null 정책 선언 |

## 1-bis. 어제까지 쌓인 «파일의 NULL 층» 세기 (S-243-b — 읽기만)

```bash
python scripts/count_absent_null_layers.py
```

판정 405 는 «앞으로의 쓰기»를 고쳤습니다 — 파일의 빈 칸은 이제 층을 안 세웁니다. 그런데 그전에 세워진 층은 그대로 남아 조인 값을 «오늘도» 가립니다. 이 명령은 그 수를 내며, 아무것도 쓰지 않습니다(`--apply` 가 «없습니다»).

| 답 | 뜻 | 조치 |
|---|---|---|
| 「NULL 층: 없음」 | 이 설치엔 쌓인 것이 없습니다 | 없음 |
| 가림이 **0** | NULL 층은 있지만 그 밑에 값이 없습니다 | 없음 — 지워도 화면이 안 바뀝니다 |
| 가림이 **0 아님** | 그 수만큼 조인 값이 «안 보이고» 있습니다 | 그 수를 총괄에게. 지우기는 별 지시(S-243-c)이고 «먼저 내보낸 뒤» 되돌릴 수 있게 |

🔴 이 명령이 「없음」이라 해도 «조인이 잘 보인다»는 뜻이 아닙니다 — 이 수는 «파일이 세운 빈 층» 하나만 셉니다.

## 2. 접어도 되는지 가른다 (1번이 「진짜 중복」일 때만)

```bash
python -c "import sys;sys.path.insert(0,'.');sys.stdout.reconfigure(encoding='utf-8');from database.database import SessionLocal;from virtual_join import unique_key;db=SessionLocal();print(unique_key.describe_fold_plan(unique_key.fold_plan(db,'<표>',['<조인 키>'])));db.close()"
```

* 「접어도 되는 키 N」 → 진짜 사본. 접으면 됩니다
* 「접으면 데이터가 사라지는 키 N」 → **접지 마십시오.** 신원이 그 컬럼이 아니라는 뜻입니다

## 2-bis. 거절·경고 줄은 이제 «조치»를 실어 옵니다 (S-247)

이 자리에 «해독표»가 있었습니다 — 운영 로그를 밖으로 못 내오므로 줄의 뜻을 여기서 풀어야 했기 때문입니다. 지금은 줄 자체가 답합니다:

```
[<자리>:<규칙 또는 표>] 무엇이 일어났나 (표본: ≤3) → 다음: <무엇을 하나>
```

🔴 «다음:» 절을 그대로 따르십시오. 같은 「중복 키」라도 수리가 «정반대»인 자리가 있습니다 — 「행들을 하나로 합치십시오」(데이터)와 「선언에 컬럼을 더 적으십시오」(선언)는 서로의 질문에 대한 «틀린 답»입니다.

| 줄 앞머리 | 무엇을 말하나 |
|---|---|
| `[join_into:<규칙>]` | 오른쪽 표에 같은 키의 행이 둘 — 그 왼쪽 행만 건너뜀 |
| `[VirtualJoinUnique:<규칙>]` | 쓰기가 가상 조인의 오른쪽 유일성을 어기려 함 — 해당 행만 건너뜀 |
| `[BKConflict:<표>]` | 표의 «신원»이 두 행을 못 가름 — 그 배치 거절 |
| `[VirtualJoinIndex:<표>]` | 조인의 유일 인덱스 상태 — 없음·INVALID·빈 키·진짜 중복 |

🪦 `invalid input syntax for type double precision: ""` 는 `ddd5b3ba` 부터 안 납니다(S-245). 재기동 뒤에도 나면 «다른 자리»입니다 — 줄의 SQL 을 보고 올려 주십시오.

## 3. 급할 때 끄는 스위치 (환경변수, 재기동 필요)

```bash
ASSY_CHAIN_WORKER=0        # 체인 루프 자체를 안 띄움 (API·읽기는 그대로)
# 규칙 «하나»만 끄려면: 그 규칙이 적힌 파일(chain_rules · enrichment_rules · virtual_join_rules)에서 `enabled: false` — 부팅 줄 set(N) 이 이름을 전부 보여 줍니다
ASSY_VJOIN_AUTO_INDEX=0    # 제품이 유일 인덱스를 자동으로 세우지 않음 — 🔴 1497ea3e 부터 OFF 는 «DB 를 한 번도 안 만짐»(그 전엔 점검 SQL 이 여전히 돌아 안 멎었음)
```

## 4. 통합 뒤 모양 미리보기 (읽기 전용, 안 써도 됨)

```bash
python scripts/preview_unified_declarations.py --out ../unified_preview.json
```

왕복이 하나라도 깨지면 **아무것도 안 씁니다.**

---

## 부팅 로그에서 볼 것 (재기동 뒤)

```
[ChainRules] set(N): 이름[출처,방식] 표=값 ... (꺼진 건 OFF)   <- 무엇이 도는가. synt = 제품이 만든 규칙
     🆕 (09-17) «방식» 칸이 고쳐졌습니다 — 지금까지 조인·자동확정·중복제거가 전부 `mapper` 로 찍혔습니다.
        조인을 선언하시고 이 줄에서 `join` 을 찾으셨다면 0 건이었을 것입니다 — 이제 `join` 으로 뜨는 것이 맞습니다
[ChainRules] <규칙이름> refused: no execution path picks it up ...
     🆕 (09-17) «선언은 받는데 아무도 안 돌리는» 규칙을 이제 이름 대어 거절합니다 (판정 500).
        제일 흔한 모양: 파일 맵퍼에 `follow_up: true` — 본 단계는 «미룸»이라 안 집고,
        뒤따르는 단계는 «자기가 쓰는» 규칙만 돌릴 수 있어 양쪽 다 빠졌습니다
        → `follow_up` 을 빼시거나, 스스로 쓰는 종류를 적으십시오 (통합 선언의 `derive.kind`). 거절 줄이 둘 다 말합니다
        ⚠️ `enabled: false` 로 끈 규칙은 거절되지 «않습니다» — 꺼진 것이지 못 도는 것이 아닙니다
        🔴 그리고 뒤따르는 단계가 이제 `enabled: false` 를 «지킵니다» — 지금까지는 끈 규칙도 그 단계에서 돌았습니다
[ChainRules] refused(N): 이름(사유)                          <- 무엇이 «안» 도는가
[ChainRules] 지난 적재와 다름 - 사라짐 1: ...                  <- 선언을 바꾼 뒤 무엇이 달라졌나
[VirtualJoin:이름] 조인 키 (...) 가 ... 의 «신원»(...)보다 좁습니다   <- 1번과 같은 진단
[VirtualJoin] 인덱스 uq_vjoin_… (표) 를 «제품이» 걷어냈습니다 … 다음: 없음   <- 규칙 없는 인덱스가 그 표의 쓰기를 막던 것이 풀림 (S-248). 이 줄 뒤 dedup 영구 실패가 멎어야 함
Transaction … permanently failed: N event(s) -> FAILED. 원인: <예외 문장>   <- 이제 traceback 첫 줄이 아니라 «원인»이 실림
[ChainRule] rule=… kind=… target=… rows_in=N rows_out=M|None written=M|None refusal=… error=…|None elapsed=…
     🆕 (09-17) 「이 규칙이 돌았나」는 «이 한 줄»로 봅니다 — 종류를 «몰라도» 됩니다.
     그리고 이제 «모든» 규칙이 이 줄 하나를 씁니다 — `[mapper@<로그파일>] … START/END/RAISED`
     세 줄은 «없어졌습니다»(판정 498). 파일 맵퍼를 그 태그로 찾고 계셨다면 이 줄로 오십시오
     kind   그 규칙이 넣은 «맵퍼 이름» 그대로입니다
     🆕 (09-17 저녁) 이 칸의 «낱말이 바뀌었습니다». 로그에서 `builtin:…` 을 찾고 계셨다면:
            builtin:join_into    ->  declared:join            통합 선언의 derive:{kind:"join"}
            builtin:auto_confirm ->  declared:decide          derive:{kind:"decide"}
            builtin:join         ->  declared:virtual_join    virtual_join_rules.json 의 materialize:true
            `declared:` 는 「제품이 «선언에서» 지은 맵퍼」라는 뜻입니다. 옛 이름은 «안 풉니다» —
            저장된 규칙에 옛 값이 있으면 거절되고 그 이름이 거절문에 찍힙니다
     ⚠️ (09-17) 이 자리는 「builtin:… 이면 스스로 씀」이라 적혀 있었고 «이제 거짓»입니다 —
            종류표가 없어졌습니다. 「스스로 쓰나」는 이름이 아니라 아래 written 칸이 답합니다
     rows_out  그 규칙이 «낸 행 수». 종류가 달라도 «같은 함수»가 셉니다 — 화면의 수와 같은 수입니다
     written   스스로 쓴 행 수. 제안하는 규칙은 «항상 None»
     🔴 None 은 «0 이 아니라» 「안 셌다」입니다 (rows_out·written 둘 다)
     🆕 (09-17 저녁) «조인»이 뒤따르는 바퀴와 소급·백필에서 written=None 으로 뜨고 표에는
            아무것도 안 써졌다면 «그 증상은 고쳐졌습니다». 같은 줄의 rows_out 에는 수가 찍히는데
            written 만 None 이었습니다 — 제안만 하고 «쓰는 사람»이 없었습니다.
            한 페이지가 아웃박스 이벤트를 «행마다 하나» 만들던 것도 같이 닫혔습니다
     🆕 error=…  그 규칙이 «던졌다»는 뜻입니다. 던져도 이 줄은 «찍힙니다» — 줄이 없으면 「안 돌았다」가 맞습니다
     어느 로그 파일인지는 어드민 chain 큐 응답의 `log_filename` 이 말합니다
[ChainWaiting] <표>: N group(s) deferred behind <tx> (sweep #k) | rules: … | head: <사유>
     🆕 (09-17) 이 줄의 태그가 `[HOL Guard]` 였습니다 — 소유자 09-15 「hol 가드란 용어 쓰지마」.
     뜻은 그대로입니다: «앞 그룹이 막혀서 뒤 그룹이 기다린다». `rules:` 가 «끄면 되는 규칙 이름»이고,
     `head:` 가 «머리가 왜 막혔나»입니다. 이 줄이 쌏하면 첫 줄 «위»의 `Failed to execute mapper` 를 보십시오
[ChainRule] rule=… table=… ← woke_by=<표>#<tx> hop=h/max (xN)
     🆕 (09-17) 후속 랩도 «같은 태그»를 씁니다 — 이전에는 `[ChainBuiltin]` 이었고, 그 이름으로 찾으시던 분은 이제 이 줄로 오십시오
     ← woke_by  «무엇이» 이 랩을 깨웠나 (표#트랜잭션)  ·  hop  몇 번째 홉인가
     (xN)      같은 규칙·표의 같은 줄이 N 번 접혔다는 뜻 (첫 줄 + 500 마다)
     🔴 rows_in·written 은 이 줄에 «없습니다» — 같은 실행의 바로 위 `[ChainRule]` 줄이 이미 말합니다
     쓴 것이 0 이면 DEBUG(안 보임). 거절이면 ` REFUSED: <사유>` 가 같은 줄에 붙습니다
[Chain Depth] outbox#… reached hop N, over the declared limit of M; refusing it   <- 고리가 상한에서 끊김(정상). 더 길게 가야 하면 chain_rules.json 최상위 max_chain_depth
[ChainRules] 고리: A → B → A (순서는 선언 순 · 홉 상한 …)   <- 오류 아님. 한 번만 뜸
```

## 5. 통합 선언에 «join» 적기 (S-237 착지 — 가상 조인은 그대로, 이건 «체인»)

🔴 **지금은 «파일에 직접» 적으십시오.** 어드민 chain 탭의 저장은 이 모양을 옛 문법으로 재서 «거절»합니다(S-244, 구현자 맨 먼저). 적은 뒤 재기동하거나 어드민 「설정 반영」(SYSTEM_RELOAD)을 누르면 로더가 읽습니다.

`chain_rules.json` 의 `rules` 에 이 모양 하나를 적으면 체인 규칙 **둘**이 섭니다(왼쪽 트리거 + 오른쪽 트리거는 페이싱). 값은 대상 표의 «진짜 컬럼»에 «자기 층»으로 써지고, 원장이 그대로 봅니다.

```jsonc
{
  "name": "inventory_confirmed",
  "on":     { "table": "dt_inventory" },                       // columns 는 적지 마십시오 — 왼쪽 키에서 유도됩니다(판정 398)
  "derive": { "kind": "join",
              "join": { "right_table": "dt_job_attribution",
                        "on":   [ { "left": "dt_job", "right": "dt_job" } ],
                        "take": [ "dt_lot_confirmed", "dt_slot_confirmed" ] } },
  "into":   { "table": "dt_inventory" },
  "key":    { "unique": true }                                 // 재기동/반영 때 «제품이» 오른쪽 표에 유일 인덱스를 세웁니다(S-240)
}
```
* fold 는 적지 않습니다 — 두 표의 표기 선언에서 «계산»됩니다(판정 397). `max_rewrite_rows` 도 없습니다 — 페이싱(판정 396).
* `key.unique: true` 면 재기동/반영 때 «오른쪽 표»에 유일 인덱스가 섭니다. 중복이 있으면 «안 세우고» 값과 건수를 로그에 냅니다 — 그때는 2번(접기)으로 가르십시오. `ASSY_VJOIN_AUTO_INDEX=0` 이면 아무것도 안 만듭니다.
  `key.columns` 는 «맞는지 보는» 칸입니다 — 조인의 오른쪽 키와 다르면 두 목록을 이름 대고 «안 세웁니다»(다른 컬럼에 세우면 이 조인이 그 인덱스를 안 씁니다). 안 적어도 됩니다.
* 재기동 뒤 부팅 줄에서 확인: `[ChainRules] set(N): inventory_confirmed[decl,join] …` 가 «둘» 보여야 합니다.
* 오른쪽 값이 null 이면 «null 로» 써집니다. 오른쪽 행이 없으면 안 씁니다.
* 오른쪽 행이 «둘 이상» 이면 그 왼쪽 행은 안 씁니다(둘 다 답이 아님) — 나머지 행은 그대로 써집니다. 로그 한 줄:
  `[join_into:이름] N left row(s) matched MORE THAN ONE right row and are skipped by name ...`
  이 줄이 보이면 «오른쪽 표의 데이터»에 같은 키가 둘인 것입니다 — 2번(접기)으로 가십시오. 선언을 고칠 일이 아닙니다.
* 켜면 «지금부터 바뀌는 행»이 조인됩니다. **기존 행 소급(S-242 착지):** 어드민 소급 탭 → `chain_replay` → 규칙 이름은 **왼쪽 규칙**(오른쪽 `…:reference` 는 이름 대고 거절됨) → pace `slow` 또는 `trickle` → «세기»(「다시 계산할 행」 수가 뜸) → 실행. 왼쪽 표 전체 행이 대상입니다(박스 실측 488,429 행). 한 페이지가 던지면 그 페이지만 세고 계속 갑니다(`pages_failed`).

### 5-bis. 「읽기 시점 조인」은 «은퇴했습니다» — 적지 마십시오

⚰️ 이 자리에 «복붙 예제»가 있었습니다(`into: { "read": true }`). **오늘 그 선언은 이름 대고 거절됩니다.**
적는 법은 §5 의 `into: { "table": … }` «하나»뿐입니다. 남아 있는 선언을 어떻게 하는지는 **맨 위 ③** 에 있습니다.

## 6. 재기동 «전» PG 시험 (S-256)

```bash
python scripts/run_pg_tests.py
```

* SQLite 가 «받아 주는» 것을 PostgreSQL 이 «거절하는지»를 이 박스의 PG 서버(격리 DB, 운영 DB 는 이름으로 거절)에서 잽니다 — `passed` 면 재기동 · `skipped` 는 «통과가 아닙니다»(`-rs` 줄이 이유: 서버 도달 불가·확장 없음) · `failed` 면 재기동 «전»에 고침 · `REFUSED` 한 줄(종료 64)이면 서버 DB 가 PG 가 아니거나 격리 DB 선언이 없는 것 — `ASSY_PG_TEST_DATABASE_URL=postgresql://…/assy_qa` 로 선언합니다.
