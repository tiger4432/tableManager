{
  "name": "client2",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check:clipboard": "node scripts/check_clipboard_convention.mjs",
    "check:contracts": "node scripts/check_contracts.mjs",
    "check:suggest-keys": "node tests/value_suggest_keys_harness.mjs",
    "prebuild": "npm run check:clipboard && npm run check:contracts && npm run check:suggest-keys",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  },
  "dependencies": {
    "ag-grid-community": "^35.3.0"
  }
}
