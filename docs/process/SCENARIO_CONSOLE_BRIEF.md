# 시나리오 콘솔 지시서 — 사용자가 직접 조사·분석·액션하는 UI/API 초안

> ## 🔴 §0-bis. 오늘의 지시 (2026-08-14, 제품 소유자 — 메시지 채널 미배달분의 정본)
>
> `send_message` 4건이 긴 턴에 막혀 미배달이므로 여기가 정본이다. 접수 시 이 블록을
> 읽었다고 보드에 표시할 것.
>
> **기한: UI 완성까지 오늘내로.** 오늘의 진짜 목표: **진정한 온톨로지 기반 분석 가동.**
>
> 1. **eds_fail_map은 나중에** — census만 대기열에, 오늘 범위 제외.
> 2. **오늘의 산출물 = 불량 케이스-컨트롤 분석 콘솔** (보이드는 기본값 예시):
>    - **현황판**: finding_kind별 발생 수·율 (분모 = inspection_run, 항상 표시),
>      eqp/recipe/lot/기간 슬라이스.
>    - **양쪽 모집단 트레이스**: 난 것 + **안 난 것**(스캔됐는데 0건) 모두 같은 추적
>      기계로 — 자재·공정 조건(processed_with)·MI 팩트까지. 미스캔은 **제3 부류**로
>      따로 셈(조용히 clean 합류 금지).
>    - **차이·공통 분석**: 한 API의 두 프레이밍 — 공통점(교집합) + **차이점**(난 쪽
>      대 안 난 쪽의 요인 농축, 양쪽 분모 표시 — 사다리 2단 «기저율 대비 놀라움»).
>      G1 siblings에 `contrast` 모드로, 둘째 엔드포인트 금지.
>    - **메타 액션 부착**: 지식 획득 종류만 오늘 개시 — `collect_request`(설명 실패
>      → 표적 수집 + 수집 대기 모드) · `fill_missing`(결측치 워크리스트 — "채우면
>      뭐가 풀리는지" 정렬). 웹 최초 원장 쓰기: 같은 gate/store, 요청 id 멱등,
>      admin 토큰, released_by. 처분 종류(hold 등)는 오늘 제외.
> 3. **일반화 (§0의 [일반화] 블록이 정본)**: `finding=<kind>` 파라미터, void는
>    기본값이지 특례 아님. 분모는 kind 시그니처의 `observed_by: [methods]`로. 분모
>    없는 종류는 「분모 없음 — 대조 불가」를 내용으로 렌더. 정답지에 둘째 kind(다른
>    심은 요인) 추가 — kind 전환이 각자의 요인만 찾는 것을 단언.
> 4. **순서**: 가상 소스 확장판(MI·processed_with·Recipe·정답지+대조 정답지, 픽스처
>    2,500웨이퍼 위) → G1 siblings+contrast → 콘솔 → 메타 액션. 병렬 UI
>    살(신호등·admin 탭·그래프 토글·enrich 1클릭·G4)은 파일 안 겹치는 한 계속,
>    경합 시 임계 경로 우선.
> 5. **종료 조건**: 소유자가 화면에서 아무 불량 종류나 골라 분모 달린 현황을 보고,
>    난/안 난 대조로 차이·공통 요인을 읽고, collect_request를 발급해 그 Action
>    노드가 원장에 앉는다.

> **Status:** 🟡 지시서 v0 (제품 소유자 2026-08-13 밤: "시나리오들을 사용자가 직접
> 진행할 수 있게 … UI 및 API 시스템 초안, 인간이 쓰기 편하게") | **Owner:** 온톨로지 포크
> 시나리오 정의: [PHYSICS_ONTOLOGY_SETUP §5](../architecture/PHYSICS_ONTOLOGY_SETUP.md).
> 원칙: 질문 하나 = URL 하나(답은 링크) · 새 모드/모달 금지 · 근거 없는 숫자 화면 금지 ·
> 액션은 1클릭 + 확인 1회 + 근거 자동 첨부.

> ## 🔴 §0-ter. 소유자 재지시 (2026-08-14 ~11:05, 연속 발화 3건의 정본 — 태그: **오늘 반영**)
>
> **원문 요지**: 「지금 만든 불량 차이 추적은 내 의도랑 다르다. 내가 궁금한 건
> **이 랏에서 보이드가 왜 났냐? 그래서 뭘 해야 하냐?**인데, 지금은 보이드 9만 개
> 있다고 띄우고 전체 차이만 보여준다. 그 9만 개엔 이상한 데이터·특수 평가도 섞여
> 있는데 다 뭉뚱그리는 건 아니다. **핀포인트로 불량 랏 몇 개를 지정**해서, 이 랏은
> 어떤 특성이 있고 정상과 어떤 차이가 있는가로 가야 한다. 이러려면 **핵심 객체별
> trend/불량 요약 표**를 놓고, 거기서 **클릭하면 참조뷰 느낌으로 그 항목의 온톨로지
> summary**가 뜨고, **그 기반으로 액션 산출**이 되어야 한다.」
> + 「**그래프 뷰랑도 병행**되어야 할 듯」 + 「아직 목표 달성 한참 남음」
>
> **설계 번역 (포크)** — 전역 화면은 폐기가 아니라 **강등**이다. 엔진은 전부 산다:
>
> 1. **진입점 교체**: 전역 현황판 대신 **핵심 객체별 불량 요약 표** — 행 = 랏(코어
>    lot 기준, 필요시 wafer 드릴), 열 = finding_kind별 발생·율·분모, 기간 trend.
>    정렬 기본값 = 이상 농축 순(그 랏의 율 vs 전체 기저율).
> 2. **모집단 위생 — 뭉개지 말고 버킷**: 이상 데이터·특수 평가를 조용히 합산 금지.
>    분류: 양산 / 특수 평가 / 미상(구분자 없음). 필터로 숨기지 말고 버킷으로 보여
>    준다(자기를 반증할 것을 필터가 먼저 지운다). **실데이터에서 특수 평가를 무엇으로
>    구분하는지는 소유자 입력 대기 항목** — 마커가 확인되기 전엔 미상 버킷으로.
> 3. **행 클릭 → 참조뷰 온톨로지 summary**: 그 랏 하나의 — 관측 요약(kind별)·공정
>    조건(processed_with)·전이 혈통(transferred 걷기)·MI 요약·기존 액션. 새 모달
>    금지 원칙 유지 — URL 있는 뷰로(질문 하나 = URL 하나).
> 4. **케이스-컨트롤은 참조뷰 안으로 스코프**: 지금 만든 교집합/차이(contrast·Katz
>    하한·3분할 분모) 엔진에 **subject 스코프 파라미터**(이 랏[들] vs 정상 모집단)를
>    더해 재사용. 전역 모드는 그 특례일 뿐.
> 5. **그래프 뷰 병행**: 참조뷰의 모든 핵심 객체(랏·웨이퍼·장비·레시피)에서 같은
>    subject로 기존 그래프/trace 뷰로 건너가는 링크. 신규 그래프 구현 금지 — 있는
>    뷰에 잇는다.
> 6. **액션 산출 자리 이동**: collect_request/fill_missing 부착점은 전역 화면이
>    아니라 **참조뷰**다 — "이 랏의 설명 실패"가 근거로 자동 첨부되는 그 자리.
>
> **종료 조건 대체 (§0-bis 5 대신)**: 소유자가 요약 표에서 보이드 많은 랏을 골라
> 클릭 → 참조뷰에서 그 랏의 특성과 정상 대비 차이를 읽고 → 그 화면에서
> collect_request를 발급해 원장에 앉는다. **소유자 선언: 아직 한참 남음** — 완료
> 보고는 이 흐름 전체가 화면에서 돌 때만.
>
> **⚠️ 착수 순서 (소유자, ~11:20)**: 위 추적 UI는 **보류**. 소유자가 「구조가 너무
> 숨겨져 있어 UI를 어떻게 설계할지 모르겠다」— 먼저 §0-quater의 구조 뷰. 위 설계
> 논의 지점 5개(행 단위·특수평가 구분자·대조군 정의·그래프 병행 형태·범위)는 구조
> 뷰를 보면서 소유자가 결정한다.

> ## 🔴 §0-quater. 온톨로지 구조 뷰 — 지금 유일한 착수 대상 (소유자, 2026-08-14 ~11:20, 태그: **오늘 반영**)
>
> **원문 요지**: 「지금 너무 구조가 숨겨져 있어서 UI 어떻게 설계해야 할지 모르겠어.
> 일단 **온톨로지 구조가 드러나게** 하나 만들어줘. 그다음에 추적 UI 개발하자.」
>
> **만들 것 — 유형 수준 구조 뷰 (인스턴스 그래프 아님)**:
> 1. **유형 그래프**: 주어 유형 × 술어 × 목적어 종류를 노드-엣지로. 원장에서 한 방
>    집계(GROUP BY subject.type, predicate, object.kind)로 유도 — 엣지마다
>    실건수·기간(min/max occurred_at)·해소 등급 분포(핀/확정/관측/추론). 데이터가
>    흐르는 축과 선언만 있는 축이 색/굵기로 갈릴 것.
> 2. **어휘 패널**: 술어 10개 각각의 한 줄 뜻 (vocabulary.py의 정의에서 유도).
> 3. **kind 레지스트리 패널**: finding_kinds — kind별 observed_by(=분모 정의)·
>    관측 테이블·건수.
> 4. **선언 지도**: 어떤 config가 무엇을 선언하는가 — ledger_config(소스→원장 번역,
>    소스 테이블별 술어), siblings_axes(요인 축), finding_kinds.json 오버라이드
>    여부. 각 항목에서 해당 유형 그래프 엣지로 연결.
>
> **규율**:
> - **손그림 금지** — 화면 전체가 선언+원장 집계에서 **생성**된다. 구조가 바뀌면
>   그림이 따라 바뀐다. 하드코딩된 노드/엣지 목록이 보이면 실패.
> - 자리: 기존 ledger 페이지의 한 뷰(질문 하나=URL 하나, 새 모달 금지). 기존 그래프
>   렌더러 재사용 우선 — 새 그래프 라이브러리 금지.
> - 가독성=기능: 노드·엣지 라벨은 한국어, 작은 글씨 금지.
> - 빈 축 정직: 건수 0인 선언 엣지는 숨기지 말고 「선언됨·데이터 0」으로 렌더.
>
> **수락 기준**: 소유자가 이 화면만 보고 「어떤 객체가 어떤 관계로 이어져 있고,
> 어디에 데이터가 얼마나 있는지」를 남에게 설명할 수 있다. 그 위에서 추적 UI 설계
> 논의를 재개한다.

## 0. 무엇을 만드는가 — 한 문장

**조사자가 보이드 하나에서 출발해, 혈통·계측·공정 조건·공유 요인을 한 화면에서 읽고,
근거가 자동으로 붙는 액션을 그 자리에서 내리는 콘솔** — 그리고 그 전 과정이 URL이라
동료에게 링크로 넘어간다.

**[일반화 — 제품 소유자, 2026-08-14]** 보이드는 예시 하나일 뿐이다. 콘솔과 API는
**모든 불량 관측(`observed`의 finding_kind 전부)**에 같은 분석을 제공한다 —
`finding=<kind>` 파라미터, void는 기본값이지 특례가 아니다. 케이스-컨트롤(난 것/안 난
것 대조)이 본 분석이고, 분모는 kind의 시그니처에 선언된 `observed_by: [methods]`의
`inspection_run`이 정의한다(3분할: 발견/깨끗-스캔됨/미스캔). 분모 없는 종류(OM 임의
관측)는 대조 패널이 「분모 없음 — 대조 불가」를 내용으로 렌더한다. 코드에 기본값
아닌 `finding_kind='void'` 하드코딩이 보이면 일반화가 소실된 것. 정답지는 둘째
finding kind(다른 심은 요인)로 확장해 kind 전환이 각자의 요인만 찾는 것을 단언한다.

## 1. 목표 4개 (투입 순서대로, 각각 독립 착지 가능)

### G1. 질문 API 셋 — 읽기 전용, 정답지로 검증

| 라우트 | 답하는 질문 | 시나리오 |
|---|---|---|
| `GET /api/ledger/siblings?finding=void&window=` | 「이 결함들이 공유하는 요인은?」 — 교집합 + **기저율**(공유 5/6 vs 전체 노출 비율 — 놀라움 산출용 분모 필수) | S1 |
| `GET /api/ledger/compare?type=Recipe&a=&b=` | 「rev4 대 rev5, 뭐가 다르고 결과가 어땠나?」 — `has_param` diff + 결과율(**분모 = inspection_run**) + 교란 항목(장비·제품 구성) | S2 |
| `GET /api/ledger/worklist?model=` | 「모델이 지금 뭘 먼저 보라는가?」 — 메커니즘 그래프 순방향 보행, 행마다 근거(입력 원자 refs) | S3 |

규율: 셋 다 hop/fact와 같은 봉투 규율 — `state`·`basis`·발화자 배지 재료를 응답에
구조화 필드로 싣는다(산문 파싱 금지, R-C). **모든 비율은 분모와 함께**(핵심가치 V1
계기 규율의 일반화). 응답에 `generated_at`·투영 세대.

### G2. 조사 콘솔 — S1 화면 (`ledger.html`의 성장, 새 페이지 아님)

- **진입**: 랏/보이드/패키지 어느 것으로든 (커버리지 카탈로그가 이미 있는 그 입력).
  보이드 진입은 void_obs 행 → base 신원 → 콘솔.
- **2계층 뷰** (브리프 §10.1의 그것): 상단 패키지 수렴 토폴로지(10+칩), 하단 선택
  위치의 상세 — 혈통 사슬 + **자재 팩트 칩**(MI `measured` · 공정 `processed_with` ·
  디펙 `observed` — 통일 어휘라 렌더러 하나).
- **공유 요인 패널**: G1 siblings 호출 — 「이번 주 보이드 6건 중 5건이 B-3×R-12r4」
  한 줄이 이 화면의 가치 증명(브리프 §10.4의 그 문장).
- **액션 버튼**: 선택 대상 + 자동 수집된 근거(화면에 보이는 그 원자들의 refs + 투영
  세대 + 모델 버전)로 `POST /api/ledger/actions` — **확인 1회**, 성공 시 액션 노드가
  그래프 뷰에 즉시 나타난다.
- 그래프 뷰(뷰어 추출 산물)와 같은 화면 토글 — 같은 trace 응답, 렌더만 다름.

### G3. 액션 축 — 이 지시서의 유일한 «쓰기» 결정

`POST /api/ledger/actions` = **웹 프로세스 최초의 원장 쓰기**다. 지금까지 웹은
ledger를 읽기만 했다. 결정 사항이라 명시한다:

- 쓰는 원자: `(Action, register)` + `applies_to`×N + `based_on`(event_ref, 근거) —
  PHYSICS_ONTOLOGY_SETUP §5-bis 그대로. **같은 gate·같은 store를 통과**한다(웹 전용
  쓰기 경로 신설 금지 — 두 번째 철자가 된다).
- 게이트 규율 동일: 어휘 검사·subject_types·거절 계수. 멱등: 같은 요청 재전송이
  액션을 두 번 만들지 않게 요청 id를 dedupe 재료에 포함.
- 권한: `/admin/*`과 같은 토큰 게이트 아래. 액션 종류 v0는 **되돌릴 수 있는 것만**
  (`hold`·`priority_inspection`·`recipe_review`) — 브리프 §10.4의 그 판정 유지.
- `released_by`로 해제 — DELETE 없음, 원장 규율 그대로.

### G4. S2·S3 뷰 — 같은 콘솔의 질문 유형 둘

- **비교 뷰**: `?compare=Recipe:R-12@4,5` URL → param diff 표 + 결과율(분모 표시) +
  교란 점검 줄 + `recipe_review` 액션 버튼.
- **예방 워크리스트**: `?worklist=void_formation_v0` URL → 우선 검사 목록, 행마다
  근거 칩 + 그 행의 조사 콘솔 링크. SAT 결과가 들어오면 행이 적중/빗나감으로 닫히는
  것이 보인다(모델 채점의 가시화 — OODA).

## 2. 온톨로지 세팅의 «효율» — 세팅 현황판 (admin 원장 탭의 성장)

사람이 세팅 상태를 한눈에: 어휘(개체·술어 v0 목록과 사용 수 — 소비자 0인 어휘 표시),
소스별 커버리지(coverage 재사용), 모델 선언 목록(버전·검증 상태), **수집 간극**(named:
본딩 base 신원 · 실기하 선언 · MI 실물). R-G 유지 — 전부 읽기 전용, 편집은 파일로.

## 3. 수락 기준 (전 목표 공통)

1. **정답지 검증**: S1~S3 각각, 가상 소스에 심은 인과·요인을 **찾아내고**, 심지 않은
   미끼는 **찾아내지 않는다** — 둘 다 단언.
2. 브라우저 실검증(빌드된 dist, 소유자 스택), 복잡도 예산 측정·단언(L3 방식),
   액션 흐름은 effort 계기에 포함.
3. 모든 화면 숫자에 분모·근거·발화자 배지 — 이 셋 중 하나라도 없는 숫자는 출고 금지.

## 4. 순서와 의존

```
E0(basis·contested — 착수됨) → 가상 소스 확장판(공정·RCP·정답지 — 지시됨)
  → G1 (정답지 위에서 검증) → G2 ∥ 세팅 현황판 → G3(쓰기 결정 포함) → G4
```

R-H-bis 3건·상태 스트립·뷰어 추출은 기존 순번 그대로 — G2가 뷰어 추출 산물을,
G3가 R-H-bis의 스코프 구조를 쓴다. 레인 패킹은 구현 측 리드 재량, 파일 겹침 금지
규율 유지.
