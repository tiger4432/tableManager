# 🗂️ DOC_OWNERSHIP — 서브시스템 ↔ 문서 소유 매핑

> **Status:** 🟢 Living | **Last-verified:** 2026-08-11 | **Owner:** Lead / PM
>
> 🔴 **행을 더하거나 고치면 이 헤더의 「이번 라운드」도 함께 쓴다** — 직전에 F9 행이 추가될 때 그것을 어겼고, 그래서 새 행이 어느 라운드의 것인지 알 수 없었습니다.
> 🔴 **소유 행이 없는 문서·계약은 조용히 낡습니다.** 새 문서를 만들거나 새 계약(`contracts/<name>/`)을 추가하면 **그 자리에서 행을 만드십시오.**
>
> **이번 라운드 (2026-08-11 4차 · doc-keeper · SSOT 동점규칙 승인 반영 + `c4a3159` living-doc 결과)**
> - ✅ **총괄 보고 대상 ④가 닫혔다** — 2026-08-11 1차가 「`SYSTEM_OVERVIEW §「우선순위 결정」`이 동점 규칙 없이 서열만 적고 있다」를 SSOT라 손대지 못하고 보고로만 남겼는데, **제품 소유자가 정확한 세 층 문장을 승인**해 SSOT에 반영했다(`crud.compute_priority_value` 대조 완료 — 등재 우선순위 → `ingested_at` 내림차순 → `source_name` 오름차순, 2·3층은 계층을 못 넘는다). `347de78`의 결함(동점이 `sorted()` 안정성 때문에 dict 삽입 순서로 갈렸다, 200/200 실측)이 서열만 적고 동점을 안 적은 문장의 결과였다는 한 줄도 함께 실었다.
> - 🔴 **`c4a3159`(목록의 블랭크 결정키 500 수리)는 서버 변경만 있고 문서 변경이 0건이었다.** 새 사유 코드 `REASON_UNIT_KEY_INCOMPLETE`가 어떤 리빙 문서에도 없었다(grep 실측: `unscorable_reasons`·`map_keys_unavailable`·`reference_absent` `docs/` 0건). [spec/MAP_ALIGNMENT_SPEC §9.11](../spec/MAP_ALIGNMENT_SPEC.md) 신설로 목록이 낼 수 있는 사유 코드 다섯 + 상세와 공유하는 둘을 전수로 냈고, 감사 리포트(`agent_workspace/reports/Alignment_confirmation_flow_and_config_audit.md`)가 코드 읽기로만 짚은 **미측정** 위험 넷 — `apply_batch_updates`의 all-keys-dropped INSERT · `dt_standard_map_mapper.py:104-105`의 `replace_map: True` 조합 · `dt_inventory.dt_frame`의 타입 모호성(체인은 JSON을 쓰고 인리치먼트는 문자열을 쓸 수 있다) · 자동 경로가 `FrameConfirmation`을 안 남겨 이미 확정된 단위가 수동 목록에서 `pending`으로 뜨는 것 — 을 **발견으로만** 기록했다(수리 아님).
> - 🔴 **총괄의 2026-08-05 판정 하나가 실제보다 넓게 적혀 있었다.** 「목록의 `state`·`reason_code`는 안 바뀐다」는 주어가 `state`(닫힌 3종)였는데 `reason_code`까지 얼린 것으로 읽혔다. `c4a3159`가 그 문장을 다시 쓰지 않고 멈춰 물은 것이 옳았음을 확인하고 [MAP_ALIGNMENT_SPEC §9.1](../spec/MAP_ALIGNMENT_SPEC.md)의 문장을 좁혔다(2026-08-05 귀속은 유지, 2026-08-11 좁힘을 표시). **같은 패턴(넓게 적힌 절대 표현)을 이 파일 전체에서 찾아 스캔했으나 추가 인스턴스는 없었다.**
> - **PRIMITIVES 신규 1** — §7 ⭐「한 항목의 결함이 목록 전체를 죽이면 안 된다 — 배치 판정에선 예외를 사유 코드로 접어라」(`c4a3159` — 형제 셋에 있던 가드가 넷째(목록)에만 없었다. 가드가 하나로는 부족한 사례 — 뮤테이션 채점으로 두 겹 다 필요함을 확인).
> - `docs/README.md`의 맵 정렬 행에 §9.11 포인터 추가.
>
> **직전 라운드 (2026-08-11 3차 · 리빙 동기화 — `2ec8e24..3d43a6c` 6커밋 + 스펙이 가르친 결함 하나)**
> - 🔴 **[spec/MAP_ALIGNMENT_SPEC §5](../spec/MAP_ALIGNMENT_SPEC.md)가 한 룰의 선언을 스키마로 인용하고 있었다** — `decision_key: ['dt_eqp', 'product']`가 계약이 아니라 `eqp_frame_attribution` 룰 **하나**의 값이었는데, 그 인용을 글자 그대로 옮긴 클라 `keyFrom`이 arity 2에서만 선언을 따르고 다른 모든 arity에서 그 두 이름을 하드코딩했다(`3d43a6c`). 컬럼 하나만 선언하는 운영 룰의 프레임 확정이 **한 번도 성립한 적이 없었다.** §5를 다시 써서 "룰이 선언한 컬럼 전부가 채워져야 확정 가능"이라는 관계로 표현하고 예시 블록에 「한 룰의 선언」이라는 표시를 달았다. 사본: [architecture/frontend §4.2](../architecture/frontend.md) · [qa/FEATURE_CHECKLIST](../qa/FEATURE_CHECKLIST.md) 헤더(Map Editor 2는 여전히 §1/§2 행이 없어 표에는 안 넣었다).
> - **신규 행: 읽기 전용 진단 도구 셋** — `diagnose_socket.py`·`diagnose_db_health.py`(2026-08-04)·**`diagnose_slow_after_ingest.py`**(신규, `2b8a5ab`+`1a1947b`)가 **[spec/DEBUGGING_GUIDE §0](../spec/DEBUGGING_GUIDE.md)에는 있으면서 이 표에는 셋 다 행이 없었다.** 한 번에 등재.
> - **`5b09d69`(잡 컬럼 이름을 config에서 읽는다)의 리빙 문서 갱신을 검증했다 — 그 라인이 직접 고쳐 정확했다**([architecture/DT_CORE_FRAME_CHAINS](../architecture/DT_CORE_FRAME_CHAINS.md) · [guide/DT_CORE_FRAME_CHAINS_GUIDE](../guide/DT_CORE_FRAME_CHAINS_GUIDE.md) · [guide/chain_ingestion_guide](../guide/chain_ingestion_guide.md) · [architecture/PRIMITIVES](../architecture/PRIMITIVES.md)). **추가로 두 가지를 기록**(수정은 다른 레인 소관, 여기는 발견만): ① `replace_map`의 파생 스코프 경로가 `map_key_columns` 2개 이상인 테이블(`map_split_registry`·`map_doe`)에서 철자 오류를 삭제 확대로 바꾼다는 사실은 `chain_ingestion_guide.md`가 이미 상세표로 담고 있었다 — [architecture/PRIMITIVES](../architecture/PRIMITIVES.md)의 `replace_map` 항목에 짧은 포인터만 추가. ② `dt_job` 리네임이 `add_dt_log_trigger_indexes.sql`의 인덱스 정의 둘과 `enrichment_rules.json`의 자유 SQL 다섯 곳을 고아로 만든다는 사실에는 리빙 문서 사본이 **0건**이었다 — [guide/DT_CORE_FRAME_CHAINS_GUIDE §1-bis](../guide/DT_CORE_FRAME_CHAINS_GUIDE.md)와 [guide/DEPLOY_SETUP §6-3](../guide/DEPLOY_SETUP.md)에 신설.
> - **`fde424c`(감사 이력 엔벨로프) 서술이 `b1b7f85`의 un-hedge 이후에도 정확한지 검증** — `backend.md` §「감사 이력 조회」·`guide/config/audit_history_config.md`를 재대조, 어긋남 없음.
>
> **병행 라운드 (2026-08-11 · `347de78` — 해결 순서 전순서화 + R3 + 미선언 드롭 계수기)**
> ⚠️ **작업 착수 시점에는 미커밋 워킹트리였고 작업 도중 `347de78`로 착지했다.** 그래서 아래 항목 중 「아직 안 된 것」으로 적을 뻔한 것 하나가 **커밋 안에 이미 들어 있었다**(총괄 보고 ②) — **미커밋 변경을 문서화할 때는 종료 직전에 `git log`를 다시 본다.**
> - **신규 행 하나: 표시값 해결 순서(`compute_priority_value`)** — 종전에는 「데이터 모델/레이어링」 행에 묻혀 있었고, **이번에 그 함수가 결함의 진원지로 드러났으므로** 자기 행을 갖는다. 세 층(등재 우선순위 → `ingested_at` 내림차순 → `source_name` 오름차순)·정본 위치·「계층을 넘지 마라」 제약을 그 행에 모았다.
> - 🔴 **갱신 행 셋**: 「Chain Replay」가 **R3까지 셋**을 소유(가르는 축 = **저장된 것을 바꾸는가**) · 「소급 운영 경로」의 **기수를 지우고** ⓕ 추가(**ⓕ만 어드민 표면에 없다** — `retroactive.OPERATIONS` 실측 5종) · 「체인 인제션」이 **드롭 다이제스트 박동 note**를 소유(**두 번째 채널을 만들지 않았다**).
> - 🔴 **거짓이 된 문장 하나를 두 파일에서 고쳤다 — 「값 결정은 timestamp를 읽지 않는다」**(`87a944e` 라운드가 옳게 적은 것이 이번 변경으로 뒤집혔다). 사본 위치: [architecture/data_model §2.1-ter](../architecture/data_model.md) · [architecture/backend §3](../architecture/backend.md). ⚠️ **세 번째 사본이 [CODE_MAP](../architecture/CODE_MAP.md)에 있고 그것은 code-mapper 소관이라 손대지 않았다**(총괄 라우팅 대상).
> - 🔴 **PRIMITIVES 신규 3 · 갱신 1** — §1「후보를 고르는 정렬은 **전순서**여야 한다」·§1「판정 결과가 **박제**되면 규칙 수리는 앞으로의 쓰기만 고친다」·§1「침묵을 고칠 때 warn-once는 **고쳐졌는지**를 못 답한다」. **갱신된 항목의 마지막 줄이 *같은 날* 거짓이 됐다** — 2026-08-11 1차가 「드롭 자체를 세는 계수기는 아직 없다」라고 적었고 이 변경이 그것을 만들었다.
> - ⚠️ **총괄 보고 대상 넷**: ① **「R3는 아웃박스/WS 이벤트를 내지 않는다」를 문서에 싣지 않았다** — `setattr`이 동적 행을 dirty로 만들고 `database.auto_stage_database_outbox`가 **모든 세션에 걸린 전역 `before_flush`**라 커밋마다 `EDIT`이 실릴 것으로 읽힌다(R2도 같은 모양). **정적 판독이고 측정하지 않았다** — 확인 뒤 확정 서술로 교체 필요. ② ✅ **`server/main.py`의 `cell_sources` SELECT 셋은 「아직 안 됨」이 아니라 *이미 됨*이었다** — 지시서는 미적용(727·3702·4003)이라고 알려 줬는데 `347de78`이 셋 다 `ORDER BY source_name`을 붙여 착지했다(HEAD 실측 746·3726·4028). **같은 커밋이 `fetch_and_merge_metadata`에 `ingested_at` 조회 + `ingested_map`도 넣어** 그 호출자가 `ingested_at_by_source=`를 실제로 넘긴다 — 그 키워드가 존재하는 이유가 배선됐다는 뜻이다. ③ **저장 증가는 미해결**이다 — 이번 것은 정확성 절반뿐이고 `run_auto_update`는 여전히 실행마다 재사용 불가 소스명을 만든다. ④ **`SYSTEM_OVERVIEW §「우선순위 결정」`이 동점 규칙 없이 서열만 적고 있다**(SSOT라 손대지 않았다 — 초안은 보고서).
>
> **직전 라운드 (2026-08-11 2차 · 리빙 동기화 — `1e29078..2630790` 13커밋, 감사 이력 성능 + config 상속 + enrichment 재배치)**
> - **신규 행: 감사 이력(Audit History) — 최근 패널 + 행/셀 페이징** — `server/audit_cache.py`·`server/audit_history.py`가 **이 표에 행 없이** 출하돼 있었다(`dab9152`+`2630790`). 신설 `guide/config/audit_history_config.md`도 함께 등재.
> - **갱신 행 — config 해석 보고서(F9)**: 세 번째 도메인 `binding`(`68db020` — 키를 지우면 관례가 아니라 `table_config` 파생으로 상속. `⚠️ 총괄 보고 대상`이던 「`get_cell_history` 두 번 정의」가 `dab9152`에서 해소됨 — 아래 참조).
> - **갱신 행 — Enrichment Queue**: `enrichment.html` 페이지 자체가 삭제됐다(`1e29078`이 배지·nav를 걷고 `ab36fab`이 파일과 vite 진입점을 지웠다). 컨베이어(결손 입력 워크플로)는 **대체 없이 소멸** — 조회 절반만 메인 그리드 사이드바 참조뷰(`enrichment_reference_view.js`)로 옮겨갔다. 🔴 **`client2/src/enrichment.js`는 소스에 남아 있지만 어떤 HTML 진입점도 갖지 않는 죽은 모듈이다**(effort_meter.js가 `2026-08-11` 주석으로 이미 이 사실을 적어 두고 정리를 미룬다고 명시).
> - 🔴 **거짓이 된 문장을 다섯 파일에서 고쳤다 — 「`enrichment.html`은 살아 있는 페이지」**: `architecture/frontend.md`(HTML 표 + 모듈 표 + ui.js/§3.2/§5 서술) · `overview/SYSTEM_OVERVIEW.md` · `qa/FEATURE_CHECKLIST.md`(컨베이어·배지 행 취소선) · `spec/ENRICHMENT_QUEUE_SPEC.md` · `guide/ROLLBACK_PROCEDURE.md`.
> - 🔴 **`architecture/frontend.md:147`의 「`timeline.js` 722줄」이 실측 899줄로 낡아 있었다** — 지시서가 정정을 요구했고, `wc -l` 재실측으로 고쳤다.
> - **인덱스 3종을 배포 게이트로 명명했다** — `guide/DEPLOY_SETUP.md` §6 8-ter + `process/PRODUCTION_READINESS.md` C4(신규) — 마이그레이션 없이는 오늘의 성능 수리가 프로덕션에 적용되지 않는다.
> - ⚠️ **총괄 보고 대상 셋**: ① `server/main.py`의 `get_cell_history` 이중 등록(직전 라운드가 총괄 보고로 남겼던 것)이 **이번 배치(`dab9152`)에서 코드 쪽이 이미 해소했다** — 문서 표시만 걷었다. ② `server/config/audit_history_config.json.sample`이 `recent_*` 네 키를 아직 안 싣고 있다(코드는 이미 그 키들을 읽는다 — 새 가이드가 정본, `.sample`이 낡은 쪽). ③ `X-Audit-Truncated`/`X-Audit-Next-Cursor` 두 헤더가 CORS `expose_headers`에 없다(`server/main.py:164`) — 오늘은 클라가 안 읽어 무해하지만, `timeline.js`가 이 헤더를 읽게 되는 커밋에서 같이 고쳐야 한다(WWW-Authenticate가 같은 계급으로 한 번 걸렸던 함정).
> - 지시서의 「18커밋」은 실측 **13커밋**이었다(`1e29078^..2630790`, `git log --oneline` 전수) — 차이를 보고에 남긴다.
> - **후속 (같은 라운드, `fde424c` 착지 후)**: 작업 도중 관측했던 uncommitted 엔벨로프 전환이 커밋돼 정본이 됐다 — `backend.md`·`guide/config/audit_history_config.md`·`FEATURE_CHECKLIST.md`·이 표의 감사 이력 행에서 헤지를 걷고 확정 서술로 교체했다(`/audit_logs/recent`도 바디 엔벨로프, 리스트 키 `groups`, 헤더는 폐기 아님, `truncated:true`+`next_cursor:null` 제3상태는 이 라우트 고유). **신규 행 하나 더**: 제안·연구 카테고리(바로 아래) — `docs/README §3.6` 신설과 짝.
>
> **직전 라운드 (2026-08-11 1차 · 리빙 동기화 — 정렬 아크 `db1ee42..7097a67`)**
> - **신규 행 하나: DT/Core 프레임 파생 체인** — 문서 **셋**(`architecture/DT_CORE_FRAME_CHAINS` · `guide/DT_CORE_FRAME_CHAINS_GUIDE` · `spec/DT_ALIGNMENT_METADATA_CHAIN_SPEC`)과 코드 열 하나(매퍼 넷 · `dt_frame_transform.py` · `alignment_view_service.py`)가 **이 표와 [docs/README](../README.md) 양쪽에 행 없이** 출하돼 있었다. 🔴 **MAP_ALIGNMENT_SPEC·TRACE_FIXTURE_SPEC에 이어 *세 번째* 같은 실패다** — 문서를 만든 커밋이 등재까지 하지 않으면 그 문서는 **다음 감사까지 안 보인다.**
> - 🔴 **거짓이 된 문장을 여섯 파일에서 고쳤다 — 후보 공간이 「4회전 × 2면」이 아니다**(`db1ee42`). 지금은 **4회전 × 2시작모서리**이고 거울은 후보에서 나갔다. 사본 위치: `MAP_ALIGNMENT_SPEC §9.7`(§2.4는 이미 교체돼 있었다) · `data_model` · `frontend §4.2`+헤더 · `docs/README`의 맵 정렬 행 · `PRIMITIVES §4`(두 항목).
> - 🔴 **`alignment.sides` 선언이 은퇴했는데 config 가이드 둘이 세팅 절차를 가르치고 있었다** — `guide/config/map_overlay_config §5`와 `CONFIG_GUIDE §S9의 4행`. **행을 지우지 않고 은퇴 표시했다**: 그 키가 남긴 교훈(「탐색을 좁혀도 **보고는 좁히지 마라**」)은 키와 함께 죽지 않았고, `STATE_NOT_CONSIDERED`가 **생산자 없이** 코드에 그대로 남아 있다.
> - 🔴 **`PRIMITIVES`가 *삭제된 장치*를 재사용하라고 가르치고 있었다** — 「프레임 창으로 규격 갈아끼우기」의 「어디」가 `physFrameOverride`/`withPhysFrame`인데 `62520b9`가 지웠다(HEAD 실측 정의 0건). `frontend §4`는 2026-08-06부터 옳게 적고 있었으므로 **두 문서가 닷새 어긋났다.**
> - **`MAP_ALIGNMENT_SPEC §9.10 신설** — 앵커가 기준의 좌상단에 고정되지 않는다(`3dc79e6`+`014b5d3`+`8d37cd1`). 그리고 **§9.9의 「`ruling["anchor"]`는 앵커 갈래에서만 실린다」가 *같은 날* 뒤집혔다**(`1fbd4b1`) — 그 게이트가 서 있는 동안 확정된 원점이 상자를 모른 채 저장됐다(화면 3칸 왼쪽).
> - **PRIMITIVES 신규 2 · 갱신 3**: §4「후보마다 달라지지 않는 축은 축이 아니다」(신규) · §4「넘겨줄 값은 받는 쪽의 좌표계에서 푼다」(신규) · 「프레임은 인자다」(재작성) · 「시작 모서리는 축이다」 · 「그룹 최소화」.
> - ⚠️ **총괄 보고 대상**: 실 config `map_overlay_config.json:88`의 `__alignment_sides_comment` 주석 키가 낡았다(config는 문서 소관 밖) · `server/main.py`에 `get_cell_history`가 **두 번 정의**돼 같은 경로를 두 번 등록한다(2296·3765) · `docs/proposal/`의 신규 제안 둘은 **살아 있는 제안**이며 위 커밋들이 구현한 것이 아니다.
>
> **직전 라운드 (2026-08-06 · 실주행 사용성 이관 `372c538` + 일곱째 토큰 정정)**
> - **갱신 행 둘**: 「웨이퍼 맵 에디터」 행이 **[spec/MAP_EDITOR_SPEC §7](../spec/MAP_EDITOR_SPEC.md)(조작 가능성 요구사항)**과 그 근거인 **[process/EDITOR_USABILITY_FINDINGS](./EDITOR_USABILITY_FINDINGS.md)**를 함께 소유한다. 🔴 **실측 문서는 증거 기록이라 삭제 금지**이고 스펙은 수를 다시 적지 않는다 — **양방향 링크가 그 분업의 전부**다. ⏸️ **수리는 하나도 예정돼 있지 않다**(제품 소유자 판정 2026-08-06).
> - 🔴 **「좌표계 확정 기록」 행의 ⏳가 닫혔고, *왜 그 표시가 틀렸는지*를 함께 남겼다** — 표시는 *「토큰 목록 + 벡터 케이스」*만 적었지 **`geometryDeclaration`의 `phys_confirmed_from` 분기**를 안 적었는데 그쪽이 더 큰 절반이었다. **절반만 적은 대기 표시는 절반에서 닫힌다.**
> - 🔴 **같은 날 어휘의 *개수*를 적은 문장 셋이 한꺼번에 낡았다** — `MAP_EDITOR_SPEC §1-ter.1`의 「여섯」 · `PRIMITIVES §7`의 「여섯을 다 확인했다」 · 클라 하네스의 `TOKENS.length === 6`. **여섯을 일곱으로 고치지 않고 기수를 지웠다**(목록 옆의 수는 목록의 두 번째 사본이다). `MAP_EDITOR_SPEC §4-bis.3-ter`의 반증 표에는 `confirmed` 칸을 더했다 — **그 표는 토큰마다 한 칸이라 어휘와 함께 자란다.**
> - ⚠️ **총괄 보고 대상**: [docs/README](../README.md)에 `EDITOR_USABILITY_FINDINGS` 행이 아직 **없다**(이 라운드의 파일 범위 밖) · `MAP_EDITOR_SPEC §7.5`의 다섯 어긋남과 그 뒤의 「바인딩의 외연이 §6.0-ter와 §5.6-bis에서 갈릴 수 있다」는 **판정 대기**다.
>
> **직전 라운드 (2026-08-05 · 2차 — 설계 협의 기록 `d4e0fed`+`04ed01b`+`b9a0ab1`+`b445c2e`)**
> - **갱신 행 하나**: 「캔버스 축척 · 합성 규격 표지」 행이 **프레임 선택 표지(`frame_chosen_from`)**까지 소유한다. 🔴 **토큰이 아니라 키다**(provenance 토큰들은 「축의 값이 어떤 증거인가」에 답하고 이것은 「행에 무슨 일이 있었나」다) — 선례는 `phys_assumed_from`. ⚠️ **서버 쌍둥이 없음**: 이 키를 읽는 서버 코드가 **0건**이라 오늘 갈릴 것이 없다.
> - **[spec/MAP_EDITOR_SPEC](../spec/MAP_EDITOR_SPEC.md)에 §5.9(정렬 사슬)·§4-bis.3-ter(프레임 선택 표지) 신설.** 🔴 **§5.9는 실측과 어휘를 다시 적지 않는다** — 정본은 [spec/MAP_ALIGNMENT_SPEC §9.1·§9.4·§9.5·§9.6](../spec/MAP_ALIGNMENT_SPEC.md)이고, 이 라운드가 고른 것은 **사슬을 한 번에 읽히게 놓는 것**뿐이다.
> - 🔴 **뒤집힌 문장 하나를 두 곳에서 「고쳐 쓰지 않고 표시」했다** — 「빌리는 것은 `PHYS_KEYS` 여섯뿐」(`MAP_EDITOR_SPEC §1-ter.1` · `PRIMITIVES §4`). §9.5가 격자 치수와 `grid_start_*`를 빌림 쪽으로 넘겼다. **분류는 옳았고 빈도가 틀렸다**는 것이 남겨야 할 내용이라 옛 문장을 지우지 않았다.
> - **PRIMITIVES 신규 1 · 갱신 2**: §7「행에 대한 사실은 어휘를 늘리지 않는다」(신규) · §4「빌리되 쓰지 않는다」(빌림 축 반전 + 함정 셋) · §7「선언되지 않은 것에 표현을 지어내지 마라」(**판정을 초대하는 도형은 아예 그리지 마라** 예외).
> - ⚠️ **총괄 보고 대상 넷**은 이 라운드 보고서에 있다(코드가 지시서와 어긋난 자리 · `insideMask` 오기 · `assume_phys_from` docstring의 낡은 근거 · `map_editor/architecture_and_management` 갱신 필요).
>
> **직전 라운드 (2026-08-05 · 리빙 문서 동기화 — `dacf017..6f0a328` **35커밋**, 정렬/Map Editor 2 아크)**
> - 🔴 **[spec/MAP_ALIGNMENT_SPEC](../spec/MAP_ALIGNMENT_SPEC.md)이 고아였다** — 527줄짜리, 스스로 *「맵 에디터 재개발에 착수하기 전에 이 문서를 먼저 읽는다」*고 적는 A급 문서인데 **이 표에도 [docs/README](../README.md)에도 행이 없었다.** TRACE_FIXTURE_SPEC과 정확히 같은 실패이고, 직전 라운드가 그것을 고치면서 **바로 옆에서 새 고아가 태어났다.** 이번에 양쪽에 등재.
> - **신규 행 둘**: **맵 정렬 화면(Map Editor 2) 클라**(`client2/src/map2/*` 17모듈 + 페이지 하나가 **어느 문서에도 없이** 출하돼 있었다) · **스키마 드리프트 부팅 점검**(`server/schema_drift.py`).
> - 🔴 **거짓이 된 문장 셋을 사본까지 고쳤다.** ① **좌표계 선택 모달의 트리거**(`98b48e9` — *「규격 행이 없는 맵」* → **「없거나, 있어도 START X,Y를 읽을 수 없는 맵」**): `MAP_EDITOR_SPEC §4-bis.2/§4-bis.3/§5.8-bis` · `PRIMITIVES §1` · `FEATURE_CHECKLIST` 3자리 · `map_editor/architecture_and_management §3.1` · `config/table_config`. ② **정렬 캔버스의 웨이퍼 테두리**(`d4e0fed` — 리빙 문서에 사본 **0건**이었고 대신 `frontend §4.2`를 신설해 재유도를 막았다). ③ **provenance 토큰 목록**: `map_editor/architecture_and_management §2.3-ter`가 **넷**을 인쇄하고 있었다 — 지시서가 지목하지 않은 사본이다.
> - 🔴 **그 확대가 클라·서버의 술어를 갈랐다(총괄 판정 대기)** — 클라는 읽을 수 없는 규격 행을 통째로 버려 「선언 없음」으로 흘리는데 서버 프리셋 라우팅은 **행의 존재**로 `meta_present`를 답한다. 에러가 아니라 **침묵**이고, 하필 기본값이 가장 필요한 맵에서 그렇다 → `MAP_EDITOR_SPEC §5.8-bis` · `config/map_overlay_config`.
> - 🔴 **`map_editor/`의 §3.1을 사본에서 포인터로 축약했다** — 그 사본이 **같은 자리에서 두 번** 거짓이 됐다(`019140c`·`98b48e9`). **사본을 계속 고치는 대신 사본을 없앴다.**
> - **PRIMITIVES 신규 4**: 마스크 중립 합성 프레임(§4 — **카탈로그의 결함을 고치려고 만든 항목**이다. 구현은 이미 있었는데 §7 아래에만 색인돼 있어 한 레인이 하마터면 **세 번째 합성기**를 쓸 뻔했다) · 좌석은 등록만 한다(§4) · 빌리되 쓰지 않는다(§4) · 닫힌 어휘를 늘릴 때(§7) · 배포 사실은 부팅이 스스로 묻는다(§6).
> - ⚠️ **지시서와 실측이 어긋난 것 넷**(총괄 보고): ① 「116커밋」 → 직전 리빙 동기화 `dacf017` 기준 **35커밋**(113은 그 앞 `ce5ccb8` 기준) ② 「`backend.md` §1.3-ter가 그 레인이 쓴 것」 → 스키마 점검은 **§1.3의 항목 ①-ter**이고 §1.3-ter는 **듀얼스택 바인드**다(둘 다 실재하는 별개 절) ③ 「`MAP_EDITOR_SPEC.md:497`」 → 그 줄이 맞지만 **같은 파일에 사본이 셋 더** 있었다 ④ 「웨이퍼 테두리를 그린다고 적은 곳」 → **리빙 문서에 0건**(보드 M6만이 알고 있었다).
>
> **직전 라운드 (2026-08-04 · 리빙 문서 동기화 — `ce5ccb8..f7ac005` **72커밋**)**
> - 🔴 **하루 만에 거짓이 된 UI 계약 하나를 6파일에서 고쳤다 — 「🎯 APPLY / 💾 SAVE 두 버튼」.** 직전 라운드가 그 둘을 계약으로 적었는데 `5b15c24`가 **같은 날 삭제**했다. 지금은 **고르는 것이 곧 적용**이고(드롭다운 `change` 즉시 / 폴백 입력칸은 **`Enter`만**), 기록은 `⚡ Push` 또는 신설 **`📐 규격만 저장`**(`saveMapSpecOnly` — 셀 0건, 신원은 **화면 컨트롤**, **없는 등록을 만든다**). 사본 위치: `MAP_EDITOR_SPEC §5.7-b`(3개 하위절로 재작성) · `VALID_DIE_MAP_GUIDE §4`/§5/§7 · `frontend §4` · `FEATURE_CHECKLIST §1.7·§2.9` · `docs/README` · 이 파일의 M4 행. ⚠️ **`CODE_MAP`의 사본은 code-mapper 소관이라 손대지 않았다**(QA 실측으로 유효 다이 앵커가 **200~430줄** 낡았다 — 별도 라운드 필요).
> - 🔴 **`notation_rules.json`의 모델이 통째로 뒤집혔고(`8d306a5`) 운영자 문서 4종을 다시 썼다** — 직전 라운드가 「층 셋」으로 등재한 파생 컬럼(`<컬럼>_norm`)이 **아무도 소비하기 전에** 철회됐다. 지금은 **한 줄 선언 + 조회 시점 SQL 폴드**이고 **저장되는 것이 없다**. 함께 소멸: 재파생 스크립트 · 쓰기 거부 400 · 거절 `would_rewrite_raw`/`key_column`(**완화가 아니라 주어의 소멸**). 🔴 **접히는 조인 키에서는 유일성 게이트의 인덱스 배제가 뒤집힌다**(함수 인덱스만 후보 — 이유는 성능이 아니라 **정확성**) → `virtual_join_rules §2-bis` 신설.
> - 🔴 **거짓 성능 주장 하나를 발견했으나 **코드에만** 있다 — 「오른쪽은 UNIQUE 인덱스를 그대로 탄다」.** 실측 플랜은 `Hash Left Join` + 오른쪽 `Seq Scan`이고 인덱스의 성능 역할은 한 번도 행사된 적이 없다. **리빙 문서에는 사본이 0건**이었고(전수 확인), 대신 `virtual_join_rules`에 **실측 플랜 절**을 신설해 재유도를 막았다. 코드 사본 둘은 총괄에 라우팅(보고서).
> - **신규 행 셋**: **캔버스 축척 · 합성 규격 표지(`auto_registered`)** · **런처 인자 표면(`launcher_args.py`)** · 그리고 **실시간 동기화(WS) 행에 감시견·배지 3상태**를 등재.
> - 🔴 **`FEATURE_CHECKLIST §2.15`가 점검자에게 「`--help`가 없고 오타는 조용히 무시된다」고 지시하고 있었다** — 이미 고쳐진 결함을 통과시키라는 지시다. 삭제하고 인자 관문 점검 5건으로 대체했다.
> - **PRIMITIVES 신규 5**: 조회 시점 폴드(§3) · 축척의 정박점(§4) · 합성값 표지(§7) · 「응답 없음」이라는 세 번째 상태(§7) · 모르는 인자는 거절(§6).
> - ⚠️ **지시서와 실측이 어긋난 것 셋**(총괄 보고): ① 「약 100커밋」 → 직전 리빙 동기화 `ce5ccb8` 기준 **72커밋**(109는 그 앞 `cc36ef4` 기준) ② 「가상 조인 성능 주장을 리빙 문서에서 고치라」 → **리빙 문서에 사본 0건** ③ 「같은 웨이퍼는 어느 맵에서나 같은 크기」 → **격자가 웨이퍼보다 크면 격자가 축척을 가져간다**(`min()`이 데이터 보호를 위해 그렇게 만든다).
>
> **직전 라운드 (2026-08-04 · `cc36ef4..41b17ee` 33커밋)**
> - 🔴 **거짓이 된 문장 하나를 4파일에서 고쳤다 — 「`/health`가 200이 아닌 상태로 답하면 앞단이 답한 것이다」.** `/health`는 체크 하나만 실패해도 **설계상 503**(`health.HTTP_UNHEALTHY`)이라 그 규칙은 **아픈 스택을 매번 프록시로 고발**했고, 2026-07-31 중복 런처 인시던트에서 워커 둘이 프록시 장문을 찍어 진단을 오도했다. 지금 판별자는 상태코드가 아니라 **응답 BODY**(`internal_event_client.own_health_payload` — `status` + dict `checks`). 사본 위치: `PRIMITIVES §6` · `DEPLOY_SETUP` 헤더/§1-5-A/§1-5-C-4 · `FEATURE_CHECKLIST §2.16-D` · 이 파일의 loopback IPC 행 · `INCIDENTS_2026-07`(후속 정정으로 처리). ⚠️ **`CODE_MAP §5`의 사본은 code-mapper 소관이라 손대지 않았다.**
> - **신규 행 둘**: **컬럼 필터 번역기**(`server/column_filter.py` — 「`main`은 워커 안에서 안정된 이름이 아니다」의 소유자) · 그리고 **`crud.py`의 타입 깔때기**를 데이터 모델 행에 등재했다.
> - **갱신 행 여섯**: 프로세스 감시·헬스(**포트 충돌 = 세 번째 실패 계급** `06b7761`) · 이벤트 기반(**브로드캐스트 복구** `2aab7e2`) · 제품 소유 테이블(**`valid_die_ref` 승격** `6420ad0`) · 범용 맵 오버레이(**점 색 = 선언된 것만** `376e1c8`) · Virtual Join(**N2 예고가 청구서로 돌아왔고 N8이 갚았다**) · 데이터 모델.
> - ✅ **Virtual Join 행의 「`datetime`에 같은 크래시가 살아 있다」가 해소됐다** — 그리고 그 해소가 **타입을 하나 더 더한 것이 아니라 판정을 뒤집은 것**이라는 사실을 행에 남겼다(「이미 텍스트인가」 ↔ 「우리가 아는 문제 타입인가」). 🔴 **한 타입만 고치면 나머지는 그대로다**는 PRIMITIVES에 예고로 적혀 있었고 **그대로 청구됐다** — 예고를 적었으면 그 자리에 픽스처까지 만드는 것이 한 작업이다.
> - ⚠️ **지시서와 실측이 어긋난 것 둘**(총괄 보고): ① 「67커밋」 → 직전 리빙 동기화 `cc36ef4` 기준 **33커밋**(71은 그 앞 `6f328c0` 기준) ② 「stale-edge sweep refresh set」 → `graph_stale_edges.py`에 `refresh`/`broadcast`/`announce`가 **0건**이고, 그 이름은 `chain_ingestion_worker.refresh_targets`(브로드캐스트 복구)의 것이다.
> - 🗄️ **[이번 라운드에 대체됨 — 아래 내용은 철회된 파생 컬럼 모델의 기록이다]** **신규 행: 표기 정규화(`notation_rules.json`)** — `92b8d6f`가 착지했고 **어느 행도 `server/notation_norm.py`를 지목하지 않았다**(config 행이 파일명을 덮지 못한다 — 판정의 소유자는 그 모듈이고 함정은 **`table_config.json`의 `display_columns`까지 걸친다**). 🔴 **행의 핵심 셋**: 원본은 **절대 안 고친다**(거절 셋이 강제), 켜는 것은 **층이 셋**(물리 컬럼 · 파생 쌍 · 가시성), 「선언이 유효」와 「값이 나온다」는 **다른 진술**이다. ⚠️ **1단계라 아무도 파생값을 읽지 않고**, 첫 소비자가 생기면 그 행의 마지막 문장부터 거짓이 된다. 신설 문서 [guide/config/notation_rules_config](../guide/config/notation_rules_config.md).
> - **신규 행: 버전 게이트(`version_column`)** — `092b83f`가 착지했는데 **어느 행도 이 키를 지목하지 않았다**(`table_config.json`의 키는 config 행이 덮지만, 판정의 소유자는 `crud.py`이고 그 함정은 **`chain_rules.json`까지 걸친다**). 🔴 **행의 핵심 둘**: 게이트는 **거부권이지 승급권이 아니고**(사람의 교정은 더 높은 버전에도 밀리지 않는다), **파생 쓰기는 버전 컬럼을 안 들고 온다**(체인·재적용·자동확정). ⚠️ **선언한 테이블이 아직 없어 전부 무동작**이고, 첫 선언이 생기면 그 행의 그 문장부터 거짓이 된다.
> - 🔴 **「`mm`은 일부러 비어 있습니다」를 M4 행에서 삭제했다 — 클라에 mm 공간이 생겼다**(`cd3e0f4`, 2026-07-31). 그 문장은 **최소 7파일에 복사**돼 나흘간 거짓이었다. 소유 행 밖의 사본은 각 소유자에게 넘긴다(보고서). ⚠️ **저장 좌표가 칸수라는 것은 안 바뀌었다** — 둘은 다른 얘기이고, 섞으면 없는 결함이 만들어진다.
> - **맵 클라 코드 열이 세 파일이 됐다** — `client2/src/map_key.js`(R1 `689ebb9` · 맵 키 정준형)와 `client2/src/split_registry_row.js`(R2 `636f867` · `map_split_registry` 행 정규형). 🔴 **분할은 진행 중이므로 「맵 에디터 = `map_editor.js`」로 적지 마라** — 다음 라운드에 또 하나가 빠져나간다. 심볼의 거처는 CODE_MAP grep으로 확인한다.
> - ⚠️ **[spec/TRACE_FIXTURE_SPEC](../spec/TRACE_FIXTURE_SPEC.md)의 고아 상태 해소** — 2026-08-02에 총괄 판단 대기로 남겼던 634줄짜리 스펙에 소유 행을 만들었다(그래프 뷰어·추적 리포트 행의 짝).
> - ⚠️ **[guide/config_reference/](../guide/config_reference/README.md)의 소유자·갱신 주기를 기록했다** — 실 config의 **사본**이라 원본이 바뀌면 조용히 낡는데 그것을 아무도 안 보고 있었다.
> - **본딩 가용 완화**(`2c2a777`+`101311f`)와 **N7 숫자 가상 조인**(`5be96f5`)의 소유 행을 갱신했다. 🔴 **부재는 고장이 아니다**가 완화의 전부이고, 그 판정은 **키 존재**(`role_is_declared`)로 한다 — 진리값(`cfg.get(k)`)으로 재구현하면 「사이트가 안 쓴다」와 「사이트가 잘못 적었다」가 다시 하나로 접힌다.
>
> **직전 라운드 (2026-08-04 · 앞선 배치)**
> - 표기 정규화 · 버전 게이트 · 본딩 완화 · N7 숫자 가상 조인 · 맵 클라 분할 R1/R2(위 항목들).
>
> **그 앞 라운드 (2026-08-02)**
> - **config 행 확장 + [guide/CONFIG_ROLLOUT_GUIDE](../guide/CONFIG_ROLLOUT_GUIDE.md) 신설**(사용자 요청 「지금 모든 config들 가이드 느낌으로 올려줘 운영에서 보고 따라하게」). 목록은 이미 CONFIG_GUIDE에 있었고 **없던 것은 순서와 조용한 실패의 모양**이다 — 세 문서의 경계를 행에 못박았다. 코드 열에 **로더 6개의 진입점**과 반영 경로 둘을 등재했다.
> - 🔴 **보고서 서술 하나가 현 트리에서 거짓이다** — 「`config_watcher`는 원자적 쓰기(temp+rename)를 감지하지 못한다」는 **`46a67c7`(2026-07-29)에 해소**됐다(`on_moved`·`on_created` 처리 + 트레일링 엣지 디바운스). [CONFIG_GUIDE §4.4](../guide/CONFIG_GUIDE.md)가 이미 정확하게 적고 있었고, 런북은 그 문장을 옮기지 않고 **해소됐다고 적었다.** 함께: watcher가 감시하는 파일은 **`table_config.json` 하나뿐**이라는 사실이 순서 규율의 절반이다.
> - ⚠️ **[spec/TRACE_FIXTURE_SPEC](../spec/TRACE_FIXTURE_SPEC.md)에 소유 행이 없다**(README에도 없다) — 634줄짜리 A급 스펙이 고아다. 런북이 §6에서 링크했으나 **행은 총괄 판단이 필요하다**(어느 서브시스템의 것인가).
>
> **그 앞 라운드 (2026-07-31 · 2차 — `9200f20`·`4b50135`·`fbc1053`·`1948338`·`9c6a1c9`)**
> - **소급 행에 어드민 표면 + 신규 모듈 둘 등재** — `server/retroactive.py`(등록부·순수 디스패치)와 `server/enrichment_backfill.py`(ⓒ의 알맹이가 `scripts/`에서 나온 것). 🔴 **`server/scripts/`는 한 방향 문**이라는 규율을 행에 못박았다 — 런타임이 그쪽을 import하면 **운영에서만** 죽고, 트리거는 **200 `queued`를 돌려준 뒤 워커 스레드에서** 죽는다(초록 버튼·쓰인 행 0). 판정기는 별도 프로세스(`server/tests/prod_import_check.py`).
> - **Virtual Join 행에 알림 절반 등재** — `/schema`의 `virtual_columns`(`main.get_table_schema` + `virtual_join_executor.announced_columns`)와 클라 렌더(`grid.buildColumnDefs` append · `state.isVirtualColumn`). 🔴 **§9의 첫 미해결이 해소되고 새 미해결 둘**(CSV 추출 누락 · `미상` 행 검색 불가)이 그 자리에 들어왔다 — 목록의 단독 소유자는 여전히 `virtual_join_rules.md §9`다.
> - **`cell_sources` 인덱스 `idx_sources_by_source` 등재**(회수 범위) — **`models.py`와 `setup_db_performance.py` 두 곳에 선언**돼 있고 **둘 다 고쳐야 한다**(`create_all`은 기존 테이블에 인덱스를 안 만든다).
>
> **이전 라운드 기록은 [`docs/history/`](../history/)에 있습니다** — 🔴 이 헤더에 쌓지 마십시오(2026-07-30에 3,017자까지 자랐습니다). 각 행이 자기 규율을 담고, 헤더는 **날짜 · 이번 라운드에 바뀐 것**까지입니다.
> 상위: [SYSTEM_OVERVIEW](../overview/SYSTEM_OVERVIEW.md) · 규율: [CONTRIBUTING](./CONTRIBUTING.md)

각 서브시스템을 **어느 코드가 구현하고, 어느 문서가 설명하는지** 매핑합니다. 코드를 바꾸면 "문서" 열의 리빙 문서를 함께 갱신합니다([docs-as-code](./CONTRIBUTING.md#2-docs-as-code-갱신-규율-핵심)).

| 서브시스템 | 코드(진실 원천) | 리빙 문서 | Owner 역할 |
|---|---|---|---|
| 시스템 전체 | `run_decoupled_app.py`, `server/main.py` | [overview/SYSTEM_OVERVIEW](../overview/SYSTEM_OVERVIEW.md) | Lead |
| **근본 결함 전담 트랙** | (증상이 아니라 설계 결정 — 뿌리 넷: 「거절 못 함」·「대조 안 함」·「박제」·「표식이 열쇠로」. 🔴 **번호 금지** — `R1`~`R3`는 `chain_replay`의 수리 연산 이름이다) | [architecture/ROOT_DEFECTS](../architecture/ROOT_DEFECTS.md) — **총괄(lead-pm) 전담. doc-keeper 위임 금지** (보드와 같은 이유: 여기 적히는 것은 서술이 아니라 **판정**이다). 증상 표의 상태 넷(`측정·수리`/`측정·열림`/`주장·미측정`/`기각`)은 **측정이 바꾸지 산문이 바꾸지 않는다**. 🔴 **뿌리 진술을 판정한 것보다 넓게 쓰지 마라** — 2026-08-11에 실제로 그 사고가 있었다 | 총괄 전담 |
| **재사용 가능한 연산·패턴 카탈로그** | (전 모듈에서 추출한 개념) | [architecture/PRIMITIVES](../architecture/PRIMITIVES.md) — **유지 doc-keeper 전담**. 정비 사이클마다 신규 프리미티브 추가·소멸분 삭제 | 전 에이전트 공용 |
| **제안·연구 (미확정 설계)** (2026-08-11 신설 행) | (해당 없음 — 결정되지 않은 설계는 코드가 아니다) | `docs/proposal/` 전 문서, 인덱스는 [docs/README §3.6](../README.md) — **7개 문서가 이 행 없이 굴러가고 있었다**(등재 누락의 네 번째 사례). 🔴 **이 폴더 자체는 SSOT가 될 수 없다** — 채택된 설계는 내용을 해당 서브시스템의 리빙 문서로 옮기고, 이 폴더에는 "왜 이렇게 설계했나"의 기록만 남긴다. 새 제안 문서를 추가하면 **그 자리에서 §3.6 표에 행을 만들 것** | 총괄 |
| **알려진 중복 원장 (같은 연산의 N개 구현)** | (전 모듈 — 실측된 사본 목록) | [architecture/DUPLICATION_LEDGER](../architecture/DUPLICATION_LEDGER.md)(2026-07-31 신설 · 사용자 요청) — **유지 doc-keeper 전담**, PRIMITIVES의 **짝 문서**. 저쪽은 「할 줄 아는 것」이고 만들기 **전에** 읽고, 이쪽은 「두 번 이상 만들어져 있는 것」이고 그중 하나를 **건드리기 직전에** 읽는다. 🔴 **등재는 grep 실측만** — 「있을 것 같다」는 사유가 아니고 파일·라인이 붙어야 한다. 🔴 **상세는 한쪽에만 둔다**: 이미 PRIMITIVES에 서술이 있는 항목(D-2 크로스 테이블 조회 · D-3 `_` 관례)은 **원장이 요약만 싣고 링크한다** — 중복을 기록하는 문서가 자기 서술을 중복시키면 그 자체가 반례다. 🔴 **「왜 아직 안 합쳤나」가 이 문서를 정당화하는 필드다** — 이유가 없으면 **「이유 없음 — 그냥 안 했다」**라고 적고 지어내지 마라(합치기 가장 싼 항목이 그것들이다). ⚠️ **보드(PROJECT_STATUS)와의 역할 분담**: 보드 행은 **지금 작업 중인 항목**이고 이 원장은 **상시 기록**이다 — 대기열 20번(정렬 변환 중복)이 `4ba13ae`로 이미 해소됐는데 보드에 살아 있던 것이 이 문서가 필요한 이유의 실례다. **보드 행은 총괄 전담이므로 원장에서 고치지 말고 보고한다** | 전 에이전트 공용 |
| **프로세스 감시·헬스** | `server/process_supervisor.py`(2026-08-04 `06b7761` — **포트 충돌 축 신설**: `port_is_taken`/`port_owner`/`describe_port_conflict`/`preflight_port_check`/`port_conflict` · 평결 `VERDICT_PORT_CONFLICT`·`VERDICT_BROKEN_CHILD` · 자식 로그 `ChildSpec(log_file=...)`+`_attach_log_pump`+`CHILD_LOG_MAX_BYTES`), `server/health.py`, `server/utils/heartbeat.py`, `run_decoupled_app.py`(`refuse_if_ports_are_taken` · `--preflight-only` · `GRAPH_BIND_HOST`), 회귀 그물 `server/tests/test_duplicate_launcher.py`(24) | [architecture/backend §1.3 / **§1.3-bis**](../architecture/backend.md) · 재사용 관점은 [architecture/PRIMITIVES §6-bis](../architecture/PRIMITIVES.md) · 점검은 [qa/FEATURE_CHECKLIST §2.15](../qa/FEATURE_CHECKLIST.md) · 게이트 판정은 [process/PRODUCTION_READINESS](./PRODUCTION_READINESS.md). 🔴 **실패 계급이 둘이 아니라 셋이다** — 환경 장애(무한 완만 재시도) / 고장난 자식(6회 뒤 포기) / **자원을 남이 쥔 실패**(재시도가 원리적으로 못 고침 → 즉시 영구 실패). 새 실패 계급을 앞의 둘에 욱여넣지 마라. 🔴 **포트 프로브에 `SO_REUSEADDR`을 세우면 가드가 조용히 통과한다**(bind가 성공해 버린다) — 탐지는 **bind와 connect 둘 다**이고 그 축은 `test_the_bind_probe_and_the_connect_probe_are_both_load_bearing`이 지킨다. 🔴 **가드는 열려 실패한다** — 프로브 예외 시 런처는 경고만 남기고 기동하고, 감시자는 아무것도 결정하지 않는다. 🔴 **자식 stdout는 바이트 그대로 통과시킨다**(디코딩하면 cp949 자식의 줄이 사라진다). 🔴 **런처 인자는 `server/launcher_args.py`가 파싱한다**(2026-08-04 — 종전 `sys.argv` 멤버십은 **모르는 인자를 조용히 무시**해서 `--server_only` 오타 하나가 풀스택을 띄웠다). 플래그를 추가·변경하면 **`FLAGS` 튜플만** 고쳐라 — 파싱·오타 제안·`--help`가 전부 거기서 나온다. 기존 철자는 절대 바꾸지 마라(운영자 근육기억). 회귀 그물 `server/tests/test_launcher_arguments.py`(44). 🔴 **바인드 주소는 `bind_targets`가 단일 정의**(2026-08-04 — `ASSY_API_HOST` 기본값이 `0.0.0.0`에서 듀얼스택 와일드카드 `DUAL_STACK_HOST = ""`로. IPv4만 듣고 있어 `localhost` WebSocket이 영구 CONNECTING에 걸린 사고). **`::`로 바꾸면 IPv4를 잃는다** — asyncio가 `IPV6_V6ONLY=True`를 세우므로 듀얼스택 소켓이 아니다. 명시 `ASSY_API_HOST` 값의 의미는 **불변**(기본값만 이동). 회귀 그물 `server/tests/test_dual_stack_bind.py`(11) · 근거표 [architecture/backend §1.3-ter](../architecture/backend.md) · 점검 [qa/FEATURE_CHECKLIST §2.15](../qa/FEATURE_CHECKLIST.md) | Backend / Ops |
| **데이터 루트·격리 환경** | `server/paths.py`, `server/scripts/dev_env/devenv.py`, `iso_watcher.py`, **`server/db_safety.py`**(2026-07-31 #16ⓐ — "테스트 프로세스는 실제 DB에 닿지 못한다"는 판정의 단독 소유자. `under_pytest()`·`check_test_database()` 허용목록·세 겹 훅. 호출자는 `database/database.py`와 `main.bootstrap_database_schema`) | [guide/DEPLOY_SETUP §5 / §5.2](../guide/DEPLOY_SETUP.md) · 설정 관점은 [guide/CONFIG_GUIDE §1](../guide/CONFIG_GUIDE.md) · 부팅 DDL 계약은 [architecture/backend §1](../architecture/backend.md). 🔴 **허용목록에 대상 종류를 추가하는 것은 무결성 변경이다** — `server/tests/test_ddl_never_reaches_production.py`가 결함 주입으로 채점하므로 판정을 넓히면 그 그물부터 확인할 것 | Backend / Ops |
| **롤백(배포 되돌리기)** | `run_decoupled_app.py`(재기동 단위), `database/config_watcher.py`, `models.sync_dynamic_tables_schema`/`create_missing_dynamic_tables`(한 방향 DDL), `server/scripts/list_undeclared_tables.py`(잔여물 검출 — 읽기 전용) | [guide/ROLLBACK_PROCEDURE](../guide/ROLLBACK_PROCEDURE.md) — **반영 시점(코드/config/스키마)이나 재기동 단위를 바꾸면 필수 갱신**. 요약은 [guide/DEPLOY_SETUP §7](../guide/DEPLOY_SETUP.md), 게이트 판정은 [process/PRODUCTION_READINESS B4](./PRODUCTION_READINESS.md) | Backend / Ops |
| **config 백업(복원 원본)** | `server/config_backup.py`(스냅샷·보관·신선도 판정), `server/scripts/backup_config.py`(운영자 CLI), `run_auto_update.MultiDiscoveryScheduler.maybe_backup_configs`(주기 호출), `health.probe_config_backups` | [guide/ROLLBACK_PROCEDURE §3.1-bis](../guide/ROLLBACK_PROCEDURE.md)(복원 절차 — **정본**) · [guide/CONFIG_GUIDE §1](../guide/CONFIG_GUIDE.md)(`.bak` 3종 구분·규격) · [guide/AUTO_UPDATE_GUIDE §4-bis](../guide/AUTO_UPDATE_GUIDE.md)(왜 수집기가 아닌가) · 게이트 판정은 [process/PRODUCTION_READINESS C3](./PRODUCTION_READINESS.md). **명명 규칙·보관 주기·제외 대상을 바꾸면 네 곳 모두 필수 갱신** | Backend / Ops |
| **제품 소유 테이블 배포** | `server/product_tables.py`(단일 정의), `server/scripts/install_product_tables.py`. **2026-08-04 `6420ad0`: `valid_die_ref` 승격** — 유효 다이 맵의 저장 테이블이 사용자 확정으로 **이름·컬럼 고정**됐다(`business_key=cell_key` · `composite_key_source=[product,type,x,y]` · 구분자 `_` · `map_key_columns=(product,type)`이라 맵 키는 `PRODUCT_TYPE`) | [guide/DEPLOY_SETUP §1-2](../guide/DEPLOY_SETUP.md) · [guide/CONFIG_GUIDE §5.8-ter](../guide/CONFIG_GUIDE.md) · `valid_die_ref`의 **의미 계약은 [spec/MAP_EDITOR_SPEC §5.7-a](../spec/MAP_EDITOR_SPEC.md)**. 🔴 **「이 다이가 유효한가」 컬럼을 추가하지 마라 — 행의 존재가 곧 답이다.** 🔴 **여기 행이 있는 것만으로는 참조가 안 된다**: 그 맵이 `wafer_map_metadata`에도 등록돼 있어야 무엇도 가리킬 수 없다. 🔴 **고정된 것은 *저작*이지 저장 형식이 아니다** — `parseValidDieRef`는 종전 두 형식(맨 문자열 = 내 테이블의 맵 · 객체형)을 그대로 받고, 옛 선언을 전량 재조준하면 남의 맵을 가리키던 참조가 통째로 끊어진다 | Lead / Backend |
| **프로덕션 게이트** | (전 서브시스템 — 운영 관점) | [process/PRODUCTION_READINESS](./PRODUCTION_READINESS.md) — 차단 항목 해소 시 갱신 | Lead |
| DOE 저장 분해도 | **`client2/src/split_registry_row.js`**(2026-08-04 R2 `636f867` — **행의 정규형**: 저장 페이로드 `buildLegendRegistryUpdates`·컬럼 목록 `LEGEND_PAYLOAD_COLUMNS`·응답 파서 `parseLegendRegistryRows`·지문 `registryFingerprint`·서명 `legendRowSignature`. 🔴 **`LEGEND_PAYLOAD_COLUMNS`를 그 세 소비자에게서 떼지 마라** — 「저장되는 필드는 필연적으로 비교되는 필드」라는 성질이 셋이 한 파일에 있어서 성립한다. 🔴 `server/tests/test_install_product_tables.py`가 이 파일의 페이로드 리터럴을 **정적으로 읽어** `product_tables.py` 선언과 대조하는데, **파일명이 안 맞으면 `pytest.skip`이라 오탐이 통과처럼 보인다** — 경로를 옮기면 그 테스트가 *실제로 돌았는지*(`skipped: 0`) 확인하는 것까지가 한 작업이다), `client2/src/map_editor.js`(**쓰기 자체** — `saveLegendToServer`가 여전히 유일한 기록자이고, 그것이 쓰는 legend 클러스터 상태는 이 파일을 떠날 수 없다), `client2/src/transfer_plan.js`(읽기·파생), `server/transfer_plan.py`, `map_split_registry` | 현행 계약은 [spec/MAP_EDITOR_SPEC §6](../spec/MAP_EDITOR_SPEC.md) · [guide/CONFIG_GUIDE §5.8](../guide/CONFIG_GUIDE.md) · **사용자 안내는 [guide/DOE_GUIDE](../guide/DOE_GUIDE.md)**(2026-07-28 신설 — UI 동작·계약 변경 시 함께 갱신). [spec/DOE_STORAGE_MAP](../spec/DOE_STORAGE_MAP.md)은 🗄️ **폐기된 3테이블 모델**로, 기존 데이터 해석용으로만 보존(M2.6 양측 착지 `cdcddee`+`0f8d35f`) | UI/Map |
| **교차 구현 계약 벡터(`contracts/`)** | `contracts/<name>/` — 발견식 러너 `client2/scripts/check_contracts.mjs`(경로가 `contracts/<name>/client_harness.mjs`가 **아니면 발견되지 않는다**), 빌드 게이트 `client2/package.json`의 `check:contracts`. 2026-08-04 실측 **6개**: `band_arithmetic`(레거시 `bands` 산술) · **`blank_predicate`**(빈 값 술어의 SQL 철자 ↔ 파이썬 철자 — 2026-08-04 `5be96f5`에 **숫자 축**이 붙었다: `crud.numeric_text_sql`) · `config_resolve_report`(F9) · `doe_band_rules`(zone 모델 V1–V6·토큰 문법·수요 산술·Excel 왕복) · `legend_map_scope`(legend map_key 스코프 — 벡터 파일 없이 하네스 단독) · `map_seam`(맵 서버/클라 이음새). ⚠️ **2026-08-04 클라 파일이 갈라졌다** — `map_seam`은 `client2/src/map_key.js`를, `legend_map_scope`·`band_arithmetic`·`doe_band_rules`는 `client2/src/split_registry_row.js`를 **두 번째 소스로** 읽는다. 🔴 **텍스트 슬라이서는 `export`에 눈이 멀다**: 이 저장소의 슬라이서 다섯 중 셋만 우연히 선행 `export `를 허용하고 둘은 아니며, 관용을 넣을 때는 **`export `를 슬라이스에서 빼야** 한다(vm 안의 `export` 문은 SyntaxError라 순진한 수정이 초록 하네스를 하드 실패로 바꾼다) | 각 계약의 **의미**는 소비 서브시스템 행이 정본이다 — `band_arithmetic`·`doe_band_rules`·`legend_map_scope`는 [spec/MAP_EDITOR_SPEC §6](../spec/MAP_EDITOR_SPEC.md)(DOE 저장 분해도 행), `map_seam`은 M4·오버레이 행, `config_resolve_report`는 config 해석 보고서 행. 게이트 관점은 [architecture/frontend §2](../architecture/frontend.md)와 [qa/FEATURE_CHECKLIST §2.0](../qa/FEATURE_CHECKLIST.md). 🔴 **계약을 추가·삭제하면 그 두 곳의 계약 *수*와 목록을 함께 고쳐라** — 기대값이 실제보다 작으면 스캔이 조용히 비어도 눈이 통과시킨다(실제로 일어났고, 산문만 고치고 코드 샘플을 놓친 재발까지 있었다). 🔴 **소비 서브시스템 행이 없는 계약을 만들지 마라** — `band_arithmetic`·`doe_band_rules`·`legend_map_scope` 셋은 2026-07-30까지 **어느 행에도 지목되지 않아** 코드맵 밖에서는 고아였다 | Integrity/QA · contract-keeper |
| 코드 구조 지도 | `server/*`, `client2/src/*` (전 모듈) | [architecture/CODE_MAP](../architecture/CODE_MAP.md) — 갱신 **code-mapper 전담**, 정합 감사 **doc-auditor 전담**(2026-07-27 분할. 구현 에이전트는 보고서에 변경 함수 목록만) | 전 에이전트 공용 |
| 백엔드 API/워커 | `server/main.py`, `server/*_worker.py`, `server/run_*.py` | [architecture/backend](../architecture/backend.md) | Backend/Sync |
| 이벤트 기반(Outbox/EDA) | `server/database/database.py`, `chain_ingestion_worker.py`(`sweep_undelivered_broadcasts` — **재발사 집합 `refresh_targets = affected_targets \| source_tables`**), `graph_sync_worker.py`, **2026-08-04 `2aab7e2` 브로드캐스트 복구**: `internal_event_client.record_undelivered_notification`(호출은 `run_watcher.post_event` 하나) + `event_constants.EVENT_BROADCAST_RECOVERY`(`CONTROL_EVENT_TYPES` 소속), 회귀 그물 `server/tests/test_broadcast_recovery.py`(12) | [architecture/event_driven_backend](../architecture/event_driven_backend.md) · 재사용 관점은 [architecture/PRIMITIVES §6](../architecture/PRIMITIVES.md) · 점검은 [qa/FEATURE_CHECKLIST §2.7](../qa/FEATURE_CHECKLIST.md). 🔴 **스위퍼가 「발사할 것이 없다」고 판단하는 분기가 조용한 소각로다** — 규칙에 안 걸리는 미전달 행(규칙 비활성·트리거 아님·워처 마커)이 **아무것도 발사되지 않은 채 확정만** 찍혔다. **무한 재스윕을 막는 것은 「발사 안 함」이 아니라 「확정을 찍는 것」**이므로 발사 집합에 **행이 기록된 테이블 자신**을 항상 넣어라. 🔴 **마커에 실패한 통지의 페이로드를 복사하지 마라**(batch-refresh는 감사 로그 500건까지 나른다 — 2026-07-25 약 50 MB 사고). 🔴 **마커 쓰기는 절대 raise하지 않는다.** 🔴 **새 이벤트 타입을 만들면 그것이 *안* 들어가야 할 집합을 테스트로 못박아라**(`test_the_marker_type_is_never_read_as_a_data_change`) | Backend/Sync |
| 프론트엔드(웹+셸) | `client2/src/*`, `client/desktop_wrapper.py` | [architecture/frontend](../architecture/frontend.md) — **§3 모듈 표는 `client2/src/*.js` 전수를 덮어야 한다**(행 없는 모듈은 조용히 낡는다: 2026-07-30에 `value_suggest.js`·`doe_bands.js`·`tsv.js` 셋이 행 없이 굴러가고 있었다). 모듈을 추가/삭제하면 그 표를 함께 갱신. 아래 전용 행이 있는 서브시스템(값 제안·맵 에디터·전사 계획)은 그쪽이 정본 | UI/Excel |
| **값 제안(F3 — 접두 조회 + 셀 에디터 + 인덱스 정책)** | **서버**: `server/value_suggest.py`(`suggest_values`·`prefix_conditions`·`db_fold`·**`index_targets`가 인덱스 대상 정책의 단독 소유자**·`suggest_index_name`/`suggest_index_definition`), `GET /tables/{t}/columns/{c}/values`(`main.get_column_unique_values` — `/tables/{t}/{row_id}` **위에** 등록돼야 함), `server/scripts/setup_db_performance.py`(인덱스 빌더 — 정책을 재구현하지 않고 `index_targets`에 **물어본다**), `server/config/suggest_config.json`. **클라**: `client2/src/value_suggest.js`(`SuggestCellEditor`/`handleEditorKey`), `client2/src/grid.js`(에디터 갈아끼움 + `suppressKeyboardEvent` 첫 분기) | [architecture/frontend §3.3](../architecture/frontend.md)(**클라 계약 정본** — `Enter` 한 번 · Escape 술어 `suggestionsEngaged` · 한도 12의 두 근거) · **설정 절차·키 사전은 [guide/config/suggest_config](../guide/config/suggest_config.md)** · 엔드포인트 계약은 [architecture/backend §2](../architecture/backend.md) · 점검 절차는 [qa/FEATURE_CHECKLIST §1.1/§2.1](../qa/FEATURE_CHECKLIST.md) · 회귀 그물 `client2/tests/value_suggest_keys_harness.mjs`(⚠️ **`check:suggest-keys`는 2026-07-30 밤 `prebuild`에서 빠졌지만 약해지지 않았다** — 신설 `check:harnesses`가 `client2/tests/`를 발견식으로 훑어 같은 파일을 매 빌드마다 돌린다. 스크립트는 단독 실행용으로 남아 있다). **키보드 계약·한도·플로어·TTL을 바꾸면 frontend §3.3 + FEATURE_CHECKLIST 필수 갱신**이고, 인덱스 대상 정책을 바꾸면 `suggest_config` 가이드까지 셋 | Backend / UI |
| **데스크톱 셸의 서버 주소 해석(경계 계약)** | **`client/desktop_wrapper.py`**(2026-07-30 `e9b3a36` — `resolve_server_target`/`base_url`/`settings_file_path`. **셸이 어느 서버를 보는지의 유일한 결정 지점**이고 `http://` 조립도 한 곳이다) | [architecture/frontend §1.1](../architecture/frontend.md)(**정본** — 우선순위 4단·거절 대상·`--print-target`·`NO_PROXY`·exe 번들 제외) · 점검 절차는 [qa/FEATURE_CHECKLIST §1.10](../qa/FEATURE_CHECKLIST.md). **우선순위·거절 규칙·시작 로그의 `source` 어휘를 바꾸면 두 곳 모두 필수 갱신** | UI / Ops |
| **클라 측 설정 파일** | **`client/client_settings.json`**(git **추적** 대상 — `server_host`/`server_port`/`current_user`. 읽는 곳은 `desktop_wrapper.settings_file_path()` 하나이고, frozen exe에서는 `sys.executable` 디렉터리를 먼저 본다) | [architecture/frontend §1.1](../architecture/frontend.md) — **`server/config/*` 행들은 이 파일을 덮지 않는다**(서버 config는 gitignored + `.sample` 관례이고 `config_watcher`·`config_backup`·[CONFIG_GUIDE](../guide/CONFIG_GUIDE.md)의 대상이지만, 이 파일은 **추적되고 리로드 대상이 아니며 백업 스냅샷에도 들어가지 않는다**). 키를 추가/개명하면 frontend §1.1 + FEATURE_CHECKLIST 갱신 | UI / Ops |
| **프로세스 간 내부 HTTP(loopback IPC)** | **`server/internal_event_client.py`**(2026-07-30 `23a346d` — `internal_event_session()`이 **워커→웹서버 HTTP 클라이언트의 유일한 소유자**이고 `trust_env=False`로 프록시 참조를 구조적으로 막는다. `api_base_url()`·`proxy_environment_summary()`·`is_loopback()`·`check_api_reachable()`·`startup_lines()`), 진단 문구는 `server/admin_auth.internal_event_failure_note`, 소비자는 `run_watcher`/`chain_ingestion_worker`/`graph_sync_worker` + `main.py`의 GraphSync 홉(`httpx`, 같은 설정) | [architecture/PRIMITIVES §6](../architecture/PRIMITIVES.md)(**재사용 정본** — 왜 소유자가 하나여야 하고 왜 `NO_PROXY`가 대체재가 아닌지) · 운영 절차는 [guide/DEPLOY_SETUP §1-5](../guide/DEPLOY_SETUP.md) · 점검은 [qa/FEATURE_CHECKLIST §1.12/§2.16-D](../qa/FEATURE_CHECKLIST.md). 🔴 **새 발신자를 추가하면 세션을 직접 만들지 말 것** — `test_admin_auth.py::test_no_sender_builds_its_own_client`가 `SENDERS`를 파라미터화해 막고 있고, 그 목록에 새 모듈을 **추가하는 것까지가** 발신자 추가 작업이다. 🔴 **로그 문구는 cp949 인코딩 가능해야 한다**(운영 콘솔이 한국어 Windows — em dash 하나가 그 줄을 통째로 삭제시킨다). 🔴 **판별자는 상태코드가 아니라 응답 BODY다 (2026-08-04)** — `own_health_payload(response)`가 `status` 키와 **dict인 `checks`**를 **함께** 요구하는 지문이고, 그것이 있으면 **`WARNING`**(이 애플리케이션이 답했고 스스로 unhealthy라고 말한다), 없으면 `ERROR`(앞단). 종전의 「200이 아닌 모든 상태 = 앞단이 답했다」는 **거짓이다** — `/health`는 체크 하나만 실패해도 **설계상 503**(`health.HTTP_UNHEALTHY`)이라 그 규칙은 **아픈 스택을 매번 프록시로 고발**했고, 2026-07-31 중복 런처 인시던트에서 워커 둘이 프록시 장문을 찍어 진단을 오도했다. **탐지가 없어진 게 아니라 방아쇠만 좁아졌다.** ⚠️ **이 문장은 최소 4파일에 복사돼 있었다**(`PRIMITIVES §6` · `DEPLOY_SETUP §1-5 A/C-4` · `FEATURE_CHECKLIST §2.16-D` · 이 행) — 소유 행 밖의 사본은 각 소유자에게 넘긴다. `CODE_MAP §5`의 사본은 **code-mapper 소관**이라 손대지 않았다(보고). ✅ **해소(`98956fd`)**: `check_api_reachable()`의 ERROR 문구가 `NO_PROXY`를 권하던 것 → **금지로 뒤집었다**. ⚠️ **이 행이 하루 만에 거짓이 됐다는 사실 자체를 규율로 남긴다** — 「미해결」 표기는 인용한 문자열을 **grep으로 실측한 뒤에만** 쓰고, 같은 문장을 복사한 다른 문서를 그 자리에 함께 적는다(이 문구는 `DEPLOY_SETUP §1-5 C-4`에도 복사돼 있었고 둘 다 낡았다) | Backend / Ops |
| **컬럼 필터 번역기 — 그리고 「`main`은 워커 안에서 안정된 이름이 아니다」** | **`server/column_filter.py`**(2026-08-04 `cd80d66` 신설 181줄 — `get_column_filter_condition`이 `main.py`에서 **통째로 나왔다**. `main.py`가 그 이름을 **재수출**하므로 `main.get_column_filter_condition`은 계속 풀린다 — 옮긴 것이지 계약을 깬 것이 아니다), 소비자 `server/enrichment_analysis.py`, 회귀 그물 `server/tests/test_entrypoint_import_isolation.py`(2) | [architecture/backend §1.2](../architecture/backend.md)(**정본**). 🔴 **어떤 `server/` 모듈도 `import main`을 하면 안 된다** — `run_auto_update.py`가 수집기마다, `parsers/directory_watcher.py`가 테이블마다 그 디렉터리를 `sys.path[0]`에 넣으므로, 그중 하나에 **사용자가 만든 `main.py`**가 있으면 그것이 먼저 바인딩되고 우리 심볼은 거기 없다. 🔴 **증상이 프로세스에 따라 갈린다** — 실측: 같은 스윕이 **CLI에서는 통과하고 워커에서만** `AttributeError`. 그래서 그물이 둘이다(소스 규칙 + **미끼 `main.py`를 `sys.path` 맨 앞에 둔 subprocess 프로브**). 🔴 **규율**: 워커·수집기·파서가 부를 수 있는 것은 애플리케이션 진입점이 아니라 **자기 이름을 가진 모듈**에 둔다 — 웹 진입점은 조립하는 곳이지 라이브러리가 아니다. ⚠️ **이 행은 `server/scripts/`의 「한 방향 문」과 같은 계급이고 다른 축이다**(저쪽은 디렉터리가 `sys.path`에 **없어서**, 이쪽은 이름이 **선점당해서**) | Backend |
| **좌표계 확정 기록 (맵 정렬 스펙 §0.2 층 ⑧ — 사슬에서 쓰는 유일한 층)** | **`server/frame_confirmation.py`**(2026-08-05 신설 — 쓰기는 `record_confirmation` 하나. `weakest_contributor`·`live_confirmation`·`derived_cell_scope`), 모델 `server/database/models.py`(`FrameConfirmation`·`FrameConfirmationSource`·**`CellSource.confirmation_uid`**), 스키마 `server/migrations/add_frame_confirmation.py`(추가 전용·멱등), **라우트 `POST /api/maps/alignment/confirm`**(`main.py`. 🔴 **[2026-08-11] 「사슬에서 쓰는 유일한 요청」이 거짓이 됐다** — 아래 ⚠️ 참조), 인덱스 두 번째 선언 `server/scripts/setup_db_performance.py` Step 3.10, 회귀 그물 `server/tests/test_frame_confirmation.py` + **`server/tests/test_frame_confirmation_meta.py`**(2026-08-06 [D7] — 확정이 쓰는 메타) | [architecture/data_model §4-bis](../architecture/data_model.md)(**정본**) · 목적 사슬과 층 정의는 [spec/MAP_ALIGNMENT_SPEC §0.1/§0.2](../spec/MAP_ALIGNMENT_SPEC.md) · 채점·판정(층 ⑤⑥⑦)은 `server/map_alignment.py`. 🔴 **enrichment `eqp_product_frame_attribution`을 대체하지 않는다** — 확정의 몸짓·단위·사람 확인 경로·auto_confirm은 그쪽 것을 그대로 쓰고, 담을 수 없는 **소스 목록·소스별 정렬(시프트 포함)·판** 셋만 여기서 담는다. 🔴 **도장을 `source_name`에 철자하지 마라** — `get_source_priority`가 정확 일치 조회라 확정마다 나는 이름은 99로 가라앉고 도장이 자기 값을 강등한다. 🔴 **최약 기여자는 `graph_materializer`와 같은 식이고 둘 다 `crud.get_source_priority`에 도달한다**(서열 이원화 금지). 🔴 **재파생 철자를 만들지 마라** — `derived_cell_scope`는 질의만 돌려주고 회수는 `chain_replay.withdraw_source`다. 🔴 **`idx_sources_confirmation`은 `models.py`와 마이그레이션 두 곳에 선언돼 있고 둘 다 고쳐야 한다**(`idx_sources_by_source`와 같은 계급). 🔴 **쓰기 라우트에 GET 쌍둥이를 만들지 마라** — 탐색은 아무것도 쓰지 않는 것이 §8 합격선이고, 같은 일을 하는 GET은 그 선을 지운다. 🔴 **쓰기 경로에서 재채점하지 마라** — `ruling`·`sources`는 요청이 명시적으로 실어 오고, 기록해야 하는 것은 조작자가 본 판정이다. 🔴 **결정 단위에 컬럼명을 적지 마라** — 정본은 규칙의 `decision_key`이고 저장은 `rule_name`+`unit_key`+`decision_key` JSON이다(`dt_eqp`/`product`는 첫 선언의 흔적). ✅ **층 ⑨ 연결됨 (2026-08-05)** — 읽는 자리는 `bonding_plan.canonical_basis` 하나이고 `get_core_summary`·`transfer_plan._canonical_origin_meta` 둘이 그것을 부른다(한쪽만 읽으면 같은 웨이퍼가 M1·M2에서 다른 수치를 낸다). 조회는 `frame_confirmation.live_confirmation_for_maps` — 🔴 **단위를 (설비, 제품)으로 되짚지 마라**(계획의 신원은 (lot, slot)이고, 되짚으려면 계획이 `dt_log` 컬럼명을 알아야 해서 「결정 단위에 컬럼명을 적지 마라」와 어긋난다). 🔴 **`excluded_reason` 기여자는 확정을 주장할 수 없다.** 🔴 **인덱스 `idx_frame_conf_src_map`도 두 곳 선언**(`models.py` + 마이그레이션). 퇴화형은 폴백으로 남고 `frame_basis`가 어느 길로 왔는지 말한다 → [spec/MAP_EDITOR_SPEC §6.2-ter.2](../spec/MAP_EDITOR_SPEC.md), 회귀 그물 `server/tests/test_plan_frame_basis.py`. ✅ **층 ⑧이 `wafer_map_metadata`까지 간다 (2026-08-06 [D7], 제품 소유자 확정)** — `map_alignment.confirmed_meta_for`(무엇을 쓰는지의 단독 판정) + `frame_confirmation._write_confirmed_meta`(쓰기) + `map_overlay`의 일곱 번째 토큰 `confirmed`와 표지 `phys_confirmed_from`/`frame_confirmed_from` → [spec/MAP_ALIGNMENT_SPEC §9.7](../spec/MAP_ALIGNMENT_SPEC.md) · [architecture/data_model §4-bis](../architecture/data_model.md). 🔴 **§9.1의 「빌린 값은 `wafer_map_metadata`에 도달하지 않는다」가 뒤집혔다** — 금지는 **표지 없는** 빌림에 대한 것이었고 그 금지는 유효하다. 문장을 고쳐 쓰지 말고 §9.7을 가리켜라. 🔴 **`grid_y_invert`는 확정 대상이 아니다**(후보 축이 아니라 아무것도 그것을 채점하지 않는다 — 덮으면 확정된 회전·면의 뜻이 바뀐다). 🔴 **격자를 그 맵의 셀에서 다시 유도하지 마라** — 채점은 빌린 격자 위에서 돌았고, 새로 합성하면 채점이 쓴 적 없는 프레임이 기록된다. 🔴 **`user` 서열로 써라** — 낮으면 `custom_script` 셀이 이겨 아무것도 안 바뀐 채 200이 나간다. 🔴 **업무 키 철자는 `map_meta_registrar.meta_business_key` 하나**(등록기와 공유 — 두 철자면 재확정이 맵 하나를 두 행으로 쪼갠다). 🔴 **[D8/D9 보드 #23 감사 2026-08-06 — 기록 둘이 갈리는 자리를 닫았다]** ① **기존 행 갈래도 「채점이 선 프레임」을 적는다** — 종전에는 격자를 안 건드려서 등록기의 bbox 격자(부분 맵의 하한)와 바닥의 phys를 짝지어 기록했고, 그 조합 위에서는 아무 채점도 돈 적이 없다(실측: 채점 13×13@(0,0) / 기록 5×5@(2,1)). ② **`auto_registered`는 phys를 다시 쓰면 뗀다 — 단 격자 출처를 댈 수 있을 때만.** 그냥 떼면 `grid_start_*`가 `declared`로 올라선다(그 표지가 start를 설명하던 유일한 표지다). ③ **쓰기의 phys 술어는 채점의 것과 다르다** — 채점은 「빌려야 하는가」, 쓰기는 「잰 값 위인가」. 합치면 **다른 바닥에 대고 재확정할 때 첫 바닥의 규격이 둘째 바닥의 배지 아래 남는다.** ④ **어휘 검사가 한쪽에만 있었다** — `frame`·`applied_frame`을 `map_alignment.parse_frame`에 통과시킨다(`_reject_unreadable_frame`). 🔴 **`frames`(`target_fields`) 값에는 걸지 마라 — 그것은 프레임이 아니다**(lot·slot이 들어간다). → [spec/MAP_ALIGNMENT_SPEC §9.7-bis/§9.8](../spec/MAP_ALIGNMENT_SPEC.md). ⚠️ **확정은 규격 셀의 핀(`manual_priority_source`)을 푼다** — `user` 쓰기의 일반 규칙(2026-06-02)이고 확정 고유의 파괴가 아니다(통제군 대조 완료). 조작자용 서술은 [guide/data_preservation §3](../guide/data_preservation_and_signature_change.md). 🔴 **미해결(총괄 배정 대기)**: `bonding_plan.canonical_basis`가 프레임 **값**을 메타 행(기록 B)에서 읽으면서 `kind: BASIS_CONFIRMATION` + 확정 uid를 붙인다 — `confirmed_frame`은 `bonding_plan.py`·`transfer_plan.py`에 **0건**이다. 게다가 저쪽은 **기준 맵**의 행을 읽는데 확정은 **기여자** 맵만 도장하므로, 확정이 건드린 적 없는 행을 읽을 수 있다. ✅ **클라 절반 착지 (2026-08-06)** — `client2/src/map2/declaration.js`의 `DECLARATION_TOKENS`/`COMPUTABLE_TOKENS`에 `confirmed`가 들어왔고 `contracts/map2_seam/`가 양쪽을 채점한다. 🔴 **그런데 이 ⏳ 표시 자체가 일을 절반으로 적고 있었고, 그것이 이 행이 남길 교훈이다** — 표시는 *「토큰 목록 + 벡터 케이스」*만 말했지 **`geometryDeclaration`의 `phys_confirmed_from` 분기**를 말하지 않았는데 **그쪽이 더 큰 절반**이었다. 목록만 늘린 포트는 조용히 틀린다: `confirmed`는 `assumed`와 달리 **행에 저장되므로** 클라가 DB에서 곧바로 읽고, 분기가 없으면 **아무도 잰 적 없는 맵에 클라가 `declared`라고 답한다**(`assumed`는 저장되지 않아 클라가 생성조차 못 하므로 둘을 「근거이고 선언 아님」으로 접으면 안 된다 — [spec/MAP_EDITOR_SPEC §1-ter.1](../spec/MAP_EDITOR_SPEC.md)). 🔴 **절반만 적은 대기 표시는 절반에서 닫힌다** — 이 표에 ⏳를 달 때는 **미착수 작업의 경계를 다 적어라.** 그리고 같은 날 **어휘의 *개수*를 적은 문장 셋이 한꺼번에 낡았다**(`MAP_EDITOR_SPEC §1-ter.1`의 「여섯」·`PRIMITIVES §7`의 「여섯을 다 확인했다」·클라 하네스의 `TOKENS.length === 6`) — **기수는 목록의 두 번째 사본이니 구성원을 이름으로 불러라.** ⚠️ **WS 이벤트 없음**(총괄 결정 2026-08-05 — 듣는 쪽이 없다). 🔴 **클라 `client2/src/map2/api.js`의 `ROUTES`는 네 경로 전부 옛 철자(`/map/align/*`)이고 서버에 그런 경로는 없다** — 총괄 판정 대기.<br>🔴 **[2026-08-11 · 총괄 판정 대기 — 확정 기하의 *생산자가 둘*이 됐다]** DT/Core 프레임 파생 체인(`a501d6d`, 위 신설 행)이 `map_alignment.confirmed_meta_for`를 **직접 부르고** `wafer_map_metadata`를 쓴다 ― `server/mappers/dt_alignment_metadata_mapper.py`. **그 경로는 `frame_confirmation.record_confirmation`을 지나지 않으므로 `FrameConfirmation` 행도 `CellSource.confirmation_uid`도 남기지 않는다**(실측: 매퍼 넷에 `FrameConfirmation`·`confirmation_uid` 철자 **0건**). 결과: **확정 기하를 든 메타 행 중 일부는 「어느 확정에서 왔는가」를 되짚을 수 없다.** 🔴 **이 행의 서두와 [spec/MAP_ALIGNMENT_SPEC §0.2](../spec/MAP_ALIGNMENT_SPEC.md)의 「쓰는 층은 ⑧ 확정 하나」가 함께 낡았다** — 어느 쪽이 정본인지(체인 경로도 확정 기록을 남겨야 하는가 / 자동 파생은 기록 대상이 아닌가)는 **총괄 판정 사항**이라 문장을 고쳐 쓰지 않고 표시만 한다. | Backend/Integrity |
| **맵 정렬 화면 (Map Editor 2) — 클라** (2026-08-05 신설 행) | `client2/map_editor2.html` · `client2/src/map_editor2.js` · **`client2/src/map2/*` 17모듈**(신원·`declaration.js`·`seating.js`·`verdict.js`·`api.js`·`session.js`·`view_model.js`·`main.js`(조립)·`painter.js`·`excel_io.js`·`decode.js`·`brush.js`·`legend.js`·`candidates.js`·`artifact_gateway.js`·`authoring.js`·`verdict_bridge.js`), 진입점 선언 `client2/vite.config.js`, 서버 라우트 `39b43ab`, 하네스 `client2/tests/map2_*`·`map_editor2_*`, 이음매 `contracts/map2_seam/` | **설계 계약의 정본은 [spec/MAP_ALIGNMENT_SPEC](../spec/MAP_ALIGNMENT_SPEC.md)**(층 정의 §0.2 · 작업 순서 §0.3 · 불변식 §7 · 가정 §9.1 · 어휘 §9.2/§9.3) · 모듈 경계와 「무엇을 안 하는가」는 [architecture/frontend §4.2](../architecture/frontend.md) · 어휘 사본은 [spec/MAP_EDITOR_SPEC §1-ter.1](../spec/MAP_EDITOR_SPEC.md). 🔴 **레거시를 대체하지 않고 옆에 선다** — 새 화면이 프레임을 확정할 수 있게 되기 전까지 `map_editor.html`이 그대로 출하된다(`vite.config.js`에 그렇게 적혀 있다). **레거시를 여기로 이식하지 마라** — 동작과 함께 결함까지 복사하고 둘을 구별할 방법이 없어진다. 🔴 **새 코드는 모듈이다**(인자로 받고 값을 돌려주고 모듈 상태를 갖지 않는다) — 그래야 하네스가 소스를 자르지 않고 `import`한다. **기존 레거시 하네스는 전환하지 않고 수명이 다하게 둔다.** 🔴 **좌석(`seating.js`)과 그리기(`painter.js`)를 다시 합치지 마라** — 레거시가 정확히 그 결함이고(화면 밖 `continue`가 등록보다 앞), `seating.js`에는 캔버스 크기가 없어서 `continue`를 놓을 자리가 없는 것이 설계다. 🔴 **정렬 캔버스에 웨이퍼 테두리를 그리지 마라**(`d4e0fed` — 하드코딩 원이 모든 맵에서 넘쳤고 제품 소유자가 그것을 결함으로 읽었다). 근거는 **그 맵 자신의 선언된 phys**뿐이고, 없으면 안 그린다. CSS `.me2-wafer-edge`는 생산자 없이 DORMANT로 남아 있다. ⚠️ **화면이 매일 바뀌는 중이다** — 문서에 컨트롤 이름·문구·줄 수를 옮기지 마라 | UI-Map / 설계 |
| **읽기 전용 진단 도구(운영 박스에서 직접 돈다)** (2026-08-11 신설 행) | `server/scripts/diagnose_socket.py` · `server/scripts/diagnose_db_health.py` · **`server/scripts/diagnose_slow_after_ingest.py`**(신규, `2b8a5ab`+`1a1947b` — 대량 인제션 직후의 느려짐을 스레드 토큰/DB 커넥션/쿼리 자체로 끊어 가르는 §4b 사다리. 읽기전용은 의도가 아니라 `default_transaction_read_only=on` **연결 옵션**으로 서버가 강제하고, 유일한 쓰기는 선택적 `--json` 파일뿐이다. 운영 박스가 출력 복사를 금지해 **결론이 검산 다이제스트가 붙은 한 줄로 끝난다**) — 셋 다 **이 표에 행 없이** 두 라운드(`diagnose_socket`/`diagnose_db_health`는 2026-08-04)를 출하돼 있었다 | [spec/DEBUGGING_GUIDE §0](../spec/DEBUGGING_GUIDE.md)(**정본, 도구 셋 전부**) | Ops / QA |
| **스키마 드리프트 부팅 점검** (2026-08-05 신설 행) | **`server/schema_drift.py`**(로직 단독 소유자 — `check`·`run_at_startup`·`banner_lines`·`_owner_from_sources`·`MIGRATION_OWNER`·`SEVERITY_ORDER`), 발화 지점 둘 `run_decoupled_app.report_schema_drift()`(포트 점검 옆·자식 spawn 전·`--preflight-only`에 포함) + `server/main.py` `startup_event`(자기 outbox 마이그레이션 **뒤**), CLI 껍데기 `server/scripts/check_schema_drift.py`, 회귀 그물 `server/tests/test_schema_drift_startup.py`(24). 인접: `server/tests/test_system_schema_drift.py`(`SYSTEM_TABLE_COLUMNS` 게이트) | [architecture/backend §1.3 ①-ter](../architecture/backend.md)(**정본**) · 재사용 관점 [architecture/PRIMITIVES §6](../architecture/PRIMITIVES.md) · 배포 절차 [guide/DEPLOY_SETUP §6](../guide/DEPLOY_SETUP.md) · 롤백 [guide/ROLLBACK_PROCEDURE §5](../guide/ROLLBACK_PROCEDURE.md). 🔴 **거절하지 않는다 — 포트 충돌과 일부러 다르게 갈랐다.** 드리프트는 「드리프트난 테이블만 빼고 제품이 돈다」이므로, 여기서 막으면 컬럼 하나가 **무인 재기동에 스택 전체를 계속 죽여 놓을 권한**을 갖는다. 🔴 **게이트는 `--preflight-only` 하나이고 그 종료 코드는 안 바뀐다.** 🔴 **소유 마이그레이션을 손으로 적지 마라** — `_owner_from_sources`가 `server/migrations/*.py`·`server/scripts/setup_*.py`·`*migrate*.py`를 스캔한다. 딕트에는 **파일로 존재할 수 없는 처방**(부팅 시 적용되는 outbox 컬럼 둘)만 남는다. ⚠️ **`server/schema_drift.py:236`의 폴백 문구가 아직 「`MIGRATION_OWNER`에 기록하라」고 말한다** — 유도 모델과 어긋나는 지시이고 **코드라 doc-keeper가 못 고친다**(총괄 라우팅). 🔴 **점검 모집단이 곧 값어치다** — 동적 테이블을 초기화하지 않으면 26개 중 12개만 보고 나머지 14개를 건강하다고 답한다. 단 **이미 초기화한 프로세스에서는 건드리지 않는다**(`DYNAMIC_TABLES`가 빌 때만). 🔴 **`TESTING`을 남기지 마라** — 런처가 `os.environ.copy()`를 자식 다섯에 물려주므로 새면 웹서버가 자기 부팅 DDL을 거부하고 워커가 전부 일을 건너뛴다. 🔴 **런타임은 `server/scripts/`를 import하지 않는다**(판정기 `server/tests/test_prod_import_env.py`) | Backend / Ops |
| 데이터 모델/레이어링 | `server/database/models.py`, `crud.py`(**타입 깔때기 `column_text_sql` + 렌더러 셋 `numeric_text_sql`·`boolean_text_sql`·`temporal_text_sql` + `TEMPORAL_TEXT_FORMAT`의 단독 소유자** — 2026-08-04 N7 `5be96f5` + N8 `9e02e3f`. 의미 계약은 위 Virtual Join 행과 [guide/config/virtual_join_rules §4-ter](../guide/config/virtual_join_rules.md). **표시값 해결의 단독 소유자 — `compute_priority_value` + `resolution_ingested_at`(2026-08-11 신설) + `resolve_priority_map`/`get_source_priority`**. 미선언 컬럼 드롭 계수기 `_warn_undeclared_column_once` + 공개 조회 **`undeclared_column_drops()`**도 여기) | [architecture/data_model](../architecture/data_model.md) — **§2.1이 해결 순서의 정본**, 소급은 §2.2-ter(R3) | Backend/Integrity |
| **표시값 해결 순서 (`compute_priority_value` — 전(全)순서여야 한다)** (2026-08-11 신설 행) | **`server/database/crud.py`**: `compute_priority_value`(판정 단독 소유자 · 선택 키워드 `ingested_at_by_source=`) · **`resolution_ingested_at`**(같은 컬럼의 **세 철자**를 정규화하는 단독 소유자 — `{"timestamp": ISO}` / `{"ingested_at": datetime}` / out-of-band 맵) · 서열 원천 `resolve_priority_map`/`get_source_priority` · 결정성 보조로 `crud.py`의 `cell_sources` SELECT **다섯**에 `ORDER BY source_name` · 유일성 근거는 `models.CellSource.idx_sources_lookup_source`(table,row,column,source_name UNIQUE) | **정본은 [architecture/data_model §2.1](../architecture/data_model.md)** — 세 층(등재 우선순위 → `ingested_at` 내림차순 → `source_name` 오름차순)·수리 전 결함·실측. 재사용 관점은 [architecture/PRIMITIVES §1「후보를 고르는 정렬은 전순서여야 한다」](../architecture/PRIMITIVES.md) · 시그니처 규율은 [guide/data_preservation §1-bis](../guide/data_preservation_and_signature_change.md) · 운영자 관점은 [guide/BACKFILL_GUIDE §1.1](../guide/BACKFILL_GUIDE.md). 🔴 **2·3층은 한 우선순위 *안에서만* 동점을 가른다** — 계층을 넘게 만드는 순간 `user`(0)가 밀리고 `test_human_correction_survives_a_newer_version` 계열이 지키던 계약이 깨진다. 🔴 **정렬 키의 마지막 성분이 유일하지 않으면 그 정렬은 답을 입력 순서에 위임한 것이다** — 종전 결함이 정확히 그것이었고, 대가는 「가끔 틀림」이 아니라 **「항상 기존 값이 이김」**이었다. 🔴 **승자는 박제된다** — 이 함수를 고쳐도 이미 확정된 셀은 안 움직이고, 그 소급은 **Chain Replay 행의 R3**다. ⚠️ **`ingested_at`을 새로 읽는 호출자를 추가하면 철자를 네 번째로 늘리지 말고 `resolution_ingested_at`에 넣어라** — 판정이 호출자별로 갈리는 자리다. ⚠️ **저장 증가(같은 셀에 재사용 안 되는 소스명이 쌓이는 문제)는 이 행이 아니고 해결되지도 않았다** — 분석은 `agent_workspace/reports/Cell_sources_growth_diagnosis.md` §6, 구현 0건 | Backend/Integrity |
| **감사 이력(Audit History) — 최근 패널 + 행/셀 페이징** (2026-08-11 신설 행 — `dab9152`+`2630790`+`fde424c`) | **`server/audit_cache.py`**(전역 「최근」 패널의 인메모리 프로젝션 — bounded discovery + capped hydration 두 단계, `RECENT_DEFAULTS`), **`server/audit_history.py`**(행/셀 페이징의 공용 원시 — `resolve_settings`/`resolve_limit`/`encode_cursor`/`decode_cursor`/`order_desc`/`apply_cursor`/`fetch_page`. `audit_cache`가 이 모듈의 커서 원시를 **재사용**하지 재구현하지 않는다), 인덱스 셋 `server/database/models.py`(`AuditLog.__table_args__`) + 마이그레이션 `server/migrations/add_audit_recent_groups_index.sql`·`add_audit_history_keyset_indexes.sql`, config `server/config/audit_history_config.json.sample` | [architecture/backend §2 이력/감사](../architecture/backend.md#이력--감사) · 인덱스 정본 [architecture/data_model §2.5](../architecture/data_model.md) · 세팅 절차 [guide/config/audit_history_config](../guide/config/audit_history_config.md) · 배포 게이트 [guide/DEPLOY_SETUP §6 8-ter](../guide/DEPLOY_SETUP.md) + [process/PRODUCTION_READINESS C4](./PRODUCTION_READINESS.md). 🔴 **`LIMIT`만으로는 DB 비용이 안 준다** — 인덱스가 실제 수리이고 config는 그 위의 상한일 뿐이다. ✅ **[`fde424c`] `/audit_logs/recent`도 바디 엔벨로프가 됐다** — `{groups, truncated, next_cursor, limit_groups, returned}`(리스트 키는 `logs`가 아니라 **`groups`**, 그룹마다 자기 `logs`를 또 갖는다). 헤더 `X-Audit-Truncated`/`X-Audit-Next-Cursor`는 **폐기가 아니라 병행 발행**. 클라는 `timeline.js`의 리더 하나(`readHistoryPage(body, listKey)`)를 세 라우트가 공유. 🔴 **이 라우트만 `truncated:true`+`next_cursor:null` 제3상태가 정상 존재**(라이브 병합이 재개 위치를 자름) — 행/셀 이력의 불변식을 그대로 옮기지 말 것. ⚠️ **그 두 헤더가 여전히 CORS `expose_headers`에 없다**(`server/main.py:164`, 총괄 보고) — 오늘은 클라가 헤더가 아니라 바디를 읽어 무해. 🔴 **`get_cell_history`의 이중 등록은 이 라운드에서 삭제됐다**(직전 라운드 총괄 보고 항목이었음, `server/main.py` 2296·3765 중 하나) | Backend / Ops |
| **버전 게이트 (`version_column` — 버전이 권위이고 도착 순서가 아니다)** | **`server/database/crud.py`**(2026-08-04 `092b83f` — `version_gate_verdict`(판정 단독 소유자) · `parse_version_key`(비교 가능성의 단독 소유자) · `_same_source_content_differs` · `log_version_gate_summary` · `REASON_*`/`NOTE_*` 어휘 · 호출 지점은 `apply_row_update_internal`(행 확정 직후) + 누산기는 `apply_batch_updates`), 선언은 `server/config/table_config.json`의 `version_column`, 회귀 그물 `server/tests/test_version_gated_overwrite.py` | **운영자 정본은 [guide/config/table_config §7](../guide/config/table_config.md)**(선언 절차 · 🔴 **§7.2 선언 전 확인** · 판정표 · **로그 문구** · 되돌리기) · 레이어링 관점은 [architecture/data_model §2.1-bis](../architecture/data_model.md) · 지도·함정은 [guide/CONFIG_GUIDE §5.1 / §6-P](../guide/CONFIG_GUIDE.md) · 점검은 [qa/FEATURE_CHECKLIST §1.3 / §2.5](../qa/FEATURE_CHECKLIST.md). 🔴 **게이트는 거부권이지 승급권이 아니다** — 통과한 행도 셀별 우선순위를 그대로 지나가므로 더 높은 버전이 사람의 교정을 밀지 못한다. 「버전이 권위」의 과대 해석(통과 시 페이로드 강제 적용)은 `test_human_correction_survives_a_newer_version`을 빨갛게 만들고, 그 테스트가 이 행의 상위 제약이다. 🔴 **파생 쓰기는 버전 컬럼을 안 들고 온다** — 체인 워커 · 체인 재적용 R1(`chain_replay`) · 결손 보정 자동 확정(`enrichment_candidates`)이 그렇고, `map_meta_registrar`(부재 행만)와 `enrichment_backfill`(새 신원만)은 영향 없다. **쓰기 주체를 추가하면 그 주체가 이 표의 어느 쪽인지 판정해 §7.2의 표를 함께 고쳐라** — 그 표가 운영자의 유일한 사전 확인 근거다. 🔴 **로그 문구를 바꾸면 §7.7이 먼저 거짓이 된다**(그 절은 실측한 줄을 통째로 인용한다 — 인시던트 중에 읽는 자리라 낡은 인용이 가장 비싸다). ⚠️ **선언한 테이블이 아직 하나도 없어 기능은 전부 무동작이다** — 그것이 의도된 출하 상태이고, 첫 선언이 생기면 이 행의 이 문장부터 거짓이 된다 | Backend / Ops |
| **표기 정규화 (`notation_rules.json` — 조회 시점 폴드. 파생 컬럼 **없음**)** | **`server/notation_norm.py`**(2026-08-04 재설계 — 파이썬 폴드 `fold_notation` · **SQL 폴드** `fold_notation_sql`/`fold_sql_text`(PostgreSQL은 `translate(regexp_replace(...,'g'),...)`, 그 외 다이얼렉트는 등록 스칼라 함수 `assy_fold_notation` = `install_sqlite_fold`) · 공유 상수 `SEPARATOR_CODEPOINTS`→`SEPARATOR_PATTERN`·`CASE_*_ALPHABET` · `validate_notation_rules`/`_validate_column`/`_normalize_rules`(거절 코드 5종) · `join_pair_rules`(**어느 한쪽이라도 선언되면 양쪽, 규칙은 합집합**) · `fold_preview`/`declared_previews`(병합군) · TTL 5.0s + `reset_cache`). 소비 지점은 `virtual_join_executor.join_onclause`(**ON 절의 유일한 철자** — `execute_rule`과 `resolved_expression`이 함께 쓴다), 게이트는 `virtual_join_config.unique_index_covering(..., folds=)` + `index_key_expression`/`required_index_ddl`(**함수 인덱스**) + 순수 매처 `normalize_index_expression`, 캐시 무효화 `main.reload_local_process_cache`, 라우트 `GET /admin/config/notation/preview`, 보고 `config_resolve_report._resolve_notation`/`notation_preview_detail`, 회귀 그물 `server/tests/test_notation_normalization.py`(30) + **계약 `contracts/notation_fold/`**(43벡터×4조합, 시임 `server/tests/test_notation_fold_contract.py`) | ✅ **[2026-08-04 해소] 운영자 문서 4종을 전부 다시 썼다**: [guide/config/notation_rules_config](../guide/config/notation_rules_config.md)(**전면 재작성** — §0에 옛/새 모델 대조표를 남겨 어제 판을 읽은 사람이 무엇이 뒤집혔는지 알게 했다) · [guide/CONFIG_GUIDE §1 표 + §5.6-quater](../guide/CONFIG_GUIDE.md) · [guide/config/table_config §1·§5](../guide/config/table_config.md)(진입점 **삭제** — 이 파일에서 할 일이 없어졌고 남은 접점은 「`"string"`이어야 정규화 가능」 하나) · [guide/config/README](../guide/config/README.md). 함께 [guide/config/virtual_join_rules §2-bis](../guide/config/virtual_join_rules.md)에 **인덱스 배제가 뒤집히는 조건**을, [qa/FEATURE_CHECKLIST §2.8-ter](../qa/FEATURE_CHECKLIST.md)에 점검 10건을 신설했다. 🔴 **부분 패치 금지는 그대로 유효하다** — 다음에 이 모델이 또 움직이면 네 문서를 **같은 라운드에** 고쳐라(하나만 고치면 나머지 셋이 더 그럴듯해진다). 🔴 **저장하는 것이 하나도 없다** — 파생 컬럼도, 쓰기 훅도, 재파생 스크립트도, 쓰기 거부도 없다. 규칙이 틀려도 되돌릴 것이 없는 이유가 그것이다. 🔴 **켜는 것은 한 층이다**(이 파일 한 줄). `table_config`에는 아무것도 더하지 않는다. 🔴 **한쪽만 접는 코드 경로가 없다** — 실측 `dt_log.core_lot` 병합군 15개 / `core_wafer_map.core_lot` 0개라 깨끗한 쪽에 선언할 운영자가 없고, 한쪽만 접힌 조인은 **이미 맞던 매치를 조용히 잃는다**. 🔴 **파이썬 폴드와 SQL 폴드는 계약으로 채점된다** — `\s`/`[[:space:]]`와 `upper()`는 두 엔진이 다른 답을 내므로(실측) 문자류는 **열거**, 케이스는 **ASCII 전용 `translate`**다. 되돌리지 말 것. 🔴 **접히는 조인 키는 함수 인덱스를 요구한다** — 평범한 UNIQUE 인덱스는 접힌 키의 유일성을 말하지 않는다('CL-1'과 'CL_1'은 접히면 한 값). ⚠️ **비싸다**(실측 3.06 us/행 = 1,000만 행 약 31초). ⚠️ **맵 키는 건드리지 않는다**(`wafer_map_metadata`가 원본 신원으로 등록돼 있다 — 설정 스위치가 아니라 데이터 마이그레이션). ⚠️ **실파일은 아직 없다**(`.sample`만, `"columns": {}`) | Backend / Ops |
| 파일 인제션 파이프라인 | `parsers/directory_watcher.py`, `parsers/pipeline_base.py`, **`parsers/advanced_ingester.py`**(2026-07-30 `600b49d`+`a5eb934` — 선언 규칙 3가족 `rules`/`header_rules`/**`filename_rules`** + 병합 우선순위. 선언 파일은 `server/config/*`가 **아니라** `ingestion_workspace/<table>/config/*.json`이므로 [guide/config/](../guide/config/README.md) 소관 밖이다) | [guide/INGESTION_GUIDE](../guide/INGESTION_GUIDE.md) — §1.9 제자리 적재 · **§1.9-bis `filename_rules` 선언 규격이 정본**. 🔴 **명명 상태(`no_match`/`ambiguous_reference`/`cast_failed`/`path_value_discarded`)나 병합 우선순위(`filename < header < row` — 사용자 판정)를 바꾸면 필수 갱신.** 우선순위는 **판정이지 dict 순서의 부산물이 아니므로** 새 판정 없이 뒤집지 말 것 | Ingester |
| 체인 인제션 | `chain_ingestion_worker.py`(**+ 2026-08-11 `_undeclared_drop_note`/`_DROP_NOTE_TOP_N` — `crud.undeclared_column_drops()` 다이제스트를 *기존* `heartbeat.beat("chain", note=...)`에 실어 `/health`가 읽게 한다. 🔴 두 번째 채널을 만들지 말 것 · 건강하면 `note=None`이라 깨끗한 배포의 박동 파일은 바이트 동일**), `mappers/` | [guide/chain_ingestion_guide](../guide/chain_ingestion_guide.md) · 드롭 계수기·박동 note는 [architecture/backend §1.3 / §3 항목 4](../architecture/backend.md) · 재사용 관점은 [architecture/PRIMITIVES §1「침묵을 고칠 때 warn-once는 *고쳐졌는지*를 못 답한다」](../architecture/PRIMITIVES.md) | Ingester |
| **DT/Core 프레임 파생 체인** (2026-08-11 신설 행 — **고아 해소**) | `server/dt_frame_transform.py`(프레임 방정식의 단독 소유자 — 6스칼라 × 좌표 공간 둘), `server/alignment_view_service.py`(정렬 뷰 페이로드를 체인이 부르는 자리 — 라우트와 **같은 계약**), 매퍼 넷 `server/mappers/{dt_alignment_metadata,dt_inventory_metadata,dt_standard_map,core_alignment,core_usage}_mapper.py`(+`.sample`), 선언 `server/config/chain_rules.json`, 시드 `server/scripts/seed_syn_core_defect_jobs.py`·`seed_syn_dt_alignment_samples.py`, 프로브 `server/scripts/probe_core_occupancy_alignment.py` | [architecture/DT_CORE_FRAME_CHAINS](../architecture/DT_CORE_FRAME_CHAINS.md)(**구조·권위표·불변식 = 정본**) · [guide/DT_CORE_FRAME_CHAINS_GUIDE](../guide/DT_CORE_FRAME_CHAINS_GUIDE.md)(운영 설정 순서) · [spec/DT_ALIGNMENT_METADATA_CHAIN_SPEC](../spec/DT_ALIGNMENT_METADATA_CHAIN_SPEC.md)(메타데이터 홉 계약) · 정렬기 자체는 [spec/MAP_ALIGNMENT_SPEC](../spec/MAP_ALIGNMENT_SPEC.md).<br>🔴 **`dt_log` 한 잡이 좌표 공간 *둘*을 든다** — DT(`b_wx`/`b_wy`)와 그 소스 core(`c_wx`/`c_wy`). 각각 **한 번 확정해 `dt_inventory`에 적고** 표준 좌표 맵(`dt_map`·`core_usage_map`)을 파생한다. 🔴 **core 정렬은 그 행의 DT 맵 메타를 무시하고 원본을 읽어야 한다** — DT 메타는 DT 프레임의 서술이지 core 프레임에 대한 증거가 아니다. 🔴 **파생 맵을 원본처럼 고치지 마라** — 프레임이나 원본이 바뀌면 **소유 체인을 다시 돌린다**(둘 다 `replace_map`이라 재실행이 스코프를 교체하지 사본을 만들지 않는다). 🔴 **기준 부재·`core_wafer` 미해결은 정상적인 무출력**이고 프레임을 지어낼 근거가 아니다. 🔴 **`core_usage_map`의 신원은 보정된 `core_wafer` 하나뿐** — lot/slot을 더하면 물리 웨이퍼 한 장이 맵 둘로 갈린다. ⚠️ **문턱을 낮춰 승자를 만들지 마라**(모호를 숨긴다 — 기준·좌표·선언을 고칠 것).<br>⚠️ **§2.5의 dt→core 채점 축(`index_group_count`·`bin_fingerprint_shift`)을 물린 것이 아니다** — HEAD 실측 호출자 **0건**. 이 체인이 부르는 것은 `map_alignment.confirmed_meta_for`다. **둘을 한 문장에 접지 마라.**<br>⚠️ **이 셋은 2026-08-11까지 이 표와 [docs/README](../README.md) 양쪽에 행이 없는 고아였다** — MAP_ALIGNMENT_SPEC(2026-08-05)·TRACE_FIXTURE_SPEC(2026-08-04)에 이은 **세 번째 같은 실패**다. 🔴 **문서를 만든 커밋이 등재까지 하지 않으면 그 문서는 다음 감사까지 안 보인다.** | Lead / Backend |
| **Chain Replay (R1 재적용 / R2 stale 소스 철회 / R3 표시값 재계산)** | **`server/chain_replay.py`**(2026-07-30 — 룰 재적용 + 셀 레이어 단위 철회 · **2026-08-11 `recompute_display_values` + `R3_AUDIT_SOURCE="resolution_recompute"`**), **`server/keyset_scan.py`**(공용 키셋 페이지 순회 — `backfill_enrichment`·`enrichment_analysis`·R1·R3가 공유), `scripts/chain_replay_cli.py`(서브커맨드 `replay`/`replay-all`/`withdraw`/**`resolve`**/`list`), `config/chain_rules.json` | [guide/chain_ingestion_guide §5](../guide/chain_ingestion_guide.md)(**정본** — 루프 가드 3겹·재적용 순서·사람 값 보호·철회 가시화 · **§5.5 R3**) · 레이어링 계약은 [architecture/data_model](../architecture/data_model.md)(**§2.2-ter가 R3**) · **운영자 진입점은 [guide/BACKFILL_GUIDE §2](../guide/BACKFILL_GUIDE.md)**. 🔴 **세 연산을 가르는 축은 「저장된 것을 바꾸는가」다** — R1은 매퍼를 다시 돌려 **새 층을 쓰고**, R2는 **한 층을 지우고**, R3는 **아무것도 안 바꾸고 승자만 다시 계산한다**. 넷째를 만들기 전에 이 축의 어디에 앉는지 먼저 답할 것. 🔴 **R3의 안전성 전부가 「층 2개 미만은 안 건드린다」 관문 하나다**(0개면 `(None, None)`으로 해결돼 **다른 쓰기 경로가 소유한 컬럼을 비운다**) — 최적화로 오해해 걷어내지 말 것. ⚠️ **R3만 어드민 소급 표면에 없다**(`server/retroactive.py`의 `OPERATIONS` 5종에 미등재, 2026-08-11 실측) — 등재하면 `BACKFILL_GUIDE §0·§7` + 이 행을 함께 고친다 | Backend / Ingester |
| **소급(backfill) 운영 경로 — 진입점 목록 + 어드민 표면** | **CLI 4스크립트 · 연산 여섯**: `scripts/chain_replay_cli.py`(ⓐ `replay`/`replay-all` = R1 · ⓑ `withdraw` = R2 · **ⓕ `resolve` = R3**(2026-08-11 — 저장된 층을 안 바꾸고 표시값만 재계산) · `list`) · `scripts/backfill_enrichment.py`(ⓒ — **2026-07-31 `9c6a1c9`부터 143줄 CLI 껍질이고 알맹이는 `server/enrichment_backfill.py`**) · `scripts/enrichment_insights.py`(ⓓ `confirm` — 나머지 `classify`/`propose`는 읽기 전용 조사) · **`scripts/graph_orphan_sweep.py`(ⓔ — 유일하게 *지우는* 경로. `server/graph_orphans.py`가 로직 소유자이고 스케줄러도 같은 모듈을 돈다)**. 의미론 소유자는 `server/chain_replay.py`·`server/enrichment_backfill.py`·`server/enrichment_analysis.py`·`server/enrichment_candidates.py`·`server/graph_orphans.py`, 공용 페이지 순회는 `server/keyset_scan.py`. **어드민 표면(2026-07-31 `fbc1053`)**: 등록부 **`server/retroactive.py`**(`OPERATIONS`·`inventory`·`validate`·`count`·`publish`·`execute` — **순수 디스패치이고 새 연산을 구현하지 않는다**), 라우트 `main.py`의 `/admin/retroactive/{operations,{op}/count,{op}/run}`, 실행은 `run_auto_update.start_retroactive_run`(**전용 스레드 · 동시 1건**), 이벤트 상수 `event_constants.EVENT_RETROACTIVE_RUN`(`CONTROL_EVENT_TYPES` — 체인 워커가 **이름이 아니라 그 집합**을 본다), 인덱스 `models.CellSource.idx_sources_by_source` + `scripts/setup_db_performance.py` Step 3.10/3.11 | [guide/BACKFILL_GUIDE](../guide/BACKFILL_GUIDE.md)(2026-07-31 신설 — **운영자 정본**: 증상→도구 결정표 · 공통 규율 · 도구별 함정). 각 경로의 **개발자 계약은 소유 행이 정본**이다(ⓐⓑ**ⓕ** = chain_ingestion_guide §5 · ⓒⓓ = ENRICHMENT_QUEUE_SPEC + config/enrichment_rules §7 · ⓔ = AUTO_UPDATE_GUIDE §4-ter) — **여기에 사본을 만들지 말고 링크하라**. 🔴 **2026-08-11: 제목·§0·§1·§7의 「다섯」을 여섯으로 고치지 않고 *기수를 지웠다*** — 목록 옆의 수는 목록의 두 번째 사본이고 이 문서에서 그 수는 **네 자리**에 있었다. **ⓕ만 어드민 표면에 없다**(`retroactive.OPERATIONS`는 5종 — 등재하면 `§0·§7` + 이 행을 함께 고친다). 🔴 **플래그를 추가·개명·삭제하면 이 가이드가 먼저 거짓이 된다** — 존재하지 않는 플래그를 찍는 가이드는 없느니만 못하다(운영자가 도구가 고장 났다고 결론짓는다). 인용한 명령은 **argparse 소스 + `--help` 실행**으로 재확인하고 고칠 것. 🔴 **「기본 dry-run · `--apply`만 쓴다 · 진짜 매퍼/쓰기 경로 · 페이지 단위 커밋」 4항 중 마지막은 ⓔ에 거짓이다**(`graph_orphans.apply_sweep`은 청크 삭제 후 **끝에 1회 커밋**) — 공통 규율을 넓힐 때 다섯 전부를 실측하라. ✅ **어드민 API 착지 `fbc1053`**(§7 재작성) — ⚠️ **화면(버튼)은 `77d27d3` 기준 아직 없다**(그리는 클라 코드가 커밋 트리에 없다 — **화면 작업은 진행 중**). 「어드민에 자리가 없다」를 **통째로 걷지 말 것**: 착지한 것은 라우트뿐이고 점검은 `curl`이다. 🔴 **화면이 착지하면 `BACKFILL_GUIDE §7 서두·§7.4·§0` + `FEATURE_CHECKLIST §1.8 행·§2.8-quinquies 서두` + `docs/README`의 BACKFILL 행을 함께 고친다** — 목록은 이 행에만 둔다. 🔴 **`server/scripts/`는 한 방향 문이다** — 런타임 코드가 그쪽을 import하면 **운영에서만** `ModuleNotFoundError`가 나고, 스위트는 초록이다(테스트 파일 하나가 자기 용도로 `sys.path`에 넣는다). 더 나쁜 것은 **증상의 모양**이다: 트리거는 검증만 하고 아웃박스에 쓰므로 **200 `queued`를 돌려준 뒤** 스케줄러 스레드에서 죽는다 — 초록 버튼·쓰인 행 0·표면에 에러 없음. 새 소급 로직은 **`server/`에 두고 `scripts/`는 argparse만** 갖는다(판정기 `server/tests/prod_import_check.py`, 진입 `test_prod_import_env.py`). 🔴 **인덱스 `idx_sources_by_source`는 두 곳에 선언돼 있고 둘 다 고쳐야 한다**(`models.py` + `setup_db_performance.py` Step 3.10 — `create_all`은 기존 테이블에 인덱스를 추가하지 않는다) | Backend / Ops |
| Auto-Update 스케줄러 | `run_auto_update.py` | [guide/AUTO_UPDATE_GUIDE](../guide/AUTO_UPDATE_GUIDE.md) | Ingester |
| 웨이퍼 맵 에디터 | `client2/src/map_editor.js` + **`client2/src/map_key.js`**(2026-08-04 R1 `689ebb9` — 맵 키 정준형 `canonicalKeyValue`/`composeMapId`/`decomposeMapKey`/`canonicalMapKey`/`getMapIdFromMeta`. 🔴 **서버 `map_overlay.py`와 같은 답을 내야 하는 이음새**이고 양측 채점은 `contracts/map_seam/` + `client2/tests/seam_7b_oracle.py`다) + **`client2/src/split_registry_row.js`**(위 DOE 행이 정본), `utils/physical_wafer_engine.py`, `utils/coordinate_transformer.py`. 🔴 **분할이 진행 중이라 이 목록은 라운드마다 길어진다** — 「맵 에디터 = 파일 하나」로 읽지 말고 심볼의 거처는 [CODE_MAP](../architecture/CODE_MAP.md)을 **grep**해서 확인하라. 🔴 **경계는 「순수한가」로 긋는다**: 모듈 상태를 안 건드리는 덩어리만 나가고, 상태를 쓰는 오케스트레이션은 **영구히** 남는다(미룬 것이 아니다). 🔴 **심볼을 옮기면 하네스가 인질이다** — `client2/tests/*`와 `contracts/*/client_harness.mjs`가 **소스 텍스트를 잘라 vm에서 돌리므로** 경로 사전을 함께 옮기고 `export ` 키워드는 슬라이스에서 **빼야** 한다(vm 안의 `export`는 SyntaxError). 인질은 심볼 이름이 아니라 **파일 경로**로 grep해서 세라 | [map_editor/](../map_editor/README.md), [spec/MAP_EDITOR_SPEC §1~§4](../spec/MAP_EDITOR_SPEC.md)(**§1-bis-2가 파일 경계표의 정본**), **[spec/MAP_EDITOR_SPEC §7](../spec/MAP_EDITOR_SPEC.md)**(2026-08-06 신설 — **조작 가능성 요구사항**, ⏸️ **수리 미착수**. 🔴 **고장과 사용성 두 부류를 한 호흡에 고치지 마라** — 완료 판정의 기준이 다르다. §7.5는 **이 스펙이 스스로와 어긋나는 자리 다섯**이고 **총괄 판정 대기**다) ← 그 근거의 정본은 **[process/EDITOR_USABILITY_FINDINGS](./EDITOR_USABILITY_FINDINGS.md)**(`372c538` 브라우저 실주행 — **증거 기록이라 삭제 금지**. 스펙은 수를 다시 적지 않고 이 문서를 가리킨다) | UI/Map |
| **유효 다이 맵(M4) — 원이 아닌 유효 영역** (🔴 **F6의 프레임 채택 기계장치는 2026-07-30 폐기** · 🔴 **`🎯 APPLY`/`💾 SAVE` 두 버튼은 2026-08-04 `5b15c24`에 삭제 — 하루만 살았다**) | **[2026-08-04 `5b15c24`] 저작 표면이 다시 바뀌었다**: 키 컨트롤은 진짜 `<select>`(`renderValidDieKeyControl`)이고 **`change`가 곧 적용**(`onValidDieRefChanged`), 목록이 잘렸거나·읽지 못했거나·지금 키가 목록에 없거나·항목이 0개면 **텍스트 입력으로 폴백해 `keydown` Enter만** 받는다(`input`은 다시 그릴 뿐이고 `blur`/`change` 리스너는 **없다**). 기록은 `⚡ Push` 또는 신설 **`saveMapSpecOnly`**(`📐 규격만 저장` — `grid_metadata` 한 필드, **셀 0건**, 신원은 **화면 컨트롤**이고 `loadedIdentity`가 아니며 **없는 등록을 만든다**, `MAP_SPEC_SAVE_TIMEOUT_MS` 15s + `AbortController`). 미저장 경고는 `unsavedWorkNotice()` 하나가 만들고 **문이 둘**(뒤로 가기 · **다른 맵 로드** `9e41995`). 🔴 **`btn-valid-die-apply`/`btn-valid-die-save`를 이 행에서 찾지 마라 — 0건이다.** 🔴 **문구를 바꾸면 사용자 가이드가 먼저 낡는다**(§4가 하루 만에 두 번 다시 쓰였다) · `client2/src/map_editor.js`(`resolveValidDie`/`parseValidDieRef`/`validDieChainError`/`frameDimBounds`/`frameDimError`/`projectCellsToPhys`/`isValidDieAt`, **원점 상자 `getWaferBoundingBox`**, **재배치 반응 `reseatCellsToStoredCoords` + 기록 `cellsSeatedUnder`/`seatingSnapshot`**), `server/map_overlay.py`(`parse_valid_die_ref`/`resolve_valid_die_basis`/`resolve_valid_die_set`, **2026-08-04 읽기 고정**: 상수 `VALID_DIE_TABLE` · 저장 바이트 판독 `valid_die_ref_display` · 거절문 `valid_die_redirect_note`). 🔴 **읽기 고정과 쓰기 보존은 다른 두 함수의 이야기다** — 조회 대상은 언제나 `valid_die_ref`이고(`parse_valid_die_ref`), 저장 바이트는 손대지 않는다. 저작 계약의 `expect_table`을 **고정된 파서로** 되읽으면 「쓰기가 선언 테이블을 보존한다」가 반증 불가능해지므로 그 축은 `valid_die_ref_display`로 채점한다(양측 동일 — 클라 `validDieRefDisplay`). ⚠️ `adoptFrameSpec`·`storedCoordRepositionPlan`·`applyStoredCoordReposition`·`repositionRefusalReason`·`adoptionCoordinateCost`·`adoptedFrameOf`·`dbCoordsByPhysKey`·`announceFrameAdoption`은 `61440e6`+`94b9baa`에서 **삭제됐습니다**(`client2/src/`에 0건) — 이 행에서 찾지 마십시오. ⚠️ **2026-07-31 개명**(`35e84c3`): `getVisualCoords`→`getDbCoords` · `getPhysicalCoords`→**`getDieIndex`**(mm가 아니라 칸 번호) · `getCellFrom*`→`getCanvasCellFrom*` · `xv`/`yv`→`dbX`/`dbY`. 🔴 **[2026-08-04 정정] 「`mm`은 일부러 비어 있습니다」는 삭제됐습니다** — `cd3e0f4`가 클라에 웨이퍼 mm 공간을 만들었고(`dieIndexToWaferMm`/`waferMmToDieCell`/`projectCellsToWaferMm`/`seatWaferMmInFrame`, 정본은 [MAP_EDITOR_SPEC §1-bis](../spec/MAP_EDITOR_SPEC.md)) 그 문장은 나흘간 최소 7파일에서 거짓이었습니다. ⚠️ **저장 좌표가 칸수라는 것은 안 바뀌었습니다** — mm 공간이 생긴 것과 저장 좌표의 뜻은 다른 얘기이고, 섞으면 없는 결함이 만들어집니다 | **사용자 안내는 [guide/VALID_DIE_MAP_GUIDE](../guide/VALID_DIE_MAP_GUIDE.md)**(운영자가 실제로 만나는 동작 — **좌표는 안 움직이고 칸이 좌표를 따라간다**, 알람 축은 치수가 아니라 **원점**, 거절은 치수 정의역 하나뿐) · 개발자 계약은 [spec/MAP_EDITOR_SPEC §5.7/§5.7-bis/§5.7-ter](../spec/MAP_EDITOR_SPEC.md) · 점검은 [qa/FEATURE_CHECKLIST §1.7 + §2.9](../qa/FEATURE_CHECKLIST.md) · 재사용 관점은 [architecture/PRIMITIVES §4](../architecture/PRIMITIVES.md) · 회귀 그물 `client2/tests/geometry_origin_reseat_harness.mjs`(**DOM 이벤트에서 출발** — 함수를 직접 부르는 픽스처는 배선이 끊겨도 초록이었다, `5441706`) + `valid_die_frame_adoption_harness.mjs`(**파일명은 역사적**이고 지금은 부채 목록에 있다) + 라이브 프로브 `reposition_regime_probe.mjs` · 양측 채점 `contracts/map_seam/`. 🔴 **판정이나 알림 문구를 바꾸면 사용자 가이드가 먼저 낡습니다** — 스펙만 고치고 넘어가면 운영자가 읽는 문서가 거짓이 됩니다. **§4-bis는 이틀 새 네 번 거짓이 됐고, 그중 두 번은 제품에 없는 토스트·거절 문구를 인용한 코드 블록을 남겼습니다** — 문구를 인용한 문서는 문구가 바뀔 때 **반드시** 함께 고칩니다. 🔴 **2026-07-31 판정 반전**: 「지정은 어떤 축도 채택하지 않는다」는 **두 축에서 거짓**이 됐습니다 — 사용자 기준 가치 1)에 따라 **물리 규격을 갈아끼우고 격자 치수를 그 규격에서 파생**시킵니다(참조가 **선언한** 수를 베끼는 폐기 동작과 다릅니다). 그리고 **선언이 없는 맵의 기하 편집도 같은 반응을 탑니다**(선언 없음 = 유효 영역이 곧 원) | UI-Map / 총괄 |
| 파일 인제션 체크포인트·dedup(P2) | `server/ingestion_checkpoint.py`, `models.FileIngestionCheckpoint`, `config/ingestion_settings.json` | [guide/INGESTION_GUIDE §1.8](../guide/INGESTION_GUIDE.md) | Ingester |
| 실시간 동기화(WS) | `client2/src/websocket.js`(2026-08-04 — **연결 감시견** `armConnectWatchdog`/`abandonConnectingSocket` + 배지 어휘 3종), `client2/src/main.js`(**`init()`의 첫 문장이 `initWebSocket()`** — `2b90009`+`4132704`), `client2/src/api.js`(`tablesLoadInFlight` 걸쇠), `client2/src/config.js`(`WS_CONNECT_TIMEOUT_MS` 8000 · `WS_CONNECT_STALE_MS` 1000 · `WS_RECONNECT_CEILING_MS` 5000), `main.py` ConnectionManager | [architecture/frontend §3.1 · **§3.5 · §3.5-bis**](../architecture/frontend.md) — **유일한 정본**. 점검은 [qa/FEATURE_CHECKLIST §2.13](../qa/FEATURE_CHECKLIST.md), 재사용 관점은 [architecture/PRIMITIVES §7](../architecture/PRIMITIVES.md). 🔴 **소켓은 「좀 더 앞」이 아니라 전부보다 앞이다** — 재연결 사다리 전체가 `initWebSocket` 안에 살아서, 그 줄에 닿지 못한 페이지는 세션 내내 소켓도 재시도도 없다. 🔴 **응답이 오지 않는 경로는 `onopen`도 `onclose`도 부르지 않는다** — 그래서 상한(8s)이 필요하고, 버리기 전에 **핸들러를 먼저 `null`로** 만들어야 정리가 `onclose`로 재진입하지 않는다. 🔴 **배지 문자열을 합치지 마라** — 「소켓을 만든 적 없음(= `index.html`의 초기값이 그대로)」·「협상 중」·「경로가 삼킴」 셋이 각자 다른 글자여야 하고, 특히 **첫째가 §3.5 결함의 유일한 화면 신호**다. ⚠️ 무결성 가드의 *문제* 서술은 2026-07-27에 §3.1로 이관 완료됐고, `spec/DATA_SYNC_SPEC`은 **2026-08-06에 `_archive/`로 이관됐다**(종전 「아카이브 대기」 — 조건이던 「SSOT 링크 정리」는 **이미 충족돼 있었고 아무도 다시 재지 않았다**. 🔴 **조건부 대기는 조건을 다시 재는 사람이 없으면 영구 대기가 된다**) | Sync |
| **캔버스 축척 · 합성 규격 표지(`auto_registered`) · 프레임 선택 표지(`frame_chosen_from`)** | **`client2/src/map_editor.js`**(2026-08-05 `b9a0ab1` — **`frame_chosen_from`**: 쓰기 `markFrameChosen`, 읽기 `frameChosenFrom`, 왕복 `buildPushGridMetadata`, 거울 상수 `client2/src/map2/declaration.js`의 `FRAME_CHOSEN_KEY`/`FRAME_CHOSEN_FROM`. 🔴 **토큰이 아니라 키다** — provenance 토큰들은 「축의 값이 어떤 증거인가」에 답하고 이것은 「행에 무슨 일이 있었나」다(⚠️ **[2026-08-06] 「여섯」을 지웠습니다 — `confirmed`가 들어와 거짓이 됐고, 목록의 정본은 `client2/src/map2/declaration.js`의 `DECLARATION_TOKENS`입니다**). 선례는 `phys_assumed_from`. 🔴 **양방향이다** — 표지 없는 메타를 불러올 때 지우지 않으면 진짜 선언이 「고른 것」으로 읽힌다. ⚠️ **서버 쌍둥이 없음**(읽는 서버 코드 0건, 보드 항목). 같은 커밋: **빈 프레임 칸은 0으로 지어내지 않고 이름을 대며 거절**(`readGridFrameControls`의 `silent` — `⚡ Push`·`📐 규격만 저장`·⚙️ 분기 셋이 같은 독법을 쓴다) · 2026-08-04 `102cdea`+`edc7ef6` — **`cellMetrics()`가 축척의 단독 생산자**이고 렌더·마우스 매핑 둘 다 그것을 부른다. `s = min(sGrid, sWafer)` · 정박은 **선언된 `waferDia`**(`effectiveRadius` 아님) · 마커는 축별 `markerAxisRadius`. `cfg`… `cfc09de` — 표지 읽기 `geometryIsAutoRegistered`/`physDeclaration`, 쓰기 `markGeometryAutoRegistered`, 왕복 `buildPushGridMetadata`), **`server/map_overlay.py`**(`AUTO_REGISTERED_KEY`·`GEOMETRY_*` 어휘 3종·`_GEOMETRY_REFUSAL_TEXT`·`geometry_declaration`/`geometry_refusal` → `make_frame_transform`의 거절, `cd37e2c`), **`server/map_meta_registrar.synthesize_grid_meta`**(합성값을 쓰는 쪽 ①) | 계약 정본 [spec/MAP_EDITOR_SPEC §1-ter / §1-ter.1](../spec/MAP_EDITOR_SPEC.md) · 클라 구조는 [architecture/frontend §4](../architecture/frontend.md) · 점검은 [qa/FEATURE_CHECKLIST §2.9](../qa/FEATURE_CHECKLIST.md) · 재사용 관점은 [architecture/PRIMITIVES §4·§7](../architecture/PRIMITIVES.md). 🔴 **판정은 값이 아니라 표지다** — `chip === 1`을 보면 진짜 1mm 다이를 언젠가 삼킨다. 레거시 폴백은 **세어 보고** 넣지 않았다(668행 중 320행, 표지 없는 `1×1` **0건**). 🔴 **합성 규격 위에서 정렬하지 말고 이름을 대며 거절한다** — 종전에는 1mm 피치로 「멀쩡해 보이는 좌표」를 냈다. 🔴 **사유에 열거값을 흘리지 마라**(`소스 auto_registeredxauto_registered`가 실제로 사용자에게 나갔다 — `7ea2c2f`). 🔴 **`min(sGrid, sWafer)`의 `min`을 빼지 마라** — 캔버스를 넘친 칸은 `gridCells2D` 등록 전에 걸러져 **저장 페이로드에서 조용히 사라진다**. ⚠️ **문구와 코드가 아직 어긋난 자리 하나**: 안내는 *「웨이퍼 원을 그리지 않습니다」*인데 원을 그리는 블록은 `isotropic`/`waferAnchored`로 가려져 있지 않고 **결과적으로 캔버스 밖으로 나가 안 보일 뿐**이다(총괄 판단 대기) | UI-Map |
| **런처 인자 표면** | **`server/launcher_args.py`**(2026-08-04 `63b17f7` — `FLAGS` 튜플이 **파싱·오타 제안·`--help`의 단일 출처**, `parse_launcher_args`/`suggest_flag`(difflib cutoff 0.6)/`help_lines`, `EXIT_BAD_ARGUMENT = 2`), 호출은 `run_decoupled_app.main()`의 **첫 문장**, 회귀 그물 `server/tests/test_launcher_arguments.py` | [architecture/backend §1.3의 ①-bis](../architecture/backend.md) · 점검은 [qa/FEATURE_CHECKLIST §2.15](../qa/FEATURE_CHECKLIST.md) · 재사용 관점은 [architecture/PRIMITIVES §6](../architecture/PRIMITIVES.md). 🔴 **플래그를 추가·변경하면 `FLAGS`만 고쳐라** — 셋이 어긋날 자리가 없는 것이 이 모듈의 존재 이유다. 🔴 **기존 철자는 절대 바꾸지 마라**(운영자 근육기억). 🔴 **거절이 `--help`보다 앞이다.** 🔴 **거절 배너는 cp949 안전**(`―` U+2015만) — 안 그러면 거절이 무동작과 같아진다. ⚠️ **[2026-08-04 정정] `FEATURE_CHECKLIST §2.15`가 「`--help`가 없고 오타는 조용히 무시된다」고 점검자에게 지시하고 있었다** — 고쳐진 결함을 통과시키라는 지시였고 이번에 삭제했다 | Backend / Ops |
| 배치 업서트 | `crud.apply_batch_updates` | [spec/batch_update_technical_specification](../spec/batch_update_technical_specification.md) | Backend |
| 실패 관리/재시도 | `FileIngestionLog`, outbox retry, `admin/*` | [spec/FAILURE_MANAGEMENT_SPEC](../spec/FAILURE_MANAGEMENT_SPEC.md) | Integrity/QA |
| 온톨로지 그래프(materializer·엣지 스토어) | `graph_sync_worker.py`, `graph_materializer.py`, `ontology_config.py`, `config/ontology_mapping.json` | [architecture/event_driven_backend §4](../architecture/event_driven_backend.md)(승격 흐름), [spec/ONTOLOGY_GRAPH_SPEC](../spec/ONTOLOGY_GRAPH_SPEC.md)(트랙 스펙 — Owner 총괄) | Sync / 총괄 |
| 그래프 뷰어·추적 리포트 | `main.py /graph/*`, `client2/src/graph_viewer.js`, `trace.js`/`trace_core.js`/`trace_launch.js` | [architecture/frontend §6](../architecture/frontend.md) | UI / Sync |
| **추적 시뮬레이션 픽스처(합성 데이터 규격)** | **아직 코드가 없다** — 데이터를 만드는 스크립트는 이 폼이 채워진 뒤 `server/scripts/`에 생긴다. 소비자는 위 두 행(그래프 머티리얼라이저 · 추적 리포트) | [spec/TRACE_FIXTURE_SPEC](../spec/TRACE_FIXTURE_SPEC.md) — 🟡 **작성 중이고 채우는 주체가 사용자다**(총괄 검수 → server-pm 생성). 2026-08-02~08-04까지 **소유 행 없이** 굴러간 634줄짜리 고아였고, 2026-08-04에 이 행이 생겼다. 🔴 **이 문서의 존재 이유는 온톨로지 순환의 ④(발견 → 다시 보강)를 한 바퀴 완주시키는 것**이고, 그래서 **불완전성(§6)과 정답(§7)이 데이터만큼 필수다** — 완벽한 데이터는 교정할 것이 없어 이 시스템의 명제를 증명하지 못하고, 정답 없이 만든 trace 결과는 채점할 방법이 없다. 🔴 **폼이 채워져 스크립트가 생기면 그 코드 경로를 이 행의 코드 열에 등재하고 Status를 🟢로 올리는 것까지가 한 작업이다** — 지금 이 행은 「아직 데이터가 없다」는 사실의 소유자다 | 사용자 / 총괄 · 생성은 Backend |
| Enrichment Queue | `enrichment_config.py`, `enrichment_mapper.py`, **`enrichment_candidates.py`**(2026-07-30 ① 후보 1개 자동 확정 — 노브 `auto_confirm`·선언 `candidate_for`), **`enrichment_analysis.py`**(② 룰 승격 제안 · ④ 결손 원인 분류 · ① 소급 sweep), `scripts/enrichment_insights.py`, `scripts/backfill_enrichment.py`. 🔴 **[2026-08-11] `client2/src/enrichment.js`는 죽었다** — `enrichment.html`이 배지·nav(`5116f67`)에 이어 파일과 vite 진입점까지(`ab36fab`) 삭제되면서, 컨베이어(결손 target 순차 입력) 워크플로는 **대체 없이 소멸**했다(커밋 스스로 그렇게 적는다). **조회 절반만** `client2/src/enrichment_reference_view.js`로 이식 — 메인 그리드 History 패널의 사이드바 탭(Cell/Row History 옆), 같은 백엔드 라우트(`GET /enrichment/rules/{r}/references/{i}`)를 재사용한다 | [spec/ENRICHMENT_QUEUE_SPEC](../spec/ENRICHMENT_QUEUE_SPEC.md)(§5.2 ① · **§5.2-bis 선언의 효과 노출** · §5.3 ② · §5.4 ④ — 🔴 **v1 UI 서술은 낡았다, 2026-08-11 정정 필요**) · **설정 절차는 [guide/config/enrichment_rules §7](../guide/config/enrichment_rules.md)**(**§7.2-bis** 후보 판정은 뷰 `limit`에 안 잘린다 · **§7.3-bis** 어드민에서 확인) · 체인 훅은 [guide/chain_ingestion_guide §4.4](../guide/chain_ingestion_guide.md) · 사이드바 참조뷰는 [architecture/frontend](../architecture/frontend.md) 모듈 표 | 총괄 |
| **Virtual Join 선언 + 팬아웃 가드 + 실행** | **`server/virtual_join_config.py`**(2026-07-31 신설 — 선언 로더·검증기·유일성 가드), **`server/virtual_join_executor.py`**(2026-07-31 `d70a33d` 신설 — **실행 정본**. `rules_for`/`execute_rule`/`_resolve_one`/`attach`/`virtual_only_columns`/`reset_cache`. 입구는 `load_verified_rules` **하나**이고 그것이 가드를 우회할 경로가 없다는 뜻이다), **읽기 경로 `main.fetch_and_merge_metadata`**(행 페이로드의 **유일한 직렬화 지점** — 그리드 페이지·단건 조회·배치 응답·WS가 전부 여기를 지나므로 조인 컬럼이 한 곳에만 붙는 일이 없다), **쓰기 거부 `crud.refuse_virtual_join_columns`**(`crud.apply_batch_updates`의 **첫 문장**), 캐시 무효화 `main.reload_local_process_cache`, `server/config/virtual_join_rules.json`(사용자 · `.sample` 有), `config_resolve_report._resolve_virtual_join`, `main.py`의 **`GET /admin/config/virtual-join/verify`**, **알림 `virtual_join_executor.announced_columns` + `main.get_table_schema`의 `virtual_columns` 키**(2026-07-31 `9200f20` — 🔴 **`columns`에 합치지 않는다**. 최종 목록과의 중복 제거는 **라우트가** 한다: 시스템 꼬리는 config 밖이라 실행기의 `collide` 판정이 못 본다), **클라 렌더 `client2/src/grid.js`의 `buildColumnDefs` 말미(append) + 술어 `client2/src/state.js`의 `isVirtualColumn`**(2026-07-31 `4b50135` — 쓰기 가드 4곳: `clipboard.js`의 붙여넣기 2분기·delete 비우기 + `ui.applyValueToSelectedRange`. **복사 술어 2곳은 반대로 받아들인다**), 테스트 `server/tests/test_virtual_join_guard.py` + **`server/tests/test_virtual_join_executor.py`** + **`server/tests/test_schema_virtual_columns.py`** + 하네스 **`client2/tests/virtual_column_render_harness.mjs`** | **설정 절차·실측 근거는 [guide/config/virtual_join_rules.md](../guide/config/virtual_join_rules.md)**(정본 — **충돌 규칙 §4-bis · 미해결 §9**) · 파일 목록은 [guide/CONFIG_GUIDE §1](../guide/CONFIG_GUIDE.md) · 재사용 관점은 [architecture/PRIMITIVES §1·§3](../architecture/PRIMITIVES.md)(absent-only 병합 · 쓰기 깔때기 · 조회 시점 조인) · 보고서 어휘는 아래 행. 🔴 **승인 근거는 UNIQUE 인덱스 하나다**(사용자 판정 2026-07-31 「인덱스 없으면 거절해」 · 「유니크 INDEX 걸면 그냥 DB 영속 아닌가」) — 인덱스는 config가 아니라 DB에 살아 영속이므로 등급·스냅샷·예산이 없다. 무효(`indisvalid=false`)·부분(`indpred`)·표현식(`indexprs`) 배제가 판정의 근거 전체이니 **그 셋을 지우지 마라**. 🔴 **라우트가 둘인 것은 계약 때문이다** — `/admin/config/resolve`는 「DB 질의 0건」이라 인덱스 존재를 모르고(어떤 선언도 `effective`가 되지 않는다), `/admin/config/virtual-join/verify`가 카탈로그를 읽어 승인 여부와 필요한 DDL을 답한다. ⚠️ **중복 프로브·예산·`incomplete`는 같은 날 삭제됐다** — 게이트가 아니게 되어 소비자를 잃었고, `CREATE UNIQUE INDEX`가 같은 진단을 더 정확히 내놓는다. 되살리지 마라. ✅ **실행기 착지 2026-07-31 `d70a33d`** — 「조인 실행 코드는 아직 없다」는 이제 거짓이다(⏳는 `CONFIG_GUIDE §1` · `virtual_join_rules.md` 헤더/§5/§8과 `.sample`의 `_fields_comment`까지 **다섯 곳**에 복사돼 있었다). 🔴 **`expose` 이름 충돌의 거부는 해제됐다** — 「같은 이름이 있으면 거부」는 판정이 뒤집힌 문장이므로 어디서 보든 고쳐라. 지금의 규칙은 **부재일 때만 채운다**(왼쪽 값 있음 → 그대로 · 비었음 → 조인 값 · 둘 다 없음 → `unresolved_label`)이고, **완화의 조건이 둘**이라 하나만 빼면 거부를 되살려야 한다: ① absent-only ② **셀 단위 provenance**(`sources.virtual_join` + `priority_source` — 왼쪽이 이긴 셀은 **손대지 않는다**. 조인이 참여했다가 진 것은 출처가 아니다). 🔴 **`cell_sources`·`SOURCE_PRIORITY`에 등록하지 마라** — 조회 시점 계산이라 `CellSource` 행이 존재할 수 없다. 🔴 **쓰기 거부는 `virtual_only`에만, 깔때기 하나에서** — `collide`는 실재하는 저장 컬럼이라 **계속 써야 하고**, 그 쓰기가 곧 absent-only의 「왼쪽 값 있음」이다. ✅ **화면 착지 2026-07-31 `9200f20`+`4b50135`** — 「`/schema`가 알리지 않아 `virtual_only`가 그리드에 안 뜬다」는 이제 거짓이다. 🔴 **그 라운드의 정정 하나를 규율로 남긴다**: 「목록(`currentColumns`)에 안 넣으면 붙여넣기 대상이 아니다」는 **거짓**이었고, 붙여넣기는 그 목록이 아니라 **그리드 컬럼 id**로 배치를 만든다 — **렌더되는 순간 쓰기 대상**이다. 그래서 쓰기 문 넷에 각자 가드가 있고 **복사 두 곳은 반대로** 받아들여야 한다(거르면 복사한 직사각형 가운데가 빠져 오른쪽이 밀린다). ✅ **숫자 expose 컬럼 착지 2026-08-04 `5be96f5`** — 숫자 컬럼을 `expose`에 적으면 그 컬럼을 지목하는 **모든 읽기가 500이었다**(운영 PG: `blank_to_null`의 `col = ''`가 `InvalidTextRepresentation`, `COALESCE(double precision, text)`가 `DatatypeMismatch`). 🔴 **레이블은 텍스트이므로 숫자도 텍스트로 말해야 한다** — `crud.numeric_text_sql`이 `clean_str_value`의 숫자 분기와 **같은 철자**를 SQL로 낸다(정수값은 정수 철자). `virtual_join_executor._text_part`가 컬럼 타입으로 갈라 숫자면 그쪽, 나머지는 종전 `blank_to_null`이다(문자열 컬럼은 컴파일 결과가 바이트 동일). 🔴 **출하 시점에 아무것도 빨갛지 않았던 이유는 그 축이 이 환경에 0개였기 때문이다** — 「이 환경에 없다」는 출하 근거가 못 되고, 선언 하나로 생길 수 있는 축은 픽스처(`server/tests/test_virtual_join_numeric.py`)가 **상시** 활성화한다. ✅ **해소 2026-08-04 `9e02e3f`(N8) — 「같은 크래시가 `datetime` expose 컬럼에 아직 살아 있다」는 이제 거짓이다.** 그 예고가 **청구서로 돌아왔고**(`timestamp`·`boolean` 둘 다 같은 500), 수리는 타입을 하나 더 더한 것이 아니라 **판정을 뒤집은 것**이다: `_text_part`는 이제 깔때기 **`crud.column_text_sql` 하나만** 부르고, 그 깔때기의 질문은 **「이미 텍스트인가」**이지 「우리가 아는 문제 타입인가」가 아니다(아는 셋 — `numeric_text_sql`·`boolean_text_sql`·`temporal_text_sql` — 을 렌더하고, `String`(단 `Enum` 제외)만 그대로 두며, **그 밖의 모든 것은 먼저 `CAST(... AS String)`**). 🔴 **못박은 것 둘**: `boolean`은 **NULL 분기가 먼저**여야 하고(없으면 짝 없는 행이 전부 `'false'`라는 **값**이 되어 `미상`으로 안 접힌다) 철자는 `'true'`/`'false'`다(파이썬의 `'True'`가 아니다). `timestamp`는 **방언 기본 캐스트를 쓰지 않는다** — PG는 세션 `TimeZone` GUC를 따라 오프셋을 붙이고 소수부가 0이면 떨어뜨려서, 같은 행을 든 두 서버가 다른 답을 낸다. 못박은 형식은 UTC·공백 구분·마이크로초 **항상 6자리**이고 그 고정폭이 **사전순 = 시간순**을 덤으로 준다. **타입 전 집합은 목록이 아니라 테스트가 지킨다**(`test_the_expose_type_universe_is_exactly_what_this_file_covers` — 노출 컬럼의 타입 집합을 고정하고 전부가 깔때기를 `String`으로 빠져나오는지 검사). ⚠️ **|v| < 1e-4에서 SQL 철자와 브라우저가 그리는 숫자가 갈린다**(N1 — 페이로드는 원시 숫자를 나르고 JS가 자기 규칙으로 그린다: SQL `5.5e-05` vs 화면 `0.000055`. 필터에 화면의 철자를 치면 0행). 둘 다 보드 항목이다. ⏳ **미해결 3건은 `virtual_join_rules.md §9`가 단독 소유한다**(**CSV 추출(`/tables/{t}/export`)에 안 실린다** — 그 라우트는 `attach`를 부르지 않는다 · **`filter: false`라 `미상` 행을 찾을 방법이 없다** — 서버측 필터인데 서버가 그 컬럼을 모른다, **서버측 수정이 필요해 클라만으로는 못 고친다** · 사용자가 일부러 비운 셀은 조인 값을 보여 준다 — **사용자 판정으로 정상**). 🔴 **여기에 사본을 만들지 마라** — 직전 라운드가 「착지하면 함께 걷어라」 목록을 두 곳에 적어 갈렸다(§F9 행 아래 각주) | Backend / 총괄 |
| **config 해석 보고서 (「내가 쓴 게 먹었나」)** | **`server/config_resolve_report.py`**(2026-07-30 [F9] — 도메인당 등록기 1개인 `_RESOLVERS`. **2026-08-11 현재 셋**: `enrichment` · `virtual_join` · **`binding`**(`68db020` 신설 — 키마다 `declared`/`inherited`/`absent`/`refused`, `map_overlay.derive_binding_parts`가 소유자이고 **`binding`은 반드시 `domains[0]`이 아닌 마지막에 등록한다** — `contracts/config_resolve_report`가 `resolve_report()["domains"][0]`을 `enrichment`로 고정). `virtual_join`은 팬아웃 거부를 `scope_unresolved`로, 문법·미구현 형태를 `mapping_unavailable`로 사상한다 — **새 단어를 만들지 않았다**), `main.py`의 `GET /admin/config/resolve` + `GET /admin/enrichment/auto-confirm/dry-run`, 계약 벡터 **`contracts/config_resolve_report/`**(`vectors.json` · `test_report_contract.py` · `client_harness.mjs`), 기본 스위트 진입 `server/tests/test_config_resolve_report_contract.py`(shim) + `server/tests/test_config_resolve_routes.py` | **운영 진입점은 [guide/CONFIG_GUIDE §4.2-bis](../guide/CONFIG_GUIDE.md)**(`reload-configs`가 **아무것도 알려주지 않는다**는 사실과 짝으로 읽어야 한다) · 계약·어휘 정본은 [spec/ENRICHMENT_QUEUE_SPEC §5.2-bis](../spec/ENRICHMENT_QUEUE_SPEC.md) · 라우트 계약은 [architecture/backend §2](../architecture/backend.md) · 점검은 [qa/FEATURE_CHECKLIST §2.8-ter](../qa/FEATURE_CHECKLIST.md). 🔴 **사유 어휘에 단어를 추가하는 것은 계약 변경이다** — `config_resolve_report.REASONS`가 `main.CHIP_TRACE_*`의 부분집합인지 계약 테스트가 검사하므로, 새 단어는 총괄 + `vectors.json` + node 하네스를 **함께** 고쳐야 한다. 🔴 **도메인을 추가하면 이 행에 등재하라** — 등록기는 한 줄이라 붙이기 쉽고, 그래서 문서가 먼저 낡는다. ✅ **클라 렌더러 착지 2026-07-31 `93610cb`** — 어드민 Overview 탭의 세 번째 계기 줄. 뷰 모델은 **DOM 없는 별도 모듈 `client2/src/config_resolve_view.js`**이고, 분리한 이유는 **node에서 실행 채점**하기 위해서다: `client_harness.mjs`가 그것을 임포트해 INV-F9-4(렌더된 문장 == 서버 `detail`)를 `PENDING`이 아니라 **실행 채점**한다. 🔴 **뷰 모델이 DOM 빌더 안으로 되돌아가면 그 절반이 다시 `PENDING`이 된다** — 모듈 경계가 취향이 아니라 채점 가능성이다. 🔴 **읽기 실패의 분류는 상태코드가 아니라 `WWW-Authenticate: X-Admin-Token` 헤더로 한다**(`1dc761b`) — 401이 우리 게이트라는 보장은 상태코드에 없고, 앞단 프록시가 같은 포트에 자기 `Basic realm=…`으로 답한다(2026-07-30 인시던트). 판정자는 `admin.js`의 `isGateRejection` **하나**이고 사본을 만들면 갈린다. 그리고 그 헤더는 **CORS `expose_headers`에 있어야** 교차 출처(vite dev `:5173`)에서 지워지지 않는다(`cde3398`).
  - ⚠️ **⏳ 표기를 걷을 때 목록 두 개가 서로 달랐다**(2026-07-31에 함께 정리). 보드는 5곳(`backend §2`·`CONFIG_GUIDE §4.2-bis`·`enrichment_rules §7.3-bis`·`FEATURE_CHECKLIST §1.8`·`§2.8-ter`)을 적었고 이 행은 **자기 자신을 포함하되 `enrichment_rules`를 빠뜨렸다** — 합집합은 **6곳**이다. 🔴 **「착지하면 함께 걷어라」식 목록을 두 곳에 적으면 반드시 갈린다.** 다음에는 **소유 행 하나에만** 적고 다른 곳은 그 행을 링크할 것. ⚠️ **「나머지 N개 config」식 분모를 이 행에 쓰지 않는다** — 코드 주석(`config_resolve_report.py`)은 10을 말하고 `CONFIG_GUIDE §1` 표는 사용자 소유 선언 파일 **13개**(`database.json` 제외 시 12)를 싣는다. 셋이 어긋난 채로 복사돼 있었고, **정본이 정해지기 전까지 수를 적지 않는 것**이 옳다(총괄 판정 대기) | Backend / 총괄 |
| HTML 토폴로지 파서 | `parsers/html_topology_parser.py` | [guide/HTML_TOPOLOGY_PARSER_GUIDE](../guide/HTML_TOPOLOGY_PARSER_GUIDE.md) | Ingester |
| 어드민(파이프라인 5탭 + 코드 에디터) | `client2/src/admin.js`, `main.py /admin/*` | [architecture/frontend §5](../architecture/frontend.md) | UI/Panel |
| 설정 주도 스키마 | `config/table_config.json`, `database/config_watcher.py` | [architecture/data_model §5](../architecture/data_model.md) | Backend |
| **설정 전반(온보딩 지도 + 파일별 세팅 + 전개 순서)** | `server/config/*` 전체, `.gitignore` config 규칙. 로더별 진입점: `crud.load_table_config` · `enrichment_config.load_enrichment_rules` · `virtual_join_config.load_verified_rules` · `ontology_config.load_ontology_mappings` · `map_overlay.load_overlay_config` · `chain_ingestion_worker.load_chain_rules`. 반영 경로: `database/config_watcher.py`(**`table_config.json` 하나만 감시**) · `main.reload_local_process_cache` + `POST /admin/reload-configs`(**신규 CREATE는 하되 기존 테이블 ALTER는 하지 않는다**) | [guide/CONFIG_GUIDE](../guide/CONFIG_GUIDE.md)(지도·시나리오·함정) + [guide/config/](../guide/config/README.md)(**파일별 세팅 절차·키 사전** — 2026-07-28 신설, config 파일당 가이드 1개) + **[guide/CONFIG_ROLLOUT_GUIDE](../guide/CONFIG_ROLLOUT_GUIDE.md)**(2026-08-02 신설 · 사용자 요청 — **운영자가 순서대로 따라 하는 런북**: 최소 선언 · 반영 확인 명령 · 조용한 실패의 모양) — **config 파일 추가/폐지·리로드 경로·키 변경 시 세 곳 모두 필수 갱신**. 상세 동작은 각 서브시스템 가이드로 링크(중복 서술 금지). 🔴 **세 문서의 경계**: CONFIG_GUIDE = 목록과 매트릭스(리로드 매트릭스·watcher 발화 조건·물리 검증의 **정본**) · config/ = 키 사전 · ROLLOUT = **순서와 실패 모양**. 런북이 매트릭스를 베끼기 시작하면 갈린다 — 링크만 할 것. 🔴 **런북이 인용하는 명령·로그 문구는 실측이다** — `chain_worker.log`의 `Synthesized N dedup chain rule(s)` / `[Enrichment:<규칙>] rule skipped:`를 바꾸면 그 문구를 인용한 §3④·§4.1·§5가 먼저 거짓이 된다. 🔴 **`.sample`에 없는 키를 런북에 적지 마라**(실 config는 gitignored라 독자가 대조할 수 없다) — `map_overlay_config`의 `preset_routing`이 그 경우이고 런북은 **없다고 명시하고 링크만** 한다. 🔴 **주석 채널은 파일마다 다르고 `enrichment_rules.json`에는 없다**(최상위 `__comment`가 `rule must be an object`로 거부, 규칙 **안**에만 가능) · `ontology_mapping.json`은 매핑 **안**의 허용 키가 `description`/`event_time_column`/`node`/`edges` 넷뿐이라 그 밖의 키가 **거부**다 — 런북 §2가 그 표의 단독 소유자다. ⏳ **런북 §6에 미완 4절이 있다**(`auto_update_control`·`maps`·`bonding_plan_config`·`transfer_plan_config` — 2026-08-02 실측 **샘플이 실 파일보다 낡음**). 🔴 **샘플이 갱신되면 §6의 행을 지우고 §3에 절을 만드는 것까지가 한 작업이다** — 특히 `auto_update_control`은 수집기 활성/비활성의 단독 소유자라, 그것이 빠진 런북은 「데이터를 계속 만드는 상태」를 재현시키지 못한다. **[guide/config_reference/](../guide/config_reference/README.md)**(2026-08-02 신설 · `558a4af`)는 이 환경의 **실 config 사본**이다 — 🔴 **역할 분담을 지켜라**: 저쪽은 **무엇을 선언했나**(데이터), 런북은 **어떤 순서로 올리고 어떻게 증명하나**(절차). 「조용히 안 먹는 세 가지」는 **런북 §4가 정본**이고 그쪽 README는 요약 + 링크다 — 같은 문장을 두 곳에 두면 반드시 갈린다(그쪽이 착지 시점에 이미 낡은 문장 하나를 실어 왔다: 「`config_watcher`가 원자적 쓰기를 감지 못한다」는 `46a67c7`로 해소됐고 정정을 넣었다). 🔴 **`config_reference/`는 사본이라 실 config가 바뀌면 조용히 낡는다** — **책임자는 이 행과 같은 Lead / Backend이고, 갱신 주기는 그 README 헤더가 정본이다**(2026-08-04 기록): 트리거는 **①`.sample` 변경 ②config 파일 추가·폐지 ③키 추가·개명·삭제**이고, 그 셋이 없어도 **정비 사이클마다 한 번** 대조한다. 🔴 **갱신은 다시 복사하는 것이지 손으로 고치는 것이 아니다** — 손으로 고친 사본은 사본도 원본도 아니어서 어느 쪽이 참인지 알 방법이 없어진다. ⚠️ 실측된 낡음 1건: `transfer_plan_config.json`·`bonding_plan_config.json` 사본이 `2c2a777`이 고친 `.sample`보다 낡았다(README 헤더에 명시) | Lead / Backend |
| **범용 맵 오버레이(맵 인프라)** | `client2/src/map_editor.js`(오버레이 레이어 — **좌표 변환의 정본**, 그리고 2026-08-04 `376e1c8`+`41b17ee`부터 **점 색의 정본**: `legendColorForValue`(폴백 3단 — 맵 legend → 서버 `default_legend` → `null`) · `overlayMarkerFill` · `paintOverlayDot` · `overlayUnlistedValues`+`overlayLegendChip`. 서버 절반은 `map_overlay.get_default_legend`가 `GET /api/maps/paint-rules`로 서빙, 채점은 `client2/tests/overlay_value_colour_harness.mjs`. 🔴 **legend가 유일한 색 출처이고 `pickUnusedColor()`·`LEGEND_PALETTE`는 그리는 자리에서 안 부른다** — 팔레트는 *저작* 도구이고, 지어낸 색은 아무것도 뜻하지 않으면서 선언된 색처럼 읽힌다. 🔴 **미선언 값은 안 그리는 게 아니라 속이 빈 링 점**이고(`fillStyle`은 **대입조차 안 한다** — 대입해 두면 칠하기까지 한 줄이다), 부재의 두 이유는 픽셀이 아니라 **칩으로** 가른다(여럿=`overlayFanChip` / 미선언=`overlayLegendChip`). 🔴 **한 칸에 값이 여럿이면 값이 같아도 안 칠한다** — 대표를 고르는 순간 그 점은 자기가 하나인 척한다. 2026-07-31 `cd3e0f4`부터 **웨이퍼 mm 단계**를 포함한다: `projectCellsToWaferMm` → `seatWaferMmInFrame`. 소스와 타깃의 피치가 다르면 다이 인덱스로는 겹칠 수 없다 — 같은 인덱스가 같은 물리 자리가 아니기 때문이다. 🔴 **mm는 세 번째 변환이 아니라 단위 환산이다**(회전·반전·오프셋은 `getDieIndex` 안에서 끝난다) — 오버레이 전용 기하식을 만들지 마라. 🔴 **mm는 반올림 *전* 연속값에서 만든다**: 반올림된 인덱스에서 되만들면 칸 미만 잔여가 빠져 실측 1,836칸 중 1,789칸이 틀린 칸에 앉았다), `server/map_overlay.py`, `config/map_overlay_config.json` | [spec/MAP_EDITOR_SPEC §5](../spec/MAP_EDITOR_SPEC.md)(정렬 계약 — 도메인 규칙 §5.0 / 클라 파이프라인 §5.1 / 서버 계약 §5.2) · [guide/CONFIG_GUIDE §5.8-bis](../guide/CONFIG_GUIDE.md)(설정 관점) | Backend / UI-Map |
| 맵 정렬 메타(`wafer_map_metadata`) | `server/map_overlay.load_map_meta`, `client2/src/map_editor.js`(`fetchGridMetaFor`/`frameFromMeta`), **`client2/src/map_key.js`**(2026-08-04 — `getMapIdFromMeta`/`composeMapId`가 메타에서 맵 키를 만드는 **클라 절반**이고 `map_meta_registrar`가 등록하는 것의 짝이다. ⚠️ `getMapIdFromMeta`는 분리되면서 `tableSchema`를 **두 번째 인자로** 받는다 — 본문은 바이트 동일이다), `config/table_config.json`, **`server/map_meta_registrar.py`**(2026-07-29 M3 — 인제션 경로의 **부재 시 자동 등록**, 노브 `auto_register_map_meta`) | [spec/MAP_EDITOR_SPEC §5.0](../spec/MAP_EDITOR_SPEC.md)(정렬의 유일한 기준) · [map_editor/architecture_and_management §2](../map_editor/architecture_and_management.md)(스키마·필드 규격) · **자동 등록 계약은 [guide/INGESTION_GUIDE §1.10](../guide/INGESTION_GUIDE.md)**(설정 관점 [guide/config/ingestion_settings](../guide/config/ingestion_settings.md)) | Backend / UI-Map |
| **로드 시 프리셋 라우팅(F5)** | **`server/map_preset_routing.py`**(2026-07-30 — 맵 키에서 물리 규격을 결정, `GET /api/maps/preset-routing`), `config/map_overlay_config.json`(`preset_routing` 선언), `config/maps.json`(프리셋 본문) | [guide/config/map_overlay_config §2-bis](../guide/config/map_overlay_config.md)(**운영 선언 절차 — 조회 테이블이 운영에만 있어 문서가 유일한 전달 수단**) · [guide/CONFIG_GUIDE §5.8-bis](../guide/CONFIG_GUIDE.md) · [architecture/backend §2](../architecture/backend.md)(엔드포인트 계약). 우선순위(`wafer_map_metadata` > 라우팅 > 패널)를 바꾸면 세 곳 모두 필수 갱신 | Backend / UI-Map |
| 본딩·전사 계획 엔진(역할 바인딩) | `server/bonding_plan.py`(**`STATUS_NOT_DECLARED`·`role_is_declared`·`SUBTRACTION_ROLES`의 단독 소유자** — 2026-08-04 `2c2a777`), `server/transfer_plan.py`(**가용 판정의 알맹이**: `_summarize_inline`·`assess_degradation`·`_status_is_degraded`·`_aux_role_status`·`validate_plan`. ⚠️ 보드가 「본딩 가용 게이팅」을 `bonding_plan.py`로 추정했지만 **실제 게이팅은 이 파일에 산다** — M1은 core-kind 경로만 댄다), `client2/src/transfer_plan.js`(**자격 표시 `*`의 소유자** — `2c2a777`+`101311f`), `config/bonding_plan_config.json`, `config/transfer_plan_config.json` | [spec/MAP_EDITOR_SPEC §6](../spec/MAP_EDITOR_SPEC.md)(엔진·클라 계약 — **§6.2-ter가 세 상태와 `inactive_subtractions`의 정본이고 §6.2-ter.1이 화면 절반**) · [guide/CONFIG_GUIDE §3-S6/§5.8](../guide/CONFIG_GUIDE.md)(설정 관점) · **키 사전은 [guide/config/transfer_plan_config](../guide/config/transfer_plan_config.md)·[guide/config/bonding_plan_config](../guide/config/bonding_plan_config.md)**. 🔴 **config 경계의 상태는 셋이다: 부재 / 있는데 깨짐 / 있고 동작**(2026-08-04 사용자 요청 완화). **부재는 고장이 아니라 사이트의 선언**이고 그 감산 없이 **숫자**가 나간다. 🔴 **판정은 키 존재(`role_is_declared`)로만 한다** — 진리값(`cfg.get(k)`)으로 재구현하면 앞의 두 상태가 다시 하나로 접히고, 「사이트가 안 쓴다」와 「사이트가 잘못 적었다」를 구별할 수 없게 된다. 🔴 **`total_chips`는 예외이고 계속 필수다** — 분모가 없으면 가용이 성립하지 않는다. 이 완화를 그쪽으로 넓히지 마라. 🔴 **가용 수치를 내는 라우트를 늘리면 `inactive_subtractions`를 그 라우트에도 실어라** — 요약에만 달았더니 **판정을 내는 `validate`가 그 필드를 모른 채 숫자만 읽었고**(QA B1), 판정 라우트가 정확히 사고가 나는 자리다. 열거 기준은 「그 필드를 읽는 곳」이 아니라 **「그 숫자를 읽는 곳」**이다. ⚠️ **`not_declared`는 `config_resolve_report`의 사유 어휘에도 같은 철자로 있다**(CONFIG_GUIDE §4.2-bis) — 술어는 같고 **축이 다르다**(저쪽은 `ineffective` 아래의 *사유*, 이쪽은 *역할 상태*). 두 표를 서로 참조해 찾지 마라. 🔴 **2026-08-05 — 기준 프레임은 이제 층 ⑧이 정한다**: `bonding_plan.canonical_basis`가 확정 기록을 먼저 보고, **확정이 있으면 `CANONICAL_FRAME_ROLES`는 상의되지 않는다**(그 튜플은 폴백이다). 응답에 `frame_basis`(추가 전용)가 실리고 중간 등급 `connected(not_declared)`가 생겼다 — 「정렬은 됐는데 근거가 약함」. **여섯째 토큰을 만들지 마라**(`config_resolve_report.py:404`). `transfer_plan._canonical_origin_meta`도 같은 함수를 부르므로 **한쪽만 고치면 M1·M2가 갈린다** → [spec/MAP_EDITOR_SPEC §6.2-ter.2](../spec/MAP_EDITOR_SPEC.md)(정본) · 층 정의는 [spec/MAP_ALIGNMENT_SPEC §0.2](../spec/MAP_ALIGNMENT_SPEC.md) | Backend / UI-Map |
| QA 기능 점검 | 전 서브시스템(사용자 관점 기능 단위) | [qa/FEATURE_CHECKLIST](../qa/FEATURE_CHECKLIST.md) — 갱신은 doc-keeper 전담. 정합 감사는 doc-auditor. **§2.0 자동 게이트의 정본은 `client2/package.json`의 `prebuild` 한 줄**이고 이 문서는 사본이다(실제로 한 번 낡았다 — `check:suggest-keys` 추가 후 2행으로 적혀 있었다). 사용자 눈에 보이는 기능이 착지하면 **§1의 인벤토리 행이 먼저 필요하다**: 행이 없으면 그 기능은 회귀 점검 대상에서 구조적으로 빠진다 | Integrity/QA |
| 운영/셋업 | 환경·DB | [guide/*_SETUP_GUIDE](../guide/CONDA_SETUP_GUIDE.md) | Ops |
| 엔지니어링 규율 | CRUD 시그니처·병합 | [guide/data_preservation_and_signature_change](../guide/data_preservation_and_signature_change.md) | Integrity/QA |

> 에이전트 역할 정의는 [prompts/starting_prompt](../prompts/starting_prompt.md) §5 및 `.agents/skills/` 참조.
