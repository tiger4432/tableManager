# 🗂️ 대기열·백로그 아카이브

> **Status:** 🟢 Living | **Last-verified:** 2026-08-06 | **Owner:** Lead / PM
> ⚠️ **[2026-08-06 상태 헤더 신설]** 2026-08-01 보드 분리 산물.

> 보드에서 분리(2026-08-01). **옛 개발 대기열·현재 초점·완료 롤업·장기 백로그**를 담는다.
> 🔴 **다음 이어서 할 일의 정본은 [`PROJECT_STATUS.md`](PROJECT_STATUS.md)다.** 여기 표의 미완 항목은 그쪽으로 승격되었거나, 아직 트리아지되지 않은 것이다.

## 📋 개발 대기열 (Queue) — 사용자 지시 2026-07-27

> **규율:** 새 요청은 **즉시 착수하지 않고 이 대기열에 넣는다.** 위에서부터 순차 처리하며, 파일이 겹치지 않을 때만 병렬로 돌린다.
> **이유:** 대기열 항목은 **고쳐도 공짜**지만 이미 던진 항목을 고치면 에이전트-시간이 날아간다. M2.6은 착수 전 5회 수정돼 비용이 0이었고, 좌표 건은 착수 후라 2라운드를 잃었다.
> **예외:** 사용자 작업을 지금 막고 있거나 프로덕션이 깨진 건은 앞으로 당긴다 — 단 **당긴다고 말하고** 당긴다.
> **순서 변경은 사용자 권한.** 총괄은 제안만 한다.

> **포맷 (2026-07-28 통일):** 한 항목 = **제목 한 줄 + 착수에 필요한 핵심 1~3문장**. 경과·근거·수치는 히스토리/보고서에 있다 — 여기 쌓지 않는다. 완료 항목은 표에서 내리고 아래 「종결」 한 줄로 압축한다.
> **열:** 순번(처리 순서) · 항목 · 담당 · 등급([[review-tiers]] T1/T2/T3) · 상태

> ⏱️ **2026-07-29 확정 순서 (사용자 지시, 마감 12:00)**: 계측(진행중) → **[7b+7c]+[M4①] 병합 라운드** → **1c Copy to Excel** → **1·1b·1d·1e UI 개편** → 0b 공수절감 → 슬라이스 1호.
> R2+R5를 묶은 이유: 둘 다 map-pm+server-pm의 **메타/기하 선언 영역**이고(7b는 `map_id` 조회 키, M4①은 그 메타가 참조하는 `valid_die_ref`) 같은 코드 경로를 두 번 열 이유가 없다 — QA·병합 사이클 각 1회 절감.
> ⚠️ **직렬 제약**: 이 라운드들은 전부 `map_editor.js`다. 병렬로 쪼개면 병합 충돌로 라운드를 잃는다. 1c의 **원인 규명만은 읽기 전용**이라 계측 라운드와 병행 착수됨(커밋 블롭 기준).

| 순번 | 항목 | 담당 | 등급 | 상태 |
|---|---|---|---|---|
| — | ~~H1·H2~~ ✅ **완료** (`6db517d`, 퀵 QA FIXED×2) — H1: 로드 경로가 초안을 덮기 전에 읽도록 + "복구=화면 변화"로 정정. H2: 대조 가드(화면 비어있지 않은 셀 − 직렬화 셀, 0 오탐)가 파괴 방지 게이트 3호로. **잔여 소형 3건 대기열 편입** ↓ | — | — | ✅ |
| 5b | ✅ **완료** (`0052d76`) — 맵 에디터 소형 7건(ⓐ~ⓖ). 퀵 QA 6/6 FIXED + ⓕ 실측 3폭 | map-pm·ui-designer | T2/T3 | ✅ |
| — | ~~U8·U9~~ ✅ **완료** (`2baf9ff`, QA GO) — 마커 값·V6 양측, 가용 사유 노출, bin_map 선언 경로 E2E 증명. 잔여 LOW 2건: JSON 숫자 `0.5`→marker 승격(양측 일치라 비긴급, 다음 계약 라운드에 동승) · M1 위임 stage에서 bin_map 무효(라이브 dt 점등 시 inline 전환 필요 — 현장 결정) | — | — | ✅ |
| — | ~~U6~~ ✅ **완료** (`95bf072`, QA 2레인 GO — 분할 구조 첫 실전) — `default_legend`·`value_column_candidates` 선언 신설(paint-rules 서빙 단일 소스), 클라 하드코딩 6종 삭제(후보 목록 2사본·builtin stage·팔레트 3사본·E1/E2 색). 라운드 내 High 1건(같은 테이블 연속 로드 legend 유출) 발견·수리 완료. 브라우저 레인이 낡은 결함 2건 추가 검출 → 5b 편입 | — | — | ✅ |
| — | ~~U7·U1·U3~~ ✅ **완료** (`a98dc72`, CSS-only) — U7 원인은 `b35bc9f` CSS 재작성의 3·4번째 희생(`.map-breadcrumb`·`.plock-chip` 무스타일) → 복원+보강. U1은 규칙이 이미 반반이었고 **주석이 거짓말** → 주석 정정+죽은 `.tp-scroll` 삭제. U3 하단 중앙 배너 + `--toast-inset-right` 참조 0 증명 삭제. 부수 관찰: 메타 모달 취소 시 `openMapFrame`이 조용히 롤백(토스트 없음) → 5b-ⓖ | — | — | ✅ |
| — | ~~5c~~ ✅ **완료** (`1fefd12`) — 트리아지 REAL(+101 유령 실측) → `connected(count_only)` 강등(remaining null+진짜 상한, per-core 양 경로 null — 퀵 QA가 log 경로 맨 숫자 잔재를 잡아 마감) + 선언 컬럼 오타 `column_unresolved` 강등 + val 오타 fail 카운트 거부(상한 불변식). 테스트 21, 뮤테이션 전멸. **형제 결함 제안 중**: self-frame fail 소스 x/y 미바인딩 — 같은 유령 계급, 승인 대기 | — | — | ✅ |
| — | ~~소급 백필 + 빈 키 큐 필터~~ ✅ **완료** (`1fefd12`) — `backfill_enrichment.py`(dry-run 기본·기존 행 불가침·멱등·실 mapper 경로·루프 가드 라이브 검증) + 큐 판정식 서버 단일화(`queue_filters` — 클라 3사본 소멸, 빈 키 제외). 빈 키 출처 = 그리드 빈 행 추가·부분 편집(레거시 mapper 무죄) | — | — | ✅ |
| — | ~~raws 폴더 평탄화~~ ✅ **완료** (`0c6ac1a`, 퀵 QA 5/5) — 중첩 폴더 투하 → 정온 대기 → 파일 승격 → `rmdir`-only 폐기. 충돌 `~` 접두(`__force__` 위조 차단), 핫 토글 | — | — | ✅ |
| — | ~~오버레이 F1~F5~~ ✅ **완료** (`17f65bd`, 원 진단 QA 재판정 5/5 FIXED — 결함 번들이 미끼 4칩을 그리는 것까지 실측한 판별 검증). paint-rules `binding` 단일 소스, 클라 유도 사본 ~40줄 삭제, 추측은 데이터 경로 거부·Load 사전선택만 경고. **table_bindings 선언만으로 tx/ty·대문자 테이블이 로드·겹치기 동작** | — | — | ✅ |
| — | ~~게이트 4호 + map_push_ok~~ ✅ **완료** (`deed6d2`, QA 4/4 + 하네스 15/15) — 판별자 = "payload/서버가 그 컬럼을 재구성할 수 있는가"(조합키 pkg_id 생존, 행 고유 dt_id 차단, config 불필요). R&D 수동 계측용 `map_push_ok: true` 선언 시 차단→소실 인지 1회 확인(JSON boolean만, `is True` 엄격) | — | — | ✅ |
| — | ~~self-frame fail count_only~~ ✅ **완료** (`deed6d2`) — 유령 계급 소탕 종결: 좌표 없는 fail 소스가 전 칩을 fail로 세며 remaining reliable:true 서빙하던 것 → count_only 강등+진짜 상한. fallback 경로는 의도적 무변경(개수 감산이 옳은 곳). 잔여 엣지 1건(fallback+region 부분집합 신뢰도) 트리아지 대기 | — | — | ✅ |
| — | ~~#6 replace_map scope~~ ✅ **완료** (`deed6d2`) — 무동작 200 → 400 사유, 응답 `scope:{filters,deleted,inserted}` 단일 해석기, 명시 scope로 의도적 전체 삭제 표현 가능. **후속 등재**: 교체 삭제 행 WS 미방송(유령 행) — 경계 계약 건 | server-pm | — | 후속 대기 |
| — | ~~D1 헌장 공백~~ ✅ **완료** (`deed6d2`) — 4문서를 server/client PM 헌장에 도메인별 의무와 함께 등재 | — | — | ✅ |
| — | ~~DB 쓰기 2건~~ ✅ **완료** (로컬 라이브 적용 — 인덱스 6종+정리 4종+재교정 인덱스, CONCURRENTLY 무중단. 운영 실행 가이드 전달) | — | — | ✅ |
| 2 | 🟠 **죽은 계약 하네스 + 그걸 놓친 가드의 범위 구멍** (2026-07-29, map-pm·code-mapper가 **서로 다른 경로로 독립 도달** — 확증). `client2/tests/split_registry_harness.mjs`(272줄)가 `loadLegend`·`fetchLegendFromServer`·`maybeOfferLegendMigration`·`loadLegendFromStorage`·`DEFAULT_LEGEND`를 **이름으로 추출**하는데 5개 전부 CODE_MAP §0 tombstone(삭제됨) → **추출 단계에서 throw**. 클린 트리에서도 재현되므로 이번 라운드 무관. **더 중요한 건 왜 안 걸렸나**: 심볼 개명을 exit 2로 잡아주는 §0 체크 ②-b가 `client2/src contracts`만 스코프해서 **`client2/tests`를 안 본다**. ⓐ 하네스 처분(개명 대응 복구 vs 폐기 — U6에서 하드코딩 기본값을 `default_legend` 선언으로 옮긴 뒤 계약 자체가 유효한지 판정 필요) ⓑ **체크 스코프에 `client2/tests` 편입** | client-pm+map-pm | T2 | 대기 |
| 4 | **D1 — 에이전트 헌장 공백**: `CONFIG_GUIDE`·`DEPLOY_SETUP`·`PRODUCTION_READINESS`·`FEATURE_CHECKLIST`가 어느 헌장에도 없음(감사 발견). 문서 아닌 헌장 문제 | 총괄 | — | 대기 |
| 5 | **DB 쓰기 2건** — ⓐ 재교정 집계 부분 인덱스(`setup_db_performance.py` 준비됨, 현재 512ms 순차 스캔을 60초 캐시로 방어 중) ⓑ 낡은 outbox 인덱스 정리 | 총괄+사용자 | — | DB 쓰기 필요 |
| **0** | 🔴 **V1 계기 계측 — 완료까지의 상호작용 점수** (사용자 2026-07-29 사양 확정). **단위 = 한 tx 묶음 교정 완료**(서버가 이미 긋는 경계 — `PUT /tables/{t}/data/updates` + `AuditLog.tx_id` 재사용, 새 개념 신설 없음). **배점: 키입력 1 · 마우스 3 · 화면이동 5, 낮을수록 좋음.** ⚠️ **컨텍스트를 유지한 자연스러운 리다이렉트는 0점**(예: DOE → dt map 라우팅) — 판정은 **config 선언형**, 기본은 "상실(5점)"이고 유지 전이만 선언한다(낙관 편향 방지). 집계 = **세션별 평균**. 계약: `effort:{session_id,key,mouse,nav}`는 **선택 필드** — 워커·인제션 경로는 미계측이 정상이며 **0이 아니라 없음**이어야 평균이 조용히 희석되지 않는다. 원시 카운트를 저장하고 합산은 조회 시점(배점 변경 시 과거 데이터 재해석). **R1보다 먼저 머지** — 소급 산출 불가라 UI를 먼저 고치면 "before"를 영영 못 얻는다 | client-pm+map-pm+server-pm | T2 | **승인됨 — 투입 대기** |
| **0b** | 🟡 **공수 절감 기능군** (계기에서 파생, 사용자 방향 제시 2026-07-29). 배점이 마우스 3·이동 5이므로 **손을 줄이는 기능이 곧 지표 개선**이다: ⓐ **드롭다운·입력 추천** 적극 활용 ⓑ **최대한 미리 값이 채워지게**(prefill) ⓒ **범위 묶어 Ctrl+Enter 일괄 채우기**. ⚠️ 착수 전 필수 확인: 그리드에 **이미 있는 범위·클립보드 프리미티브**(`clipboard.js`)와 겹치는지 — 없는 것만 만든다([[check-existing-primitives-first]]) | client-pm | T2 | 🟡 **ⓐ 완료 · ⓒ 이미 존재 · ⓑ만 남음** — ⓐ는 F3 클라 절반으로 착지하고 **계기가 92% 절감을 실측**(위 F3). ⓒ는 만들 것이 없었다: 기존 `Ctrl+Enter` 일괄 채우기가 그대로 살아 있고 **제안값과 합성된다**(25자 값 3칸 = 5타 vs 78타) — [[check-existing-primitives-first]]가 두 번째로 값을 했다. 남은 ⓑ prefill은 별 라운드 |
| **1** | ✅ **전건 완료** — DOE UI 개편 사용자 VOC ⓐ~ⓘ 9건. 8건 `7694b42`, ⓗ splitter `ae2811c` | ui-designer+map-pm | T2 | ✅ |
| **1b** | ✅ **완료** (`7694b42`) — P0 입력 연속성. blur 커밋이 표①을 무조건 재생성해 mousedown~mouseup 사이 행 DOM을 파괴하던 것 → 행 집합이 바뀔 때만 재생성 | map-pm | T2 | ✅ |
| **1c** | ✅ **완료** (`7694b42`) — Copy to Excel. 원인은 **평문 HTTP라 `navigator.clipboard` 미정의**였고 폴백이 같은 객체를 다시 불렀다. 합성 copy 이벤트로 대체, 규약은 `prebuild` 검사로 강제 | map-pm | T2 | ✅ |
| **1d** | ✅ **완료** (`7694b42`) — 삭제 프리뷰가 주간 테마에서 맵을 가리던 것. 캔버스 전용 반투명 토큰 신설, 범용 `--danger-weak`는 무손상 | ui-designer | T3 | ✅ |
| **1e** | ✅ **완료** (`c24d47b`) — 토스트 62→53, 깨끗한 경로 발화 13건 감소. 규칙: **성공 토스트는 그 효과가 화면에 없을 때만 살아남는다** | map-pm | T2 | ✅ |
| 7b·7c | ✅ **완료** (`91386f0`, 2026-07-29) — 맵 키 canonical화 세 자리(`composeMapId`·`decomposeMapKey`·`canonicalMapKey`)가 한 함수. `parseInt` 아닌 엄격 정규식. 🔴 **남은 구멍: 계약 벡터가 없다** — pytest와 node가 각자 자기 기댓값을 본다(이 버그를 만든 바로 그 실패 모드) → contract-keeper 별건 | map-pm | T2 | ✅ (벡터는 별건) |
| 7c | **`transfer_log: "none"` 선언 — 소비 기록 없는 현장 지원** (사용자 요청 2026-07-28 밤 "기록 없이도 되게"): 선언 시 `connected(untracked)` 상태로 잔량을 **상한 표시(`≤N` = 총량−fail)** + 툴팁 "기전사 미차감". 사고성 missing은 미상 유지 — 선언=인지 패턴(map_push_ok와 동형). 서버 upper_bound 배관 기존재, 선언 파싱+클라 표시만. 가용 풀 바인딩 canonical화(7b)와 같은 파일권 — 동반 가능 | map-pm | T2 | ✅ **완료 — `91386f0`** (`transfer_plan.js:485-503`, 상한 표시 `≤N`) |
| 7 | **M3 맵 메타 자동 등록** (ingestion 체인) — `bonding_map` 맵 키 39만 vs 메타 9건 해소 경로 | server-pm | — | 대기 |
| 8 | **P3 대형 파일** (backpressure·COPY·상한) | server-pm | — | 대기 |
| **F3** | 🟠 **unique values 조회 API — UI 전영역 입력 제안의 전제 프리미티브** (사용자 요청 2026-07-29). 인자 `(table, column, prefix)` → **prefix로 시작하는 unique 값 목록**. 목적: 전 UI의 필드 입력 드롭다운 제안. **0b 공수절감의 전제**이므로 0b보다 먼저.
  - ⚠️ **확장성이 이 건의 전부다**. `bonding_map` ~1.7M 행에서 `SELECT DISTINCT col WHERE col LIKE 'p%'`는 인덱스 없이는 재앙이다. **btree는 비-C 콜레이션에서 `LIKE 'prefix%'`에 안 걸린다 — `text_pattern_ops`가 필요**하다. 이 함정은 이미 보드에 있다(그래프 search ILIKE 프리픽스 항목) — **같은 미해결 문제의 두 번째 소비자**이므로 한 번에 푼다. `LIMIT` 필수(드롭다운은 20~50개면 된다), **빈 prefix는 전량 DISTINCT 스캔**이므로 최소 길이 요구나 하드 캡 중 택.
  - 🔒 **table·column 이름이 클라에서 온다** — `table_config` 선언과 대조해 검증하고 **원문을 SQL에 보간 금지**. F1의 "요청은 테이블을 지목하지 스크립트를 지목하지 않는다"와 같은 원칙.
  - **정직한 열화**: `LIMIT`으로 잘렸으면 **잘렸다고 말해야** 한다(`truncated`). 안 그러면 드롭다운이 "이게 전부"라고 암시한다.
  - **7b 정합**: number 선언 컬럼이면 저장형(`1`)이 제안돼야 한다 — 토큰형(`01`)이 아니라.
  - null·빈 문자열 제외. 타이핑 중 반복 호출이므로 클라 디바운스 필수.
  - 📏 **V1 계기 실측**: 25자 값에서 타이핑 26타/가중 29 → 접두+Enter **2타/가중 5 (92% 절감)**. 「짧은 값에선 드롭다운이 더 비쌀 수 있다」는 우려는 산술로 상한이 잡힌다 — 후보 k번째는 k타가 드므로 목록 길이를 넘겨 얻을 이득이 없다 |
| **F3-fix** | ✅ **완료** (`d5f75a8`) — 제안 입력 `Esc`가 타이밍으로 갈리던 것(친 글자 소실 / 틀린 값 저장). 판정 항을 **「이 편집기가 스스로 물어본 적이 있는가」**로 바꿔 타이머·RTT와 무관하게 만듦. MEDIUM 4건 동반(백오프·floor 비가역·공유 DOM·가로 클램프) | client-pm | T2 | ✅ |
| **F8** | 🟠 **R0 업로드 경로 정화에 테스트가 0건** (doc-historian 대조 2026-07-30). `0d4798a`가 경로 탈출 2벡터(`file.filename`·`user` 쿼리 파라미터)를 막고 결과 기반 검증(destination이 `target_dir`의 **직접 자식**인지)까지 넣었는데, `server/tests/` 어디에도 `upload_file`을 태우는 케이스가 **하나도 없다**. 내 검증은 정화 함수를 직접 부른 10케이스뿐이라 **핸들러 배선은 무보증**이고, 400 분기는 「도달 불가」가 아니라 **「어떤 샘플도 도달하지 않았다」**까지만 측정됐다. 폴더 단위 업로드는 이 가드 위에 얹히므로 그 전에 필요 | server-pm | T3 | 대기 (F7 다음) |
| **F12** | ✅ **완료** (`61440e6`) — 유효 다이 채택 차단 제거(사용자 재확인 「그냥 틀어지는대로」). 후속 정합은 오리진 라운드가 흡수 | map-pm | T2 | ✅ |
| **F13** | ✅ **완료** (`4e23a9f`+`0da44e4`) — `DEPLOY_SETUP §1-4/§1-5`에 영구 설정 형태 · **`SetEnvironmentVariable`은 replace라는 경고** · 지문 대조 · 증상 기준 재구성. 🔴 **핵심 블록**: 「프로세스는 태어날 때의 환경을 들고 있다」 — 변수를 바꿔도 이미 도는 프로세스엔 안 닿고 새 터미널이 더 오래된 것의 자식이면 소용없다(오늘 하루를 먹은 사실) | doc-keeper | T3 | ✅ |
| **F10** | 🔴 **하네스 13개 중 3 적색 · 배선 1 · 미배선 12** (code-mapper 전수 실행). ⓐ `copy_header_count_harness` ⓑ **`effort_instrument_harness` — 첫 그룹에서 죽어 채점 0. 이번 분기 UI 작업의 성패를 재는 계기가 아무것도 보고하지 않고 있었다** ⓒ `split_registry_harness`. ⓐⓑ 둘 다 `ae2811c`가 헬퍼를 추출하며 다른 하네스 2개만 갱신한 결과 — 어느 하네스가 어느 함수를 슬라이스하는지 알려주는 장치가 없다. <br>**구멍**: `check_contracts.mjs`가 `contracts/*/client_harness.mjs` 한 패턴만 봐 `client2/tests/**`가 구조적으로 밖. 그런데 `package.json`의 `check:suggest-keys`는 이름을 박아 넣었다(총괄이 넣었고, 그 러너 헤더에 「DISCOVERY, NOT A LIST」라고 쓴 것도 총괄). <br>⚠️ **구현 주의**: 세 실패는 `die()` exit 2가 아니라 **exit 1**(추출 가드 통과 후 실행 중 사망)이고, `reposition_regime_probe.mjs`는 인자를 받는 프로브라 순진한 `*.mjs` 규칙은 **없던 적색을 만든다**. 대기열 2번과 같은 구멍 — 한 라운드로. <br>🔴 **동반**: 형제 하네스 전부에 **슬라이서 버그** — 함수 본문을 `(` 뒤 `indexOf('{')`로 찾아 `f(opts = {})` 같은 기본값 객체에서 깨진다 | client-pm | T2 | 🔴 **다음 클라 라운드 1순위** |
| **F11** | 🔵 **`c_zero`가 두 방식으로 계산되고 `side==='back'`에서 갈린다** (사용자 질문에서 발견 2026-07-30). `:1157` `getGridCellObject`는 `box.minC − startX`로 **거울 분기가 없고**, `:2924`는 `isXMirrored ? (box.maxC + startX) : (box.minC − startX)`로 있다. `MAP_EDITOR_SPEC §1`이 문서화한 미러 분기가 코드 한쪽에만 있다. ✅ **QA 실측으로 등급 하향**: 두 곳 다 원점 마커 지역 변수이고(`hasZeroZero` → `isOriginCell` `:3009`) **저장 경로에 닿지 않는다** — 재배치 경로는 `getPhysicalCoords`/`getVisualCoords`/`getWaferBoundingBox`만 탄다. 즉 back 면에서 **원점 표시만** 틀릴 수 있다. 수리는 한 정의를 공유하게 하는 것 | map-pm | T3 | 대기 (표시 전용, 비긴급) |
| **F9** | 🔴 **config가 먹었는지 확인할 방법이 전무** (사용자 2026-07-30). 어드민의 config 라우트는 `POST /admin/reload-configs` 하나뿐이고 **무엇이 먹었는지 아무것도 반환하지 않는다**. 서버가 읽는 config는 10개. 실증: `auto_confirm: true`를 `candidate_for` 없이 켜면 경고 한 줄 남기고 조용히 비활성 — **지금 운영이 그 상태**. <br>**계약(확정)**: 서버가 `effective` / `ineffective`(+명명된 이유) / `rejected`(+이유) 세 모집단을 반환하고 **클라는 서버 문자열을 그대로 렌더**(유도 금지). 어휘는 런타임 열화어 재사용, `contracts/config_resolve_report/` 벡터로 고정(**착지함**). <br>**범위**: enrichment 먼저(사용자 확정) → 나머지 9개는 같은 틀 <br>**서버 착지 `f3fd785`**: `GET /admin/config/resolve`(**`db` 인자가 없어** 질의 0건이 서명으로 보장) + `GET /admin/enrichment/auto-confirm/dry-run`(`apply` 파라미터 부재를 라우트 서명에서 테스트가 검사). 도중 결함 2건: ⓐ **SQLite가 해석 안 되는 큰따옴표 식별자를 문자열 리터럴로 강등** → 없는 컬럼 조회가 「그 이름을 값으로 갖는 행 1개」로 돌아와 **컬럼명 자체를 자동확정**할 뻔했다(별칭 한정으로 수리) ⓑ 서버가 자른 행을 파이썬에서 세다 보니 뷰의 `limit` 너머로 `ambiguous`가 **도달 불가** — 실측 `wafer_process`는 `limit: 50`인데 80키 전부 69~217행. 후보 탐침만 GROUP BY로, 표시 경로는 행 제한 유지 | server-pm+client-pm | T2 | 🟡 **서버 착지·QA 중 · 클라 대기** |
| **F7** | ✅ **완료** (`a5eb934`) — `wafer_process`가 인덱스 없이 217ms를 「정답·완전」처럼 답하던 것. 임계 넘긴 테이블 전수 점검(이 하나만 누락, 14개 생성) + **성공 경로가 자기 비용을 보고**(`elapsed_ms`) + 계획 형태 검사기(`Index Cond` vs `Rows Removed by Filter`). 302→21ms | server-pm | T2 | ✅ |
| **F6** | ✅ **완료** — 유효 다이 지정 시 「grid x,y 안 맞다」 거절. 최종 설계는 **아무것도 채택하지 않는 것**(`61440e6`+`94b9baa`): 크기가 달라도 마스크만 화면에서 밀려 보이고 **저장 좌표는 한 개도 안 움직인다**. 사용자 판정 「그리드 크기가 달라도 좌표는 db값 그대로 보존하고 화면 표기 밀리게 그냥 보여주기」. 정본 서술 [SPEC §5.7-bis](../spec/MAP_EDITOR_SPEC.md) · 경과와 정정 이력은 [히스토리](../history/README.md) | map-pm | T2 | ✅ |
| **F4** | 🟠 **방금 만든 맵이 다음 화면에서 「없음」으로 읽힌다** (표준 시나리오 실측 2026-07-30, **기존 결함**). dt map을 315행으로 저장하고 본딩맵을 **다시 로드**해도 롤업이 `MAP X`를 유지한다 — **`↻ 가용`을 눌러야** `O`로 바뀐다. 네트워크 로그상 재로드 시 맵 존재 확인 요청이 **아예 나가지 않는다**. `transfer_plan.js`는 그 라운드 diff에 없으므로 기존 결함. ⚠️ **핵심가치 #3(실시간 신뢰 전파)** 위반 — SSOT가 *"반영이 안 믿기면 교정이 멈추고 온톨로지가 틀린 채 굳는다"*고 쓴 지점이고, 사용자가 **방금 자기가 만든 것**을 시스템이 부정하는 형태라 신뢰 손상이 특히 크다 | map-pm | T2 | 대기 |
| **F2** | 🔴 **화면의 DOE 수량이 저장 불가능한 셀을 포함한다** (기존 결함). `🎨 Fill All`이 `inside`와 무관하게 사각 전체를 칠하고 `updateLegendCounts`가 그것까지 세므로, 화면 숫자가 실제 저장 가능량보다 **35~51%** 많다(프리셋별 CORE 35/BASE 41/TEST 44/TAPE 51). `pushMapData`는 `!inside`를 떨구므로 **화면과 저장이 갈린다**. ⚠️ 셀 카운트는 DOE 산술이라 영향 범위 확인 선행. F1ⓐ와 같은 축 | map-pm | T2 | 대기 |
| **F1** | 🟠 **회사 본딩맵 포맷 왕복 — COPY HEADER MODE + 파서 붙여넣기** (사용자 요청 2026-07-29, 스크린샷). **둘은 한 쌍이다**: 내보내기만 만들면 "쓸 줄은 아는데 읽을 줄 모르는" 포맷이 생긴다. 묶으면 **INV가 자명해진다 — 복사한 것을 그대로 붙여넣으면 격자가 동일하다**(한 줄로 양쪽 동시 채점). ⓐ **COPY HEADER MODE 체크박스**: 헤더(`TITLE` + 구역/자재 열 그룹 `Base(map id)·4B12·1H·AF_03·MID·MIDLOT_01·TOP·TOP_01`)와 보조표(`VALUE·COUNT·STACK·DESC`)를 함께 복사. **COUNT는 격자 집계**(실측: 노랑 18·빨강 129), **STACK·DESC는 표①(DOE) 선언** — 즉 집계와 선언의 결합이다. ✅ **확정(사용자 2026-07-29): COUNT는 원 안(inside)만 센다** — M4 유효 다이 맵이 들어오면 "원 안"의 정의가 원 기하→유효 다이 맵으로 바뀌므로 COUNT가 자동으로 따라간다. ⓑ **파서 붙여넣기**: 인제션의 파서(`parsers/`·`html_topology_parser`·std 폴백)를 **에디터의 새 진입점**으로 노출 — 새 파서를 만들지 않는다. 사용자는 실포맷을 HTML로 파싱하도록 인제션을 이미 세팅해 둔 상태. ⚠️ **인제션 경로는 DB에 쓰지만 에디터 경로는 파싱 결과만 돌려줘야 한다** — 붙여넣기가 곧 저장이 되면 게이트 4종을 우회하고 「쓰기 1회 확인」이 깨진다. 현재 에디터 붙여넣기는 6칼럼 TSV 계약이라 **헤더·열그룹이 있는 실포맷을 못 읽는 것**이 이 요청의 배경.
  ✅ **설계 확정 (사용자 2026-07-29)**: 파서는 **사용자가 커스텀해 등록**하고 **서버가 등록된 파서를 실행해 결과만 반환**한다. **기존 인제션 스크립트 재활용**, **config로 맵 테이블별 바인딩**.
  - **재활용 방법(스크립트 무수정)**: 붙여넣기 내용 → 서버가 **임시 파일로 기록** → **기존 인제션 스크립트를 그대로 호출** → 셀 반환 → 임시 파일 폐기. 스크립트를 한 줄도 안 고치고 인제션·붙여넣기가 같은 파서를 탄다. 사용자가 파서를 커스텀하면 양쪽에 자동 반영. ⚠️ **선행 확인**: 스크립트가 파싱만 하고 쓰기는 파이프라인이 하는가 — 스크립트가 직접 DB에 쓰면 그 경계부터 갈라야 한다.
  - 🔒 **요청은 「테이블」을 지목하지 「스크립트」를 지목하지 않는다.** 클라가 스크립트를 지명할 수 있으면 **등록된 아무 코드나 실행하는 통로**가 된다. 요청은 테이블만, 파서 선택은 서버가 config에서.
  - **선언은 하나.** 인제션용/붙여넣기용을 따로 두면 갈라진다 — 한 선언을 양쪽이 읽는다([[check-existing-primitives-first]]) | map-pm+server-pm+contract-keeper | T2 | 대기 (ⓐ→ⓑ 순, M4② 다음 map_editor 라운드) |
| **M4②** | 🔴 **유효 다이 맵 저작·등록 — 운영 차단 해소 (사용자 2026-07-29 "지금 운영해야 해서")**. ①(`valid_die_ref` 소비)이 착지했으므로 그 위에 **저작 경로만 가산적으로** 얹는다: 프리셋(원/사각)에서 **템플릿 생성** → 맵으로 편집(페인팅) → 저장 → 메타에서 `valid_die_ref` 지정 → 목록·재사용. **등급 T1→T2 강등, 근거 명시**: ②를 "프리셋 동작 자체를 바꾸는 것"으로 잡으면 T1이지만 **"새 저작 경로 추가"로 자르면 기존 맵 영향이 0**이다. 수용 기준 = **`valid_die_ref` 없는 맵은 오늘과 완전히 동일**. 원 기하 은퇴·메타 188건 이관은 ③(T1 유지)에 남긴다 | map-pm+server-pm | **T2** | **DOE 종료 즉시 착수** |
| 8.5 | **M4 — 유효 다이 맵 기반 전환 트랙** (사용자 승인 2026-07-28 밤, **mm 정렬 미사용 확인** — 손실 0 확정). 원 기하를 판정자에서 **생성기로 강등**: ①`valid_die_ref`(템플릿 맵 참조) 소비 — 가산적 공존 ②프리셋=템플릿 생성기 + E1/E2 침식(이웃) 기반 ③inside에서 원 은퇴 + 기존 메타 188건 이관 스크립트. 결함 계급 #11·#18·#20·마스크 트릭의 근원 제거. "유효 다이도 맵이다 — 마스크 편집이 곧 페인팅" | map-pm+server-pm | ①T2 ②③T1 | ①은 7b·7c·M3 묶음과 동반 |
| 9 | 🔵 **온톨로지 슬라이스 1호 — 자재 knob tag 추적** (사용자 로드맵 모델 2026-07-29). "자재의 knob tag를 잡으면 **공정 이력과 DT 이력이 엣지 트레이스만으로** 따라 나온다". 기계는 이미 있다(G1 materializer·`POST /graph/trace` 멀티시드 BFS·`trace.html`) — **비어 있는 건 매핑과 label 정합**이다. 슬라이스 범위: ⓐ knob tag 노드 승격 선언 ⓑ **테이프 label 분리**(lot=테이프 확정으로 지금 가능, #15 해소) ⓒ 슬라이스 테이블 `event_time` 교정 ⓓ dt_log 매핑 ⓔ trace 결과 검수. **전면 §7.5c·정책 엔진·M3 39만은 이 슬라이스에 불필요** — 다음 use case가 요구할 때 당긴다 | ontology-pm | T2 | 대기 (범위 확정 후) |
| 9b | **온톨로지 잔여 기반** (슬라이스가 요구할 때 당김): §7.5c node_class 전면 · 4대 탐색 정책 엔진(G2.5 전제) · DELETE 시 그래프 정리 정책(stale 엣지) · M3 맵 메타 39만 · G3 불량추론(PPR·heat propagation) | ontology-pm | — | 대기 |

**종결 (2026-07-25~28)**: 운영 강화 2차 `d56e7e2` · M2.6 DOE 통합 `cdcddee` · 문서 4분할 `52a668b` · 수집기 exec 스코프+`out=None` FAIL · 그리드 Ctrl+C 중복 분기 삭제 · C1 접근 통제(+경로 탈출·낡은 번들) `90e284f` · 재교정률 계기 `ec75d4c` · B4 롤백 드릴(30초 실측) · C3 백업(복원 드릴 2회) `b35bc9f` · U2 없는 키 라우팅 `280ebf0` · 문서 감사 후속(아카이브 7건·헬스 어휘) — 상세는 [히스토리](../history/README.md).

**✅ 사용자 결정 (2026-07-29) — 대기 4건 전부 해소**:
- **V1 계기 교체** — 재교정률이 아니라 **완료까지의 상호작용 횟수**(클릭·화면 이동)가 정본. 재교정률은 보조로 강등. SSOT §1 갱신 완료. **미구현이고 소급 불가** → 계측이 R1 UI 라운드보다 **먼저** 머지돼야 전/후 비교가 성립
- **`bonding_log` lot/slot = 테이프** — 코어 아님. 스펙 §7.5b DT/Tape 계층 확정. 파생: `Wafer` label에 섞인 `core_lot|core_slot`은 **테이프 위치**이므로 별도 label로 분리(#15), 해당 엣지는 DT 계층으로 retarget
- **재교정 인덱스 승인** — `python server/scripts/setup_db_performance.py` (CONCURRENTLY·멱등·무중단). 절차는 [POSTGRES_OPERATIONS §3.1](../guide/POSTGRES_OPERATIONS_GUIDE.md)
- **선언 없는 테이블 처분 허용** — 총괄은 **선언 제거만**(워처 부활 차단) 수행하고 물리 `DROP`은 사용자가 직접 실행(기존 규율 유지)

**🔁 로드맵 모델 (사용자 2026-07-29) — 얇은 수직 슬라이스 루프**:
> 온톨로지를 **작게 활성화** → 작은 use case 완성 → 그 use case로 온톨로지 **강화** → use case 추가 → 반복.
> **1호 use case = 자재 knob tag 추적** (공정 이력·DT 이력을 온톨로지 **엣지 트레이스만으로** 소환).
> 이 모델의 값: 기반 공사(§7.5c 전면·정책 엔진·M3 39만)를 **뒤로 미룰 수 있다**. 단 **두 가지는 슬라이스 안으로 딸려 들어온다** — ⓐ 테이프 label 분리(안 하면 trace가 엉뚱한 개체를 지목) ⓑ **슬라이스에 포함된 테이블의 `event_time`**(공정 이력의 *순서*가 틀리면 use case가 능동적으로 오도한다). 전면 교정은 불필요.

> 🧭 **핵심 가치 추가 (사용자 2026-07-29)**: **"작은 글씨는 없느니만 못하다."** 읽히지 않는 정보는 정보가 아니라 잡음이다 — 크기·대비·정렬이 안 맞으면 그 요소는 있으나 마나이므로, UI 판정에서 **가독성은 기능과 동급**으로 다룬다. 야간 감사가 측정한 10.9px 메시지·20×13px 버튼·placeholder 정렬 불일치가 모두 이 기준 위반이다.

> 🧪 **진행중 실험 — 선(先)계약 방식 (2026-07-29, 사용자: "효과 확인 후 헌장에 쓴다")**
> **동기**: 계측 라운드에서 QA가 HIGH 2·MED 3을 잡아 **수정 라운드 1회(≈40분)**가 발생했다. 귀속을 따져보니 HIGH·MED 5건 중 **3건이 이음새**(각 도메인은 자기 절반에서 정상), **2건이 총괄 지시 결함**(메커니즘만 주고 가치 순위 미제시 / 스코프 과다 절단)이었고 **구현 에이전트의 순수 누락은 LOW 3건뿐**이었다. 즉 "꼼꼼히"의 문제가 아니라 **아무도 왕복을 시험하지 않는 구조**의 문제다.
> **적용(다음 라운드 [7b+7c]+[M4①]부터)**: ① 지시서에 **실패하는 왕복 단언(INV-n)을 먼저** 준다 — `contracts/` 벡터 프리미티브를 쓴다(이번 라운드엔 있는데 안 썼다) ② 양 도메인 라운드는 **이음새 담당 1명** 별도 지정 ③ 지시서에 메커니즘이 아니라 **가치 순위**를 쓴다 ④ **이번에 뺀 스코프**를 QA 브리프에도 넣어 구현 전에 반박받는다.
> **성공 판정(사전 선언)**: 다음 라운드에서 **수정 라운드를 유발하는 이음새·불변식 계열 결함 0건**. 적대적 엣지·라이브 프로브 발견은 **실패로 치지 않는다**(QA 고유 영역이며, 이번 F1도 no-op PUT 실제 응답을 찍어야 보였다). n=1이라 방향 판단까지만.

## 🎯 현재 초점 (Current Focus)

> 완료된 트랙은 여기서 내리고 §최근 완료로 옮긴다. **지금 손이 가 있는 것과 바로 다음 관문만** 남긴다.
> 항목 형식은 **목표 / 할 일 / 문제** 세 줄. 경과는 히스토리와 보고서에 있다.

1. **🟢 DOE 존 모델(M3) — 양측 착지, 라이브 동작 중** (`b35bc9f`+`280ebf0`, 2026-07-28)
   - **모델**: 표① 값 정의(`COLOR·VALUE·STACK·DESC·1H·MID·TOP`) + 표② 자재 롤업(`MAT·MAP·KNOB·가용·사용·잔여`). `STACK` 숫자 하나가 층 구조 전부, 세 구역이 `1..STACK`을 구조적으로 채움. `dt_map`은 `STACK=1`·MID만. 자재 토큰 `lot[_slot][:BIN]`. 엑셀 6칼럼 TSV 계약(`tsv.js`).
   - **저장**: `pushMapData` 유일 기록자(참조 정의 1 + 호출 1). 검수는 보고만. 존 컬럼 4종 **물리 설치 완료**(`stack`은 **string** — number였으면 `'7.5'→7` 조용한 절단). 실 `replace_map` 저장 E2E 통과, `'0x10'` 원문 왕복 확인. 스위트 815.
   - **새로고침 생존**: 초안(DOE+셀) 10개 편집 경로 전부 + **마지막 맵 자동 재열기**(`map_editor_last_open`, 부팅 시 기존 LOAD 경로 재사용).
   - **할 일**: ⓐ 사용자 DOE 실입력(이관 없음 — 확정) ⓑ **U8** 가용 버튼(대기열, `bin_map` 선언 대기) ⓒ `map_doe`·`map_doe_source` 처분(승인 대기 — 부활 사건 포함).
   - **파생 계약**: `contracts/` 3종(304/71/82)을 pytest + node 하네스 양쪽이 읽는다. 계약 변경 = 두 구현 동시 이동.

2. **🟢 프로덕션 게이트 — 차단 4건 전부 해소** (2026-07-28)
   - **해소**: B1 프로세스 감시 · B2 헬스 라우트 · B3 워커 로그 배선 · **B4 롤백 절차(드릴 실측 30초)**.
   - **조건부도 착지**: C1 접근 통제(운영에서 401/403 발효, 워커 토큰 상속 실측) · **C3 백업(복원 드릴 2회 실행)** · C2는 M2.6 재읽기·거부로 완화(재등급 대기).
   - **남은 것**: `check` §의 config 트리 외 대상 — **PostgreSQL 덤프·`ingestion_workspace/`**(실제 복구불능 손실이 났던 곳)가 다음 백업 대상으로 명명됨.

3. **🔑 도메인 규칙 — `wafer_map_metadata`가 정렬의 유일한 기준**
   - 맵을 담는 모든 테이블은 **메타 등록이 전제**. 미등록은 정상이 아니라 **누락**. 정렬은 소스·타깃 메타 델타에서 유도하고 계측 결과도 메타에 기록한다. 셀 레벨 `grid_metadata`는 **폐기 스킴**.
   - **문제**: `bonding_map` 맵 키 **약 39만 vs 메타 9건** — 정렬이 사실상 9개 맵에서만 일한다. 나머지는 `화면기준` 칩으로 노출된다. → 대기열 7번(M3)에서 해소.

4. **⚪ 대기 트랙** — ⓐ **G2.5** §7.5c 탐색 정책 엔진 → LLM 도구 API ⓑ **enrichment 실전 규칙**(사용자 실 스키마 확보가 조건) ⓒ **Chain Replay R1**.

## ✅ 최근 완료 (Recently Done) — 2026-07-25~27 롤업

| 영역 | 요약 | 근거 |
|---|---|---|
| 문서 | **문서 관리 4분할 첫 실행** — historian(히스토리)·mapper(CODE_MAP)·keeper(리빙 문서) 동시 실행 후 auditor 적대 검수. 분리가 첫 회부터 값을 했다: mapper가 "해제 지점 3곳"(실제 2)·`WARN_*` 12(실제 14) 같은 **개수 주장의 오류**를, auditor가 `PRIMITIVES.md`가 **폐기된 재시작 설계를 가르치던 것**(매 착수 로드 문서라 심각)을 잡았다. 읽기 트리거 측정으로 49개 중 18개만 실제로 읽힌다는 사실도 확인 | `d411386`·`87cbf35`·`e6a0f44`·`52a668b` |
| 서버+클라/맵 | **오버레이 변환 일원화** — 정렬을 서버에서 받지 않고 클라가 `소스 메타 프레임 → 물리 → 타깃 현재 컨트롤`로 배치. 오버레이 전용 변환 코드 0줄(메인 로드와 같은 함수). 화면에서 변환을 바꾸면 오버레이가 따라온다. 검수 A(기하) 15개 공격 전패, 검수 B가 잡은 fail-open 2종 수정. UI 순증 0. 이슈 #18 종결 | `7d931dc`+`251dbfd` · [검수 A](../../agent_workspace/reports/QA_overlay_unify_geometry.md) · [검수 B](../../agent_workspace/reports/QA_overlay_unify_behavior.md) |
| 서버+클라/계획 | **M2-v2 「계획 = 그 맵 자체」** — `bonding_map` 열면 본딩 계획, stage는 열린 테이블에서 유도. `plan_id`·별도 계획 테이블 폐기, 정체성은 `(ref_table, map_key)`. 병렬 QA 첫 적용(A·B 동시) → 양쪽 NO-GO → A1(프레임 규격 좌표)·C1(DOE 전량 삭제) 수정 후 병합 | `da65a87` · [히스토리](../history/20260726_204344_m2_v2_plan_as_map_redesign.md) |
| 인프라/테스트 | **스위트에서 허용 실패 제거 — 414 passed / 0 failed** — 라이브 사용자 자산에 쓰던 테스트 2종 격리(`maps.json`, 워크스페이스 `config.json`). `.sample` config가 v1 잔재라 신규 환경이 안 뜨던 문제 수정 | `9a8ede8` |
| 서버/인제션 | **대형 파일 P1 — heavy 레인 분리 + 진행 가시화 + 재기동 경고, 라이브 드릴 PASS** — 크기 임계(기본 10MB, `ingestion_settings.json` 핫리로드) 라우팅·워크스페이스 FIFO 3중 보존·스윕 경로 포함, push 진행 스냅샷(`/admin/file-ingestion/active`)+admin File 탭 HEAVY 배지/경고 배너. 드릴 실측: **비차단 180배(2.3s vs 415s)·10만 행 유실 0·bk 중복 0·created_logs 정확 500건 절단·이벤트 루프 p50 3.5ms**. QUEUED 통지 역전·total_log_count 비대칭 후속 수정 완료. 테스트 278 passed(+27) | `4fd8ac9`+`8b0fd03` · [히스토리](../history/20260726_093100_large_file_p1_heavy_lane_and_live_drill.md) · [드릴 보고서](../../agent_workspace/reports/QA_p1_live_drill_report.md) |
| 서버+클라/맵 | **본딩 실험계획 M1(조회 전용)** — 역할 바인딩 config + `GET /api/bonding-plan/core-summary`(`server/bonding_plan.py` — align은 cell_to_physical 순수 인덱스 변환만, QA F1/F2 결함 지점 무참여) + map editor Info 패널(`bonding_plan.js` — 층 배정·수량/FAIL/조건 이탈 경고 3종·knob 비교·localStorage 초안) + fake 원천 2종(eds 180° align 실증). **rect 영역 선택 모드는 개발 중 폐기**(M2 값 페인팅 정본). 테스트 275 passed(+18) | `e6eabe4`+`24753d3` · [히스토리](../history/20260726_093200_bonding_plan_m1_info_panel_and_core_summary.md) |
| 설계/온톨로지 | **DT/Tape 계층 편입(스펙 §7.5b) + Universal Transfer Plan/DOE 관리 단위 확정** — bonding의 core lot/slot=실제 DT lot/slot, 전사 프리미티브 일반화, value=DOE 조건군 | `63ac0c3`·`437d6d5` · [히스토리](../history/20260726_093300_dt_tape_layer_universal_transfer_plan_design.md) |
| 클라/그래프 | **뷰어 stats 라벨 카드 클릭 → 노드 리스트**(빈 q+label 서버 리스팅 캡 200, 행 클릭 explore 연동) | `df63f3a` · [히스토리](../history/20260726_093400_graph_viewer_label_node_list.md) |
| 클라/그래프 | **뷰어 Connections 테이블 + 검색 시드 연동** — 노드 클릭=선택+관계 테이블(비중심은 depth-1 보강, 80행 페이지), 행 클릭 → 중심 재조회+URL push/popstate+검색바 반영, 패널 접기. ⚠️ 중심 이동이 클릭→**더블클릭**으로 변경(사용자 공지 권장) | `18218da` · [히스토리](../history/20260725_222215_graph_viewer_connections_table.md) |
| 스펙/그래프 | **§7.5c 정적/동적 노드 분류 + 4대 탐색 정책** 수렴(S→D 기본 금지·2단계 백본→ROI·EqpState 허브앤스포크) — 정책 엔진이 **G2.5 전제 조건**으로 승격 | `99c4cb6` · [히스토리](../history/20260725_222347_ontology_spec_static_dynamic_traversal_policy.md) |
| 서버/인제션 | **워크스페이스 config.json 폐지** — `table_name`/`std_parse`를 글로벌 table_config의 `workspace_name`/`std_parse`로 흡수(옵트아웃 핫리로드화 → F4 자연 해소), 신규 생성 중단+하위호환 읽기, QA 6건 반영(파일당 config 스냅샷·별칭 섀도잉/경로탈출 방어)·테스트 21건(스위트 229 passed) | `5fac5f0`+`20d6898` · [히스토리](../history/20260725_220619_workspace_config_deprecation.md) |
| 서버/그래프 | **온톨로지 G1** — graph_nodes/edges/graph_sync_state + 매핑 v2(description 필수, enrichment `RESOLVED_AS` 자동 승격) + materializer(증분 소비·QA H1/H2 provenance·retarget) | `6da2276`→`7c40a33`→`d130c65` |
| 서버/그래프 | **조회 API 5종** — stats/neighbors/search(뷰어) + trace/mapping-summary(G2, 공용 BFS 추출) | `c63b881`, `d8d109d` |
| 클라/그래프 | **그래프 뷰어 + 추적 리포트** — graph.html(BFS 동심원 캔버스)·trace.html(그룹+타임라인) + index 「🕸️ 추적」 진입점, 양방향 크로스링크 | `eea929d`/`f41ca3e`, `6c0a722`/`83507aa` |
| 클라/admin | **파이프라인 생애주기 5탭 IA 재편**(Overview/File/Chain/AutoUpdate/Enrichment, Code Editor 딥링크 공용 뷰, 구 해시 별칭 호환) — 라이브 검증 통과 | `7d02989`(소안), `3e599d2`/`387d987`(중안) |
| 서버/인제션 | **온보딩 완결** — std parser 폴백 + 워크스페이스 자동생성(`f90717f`) + 런타임 테이블 CREATE(#7, `6c447ee`) → "config 추가→리로드→즉시 사용" | [20260725_113212](../history/20260725_113212_std_parser_fallback_and_workspace_autoprovision.md), [20260725_170000](../history/20260725_170000_issue7_runtime_table_create.md) |
| 클라/테마 | **듀얼 테마(기본 라이트)** tokens.css SSOT + 다크 심화, 헤더 드롭다운 z-order 수정 | `765c7e5`~`cd3f90c`, `4229d9f`, `d48f25b` |
| 서버/체인 | 이슈 #0 종결 — outbox 지연·신뢰성(F1~F5·인라인 발사·웜업), 정상 31ms(SLO 100ms) | [task/chain_outbox_latency.md](../../task/chain_outbox_latency.md) |
| 서버 | 경합 수정 배치 1(C-1/C-2/C-3/C-5) + 감사 로그 미저장 수정(#6) | `4329c29`, `5fd8d24` |
| 전체 | **Enrichment Queue v1**(서버 dedup mapper + 컨베이어 + 참조뷰 + 결손 배지) — 스펙 Living 승격 | [20260725_130000](../history/20260725_130000_enrichment_queue_v1_complete.md) |
| 프로세스 | 코드맵+교훈 파일 체계(유지보수 doc-keeper 전담) · 기능 체크리스트 초판 · 에이전트 로스터 확장(qa-reviewer/doc-keeper/ui-designer) | `de79c50`, `d0c14a5`, `cbdc1e2` |
| 서버/체인 | **인시던트(21:29) 수정** — 체인 워커 created_logs 무절단(~50MB/6.5만 건) 전송 → :8080 이벤트 루프 GIL 동결 → 알림 타임아웃 연쇄. 발신측 500건 절단+`total_log_count`(C-5 계약 확장, `event_constants.py` 공용 상수). QA GO-WITH-FIXES(D-2 편승 적용) | [히스토리](../history/20260725_215500_chain_created_logs_truncation_incident.md) · [QA 리뷰](../../agent_workspace/reports/QA_chain_created_logs_truncation_review.md) |
| 서버/온톨로지 | **wafer_process lot/slot 확장**(사용자 config·핫리로드) — 수집기 lot_id/slot_no 기록, ProcessEvent props, enrichment 공정 이력 뷰 노출. 라이브 검증 통과(LOT-E\|25 분기 발화) | [보고서](../../agent_workspace/reports/Server_wafer_process_lot_slot_report.md) |

2026-07-24 이전 완료분은 [history/README.md](../history/README.md)와 [RELEASE_LOG](./RELEASE_LOG.md) 참조.

## ⏭️ 다음 단계 / 백로그 (Next / Backlog)

> 가동 중 트랙은 §현재 초점에 있다. 여기는 **대기열**이다.

**우선 순위 높음 (현재 초점 연동)**
- **대형 파일 인제션 — P1·P2 완료, P2 드릴 미실행, P3 미착수**
  - **P1** heavy 레인 ✅ 병합·드릴 PASS(비차단 180배, 유실 0). **P2** 체크포인트 재개 + sha256 dedup + #10 ✅ 병합·라이브 가동(`file_ingestion_checkpoints` 643행 실적재 확인).
  - **✅ P2 드릴 3종 PASS** (격리 환경 첫 사용, 2026-07-27). **D1 재개**: 30,000/100,000 지점에서 `taskkill /F` — 커밋 오프셋과 실제 행수가 정확히 일치(주입 예외가 아닌 실제 강제 종료에서 원자성 관측), 재개 후 10만 행·10만 고유키·**기대 키 누락 0**(`generate_series` 안티조인으로 "10만 행인데 다른 10만"까지 차단), 건너뛰기 **275ms** vs 재적재 ~219초. **D2 dedup**: 동일 재투입 1.22초 `SKIPPED` vs `__force__` 1,687초 `SUCCESS` — **1,383배** 분리. **D3 #10**: 비대칭 5+2로 총계 7(구 SET 의미론이면 2), 단일 타깃 대조군 1. **회귀 없음**: 이벤트 루프 11,371 샘플 p50 5.1ms·250ms 초과 0건, 체크포인트 UPDATE는 청크의 **0.0097%**. 보고서 `agent_workspace/reports/QA_p2_drills_isolated.md`.
  - **P3(미착수)**: 후단 backpressure(outbox 파일 단위 집계, 경합 배치 2·C-4와 통합) · PG COPY 벌크 경로(프로파일링 선행) · `batch_row_upsert` items 행 데이터 상한 · audit `old/new_value` 길이 상한(`crud.py:224-236` — 대형 텍스트 셀이면 500건 절단으로도 수십 MB 재발) · heavy 워커 수 설정화(heavy 간 직렬 해소, outbox 파도 증폭 주의).
  - 잔여 QA 후속: F2 라우팅 원자화 · F4 공유 큐 대기 · F5~F7. 운영 수칙: AUTO_UPDATE_GUIDE에 증분(delta) 산출 가이드.
- G2.5 서브그래프 직렬화 → G3(그래프 시각화 고도화, Neo4j 병행 타깃). 시간 범위 스캔용 엣지 인덱스(event_time)는 G2.5 쿼리 설계와 함께.
- **[신규 2026-07-26] Chain Replay(룰 재적용)** — 룰 변경 시 기존 데이터 재적용. 설계: 원천 keyset 재계산(그래프 resync 패턴) + 레이어링의 user 보호 + stale 소스 철회(H2-b 패턴 셀 버전) + dry-run 우선. 단계 R1(dry-run+적용)→R2(stale 철회)→R3(admin 위저드). 착수 전 확정: 매퍼 파일 컨텍스트 의존성·다중 룰 의존·enrichment dedup 별도 취급. P1 병합 후 R1 권장.
- map_split_registry(현재 초점 #2) — client-pm 착수.
- **[본딩 실험계획 — ✅ M1 완료(2026-07-26), 다음은 M2 = Universal Transfer Plan]** M1 산출물(Info 패널·역할 바인딩 config·core-summary·align 서버 단독 변환·rect 모드 폐기)은 [히스토리](../history/20260726_093200_bonding_plan_m1_info_panel_and_core_summary.md), 설계 확정(DT/Tape·전사 프리미티브·DOE)은 [히스토리](../history/20260726_093300_dt_tape_layer_universal_transfer_plan_design.md) 참조. **M2 골자 — Universal Transfer Plan 프레임워크**(사용자 확정): 모든 단계=전사 프리미티브 `(stage, target 맵 페인팅, assignments[소스, 소스 영역, 타깃 값(층/코어), 수량])`, 가용=총−fail류(역할 바인딩)−기전사(단계 전사 로그), 테이프 가용은 코어 fail의 DT-조인 투영으로 제외, 신규 단계=config stage 선언만(코드 불변). **영역 지정 정본 = 값 페인팅**(base 맵 값=층 번호, 코어 맵=사용 영역 — rect 모드는 폐기됨, 서버 region 계약은 M2 cells 모드용 존치). **관리 단위 = value(DOE)**: value ↦ {소스, knob/조건, 수량, 자연어 설명} — map_split_registry 직계 확장, SplitCondition=DOE로 온톨로지 정합(G3 "어느 DOE에서 불량 군집" 질의). M2 작업 항목: 역할 바인딩에 dt_log/dt_map 추가 + 잔여 계산 2단계(코어 잔여 vs 테이프 위 가용) + 계획 페인팅은 DT 테이프 맵 위 + 관리 테이블 2종(`bonding_experiment_plan`/`bonding_plan_layer` — localStorage 초안 승격) + 온톨로지 ExperimentPlan·PlanLayer·TransferEvent 일반화 + **by_eqp 장비별 align 적용·align 보정 모드**(시험 align 서버 변환 오버레이 + 확정 시 config 원자 저장 — `make_align_transform` 주입형이라 재사용 가능) → M3(실적 대조·중복 배정 감지·EDS 연동). M1 이월 잔재: total_chips는 실 운영에서 칩 레벨 total 테이블로 재바인딩 필요(현 config는 core_defect_map 풀맵 겸용 — escalation 승인분), 기존 bonding fake의 마스크 밖 (cx,cy) 미세 왜곡(미접촉). 착수 전 사용자 확인(잔여 2건): ①defect/EDS 원천 위치 ②실로그의 knob 형태.
- enrichment 실전 규칙(현재 초점 #3).

**[신규 2026-07-29] Enrichment 강화 3안 + 조인 vs 비정규화 검토**
- **①후보 1개면 판단이 아니라 확인** — `reference_views` 결과가 유일값이면 사람은 판단이 아니라 확인 중이다. 1클릭 확정, 또는 config 노브로 **최저 우선순위 자동 확정**(M3 `auto_register_map_meta`와 동형 — absent-only, 사람이 언제든 덮음). V1 점수로 `키n+마우스3` → `마우스3` → `0`. **오늘 착지한 계기로 효과가 바로 증명된다.**
- **②반복 판단을 룰로 승격** — 같은 패턴을 N번 풀었으면 체인 룰을 제안한다. ①③이 건당 공수를 낮추는 반면 **②는 큐 자체를 줄인다** — 시간이 갈수록 일이 줄어드는 유일한 방향이고, SSOT의 "사람이 푼 판단이 기계 규칙이 된다"가 실제로 성립하는 지점.
- **③-bis 【유력안, 사용자 제안 2026-07-29】 가상 조인 — 저장하지 않고 조회 시점에 붙인다.** 이건 **오버레이의 행(row) 버전**이다 — `/api/maps/overlay`가 이미 "다른 테이블 값을 저장 없이 선언된 바인딩으로 끌어와 현재 프레임에 투영"한다. 맵은 좌표로, 이건 `decision_key`로 조인할 뿐 **구조가 같다**([[check-existing-primitives-first]]). 오버레이의 교훈 3개를 그대로 승계할 것: **①선언만·유도 금지**(`derive_table_binding`이 첫 데이터 컬럼을 추측해 DECOY를 붙이는 것이 라이브 실증됨) **②바인딩 미해결 시 데이터 경로에선 거부**(상태값만) **③서버가 푼 결과를 서빙**(클라 재유도 금지 = 두 구현 방지).
  - ⚠️ **확장성 타협 불가**: 조인은 **SQL 안**에서. 파이썬 사후 결합은 뷰포트 가상로딩·키셋 페이지네이션을 깬다. 조인 컬럼 **필터·정렬도 푸시다운** 필수.
  - **가장 큰 파생 효과**: 미해결 행이 **분석가가 일하는 화면에 `미상`으로 그대로 보인다.** 여기서 **「읽기는 조인, 쓰기는 원본 테이블로」**까지 가면, 그 셀을 그리드에서 고치는 순간 `core_wafer_map`에 쓰이고 **같은 키 N행이 한 번에 해결**된다 → **자동 join과 Enrichment 컨베이어가 한 화면으로 합쳐지고 별도 페이지가 없어진다**(새 영역이 생기는 게 아니라 하나 사라짐 — [[ui-simplicity-first]]와 정합). ①(후보 1개 자동제안)이 얹히면 **클릭 1회**가 된다.
  - 🚧 **설계 미결**: 셀 하나에 **읽는 곳과 쓰는 곳이 다른 전례가 이 시스템에 없다.** 레이어링 계약(`{value,is_overwrite,priority_source}`)에서 어떻게 표현할지 + **그 컬럼이 남의 테이블 것임이 화면에 보여야** 한다(안 그러면 사용자가 어디에 쓰는지 모르고 쓴다).
  - ✅ **확인 완료 (사용자 2026-07-29)**: 분석가는 **Spotfire에서 REST API로 테이블 둘을 땡겨 직접 조인**한다. psql 직결 소비자가 없으므로 **DB 뷰 불필요 — API 라이브 조인이 정답**이고, API가 곧 서빙 계층이다.
  - 🔴 **이 확인에서 파생된 위험 2건 (측정 필요, 단정 아님)**: ⓐ **Spotfire 조인은 원문자열 비교라 7b 정규화를 모른다.** slot이 `number` 선언이면 저장은 `1`인데 토큰은 `LOT_01`로 들어오고, 이건 운영에서 실제로 메타 조회를 빗나가게 했던 사건이다(`canonical_key_value`가 그래서 생겼다). **분석가들의 조인이 지금 조용히 덜 매칭하고 있을 수 있다** — 조인이 덜 맞으면 에러가 아니라 결과가 그냥 적게 나오므로 아무도 모른다. → 실제로 표기가 섞여 있는지 측정할 것. ⓑ **inner join이면 미해결 행이 분석에서 조용히 사라진다.** 서버 조인은 **무조건 LEFT**, 미해결은 `미상`(행 소실 금지 — 정직한 열화 규율).
  - **설계 방향**: 새 엔드포인트 없이 **선언된 조인 컬럼이 `GET /tables/{t}/data`에 컬럼으로 딸려 나온다**(Spotfire는 테이블 하나만 땡기면 됨). 단 **경계 계약 변경**이므로 `/schema`도 같은 컬럼을 선언해야 하고(그리드가 `/schema`로 컬럼 구성), **읽기 전용·타 테이블 소유**임이 드러나야 한다.
- **③해결값 자동 join(물리 복제) — 검토 완료, 결론은 「보류」** (사용자 문의: 분석가들이 이미 수동 조인 중, 정규화 원칙과 충돌 우려). **증상 3개(조인 키 발견성·내보내기 누락·속도) 중 저장 구조를 바꿔야 풀리는 것은 없다.** 이 시스템은 `CellSource`+우선순위 서열이 있어 **갱신 이상 위험의 절반이 구조적으로 이미 없다**(복제가 사본이 아니라 레이어). 그러나 나머지 절반인 **철회(Chain Replay R2 stale 소스 철회)가 미착수**라, 물리 복제 시 틀린 판단 하나가 N행에 화석화된다. → **기록계는 정규화 유지, 비정규화는 서빙 계층에서**: **B(뷰)로 시작 → 느리면 C(materialized view)로 승격 → D(물리 컬럼)는 R2 착지 전까지 금지.** 착수 전 측정 2건: ⓐ `(core_lot, core_slot)` 인덱스 유무와 실제 조인 비용 ⓑ 동적 테이블 체계(`table_config` 런타임 CREATE)가 뷰 선언을 다루는가.
- **④결손 원인 분류** — "소스에 원래 없다"(진짜 일감) vs "소스엔 있는데 매핑이 떨궜다"(**일감이 아니라 버그**)가 한 큐에 섞여 있다. 후자를 손으로 메우면 파서 버그를 사람이 대신 갚는 것이다. 한 번 분류하면 그쪽은 영구 소멸.
- 우선순위 제안: **①②** > ③ > ④ (①②는 판단을 없애고, ③은 판단을 싸게 만든다)

**그래프 트랙 미결 정책**
- 행 DELETE 시 그래프 정리 정책(스펙 §8 — materializer는 DELETE 스킵, stale 엣지 잔존). `idx_graph_edges_row_ref`가 구현 기반.
- 운영 수칙: outbox 7일 purge보다 materializer 장기 정지 시 증분 유실 → `/api/graph/sync {"table_name":"all"}` 복구(문서화됨 — [event_driven_backend §4.3](../architecture/event_driven_backend.md)).
- search ILIKE 프리픽스 인덱스 한계(pg_trgm/text_pattern_ops 검토) · stats GROUP BY 캐시 — 그래프 대형화 시.

**admin 이관 목록** ([중안 보고서 §E](../../agent_workspace/reports/Client_admin_ux_mid_report.md))
- Enrichment 규칙 CRUD API · Chain rule CRUD API · 워크스페이스 생성/검증 API · 파이프라인별 "신규 추가" 위저드 UI · 헬스 시간창 집계 API(+파일 로그 서버 검색/정렬).

**관찰/저순위**
- [드릴 2026-07-26] :8080 이벤트 루프 지터 0.68%(100~846ms 단발, 동결 아님) 발생원 미규명 — 장기 프로파일링은 별도 태스크(qa-reviewer 위임 후보).
- [드릴 2026-07-26] 드릴 생성물 정리 대기(총괄 수행) — config 항목·물리 테이블 `hvy_drill_big`(100,008행)/`hvy_drill_small`·워크스페이스 2식·FileIngestionLog 5행: [드릴 보고서 §7](../../agent_workspace/reports/QA_p1_live_drill_report.md) 목록 참조.
- 워크스페이스 레거시 config.json **읽기 경로의 최종 제거 시점** — 총괄 결정 대기(현재는 하위호환 읽기 + deprecation 경고 가동, 실 워크스페이스 14곳 전수 무영향 확인).
- 레이어링 표시 정합 의심 1건: `priority_source: chain_ingestion`인데 표시 값은 system 소스 값(38320 vs 3832) — chain_ingestion 서열 등재(#5 배치에 동승 가능) 후 재확인.
- 재생성 소스 삭제 시 경고 표시 UX(파이프라인이 소스를 재생성하는 것은 레이어링 설계상 정상 — 비이슈 종결됨).
- main.py 셀 히스토리 라우트 이중 정의(~2020 사장) · `client2/src/counter.js` 템플릿 잔재 — 소규모 정리 후보.
- 재기동 첫 체인 579ms(수용) — 잔여 mapper 첫 쿼리 웜업.
- [라이브 검증 PASS 관찰 3건, 다음 서버 배치 동승 후보] ① pytest가 라이브 로그 파일 오염 → 테스트 로거 분리 ② created_logs 절단 발동 시 무음 → `truncated N→500` 1줄 로그 ③ wafer_process lot_id UndefinedColumn 1회(21:48, 컬럼 핫추가 과도기 — #9와 같은 뿌리 추정).
- wafer_process에 `lot`/`slot`(기존)과 `lot_id`/`slot_no`(신규)가 중복 공존 — 데모 테이블이라 수용, 실전화 시 하나로 통일 필요. Lot 노드 label 신설 여부도 미결(현재 props까지만).
- 루트 `task/` 대기: `cursor_based_pagination_pending.md`, `total_count_sync_pending.md`, `desktop_hybrid_wrapper_plan.md`.



---

## 🗄️ 보드에서 옮겨온 (구) 현재 상태 블록 (이관 2026-08-11)

> 보드가 894줄까지 자라 「지금 열린 것만」 규칙을 다시 어겼다. 아래는 날짜가 박힌 **옛 상태 서술**이고, 각 라운드의 정본은 [`docs/history/`](../history/README.md)에 있다. 삭제가 아니라 이동이므로 문장은 그대로다.

## 🔁 (구) 현재 상태 (2026-08-07 새벽) ― P3를 커밋 직전에 멈춰 세웠다 (이후 D3와 함께 착지)

> **착지·푸시**: `a14a098`(미선언 컬럼 → **라이브 500**) · `069b4e9`(dt→코어 축, **미배선**) · `e943e46`(클라 워크리스트 재요청). **미푸시 0.**
> **깨끗한 스위트**(QA 실행, 오염 아님을 mtime·md5로 자증): **`3 failed, 3105 passed, 12 skipped, 1 xfailed`**. 실패 3건 판정 완료 ― **P3 회귀 0건**(① 정당하게 낡은 단언 ② 기존 빈 키 결함 ③ 불안정).

**🔴 P3 커밋 보류 중 ― 사유 하나(M2).** `probed_identity`가 청크당 한 번 만들어지는데 `apply_batch_updates`를 부르는 **프로세스가 셋**이라, 다른 프로세스가 도중 커밋한 행이 낡은 증명에 안 보인다. 창 **~0 → ~3.7초**, 그리고 `business_key_val`에 **유니크 인덱스가 없다** → **조용한 중복 행**. 성능 2.12배를 위해 레이어링 코어에 중복 창을 여는 것은 가치 순서와 반대. **도달 가능성 실측 레인 투입됨**(ⓒ) ― 「도달 불가」와 「재현 안 됨」을 섞지 말 것. 구조적 답은 보드에 오래 열린 **D3(유니크 인덱스) ← D2(잉여 정리)**.

**P3 성과(격리 `assy_qa`, 이 워크스테이션)**: 796→**376초**(2.12×) · SQL **30.1만→1,200**(251×) · 레이어링 5시나리오 **바이트 동일**(행 수가 아니라 내용 대조). 천만 행 환산 **22.1시간 → 10.4시간**으로 프로파일이 제시한 천장(7~10시간) 근처. **저장은 아직 하나도 안 줄었다**(~50GB).

**⚖️ 쓰기 증폭 판정 (사용자 2026-08-07)**: ①③④ 진행, **② 접음**. ①은 **운영도 같다**는 제품 소유자 확인이 있어 「이 박스 수치」 단서가 떨어졌다.
- **①** `cell_sources` 셀당 층 → **행당 출처**, 충돌 시 셀당으로 승격. 실측 근거: 셀당 출처가 **1개인 셀이 99.7%**(8,813,793 / 층 2개 이상은 7,800, 0.09%). ⏸ `crud.py` 해제 대기
- **②** `x`·`y` 정체 컬럼 층 제거 → **사용자 판정으로 접음**
- **③** 최초 INSERT 감사 행 생략(옛값이 없다). 실측: `drill_big_100k.csv` → 감사 정확히 100,000행. ⚠️ 사람 편집 감사 **170,742행은 불가침**. ⏸ `crud.py` 해제 대기
- **④** 아웃박스 행당 1개 → **트리거 `row_id`+테이블만 싣고 파생은 본 표를 다시 읽는다(제품 소유자 지시: 「체인 리플레이처럼」)**. 🟩 **그 모양이 이미 셋 있다** ― 체인 리플레이 · `dt_map_mapper` 교정 경로 · `graph_materializer.resync_table(row_ids=[...])`. **라이브 트리거 경로만 페이로드를 먹는 예외다.** ⚙️ 레인 중
  - 실측(레인): 아웃박스가 **행당 2,108 B → 천만 행에 19.6 GiB**(보드의 10GB 추정은 낮았다). 접으면 **행당 27.2 B · 이벤트 1만 · 260 MiB**. 퍼지 한 사이클(5만)의 **1/5**
  - **날카로운 표현**: 하루 **120만 행이 배수의 정상 상태 상한**이고 그 위로는 **정상 상태가 없다**
  - ⚖️ 총괄 판정 둘: 사람 경로는 **행 단위 유지**(`request_outbox_mode` 기본 `per_row` ― `request_source`는 파일명이고, 행 수 추론은 **사람 맵 푸시가 원래 수천 행**이라 지켜야 할 경로를 접는다) · 실패한 청크는 **행 단위로 재확장**해 재시도(격리 입도 손실을 받지 않는다)
  - ⚠️ 이름 붙일 차이: 페이로드는 **시점 스냅샷**, 다시 읽기는 **현재 상태**. 삭제된 행은 **이름 있는 결과**여야 한다

**🟠 backpressure는 ④ 다음에 다시 잰다.** ④가 천만→1만이면 5만/시간 퍼지가 **12분**에 비운다 ― 관리할 압력 자체가 없어질 수 있어, 조절 장치를 먼저 만들지 않는다.

**🔴 D1 ― dt→코어 축의 배선 차단 항목** (QA-DT 실측): `_BINFP_MIN_SUPPORT = 3`이 **자기 주석이 말하는 실패를 못 막는다.** 이 박스 `core_wafer_map`은 `c_bn`이 2종이라, 잘못된 리비전 기준에 121다이 소스를 대면 **`matched=70`, `reason=None`**으로 답을 낸다(QA_MAP이면 ~750). 바닥이 발화하는 건 **최고 매칭 1~2인 장난감 픽스처뿐.** 호출자 0이라 지금 깨질 건 없으나 **배선 전 필수.**
그 외 QA-DT: `dt_index`가 `double precision`인데 `int()` 절단 → 서로 다른 원값이 같은 `k`로 붕괴 가능(레인은 **원값** 유일성을 확인 ― 술어가 틀렸다) · **126개 `dt_job` 중 120개가 `dt_index` 전 행 NULL** · `_residual_shift` 상한 사실 소실은 **이 박스 도달 불가(1,281 < 4,096), 운영 웨이퍼 크기에선 도달** · `.limit(cell_cap+1)`에 `ORDER BY` 없어 상한 초과 시 **셀 집합도 비결정적**.

**📌 대기열 5(거짓 불변식) 범위 확정** ― `069b4e9`은 **주석을 안 고쳤다**(`map_alignment.py:1256`이 여전히 「앵커가 틀린 것의 크기」). 🔴 **고칠 것은 라벨뿐이고 숫자 4는 건드리지 말 것**: 4는 참이다(8후보 최고점, **틀린 프레임**이 쥠, 88다이 바닥 기준). `map_overlay_config.json:92`의 **문턱 20 유도는 그 4를 바르게 쓰므로 살아남는다.** 사이트는 **9곳**이고 그중 둘이 gitignore 안(`map_overlay_config.json:17`·`:92`)이라 추적 소스 grep에 안 걸린다.

**🟠 cp949 콘솔이 로그 줄을 죽인다** (사용자 신고 2026-08-07). `—`(U+2014)를 만나면 그 줄 대신 트레이스백이 나온다. 실측: 소스의 해당 문자 **2,725개는 대부분 주석**이고, **콘솔에 도달하는 로그·print 호출은 72곳**(`directory_watcher.py` 21 · `transfer_plan.py` 8 · 나머지 19파일). 🟩 **고칠 자리가 이미 있다** ― `map_alignment.py:2327`의 **`_ConsoleSafeHandler`**가 「cp949가 못 그리면 치환해 **블록을 살린다**」를 이미 한다. **72곳을 고치는 게 아니라 이걸 공용으로 올려 워커 로깅에 물린다.**

---

## 🔁 (구) 현재 상태 (2026-08-06 밤) ― 정렬기가 운영에서 돌기 시작했다, 그리고 총괄이 회귀를 둘 냈다

> **미푸시 0.** 오늘 착지: `72cc8a9`·`8f40910`(무지개) · `55142ee`(1등·램프) · `18735da`(분모) · `f894a0c`(정렬기 자리) · `fac206c`·`17d8d00`·`4947a65`·`88ffc74`·`34b4a93`·`0f1b26a`(정렬기 서버).

**하루 종일 한 항이었다.** 사용자 신고 셋(화면 틀어짐 / 무지개 안 나옴 / 오른쪽 진행 1순위 미반영)이 전부 **좌표 두 개를 서로 다른 원점으로 비교한 것**이었고, 총괄이 그 사이에 엉뚱한 원인을 여럿 세웠다.

### ✅ 닫힌 것

| 증상 | 원인 | 커밋 |
| --- | --- | --- |
| 무지개 안 나옴 | `.me2-cell-rank { fill: none }`가 칠한 색을 매번 덮음. 렌더가 아니라 CSS | `72cc8a9`·`8f40910` |
| 「만점인데 어긋남」 | 화면이 `일치 ÷ 판별`로 나눔. **`판별 ⊆ 일치`**라 항상 N/N. 진짜 분모 `index_total`은 서버가 계속 실어 보내는데 아무도 안 읽었다 | `18735da` |
| 오른쪽 진행 1순위 무시 | ⓐ 판정기가 **앵커 적용 전** 좌표를 읽음 ⓑ 화면이 서버 1등을 버리고 자기 점수로 재선출 ⓒ **다이 단위** 문턱이 **걸음 단위** 판정을 매번 거부(구조상 마진 0) | `fac206c`·`55142ee` |
| 정렬기 캔버스 틀어짐 | 화면이 서버가 준 자리를 안 쓰고 `seatOf(frame)+shift`로 재조립. 프레임을 **페이로드에 없는** 필드로 만들었다. 312셀 중 **232셀** 이동 | `f894a0c` |
| shift 항상 (0,0) | 배치가 `anchor_ref + L·(cell−anchor_src)`라 `placed[i_min] ≡ anchor_ref`. **동어반복**이었다 | `34b4a93` |
| start 키우면 비례해 틀어지고 극단값이면 계산 불가 | 앵커를 `make_frame_transform`으로 잡음 → **한쪽만 start를 탐**. `_placed=None`도 같은 원인(거절된 후보는 키를 못 받는다) | `0f1b26a` |

**지금 형태** — `t = anchor_ref − anchor_src`, 양쪽 다 DB 생좌표. `L`은 `frame_linear_part`(phys·start 안 읽음). 확정 start는 `start_from_placement`(오라클 32/32). 사용자 스윕 0→100000 **바이트 동일**.

### 🔴 총괄이 낸 회귀 2건 ― 원인을 남긴다

**① `_residual_shift`를 적용한 것 (`4947a65` → `88ffc74`에서 되돌림).** 「중간부터 도는 부분 맵」은 **총괄이 만든 시나리오**고 실재를 확인한 적이 없다. 근거로 댄 실측표(126셀·117셀)도 **총괄이 심은 픽스처**에서 나왔다. 제대로 앉은 맵을 `(5,26)`으로 옮겼고, 사용자가 HEAD를 `ec8c0e7`로 옮겨 보고서야 드러났다.

**② `tf(x,y)` 복원 시 앵커까지 태운 것 (`34b4a93` → `0f1b26a`).** 「앵커는 생좌표 그대로」라는 판정을 레인에 전달해 놓고 정작 앵커 위치를 **start를 읽는 변환**으로 잡게 뒀다.

**공통 원인**: 사용자는 **운영 화면**을 보고 말하는데 총괄은 **자기가 만든 픽스처**를 보고 판정했다. 어긋날 때 자기 쪽을 믿었다. → [[reproduce-the-report-not-a-scenario]]·[[sim-config-is-not-evidence-about-production]]

**그리고 「없어서 0」과 「빠져서 0」을 같게 읽었다.** 사용자가 「유효다이 start 때문 같다」고 짚었을 때 레인 측정이 「start 기여 x 0, y 0」이라 기각했는데, 그 0은 **새 클라에서 그 필드를 아예 안 읽어서** 나온 값이었다. 사용자가 처음부터 맞았다.


## 🔁 (구) 현재 상태 (2026-08-06 낮) ― 확정이 맵메타까지 닿았고, 성능을 파다 데이터 유실을 밟았다

> **미푸시 0.** `372c538`(사용성 목록) · `3e96747`(확정→맵메타) · `b7a348f`+`670681b`(리팩터링 S2.2).

**🥇 확정이 `wafer_map_metadata`를 쓴다 (`3e96747`)** ― 사용자 판정 「빌린 phys도 써 / 유효다이맵 맞으면 phys도 맞는 거야」의 구현.

| | |
| --- | --- |
| 새 토큰 | **`confirmed`**, `declared`와 `assumed` 사이. **표식이 필요한 이유**: 이 유닛의 승자가 `rot0_front`인데 그건 **선언이 아예 없는 맵이 읽히는 모양과 동일**하다 ― 표식 없이 쓰면 아무도 손대지 않은 맵과 **바이트 동일**한 행이 남는다. 표식 둘(`phys_confirmed_from`·`frame_confirmed_from`), 사실이 둘이라서 |
| 격자 출처 | **채점이 돈 조합기**를 다시 부른다. 맵 자기 셀에서 재도출하지 않는다 ― 실측이 갈린다(자기 셀 **11×13 @ (2,0)** vs 기준 **13×13 @ (0,0)**). 재도출했으면 **말은 되는데 그걸 만든 판정과 어긋나는 행**이 적혔다 |
| `grid_y_invert` | **안 쓴다.** 후보 축이 아니므로 덮어쓰면 **확정된 rotation·side의 뜻이 바뀐다**(총괄 판정을 레인이 반박해 뒤집은 건) |
| 검증 | 스위트 **2799 passed / 9 skipped**, 변이 **14 주입 / 14 검출 / 생존 0** |
| **🟩 라이브 확정 완료** | QA가 **브라우저로 실제 확정**, 행 생성(`019fd3f9-…`, bk `dt_map_SYN-IDX-FULL-R90`, 07:08:59). **점검 10개 전부 PASS, 예행 예측과 모순 0.** 격자 **13×13 @ (0,0)**(기준의 격자) ― 같은 시점 그 맵 **자기 셀 bbox는 11×13 @ (2,0)**로 실측됐고 **안 적혔다**. phys 6개 전부 기준값, `grid_y_invert: indeterminate`, `declared`로 읽히는 축 **0**. **재확정은 갱신, 분기 아님**(673→673, 감사 로그에 옛값→새값). **확정 후 화면이 바뀐다** ― 빌린 기하 배너·「기하 가정」 줄 소멸(레인이 가장 경계한 「확정 전후 동일」은 **안 일어났다**) |

**⚠️ 개발 박스 상태 정정 (2026-08-06)** ― 보드가 들고 있던 두 문장이 이제 거짓이다.

- **`dt_map`/`SYN-IDX-FULL-R90`의 메타 행은 이제 있다.** 「빌림 제안이 보이게 일부러 지워 뒀다」는 **끝났다** ― 확정이 만들었다. 총괄 판정: **지우지 않는다**(확정 행이 증거로 더 값지다). 빌림 데모가 필요하면 다른 `SYN-IDX-*` 유닛에 새로 심는다.
- **🔴 8080은 오늘 기능이 없다.** PID 41096이 **01:31:56** 기동, `3e96747`은 **06:51:14**. 거기서 확정하면 **200이 오고 아무것도 안 써진다.** 레인 둘이 이 스큐에 각각 1분씩 「심각한 결함」으로 오독했다가 프로세스 기동 시각 대조로 빠져나왔다 ― [[single-red-in-a-shared-tree-is-a-hypothesis]]가 테스트 실패가 아니라 **버전 스큐**에 적용된 사례. **살아 있는 곳은 `:8123`**(clean HEAD + gitignored config, 같은 개발 DB). 레인들이 `server/`에 미착지 편집을 들고 있어 **8080 재기동은 보류**.

**🟠 확정 플로우에서 나온 조작자 결함 셋** (라이브 실측)

- ~~🔴 Enter가 안 먹는다~~ → **철회.** QA가 추측 대신 **계층을 분리**했다: 핸들러는 있고 의도적이며(`map2/main.js:1445-1470`, `takesEnter()`가 확정 버튼만 허용목록으로 제외 ― 드롭다운 안에서 쓰기가 닿던 것을 막으려 넣은 가드), DOM 키다운을 넣으니 `data-armed`가 **false→true**, Esc로 복귀. **결정적 단서: 네이티브 `<button>`의 Enter 동작도 같이 실패했다** ― 그건 브라우저 동작이지 앱 코드가 아니므로 **키 이벤트가 페이지에 도달조차 안 했다**는 뜻. 결함은 자동화의 입력 계층이었다. 총괄이 이걸 HIGH로 레인에 투입했다가 취소 ― [[green-proxy-is-not-a-green-claim]]의 반대 방향(**빨강 대리지표도 빨강 주장이 아니다**). 미검증으로 남는 건 OS 키보드→브라우저 한 홉뿐
- **🟠 오래 연 탭이 디스크에 없는 번들을 돌린다.** QA가 물려받은 탭이 삭제된 `map_editor2-BLMVQYMo.js`로 **서버가 제대로 채점한 유닛에 「채점 불가 · 미상」**을 그렸다. **신호 0**. 「채점 불가」는 없는 데이터 문제를 찾아 나서게 만드는 문구다
- **🟡 바닥이 섞인다.** 기준 A로 확정한 뒤 기준 B로 재채점하면 행은 A의 phys를 쥔 채 B의 격자를 빌린다. **공개는 된다**(`geometry_basis: assumed`, 판정이 `no_winner`로 물러난다 ― 답을 지어내지 않는다). 다만 `declared_frame_source: confirmed`가 **어느 기준으로 확정된 건지 이름을 안 댄다** → 남의 확정을 이 맵 것처럼 보여 줄 수 있다

**🟡 신규 ― `dt_log` 크로스스코프 채택: 보고 누락 (잠재. 성능 라운드에서 분리, 선행)**

> ⚠️ **총괄 정정 2026-08-06.** 이 항목을 처음에 **「조용한 데이터 유실」**로 적고 사용자에게도 그렇게 보고했다. **틀렸다.** 행 변경은 **감사 로그에 남는다**(3행, source `user`, 푸시한 작업자 귀속). 없는 것은 **보고**다 ― `scope`가 `deleted: 0`을 반환하고 `deleted_row_ids`가 비어 `batch_row_delete`가 안 나가서 **맵 B의 화면은 셀이 떠난 걸 영영 모른다**. 레인이 자기 발견의 등급을 스스로 낮춰 왔고 근거가 총괄 판단보다 나았다.

`_get_or_create_row`(`crud.py:1510`)가 캐시 미스 시 `get_row_by_business_key`로 떨어지는데, 이게 **`business_key_val` 하나로만 `.first()`** 한다. 맵 A로 push하면 **같은 업무키를 가진 맵 B의 행이 A로 재기록**된다 ― 맵 B는 자기 셀을 조용히 잃는다. 프로브에서 재현(`purged=0`, `is_new=False`, **동일 `row_id`가 (LOT_B,01)→(LOT_A,02)**).
📌 **차분 인식과 무관하게 오늘 도달 가능하고, P6 이전에도 그랬다.** 유니크 제약 없음(프로브 28개 중 0, 개발 DB 50개 중 0). 반경은 `dt_log` 하나 ― 다른 맵 테이블은 map key ⊆ business key라 구조적으로 불가.
📌 **총괄 판정: 성능 라운드보다 먼저 나간다.** 레인이 자기 게이트(`map_key_columns ⊆ composite_key_source`)를 **철회** ― 그게 **정확히 이 버그가 도달 가능한 유일한 테이블을 옛 경로로 보내면서 안전해 보이는** 안이었다. 대신 **선언 브랜치에서만 차분**(레거시는 현행 purge).
⚠️ **도달 가능성 실측 대기** ― 개발 DB엔 중복 업무키 0건이지만 [[this-box-is-not-production]]. `(dt_job, dt_x, dt_y)`가 재작업·재인제션으로 다른 `(dt_lot, dt_slot)` 아래 나올 수 있는지 **파서·config로** 답이 나와야 등급이 정해진다.

**🟠 `created_at`이 매 push마다 거짓** ― purge가 진짜 생성 시각을 파괴한다. 화면에 뜨고 복사·내보내기에 실리며 **Sort Latest가 기본값**이라 안 바뀐 재푸시 2만 셀이 진짜 새 것을 덮는다. **성능 부작용이 아니라 정합성 결함**으로 다룬다.

**🟡 이음새 ― 클라가 `confirmed`를 조용히 버린다** ― `map2/declaration.js`가 토큰 6개만 알고 `decode.js`의 `token()`이 그 밖을 `rejected`로 민다. 에러가 아니라 **누락**이고, **기존 계약 테스트는 심볼 5개만 고정해서 초록으로 통과**한다. 레인 진행 중(+`COMPUTABLE_TOKENS`, `contracts/map2_seam` 벡터).
📌 판정: `confirmed`를 `declared`에 **접지 않는다**(그 블록은 「적혀 있는 것」이고 아무도 안 적었다). 다만 **확정한 맵이 화면에서 「아무도 결정 안 함」으로 읽히면 안 되므로** 별도 가시 상태를 준다.

**🔬 변이의 세 번째 실패 모드 ― 부패(rot)** (`b7a348f`)

앵커가 **존재하고 유일한데** 겨냥한 코드가 밑에서 빠져나가 **아무것도 테스트하지 않게 된** 변이. `overlay_wafer_mm`이 S2.7용으로 예정됐던 변이를 **S2.2에서** 무력화하며 `20/21`로 생존했다. **존재도 유일성도 통과하고 생존 수만 본다.** [[a-repaired-mutation-proves-nothing]]의 형제 ― 초록이 「잡았다」가 아니라 「변이가 없었다」는 뜻인 자리.
📌 규칙 둘: **생존은 커버리지 공백보다 부패를 먼저 의심**한다. **앵커 조사는 호출 형태가 아니라 변이 텍스트에서 함수명을 grep** 한다(`map_spec_only_save`·`marker_shape_wafer_anchor`는 호출 없이 잘라 변이시켜 호출 조사에 구조적으로 안 잡힌다 ― 총괄도 이것 때문에 넷 중 둘만 커밋했다).

**도는 레인 다섯**: 업서트 Tier1 · 리팩터링 S2.6 · 하이잭 실측 · 클라 `confirmed` 토큰 · QA 라이브 확정 E2E. **`crud.py`·`main.py`는 Tier1이 쥐고 있고 뒤에 둘이 줄 서 있다.**

## 🔁 (구) 현재 상태 (2026-08-04) ― 리팩토링 선행조건이 서고 R1이 착지했다

> **미푸시 8건.** `b322267` · `c0a3715` · `2664004` · `5be96f5` · `db46525` · `2c2a777` · `efc4514` · `689ebb9`. **스위트 `1859 passed / 2 skipped`**(QA 실행). ⚠️ **`2c2a777`은 푸시 보류** ― 아래 참조.

**🥇 맵 에디터 리팩토링 ― 이음새 순서 확정 + R1 착지** ([DESIGN_TRACKS](DESIGN_TRACKS.md) 참조가 정본)

| | |
| --- | --- |
| 이음새 실측 | 함수 **235개(7,716줄)** · 모듈 전역 상태 **92개** → 이음새 **17개**. 🔴 **「서버 IO 먼저」는 기각** ― `fetch` 24곳이 이음새 10개에 흩어져 **전부 건드리고 ~100줄**을 얻는다 |
| **R1** `689ebb9` | `map_key.js` 158줄 신설 · `map_editor.js` **9,632 → 9,495**(−137, 총괄 재측정). 강제된 변경은 `export`와 `getMapIdFromMeta(metaDict, tableSchema)` **둘뿐**이고 인자명을 같게 둬 **본문이 바이트 동일**(텍스트로 잘라내는 하네스 3개 때문). **임시 export 0** |
| 오라클 | 하네스 22 + 계약 6의 **stdout 바이트 동일**(그 안에 좌표 단언 **18,042**). 7b 서버 대조 30벡터 PASS ― **그리고 실패함을 증명**(분기 끄면 7/30 빨강, 모듈 없으면 exit 2) |
| ⚠️ 규모 정정 | 총괄이 기준선을 **8,900줄로 잘못** 말했다. 실제 9,632. 실측 비율(제거 137 / 측정 LOC 97 = **1.41배**)로 R6 이후 **~7,800줄**이 남는다 ― 부피는 오케스트레이터 셋(**1,614줄**)에 있고 **이음새 목록에 하나도 없다** |
| 사용자 판정 | **ⓐ 8라운드 먼저, 오케스트레이터는 그때** ― 싼 자리에서 8번 옮겨 방법을 실증하고, 주변이 빠진 뒤의 `loadExistingMap`이 더 싸다 |

**🔬 선행조건이 첫날에 값을 냈다 (`b322267`·`2664004`·`db46525`·`efc4514`)**

- **초록 하네스에도 바닥이 필요한 이유가 실증됐다**: `push_gate` 단언 15개 중 4개를 지우면 하네스는 **exit 0 「11 passed, 0 failed」**로 완전한 초록인데, `efc4514` 전 러너는 **커버리지 27% 손실 위에 「every gated harness is green」**을 찍었다.
- 총괄이 커밋한 **포매터 재정렬(`c0a3715`)이 변이 앵커 2개를 깼고 새 러너만 잡았다.** 단언은 46/0 초록이었다 ― 빨간 것은 **「이 하네스가 결함을 잡을 수 있음을 증명하지 못했다」** 쪽. 수리는 앵커를 **재정렬 내성**으로(`onceRe`는 0회뿐 아니라 **2회 이상 매치해도** 죽는다).
- **부채 산문에서 수치를 걷어냈다** ― 러너가 바로 옆에 실측을 찍는데 산문이 「28」을 따로 들고 있었다(그래서 보드도 41을 옮겨 적었다). **규칙: `why`는 기계가 모르는 것만 ― 사유와 트리아지 상태, 수치는 절대.**
- 죽은 하네스 3개 원인 규명: `effort_instrument` 샌드박스 리프트 누락(**수리**) · `split_registry`가 `DEFAULT_LEGEND`를 찾는데 `95bf072`이 서버로 이관해 **소스에 0회**(보류) · `reposition_regime_probe` 라이브 캡처 인자 필요 + 단언 0(**자동탐색 밖으로**).

**⚠️ `2c2a777`(본딩 완화) 푸시 보류 ― QA GO-WITH-FIXES**

사용자 요청(가혹한 가용 조건 완화)은 맞게 구현됐고 가드 쪽·산술은 건전한데, **총량이 표식 없이 판정 화면에 도달한다**: `validate_plan`이 `remaining_reliable` 하나만 보는데(`transfer_plan.py:3290`) 완화가 그것을 `true`로 만들고, 응답(`:3412-3424`)에 `inactive_subtractions`도 경고도 없다. **8칩·6전사·5필요** → 전에는 `availability_unreliable`, 지금은 **경고 0의 `ok`**. 그리고 그걸 막으려고 만든 필드는 **`client2`에 읽는 곳이 0**이다.
📌 **총괄 판정: 차단이 아니라 표식.** 선언 안 한 현장은 그 데이터를 **아예 추적하지 않는다**(사용자 실사용: dt_map에 defect 오버레이 후 자체 차감) ― 차단 복원은 없애 달라던 그 가혹함이다. `validate` 응답이 **안 뺀 것을 말하고** 화면이 그것을 **읽게** 한다. 2레인 진행 중.
📌 **문서 0건 갱신** ― `DOC_OWNERSHIP:77`의 소유 문서 둘 다 미수정, `CONFIG_GUIDE:246`이 이제 거짓, 그리고 **`not_declared`가 `CONFIG_GUIDE:338`에 다른 어휘의 상태로 이미 존재**(이름 충돌).

**🧹 정리**: 워크트리 **12 → 1**, 브랜치 **14 → 2**(전부 `--force` 없이·`-d`로, git이 거부하게 두고). 미병합으로 보이던 어드민 브랜치는 **이미 착지한 작업의 껍데기**였다 ― `f8355fa`(07-25 20:50)의 결과물이 main `69262ef`(20:51)과 **바이트 동일**. 사용량 한도와 무관.

## 🔁 (구) 현재 상태 (2026-08-02) ― 트레이스 픽스처가 서고 1회차 채점이 끝났다

> **미푸시 0 · 트리 깨끗.** 시뮬 config를 픽스처 기준으로 재구성하고(`table_config` 13선언), 생성기·정답 파일·채점기까지 세웠다.

**🎯 1회차 채점 ― 픽스처가 목표한 실패를 첫 판에 노출했다**

|                                    |                                                                                                                                                                                                                                                         |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 계보 재현율                        | 정답**13.4%** · 오답 **38.8%** · 정직한 미상 **47.8%**                                                                                                                                                                              |
| 🟩**오답의 구성이 결론이다** | **오답 2,053건이 전부 「웨이퍼는 맞고 좌표가 틀림」** ― 계보 추적이 **엉뚱한 웨이퍼를 지목한 적이 0회**다. split·merge를 시점 기준으로 거스르는 **제일 어려운 부분이 이미 맞고**, 남은 실패가 **좌표 프레임 하나로 격리**됐다 |
| 🔴 거짓 확신                       | 대칭 부분집합에서**데이터가 결정할 수 없는 케이스에 확신 있게 답했다.** 픽스처를 만든 이유가 이것이고 1회차에 나왔다 ― 그 자리 `auto_confirm`이 `false`인 근거                                                                               |
| 정직성                             | 모호 62건 중**20건을 「미상」으로 답해 정답 처리.** 41건은 채점 불가(**입도 불일치**: 정답은 job별, 시스템은 설비·제품별)                                                                                                                  |
| enrichment                         | 드라이런**발화 확인**(`refused_reason: null`) ― 보드가 적어 둔 「침묵」이 닫혔다                                                                                                                                                               |

**➡️ 다음 한 수 = ④번 화살표를 재는 실험**: 사람이 **설비·제품 조합마다 한 번** 프레임을 판정하고 재현율을 다시 잰다. 오답이 전부 프레임 탓이므로 **13.4% → 52%대**가 나와야 한다. **「판정 몇 번에 재현율 얼마」가 이 시스템 명제의 첫 직접 증거**다.

**오늘 새로 드러난 것**

| #             | 항목                                                                                 | 한 줄                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **O4**  | 🔴**규칙이 디스크에서 유효한데 워커 안에서 죽어 있을 수 있다**                 | 체인 워커가 자기`TABLE_CONFIG`보다 **먼저** 새 규칙을 읽고 `source_table not registered`로 거절한 뒤 **재시도하지 않았다** ― 인제션 내내 옛 규칙으로 돌았다. 회복 경로는 재기동 또는 **2차 리로드**이고, 확인은 `chain_worker.log`의 `Synthesized N`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **O5**  | **앵커 밴드가 추론 ①을 안 가른다 (총괄 설계 오류)**                           | 후보 뷰가`track_in` + 단조 순위로 풀고 `core_wafer`를 **안 본다**. 밴드는 추론 ①이 아니라 **계보**를 가른다 ― 픽스처 설계에서 변수를 잘못 걸었다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **O6**  | **정직성 채점의 입도 불일치**                                                  | 정답은**job별** 대칭성, 시스템은 **설비·제품별** 판정 → 41건이 채점 밖. 둘 중 하나를 상대에 맞춰야 한다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **O7**  | 🔴**선언이 「구조적으로 불가능」한데 아무도 안 막는다**                        | 실증(사용자 발견 2026-08-02):`chain_rules`의 `dt_log_to_dt_map`이 `mapper_module: mappers.dt_map_mapper`를 가리키는데 **그 모듈이 디스크에 없었다** ― `.sample`만 있고 라이브 `.py`가 없다(`production_mapper`는 둘 다 있는 게 정상 모양). **config 재구성 · 전체 스위트 · 총괄 검수를 전부 통과했다.** ⚠️ 켜면 시끄럽게 죽는 게 아니라 **조용히 안 돈다** ― 워커가 로드 실패한 규칙을 거절하고 **재시도하지 않기 때문**(O4와 같은 뿌리). **할 일**: 로더가 로드 시점에 `mapper_module` 임포트 가능성을 검증하게. ⚠️ 「import 되는가」는 **공유 인터프리터에서 테스트 가능한 속성이 아니다**(다른 테스트의 `sys.path` 삽입이 보인다) ― 판정은 **자식 프로세스**로                                                                                                                                                                                                                                                                                            |
| **O8**  | ✅**`dt_log`가 맵 에디터에 뜬다** (`5e03f85`)                              | 목록의 게이트는 **`map_key_columns`**였다(`map_editor.js:1485-1494`가 전 테이블 스키마를 받아 그것으로 거른다). `map_overlay_config`의 바인딩은 그 판정에 **끼지 못한다**. 메타 등록기도 같은 조건에서 걸렸다. 실측(8080): 목록 등장 · 키 `dt_job` 자동 · 한 job이 13×13 **77셀**로 렌더(API 행수와 일치). ⚠️ **화면기준 프레임**이라 정렬은 아직 미판정                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **O9**  | 🟩**`bonding_log`의 DT 쪽은 오늘 그냥 된다** ― 설정·코드·DDL 0            | 에디터의**Advanced Column Mapping**에서 X→`dt_x`, Y→`dt_y`로 바꾸면 끝. 실측: 같은 56셀이 다른 위치로 재렌더(캔버스 해시 상이). **이것이 `DtCellClaim` 오버레이**이고 온톨로지에서 끊긴 간선을 눈으로 보는 자리다. ⚠️ 프레임은 **본딩 맵의 등록 메타 그대로**이고 좌표만 다시 읽는다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **O10** | 🔴**`dt_log` 코어 쪽은 같은 수를 쓰면 안 된다** ― 판정 기준이 나왔다        | >**두 번째 프레임의 키가 선언된 맵 키와 1:1인가?** 그렇다 → x/y 재지정이 두 번째 뷰의 전부. 아니다 → **다른 맵 정체성**이라 x/y만 바꾸면 **조용히 겹쳐 쌓인다.** 실측: `bonding_log` bond↔DT는 **1:1**(120↔120, 충돌 0) · `dt_log` dt_job↔core는 **아님**(한 job이 코어 웨이퍼 **2~12개**를 섞는다). 그래서 `dt_log`에 그대로 하면 **77행이 58셀**로 그려지고 **19개가 서로 덮어쓰며 셀 수 말고는 신호가 없다**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **O11** | ✅**사용자 판정 2026-08-02: 코어 쪽 뷰는 「표기 정규화」로 간다**              | 후보는 둘이었다 ―`core_wafer`(깨끗, **2,491행**) vs `(core_lot, core_slot)`(**7,440행**, 더러움). 후자를 택하되 **정규화 선행**. 🔴 **더러움의 정체**: `core_lot` **766행에 `_`가 들어 있는데** 맵 키 파서(`map_overlay.map_key_parts`)가 `_`로 쪼갠다 → `CL_2601_001_09`가 `core_lot='CL'`+`core_slot='2601_001_09'`로 분해돼 **셀 0개**. (`dt_job`이 안전했던 건 **키 컬럼이 하나라 문자열 전체를 먹기** 때문) `_`→`-` 접기로 철자 30→15, 쌍 369→151. ⚠️ **그래도 남는 것**: 8,700행 중 **1,260행(14.5%)에 `core_lot`/`core_slot`이 아예 없다**(`core_wafer`만). 정규화로도 안 덮인다                                                                                                                                                                                                                                                                                                                                              |
| **O12** | **빠진 매퍼를 아무도 못 잡는 이유**                                            | 워머업 임포트(`chain_ingestion_worker.py:630-639`)가 **비활성 규칙을 건너뛰고**, 실패해도 로그 남기고 **설계상 계속한다.** 그래서 꺼진 규칙은 없는 모듈을 무한정 가리킬 수 있다. **제안(미착수)**: `chain_rules.json`의 모든 `mapper_module`을 임포트하고 `mapper_function`이 callable인지 단언하는 테스트 ― ⚠️ 자식 프로세스에서(O7 참조)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **O13** | 🔴**테스트 스위트가 라이브 config를 쓴다 (16-ⓐ 가족)**                        | pytest 도는 동안**운영 워처가 `wscfg_test_*` 가짜 테이블에 대해 에러를 냈다** ― 몇 초간 **돌아가는 시스템이 존재하지 않는 테이블을 믿었다.** 파일은 사후 정상(13선언 확인). 기존 결함                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ~~**M1**~~  | ✅**해소 `98b48e9`** — 하네스는 `client2/tests/startxy_probe.mjs`에 있고 인자 없이 돈다(기본값=라이브 소스). 63/63, 수리 전 20적색                | 같은 커밋이 **원래 수리의 결론을 뒤집었다**: START를 못 읽는 행은 이제 거절이 아니라 **좌표계 선택 모달**로 간다 ― 진단(조용한 0)은 맞았고 결론(거절)이 틀렸다. 🔬 **부재의 철자가 둘이고 결함도 둘이다**: 키 부재는 `NaN`으로 시끄럽게 무너지고 `grid_start_x: null`은 `Number(null)===0`으로 **44셀을 전부 다른 다이에 조용히 앉힌 뒤 성공을 보고한다.** `declaredNum`을 맨 `Number()`로 바꾸는 변이에서 **키 부재 케이스는 전부 통과**하고 null 케이스만 죽는다 ― 픽스처가 시끄러운 쪽만 쓰면 변이가 안 죽는다 |
| **M4**  | 🔴**고른 프레임이 선언과 바이트 동일하게 저장된다** (실측 2026-08-05, 수리 라운드 진행 중) | 모달에서 고른 프레임이 `pushMapData`로 `wafer_map_metadata`에 **표식 없이** 들어간다 ― 「규격 없음+⚙️현재 패널」이 쓰는 행과 「원래 선언된 맵」이 쓰는 행이 **바이트 동일**하다. `auto_registered`는 이 구멍을 안 막는다: `geometryIsAutoRegistered()`가 `physChipX/physChipY` 데이터셋을 읽고 `physDeclaration`이 `chipX/chipY`에서만 참조하므로 **프레임 절반에는 출처 채널이 아예 없다.** 곁가지: 빈 START 박스가 `parseInt('') \|\| 0`으로 **0을 영속화**(실측). ⚠️ 수리는 **쓰는 쪽**이다 ― 읽는 쪽은 관대해도 된다(그게 모달의 존재 이유). 🔗 술어가 `frame_declaration_harness.mjs:137`에 **map2 `declaration.js`의 오라클로 전사**돼 있어 양쪽이 같이 움직여야 한다 |
| **M5**  | 🟠**레거시가 폐기된 셀 단위 `grid_metadata`로 폴백한다**                     | `resolveDeclaredGridMeta`(`map_editor.js:5395`). 실측 census: `wafer_map_metadata` **668행 전부**가 13키를 온전히 갖는다 → 부분 선언은 그 표가 아니라 **폐기 스킴**에서 온다. 사용자 신고의 유력 출처. **판정 필요**: 폴백을 떼느냐, 두되 표시하느냐. ⚠️ **운영에서 프레임을 잃는 맵 수를 먼저 재고** 정한다 ― 이 박스 숫자로는 못 정한다 |
| **M6**  | 🟠**Map Editor 2 웨이퍼 마스크가 없다** (`d4e0fed`로 장식 원 제거)          | 종전 원은 `r="86"` 하드코딩에 **JS가 읽지도 쓰지도 않았다** ― 자리표시자 목업에 맞춰 그려진 장식이었고, `layoutFor`가 격자를 스테이지 전체(반폭 ≈99)에 채우므로 **모든 맵이 삐져나왔다.** 진짜 마스크는 `seating.insideMask`가 이미 실제 mm로 판정한다 → **맵 자신의 선언된 phys일 때만**, 격자와 **같은 스케일로** 그린다. 근거 없으면 안 그린다. CSS 규칙은 남겨 두고 DORMANT로 표시됨 |
| **M2**  | **`map_meta_registrar.synthesize_grid_meta`가 배치 bbox를 start로 쓴다**     | absent-only라 사용자 선언은 안 덮지만,**파일 둘로 나뉘어 인제스트된 맵은 첫 파일의 bbox가 영구히 박힌다**(`_known_present` 캐시가 두 번째 파일을 안 보게 한다). 그 맵을 열면 **모달도 없이** bbox start가 나온다 ― 사용자 신고 문구와 제일 가까운 경로. ⚠️ **「선언 없는 인제스트 맵의 start는 무엇이어야 하나」는 설계 판단**이라 빠른 수리가 아니다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **M3**  | 🔴**`start` 검증에 저장 좌표 왕복 오라클을 쓰지 말 것 (총괄 지시 오류)**     | 내가 그 오라클을 지시했는데**이 결함에 구조적으로 눈이 멀었다** ― `start`가 **배치자이자 판독자**라 자기 대조가 되어 **깨진 버전에서도 0**이 나온다. 올바른 축은 **셀이 앉는 물리 다이 키 집합** + **Push가 영속화하는 `grid_start_x/y`**(실측 41/46). 📌 「물리 키로 비교하지 말라」는 **재배치** 검증의 규칙이고 **start 검증은 정반대**다 ― 두 규칙을 섞지 말 것                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **N10** | 🟠**깨진 `fail_sources`는 아직 표식 없이 총량을 낸다** (총괄 판정 완료, 미착수) | `c09bb0e`이 **분류**는 고쳤다(깨진 선언이 「미선언」으로 오분류되던 것). 남은 것은 **표시**다: `fail_sources`는 `transfer_log`/`origin_log`와 달리 **매달 소스 이름이 없어** `missing` 상태를 걸 자리가 없고, 완화 이전에도 없었다 → `"fail_sources": null`이 **표식도 경고도 없는 숫자**를 낸다. `{}`(명시적 빈 컨테이너)도 같다(술어가 `key in block`이라 「선언됨」). ⚠️ **오타 난 config가 QA가 지목한 바로 그 결함을 그대로 낸다** ― 흔한 경로다. ✅ **총괄 판정: 규칙 하나로 덮는다 ― `inactive_subtractions`는 「차감에 기여하지 않은 역할 전부」를 담는다, 이유와 무관하게.** 부재든 깨짐이든 빈 컨테이너든 **차감이 안 일어난 건 참**이므로 목록이 거짓말하지 않는다. 그리고 **부재/깨짐의 구별은 `sources.<role>` 상태에 그대로 남으므로** 두 상태가 뭉개지지 않는다 ― 특례 둘이 아니라 규칙 하나 |
| **N8**  | 🔴**`datetime` expose 컬럼에 N7과 같은 크래시가 살아 있다** (QA 발견 2026-08-04)                | `_text_part`(`virtual_join_executor.py:351`)가 **숫자만** 검사한다. QA가 구현 자신의 비숫자 분기로 라이브 PG에서 `InvalidDatetimeFormat` 재현. 📌 N7과 **같은 결함의 다른 타입**이고, 고칠 때 「숫자만」이 아니라 **캐스팅이 필요한 타입 전부**로 가야 세 번째 타입이 또 안 나온다                                                                                                                                                                                                                                                    |
| **N9**  | **N7 계약이 잰 것보다 주장이 넓었다** (QA 발견 2026-08-04)                                | 계약이 SQL 대 `clean_str_value`를 채점하는데(테스트가 스스로 그렇게 적었다 `test_predicate_contract.py:779-783`), **페이로드는 원시 숫자를 싣고 브라우저가 JS 규칙으로 문자열화**한다. 실측(PG 18.3 + node): `5.5e-5` → SQL·CSV `'5.5e-05'` · 화면 `0.000055`. **`\|v\| < 1e-4` 전부 갈린다.** ⚠️ **회귀 아님**(종전엔 500) — 그러나 「모든 방언에서 일치」는 **측정되지 않은 주장**이었다                                                                                                                                                     |
| ~~**N7**~~  | ✅**해소 `5be96f5` — QA GO** (2026-08-04) | `crud.numeric_text_sql`(= `clean_str_value` 숫자 분기의 **SQL 쌍둥이**, 두 번째 철자 아님). 정수는 전 방언에서 INT 철자. 🔬 **SQLite가 타입 크래시를 「틀린 답」으로 위장했다** ― PG는 `DatatypeMismatch`, SQLite는 화면 `3` vs 검색 `3.0`. `f9289f6`(pysqlite가 SELECT에 트랜잭션을 안 연다)에 이은 **두 번째 사례**. 잔여는 N8·N9. 옛 서술↓ |
| **(구) N7**  | 🔴**가상 조인이 `number` 컬럼에서 죽는다** (사용자 신고 2026-08-02, 진행 중) | `resolved_expression`이 `COALESCE(..., <unresolved_label>)`을 만드는데 **라벨은 문자열**이라 숫자 식에 못 들어간다. 📌 **왜 출하됐나**: server-pm 실측이 당시 **「숫자 expose 컬럼 0개」**였다 ― 축이 존재하지 않아 안 걸렸다. 픽스처에도 숫자 expose가 없으니 **먼저 하나 선언해 빨갛게 만든 뒤** 고쳐야 한다. 📌 **설계는 페이로드 쪽에선 이미 알고 있었다** ― `announced_columns`가 *「값의 정의역에 `unresolved_label`(문자열)도 들어간다」*고 적고 그래서 라벨을 스키마에 함께 싣는다. **나중에 붙은 SQL 경로가 그 추론을 물려받지 않았다.** **유력 수리**: COALESCE 전에 양쪽을 텍스트로 캐스팅(필터 경로가 이미 override를 항상 텍스트로 취급한다). 🔴 **그러면 SQL은 텍스트, 페이로드(`_resolve_one`)는 원값**이 되어 **검색하는 것과 보이는 것이 갈릴 수 있다** ― `contracts/blank_predicate`가 지키라고 만든 이음새다. **숫자 컬럼으로 두 철자를 서로 채점**해 일치 여부를 답해야 하고, **일치시킬 수 없으면 그게 수리보다 값진 발견**이다 |
| **X7**  | **`config_reference/`는 사본이라 조용히 낡는다**                             | 소유자·갱신 주기 미기록.`docs/guide/config_reference/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **X8**  | **`TRACE_FIXTURE_SPEC.md`가 고아 문서**                                      | 634줄인데`DOC_OWNERSHIP`에 행이 없다                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

📌 **총괄 오류 2건 기록**: ① `dt_log` 비즈니스 키를 코어 좌표로 잡아 **충돌**(한 DT 웨이퍼가 여러 코어의 다이를 섞는다) ② **맵 키와 행 키를 혼동**해 샘플 매퍼가 한 job의 모든 셀을 한 행에 뭉갰다. 둘 다 에이전트가 잡았다.
📌 **또 낡은 보드 줄에 속았다**: 「`config_watcher`가 원자적 쓰기를 감지 못한다」는 `46a67c7`(7-29)로 **이미 거짓**인데 내가 새 문서에 옮겼다. `dist` 건과 **같은 실수, 같은 날 두 번째.**

## 🔁 (구) 현재 상태 (2026-08-01)

- **이번 주 착지·푸시 완료**: 가상 컬럼 검색·필터 + CSV 추출 + `/schema` `join_resolved_columns` · 소급 어드민 화면(사용자 실기 확인) · 오버레이 = 맵 규칙 6(클라 mm 공간) · `blank_predicate` 계약. 소스 `cd3e0f4` · dist `52a1703`.
- **게이트**: 스위트 **1818 passed / 1 skipped / 0 failed**(총괄 실행) · 계약 **6/6 무분기** · 강제 하네스 **15/15 초록**.
- ⚠️ **운영 반영 시 서버 재기동 필수** — 소급 라우트·config resolve·가상 조인·`/schema` 키는 **새 프로세스라야 존재**한다. 재기동 없이는 404가 나고 **우리 버그처럼 보인다**.
- ⚠️ **가상 컬럼 검색은 UI에서 되지만** `bonding_log.wafer_id`는 사용자의 `table_config` 변경으로 **`virtual_only` = 읽기 전용**이다(그리드에서 수정 불가).

---


## (구) A1: 소급 어드민이 운영에서 죽어 있었다 (사용자 지시 2026-08-04) ― ✅ `cd80d66`으로 해소

> 🔴 **이 절은 「다음 1순위」가 아니다.** 정본은 위 [§🧭 개발 대기열](#-개발-대기열--사용자-지시-순서대로)이다. 2026-08-06까지 「다음 세션 1순위」 제목을 단 채 **이미 닫힌 항목**을 가리키고 있었다 ― P1이 실증한 그 실패(낡은 한 줄이 판정을 뒤집는다)의 재발. 아래는 **원인 기록으로만** 읽는다.

> **사용자 신고**: 「ADMIN AUTO CONFIRM 소급 실행 시 특정 파이썬 모듈에 `get_column_filter...`가 없다면서 실행되지 않음. **CLI로 실행시에는 정상 작동함.**」(요청 6)
>
> ✅ **해소 `cd80d66` (2026-08-04).** 🔴 **총괄 진단은 반증됐다** ― 순환 임포트가 **아니다**(`main.py`에 모듈 레벨 `enrichment_analysis` 임포트가 없고, `main`이 반쯤 초기화되는 일이 없으며, 맨 프로세스에서 `import main`하면 심볼이 정상이고, 그 심볼은 최근 40개 리비전에 계속 있었다). **재기동 가설도 아니다.**
> 🟩 **진짜 원인은 이름 가림(name shadowing)이다.** `enrichment_analysis.py:72`가 **한정 없는 `import main`**을 하는데, 5프로세스 중 **셋이 사용자 소유 디렉터리를 `sys.path[0]`에 넣고 지우지 않는다** ― `run_auto_update.py:217-218 / 487-488`(수집기마다 `auto_update/`), `parsers/directory_watcher.py:~514`(테이블마다 `scripts/`). **첫 수집기가 돌고 나면 `import main`이 경로에서 제일 앞선 `main.py`에 묶인다.** 스케줄러 모양으로 라이브 DB에 대고 미끼 파일로 **재현 완료**(같은 메시지, `main`이 미끼로 해석됨).
> 🔬 **양쪽 절반이 다 설명된다**: CLI는 그 디렉터리들을 **애초에 안 넣는다**. 웹 프로세스는 **우연히 면역**이다 ― uvicorn이 `sys.modules['main']`을 선적재하고 그게 `sys.path`를 이긴다. 그래서 **건수 라우트는 성공하고 그 뒤의 버튼만 실패**했다.
> ❓ **미확인 하나**: 오늘 워크스페이스에 `main.py`가 없고 로그에 트레이스백이 없어 **방아쇠 파일이 미증명**이다 ― `ingestion_workspace/<테이블>/auto_update/` 또는 `.../scripts/` 아래에 `main.py`를 두신 적이 있는지 **사용자 확인 필요**.
> 🔴 **수리보다 값진 것 ― 검증 환경의 구멍이 정확히 이름 붙었다**: `conftest.py:41`이 수집 시점에 `from main import app`을 해서 **2,000여 테스트 전부가 `import main`을 `sys.modules`에서 답하고 `sys.path`를 건드리지 않는다.** 즉 **스위트는 웹 프로세스의 해석 규칙만 재현하고 워커 규칙은 재현하지 않는다** ― 실패한 두 표면이 전부 워커 규칙에 산다. 테스트 2건으로 닫음(`server/` 아래 `import main` 전면 금지 규칙 + 미끼를 `sys.path` 앞에 두고 도는 서브프로세스 프로브).
> ⚠️ **`CODE_MAP.md:1219`가 「그 지연 `main` 임포트는 의도적이며 옮기면 안 된다」고 적고 있다 ― 이제 규칙의 정반대**다. 문서가 다음 사람을 이 결함으로 되돌린다.
> ⚠️ **반경을 먼저 잴 것**: `import main`을 함수 안에서 하는 자리가 여기 하나인지. 같은 모양이면 다른 소급 경로도 같이 죽어 있다.
> 🔴 **수리보다 값진 것이 이미 나왔다**: 이 기능은 **총괄이 「사용자 실기 확인」으로 닫은 항목**이다. 시뮬에서 되고 운영에서 안 됐다 ― **검증 환경이 프로세스 구조를 재현하지 못한다**는 뜻이고, 그 구멍은 이 결함 하나보다 넓다. 수리와 **함께** 「분리 프로세스에서만 나는 결함」을 잡는 축을 만들 것. [[sim-config-is-not-evidence-about-production]]
> 📌 뒤이어: **D1**(`proc_id` 날짜 충돌 ― 지금도 매일 소실) → **A3**(OFFSET/ORIGIN 이탈, 요청 1-f) → **R3** 이음새.



---

## 🗄️ 보드에서 옮겨온 (구) 현재 상태 블록 (이관 2026-08-11)

> 보드가 894줄까지 자라 「지금 열린 것만」 규칙을 다시 어겼다. 아래는 날짜가 박힌 **옛 상태 서술**이고, 각 라운드의 정본은 [`docs/history/`](../history/README.md)에 있다. 삭제가 아니라 이동이므로 문장은 그대로다.

### ✅ 정렬기 ― 우상단 설비가 돌아간다 (2026-08-07 마감, `c4eaffa`·`c959368`)

🔴 **정본 사실: 거울 절반 = 우상단 절반이다.** 순번 축에서 거울 다이 집합을 왼→오로 걷는 것과 원본을 오→왼으로 걷는 것이 **같은 순위**를 낸다. ⇒ **우상단 설비의 답은 처음부터 탐색 안에 있었고 이름만 틀렸다.**

🔴🔴 **그런데 「회전 shift 없음」은 거짓이다 ― 반회전에서만 참이고 사분회전에서 회전이 바뀐다 (레인 실측 2026-08-08, 격자 3형태 93·90·120다이에서 동일).**

| 지금 저장된 것 | 같은 것 | 회전 |
|---|---|---|
| `rot0_back` | `rot0` @ 우상단 | 그대로 |
| `rot180_back` | `rot180` @ 우상단 | 그대로 |
| **`rot90_back`** | **`rot270` @ 우상단** | **90 → 270** |
| **`rot270_back`** | **`rot90` @ 우상단** | **270 → 90** |

기제는 `client2/src/map2/seating.js:36-40`에 보인다 ― `if (quarter) rm = (visualRows-1)-r; else cm = (visualCols-1)-c;`. **사분회전에서 거울은 행 축을 뒤집고**, 그것이 좌우로 읽히는 것은 회전이 축을 바꾼 **뒤**다. ⚠️ 종전 문장도 「실측」이었다 ― 정사각 격자에서는 행 뒤집기와 열 뒤집기가 구별되지 않는다([[a-hand-check-lands-on-the-degenerate-case]] 4번째). **개명표를 「`_back` → 우상단, 회전 그대로」로 유도하면 4회전 중 2개에서 틀린다**, 그리고 그 틀린 표가 지금 이 보드·스펙 §2.4·`PRIMITIVES`·`frontend.md`·`README`·히스토리 1건에 적혀 있다. **교체 라운드의 0단계는 이 표를 고치는 것이다.**

| 항목 | 상태 |
|---|---|
| 후보에 우상단이 있나 | ✅ `alignment.sides` 선언 철회로 복원 |
| 채점이 맞나 | ✅ 화면에서 지그재그 확인(사용자) |
| 화면이 뜻을 말하나 | ✅ **`270° · 우상단 시작`**(`spellFrame`) + 카드 라벨. **저장 철자는 모노로 병기** |
| 저장 좌표 | ✅ **`side: back` 그대로가 정답** ― `front` 치환은 오라클이 두 번 기각 |
| 프리미티브 | ✅ `serpentine_index/rank(left_to_right=)`. **기본값 종전과 바이트 동일** |

**🔴 남긴 것 하나와 그 이유 ― 다음 세션은 여기서 출발한다.**
채점기가 보행 방향을 **자기 축으로 탐색하지 않는다.** 시도했고 **10개가 빨개졌으며 그게 옳은 빨강**이었다(`test_direction_narrows_a_tie_that_order_alone_cannot` 포함): **거울 프레임이 후보에 남은 채 보행 축을 더하면 두 축이 중복돼 모든 후보가 쌍둥이와 동점**이 된다.
⇒ **보행 축은 거울 절반의 「교체」이지 「추가」가 아니다.** 후보는 **회전4 × 시작2 = 8**(`side` front 고정)이지 회전4 × 면2 × 시작2 = 16이 아니다. 제품 소유자가 처음부터 「front 고정으로 좌상·우상으로 나눠」라고 말했고 **총괄이 더하기로 구현해서 확인됐다.** 되돌렸고 프리미티브만 남겼다.
**교체 라운드가 건드릴 것**: 프레임 id 어휘 · 저장(`confirmed_frame`) · 테스트 · 에디터 표시.

🎯 **문제는 어휘가 아니다: DT 설비마다 번호 시작 모서리가 다른데(좌상/우상) 채점기가 그 축을 탐색하지 않는다.** 지금 우상단 설비가 맞는 건 **거울 프레임에 우연히 걸려서**이고, 거울은 **셀 위치까지 바꾸므로** 안 뒤집힌 웨이퍼엔 틀린 답이다. ⇒ **할 일 하나: 보행 방향을 채점 축으로 탐색한다**(`left_to_right`, 호출자 0). 토큰 모양·마이그레이션·`alignment.sides`는 그 뒤에 따라오는 것이지 이 문제가 아니다.

⚖️ **판정 3건 (사용자 2026-08-08)**: ① **배치까지 front**(거울 배치 4개가 탐색에서 빠진다 ― 후보는 **4배치 × 2보행**) · ② 토큰은 **회전 + 시작모서리 분리 필드**(`candidate_id` 문자열이 바뀌므로 경계 계약, `main.js:2365`가 그 문자열로 매칭) · ③ **`alignment.sides` 은퇴**(⚠️ 이 키가 소유하던 `STATE_NOT_CONSIDERED` 「봤는데 졌다 ≠ 아예 안 봤다」 보고가 같이 사라지지 않게 할 것).

🔴 **①의 대가는 측정됐다 ― 결함이 아니라 판정의 결과다.** 순번 축은 무손실이지만 **값·점유 축에서 물리적으로 거울인 소스 맵은 승자가 안 나온다**(레인 실측: 오늘 `rot0_back` 93/93으로 이기던 단위가 front 전용에서 **승자 없음, 최고 87/93 오답**). 보행은 셀을 움직이지 않기 때문이다.

🔴 **총괄이 이 스레드에서만 다섯 번 틀렸고 다섯 번 다 사용자가 잡았다** ― 「start로 보정 불가」(축이 하나라 가정) · 「같은 연산 두 구현」 · 「하나로 통일하라」 · `side=front` 두 번 · 「축을 더하면 된다」. **전부 모델을 근거로 관측을 반박하려 한 자리**였고, 매번 재 보면 관측이 맞았다. 이 항목이 다음 세션에 주는 규칙: **화면이 멀쩡하다고 사용자가 말하면 그것이 데이터이고, 내 오라클이 빨간 것은 「무엇을 재고 있나」를 다시 물어야 한다는 신호다.**


### (구) 거울축 정본 = **에디터** (사용자 판정 2026-08-07) ― 위 두 층 설계로 대체됨

**「메타는 정렬기와 상관없고 에디터 기준으로 해야 한다.」** `wafer_map_metadata`는 **에디터가 읽는 물건**이고, 정렬기는 프레임을 알아내는 도구일 뿐 저장값의 뜻을 정하지 않는다. ⇒ **정본은 `map_editor.js:1706`의 웨이퍼 중심 축**(`xRot = -xRot`)이다. 이 판정으로 **「어느 축이 정본인가」를 재는 라운드는 불필요해졌다.**

**따라오는 것 둘:**
1. 🟩 **오늘 상태가 이미 맞다** ― `side: back` 저장에서 **에디터가 맞게 그린다**(우상단 지그재그, 사용자 확인). `confirmed_meta_for`는 손댈 것이 없고, 총괄이 `front`로 바꾸려 한 것이 **정확히 틀린 방향**이었다(`51e4068`에서 되돌림).
2. 🔴 **틀린 쪽은 `map2/seating.js:39`다** ― 격자 폭(`(visualCols-1)-c`) 기준이라 정본과 다르다. **정렬기가 저장하는 값은 맞는데 자기 화면이 비대칭 맵에서 틀리게 그릴 수 있다.** 대칭 맵에서 안 드러나 지금까지 못 봤다.
   ⚠️ **정정 (같은 날, 사용자 지적)**: 「`seating.js`를 웨이퍼 중심으로 맞춘다」는 **틀린 지시일 수 있다.** 「어차피 물리 좌표가 뭐든 **유효 다이로 오리진을 잡으니까**」 ― `start`가 이미 상자 기준이고(`seating.js:196` `c = x - startX + box.minC`, `box`는 **유효 다이의 외연**), 거울도 같은 기준이면 **물리 기하가 아예 안 들어온다**(오프셋·웨이퍼 지름 무관). 그러면 **에디터의 웨이퍼 중심이 오히려 예외**다.
   🔴 **그리고 「같은 연산 두 구현」이라는 총괄의 프레이밍이 틀렸다** ― 두 축은 **서로 다른 뜻에 대해 각각 옳다**: 「번호를 우상단부터 매긴다」(격자/상자 위의 규약)는 **상자 중심**이 맞고, 「웨이퍼를 물리적으로 뒤집었다」는 **웨이퍼 중심**이 맞다. 중복 구현이 아니라 **한 낱말에 두 연산이 얹힌 것**이고, 그래서 아무도 안 잡았다 ― 둘 다 자기 뜻에 대해 맞으니까.
   ⇒ **방향은 「하나로 통일」이 아니라 「둘을 남기고 이름을 가르는 것」**(거울 키 신설). 아침에 제안했다 접었던 그 안이고 이번엔 근거가 낫다: **축이 다른 이유가 밝혀졌다.**
   **다음 라운드가 먼저 잴 것 ― 판별식이 있다**: 두 축은 **격자가 웨이퍼 중심에 얹혀 있으면 같다.** 갈리는 조건은 `phys_offset_x ≠ 0` **또는 부분 맵**(격자 폭 ≠ 웨이퍼 범위). 그런 맵을 골라 **정렬기 화면 · 에디터 화면 · 서버 `cell_to_physical`** 셋이 같은 다이를 가리키는지 본다. 셋이 갈리는 자리가 곧 각 축의 정의역이다.


## (구) 2026-07-31 마감 시점 정본

한 줄 = 한 항목. 근거·경과는 위 본문과 `agent_workspace/reports/`에 있다. **여기 쌓지 않는다.**
이번 주 착지분은 `cd3e0f4`(소스)·`52a1703`(dist)로 **푸시 완료**, 미푸시 0.

### 🔴 데이터가 걸린 것 (먼저)

| #  | 항목                                         | 한 줄                                                                                                                                                 |
| -- | -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~D1~~ | ~~`proc_id` 날짜 충돌~~ | ✅ **종결 ― 사용자 판정 2026-08-04: 「그냥 지워, 어차피 시뮬환경이야」.** 잃던 데이터가 시뮬이므로 데이터 위험이 아니다 |
| D2 | **잉여 389행 정리**                    | 원인(고아 워커)은 제거됨. 삭제는**사용자 몫** ― 어느 `row_id`가 사는지가 `audit_logs`·`cell_sources`·`cell_overwrites` 귀속을 바꾼다 |
| D3 | **`business_key_val` UNIQUE 인덱스** | 「키당 1행」을 DB가 강제하지 않는다.**D2 정리 선행** + `CONCURRENTLY`(`bonding_map` 176만 행)                                               |
| D4 | **런처 싱글턴 가드**                   | 저장소 락파일로는**다른 디렉터리의 런처를 못 잡는다** ― 공유하는 것에 걸어야 한다(포트 또는 **DB advisory lock**)                        |
| D5 | **백업 부재**                          | PostgreSQL 덤프 ·`ingestion_workspace/` ― 프로덕션 게이트에 남은 유일한 🟡. 위 넷 중 뭐가 터져도 되돌릴 원본이 없다                               |

### 🟠 이번 주가 만든 빚

| #  | 항목                                           | 한 줄                                                                                                                                                                                                                                                                                                                      |
| -- | ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| N1 | **백필이 소켓을 안 쏜다**                | 실측:`chain_replay`·`enrichment_backfill`·`scripts/backfill_enrichment` **알림 호출 0건**(증분 경로는 58건). 소급을 **버튼으로 만들었으므로** 이제 필요하다 ― 39만 행에 행마다 쏘면 클라가 죽으니 **「개별 침묵 + 명명된 총계」** 형태. ⚠️ 발행 자체가 없어 **스윕이 회수할 것도 없다** |
| N2 | **겹친 칩 점을 legend 값 색으로**        | 총괄이 안 ⓐ로 판정해 놓고**수리 지시서에 안 넣었다**(내 누락). 지금은 레이어 단색이라 「몇 개·어디」는 보여도 「무슨 값」이 안 보인다                                                                                                                                                                              |
| N3 | **서버 문장 결함 3건**                   | `truncated: true`인데 그 사실을 말하는 문장이 없다 · `graph_orphans`가 **리터럴 `**삭제**`** 를 보낸다(INV-F9-8 위반, 화면에 떠 있다) · 확인 문구 속 영어 키                                                                                                                                                 |
| N4 | **`?cols=` 드롭다운이 조인 컬럼 누락** | 서버는 이제 받는데`api.js`가 `currentColumns`로 목록을 만든다                                                                                                                                                                                                                                                          |
| N5 | **`/schema` 계약 벡터**                | contract-keeper 설계 노트(`Contract_join_resolved_columns.md`)에서 재생성. **S8(알림을 지워도 쓰기가 성공하면 안 된다)** 이 핵심                                                                                                                                                                                   |
| N6 | **ⓓ enrichment 참조뷰에 가상 컬럼**     | 승인 범위 ⓑⓒⓓ 중**ⓓ만 안 들어갔다**(실측:`enrichment_candidates`·`enrichment_config`에 `virtual_join` 참조 0건). 아래 §설계 트랙의 전제이기도 하다 ― **사전엔 결론, 참조뷰엔 후보**                                                                                                                     |

### 🧭 핵심 가치에 난 구멍 (총괄 평가 2026-08-01에서 지목했는데 목록엔 없던 것)

| #            | 항목                                                                 | 한 줄                                                                                                                                                                                                                  |
| ------------ | -------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **V1** | **F4 ― 방금 만든 맵이 다음 화면에서 「없음」**                | dt map을 315행 저장하고 본딩맵을 다시 로드해도 롤업이`MAP X`를 유지, **`↻ 가용`을 눌러야** 풀린다. **가치 3 위반이고, 사용자가 방금 자기가 만든 것을 시스템이 부정하는 형태**라 신뢰 손상이 특히 크다 |
| **V2** | **F2 ― 화면 DOE 수량이 저장 불가능한 셀을 포함**              | `Fill All`이 `inside`와 무관하게 칠하고 `updateLegendCounts`가 그것까지 센다 ― 화면 숫자가 실제 저장량보다 **35~51% 많다**. **화면과 저장이 갈리는** 계급                                           |
| **V3** | **가상 조인 값에 계보가 없다 (가치 5 구멍, 이번 주가 넓혔다)** | 조회 시점 계산이라`cell_sources`에 못 쓴다. 왼쪽 셀의 **이력 타임라인이 비어 있는데 화면 값은 바뀐다** ― 「이 칸 왜 바뀌었지」에 그 화면이 답을 못 한다                                                       |

### 🧬 온톨로지 트랙 (2번 가치가 사용자에게 값을 증명한 적이 **아직 0건**)

| #            | 항목                                                           | 한 줄                                                                                                                                                                                                                          |
| ------------ | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ⚰️ **[2026-08-14 `2ec78b9` · R-2026-08-14-H] 아래 O계열 항목들은 «은퇴한 기계»에 대해 열려 있습니다** | 머티얼라이저·엣지 스토어·`/graph/*` 라우트가 전부 은퇴했으므로 **이 항목들은 «닫는» 것이 아니라 «질문을 원장 쪽에서 다시 묻는» 대상입니다**(O2의 「재도출이 교정이 아니라 누적」은 여전히 살아 있는 설계 질문입니다). ⚠️ **어느 쪽으로 할지는 총괄 판정**이라 문서 레인은 표시만 합니다 |
| **O1** | **슬라이스 1호 착수 ― 자재 knob tag 추적**              | 기계는 이미 있다(materializer·`POST /graph/trace`·`trace.html`). 비어 있는 건 **매핑과 label 정합**뿐. **딸려 오는 것 둘만** 단단히 하면 된다: 테이프 label 분리(#15) · 그 슬라이스 테이블의 `event_time` |
| **O2** | 🔴**materializer가 DELETE를 스킵해 stale 엣지가 남는다** | **목적 온톨로지 전략 전체의 전제다.** 지우지 못하면 재도출이 **교정이 아니라 누적**이고, `exp:` 폐기가 불가능해진다. 「온톨로지는 고치기 어렵다」를 **실제로** 참으로 만드는 유일한 결함                   |
| **O3** | **`not_reached`/`mapping_unavailable`을 큐로 연결**  | 그래프가 「내가 뭘 못 했는지」를 이미 이름 대어 말하는데**아무도 안 줍는다.** 이게 순환의 **④번 화살표**(발견 → 다시 보강)이고, 없으면 ①→②→③은 순환이 아니라 **직선**이다                             |

### ⚙️ 프로세스 (총괄이 지적하고 스스로 안 지킨 것)

| #            | 항목                         | 한 줄                                                                                                                                                                                                                                                    |
| ------------ | ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **P1** | 🔴**이 보드를 쪼갠다** | 「9천 줄 파일과 500줄 보드는 같은 병」이라고 적어 놓고**오늘 하룻밤에 또 늘렸다.** 비용이 실증됐다 ― **총괄이 낡은 한 줄(「dist 최신 `1dc761b`」)을 믿고 위험 판정을 반대로 냈다.** 인시던트 경과는 히스토리로, 보드는 「지금 열린 것」만 |
| **P2** | **경합 잔여**          | C-4(체인 큐 독점·HOL) · C-6(동시 upsert 락 순서) · C-8(런타임 ALTER 락 컨보이) · C-9(풀 합계>max_connections) · C-10(워처`.tmp` 필터 부재) · C-11(WS 직렬 전송). **오늘 중복 사고가 「동시성 가정이 얇다」를 실증했다**                    |

### 🟡 하네스·구조

| #  | 항목                                        | 한 줄                                                                                                                                                                                    |
| -- | ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| H1 | **러너가 「단언 0개로 빨감」을 구분** | `check_harnesses.mjs:78`이 종료코드 한 줄이라 **죽은 하네스가 부채로 위장된다**(이번 주 3건). 하네스당 `ASSERTIONS <ran> <failed>` + 러너 ~15줄 ― **리팩토링 선행조건** |
| H2 | **모듈 로드 스모크 1건**              | 하네스는 함수를**이름으로 잘라내** 채점하므로 `validDieListCache` 계급을 영원히 못 본다 ― **리팩토링 선행조건**                                                           |
| ~~H3~~ | ~~그리드에서 admin 초기화가 던진다~~ | ✅ **종결 ― 재현 불가 (client-pm 실측 2026-08-04).** 서술된 코드가 없다: `admin.js`는 **한 페이지에서만** 로드되고(엔트리당 스크립트 1개), 공유 모듈로 새는 admin 부트스트랩이 없으며, 그리드의 admin 조건 블록은 **이미 가드돼 있고 그 DOM도 존재**한다. 브라우저에서 **6개 페이지 로드 + `?user=admin` 강제 + `:8080` 번들**까지 에러 0. 하네스 샌드박스 해석도 확인(둘 다 초록). **근거로 걸린 「위 진단」이 보드에서 이미 사라진 상태였다** ― 낡은 줄이 결함처럼 남아 있었다 |
| **N12** | 🔴**`datetime` 컬럼은 쓰기 깔때기를 통과할 수 없다 (SQLite)** (N8 라운드 부수 발견) | `cast_value_by_type`(`crud.py:338`)에 **datetime 분기가 없어** 문자열 분기로 간다. PG는 들어갈 때 캐스팅해 주지만 **SQLite의 DATETIME 바인드는 `str`을 거부**한다 → 스위트 방언에서 datetime 컬럼은 쓰기 불가. 형제 결함: `auto_stage_database_outbox`(`database/database.py:72`)가 **진짜 `datetime` 속성을 직렬화 못 한다**(`TypeError`). 운영에서 안 터진 유일한 이유가 앞의 결함이다 ― **하나를 고치면 다른 하나가 드러난다** |
| **N13** | **`/schema`가 `datetime`이라 알리는데 값은 정본 텍스트다** (경계 계약) | 조인 해결된 `datetime` 컬럼의 값은 이제 `TEMPORAL_TEXT_FORMAT` 텍스트인데 `/schema`는 오른쪽 테이블의 **선언 타입**을 알린다. 📌 **같은 거짓이 `number`에도 이미 있다**(N7 docstring이 인정). 에이전트가 **일부러 안 고쳤다** ― `/schema`는 명시적 경계 계약이라 클라와 협의가 필요하다. 안 `text_resolved: true` 플래그 또는 고정텍스트 계열은 `string`으로 알리기 |
| **N14** | 🔴**「선언을 지우세요」 조언이 잠복 결함 하나를 도달 가능하게 만들었다** (QA 배치 게이트 발견) | 선택 역할인 **`val`을 지우면 전 행이 `FAIL`로 판정되면서 `reliable: true`** ― 실측 **0 → 144행**. 잠복이었던 이유는 `fail_sources`를 선언한 곳이 없어서인데, **이번 배치가 새로 낸 「지우는 게 맞다」는 조언이 그 경로를 연다.** 📌 총괄 책임: 유도 라운드와 가이드 재작성이 「required면 지워라」를 정본으로 만들었는데 **선택 역할을 지웠을 때 무엇이 꺼지는지는 dry-run이 말하지 않는다**(이미 기록된 모서리와 같은 뿌리). **가이드가 배포되기 전에 닫아야 한다** |
| **N15** | 🔴**`Time` 타입이 「아는 타입 계급」 안에서 하드 500을 낸다** (QA가 다섯 번째 타입을 만들어냄) | N8이 우주를 **정확히 넷**(String/Float/DateTime/Boolean)으로 단언했는데 QA가 지시대로 다섯 번째를 시도해 찾았다. 📌 **N7 → N8과 같은 계급의 세 번째 반복** ― 「이미 텍스트인가」로 뒤집었는데도 `Time`이 샜다면, **폴백이 안전하다는 주장 자체를 다시 재야 한다** |
| **N38** | 💤**휴면 (사용자 판정 2026-08-04: 「디시전키는 인제션 되는거라 바뀔일 없음」)** ― 아래는 유효하나 **일정에 올리지 않는다.** 오늘 100%를 본 것은 시험 삼아 한 번 바꿔 본 것이고 되돌려져 있다(실측: 두 규칙 다 키 빈 행 0·고아 0·정체 불일치 0). **N37도 같은 이유로 휴면** ― 붕괴는 `decision_key`가 `composite_key_source`의 **진부분집합**일 때만 나는데 오늘은 둘이 같다. ⚠️ **다시 살아나는 조건은 하나뿐: 판단키를 바꾸는 순간.** 그때는 이 항목을 먼저 읽을 것 ―~~🔴🔴 **`decision_key` 변경은 지원되는 연산이 아니다 ― 그런데 아무도 안 막는다** (조사 완료 2026-08-04, 사용자 신고에서 출발) | 마이그레이션도, 재구축도, 무효화도, 경고도, **이전 키의 기록도 없다.** 로더는 새 키를 **현재 스키마**에 대고만 검증하므로 「어제와 다르다」를 **구조적으로 알 수가 없다** ― 어제를 본 적이 없다. `/admin/config/resolve`(「내 설정이 먹었나」를 답하려고 만든 라우트)도 현재 파일만 본다. 🟩 **이 시스템은 이웃 도메인에서 이미 배웠고 적어 놨다**: `product_tables.py:105` 「라벨을 키에 넣으면 라벨 수정이 곧 re-key가 되어 하위 자재 행이 고아가 된다」, 그리고 `install_product_tables.py`에 **additive vs BLOCKING 드리프트 분류기**가 이미 있다. **인리치먼트만 그 가드가 없다.** 📌 수리안: 파생 테이블이 마지막으로 어떤 키로 만들어졌는지 서명을 남기고, 불일치면 **이름 붙여 거절 + 운영자 마이그레이션 지시**. ⚠️ `/enrichment/rules`에 거절 상태가 생기므로 **경계 계약 변경 ― 총괄 승인 필요**~~ |
| **N37** | 🔴**서로 다른 판단키 둘이 한 행으로 붕괴하고, 로그는 붕괴 전 개수를 보고한다** | 업서트 정체(`business_key_val`)가 `decision_key`의 **부분집합이기만 하면** 통과한다(`enrichment_config.py:305-319`). 실측: `(EQ4,T7,LA)`와 `(EQ4,T7,LB)`를 한 배치로 넣으니 매퍼 로그는 **「2 unique decision key(s) upserted」**인데 테이블엔 **행 하나**(`line='LB'`). `(EQ4,T7,LA)`는 큐에도, 테이블에도, 에러에도 **없다.** 즉 **「판단키 하나당 행 하나」는 불변식이 아니라 가정**이고, 그게 성립하기 전까지 `total`을 「keys」라 부를 근거가 없다. 📌 `composite_key_source == decision_key`를 요구하거나 업서트 키를 전체 판단키로 |
| **N36** | 🔴🔴**enrich 100%의 정체: 분자와 분모가 서로 다른 술어로 세어진다** (N30 정정 ― 내 초기 진단이 틀렸다) | 🔴 **내가 「`decision_key`가 산식 어디에도 없다」고 적었는데 틀렸다.** `buildBlankFilters`는 서버가 합성한 **`queue_filters`**를 먼저 쓰고(`enrichment_config.py:633-635`), 그건 **{판단키 전부 notBlank} ∪ {타깃 전부 blank}**다. 그런데 `fetchTotalAll`은 **필터를 하나도 안 건다.** 그래서 **새 판단키가 빈 파생 행은 분모에 세어지고 `remaining`에서는 빠진다 = 답한 것으로 계산된다.** 🔬 **재현: 데이터 변경 0, 설정 한 줄로 33% → 100%**, 워크리스트 0행인데 실제 미답 2건이 테이블에 앉아 있었다. 📌 수리: `fetchTotalAll`이 **같은 키 술어**를 싣게 ― 다만 이건 **깨진 테이블 위의 정직한 막대**일 뿐이라 N38이 먼저다 |
| **N35** | 🔴**Push 뒤에 한 셀 편집은 여전히 조용히 버려진다** (유효다이 가드 라운드가 지나가다 실측, 기존 결함) | 가드가 `!framePushed && frameTouched`인데 `scheduleCellDraft`·`persistLegend`가 **`framePushed`를 안 내린다** ― 선언 경로는 이번에 내리게 고쳤지만 셀 경로는 그대로다. 실측: Push 성공 후 칠하면 `{frameTouched:true, framePushed:true}` → **뒤로가기 확인 0회.** 파일에 이미 같은 패턴의 전례가 있다(`:10498` 오버레이 임포트). 신고된 회귀보다 **넓은 자리**이고, 「가장 작은 수리」 밖이라 별건으로 남겼다 |
| **N34** | 🔴**`dt_log_frame_attribution`은 정보를 0비트 싣고 페이지당 ~81KB를 쓴다** (즉시 조치 가능) | `eqp_frame_attribution`이 **4행**이고 그 4행의 `core_frame`·`dt_frame`이 **전부 NULL**이다. 그래서 8,700행 **전부**가 `미상`으로 해결된다 ― 질의 하나와 페이지당 81KB를 **아무 정보 없이** 쓴다. 짝 규칙도 나쁘다: `dt_lot_confirmed`/`dt_slot_confirmed`는 8,700행 중 **360행(4.1%)**만 값이 있다. 📌 `"enabled": false` 한 줄이면 되고 되돌리기도 한 줄 ― `server/config/`는 운영자 소관 |
| **N33** | 🔴**「오른쪽은 UNIQUE 인덱스를 탄다」가 실제 플랜과 다르다** (문서-현실 간극) | `virtual_join_executor` 모듈 docstring과 `main.py:776`이 둘 다 그렇게 적었는데, `EXPLAIN`은 **`Hash Left Join` + 오른쪽 `Seq Scan`**이다. 4·120행에서는 그게 옳은 플랜이라 **결함은 아니다** ― 그러나 **인덱스의 성능 역할은 지금 한 번도 행사된 적이 없고**, 큰 오른쪽 테이블을 계획하는 사람이 이 문장을 믿는다. 인덱스는 여전히 **정정성(팬아웃 금지) 제약**으로 유효. ⚠️ 같이: `/export`는 `CHUNK_SIZE=1000`이라 **1,000행당 규칙마다 질의 1개**, 8,700행 내보내기에 조인 질의 **18개**, 매번 오른쪽 해시를 다시 만든다 |
| **N32** | ✅**「data API가 느려졌다」의 정체: 바이트이지 질의가 아니다** (조사 완료 2026-08-04) | **SQL 총 실행 1.96ms / 요청 143.8ms = 1.4%.** 조인 질의 둘 합쳐 0.71ms. 테이블별 순위는 **조인 순위가 아니라 페이로드 순위**다 ― `dt_log` **5,503 B/ms**, `bonding_log` **5,540 B/ms**, 같은 테이블에서 `limit`만 바꿔도 5,507 B/ms로 같다. 🔴 **느려진 날짜는 특정된다: 2026-07-31**(`d70a33d`+`cd3e0f4`, 가상 조인이 페이지 경로에 들어옴) ― 서서히가 아니라 그날이다. 실측 전후: 병합 8.34→17.24ms, 본문 629,541→791,341B(**+25.7%**), 컬럼 20→24. 📌 **총괄 가설(N7/N8 텍스트 래핑)은 4중 반증**: 래퍼가 이 경로에 아예 없고(`execute_rule`은 원시 컬럼 SELECT), `column_text_sql`의 운영 호출처는 **1곳**뿐이고, 조인 키에는 적용되지 않으며, 적용되는 필터 경로에는 **애초에 탈 인덱스가 없다**(양쪽 다 `Seq Scan`, `ILIKE '%…%'`) |
| **N31** | 🔴**클라 `fetch` 82곳 중 타임아웃이 있는 곳은 1곳** (클라 레인 실측 2026-08-04) | 오늘 고친 그 한 곳뿐이다. 하나 더(`value_suggest.js:400`)가 `AbortController`를 넘기지만 **타이머가 없어 대체(supersession)용**이라 걸리면 같이 걸린다. 나머지 **80곳은 둘 다 없다.** 🔴 **오늘 사고와 같은 계급**(await 둘레에서 컨트롤을 잠그고 `finally`로 푸는 모양)이 **11곳**: `map_editor.js` 7 · `admin.js` 2 · `enrichment.js` 1 · `transfer_plan.js` 1. 응답이 안 오면 그 화면은 새로고침 말고 나갈 길이 없다. 📌 제안된 모양: 손으로 80개 컨트롤러를 만드는 대신 **공유 `fetchWithTimeout(url, init, ms)` 하나**, 값은 config. ⚠️ `fetchGridMetaFor`는 호출자가 넷이라 **일부러 안 건드렸다** ― 사고 중에 얹을 라운드가 아니다 |
| **N30** | 🔴**enrich 진행률이 판단키를 안 본다 ― 키를 바꾸면 100%가 된다** (사용자 신고 2026-08-04) | 실측(`enrichment.js:284-286`): `pct = (전체 행 수 − target이 빈 행 수) / 전체 행 수`. **`decision_key`가 어디에도 안 들어간다.** 그래서 키를 바꾸면 **이전 키로 채운 답들이 여전히 「채워짐」으로 세어져** 잔여 0 → 100%가 된다. 📌 주석·코드 불일치도 같은 자리: `totalAll` 주석은 「**유니크 키 수**」인데 코드는 `result.total`(**행 수**)이고 화면은 「N / M **keys**」라 적는다 ― 복합 판단키면 둘이 다르다. ⚠️ **미확인**: 키를 바꿨을 때 파생 행이 재생성되는지 옛 행이 남는지. 남으면 「진행률이 거짓말」, 재생성이면 다른 기제 ― **후자가 훨씬 크다.** 조사 먼저 |
| **N29** | **판단키가 빈 행이 대기열 앞을 막는다** (사용자 질문에서 파생) | 대기열 질의는 `target_fields` 공백만 필터하고 **`decision_key`는 조건에 없다** ― 판단키가 비어도 목록에 뜨고, 상세는 `(empty)`, 참조뷰는 **빈 문자열로 조회**해 근거를 못 찾는다. 컨베이어는 `row_id` 오름차순으로 **앞에서 소비**하므로 판단 불가 행이 앞에 있으면 계속 걸린다. 🟩 시뮬은 **0건**(120·4행 전수). 📌 방향: **숨기지 말 것**(상류 결함이 안 보이게 된다) ― 「판단키 없음 N건」 **명명된 총계** + 앞을 막지 않게 |
| **N28** | ⚠️**등록기가 절대 안 건드리는 테이블이 하나라도 합성 규격을 받으면 전 계획이 거절로 뒤집힌다** (서버 레인 발견 2026-08-04, 예방 대상) | 등록기는 **absent-only이지만 `map_key_columns` + 좌표 바인딩을 가진 어느 테이블에도** 적용된다. `core_defect_map` 로트가 규격 없이 한 번 들어오면 그 행이 `auto_registered`로 찍히고, **그 테이블이 `bonding_plan`의 정본 프레임**이라 이후 **모든 코어 계획이 `connected(align_unavailable)`**이 된다. 동작 자체는 옳으나 **넓고 갑작스럽고, 아무도 이 라운드와 연결짓지 못한다.** 지금은 `core_defect_map` 합성 0건 |
| **N27** | 🔴**pytest가 운영 로그에 쓰고, 라이브 config 디렉터리에 watchdog을 건다** (설정 폭주 조사가 찾은 진짜 결함 2026-08-04) | `server.log`의 「기동」 25,104건 중 **24,765건(98.6%)이 `TestClient(app)`**이다 ― pytest가 테스트마다 startup을 다시 돈다. 더 나쁜 것: **`Started config folder watchdog on '...\server\config'`가 `watcher.log`에 56,986회·`graph_sync.log`에 48,876회** ― 테스트가 **사용자의 라이브 config 디렉터리에 진짜 파일시스템 옵저버를 띄운다.** `conftest.py:72-85`가 `worker_heartbeats`는 이미 그 이유로 리다이렉트하는데 **`crud.CONFIG_PATH`만 안 했다.** 📌 대가: 이 로그로 하는 모든 포렌식이 오염된다 ― 「하루 1,300회 리로드」라는 유령 수치를 정확히 이게 만들었다 |
| **N26** | 🔴**런타임 `ALTER TABLE`이 아무 로그도 안 남긴다** | `models.py:481/487/489`·`521/532/535`가 `print()`인데 **stdout이 이 로그들에 안 잡힌다**(실측: `[Schema Sync]` 전 로그 통틀어 **0회**, 같은 함수의 `logger.info`는 134/788/307/581회). **1천만 행 테이블에 런타임 ALTER가 걸려도 시스템 어디에도 줄 하나 안 남는다** |
| **N25** | **핫리로드 진입점 둘이 「런타임 ALTER가 안전한가」에 서로 다른 답을 갖고 있다** | `config_watcher._reload:156`은 `sync_dynamic_tables_schema`를 **무조건** 부른다. `/admin/reload-configs` 경로인 `refresh_dynamic_models`(`models.py:609-636`)는 **일부러 안 부른다** ― docstring이 「락 호송(lock convoy)을 피하려 ALTER를 뺐다」고 적고 있다. **둘 중 하나는 틀렸는데 지금은 판정이 아니라 조용한 불일치다.** 리로드당 왕복 ~42회 중 28회가 이것 |
| ✅**설정 리로드 폭주는 실재하지 않았다** | 실측 19.65일: 진짜 운영 config 리로드 **80회(4.56회/일, 논리적 저장 42건)** ― 운영자가 설정을 고치는 딱 그 모습. 「24,780」은 **리로드 때 절대 안 찍히는 마커에서 뺄셈으로 만든 수**였다. 85~91ms 뭉치도 파일시스템 이벤트가 아니라 **pytest의 TestClient 셋업 간격**. 🟩 **그리고 소켓과 무관함이 확정**: `_reload`는 `TABLE_CONFIG`·`DYNAMIC_TABLES`·DB만 만지고 `manager`도 아웃박스도 subprocess도 참조하지 않는다. ⚠️ 다만 **가까스로** 그렇다 ― `supervisor_status.json` 등이 감시 대상 디렉터리에 **초당 1.5회 이상** 쓰는데, 전부 basename 비교 한 줄(`config_watcher.py:80`)로 버려진다. **그 `if` 하나가 느슨해지면 이 조사가 찾으러 갔던 그 사고가 된다** |
| **N24** | 🔴**계약이 마스크 경계를 채점하지 않는다** (캔버스 레인 발견 2026-08-04) | `isCellInsideWaferFast`의 판정을 `normDistSq > 1.0` → `> 1.02`로 흔들었는데 **`check_contracts.mjs`가 완전 초록**이었다. 같은 변이에 하네스는 **6개가 빨개졌고** 그중 `valid_die_head_parity_oracle`은 아예 죽었다. `circle_mask` 벡터에 **경계에서 1% 이내인 사례가 없다.** 📌 계약은 「양쪽이 같은 답을 내는가」를 재는 물건인데, **아무도 안 서 있는 자리에서 같은 답**을 내고 있었다 |
| **N23** | **오프셋 십자선이 모든 맵에 그려진다** (캔버스 레인, 사소하나 명백) | `map_editor.js:3289`가 `physConfig.offsetX !== 0 \|\| physConfig.offsetY !== 0`을 보는데, `getTransformedPhysicalConfig`가 돌려주는 필드명은 **`origOffsetX`/`origOffsetY`**다 ― `undefined !== 0`은 **항상 참**이라 오프셋 0인 맵에도 주황 십자선이 뜬다 |
| **N22** | 🔴**미전달 마커 계약이 심볼이 아니라 컬럼값 3개다** (code-mapper 발견, 지도에서 가장 취약) | `internal_event_client.py:135-142`가 `processed_chain=True / status="SUCCESS" / broadcast_at=NULL`로 쓰고 `chain_ingestion_worker.py:802-807`이 **정확히 그것으로** 거른다. **둘을 묶는 공유 심볼이 없다** ― 한쪽을 바꾸면 **아무 테스트도 안 깨지면서 마커가 영영 회수되지 않는다.** 방금 착지한 브로드캐스트 복구(`2aab7e2`)가 이 계약 위에 서 있다 |
| **N21** | **`VERDICT_PORT_CONFLICT`에 독자가 없다** (code-mapper 발견) | 상태 파일과 이벤트 스트림까지는 가는데 `main.py`·`health.py` 어디에서도 분기하지 않는다 ― 읽는 곳은 `test_duplicate_launcher.py`뿐. **선언은 있고 화면은 「포트 충돌」이라고 말하지 않는다.** 📌 총괄 부채: 이 수리를 내가 지시했고 「운영자가 무엇을 보는가」를 수용 기준에 안 넣었다. 같은 라운드의 `--preflight-only`도 **argparse가 없어 오타(`--preflight-onlyy`)면 조용히 전체 스택이 뜬다** |
| **N20** | ✅**격자 밖 셀이 쓰기 가능했다 ― 수리됨 `941060f`** (캔버스 레인, 기존 결함) | 격자 밖 셀이 `isMatrixCell` 띠를 통과해 셀 객체를 받고 있었다 ― 15x11에서 56개, 웨이퍼가 사각형을 넘는 9x7에서는 **36개가 전부 `inside`로 등록**돼 **칠해지고·세어지고·Push 페이로드에 실려** 맵이 선언한 적 없는 좌표에 저장됐다(엔드투엔드 재현 완료). 두 길목에서 닫힘: 렌더가 등록을 안 하고, 히트 테스트가 `null`을 돌려준다. 실측 후: 채움 셀 264~331개, **등록 0 · 칠해짐 0 · 저장 0** |
| **N18** | 🔴**`/schema` 한 번이 설정 싱글턴을 영구히 바꾼다** (doc-keeper 발견 2026-08-04) | `get_table_schema`(`main.py:1929`)가 `config.get("display_columns")`로 **`crud.TABLE_CONFIG`의 살아 있는 리스트 객체를 그대로 받아** 그 아래 루프가 `columns.append(sc)`로 시스템 컬럼 5개를 **붙인다.** 즉 `/schema`를 한 번 부르면 그 테이블의 인메모리 `display_columns`가 **설정 파일이 말하는 것과 달라진다.** 🔴 **왜 무해하지 않은가**: 그 같은 리스트를 **표준 파서가 헤더 검증·적재 집합으로 쓴다**(`directory_watcher.py:1767`·`std_parser.py:62`). 멱등(`if sc not in`)이라 무한히 늘지는 않으나, **파서의 필터 집합이 첫 스키마 조회 이후로 config가 아니다.** 하류 영향 미측정 ― 측정이 먼저다 |
| **N19** | **「선언했는데 물리 컬럼이 아직 없다」를 말해 주는 곳이 없다** (doc-keeper, 표기 정규화) | 로더는 config만 검증하고(그 라우트 계약이 DB 질의 0건), `apply_derivations`는 컬럼이 없으면 **예외도 로그도 없이 건너뛴다.** 그래서 `resolve` 리포트는 `effective`라 답하는데 값은 안 채워진다 ― 운영자가 켜는 순서를 뒤집으면 **아무 신호 없이 조용히 아무 일도 안 일어난다.** 가이드가 「`information_schema`를 직접 보라」로 메꿨으나 그건 문서로 코드 구멍을 덮은 것. 📌 수리안: `declared_but_not_materialized` 신호, 또는 `apply_derivations`가 테이블당 1회 경고(**첫 목격** 규약 그대로) |
| **N17** | 🔴**워크트리에서는 게이트가 항상 빨갛다 ― 그래서 진짜 회귀를 위장한다** (map-pm 발견 2026-08-04) | 유일한 BLOCKING 하네스 `undeclared_identifier_harness.mjs`가 `rolldown`을 import하는데 **어느 워크트리에도 그 의존성이 없다.** 즉 워크트리 레인은 **손대기 전부터** 그 항목이 빨갛고, 레인이 무엇을 부수든 게이트 숫자가 안 변한다 ― [[green-proxy-is-not-a-green-claim]]의 거울상(빨강 대리지표도 빨강 주장이 아니다). 1-c는 `node --check`로 대체했으나 **메인 체크아웃에서 재실행이 필요**하다. 📌 수리 방향: 「이 트리에서는 불가」를 **초록도 빨강도 아닌 세 번째 상태**로 말하게 할 것 ― 지금은 못 잰 것이 결함처럼 보인다 |
| **N16** | **O2의 「선언은 깨끗한데 비어 있는」 구멍** (QA, 잠복) | 쓸 수 있는 쪽에 **엣지 89,027개**. ⚠️ 다만 O2는 **dry-run 기본 · 미라우팅 · 미스케줄**이고 형제의 env 게이트도 꺼져 있음을 QA가 확인했다. **라이브 dry-run 1회 전에 닫을 것** |
| ~~**M5**~~ | ✅ **철회 ― I1·I3는 결함이 아니었다 (2026-08-04, 수리 라운드가 스스로 반증)** | 🔴 **총괄이 없는 결함에 수리 라운드를 태웠다.** 근거였던 `Map_push_path_scoring.md` §2d의 「I1·I3는 실결함으로 남는다」는 **그 보고서 자신의 오기**로, 같은 문서 §2(「모든 쌍의 초록 쪽은 하네스의 헤드라인 실행이다」)와 정면으로 모순된다. 실측(양방향 실행): **게이트 4는 오늘 거절한다**(`G1`=0요청·`G2`=1alert·`G3`=0confirm, 모든 대화상자보다 먼저) · **`grid_start_x:3, grid_start_y:-2`가 그대로 전선에 실린다**. 주입하면 각각 5·4건 적색 + `mutation did not apply (source drifted)` ― **넣으려던 결함이 이미 없다는 두 번째 경보**였다. 🟩 **지시서의 「막힐 인구를 먼저 세라」가 사고를 막았다** ― 그게 없었으면 **정상 push를 새로 차단하는 진짜 blast radius를 거짓 전제 위에** 만들 뻔했다(오늘 맵 선언 테이블 7개 전부 `map_push_ok` 미선언 = `confirm` 모드 0개). [[assertion-first-then-fix-round]] |
| **M7** | 🔴**`grid_start_x/y`의 가드가 둘인데 둘 다 「읽기」 방향이다** (M5 라운드의 진짜 발견) | `startxy_probe`는 **load 전담**이라 쓰기 결함을 못 잡고 **잡았어야 하지도 않다**(주입본에 29/0 ESCAPED, 헤더가 그렇게 명시). 진짜 구멍은 `standard_frame_origin_harness`다 ― 주석(`:312`)에서 `buildPushGridMetadata`를 **이름으로 인용해 놓고** `:319`에서 레코드를 **자기가 다시 타이핑**한다(`grid_start_x: parseInt(el.gridStartX.value, 10)`) → **자기 사본과 자기를 비교**하므로 **생산자가 0으로 죽어도 초록**(주입본 19/0 ESCAPED). R6 §2a의 INV-F2 인질과 **같은 형태를 반대편에서** 만난 것. 즉 쓰기 방향의 유일한 가드는 `ef153c0`이 만든 `G9` 하나다. **어느 하네스도 확장하지 않았다 ― 보드 판단 사안** |
| **M6** | 🔴**맵 키에 철자가 둘이고, 읽을 수 없는 값은 전부 한 키로 충돌한다** | 실측: `slot="007.5"` → Push는 `L1_7.5`, `getCurrentMapKey()`는 `L1_007.5`. **Push 가드가 변환 안 된 쪽을 써서 방금 Push한 맵에 「다른 맵입니다」를 띄울 수 있다.** ✅ **정본은 타입 변환된 쪽(`L1_7.5`)** ― 근거 둘: ① 같은 변환 dict가 모든 셀 행에 퍼지므로 **`7.5`가 실제로 저장된 값**이고 `'007.5'`는 **어디에도 대응하는 저장값이 없다**(`canonicalKeyValue`가 존재하는 이유인 7-28 결함 그 자체) ② 그 함수가 「비정수 숫자는 repr 유지」라고 스스로 적어 두고 `Number.isInteger` 가드를 지나칠 때만 `'007.5'`를 낸다. 🔴 **그러나 읽을 수 없는 입력에는 두 철자 다 틀리다** ― `slot='ABC'`는 **NULL을 저장하면서 `L1_NaN`을 조립**해 **모든 판독 불가 값이 한 키로 충돌**한다. **별개 결함이므로 수리 라운드에서 합치지 말 것.** ⚠️ `recordLastOpenMap()`이 같은 패널의 **세 번째 판독자**라 함께 움직여야 한다. ⚠️ `slot=int` 규칙이 이 경로를 상시 발화로 만든다 |
| **M4** | 🔴**마스크가 엉뚱한 다이에 앉는 것을 저장소의 아무것도 재지 않는다** (R5 발견 2026-08-04) | 실측: 마스크 평행이동(`shiftX = 1`)을 되살리면 ― 소스가 **262셀 중 21셀을 틀린 다이에 올린다**고 스스로 기록한 그 결함 ― **하네스 23개가 전부 초록**이다. 이유가 구조적이다: 하네스는 **「저장 좌표가 유지되는가」**를 단언하는데 **마스크 이동은 저장 좌표를 안 움직인다.** 📌 **N7과 같은 계급**(「축이 존재하지 않아 안 걸렸다」)이고, 이번엔 **리팩토링이 기대고 있는 바로 그 안전망의 구멍**이다. 리팩토링 라운드마다 「0셀 이동」으로 초록을 받아 왔는데 **이 축은 그 오라클 밖**에 있다. ⚠️ R5는 MOVE-DO-NOT-FIX 규율대로 **건드리지 않고 보고만** 했다 ― 옳다. **필요한 것은 수리가 아니라 축의 신설**이다 |
| **N11** | 🔴**가상 컬럼 검색을 쓸 UI가 없다** (client-pm 발견 2026-08-04, 총괄 판정 필요) | 실측: `index.html`에 **`#search-cols`도 `#global-search`도 없고**, `git log -S`로 **한 번도 있었던 적이 없다.** 두 `dom.js` 게터는 잔재다(55개 중 잔재 4개). 그래서 **`q`가 항상 비어 `?q=`/`?cols=` 기능 전체에 진입점이 없다** ― 오늘 그리드 검색은 AG-Grid 컬럼 필터뿐. ⚠️ **총괄이 지난주 「가상 컬럼 검색 착지」로 보고한 것이 서버 절반이었다** ― 검색 가능한 기능을 만들고 검색할 자리를 안 만들었다. **검색창 복원은 UI 판정**이라 에이전트가 단독 착수하지 않았다(옳은 판단) |

### ⚪ 문서·환경

| #  | 항목                                                  | 한 줄                                                                                                                                                                                                                                  |
| -- | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| X1 | **`mm은 일부러 비어 있다`가 거짓이 됐다**     | 클라에 mm 공간이**생겼다** ― 7파일 11자리(`CODE_MAP:127/1452`·`frontend:284/290`·`DOC_OWNERSHIP:54`·`LEAD_PM_HANDOFF:118`·이 보드·`README:39`) + 삭제된 거절이 `MAP_EDITOR_SPEC:590`·`CODE_MAP:1700`에 남음 |
| X2 | **열린 문제 #4·#18이 낡았다**                  | #18은 이미 수리됨(실측), #4는 #16과 모순 ― 스위트 재실행으로 닫는다                                                                                                                                                                   |
| X3 | **`SERVER_STARTUP_GUIDE`가 틀린 경로를 인쇄** | `scripts/` vs `server/scripts/` + 이 시스템이 안 쓰는 `uvicorn --port 8000`. doc-auditor 후보                                                                                                                                    |
| X4 | **`frontend.md` 헤더가 약 1만 자 변경이력**   | 나머지 넷은 정리됐는데 이것만 남음                                                                                                                                                                                                     |
| X5 | **`:8081`이 가상 조인을 못 올린다**           | `assy_qa.core_wafer_map`에 중복 2건 → UNIQUE 인덱스 불가 → 선언 거부. 격리 E2E가 이 기능엔 안 쓰인다                                                                                                                               |
| X6 | **`line_model_owner_attribution` 못 켬**      | 타깃이`owner`인데 뷰가 `owner`를 SELECT 안 한다 ― 뷰 쿼리 수정 선행                                                                                                                                                               |

---

