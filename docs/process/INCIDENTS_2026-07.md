# 🔥 인시던트·경과 기록 (2026-07)

> 보드에서 분리(2026-08-01). **무슨 일이 있었나**를 담는다 ― 지금 열린 것은 [`PROJECT_STATUS.md`](PROJECT_STATUS.md).
> 라운드별 상세는 [`docs/history/`](../history/README.md). 이 파일은 **보드에 쌓여 있던 서사**를 옮겨 온 것이다.

---

> ✅ **재기동 완료 — 라이브는 `269b39e` (2026-07-28 01:00 기동, 커밋 23:35보다 뒤)**. 포트는 **8080**이다(8000 아님 — 진단할 때 여기서 자주 헛짚는다).
>
> 실측 확인: `/health` 200 · `/api/transfer-plan/stages` 200에 roles 전부 `connected` · `/api/transfer-plan/validate`는 **404가 아니라 422**(라우트 존재, 파라미터 누락) · `POST /admin/auto-update/run-now` → **503**.
>
> ✅ **어드민 표면 실제로 잠김 (2026-07-28 01:02, 운영에서 처음)** — 토큰 없음 **401** / 틀린 토큰 **403** / `/health` 200(설계대로 무인증 유지). `/internal/events/*` 4개 라우트도 전부 401.
>
> **워커 토큰 상속도 실측됐다**: 워커 4종 정상 하트비트, 아웃박스 pending 0, 워커 로그에 `401` 없음. 부분 재기동 실패 모드만 아직 미재현([ROLLBACK_PROCEDURE §미실행 표](../guide/ROLLBACK_PROCEDURE.md)).
>
> ⚠️ **PowerShell에서 `set`은 조용히 실패한다.** `set ASSY_ADMIN_TOKEN=…`은 환경변수가 아니라 동명의 PowerShell 변수를 만들고 끝나며 **에러도 안 난다**. `$env:ASSY_ADMIN_TOKEN = "…"`를 써야 하고, 설정한 **그 셸에서** 런처를 띄워야 자식이 물려받는다. 2026-07-28 실제로 여기서 막혔고 [DEPLOY_SETUP §1-4](../guide/DEPLOY_SETUP.md)에 셸별 표로 명문화했다.
>
> 남은 한 가지: **첫 실 `bands` 쓰기**. 라이브 행은 여전히 전부 `bands IS NULL`이라 파싱 경로를 아무도 안 밟았다 — 존 컬럼 설치가 끝나야 처음 밟힌다.

---

> 🔴 **후속 사고 — 내 즉효 조치가 오토업데이트를 깼다 (2026-07-30).** `NO_PROXY`를 런처 셸에 넣게 했는데, 런처가 `os.environ.copy()`로 자식을 만들므로 **오토업데이트가 실행하는 사용자 스크립트 전부**가 물려받았다. 그 스크립트들은 `urllib.request.urlopen`으로 사내 인트라넷 API를 치고 프록시 경유가 전제라 **403**이 났다. 사용자 확인: 「프록시 맞아, 본컴에서 따로 실행하면 잘됨」.
>
> 🔴 **기구 (실측):** `urllib.request.getproxies()`는 `getproxies_environment() or getproxies_registry()`다. **`or`**. 그리고 `getproxies_environment()`는 이름이 `_proxy`로 끝나는 환경변수를 전부 걷는데 **`no_proxy`도 `_proxy`로 끝난다** → `NO_PROXY` 하나만 넣어도 dict가 비지 않아 **레지스트리가 통째로 무시**되고, `http`/`https` 항목이 없으니 **전 요청이 직결**로 나간다. 측정: `NO_PROXY 없음 → {}` / `NO_PROXY 설정 → {'no': ...}`.
>
> ✅ **조치**: `NO_PROXY` 삭제 + `23a346d` 배포. 코드 수리는 `requests`/`httpx` **세션에만** 걸려 `urllib`을 쓰는 사용자 스크립트 영향이 **0**이다 — 범위를 좁게 잡은 이유가 정확히 이것인데 **임시 조치는 정반대로 냈다.**
>
> 📌 **교훈**: 프록시 문제의 즉효 조치로 **프로세스 전역 환경변수를 쓰지 않는다.** 같은 트리의 모든 자식과 사용자 스크립트에 걸린다. 영향 범위는 `getproxies()` 두 줄만 읽으면 나왔다.
>
> ✅ **인시던트 종결 — 프록시 확정 (2026-07-30). `curl.exe -s --noproxy "*"`로 운영 루프백이 200, 사용자 확인 「된다, 프록시네」.** 서버는 처음부터 정상이었다. 즉효 조치 전달됨(런처 셸 `$env:NO_PROXY = "127.0.0.1,localhost"`) — 밀린 `broadcast_at IS NULL`은 스윕이 사이클당 500건씩 회수하므로 수동 조치 불필요. 코드 수리(`trust_env = False` 3발신자 + 거부 주체를 명명하는 로그)는 라운드 진행 중. 아래는 진단 경과 기록이다.
>
> 🔴 **라이브 인시던트 (2026-07-30, 사용자 신고) — 운영 chain 워커가 `POST /internal/events/broadcast`에서 403**
>
> ✅ **데이터는 안전하다.** 통지는 fire-and-forget이고 반환값은 `broadcast_at` 스탬프 판정에만 쓰인다(`chain_ingestion_worker.py:152` — *"데이터 처리 성공/재시도 판정에는 절대 반영하지 않는다"*). 체인 쓰기는 커밋돼 있고 실패 행은 `broadcast_at IS NULL`로 DB에 durable하게 남는다. ⚠️ **단 스윕은 이 건을 못 고친다** — `sweep_undelivered_broadcasts`(`:767`)가 같은 `post_event_async`를 타므로 매 사이클 같은 403을 받는다. 스윕은 재시작·타임아웃 같은 **일시적** 유실용이다. 클라는 토큰/경로가 풀릴 때까지 stale이고, 풀리면 `LIMIT 500`씩 회수된다.
>
> 🔴 **원인은 우리 게이트가 아니다 — 실측으로 확정.** 운영 호스트 루프백에서 **토큰 없이** 쏜 결과가 **403**이었다. `admin_auth._enforce`(`:138-150`)는 헤더가 없으면 **401**이고 403은 「헤더가 있고 값이 다름」에서만 나온다. 그리고 그 브랜치는 도입된 `90e284f` 이후 **줄곧 401**이다(상태 코드 변경 이력 0 — 옛 빌드로도 설명 안 됨). 미들웨어 둘 다 403을 못 낸다(`db_context_middleware`는 컨텍스트 변수만, CORS는 preflight OPTIONS에 400). 브로드캐스트 핸들러 본문에는 403 경로가 **아예 없다**. → **403은 FastAPI 상류에서 온다**(리버스 프록시의 `/internal/*` 차단이 유력, 방화벽·엔드포인트 에이전트·8080을 물고 있는 다른 프로세스도 같은 모양). **재기동은 듣지 않는다.**
>
> 🔴 **근인 확정 — 우리 코드다. `requests`가 프록시 환경변수를 신뢰한다.** 실측 추가: **`/health`도 403**이다. `/health`에는 게이트가 아예 없으므로 우리 앱이 답한 게 아니다. 기구: `_get_http_session()`(`chain_ingestion_worker.py:145`)이 `requests.Session()`을 프록시 설정 없이 만들고 `requests`는 `trust_env`가 기본 True라 `HTTP_PROXY`와 Windows 프록시 레지스트리를 읽는다. **함정: `ProxyOverride`의 `<local>`은 점 없는 호스트명만 우회하므로 `localhost`는 면제되고 `127.0.0.1`은 안 된다.** 워커의 내부 POST가 사내 프록시로 나가고 프록시가 사설 주소 중계를 403으로 거부한다. 전 관측이 맞는다 — 401 아닌 403 · 게이트 없는 `/health`까지 · 재기동 무효 · 개발 머신엔 사내 프록시가 없어 운영에서만.
>
> **수리**: `sess.trust_env = False`(또는 호출부가 놓칠 수 없는 세션 팩토리). **발신자 3곳 전부** — `chain_ingestion_worker.py`·`graph_sync_worker.py`·`run_watcher.py`. 즉효 조치는 런처 셸에서 `$env:NO_PROXY = "127.0.0.1,localhost"`.
>
> ⚠️ **총괄 정정 2회 — 둘 다 도구를 검증하지 않은 결과다.** ① 처음엔 「403 = 토큰 불일치 → 트리 전체 재기동」이라 했다. 추론은 옳았고 전제가 틀렸다(우리 게이트는 헤더가 없으면 401이고, 그 브랜치는 `90e284f` 이후 줄곧 401이다). ② **내가 준 진단 명령이 증상을 스스로 만들 수 있었다** — PowerShell에서 `curl`은 `Invoke-WebRequest` 별칭이고 그것도 시스템 프록시를 탄다. 사용자가 「파워셸 스크립트가 하나도 안 돌아간다」고 알려 준 것이 근인으로 가는 단서였다. **교훈: 진단 도구가 진단 대상과 같은 경로를 타는지 먼저 확인한다.** 올바른 명령은 `curl.exe -s --noproxy "*"`.
>
> ✅ **프록시 인시던트 종결** (`23a346d`+`4aae627`+`98956fd`). 근인: `requests`/`httpx`의 `trust_env` 기본 True라 루프백 통지가 사내 프록시로 나가 403. 수리: `server/internal_event_client.py`가 내부 HTTP를 **단독 소유**하고 `trust_env=False`, 「네 번째 발신자가 빠뜨릴 수 없다」는 **테스트**. 스케줄러는 자기 프로세스의 프록시 정책을 확정(`bypass_proxy`). 🔴 **`NO_PROXY`를 즉효 조치로 쓰지 말 것** — 프로세스 전역이라 인제션 스크립트까지 물려받고, `no_proxy` 하나만 있어도 `urllib`이 레지스트리를 통째로 무시한다(2026-07-30 실제로 오토업데이트가 이렇게 멈췄다). 경과는 [히스토리](../history/README.md)
>
> ⚠️ **이 인시던트가 드러낸 것**: 누출 규율이 토큰을 모든 로그에서 지우다 보니 **두 토큰이 다른지 확인할 방법이 없었다.** 수리로 지문 8 hex가 서버 배너·워커 로그·운영자 셸 세 곳에 찍힌다(값은 어디에도 안 찍힘).
>
> 🔁 **현재 상태 (2026-07-31 밤 · 새 세션 인수)** ― 문서 싱크 `6f328c0` 착지, **4레인 동시 가동 중이라 트리 더러움**(미커밋: `map_editor.js`·`admin.js`·`main.py`·`crud.py`·`virtual_join_executor.py` + 신규 4)
>
> **가동 중인 4레인** ― ① map-pm **오버레이 = 맵 규칙 6**(사용자 판정 「클라 단일 구현」, 서버 `map_overlay` 정렬군은 손대지 않는다) · ② client-pm **소급 어드민 화면**(`fbc1053` 라우트 3개에 화면) · ③ server-pm **가상 컬럼 검색·필터**(**정렬은 사용자 판정으로 범위 밖**) + contract-keeper 동시 투입 · ④ doc-keeper ✅ 착지
>
> 🔴 **사용자 판정 (2026-07-31 밤) ― 빈칸 판정을 두 철자로 만들지 말고 「갈릴 입력을 저장 못 하게」 막는다.** 파이썬 `strip()`은 탭·NBSP까지 벗기고 Postgres `btrim()`은 공백만 벗기므로, `"\t"`인 셀은 **화면엔 `미상`인데 미상 검색에는 안 잡힌다**(한 방향 과소보고, 오염 아님). 수리는 SQL을 파이썬처럼 만드는 것이 아니라 **쓰기 깔때기에서 정규화**하는 것. 근거: `clean_str_value`는 읽을 때 이미 strip하고 `cast_value_by_type`는 `""`를 NULL로 만든다 ― **저장 계층만 그 얘기를 못 들었다.** ⚠️ **「막아」는 미래만 덮는다** ― 이미 저장된 행의 건수 측정이 server-pm 라운드의 첫 관문이고, 0이 아니면 소급이 붙는다
>
> ⚠️ **낡은 열린 문제 2건 (총괄 실측, 아직 표에서 안 내림)** ― **#18**(오버레이 기하 시그니처에 `phys_*` 누락)은 [`map_editor.js:8902-8916`](../../client2/src/map_editor.js)이 여섯 개를 **전부** 넣고 있고 `[C7]` 주석까지 달려 있어 **이미 수리됐다**. **#4**(`test_map_presets_api` 실패 · 「177 passed / 1 failed」)는 같은 표 **#16**이 `9a8ede8`로 격리해 허용 실패를 소멸시켰다고 적고 있어 **모순**이다 ― 스위트 재실행으로 닫을 것
>
> 📌 **총괄 측정 사고 1건**: `main.py` 줄 수를 PowerShell `Measure-Object -Line`으로 재서 4,925를 얻고 에이전트의 5,355를 의심했다. **그 커맨드는 빈 줄을 세지 않는다**(공백 줄 정확히 430 = 차이). 맞는 보고를 틀렸다고 판정할 뻔했다 ― `grep -c "✗"`가 요약줄을 센 것과 같은 계급
>
> 🔴 **새 규율 ― 레인 가동 중에는 아무도 `npm run build`를 돌리지 않는다.** map-pm 라운드가 빌드를 돌렸고 **다른 레인의 미검수 `src/`가 `client2/dist/**` 9파일에 번들됐다**(총괄이 `git checkout -- client2/dist`로 되돌림). 보드가 이미 「dist는 총괄이 라운드 착지 후 한 번만」이라고 적어 뒀지만 **지시서에 없으면 에이전트는 모른다** ― 다음 클라 지시서부터 표준 문구. ⚠️ 되돌린 뒤 참조되지 않는 새 번들 3개가 `dist/assets/`에 미추적으로 남아 있다(무해, 삭제는 사용자 승인 사항)
>
> ✅ **소급 어드민 화면 착지 (client-pm, T2 GO · 미커밋)** ― 어드민 Overview의 **네 번째 계기 줄**, F9와 같은 `<details>` 문법. 새 영역·모드·모달 0. 🔬 **정확한 척을 기계적으로 막았다**: 서버가 `affected_label`을 안 보내면 클라가 **숫자를 `null`로 만든다**(단서가 라벨 안에 살기 때문 ― 「회수할 셀 (최대)」). 총괄 재실행: **177 통과 / 0 실패 · 결함 변이 7/7 검출 · 대조 변이 2/2 탈출**
>
> ✅ **오버레이 = 맵 규칙 6, 데이터 계층 착지 (map-pm, 미커밋)** ― **클라에 mm 공간이 생겼고 새 변환은 0개다**: `getDieIndex`가 이미 계산해 놓고 **버리던 반올림 전 값**(`xCells`)을 돌려주게 했을 뿐이고 `xCells × chipX`가 곧 웨이퍼 중심 mm다. 회전·반전·offset 구현은 여전히 한 벌. 투영은 소스 → 절대 웨이퍼 mm → 타깃 셀 + **나머지**. 겹치면 `cells`에 **대표값이 아니라 `null`**을 넣고 `items` 목록이 진짜 답이다. **총괄 재실행: 13/13 변이 검출 · 저장 좌표 600셀 중 0 이동.**
> 🟩 **총괄 판정 ― 겹친 칩의 점은 legend 값 색으로 칠한다(안 ⓐ).** 한 색이면 「어디에 몇 개」만 읽히고 「무엇이었나」는 여전히 안 읽혀 대표값의 약한 버전이 된다. 호버 판독(안 ⓑ)은 캔버스에 없는 텍스트 표면을 요구하므로 **거절**
> 🔬 **덤으로 나온 기존 결함**: `outside` 계수기가 `35e84c3` **이전** 술어(`0 <= px < cols`)를 쓰고 있어, 어긋난 것이 하나도 없는 항등 오버레이에서 **402칩 중 277을 「격자 밖」으로 보고**했다. 이건 이론이 아니라 **평범한 동일 규격 오버레이에서 발화한다**
> ❓ **총괄 재측정 대기**: 「같은 선언 치수 + 다른 피치」가 라이브 `wafer_map_metadata`에 실제로 있는가 ― 없으면 596/600 구멍은 이론이었다는 뜻이고 그렇게 적어야 한다
>
> 🟡 **서버 결함 3건 대기열 편입** (소급 화면이 클라에서 고치기를 **거부**한 것 ― 옳은 판단): ① `truncated: true`인데 그 사실을 말하는 문장이 `detail`에 없다(594는 하한인데 화면엔 주황 테두리뿐. 형제 라우트인 auto-confirm 드라이런엔 문장이 있다) ② `graph_orphans`의 `detail`이 **리터럴 `**삭제**` 마크다운**을 실어 보낸다 ― INV-F9-8 위반이고 지금 화면에 떠 있다 ③ `deletes`·`commit_granularity`가 한국어 확인 문구 안에 영어로 들어간다. 셋 다 **클라가 의역하면 계약 위반**이라 서버 소관
>
> 🔴🔴 **인시던트 (2026-07-31 20:39, 사용자 신고) ― 런처가 둘 돌아서 같은 파일이 두 번 적재된다. 종결된 21번 「중복 키」의 진짜 기전이 이것이다.**
> **증상은 미끼였다.** 화면에 뜬 건 `Checkpoint planning failed ... UniqueViolation on idx_fic_identity`였고, 그건 **경합에서 3.5ms 차이로 진** 쪽이 남긴 자국일 뿐이다. ✅ **트랜잭션 오염은 없다**(총괄 가설 오답) ― `_plan_checkpoint`(`directory_watcher.py:1231`)는 **자기 세션**을 열어 `:1238`에서 롤백하고 닫으며, 폴백 적재는 `:1787`의 다른 세션이다. 「0행부터 적재했다」는 문장은 **참**이다.
> 🔴 **진짜 피해는 중복 행이다.** `_get_or_create_row`(`crud.py:853-887`)가 `business_key_val`에 대해 **check-then-insert**인데 `pg_indexes` 실측상 `wafer_process`의 UNIQUE 인덱스는 `row_id` **하나뿐**이라, 진 쪽이 그대로 **append** 했다. 실측 잉여 **389 그룹**(`wafer_process` 59 · `bonding_log` 129 · `inventory_master` 202)이고 **100%가 사본 전부 1초 안에 생성**됐다 ― 경합의 지문이다. (`bonding_map` 2,313은 **다른 모집단**, 2026-07-23, 넓은 시간 분산 ― 여기 귀속하지 않는다)
> 📌 **21번을 다시 읽어야 한다.** 사용자 판정은 「중복 키 다 실수니까 무시, 개발환경이라 그래」였고 그때 남긴 구조적 사실이 **「`business_key_val`을 덮는 인덱스 54개 전부 non-unique」**였다. 그 문장이 이 인시던트의 절반이다 ― **실수가 아니라 기전이 있고, 운영에서도 난다.**
> 🔴 **고아 워커 2개가 어제 아침부터 살아 있다 (총괄 실측, 기동 시각으로 확정)**: `run_chain_worker.py` **29852**와 `run_graph_sync.py` **31156**이 **2026-07-30 09:35:24** 기동 ― **약 35시간째**. 같은 이름의 39976·9396은 **2026-07-31 20:50:04** 기동으로 현 수퍼바이저의 정상 자식이다. 사용자가 「다른 곳에 있던 서버와 섞였다」며 조치했고 그것으로 **오늘의 워처 중복은 사라졌으나**, 이 둘은 **그 이전부터의 잔존**이라 별개다. ⚠️ **체인 워커가 35시간 둘이었다면 중복 반경이 오늘 하루가 아니다** ― 389 그룹은 워처 경로 실측치이고 체인 파생 경로는 아직 안 셌다. ✅ **사용자 승인 후 종료(총괄 실행) ― 기동 시각을 확인하고 07-31 것이면 거부하는 가드를 걸어 죽였다.** 지금 스택은 정확히 한 벌이고 `/health` `ok`.
> 🔎 **같은 배치의 셋째**: 격리 `:8081`(PID 21976)도 **2026-07-30 09:35:22** 기동 ― 죽인 워커 둘(09:35:24)과 **2초 차이**다. 그때 뜬 것은 **격리 스택 런처 한 벌**이고 워커까지 함께 띄운 모양이다. 그 워커들이 `assy_qa`를 봤으면 운영 중복과 무관하고, **운영 DB를 봤으면 그것이 「섞인 것」의 정체**다 ― 그 런처 config를 봐야 갈린다, 미확인. `:8081`은 QA 레인들이 쓰는 중이라 **살려 뒀고, 07-30 코드라 거기서 잰 값은 최신 동작이 아니다**
>
> ---
>
> 📌 **사용자가 `table_config.json`을 직접 바꿨다 (2026-07-31 21:17:53, 본인 확인)** ― `bonding_log.column_types`에서 **`wafer_id` 제거**. `server/config/`는 **일부러 git 밖**이라 이력이 없으므로 **이 줄이 유일한 기록**이다.
> **결과**: 그 컬럼이 `collide` → **`virtual_only`**로 성격이 바뀌었다. ⚠️ **딸려 오는 것: 이제 그리드에서 그 칸을 손으로 못 고친다**(쓰기 깔때기 `refuse_virtual_join_columns`가 400). 조인이 값을 전부 만들므로 의도된 모양일 수 있으나, **운영자 교정 경로가 닫힌 것**이라 핵심가치 1(최소 공수 교정)에 걸린다 ― 교정이 필요해지면 선언을 되돌리거나 「읽기는 조인, 쓰기는 원본 테이블로」를 열어야 한다.
> ✅ **부수 이득**: 라이브 `virtual_only` 컬럼이 **처음 생겼다**. 그래서 이번 주 `filter: false` 해제가 **화면에서 실제로 검증 가능**해졌다 ― 종전엔 선언 0건이라 이음새를 끝까지 확인할 방법이 없었다.
>
> 🟡 **그리드 페이지가 admin 번들을 끌어와 콘솔에서 던진다 ― 기존 결함, 오늘 것이 아니다** (사용자 신고 2026-07-31, 총괄 진단). `client2/dist/index.html`에 **`<link rel="modulepreload" href="/assets/admin-*.js">`**가 있고, `git diff HEAD~1 HEAD -- client2/dist/index.html` 실측상 **그 줄은 원래 있었고 해시만 바뀌었다**(`admin-DckjJt59` → `admin-DIdtHlq9`). admin 모듈이 그리드 문서에서 평가되면 `setupEventListeners`의 `tabDefs.forEach`(`admin.js:499`)가 **어드민 DOM이 없는 페이지에서** 돌아 `e.btn`이 null이 된다. 압축 오프셋(`1:14362`)을 디코딩해 그 자리로 확정했다.
> ✅ **그리드 자체는 무사하다** ― 사용자 로그에 AG Grid 초기화와 `[WebSocket] Connected`가 그대로 찍힌다. admin의 DOMContentLoaded 핸들러 안에서 끝나는 예외다. **시끄럽지만 무해**하고 데이터 경로에 닿지 않는다.
> 📌 **수리(다음 주, 작다)**: `setupEventListeners` 첫 줄에서 **어드민 문서가 아니면 빠져나온다**. 🔴 **null 가드로 덮지 말 것** ― 그러면 진짜 DOM 불일치까지 같이 삼킨다. 「이 페이지가 내 페이지인가」를 묻는 것과 「이 요소가 있는가」를 묻는 것은 다른 질문이다. 곁가지: `src`에 admin 동적 import가 **0건**인데 vite가 preload를 넣는 이유는 미규명
>
> ## 🚢 이번 주 마감 릴리스 체크리스트 (사용자 기준 2026-07-31: 「운영에 푸시했을 때 기능은 다 하도록」)
>
> **수용 기준이 라운드별 GO가 아니다.** 각 레인이 초록이어도 **이음새와 배포 절차**에서 나사가 빠지면 사용자는 「아무것도 안 바뀌었다」를 본다. 아래를 전부 통과해야 푸시한다.
>
> | # | 관문 | 왜 이게 나사인가 |
> |---|---|---|
> | 1 | 4레인 HIGH 전건 수리 + **총괄 재검** | 오버레이 게이트(피치 부재 570/600 오배치) · 소급 쓰기 2건 |
> | 2 | **`/schema` 3단 이음새가 실제로 이어짐** ― 서버 announce → 클라 소비 → `filter: false` 해제 → join-resolved 집합에서 Blank 제거 | 서버만 고치면 **사용자가 요청한 검색이 UI에서 안 보인다.** 지금이 정확히 그 상태 |
> | 3 | **CSV 추출 값이 그리드와 같은 행에서 일치** | 화면과 추출물이 갈리면 매체만 바뀐 같은 거짓말 |
> | 4 | 전체 스위트 **총괄이 직접 재실행**(`conda run -n assy_manager`) | 인용 금지, 맨 `python`은 가짜 실패 16건 |
> | 5 | 🔴 **`npm run build`는 소스가 전부 확정된 뒤 총괄이 딱 한 번** | 지금까지 클라 작업은 **하나도 운영에 안 닿는다**(`dist`가 HEAD). 이 한 줄을 빠뜨리면 푸시해도 화면이 그대로다 ― **가장 큰 나사** |
> | 6 | `dist`를 **소스와 같은 커밋에** | 갈라 커밋하면 번들과 소스가 어긋난 리비전이 main에 남는다 |
> | 7 | 격리 브라우저 E2E ― ⚠️ **`:8081`을 새 빌드로 재기동한 뒤에** | 지금 `:8081`은 **07-30 코드**다. 거기서 통과해도 아무것도 증명하지 못한다 |
> | 8 | 🔴 **인계 문구에 「서버 재기동 필요」를 명시** | 소급 라우트·config resolve·가상 조인은 **새 프로세스라야 존재**한다. 재기동 없이는 404가 나고 **우리 버그처럼 보인다**(`1dc761b`가 그 404를 「재기동하라」로 가르치지만, 그 수리 자체도 새 빌드에 있다) |
> | 9 | `git push origin main` | |
>
> ✅ **사용자 실기 확인 2026-07-31: 소급 어드민 화면 「잘 된다」** ― dev 서버 `:5173`에서 확인. 총괄 라이브 실측도 일치(`/admin/retroactive/operations` 200 · `graph_orphans/count` 12,790 `upper_bound`, 「(최대)」 라벨과 상한 사유 문장 동반). 이 박스는 토큰 미설정이라 쓰기 라우트가 **503 fail-closed** ― 설계대로다
>
> 🔴 **사용자 테스트가 커밋된 결함 하나를 잡았다 ― `validDieListCache is not defined` (`map_editor.js`, 총괄이 한 줄 수리)**. `dde342c`(맵 키 드롭다운)가 캐시를 `mapKeyListCache`로 통합하며 **호출부 하나를 안 고쳤고**, 선언되지 않은 이름이 **HEAD에 그대로 커밋돼 있었다**. 🔴 **정정 ― 이미 번들에 들어가 있다. 「아직 운영에 안 나갔다」던 내 앞선 서술은 틀렸다.** 실측: 추적 중인 `client2/dist/assets/map_editor-CxOtziEt.js`(생산 커밋 `dde342c`)에 **`validDieListCache`가 1회 그대로 있고 `mapKeyListCache`는 0회**다. 이유가 결정적이다 ― **선언된 지역명은 minify로 짧아지지만 선언 없는 전역은 이름을 바꿀 수 없어 원문 그대로 남는다.** 그 1:0이 곧 지문이다. <br>내 오류의 출처: 보드의 「`dist` 최신 (`1dc761b`)」 줄을 그대로 믿었다. 실제로는 `9d7d9a4`와 `4b50135`가 각각 dist를 재빌드했다 ― **보드의 낡은 한 줄이 내 위험 판정을 뒤집었다.** 운영 박스가 그 번들을 서빙 중인지는 그쪽 pull 시점에 달렸고 **여기서 잴 수 없다.** <br>⚠️ **피해는 에러 한 줄이 아니다**: 그 줄은 **Push 성공 직후**에 있고 바로 아래가 **Split Registry legend(DOE) 일괄 저장**이라, 맵은 저장되고 DOE 저장은 안 되는 상태가 된다. <br>📌 **어떤 하네스도 이 계급을 못 잡는다** ― 하네스는 함수를 **이름으로 잘라내** 채점하지 모듈을 실행하지 않으므로, 선언 없는 전역은 잘라낸 문맥에서 보이지 않는다. 「하네스가 죽었나」(러너 구멍)와 **다른 축의 구멍**이다. 다음 주: 모듈을 실제로 로드하는 스모크 1건
>
> ---
>
