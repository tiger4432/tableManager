# 체인 기저의 완전성 — 「맵퍼가 지켜야 할 계약」을 축 × 칸으로 (2026-09-11 신설, D-4)

> 🆕 **[2026-09-13 체인 기저 라운드 — 요약]** 규칙 칸 «넷»이 착지했다: `max_group_rows`(S-153 `ba4fbb95`) · `group_by`(S-154 `051cf472`) · `idempotent`(S-155 `cce52773`) · 그리고 `max_group_attempts` 가 «규칙에서 읽히게» 됐다(S-221 `fc0914a7`). 순회 순서는 «유도»되고(S-228 `2df0b798`), 합친 단위의 시도 수는 «구성원의 최대»다(S-227 `36ed2500`).
> 🔴 **네 칸의 공통점은 「맵퍼 본문에만 있던 사실이 «선언»이 된 것」**이다 — 그 본문은 gitignore 라 제품이 못 보고, 그래서 제품이 «자기 동작을 말할 수» 없었다.
> 🆕 **[2026-09-13 밤] S-156 이 «착지했다»** — `chain/rule_order.py`(신설)가 유도를 들고 워커가 `:786` 에서 읽는다(`c7bc59db`). ⚠️ **다만 착지 직후엔 «무동작»이었다**: 예외가 «이름»이라 trigger==target 인 서로 다른 규칙들이 가짜 고리를 만들었고, 그것을 «성질»로 바꾼 것이 S-232(`c70dc340`, 판정 387)이며 「표 사이를 안 옮긴다」가 「아무도 안 읽는다」와 다른 문장이라는 것이 판정 388(S-232-b `684ffb4d`)이다. 📌 **「착지」와 「돈다」가 다른 날이 오늘이다.**
> ⚠️ **열린 것**: S-226 · S-218 · S-222(실패한 병합 다시 쪼개기).
> 🆕 **[2026-09-15]** 통합 선언 종류 «둘»(join S-237 · decide S-239)이 같은 로더로 들어온다 — §1 「끄기」·§5 「고리」가 그날의 답을 얻었다(그리고 «후속» 판정 402 로 고리는 거절 사유가 아니게 됐다). 통합 join 의 `key.unique` 는 «후속 2»(S-240 `8cab58da`)부터 워커 웜업이 읽어 «제품이» 세운다. 같은 날 S-246(`38b5d8e1`)으로 `builtin:` 종류도 §7 「도는 규칙」에 든다.
> 🆕 **[2026-09-16 S-234 `5c845e67`]** 세 규칙 파일이 «한 이름공간» — §1 「끄기」의 스위치 하나가 은퇴하고(판정 408) 두 번 선언된 이름은 로더가 «어느 쪽도 안 세우고» 한 번 거절한다(판정 409).

> 🔴 **이 문서가 있는 이유:** 원장 쪽은 `LEDGER_SCHEMA_COMPLETENESS.md` 가 축 × 칸으로 재는데,
> 체인 쪽은 «한 번도 그렇게 재지 않았습니다». 소유자 「안 잰 거 재고」(09-11).
> 📎 같은 모양: 표 A 는 원장 «선언»의 완전성, 이 표는 체인 «규칙 선언 + 맵퍼 계약»의 완전성.
> 📎 기저의 정의는 `BASIS.md` §4(write · mapper_sdk). 이 문서는 그 §4 의 «grep 검증»입니다.

## 0. 읽는 법 — 표 A 와 «같은» 네 표시

```
①   선언 칸이 있다            이름이 있고, 어긋나면 «이름 대어» 거절된다
②   기본값이 덮는다            칸은 없지만 «값이 적혀» 있고 한 자리에서만 정해진다
③   없다 = 결함              적을 자리도 덮는 값도 없다
③′  칸은 있는데 읽는 쪽이 0     선언이 약속하고 아무도 안 지킨다 (표 A 와 같은 뜻)
```
⚠️ **「맵퍼 «본문»이 무엇을 계산하나」는 이 표의 칸이 아닙니다** — 소유자 결정으로 체인은
파이썬이고(「둘째 도약(선언형 맵퍼)은 필요없어」), 그래서 본문은 «셀 수 없는 것»으로 §8 에 따로 적습니다.

---

## 1. 무엇이 «깨우나» — 트리거

| 칸 | 표시 | 근거 (심볼) | 비고 |
|---|---|---|---|
| 트리거 표 | ① | `rule.trigger_table` — 워커가 outbox 사건의 표와 맞춘다 | 샘플 규칙 10 개가 전부 선언 |
| 깨우는 «컬럼» | ① | `rule.trigger_columns` + `rule_watches_changed_columns()` (S-140) | 없으면 «표 단위»가 기본값(②의 성질) |
| 선언 안 된 컬럼을 적었을 때 | ① | `_report_unwatchable_trigger_columns(rules)` 가 로드 때 «이름을 댄다» | 조용히 안 깨는 것을 막는 자리 |
| 접힌 페이로드 | ② | `outbox_expand` 가 행을 «합성»한다 — 제외 컬럼은 `OUTBOX_PAYLOAD_EXCLUDED_COLUMNS` «한 상수» | 칸이 아니라 «한 자리»가 정한다 |
| 체인이 체인을 깨움 | ① | `rule.allow_chain_trigger` (옵트인) | 기본이 «꺼짐»이라 고리가 사고로 안 생긴다 |
| 끄기 | ① | 옛 문법 `enabled` — 로드 «뒤» 걸러진다 · 통합 선언(`derive`)은 `rule_shape.is_switched_off` 가 «규칙을 안 세운다»(판정 399, ③′ · `dc29056a`) | 「걸러짐」과 「안 섬」은 다른 스위치다 — 뒤엣것만 DB 를 안 만진다. ⚰️ `ASSY_CHAIN_SYNTHESIZE` 은퇴(S-234 `5c845e67`, 판정 408) — 한 규칙은 «적은 파일»의 `enabled`, 전부는 `ASSY_CHAIN_WORKER=0`. 🆕 그리고 세 파일의 이름은 «한 집합»(판정 409): 두 번 선언된 이름은 `ingestion_worker._refuse_names_claimed_twice` 가 «어느 쪽도 안 세우고» 이름 대어 거절한다(①) |

## 2. 무엇을 «받나» — 단위

| 칸 | 표시 | 근거 | 비고 |
|---|---|---|---|
| 트랜잭션 그룹 | ② | `_group_target_tables(events_in_tx, rules)` — «엔진»이 정한다 | 원장의 π(분자 짓기)와 같은 자리 |
| 배치 여부 | ① | `rule.is_batch` | 맵퍼가 행 하나씩인지 묶음인지 |
| **그룹 «키»** | 🔴 **③** | 샘플 규칙 키 «전체»(43개)에 `group_by` 가 «없다». 묶는 기준은 맵퍼 본문의 `decision_key` 사용법이 정한다 | 원장은 `map.unit`+`group_by` 로 이 칸이 ①<br>🆕㉕ **[2026-09-13 S-154 `051cf472`, 판정 381·383] ③ → ①** — 규칙 칸 `group_by` (`chain_bindings.GROUP_BY_KEY` :439 · 문법 :366~378 이 나쁜 값과 «없는 컬럼»을 이름 대어 거절). 읽는 자리 셋이 «하나»가 됐다: `group_key(rule, payload)` :487 · `group_id(event, rules)` :531 · `group_events(events, rules)` :559. 🔴 **그전엔 그룹의 단위가 «쓴 쪽의 커밋»이었다** — 규칙이 고를 수 없으니 랏으로 묶어야 하는 맵퍼가 «자기 본문에서» 다시 묶었고, 그 본문은 gitignore 라 **제품이 자기 그룹을 말할 수 없었다**. ⚠️ 그리고 이 축엔 저자가 «둘»이었다 — 배치 루프와 미배달 스윕이 각각 `payload["transaction_id"] or f"single_{uuid}"` 를 손으로 적고 있었다. 접는 것이 이 라운드의 절반이고 **칸과 «같은 커밋»에 착지한다** — 접기만 따로 가면 그 사이에 두 답이 이미 다르다 |
| **그룹 «크기»** | 🔴 **③** | 그룹의 행 수를 워커가 «고를 자리»가 없다 — 쓰는 쪽의 트랜잭션 크기가 그대로 그룹이 된다 | 🔵 D-5 실측: 1,000 → 5,000 으로 키우면 s/1k 가 «10.5 → 7.34»(30%). 즉 «성능 손잡이인데 손잡이가 없다»<br>🆕㈥ **[2026-09-13 S-153 `ba4fbb95`, 판정 378] ③ → ①** — 규칙 칸 `max_group_rows`(`chain_bindings` :401 · 스켈레톤 :136). 🔴 **읽는 자리가 쓰는 자리와 같다** — `max_group_rows(rule)` (`chain/ingestion_worker.py` :441)가 «규칙에서» 읽는다(병합 단위가 「같은 표, 같은 규칙」이라). ⚠️ 선언 안 하면 «아무것도 안 바뀐다»(`NO_GROUP_MERGE`) · 접히는 것은 «연속»된 그룹뿐이고(순서를 안 넘기려고) · 상한은 «가장 엄한 규칙»의 것이다(합쳐진 단위는 깨운 규칙을 «전부» 돌리므로). ⚠️ 합쳐진 단위는 «실패도 하나»다 — 다시 쪼개는 것은 S-222 로 일부러 안 넣었다 |
| 기다리는 것 | ② | `[ChainWaiting] <표>: N group(s) deferred behind …` (⚰ 09-17 전에는 `[HOL Guard]`) — 앞 그룹이 실패하면 같은 표의 뒤 그룹을 «이번 배치에서» 미룬다 | 미룬 «수»는 §7 참조(③) |

## 3. 무엇을 «쓰나» — 문과 봉투

| 칸 | 표시 | 근거 | 비고 |
|---|---|---|---|
| 목적지 표 | ① | `rule.target_table` | 연쇄 그래프의 엣지 ①이기도 하다 |
| 쓰는 «문» | ② | `crud.apply_batch_updates` — 워커의 «한 루프»만 부른다(주석이 그 규율을 적어 둠) | 둘째 문이 없다는 것이 규율이고 칸은 아니다 |
| 봉투 | ② | `database._outbox_envelope` «한 자리»에서 지어진다 | 그래서 체인의 쓰기도 자기 사건을 낸다 |
| 행의 «신원» | ① | 표 카탈로그의 `business_key`(+`composite_key_*`) → `mapper_sdk.df_to_updates` 가 조립. **선언이 이긴다**(판정 190/191) | 맵퍼가 자기 키를 철자해도 선언이 덮는다 |
| 맵퍼 산출의 «모양» | ① | `mapper_sdk.MapperContractError` — `{"updates": [...]}` 가 아니거나 키가 안 맞으면 «이름 대어» 거절 | 산출은 검증된다. 🔴 «규칙 선언»은 안 된다(§6) |
| 지도 메타 · 교체 · 철회 | ① | `allow_map_metadata_upsert` · `allow_replace_map` · `allow_retraction` | 권한마다 옵트인 |

## 4. 실패하면

| 칸 | 표시 | 근거 | 비고 |
|---|---|---|---|
| 예외 → 격리 | ② | 청크 단위 격리, 안 되면 «행 단위»로 좁힌다(워커 주석이 1,000 행 청크를 통째로 버리지 않는 이유를 적는다) | 값이 적혀 있고 칸은 없다 |
| 시도 상한 | ① | 🆕㉕ **[2026-09-13 S-221 `fc0914a7`, 판정 378] `max_group_attempts(rule, document)` — «규칙 칸이 먼저 · 문서 칸은 기본값 · 없으면 1» — 합친 단위는 «최소»** ⚰️ 종전 표기 「문서 최상단 키」는 «반쪽»이었다 — 칸은 규칙에도 «열려 있었고 폼이 그렸는데» 읽는 쪽이 문서만 봤다(「폼이 그리는데 읽는 쪽이 없다」) | 판정 245a24e8 · 378 |
| 연쇄 깊이 상한 | ① | `max_chain_depth` + `event_constants.max_chain_depth(document)` | 넘으면 「뒤 큐가 돈다」고 로그가 이름을 댄다<br>🆕 **[2026-09-15 S-249 `b1e89e36`] 뒤따르기 랩도 이 상한을 «만난다»** — 홉을 찍는 자리가 그룹 스텝 «하나»뿐이라 다른 스레드의 랩은 홉이 없었고 그 길의 고리는 «무한»이었다. 랩은 `incoming+1` 로 쓰고 (원인 tx, 표, 규칙)당 «한 번» 준다. 🔴 그리고 판정 402 로 이 칸이 고리의 «유일한» 상한이 됐다(§5) |
| **미룸 상한**(읽을 수 없는 행) | 🟡 **②** (09-11 신설) | `max_rows_not_visible_defers` — 문서 최상단 칸, 기본 `DEFAULT_MAX_ROWS_NOT_VISIBLE_DEFERS = 30`(스윕 ≤ 2 s 이므로 «약 1분»). 읽으면 `_RULES_DOCUMENT` 에서 «부를 때마다» 보므로 SYSTEM_RELOAD 로 반영된다 | 🔴 **미룸은 «시도»가 아니다** — `retry_count` 와 별도 카운터(`_ROWS_NOT_VISIBLE_DEFERS`)다. 시도로 세면 `max_group_attempts`(기본 1) 때문에 멀쩡한 청크가 첫 번에 격리되고, 격리는 청크를 «행 단위»로 다시 편다(실측 쓰기 여섯 → 사건 ≈1,700). ⚠️ 그래도 «유한»하다 — 상한에서 «이름 대어» 거절한다(S-160, 판정 267). 📎 **정본 서술**: `event_driven_backend.md` §2.4~2.5 — 「전부 없음 = 거절」 / 「아직 안 보임 = 미룸」의 가름과, 그 창의 «원인»(오염된 autocommit 커넥션이 행과 사건을 다른 커밋에 씀, S-167 09-11 13:37 닫힘) |
| **재시도가 «같은 입력»인가(멱등)** | 🔴 **③** | 규칙이 「이 맵퍼는 멱등이다」를 «말할 칸»이 없고, 재시도는 같은 그룹을 그대로 다시 먹인다 | 맵퍼 본문의 성질이라 §8 과 맞물린다<br>🆕㉕ **[2026-09-13 S-155 `cce52773`, 판정 384] ③ → ①** — 규칙 칸 `idempotent`. `false` 면 **첫 실패에 격리**한다(`chain/ingestion_worker.py` :608~610 이 그 사실을 «이름 대어» 로그한다). 🔵 **상한은 «함수 하나»가 그대로 답한다** — 둘째 좌석이 `idempotent` 를 자기 상한과 견주면 「몇 번 남았나」의 저자가 다시 둘이 된다(:603 이 그렇게 적는다). 🔵 재생 쪽은 `_refuse_unless_idempotent(rule, force)` (`chain/replay.py` :246, 거절 :257~259) — **`force` 로만 넘어간다**. ⚠️ 즉 「다시 돌려도 같은가」는 «선언»이고, 선언이 없으면 오늘 동작 그대로다 |

## 5. 연쇄 — 체인이 체인을 부를 때

| 칸 | 표시 | 근거 | 비고 |
|---|---|---|---|
| 고리 ~~거절~~ «보고» | ① | `_validate_chain_cascade_graph(rules)` — 로드 시점에 «옵트인 엣지만으로 된 고리»를 «찾아 trail 을 돌려준다». ⚰️ **[2026-09-15 판정 402 `2504fbde`] 거절은 로드·저장·리플레이 «어디에도» 없다** — 고리는 «모양»이고(`dt_log ⇄ dt_inventory` 는 의도된 것) 유한성은 §4 `max_chain_depth` «하나»가 답한다. 로그는 프로세스당 trail 당 한 번(`say_cycle_once` · 줄의 모양은 다른 거절 줄과 같이 `operator_line` — S-247-b `d469bacf`), 순서는 «선언 순»(`rule_order`) | 규칙 하나가 엣지 «둘»을 긋는다(`target_table` · 메타 표)<br>🆕 [2026-09-15 `ea8f91d2`] 순서 유도(`rule_order`)는 «돌 수 없는 엣지»를 안 센다 — 꺼진 규칙 · `follow_up` 생산자. 운영이 만난 가짜 고리(통합 join 의 오른쪽 규칙 + 살아 있는 왼→오 규칙)가 그 자리다. ⚰️ **[09-16 S-278 `c41f9c6d`] 그 예는 «더 이상 가짜가 아니다»** — 오른쪽 규칙이 `follow_up` 을 잃고 트리거 경로로 와서 walk 이 그 엣지를 «센다». 그 조합이 서 있으면 고리가 «잡히고» `order_rules` 는 규칙 «전부»의 순서를 선언 순으로 되돌린다(응용 Q-2 ④, 판정 대기) |
| 깊이 | ① | §4 의 `max_chain_depth` | |
| **순서** | 🔴 **③** | 규칙 «사이»의 순서를 적을 칸이 없다. 같은 배치 안 그룹 순서는 엔진(`process_pending_groups`)이 정한다 | 「A 가 먼저 돌아야 B 가 맞다」를 선언할 자리가 없다<br>🆕㉕ **[2026-09-13] 오늘의 답: «선언에서 유도된다»** — `order_rules`(`chain/replay.py`)가 그 유도를 들고, 표 순회는 `trigger_tables_in_order` :987 로 «모든 프로세스에서 같은 답»이 됐다(S-228 `2df0b798`). ⚠️ **그러나 «워커가 규칙 순서를 읽는 것»은 S-156 이고 «미착지»다** — 오늘 선 것은 그 순서가 «설 자리»이지 순서 자체가 아니다<br>🆕㉕ **[2026-09-13 밤 S-156 `c7bc59db`] ③ → ① — 워커가 «읽는다».** 위 줄의 「미착지」는 그날의 참이고 오늘은 거짓이다. 🔴 다만 «순서 칸»이 생긴 것은 아니다 — 순서는 여전히 «선언에서 유도»되고(`chain/rule_order.order_rules`), 운영자가 적는 칸은 없다. 그래서 이 칸의 답은 「선언이 이미 말한 것을 제품이 읽는다」이지 「적을 자리가 생겼다」가 아니다 |

## 6. 🔴 그리고 «문법 자체»가 검증되지 않는다 — 이 표의 제일 큰 ③

| 칸 | 표시 | 근거 |
|---|---|---|
| **규칙 하나의 «모양»** | 🔴 **③** | `load_chain_rules()` 는 `json.load` 뒤 `data.get("rules", [])` 가 전부다. 필수 키 검사도, «모르는 키» 거절도 없다. 어드민 쓰기 경로(`ledger_admin`)의 주석이 «스스로 적어 둔다** — 「Nothing validates a single rule's shape - `chain_bindings` refuses at run time - and none was invented here」 |

🔵 **[09-11 총괄 정정] 「거절이 0」은 «아닙니다» — 오타의 «자리»에 따라 갈립니다:**
```
«키» 오타      조용히 통과.  `trigger_colums: [...]` 는 «모르는 키»라 무시되고 규칙은 표 단위로 돈다
«값» 오타      그 칸만 거절.  `trigger_columns: ["dt_jobb"]` 는 트리거 표가 «선언하지 않은 컬럼»이라
              `_report_unwatchable_trigger_columns` 가 로드 때 «이름을 댄다»(S-140 ④)
=> 즉 오늘 있는 것은 «칸 안의 값» 검증이고, 없는 것은 «칸 자체»의 검증이다.
   그래서 S-152 는 「값 검사를 더한다」가 아니라 「키 집합을 «닫는다»」이다
```

```
결과   오타 난 «키»는 «조용히 무시»된다. `trigger_colums` 로 적으면 표 단위로 굴러가고 아무도 안 알려 준다
대비   원장은 `setup_bundle` 이 `problems.exact(...)` 로 «모르는 키»를 이름 대어 거절한다
그래서 「선언을 고쳤는데 아무 일도 안 난다」가 체인 쪽에서는 «정상 동작»처럼 보인다
```
⚠️ 이것이 §2 의 `group_by`·그룹 «크기» ③ 과 «같은 뿌리»입니다 — 칸을 더해도 검증기가 없으면
그 칸은 오타 한 글자에 조용히 사라집니다. **순서는 「검증기 먼저, 칸은 그다음」입니다.**

## 7. 관측 — 「지금 무엇을 하고 있나」

| 칸 | 표시 | 근거 |
|---|---|---|
| 그룹 한 줄(두 층) | ① | `group <id>: view builds N · … · T s · MACHINERY · mapper M s · outbox read O s · write:<t> W s` (판정 241) |
| 도는 규칙 · 나이 | ① | `chain_activity.registry.snapshot()` — `rule/mapper/target_table/rows_in/running_seconds`. 🔵 [09-15 S-246 `38b5d8e1`] 등록은 `activity.running` 컨텍스트 «하나»이고 파일 맵퍼 문과 `builtin:` 문이 같이 지난다 — 종전엔 builtin 이 등록 «밖»이라 화면이 «노는 체인»을 그렸다 |
| 큐의 «나이» | ② | `oldest_waiting_seconds` «하나»가 답한다. 큐가 비면 `null` 이고 `0` «이 아니다**(main.py 가 그 구별을 적어 둠) |
| 규칙이 «한 번도 안 걸림» | ① | 로더가 규칙 이름을 «값으로» 심는다(`seed_rules`) — 부재는 「옛 서버」 하나만 뜻한다. ⚠️ 09-15 S-246 전엔 `builtin:` 종류가 «돌아도» 이 값에 남았다(등록이 다른 문 안에 있었다) — 지금은 참 |
| **HOL 로 미뤄진 «수»** | 🔴 **③** | 로그 줄 하나뿐이다. 몇 개가 미뤄졌는지를 «값»으로 내놓는 자리가 없다 |
| `rows_not_visible` 로 미뤄진 «수» | ① (09-11) | 같은 «미룸»인데 이쪽은 값이 있다 — tx 별 카운터(`_ROWS_NOT_VISIBLE_DEFERS`)가 상한과 «같은 수»를 세고, 거절문이 그 수를 든다. 🔵 즉 이 표에서 「같은 성질에 한쪽만 값이 있다」의 실물이고, HOL 쪽 ③ 의 «수리 모양»이 여기 이미 있다 |
| **소급 «전»의 비용** | 🔴 **③** | 「이 규칙을 고치면 몇 행이 다시 도나」를 바꾸기 전에 답하는 자리가 없다 (표 A 의 A5-2 와 «같은 구멍의 체인 판») |

## 8. 셀 수 «없는» 것 — 맵퍼 본문 (이름 대어 적는다)

```
무엇      맵퍼 파이썬이 «무엇을 계산하나»는 선언에 없다. 소유자 결정이고 이 표의 결함이 아니다
그래서    이 표가 답할 수 없는 물음 둘:
          ① 이 맵퍼는 «몇 번 돌려도 같은가»(멱등) — §4 의 ③ 과 같은 자리
          ② 이 맵퍼는 «어떤 행을 낼 수 있나» — 산출의 «모양»은 검증되지만(§3) «외연»은 아니다
보이는 것  `mapper_module` · `mapper_function` 이름과, 그룹 줄의 «초»와 `rows_in/rows_out`
          🔵 D-5 실측이 그 한계를 보여 준다: 정렬 맵퍼가 그룹당 «뷰 1,000»을 짓고 `rows_out` 은 0.
             「무엇을 하는지」는 표에 없고 「얼마가 드는지」만 있다 — 그것이 오늘의 가시성이다
```

## 9. 셈 — ③ «여섯» · ③′ «0»

```
③  여섯
  ㉠ 규칙 «모양» 검증 없음 (§6)          <- 나머지 다섯의 뿌리
  ㉡ 그룹 «키» 칸 없음 (§2)
  ㉢ 그룹 «크기» 손잡이 없음 (§2)        <- D-5: 이것만으로 s/1k 30%
  ㉣ 멱등을 말할 칸 없음 (§4)
  ㉤ 규칙 «사이»의 순서 (§5)
  ㉥ HOL 미룬 수 · 소급 전 비용 (§7, 관측 둘을 한 줄로 셈)
③′ 0    — 칸이 있는데 읽는 쪽이 0 인 자리는 «찾지 못했습니다»
        (체인 쪽은 반대로 「읽는 쪽이 있는데 칸이 없다」가 많습니다 — 위 여섯이 그 모양입니다)
```
🔴 **원장과 «대칭이 반대»입니다.** 원장은 「칸을 먼저 열고 읽는 쪽을 나중에」(그래서 ③′ 가 생겼고),
체인은 「기제를 먼저 짓고 칸은 안 만들었다」(그래서 ③ 이 전부 «칸 없음»입니다).

---
📎 잰 날: 2026-09-11. 근거는 `chain/ingestion_worker.py` · `mapper_sdk.py` · `chain_bindings.py` ·
`ledger/admin.py` · `chain/replay.py` · 출하 샘플 `chain_rules.json.sample`(규칙 10) ·
그리고 D-5 의 실측(`task/ontology_application_report.md` 09-11).

---

# 🔴 실행 — 선언은 «하나»인데 규칙을 «부르는 자리»는 셋, 디스패처는 둘 (2026-09-16 신설)

> **왜 이 절이 생겼나:** 이 문서는 «선언»의 완전성을 재는데, 2026-09-16 에 조인이 「안 돈다」로 하루를 먹었고
> 원인은 선언이 아니라 «실행»이었습니다. 그리고 이 문서에 `execute_custom_mapper`·`run_builtin` 이
> **한 번도 안 나왔습니다**(그날 grep 히트 0). 선언이 통합된 것만 적혀 있고, 실행이 갈라져 있다는 것은
> «어디에도» 없었습니다. 소유자: 「빌트인이라 다른 건 너가 체인문을 제대로 안 만들어서 그런 거잖아」.

## 흐름 — 쓰기에서 규칙이 도는 데까지

```
① 쓰기        파일 인제션 · 그리드 편집 · 체인 자신
              crud.apply_batch_updates  ->  cell_sources 층 + database_outbox 행 «하나»
              payload: columns · row_ids · source_name · transaction_id

② 체인 워커   run_chain_worker.py — «자기 프로세스». API 도 아니고 커밋 경로도 «아니다»
              pending_chain_events   processed_chain=false · CONTROL 종류 제외
              이벤트를 transaction_id 로 «묶어» 그룹
              관문 둘
                 rule_watches_changed_columns   trigger_columns ∩ payload.columns
                                                («columns 없음»은 「모른다」라 통과)
                 _rule_accepts_event            체인이 쓴 이벤트는 allow_chain_trigger 옵트인만
              -> 실행 (아래 표)
              그리고 «관문 위»에서 ledger_followup.enqueue — 모든 이벤트가 들어간다

③ 후속 랩     같은 프로세스의 별도 task. 속도는 pacing.json 의 `chain_followup`
              load_setup() + drain_once(원장 재번역)  ->  _run_builtin_followups
              ⚠️ load_setup 이 batch «마다» 돈다 (2026-09-16 실측 48.8 ms, 캐시 없음)
```

## 🔴 실행 — 이 표가 이 절의 요점입니다 (2026-09-16 전수 실측)

|  | 파이썬 맵퍼 | `builtin:` 종류 (조인 · 확정) |
|---|---|---|
| 선언 | `derive.kind: mapper` → `mapper_module`/`mapper_function` | `derive.kind: join`/`decide` → `mapper: builtin:…` |
| 그룹(트리거) 경로 | `rule_run.run_rule` `ingestion_worker` :1513(배치) :1557(행별) | `rule_run.run_rule` :1504 |
| 후속 랩 | — | `rule_run.run_rule` `ingestion_worker` :2867 |
| 소급(replay) | `rule_run.run_rule` `replay` :535 | `rule_run.run_rule` `replay` :504 |

```
⚰️ **[09-16 20:28 `027168c2`] 이 표의 «두 열»은 이제 «같은 좌석»을 가리킨다** — `chain/rule_run.py`. 자리 여섯 전부가 `run_rule` 을 부르고, 그 오라클의 «면제 목록이 빈 목록»이라 좌석 밖에서 규칙을 돌리는 자리가 «생기는 날» 빨개진다. 아래 세 줄은 그 전의 기록이다.
🪦 규칙을 «실행»하는 자리가 «셋»이고, 그중 둘을 «같이» 아는 것은 소급뿐이었다
   -> 그래서 조인은 소급으로는 돌고(소유자 「백필하니 돈다」) 라이브로는 후속 랩에서만 돌았다
   -> 그리고 2026-09-16 에 조인을 그 랩에서 떼자 «갈 곳이 없어졌다» — 그룹 경로엔 builtin 갈래가 없었다
   ⚰️ **[09-16] 그 칸은 «3분» 참이었다** — 이 절은 `9b3af739`(18:42)이고 갈래는 `75ef33fe`(18:45)다. 🔴 제가 먼저 «40분 · `c41f9c6d`»라고 적었는데 «둘 다 틀렸다» — 커밋 시각을 재지 않고 라운드 이름으로 짚었다(응용 Q-7).
   🔴 그런데 그 자리는 `request_source`/`request_chain_depth` 스코프(:1571·:1586) «앞»이다 —
      조인의 쓰기는 `source_name` 은 얻고(항목이 나른다) `chain_depth` 는 «못 얻는다»(응용 Q-1 ①, 판정 대기)
📌 부류: 기준 ④ 「같은 기능에 두 경로」. 그리고 최악의 판이다 — 두 경로가 갈라져 있는데
   «오류를 안 낸다». 한쪽이 조용히 아무것도 안 할 뿐이다
🔴 **아직 «한 칸»이 갈라져 있다 — 봉투의 `source`.** 좌석이 `chain_ingestion` 을 세워도 `crud:4564-4566` 이 «항목의 층 이름»으로 다시 세워, `auto_confirm` 의 쓰기는 «체인이 쓴 것»으로 안 읽힌다(옵트인 통과). 판정 425 = 「참이지만 쉬운 고침이 «층의 정체성»을 지불한다」 → S-280 (응용 Q-8)
```

## 판별식 — 체인에 무엇을 더할 때
```
「이 종류를 «누가» 부르나」를 «선언»이 아니라 «호출 자리»에서 답한다
   세 자리(그룹 배치 · 그룹 행별 · 소급) 전부에서 답이 나와야 한다. 하나라도 «—» 면 그 종류는 그 길에서 «안 돈다»
⛔ 술어(_rule_accepts_event)가 True 인 것은 「간다」이지 「돈다」가 «아니다»
   2026-09-16 에 술어 다섯 개가 초록인 채로 조인이 아무것도 안 썼다
✅ 「돈다」의 증거는 «하나»다 — cell_sources 에 그 규칙의 층이 «생긴다»
```
