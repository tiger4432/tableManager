# 🧪 DOE 설정 가이드 — 맵에 실험 계획 얹는 법

> **Status:** 🟢 Living | **작성:** 2026-07-28 · 총괄 | **Last-verified:** 2026-07-30 (**§6.1 신설 + §4.2에 거부 안내 신설** — `c24d47b`(`matMapCacheHit`/`notifyMapContext`)·`ae2811c`(`stackCrossesMarkerBoundary`/`checkPasteAgainstFrame`/`computeNotchCell`) 소스 대조. ① **§6.1 — `↻ 가용`은 더 이상 `MAP X`를 푸는 유일한 길이 아니다**: 맵 로드·Push 성공·**STACK의 `0`↔`0 아님` 경계 편집**이 자동으로 다시 묻는다. `✓`는 다시 묻지 않고 `X`/`?`만 다시 묻는 이유(부정 답이 곧 사용자가 무효화할 수 있는 답이고, `?`를 붙들면 장애가 사실로 굳는다), `3→4`·`빈칸→3`은 아무것도 발동시키지 않는 이유, 폴링 없음. ② **§4.2 — 붙여넣기 거부는 시트가 아니라 맵의 기하 문제**: ⚠️ 선언 맵 **179개 중 27개만 노치 on-grid = 152개에서 거부되는 것이 정상 동작**이고, 종전에는 경고 한 줄로 통과시켜 **뒤집힌 격자가 그대로 들어갔다**(실측 셀 120개 전부 변경). 회피는 웨이퍼 원 규격 적용, **양식 후속(제목 줄 회전/면 토큰)은 대기열이고 미구현**. 직전 `c9bf2c7`+`5a14e77` 착지 후 코드 대조 — **§4.2 맵 화면 Ctrl+V 회사 양식 되붙이기 신설**(`map_editor.js`의 `onMapGridPaste`/`readCompanyMapBlock`/`checkPasteAgainstFrame` 실측: VALUE·STACK·DESC만 복원 · **자재와 COLOR는 왕복 안 함** · COUNT는 폐기 · **붙여넣기는 값을 지우지 않음** · 확인 1회·서버 쓰기 0), **§4.1 계약 밖 단어 로스터를 13개로 확장**(`doe_bands.js`의 `IGNORED_HEADERS` 실측 — 롤업 8단어는 **예비**이며 표② 붙여넣기는 아직 없음: `rollupToGrid` importer 0건 실측). 직전 F2 `064550f` 착지 후 코드 대조 — **수량은 「저장되는 셀」만 센다**를 §1·§5·§9에 반영(감소폭 표 포함), §4 계약 밖 칸에 **`COUNT`** 추가, §5에 `Fill All`의 유효 다이 필터·토스트. 직전 2026-07-28: U9 마커·U8 가용 피드백·H2 적재 게이트 착지 후 코드 대조 — §8 적재 누락 행을 실제 게이트 동작에 맞춤) | **대상:** 맵 에디터에서 DOE를 작성하는 **사용자** (개발자용 계약은 [MAP_EDITOR_SPEC §6](../spec/MAP_EDITOR_SPEC.md))
> **한 줄 요약:** **색칠이 곧 계획이다.** 값 하나 = DOE 조건 하나이고, 그 값으로 칠한 셀 중 **저장되는 것**의 수 × 층수가 자재 수요가 된다.

---

## 1. 개념 — 3가지만 알면 된다

1. **값(VALUE) 하나 = DOE 조건 하나.** 범례에 값을 만들고 그 값으로 맵을 칠하면, 칠한 영역이 그 조건의 적용 범위다.
2. **층 구조는 STACK 숫자 하나로 끝난다.** `STACK=16`이면 이 조건은 16층이고, 세 구역이 자동으로 생긴다 — **1H**(1층) · **MID**(사이 전부) · **TOP**(16층). 구간 입력, FROM/TO 같은 건 없다. 겹침·빈 층은 **입력 자체가 불가능**하다.
3. **수량은 저장하지 않고 파생된다.** 사용 = **저장되는 셀 수** × 층수. 셀을 하나 더 칠하면 수요가 바로 바뀐다.

> **"저장되는 셀"이 무슨 뜻인가** — 유효 다이(웨이퍼 원, 또는 그 맵에 지정된 [유효 다이 맵](VALID_DIE_MAP_GUIDE.md)) **안쪽**의 칠해진 셀이다. 그 바깥은 캔버스에 색도 안 나오고 `⚡ Push`로 서버에 올라가지도 않는다 — **저장되지 않는 셀은 수량에도 안 들어간다.** 화면의 숫자와 서버에 들어간 행 수가 항상 같다는 뜻이다(§9 참조).

## 2. 시작하기

1. 맵 에디터에서 **Target Table**(예: `bonding_map`)과 키를 넣고 **Load** — **없는 키도 그냥 열린다**(빈 맵). 키는 ⚡ Push 때 생성된다.
2. 처음 여는 맵은 DOE가 **VALUE 1 하나짜리 빈 조건**으로 시작한다. 그 키에 저장된 DOE 세트가 있으면 그게 로드된다.

## 3. 표① 값 정의 — 칼럼별 입력법

| 칼럼 | 입력 | 예 |
|---|---|---|
| **VALUE** | 조건 이름(자유 문자열) | `A`, `SPLIT-C7`, `F` |
| **STACK** | 총 층수(정수). **`0` = 상태 표시 값**(§3.1) | `16`, `0` |
| **DESC** | 설명(자유) | `EDS FAIL 위치` |
| **1H** | 1층 자재. **비우면 MID가 1층부터** | `LOTA_01` |
| **MID** | 중간 자재 — 여러 개면 줄바꿈 또는 쉼표 | `LOTB` |
| **TOP** | 마지막 층 자재. **비우면 MID가 끝까지** | `LOTC_05:2` |
| **KNOB** | 공정 조건 태그(쌍 아님, 그냥 태그) — 줄바꿈/쉼표 구분. 나중에 공정 추적으로 자동 부착 예정 | `dose_28`, `focus_002` |

### 3.1 STACK 0 — "이 색은 계획이 아니라 표시"

BASE의 FAIL 위치처럼 **층 구조 없이 상태만 표시하려고 칠하는 값**은 STACK에 `0`을 넣는다. 그 값은:
- 구역 입력이 **해당 없음**으로 잠기고, 자재 수요·롤업에서 빠지고, 검수도 잔소리하지 않는다.
- `0`인데 구역에 자재가 적혀 있으면 모순이라고 **알려만** 준다(V6 — 저장은 막지 않음).

> **`0`과 빈칸은 다르다.** `0` = "층이 없다"는 선언. 빈칸 = "아직 안 적었다" — 빈칸은 검수가 계속 지적한다.

### 3.2 자재 토큰 문법 — `lot[_slot][:BIN]`

| 쓰면 | 뜻 |
|---|---|
| `ADFE1H_01` | 그 lot의 slot 01 |
| `ADFE1H_01:3` | 같은 wafer의 **BIN 3만** |
| `ADFE1H` | **lot 전체** (slot 미기입) |
| `ADFE1H:2` | lot 전체에 일괄 BIN 2 |

BIN 생략 시 **1**. 롤업 행의 정체는 `(자재, BIN)` 풀이다 — 한 DT 맵에서 VALUE별로 BIN을 나눠 쓰는 것도 이 문법으로 표현된다.

## 4. 엑셀 호환 — 표를 그대로 붙여넣는다

사람들이 엑셀에 만들어 둔 표와 **직접 왕복**된다. 계약 칼럼은 6개:

```
VALUE  STACK  DESC  1H  MID  TOP
```

- **붙여넣기**: 엑셀에서 범위 복사 → 표①에 붙여넣기. 헤더 행이 있으면 **이름으로** 매칭, 없으면 **왼쪽부터 순서대로**. 색은 무시된다(색은 에디터가 관리).
- **복사**: 표①에서 복사하면 같은 6칼럼 TSV로 나간다. `0`도 적은 그대로 왕복된다.

### 4.1 계약 밖인데 **머리줄에서는 알아보는** 단어들

머리줄을 통째로 긁어 붙여도 **그 줄이 머리줄로 인식되게** 하려고 알아만 두는 단어들이다 — 모르는 척하면 그 줄이 데이터 행으로 들어가 **값 이름이 `VALUE`이고 STACK이 `COUNT`인 행**이 생긴다. 전부 **알아보되 버린다.**

| 묶음 | 단어 | 왜 계약 밖인가 |
|---|---|---|
| 화면 표시·파생값 | `COLOR` · `COLOR*` · `칠함` · `칠함*` · `COUNT` | 「칠함」과 `COUNT`는 **같은 값의 화면 이름·내보내기 이름**이다(맵 화면 `COPY HEADER MODE`의 우측 보조표가 `VALUE \| COUNT \| STACK \| DESC`). 색은 앱이 소유한다. `*` 붙은 둘은 옛 시트 호환용 |
| 표② 자재 롤업 머리줄 | `MAT` · `BIN` · `MAP` · `가용` · `사용` · `사용≈` · `잔여` · `잔여≈` | ⚠️ **아직 붙여넣을 수 없다** — 아래 참조 |

> ⚠️ **표②(자재 롤업)를 되붙이는 기능은 아직 없다.** 위 여덟 단어가 목록에 있는 것은 **나중에 그 경로가 생겼을 때를 대비한 준비**이지, 지금 표②를 붙여넣을 수 있다는 뜻이 아니다. 지금 붙여넣기를 받는 것은 **표①(값 정의) 하나**다.

### 4.2 맵 화면에서 회사 양식 되붙이기 (Ctrl+V · 2026-07-30)

맵 화면의 `COPY HEADER MODE`로 내보낸 **회사 본딩맵 양식**을, 엑셀에서 고친 뒤 **그대로 되붙일 수 있다.** 종전에는 쓸 수만 있고 읽지 못했다.

- **동선은 맵 화면에서 Ctrl+V 하나.** 버튼도 메뉴도 없다(브라우저가 버튼 클릭으로는 클립보드를 읽지 못한다).
- **확인창 1회** 후 격자와 DOE가 화면에서 교체된다. **서버에는 아무것도 쓰지 않는다** — 저장은 여전히 `⚡ Push` 하나다.
- 되돌아오는 것과 아닌 것:

| 항목 | 되붙일 때 |
|---|---|
| **격자 셀** | 전부 복원 — **빈 칸까지 그대로**(복사본에서 비어 있던 칸은 화면에서도 비워진다) |
| **VALUE · STACK · DESC** | 복원된다 |
| **COUNT** | 알아보되 **버린다** — 칠한 셀 수는 격자에서 세는 값이지 붙여넣기로 정하는 값이 아니다 |
| **1H · MID · TOP (자재)** | ❌ **되돌아오지 않는다** — 상단 그룹 띠는 읽지 않는다(빈 그룹과 병합된 칸을 평문에서 구별할 방법이 없어서다) |
| **COLOR** | ❌ 되돌아오지 않는다 — 기존 값은 자기 색을 유지하고, 새로 생긴 값은 앱이 색을 배정한다 |

- 🔴 **붙여넣기는 값을 지우지 않는다.** 복사본에 없는 값은 "삭제하라"가 아니라 **"이 복사본이 말하지 않은 것"**이다. DOE 값을 없애려면 표①의 삭제 버튼을 쓴다.
- **안 맞으면 사유를 붙여 거부한다** — 열 수·행 수가 다르거나, 다른 맵의 복사본이거나, **복사할 때와 회전·면이 다르면**(치수가 같아도 잡힌다). 대충 얹어 놓지 않는다. 거부되면 복사할 때의 화면 상태로 되돌린 뒤 다시 붙여넣으면 된다.
- 잠긴 셀(`F` 등)은 건드리지 않고, 몇 칸을 그냥 두었는지 토스트가 말해 준다.

#### ⚠️ 대부분의 맵에서는 아직 되붙일 수 없습니다 — **시트가 아니라 맵의 기하 때문입니다** (2026-07-30)

거부 문구가 이렇게 나오면:

```
이 화면의 프레임에는 노치 표식(D)이 놓일 자리가 없어 복사본의 회전·면을 대조할 수 없습니다
```

**당신의 엑셀 시트에는 아무 문제가 없습니다.** 이 맵의 **물리 규격**이 문제입니다.

- **왜 대조가 필요한가**: 회전(0↔180)이나 면(앞↔뒤)이 바뀌어도 **격자의 가로·세로 칸 수는 그대로**입니다. 그래서 열 수·행 수·맵 이름 어느 것도 걸리지 않고, **격자가 통째로 뒤집힌 채 들어갑니다**(실측: 12×10 맵에서 셀 120개 전부의 값이 바뀌었습니다). 그것을 잡는 신호는 **노치 표식 `D` 한 칸**뿐입니다.
- **왜 그 칸이 없는 맵이 많은가**: 노치는 웨이퍼 원 **바로 밖**에 찍힙니다. 물리 규격이 등록되지 않아 "격자 전체를 웨이퍼로 보는" 기본 프레임으로 열린 맵에서는 그 자리가 **격자 밖**이라 찍을 곳이 없습니다.
- **현재 범위**: 선언된 맵 **179개 중 노치가 격자 안에 들어오는 것은 27개**입니다. **나머지 152개에서는 되붙이기가 거부되고, 그것이 지금은 정상 동작입니다.**
- **지금 할 수 있는 것**: 그 맵에 **웨이퍼 원 규격(프리셋)을 적용**해 노치가 격자 안에 들어오게 하면 왕복이 됩니다. 규격을 바꿀 수 없는 맵이라면 **지금은 되붙이기를 쓸 수 없습니다** — 복사(내보내기)는 그대로 됩니다.
- ⏳ **후속은 대기열에 있고 아직 만들어지지 않았습니다.** 제목 줄에 회전·면 토큰을 함께 실어 노치 없이도 대조하는 방식이 정해져 있지만 **구현 전**입니다. 그때까지 "회사 양식 왕복"은 **일반적으로 되는 기능이 아니라 27개 맵에서 되는 기능**으로 읽으십시오.
- 🔴 종전에는 이 상태에서 **경고 한 줄만 띄우고 통과**시켰습니다. 확인창 다섯 줄 중 한 줄이라 아무도 보지 못했고, 그래서 뒤집힌 격자가 그대로 들어갔습니다. **거부가 그 수리입니다.**

## 5. 색칠

1. 표①에서 값 행을 클릭해 **브러시로 선택**
2. 맵에서 **드래그**로 칠하기 · 우클릭 드래그로 지우기 · 전체 채우기/선택 영역 채우기 버튼
3. 칠한 순간 표②의 **사용≈**이 바로 바뀐다 (**저장되는 셀** × 층수)

`F` 등 잠금 값으로 선언된 셀은 보호되어 덮어지지 않는다.

> **`🎨 Fill All`은 유효 다이 안쪽만 칠한다.** 바깥으로 건너뛴 칸이 있으면 토스트가 몇 칸인지 말해 준다(예: *"1,842칸을 'A'로 칠했습니다 (유효 다이 밖 512칸 제외 — 저장되지 않는 셀입니다)"*). 한 칸도 못 칠했으면 그것도 말해 준다 — 그때는 물리 규격(직경·칩 크기)이나 유효 다이 맵 지정을 확인하면 된다.

## 6. 표② 자재 롤업 — 읽는 법

| 칼럼 | 뜻 |
|---|---|
| **MAT** | 자재 토큰 (행 클릭 → 그 자재의 맵으로 이동. **없는 맵도 빈 채로 열린다**) |
| **MAP** | 그 자재의 맵 존재 여부 (`?` = 확인 안 됨) |
| **가용** | 서버가 계산한 쓸 수 있는 수량 |
| **사용≈** | 이 계획의 소요 — **균등 분배 가정값**(어느 wafer를 먼저 쓸지는 기록 안 하므로 ≈) |
| **잔여≈** | 가용 − 사용 |

> 🔴 **「미상」은 0이 아니다.** 미상 = "검사 못 했다"이지 "없다"가 아니다. 가용이 미상이면 잔여도 미상이다. 시스템은 모르는 것을 절대 0으로 보여주지 않는다.

- **↻ 가용** 버튼으로 재조회. 조회 후에도 미상이면 토스트와 셀 툴팁이 **이유**를 말해준다(예: "BIN 축이 선언돼 있지 않습니다").
- BIN별 가용은 관리자가 config에 `bin_map`을 선언해야 켜진다([CONFIG_GUIDE §5.8](CONFIG_GUIDE.md)). 미선언은 고장이 아니라 **아직 배선 안 됨**이다.

### 6.1 `MAP X`가 저절로 풀린다 — `↻ 가용`을 누르지 않아도 (2026-07-30)

**종전 증상:** dt 맵을 새로 만들어 저장하고 본딩 맵으로 돌아오면 `MAP` 칸이 계속 **`X`(그런 맵 없음)**였고, **`↻ 가용`을 누를 때까지 풀리지 않았습니다.** 방금 자기 손으로 만든 것을 시스템이 없다고 말하는 상태였습니다.

원인은 "틀린 답"이 아니라 **묻지 않은 것**이었습니다. 두 가지가 겹쳐 있었습니다 — ① 화면이 갱신을 **"맵 정체가 바뀌었을 때"**만 돌렸는데 같은 맵을 다시 여는 것은 정체 변화가 아닙니다 ② 그리고 `X`라는 답이 **한 번 캐시되면 다시 묻지 않았습니다.**

**지금 자동으로 다시 확인하는 순간은 셋입니다:**

| 언제 | 무엇이 갱신되나 |
|---|---|
| **맵을 불러올 때**(📂 Load — 같은 맵을 다시 열어도) | 자재 맵 유무(`MAP`)·가용량 |
| **`⚡ Push`가 성공했을 때** | 같음 |
| **STACK을 `0`↔`0 아님`으로 고쳤을 때** | 그 값의 롤업 행이 생기거나 없어지므로 그 풀만 |

- 🔴 **`X`와 `?`는 다시 묻고, `✓`는 다시 묻지 않습니다.** `✓`(맵 있음)는 이 흐름에서 사라질 일이 없고 사용자를 행동으로 밀지도 않으므로 낡아도 해가 없습니다. 반대로 **`X`는 사용자가 방금 무효화했을 수 있는 유일한 답**이고 `?`("확인 못 했다")를 붙들고 있으면 **장애가 사실로 굳습니다.**
- **`↻ 가용`은 없어지지 않았습니다.** 위 셋 밖에서(예: 다른 사람이 방금 맵을 올렸을 때) 직접 묻는 버튼이고, **토스트로 답하는 것은 이 버튼뿐**입니다 — 자동 확인은 조용히 돕니다(읽기는 무마찰).
- **STACK을 `3 → 4`처럼 고치는 것은 아무것도 발동시키지 않습니다.** 풀이 새로 생기지 않기 때문이고, 그래야 타건마다 조회가 나가지 않습니다. **빈칸 → 3**도 경계를 넘지 않습니다(빈칸은 마커가 아니라 "아직 안 적었다"이고 그 행은 이미 롤업에 있습니다).
- 🔴 **폴링(주기 자동 조회)은 없습니다.** 그것은 "화면이 언젠가는 맞아진다"를 사 주고 "화면이 묻지 않는다"를 그대로 남깁니다.

## 7. 저장 — 규칙은 하나다

> **⚡ Push Map Data가 유일한 저장이다.** 자동 저장은 없다.

- 편집하면 **이 브라우저에 초안**으로 보관된다 — 탭을 닫거나 새로고침해도 사라지지 않고, 다시 열면 마지막 맵이 자동으로 다시 열린다.
- 계획 헤더의 칩이 상태를 말한다: `● 저장 안 됨 · [⚡ Push]로 저장` / `저장됨 HH:MM` / `변경 없음`.
- Push는 맵과 계획을 **함께** 올린다. 서버에는 Push 전까지 아무것도 쓰지 않는다.
- 다른 PC에서는 초안이 안 보인다(브라우저 보관). 공유하려면 Push.

## 8. 검수 메시지 — 막는 것과 알리는 것

**검수(V1~V6)는 보고만 하고 저장을 막지 않는다.** 불완전한 계획도 Push된다 — 고치라고 말해줄 뿐이다.

저장을 **막는** 것은 데이터가 **파괴될 때**뿐이다:
| 차단 사유 | 뜻 |
|---|---|
| 층 구조 컬럼 없음 | 지금 저장하면 층 구조가 버려진 채 계획이 교체됨 — **계획이 틀려서가 아님** |
| 옛 형식 해석 불가 | 옛 구간 데이터를 뭉개면 정보가 사라짐 |
| 적재 누락 감지 | 값이 들어 있는 셀 중 **이번 적재에 담기지 않는 것**이 있어, 이대로 올리면 그 셀들이 서버에서 **삭제**될 상황. 확인창이 뜨기 전에 막고 **몇 개가 삭제될지 개수로** 말해준다 |

> **적재 누락이 뜨면 두 가지 중 하나다.**
> - **프레임이 맵을 못 덮는다** — 격자 크기·시작 좌표·회전·물리 규격을 맵 전체가 보이도록 맞추면 풀린다.
> - **유효 다이 밖에 값이 남아 있다** — 2026-07-30 이전에 `🎨 Fill All`로 칠해 둔 뒤 저장하지 못하고 이 브라우저에 남은 **초안**이 이 경우다. 지금의 `Fill All`은 유효 다이 밖을 칠하지 않으므로 **새로 칠하는 맵에서는 더 생기지 않는다.** 이미 남아 있는 초안을 정리하는 수정은 **아직 작업 중**이라, 그런 맵은 당분간 계속 거부될 수 있다. 거부 문구가 말하는 개수가 **격자를 아무리 넓혀도 줄지 않으면** 이 경우다.

## 9. 자주 묻는 것

- **"DOE 잘못 입력해도 저장돼요?"** — 된다(의도). 검수 메시지가 뭘 고칠지 알려준다.
- **"반반 실험은?"** — 값 두 개 만들어 반씩 칠하면 된다. 값별로 STACK·자재가 다르면 그게 곧 split이다.
- **"dt_map에도 자재 적고 싶은데"** — `STACK=1`에 MID만 적으면 된다(1H·TOP 비움).
- **"수량이 어제랑 달라요"** — 누가 셀을 칠했거나 dt 맵이 바뀐 것. 수량은 저장된 게 아니라 **매번 계산**된다.
- **"아무것도 안 했는데 2026-07-30에 숫자가 내려갔어요"** — 그날부터 수량이 **저장되는 셀만** 세기 때문이다. 아래 표가 어느 맵에서 얼마나 내려가는지다. **사라진 숫자는 원래 저장될 수 없던 셀이다** — 서버에 올라간 적도, 화면에 색이 나온 적도 없다. 그러니까 **지금 보이는 숫자가 처음으로 맞는 숫자**다.

| 어떤 맵인가 | 얼마나 내려가나 |
|---|---|
| **`⚡ Push`로 저장해 온 맵을 불러온 것** | **0%** — 변화 없음(실측). Push는 유효 다이 밖 셀을 저장한 적이 없다 |
| 새로 만들면서 **`🎨 Fill All`을 쓴 맵** (규격 프리셋 사용) | **약 35~51%** — 프리셋에 따라 다르다(격자 사각형 중 유효 다이 밖의 비율) |
| 새로 만들면서 `Fill All`을 쓴 맵 (**규격 프리셋 없음**) | **약 7%** — 격자가 웨이퍼보다 작게 잘려 있어 밖에 해당하는 셀 자체가 적다 |
| **드래그로만 칠한 맵** | 사실상 **0%** — 유효 다이 밖은 원래 손으로 칠하기 어렵다 |

> ⚠️ **예외 하나 — 파서가 인제션한 맵.** 인제션은 `⚡ Push`를 거치지 않으므로, 이미 저장된 행 중 일부가 **현재 물리 규격(또는 지정된 유효 다이 맵)의 밖**에 놓일 수 있다. 그런 맵은 서버에서 불러왔는데도 수량이 내려가고, 그건 **데이터가 잘못된 것이 아니라 이 맵의 물리 규격·유효 다이 지정이 실제 데이터와 맞지 않는다는 신호**다 — 규격을 맞추면 다시 잡힌다. 🔴 **맞추기 전에는 `⚡ Push`하지 말 것**(적재 누락 게이트가 막아 주지만, 그 거부가 바로 이 신호다).

> 같이 사라진 증상: `Fill All`을 쓴 맵에서 **`⚡ Push`가 계속 거부되던 것**(*"현재 프레임이 맵 전체를 덮지 못합니다"*)도 이 원인이었다. 격자 크기를 아무리 맞춰도 안 풀렸던 이유는 그 셀들이 **격자 밖이 아니라 유효 다이 밖**이었기 때문이다.
