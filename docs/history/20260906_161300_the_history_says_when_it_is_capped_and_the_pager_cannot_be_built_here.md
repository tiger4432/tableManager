# 전역 이력이 «잘렸다고» 말한다 — 그리고 페이저는 «이 쪽에서 지을 수가 없다»

> **커밋:** `8929c756` (16:13)
> | **일자:** 2026-09-06
> **레인:** 구현자(클라)
> **측정 상자:** 이 워크스테이션. **운영이 아니다.**

## 지을 수 있는 절반을 짓고, **나머지 절반은 «존재하지 않는다»는 것이 밝혀졌다**

```
`/audit_logs/recent` 는 `next_cursor` 를 «내보내고» «받지는 않는다»:
   `get_recent_audit_logs(response, limit_groups=100, db)` 가 시그니처 «전부»다
   그리고 다른 감사 라우트 하나는 `tx_id` 와 `limit` 만 받는다
=> 🔴 그 커서는 이 탭의 «모든» 절단에 대해 «쓸 수가 없다» — 커서 없는 상태만이 아니다
=> 여기 어떤 컨트롤을 놓아도 「눌리는데 아무 데도 안 가는」 실패가 된다
```
🔵 그래서 「사실만 적는 줄」이 **유일하게 가능한 모양**이고, 그 근거가 지난 라운드에 댄 것보다
«강하다» — 「지금은 안 만든다」가 아니라 «이 쪽에서 지을 수 없다»다.

## 그리고 절단은 «본문에서» 읽는다

판정대로 «응답 본문»에서 읽으므로, 「커서 없는 상태가 도달 가능한가」에 아무것도 걸려 있지 않다.
상류의 질의 술어가 언젠가 바뀌어도 이 줄은 **그때 온 것을 그대로 읽는다.**

## 확인한 것 (diff 실측)
```
client2/src/timeline.js +35 · 하니스 `history_paging_harness.mjs` +11
```
📎 큐 등급 1 «F-12». 🔵 이 항목은 「닫힘」이 「기능을 다 지었다」가 «아닐 수» 있다는 예다 —
   물음이 답해졌고, 못 짓는 절반은 «왜 못 짓는지»가 적혔다.
