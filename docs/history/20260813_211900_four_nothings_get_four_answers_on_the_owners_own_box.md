# Four nothings get four answers on the owner's own box

**Date:** 2026-08-13 21:10/21:19 · **Domain:** Server + Client (원장) · **Status:** 착지 — `d78e1ec`, `92b3b47`

---

## 배경 — 소유자가 추적 화면을 열었고 아무것도 안 나왔다

소유자의 말: 「찾을 수 있는 것과 없는 것의 경계가 너무 막연해.」 맞는 말이었고, 원인은
**완전히 다른 네 상황이 똑같이 렌더**된 것이었다: 원장 미설치 / 원장 비어 있음 /
모르는 랏 / 아는 랏인데 혈통 주장이 없음. 게다가 소유자의 박스(`assy_manager`)에는
`ledger_events`가 **아예 없었다.**

## 서버 (`d78e1ec`) — 백필과 커버리지

`assy_manager`가 마이그레이션·백필됐다: **909 원자, 추적 가능 25랏**, 소스 44행 전부.
카운트는 **백필 전에 쓰고 다른 박스에서 보정한 오라클**(raw SQL, `server/ledger`
무임포트)과 교차검증 — `assy_qa`의 878을 먼저 정확히 재현한 뒤에야 신뢰했다.
`event_type`이 `123`인 실제 행 1건은 `undeclared_vocabulary`로 이름 붙어 거절됐다.

- **총괄의 프레이밍이 뒤집혔다**: `assy_manager`가 «더 부유한» 박스다(43행 전부 + 1,
  공유 15행은 내용이 다름 — QA 편집 마커 대 실측값). 909 > 878의 이유.
- `GET /api/ledger/coverage`가 `{state: absent|empty|ready, lots, sources, …}`를 답하고,
  부재 릴레이션의 `/trace`는 산문 대신 구조화된 503을 답했다 — R-C의 원칙 그대로.

```python
COVERAGE_STATES = ("absent", "empty", "ready")
rows = _fetch(connection, "SELECT to_regclass(%(rel)s) IS NOT NULL", ...)
```

- **이 라운드에서 가장 값진 것은 철회다.** absent-table 갈래에는 **테스트가 아예
  없었다.** 영어 문자열 매칭이 한국어 로케일에서 죽었다는 주석을 썼다가 — mutant가
  매칭이 `"UndefinedTable"`(SQLAlchemy `__str__`의 드라이버 클래스명 접두)에 **실제로
  격발함**을 증명했다. 고장은 아니었지만 아무것도 보장하지 않는 문자열 위였고,
  판정은 카탈로그(`to_regclass`) + SQLSTATE `42P01` 백스톱으로 옮겨졌다.
- `count(register)`는 **일부러 캐시하지 않았다**: 이 엔드포인트는 「방금 돌린 단계가
  들어갔나」를 답하는 자리라 5초 캐시는 정확히 그때 거짓말한다.

## 클라이언트 (`92b3b47`) — 네 가지 없음이 네 문장을 받는다

빈 상태가 「무엇을 물을 수 있나」를 답했다 — 추적 가능 랏 수·출처·기간·클릭 가능한
샘플(`<a href="?lot=…">` 실제 앵커, 핸들러 0). **페이지 전체 폼 컨트롤 여전히 1,
버튼 0.**

- **총괄의 지시서가 두 번 틀렸고 레인이 지나쳐 측정했다**: 25랏은 `CL-2601-*`만이
  아니라 12/12/1이었고, `CL-2601-005`는 `derived_from` 0건인 **뿌리**였다. 실제 사슬은
  `CL-2601-006-A1-A7 → …-A1 → …-006`.
- **서버 계약이 작업 도중에 바뀌었다**(같은 이음새에 두 레인): 시작 시점엔 `/coverage`가
  없고 거절이 맨 산문이었다. 레인이 마감 전 라이브 라우터를 재독하고 리더를 다시 썼다.
- mutant 둘이 「던져서 잡힘」이었다 — 스위트를 중단시키는 throw는 판정이 아니다.
  관용 접근자로 재무장 후 291 단언, 41/41 결함 포착, 대조군 2/2 탈출.
- 하중 쌍: 같은 캡처 추적에 두 원장 상태를 먹여 **추적 내부만으로는 빈 원장과 모르는
  랏을 구별할 수 없음**을 못 박았다. 세대 걷기도 `[root]`로 끝나므로 종료 태그만 보는
  규칙은 동작하는 답을 「혈통 없음」이라 발표한다 — 그 반대편도 mutant 둘이 잡는다.

## 그때 남아 있던 것

- `d78e1ec` 검증: 202 passed, 2 skipped(옵트인 비용 프로브). 스위트 후 재계수
  `assy_manager` 909 / `assy_qa` 878 무변동. `92b3b47` 게이트: `check:harnesses` exit 0,
  `check:contracts` exit 0(계약 7종). 레인 자체 포트의 **빌드된 dist**에서 검증.
- 런북 §6은 **개발 박스 완료**로만 마킹 — 운영은 아무것도 받지 않았다.
- 샘플 랭킹은 정확-임의-스케일이 아니라 유계다: 인덱스 순서 300행 창(≈2 ms), 20건의
  `derived_from`은 창 안에 넉넉히 들었다.
