# 스물둘을 접었더니 시험 119개가 빨개졌다 — 그래서 «열넷»이 착지하고 «여덟»은 이름만 남았다

> **커밋:** `386911e9` (18:53) · `8a7e270f` (18:59) · 보고 `71a38bc3` · 승인 `886f80e6`
> | **선행:** `98a8d82a` (09-02 21:50) · 지시 `74928b79`·`3dc3eafc` (09-02 21:30~21:37)
> | **일자:** 2026-09-03 저녁
> **레인:** 구현자(서버)
> **측정 상자:** 이 워크스테이션. **운영이 아니다.**

## 배경 — 같은 판단이 «여러 자리»에서 «각자» 철자되고 있었다

### 선행: 복합 정체가 «네 자리»에서 조립되고 있었다 (`98a8d82a`)

```
🔴 그리고 «아직 안 갈라져» 있었다 — 네 자리를 한 입력으로 몰아 본 시험이
   «한 줄도 바꾸기 전»에 통과했다
   그것도 구분자가 기본 _ 이 아니라 «|» 인 표(dt_log)를 통해서 —
   하드코딩된 구분자는 «기본값을 쓰는 표에서는 안 보인다»
=> 갈라진 뒤 «고친» 것이 아니라 갈라지기 «전»에 잠갔다
```

🔴 **철자는 공유하고 «정책»은 안 한다.** `compose_business_key` 는 값과 표를 받아
구분자·순서·`clean_str_value` 를 답하고 — **공백 여부는 «일부러» 판정하지 않는다.**

```
네 호출자가 구성요소 결손에 «네 가지» 다른 일을 한다:
   키 조립 거부 · 호출자가 준 키로 대체 · None 을 냄 · «부분 키를 그대로 조립»
   (마지막은 2026-08-05 소유자 판정 — 「부분 키는 살아남은 것으로 동작한다」)
=> 공백 검사를 공유 함수에 접으면 그 판정이 «조용히 뒤집힌다»
```

## 🔴 ① 접기 «전»에 술어를 재 봤다 — 그리고 «두 가족»이 있었다

`386911e9`. `v is None or str(v).strip() == ""` 가족과 리터럴 `clean_str_value(v) == ""`
를 `is_blank_value` 와 **열두 값 행렬**로 견줬다.

```
불일치 «0» — 0 · 0.0 · False 포함
```

그런데 **접으면 «안 되는» 가족이 따로 있다**:

```
str(v or "").strip() 가족  —  같은 행렬에서 «다섯 번» 불일치 (0 · 0.0 · False · [] · {})
=> 통합의 이름으로 «동작을 바꾸는» 것이다
```

## 🔴 ② 갈림은 «공백»에 대한 것이 «아니었다» — «import 도달 가능성»이었다

```
관련된 모든 파일이 crud 를 «함수 안에서 지연» import 한다 — 모듈 수준이면 «순환»이다
   (crud 가 virtual_join_executor 를 import 하고, 그 모듈이 자기에 대해 같은 제약을 기록해 뒀다)
=> 「한 자리에 한 줄」은 «감싼 함수가 이미 crud 를 들고 있을 때»만 성립한다
```

🔴 **눈이 아니라 «파싱»(ast)으로 자리마다 확인했다: 열넷은 이미 들고 있었고, 여덟은 아니었다.**

```
접은 열넷   database/crud.py ×6 · chain_replay:375 · enrichment_mapper:212
            · parsers/void_sat_format:436 · enrichment_candidates:465 · :923
            · enrichment_config:1199 · frame_confirmation:460
            · scripts/rebuild_blank_business_keys:200
남긴 여덟   dt_map_derivation:405 · :910 · enrichment_backfill:140
            · map_overlay:602 · :955 · transfer_plan:3206 · :3278
            · migrations/migrate_jsonb_numeric:20
```

여덟이 치를 «값»이 둘 다 나쁘다 — 모듈 수준 import 는 `map_overlay`·`transfer_plan` 이
피하던 **순환으로 곧장** 들어가고, 함수 지역 import 는 **셀마다 도는 루프 «안»에**
`sys.modules` 조회를 넣는다.

```
=> 그건 «공백»에 대한 판단이 아니라 «import 구조»에 대한 판단이고,
   이 라운드가 받은 일이 아니다
```
⚠️ 그리고 `mappers/*.py` 네 자리는 어차피 gitignore 다(추적본은 `.sample`) — 라이브
파일을 고쳐도 **출하되지 않는다**.

## 🔴 ③ 되돌린 시도 — 스물둘을 다 접었다가 «119개»가 빨개졌다

커밋 본문이 자기 제목 아래 이것을 적어 뒀다.

```
첫 시도    «스물둘» 전부 — crud 가 «모듈 수준 이름»이라는 전제 위에서
결과       시험 «119» 빨강 ·  name 'crud' is not defined
           (map_overlay · transfer_plan · dt_map_derivation · map_alignment)
복구       만진 파일 «열하나» 전부 HEAD 내용으로 되돌림
           git status 로 깨끗함 확인 · 재실행으로 잃은 것 없음 확인
           🔴 그중 «셋»은 CRLF 만 다른 상태였다 — 줄바꿈도 되돌렸다
=> 위의 «범위 검사»는 그 사고 «때문에» 존재하고,
   그래서 이 시도가 스물둘이 아니라 «열넷»이다
```
`886f80e6` 가 반쪽을 승인한 사유가 그것이다 — **「깨뜨렸다가 되돌린 것이 이 커밋을
읽을 수 있게 만든 이유다」.**

## ④ 두 번째 가족은 «세고 이름만 붙였다» (`8a7e270f`)

```
str(v or "").strip() 가족 — «67 자리»
   한 철자를 grep 한 앞선 계수는 이 가족을 «통째로» 놓쳤다 (== "" 가 안 들어 있어서)
   -> 철자가 아니라 «파싱»으로 셌다
경고는 «67군데가 아니라» is_blank_value «자기»에 붙었다
   -> 판단을 67군데에 복사하면 «불일치할 기회가 67개»다.
      그게 이 감사가 다루는 병이고, 이번 주 조인 라운드가 «커밋 넷»으로 되돌린 것이다
   -> 그리고 접으려는 사람은 어차피 그 함수에 «도착한다»
```

🔴 **물 수 있는 자리는 «정확히 하나»다** — `ledger/backfill.py` 의 등록 프로브가
`frame[column].tolist()` 를 읽어서, 프로브 컬럼의 리터럴 `0` 이나 `False` 가
«공백»으로 불리고 그 대상이 **오류도 로그도 없이 건너뛰어진다.**

```
오늘은 결함이 «아니다» — 출하 선언이 지목한 프로브 컬럼은 전부 «식별자»다
그런데 문법은 columns 를 «이름 목록»으로만 제약하고 «타입»에 대해 아무 말도 안 한다
=> 숫자 프로브 컬럼은 «설정 한 줄» 거리에 있다
```
판정대로 **고치지 않고 그 자리에 이름을 붙였다.**

## 🔴 ⑤ 레인 «자기» 정직 단서

`71a38bc3` 이 스스로 적었다 — 나머지 66은 각 가드 표현식이 «어디서 값을 받는지»
따라가서 확인했지만, **문자열 아닐 법한 자리 «열 개쯤»만 출처까지 추적**했고 나머지는
구성상 문자열 영역이라고 «판단»했다.

```
🔴 「없더라」로 읽어야지 「없다」로 읽으면 안 된다  <- 레인이 명시적으로 요청
```
그리고 지시서의 혼동 하나가 정정됐다 — 앞 보고의 「다섯」은 «자리 다섯»이 아니라
**«값 종류 다섯»**이었다.

## 아키텍처 영향

- 공백 판정의 «철자»가 `is_blank_value` 하나로 모인다 — **열넷 자리**에서.
  `compose_business_key` 는 여전히 **공백을 판정하지 않는다**(네 호출자의 정책이
  각자 다르고, 그것이 시험으로 «다름»이 단언돼 있다).
- `str(v or "").strip()` 가족(**67 자리**)은 **다른 술어**로 표시됐다 —
  경고가 `is_blank_value` 자기에 붙어 있다.

## 스위트 (전부 이 상자)

```
98a8d82a  변이 5. 그중 «기본 구분자 하드코딩»이 «동의 시험 넷»을 한꺼번에 빨갛게 한다
386911e9  술어 동치를 «열두 값 행렬»로 확인 — 불일치 0
          (되돌린 첫 시도: 시험 «119» 빨강)
```

## 그때 남아 있던 것

- **여덟 자리는 안 접혔다.** 이유는 공백이 아니라 «import 구조»이고, 그 판정은
  이 라운드 밖이다.
- **67 자리는 «세어졌고» 안 고쳐졌다.** 물 수 있는 한 자리(`ledger/backfill.py`)는
  이름이 붙었다.
- `all(v != "")` 대 `is_blank_value` 의 차이는 `98a8d82a` 가 **일부러 안 건드렸다** —
  그것이 S2 항목이라고 적혀 있다.
- 나머지 66의 판단은 **전수 추적이 아니다**(열 개쯤만 출처까지). 레인이 그렇게 보고했다.
