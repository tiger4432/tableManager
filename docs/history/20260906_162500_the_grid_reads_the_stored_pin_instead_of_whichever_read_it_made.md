# 그리드가 «저장된 핀»을 읽는다 — 그래서 「어느 읽기를 했는지」가 더는 답을 바꾸지 않는다

> **커밋:** `cab1250f` (16:25)
> | **일자:** 2026-09-06
> **레인:** 구현자(클라)
> **측정 상자:** 이 워크스테이션. **운영이 아니다.**

## 왜 「통합된 `priority_source`」가 아니라 이것인가

```
그 값은 서버에서 «두 번» 계산되고 «답이 다르다» (main.py:894-907)
   싼 읽기    `is_ow` 하나만 보고 'user' 를 켠다
   선언 읽기   `compute_priority_value` 에 맡긴다
=> 🔴 핀이 «화면이 어느 읽기를 했느냐»에 따라 보이기도 하고 안 보이기도 했다
```
`manual_priority_source` 는 **저장된 핀**이다 — 이미 서버가 싣고 있고(main.py:920),
소스 상세 패널이 이미 소비한다(main.js:1541). 그리드가 «그것»을 읽으면 두 계산을 «둘 다 우회»하고
**어느 쪽이든 같은 답**이 나온다.

```
⛔ 두 갈래를 «통합»하는 것은 C-7 이고 «서버 자리»다 — 여기서 안 한다
⛔ 새 부류도, 새 배지도 «없다» — 핀은 «덮어쓰기»이고 이 그리드가 덮어쓰기에 이미 주는 표를 단다
=> 넓어진 것은 «술어» 하나뿐이다
```

## 확인한 것 (diff 실측)
```
client2/src/grid.js +19 (셀 규칙 «둘» 다 `manual_priority_source` 를 같이 본다)
```
📎 큐 등급 1 «F-13». 🔵 기준 ④ 의 «읽는 쪽» 처방이다 — 두 계산을 고치는 대신
   «둘 다 우회하는 하나»를 읽게 했고, 통합 자체는 소유 레인에 남겨 뒀다.
