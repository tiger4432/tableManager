# A declaration nobody read becomes an allow-list that bites

**Date:** 2026-08-13 21:19→21:47 · **Domain:** Server (원장 config) · **Status:** 착지 — `5e77e93` → `8759160` → `eb1ae8b`

---

## 배경 — 가이드를 쓰다가 미끼 선언이 발견됐다

`5e77e93`이 원장이 한 번도 가진 적 없는 가이드(`LEDGER_GUIDE.md`)와 기술 스펙을 세우며
측정했다: **per-source `subject_type` 선언은 어떤 원자에도 닿지 않는다.**
`self.subject_type`은 한 번 대입되고 아무 데서도 읽히지 않으며, 호출부들은 `"Lot"`·
`"Wafer"` 리터럴을 직접 넘긴다. `config.validate()`는 결과가 없는 값을 검사하고 있었다.

문서 레인의 보고 문장은 뒤집혀 있었고 총괄이 재측정해 고쳐 실었다: 「모든 원자의
subject가 `Lot` 리터럴」이 아니라 **`Lot` 689 / `Wafer` 220** — 참인 문장은 「subject
type은 번역기의 하드코드 리터럴에서 오고 선언에서는 절대 오지 않는다」였다. 어느 쪽이
실리느냐가 중요했다: 틀린 쪽은 다음 독자를 이미 존재하는 `Wafer` 경로 사냥에 보낸다.
그리고 표면 버그보다 깊은 형태가 기록됐다 — **단수 `subject_type`은 애초에 틀린
모양이다.** `lot_event` 하나가 두 엔티티 타입에 대해 원자를 찍는다.

## 판정 R-D (`8759160`) — 물지 않는 선언은 존재하지 말 것

선언은 삭제가 아니라 모양 교정: **번역기가 말해도 되는 엔티티 타입들의 복수
allow-list**가 되고, 게이트가 멤버십을 이름 붙은 거절로 강제한다. 상설 원칙:
**모든 선언 필드는 집행 지점이 필요하고, 없이 추가하는 것 자체가 결함이다.**

## 구현 (`eb1ae8b`)

```json
"subject_types": ["Lot", "Wafer"]
```

- 범위 밖 원자는 `undeclared_subject_type`으로 거절·계수. 리터럴은 남는다 — 코드가
  원자의 «사실»을, 선언이 허용 «범위»를 소유한다.
- `screen_molecule`은 선언을 **기본값 없는 필수 4번째 인자**로 받는다 — 기본값은
  한 층 위의 미끼 필드다. 검사는 `check_subject_keys`보다 앞서 돌아 거절이 드리프트를
  이름 댄다(「이 번역기가 equipment에 대해 말하기 시작했다」).
- 양팔 격발: `Equipment` register 원자가 이름 붙어 거절되고 / 선언하면 무거절 착지
  / PG 테스트가 `Wafer`를 빼고 **실제 백필**을 돌려 분자 통째 거절·0 원자 저장.
- **mutation이 레인 자신의 테스트 결함을 잡았다 — 당일 세 번째 같은 형태**: 게이트가
  원자를 «수용»할 때 `AssertionError`를 던졌는데 공용 하네스 둘 다 그것을 성공으로
  읽는다. 가드가 꺼진 채 초록이었다. 수용 시 정상 리턴으로 고쳤고, 같은 형태의 형제
  주입 5건은 별건으로 넘겼다.

## 🔴 config 해시가 움직였다 — 재실행 의미가 바뀌었다

`translator_version`이 소스 선언 전체를 해시하므로 해시가 `d8d1c9e0` → `34311f15`.
평범한 재실행은 아무것도 안 쓴다(커서가 head). **`--reset-cursor` 재실행은 878 «새»
행을 옛 행 옆에 쓴다** — 같은 주장, 새 출처. 선언 변경의 설계된 의미다. 그래서
`assy_manager`는 일부러 재백필하지 않았다.

## 그때 남아 있던 것

- `eb1ae8b` 검증: 172 passed, 2 skipped(격리 `assy_qa`). 878 원자 claim-by-claim 대조
  — 878 built = 878 stored, 실데이터 무거절. 라이브·`.sample` 바이트 동일.
- `5e77e93`이 살려 둔 발견들: `columns.equipment`는 선언만 있고 읽는 곳 없음;
  `DISPLAY_TIMEZONE_RULING` 등 두 상수가 `#:` 블록이 아니라 모듈 docstring에 바인딩;
  **거절은 커서를 멈추지 않는다** — 거절된 `123` 행이 최대 `event_time`에 앉아 있고
  커서는 지나갔다.
- CODE_MAP §5-H: 32파일을 `aeddac8` 시점 블롭에서 읽음, 줄 번호 0개, 식별자 622개
  재-grep. §7의 Vite 엔트리 목록이 양방향으로 틀려 있던 것도 교정.
