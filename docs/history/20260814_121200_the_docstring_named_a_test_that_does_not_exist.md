# The docstring named a test that does not exist

**Date:** 2026-08-14 11:58 / 12:12 · **Domain:** 문서(원장) + Server (docstring 수리) · **Status:** 착지 — `9f24815`, `a9f1f8b`

---

## 문서 라운드 (`9f24815`)

읽기 면이 두 수준(인스턴스·타입)이 됐고 가이드·스펙·소유권 지도·프리미티브 카탈로그가
그렇게 말한다. 스펙은 응답 계약을 산문이 아니라 **번호 붙은 판정 열 개**로 든다 —
독자가 아니면 틀리게 재유도할 둘 포함: 0은 null이 아니다, 창은 카운트만 좁히고
선언된 구조는 절대 좁히지 않는다.

문서 라운드가 고칠 수 없는 결함을 찾았다: `ledger_structure.py`의 모듈 docstring이
**버린 설계**를 서술한다 — 해소 계급이 SQL CASE로 계산된다며
`test_the_sql_class_agrees_with_claim_class`라는 테스트를 인용하는데, 저장소에서 그
이름의 유일한 출현이 그 docstring이다. **존재하지 않는 테스트를 이름 대는 docstring은
단지 stale한 것보다 나쁘다** — 독자가 이름을 검증해 보고 아무것도 못 찾고도 파일의
어느 절반을 믿을지 모른다. 코드 수리로 큐잉되고 문서는 그동안 코드를 따른다. UI
설계를 바꾸는 발견도 설계자가 만날 자리에 기록됐다: 결함 관측은 원장에 없다 — 통합
어휘 전제는 오늘 참이 아니다.

## docstring 수리 (`a9f1f8b`)

헤딩이 반전됐다: 계급은 SQL에서 계산되지 **않는다** — 실제 메커니즘(페이로드 플래그를
그룹 키로 올려 `claim_class`를 그룹당 한 번)과 함께. `[SCALE]` 블록의 수는 **2배로
틀려** 있었다 — 재측정: 172 ms, 84,747 원자, 12 그룹, 2.03 µs/원자, 10M 외삽 20.3 s.
커밋의 수가 옳았고 docstring의 85 ms / 1 µs / 10 s가 아니었다. 미측정 「partition
pruned 85 ms」 줄은 아무도 안 잰 다른 수로 교체하는 대신 **삭제**됐다.

285 ms가 세 지시 대상(전체 호출 / 버린 SQL 사다리 / 재작성 전 수치)으로 유통되던
것도 스펙이 커밋의 수를 취하며 섞지 말라는 명시 경고를 달았다.

## 그때 남아 있던 것

- `a9f1f8b`의 정직한 회색: 구조 PG 테스트가 이 환경에서 **8 skipped, 0 passed**
  (픽스처가 안 붙는다). 변경이 산문뿐이라 위험은 0이지만 그 실행은 증거가 아니고
  증거로 읽혀서는 안 된다고 커밋이 스스로 적었다.
