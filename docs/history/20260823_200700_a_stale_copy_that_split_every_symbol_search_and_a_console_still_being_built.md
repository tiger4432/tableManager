# 모든 심볼 검색을 갈라놓던 사본, 경계를 말하는 임포트 줄, 그리고 dist 에 계속 실리던 은퇴한 콘솔

> **커밋:** `e13bcfdd` (17:53) · `75c32b50` (18:00) · `49484c61` (18:13) · `c002c108` (18:19)
> · `0e7e0ec4` (19:35) · `77ec3289` (19:18) · `720657aa` (20:07)
> | **일자:** 2026-08-23 저녁
> **레인:** 서버(트리 정리) + 클라(은퇴·픽스처)
> **측정 상자:** 이 워크스테이션. **운영이 아니다.**

## 배경 — 감사 셋이 「이 파일은 제외했다」를 먼저 써야 했다

`server/_qa1_head_probe.py`는 `map_alignment.py`의 **잘린 옛 사본**이었고, 심볼 검색에
**옛 코드로 답하고 있었다.** 감사 세 건이 결론을 쓰기 전에 그 제외 문장을 먼저 써야 했다.
`e13bcfdd`이 지웠다 — **삭제 5,993줄**(diff 로 정확히 확인됨).

그리고 `server/` 루트가 평평해서 **임포트 줄이 그 모듈이 워커 진입점인지 라우트 헬퍼인지를
말하지 않았다.** `75c32b50`이 원장 읽기 쪽을 패키지로 옮겼다:

```
server 루트 .py    71 -> 70 -> 58
루트에 남은 ledger_* 모듈   «7»
```

옮긴 세 파일은 임포트가 아닌 것 하나를 함께 고쳐야 했다 — 자기 위치가 한 층 깊어졌기 때문이다:

```python
# server/ledger_api/finding_kinds.py 외 2
server_dir = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
```

## 자기가 재던 코드보다 오래 산 단언 둘, 그리고 못 당기는 손잡이

`49484c61` — 게이트 뒤에 **자기가 지키던 코드보다 오래 산 단언 둘**이 있었다. 그 시점에
`binding_not_approved`는 **제품 파일 어디에도 안 나온다**(diff 로 확인 — `docs/`와 `task/`
마크다운에만 남아 있다).

`c002c108` — **아무도 당길 수 없는 손잡이**가 손잡이인 척하기를 멈췄다. 자유도 0인 선언은
계약이 아니라 사본이라는 판정을 코드 쪽에서 집행한 자리다.

## dist 에 실리고 있던 은퇴 화면

`rnd-console.html`은 **소유자가 죽었다고 판정한 화면**인데 `client2/vite.config.js`의
**엔트리로 남아 있어서 계속 빌드되고 서빙되고 있었다.** `0e7e0ec4`가 걷어냈다 —
**14파일 · 삭제 4,330줄**(내용 4,329 + vite 한 줄). 이 커밋의 인구조사 산수는 **이 묶음에서
가장 독립적으로 검증 가능한 주장**이었고, 합이 정확히 맞는다.

`77ec3289`는 장비 이벤트와 코멘트 픽스처를 **이미 원자를 들고 있는 이름들에 겨눠서** 심었다 —
`(eqp, chamber)` 22쌍 + 코멘트 대상 24 = **46 이름**(diff 로 확인).

`720657aa`는 **두 번째 레인에 두 번째 개발 포트가 필요해서** CORS 오리진 둘을 열었다.
레인이 거기서 막혀 있었다.

## 아키텍처 영향

- 원장 **읽기 쪽이 패키지(`server/ledger_api/`)를 갖는다.** 임포트 줄이 경계를 말한다.
- 서버 루트 파이썬 파일이 **71 → 58**. 루트에 남은 `ledger_*`는 일곱이고 **은퇴 울타리 안**이다.
- `rnd-console`이 빌드 산출물에서 빠졌다.

## 그때 남아 있던 것

- **시험 세 건이 빨간 채**로 남았다 — 이 변경 전에도 빨갰고, 그렇게 확인하고 안 건드렸다.
- 루트의 `ledger_*` 일곱은 **은퇴 울타리 안에 그대로** 있다.
- `0e7e0ec4`가 **하니스 셋이 빨갛다**고 스스로 신고했다(`case_control` · `ledger_trace` ·
  `load_shows_loaded_map`). HEAD 에서도 같아서 자기 것이 아니라고 적었다.
- `77ec3289`의 씨앗은 **아직 안 돌았다.**
- 맵 셀 자리표시자는 남았다 — **point 종류에 코덱이 없어서.**
