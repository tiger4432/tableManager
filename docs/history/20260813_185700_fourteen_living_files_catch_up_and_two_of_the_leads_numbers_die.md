# Fourteen living files catch up, and two of the lead's numbers die

**Date:** 2026-08-13 18:57 · **Domain:** 문서(리빙) · **Status:** 기록만 — `b2d7446`

---

## 배경

당일 2차 배치 — 원장 L1/L2/L3, void, R7, map_doe 은퇴, 타임존, 얼라이너 자동채움 —
가 리빙 문서에 도달했다. PRIMITIVES에 11항목이 붙었는데, 이름값을 하는 것은 기능이
아니라 **실패의 형태들**이었다: 레인의 픽스처가 레인과 합의하는 것, 양팔 격발이 필요한
가드, 틀렸지만 정형인 값, 거절하지 않는 라우트, 필드를 대신하는 reason 산문.

## 레인이 되돌려준 것 — 넷 다 총괄의 것이었고 여기서 고쳐졌다

1. **런북이 틀린 규칙을 인용했다.** `wafer_map_metadata` UNIQUE 인덱스가 R6(「마커는
   키가 아니다」)로 라벨돼 있었는데 의무는 **R2**(「선언된 `business_key`는 UNIQUE
   인덱스를 의무화한다」)였다. 번호를 바꿔치기하지 않고 검증했다 — 실제 R2 위반이었다.
2. **런북의 첫 규칙을 런북이 스스로 깼다.** `bee1aeb` 착지 후에도 §8이 「레인 진행 중,
   6항은 아직 돌리지 않는 편이」라고 읽혔다. 완료 항목은 지우지 않고 **마킹**했고,
   붙잡혀 있던 원장 테이블 마이그레이션 의존이 풀렸다.
3. **`docs/guide/config_reference/table_config.json`이 세 방향으로 stale**이었고
   손편집이 금지라 `.sample`에서 재복사 후 동일성 검증(19표, void 있음, map_doe 쌍
   없음, R3 수리 반영). 남은 두 표(`dt_job_attribution`·`eqp_frame_attribution`)는
   드리프트가 아니라 프레임 축 판정 대기였다.
4. **`c1aa1b9`의 미수리 결함에 소유자가 없었다.** 형태는 PRIMITIVES에, 상태(틀렸고
   미수리)는 보드에 — 수리를 일부러 안 하는 이유(레이어를 키에 넣으면 그 컬럼을
   생략하는 모든 피드가 un-key된다)와 함께.

## 지키기가 값을 낸 자리

`0b607f4`의 5쌍·22쌍 프레임 수는 개발 픽스처라 **어느 리빙 문서에도 전파하지 않았다.**
`frontend.md`의 배지가 2026-08-11에 멈춘 채 이후 라운드 내용을 담고 있는 것도 발견됐다.

## 그때 남아 있던 것

- 확인 불가라 **기록만 된 것**: 실제 SAT 파일은 한 번도 목격된 적 없음, `void_obs`는
  `bonding_log`와 조인하지 않음.
