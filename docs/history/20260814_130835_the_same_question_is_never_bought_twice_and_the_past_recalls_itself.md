# The same question is never bought twice, and the past recalls itself

**Date:** 2026-08-14 13:03 / 13:08 · **Domain:** 프로세스(본질 정본 — 질문·이력) · **Status:** 기록만 — `dfcd115`, `6ebd916`

---

## 재발행 가드 (`dfcd115`) — 소유자 질문에서 확정, 3층

① 요청 id 멱등(같은 «클릭»의 중복 — 기존) ② 조사 이력 칸(사람이 봄 — 기존 설계)
③ **의미적 서명 대조(신규)**: 발급 게이트가 액션 서명(종류 × 대상 집합 × 요구
측정/DOE 요인 셋)으로 «열린» 동일 질문을 대조해 확인 단계에 링크로 제시.

**하드 블록 금지**가 판정의 축이다 — 조건이 바뀐 재발행은 정당한 질문이므로.
대신 강행하면 `reissue_of` 참조가 강제로 붙어 「알고도 다시 물었다」가 원장에
남는다. 닫힌 동일 질문이면 발급 전에 **그 답을 먼저 보여준다** — 이미 감쇄된
놀라움에 수집 비용을 두 번 쓰지 않게.

## 과거 유사 소환 (`6ebd916`) — 하드코딩 금지, 가드의 일반화

소유자 지시: 유사 규칙을 코드로 박지 않는다 — **유사성 프로필이 이미 선언된
기계의 산출물**이므로. 두 층: ① 과거 조사 소환 = 오늘 케이스의 차이 서명을 과거
액션들의 근거 서명과 **부분 일치** 대조(재발행 가드의 일반화, 새 기계 아님) —
DOE는 명시적으로: 발급 확인 단계에 과거 유사 DOE와 **그 결론**이 자동으로 떠서
이미 답이 있는 실험 조합을 다시 사지 않는다. ② 과거 현상 탐색 = 놀라움 발생
시마다 에피소드 프로필을 축적 → 유사는 재계산이 아니라 조회.

**유사의 축 셋, 전부 선언 유래**: 요인 서명 겹침 · 공간 시그니처 · **기전 노드
공유** — 요인이 달라도 기전 그래프의 같은 중간 상태에 닿으면 유사, 문자열 공간이
아니라 기전 공간의 유사다. 선언이 자라면 유사도 공간이 따라 자란다.

## 그때 남아 있던 것

- 둘 다 문서다. 서명 대조·에피소드 프로필·상시 감시층 모두 이 시점 미구현.
  당일 스트레스 로그 S-001이 이 블록을 근거로 「같은 DOE 재발행」 시나리오를
  ✅ 처리됨으로 판정했다 — 설계로 처리됨이지 코드로는 아니다.
