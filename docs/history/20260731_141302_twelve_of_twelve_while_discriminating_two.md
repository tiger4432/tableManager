# 12/12를 찍으면서 실제로는 12개 중 2개만 구별하고 있었다

> **일자:** 2026-07-31 오후 | **관련 커밋:** `392fca4`(14:13) · 보드 정정 `282c270`(14:13)
> **담당:** 나(잘못된 수리를 승인) · 하네스를 수리한 구현자는 커밋에 기록이 없다
> **대상:** `client2/tests/copy_header_count_harness.mjs` (101줄 추가 / 33줄 삭제, 단일 파일)
> **선행 항목:** `20260731_090919_two_function_names_meant_the_opposite_of_what_they_said.md`(키 공간을 옮긴 라운드) · `20260731_125114_with_no_reference_the_circle_is_the_region_so_one_reaction.md`
> **계기·채점:** 이 항목 작성 시점에 HEAD에서 재실행 — `copy_header_count_harness` **107 통과 / 0 실패**, 변이 **12/12 검출**.
> 게이트: `check_harnesses.mjs` → **16개 중 11개 게이트, 게이트 전원 녹색**. 이 커밋이 빌드 게이트를 다시 열었다.

## 배경 — 내가 승인한 수리가 틀린 수리였다

`35e84c3`이 다이 인덱스의 원점을 격자 모서리에서 웨이퍼 중심으로 옮긴 뒤 이 하네스가 적색이
됐고, `prebuild`가 종료코드 1로 죽어 `dist` 빌드가 두 라운드 동안 막혀 있었다.

**나는 「고정 베이스라인을 옮기자」는 재베이스라인을 승인했다. 그것이 틀린 수리였고,
구현자가 거절했다 — 옳게 거절했다.**

핀이 하는 일이 하나가 아니라 셋이기 때문이다.

| 역할 | 필요한 베이스라인 |
|---|---|
| `INV-ⓐ-1` 가산성 | 변경 전후 대조 |
| `INV-F2-1` **역주입** | 그 결함이 실재했음을 증명하려면 `064550f` **이전** |
| `INV-F2b` **역주입** | 동상 |

그런데 키 공간이 맞는 커밋은 전부 `35e84c3` **이후**다. 거기로 옮기면 베이스라인 샌드박스는
`WORK_FNS` 없이 일부러 지어져 있으므로 `ReferenceError`로 죽는다.

> **핀은 낡지 않았다.** 낡은 것은 핀이 아니라 **픽스처가 리비전 사이로 나르던 것**이었다.

## 진짜 결함 — 원시 물리 키를 두 리비전 사이로 날랐다

`INV-ⓐ-1`은 베이스라인의 `gridData`를 **키째로** working tree에 건넸다. 그런데 키는 방금
바뀐 좌표계의 산물이다.

**11×9 픽스처 99셀 실측:**

| | |
|---|---|
| 두 리비전에서 키가 일치한 셀 | **0개** |
| 키가 우연히 존재해 **엉뚱한 셀**을 가리킨 것 | **30개** — 그러면서 통과했다 |
| 아무 셀도 가리키지 못한 것 | 69개 |

> 그 상태의 바이트 비교는 COPY HEADER MODE를 채점한 것이 아니라 **픽스처를 채점한 것**이다.

수리는 경계에서 한다 — 공유 픽스처를 **두 리비전이 똑같이 부르는 주소**(시각 셀 `r, c`)로
적고, 심을 때만 각 샌드박스 **자기 셀 팩토리**가 낸 키를 쓴다.

```js
function applyPaint(sb, plan) {
  const d = {};
  plan.forEach(([r, c, v]) => { d[sb.ctx.gridCells2D[r][c].key] = v; });
  sb.ctx.gridData = d;
}
```

이 파일의 다른 픽스처(loaded-map · `contaminatedDraftSandbox` · `layoutProbe`)는 이미 전부
이렇게 하고 있었다. 키를 나르던 곳은 **여기와 변이 루프 두 군데뿐**이었다.

그리고 우회가 실제로 일하고 있음을 계측해 남긴다 — 두 리비전의 키가 다시 같아지는 날
이 지표가 말을 하도록.

```js
evidence.keyspace = { cells: sameKey + diffKey, sameKeyInBothRevisions: sameKey,
                      differingKeys: diffKey, … };
```

## 하네스가 보지 못하게 된 것 둘

### ① `INV-F2-1`은 공허했다

베이스라인의 키를 working tree의 `insideByKey`에 넣어 찾았다. 키 공간이 옮겨진 뒤로 그 조회는
대부분 `undefined`를 돌려주었고, 그래서 `wronglyRemoved`는 **구조적으로 비어 있을 수밖에**
없었다. 단언은 통과했고 아무것도 보지 않았다.

대조를 `(r, c)`로 옮기고, **조회가 전부 미스가 아니었다는 것 자체를 단언**으로 추가했다.

```js
chk('INV-F2-1', 'the comparison resolved every cell it looked up (an all-miss lookup proves nothing)',
  removed.filter(rc => wInsideRC.get(rc) === undefined), []);
```

### ② 변이 스윕의 12/12는 장식이었다

축 5·6이 TSV를 `i % 2`와 `slice(-4)`로 읽고 있었다. **이 스위트 자신의 주석이 「colspan
병합 이후 그 판독은 거짓말」이라고 선언해 둔 바로 그 판독**이다(2026-07-29에 본 검사 블록은
HTML 판독으로 옮겼는데 **변이 루프만 옛 판독이 남아** 있었다). 그래서 두 축은 변이와 무관하게
**항상 적색**이었다.

증명 두 개가 나란히 있다.

1. **의미가 동일한 대조군 변이**(소스 끝에 주석 한 줄 추가)조차 「CAUGHT」로 기록됐다.
2. 꼬리가 `if (!red && f2bMutantIsRed(mutated))`라서, 앞이 항상 참이면
   **M7~M12의 진짜 탐지기는 한 번도 실행되지 않는다.**

> **12개 중 2개만 구별하면서 12/12를 찍고 있었다.** 두 축이 무조건 빨간 상태에서는
> 「잡혔다」가 「이 변이가 나쁘다는 것을 알았다」를 뜻하지 않는다 — 그냥 언제나 빨간 것이다.

수리 후에는 대조군이 올바르게 **MISS**로 나오고, 열두 개가 **각각 서로 다른 축 하나**에
잡힌다. 판독은 HTML에서 한다 — 본 검사 블록과 같은 파서로.

```js
// ⚠️ 여기를 TSV 판독으로 되돌리지 마라. 12/12라는 숫자만 남고 채점은 사라진다.
const rowsOnM = parseHtmlTable(on.html);
```

**102/2 → 107/0.** 단언 3개 추가, 삭제·약화 0.

## 내 수치 둘이 틀렸다 — 그리고 그 정정도 하나는 틀렸다

`392fca4`와 보드 정정 `282c270`이 스스로 두 수를 바로잡았다.

| | 내가 말했던 수 | 커밋의 정정 | **이 항목 작성 시점 실측** |
|---|---|---|---|
| 게이트에 걸린 하네스 | 12 | 11 (16개 파일 − 부채 5, `seam_7b_oracle.py`는 파이썬이라 스캔 대상 아님) | **11** ✅ |
| `valid_die_frame_adoption` 실패 수 | 41 | **42** | **41** (2회 반복 동일) ❌ |

**정정이 틀렸다.** 이 항목 작성 시점에 HEAD에서 두 번 돌려 `228 단언 / 41 실패`가 나온다.
`392fca4` 이후 `map_editor.js`도 이 하네스도 바뀌지 않았으므로 그 커밋 시점에도 41이다.

**그 42가 어디서 나왔는지는 재현된다**(총괄 실측, 이 항목 접수 시):

```
$ node client2/tests/valid_die_frame_adoption_harness.mjs | grep -c "✗"
42
$ node ... | grep "✗" | grep -vc "baseline:"
41
```

하네스는 마지막에 **자기 점수를 스스로 찍는다** — `✗ baseline: 228 assertions, 41 failure(s)`.
그 요약줄도 `✗`로 시작하므로 마커를 세면 **실패 41 + 요약줄 1 = 42**가 된다.
즉 42는 오측이 아니라 **세는 대상을 잘못 고른 것**이고, 고쳐야 할 습관은
「마커를 세지 말고 하네스가 찍은 줄을 읽어라」다.

그리고 두 수를 헷갈리게 만든 원인은 남아 있다 — `check_harnesses.mjs`가 이 하네스에 붙인
`KNOWN_RED` 라벨 **「28 of 228」은 정적 문자열**이고, 살아 있는 점수가 아니다.

## 이 라운드가 남긴 사실 — 「적색이 됐다」로는 부족하다

이 주에 같은 문장이 세 번 나왔다.

- 겹쳐 깐 방어에서 `RuntimeError`는 **「어떤 그물인가 하나가 버텼다」**만 증명한다
  (`20260731_063735` 항목, 주입 1).
- 배관을 부르는 픽스처는 **배선이 끊긴 것을 볼 수 없다**(`5441706`).
- 무조건 적색인 축이 섞여 있으면 **변이 검출률은 아무것도 뜻하지 않는다**(이 커밋).

세 경우 모두 초록·빨강의 **색**은 맞았고, 색이 **무엇 때문에** 그렇게 됐는지가 틀렸다.

## 그때 남아 있던 것

- **이 커밋이 빌드 게이트를 다시 열었다.** 그래서 세 커밋 앞에서 보드에 써 둔 「빌드가
  막혀 있다」 블록이 그 순간 거짓이 됐고, `282c270`이 같은 분에 그것을 고쳤다 — 그 블록은
  컨텍스트 압축을 견디려고 존재하므로 **낡은 인수인계가 없는 것보다 나쁘다**는 이유였다.
- **하네스가 실행하는 리비전 대조는 여전히 두 소스를 슬라이스한다.** 고정 베이스라인
  `7524d00`(= `064550f^`)은 옛 심볼 철자를 영원히 갖고 있고, 그래서 `SYMBOLS`가 두 철자를
  모두 담는다.
- **부채 목록의 5개는 그대로 적색이다** — `effort_instrument`(셋업에서 예외) ·
  `reposition_regime_probe`(`ERR_INVALID_ARG_TYPE`) · `split_registry`(개명된 심볼을 슬라이스) ·
  `valid_die_authoring`(98 통과 / 1 실패) · `valid_die_frame_adoption`(228 단언 / 41 실패).
  이 커밋이 회복시킨 것은 0개다.
