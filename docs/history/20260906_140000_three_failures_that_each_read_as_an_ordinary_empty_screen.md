# 실패 «셋»이 각각 «평범한 빈 화면»으로 읽히고 있었다 — 그리고 하나는 «성공»이라고 말했다

> **커밋:** `d4df1c58` (14:00) · `98dfaf7b` (14:10) · `09b170ba` (14:18)
> | **일자:** 2026-09-06
> **레인:** 구현자(클라)
> **측정 상자:** 이 워크스테이션. **운영이 아니다.**

세 커밋이 «한 부류»다: **읽기가 실패했는데 화면이 「아무것도 없음」이나 「성공」이라 말한다.**

## ① Refresh 가 «아무것도 안 묻고» 새 시각을 찍고 성공이라 했다 (`d4df1c58`, F-9)

```
탭 정의    «일곱» (overview · tables · file · chain · autoupdate · enrichment · ontology)
fetchData  갈래 «다섯», 그리고 `else` 가 «없다»
낙하       `tables` 와 `ontology` 는 «아무것도 안 맞고» `markRefreshed()` 에 닿아
          «새 타임스탬프를 찍고» `allRead` 를 true 인 채로 돌려줬다
호출자     `if (!ok) return` 을 통과하고, `messages[currentTab]` 이 undefined 라
          기본 문구 「목록을 새로고침했습니다」가 «성공»으로 올라갔다
```
🔴 즉 «묻지 않은 것»이 「방금 확인함」으로 그려졌다.

## ② 깨진 원장 선언이 「선언된 소스 없음」으로 읽혔다 (`98dfaf7b`, F-10)

```
서버   `sources_view` 가 읽기 실패를 «잡아서» 페이로드에 «담아» HTTP 200 으로 답한다
      (`error = "ClassName: msg"`). 깨진 파일이면 `document = {}` 이라
      `sources` 는 `{}`, `declared` 는 `[]`
클라   `refreshLedgerSources` 는 «HTTP 상태»로만 갈랐다 — 404, `!res.ok`, 그다음 전부 `res.json()`
      => 200 이 그 오류를 «평범한 본문»으로 통과시켰다
패널   `payload.ingestion` 만 읽으므로 «평범한 빈 화면»을 그렸다
```
🔵 **세 층이 각자 옳게 굴었는데 합치면 거짓이 된다** — 서버는 «담았고», 클라는 «상태만» 봤고,
패널은 «자기 칸만» 봤다.

## ③ 감사 라우트 실패가 「기록된 DB 이력 없음」으로 읽혔다 (`09b170ba`, F-11)

```
🔵 그 줄의 «증상»이 낡았다는 것을 먼저 말하고 갔다 —
   줄은 `/audit_logs/recent` 가 500 이라 적었는데, 오늘 «클라의 바로 그 질의»
   (`?limit_groups=100`)로는 200 에 그룹 100 이 온다.
   첫 프로브는 «다른 파라미터»였고, 결론을 내기 «전»에 클라의 것으로 다시 쟀다
🔴 그런데 줄이 «지목한 결함»은 지금 코드에 있다 — 오늘 라우트가 실패하느냐가 아니라
   «언젠가 실패하느냐»에만 달렸기 때문이다:
   `timeline.js:21` 에 `res.ok` 검사가 «없었고», 아래의 catch 가 그것을 «대신 못 한다» —
   500 은 «유효한 JSON 본문»(`{detail: …}`)으로 답하므로 catch 가 «안 걸린다»
```
🔵 **「증상이 낡았다」와 「결함이 없다」를 가른 것**이 이 항목의 값이다.

## 확인한 것 (diff 실측)
```
d4df1c58  admin.js +22   98dfaf7b  admin.js +13   09b170ba  timeline.js +9
셋 다 `client2/dist/assets/admin-*.js` 가 «같이» 바뀌었다 — 소스만 고치면 사용자에겐 없는 것이다
```
📎 큐 등급 1. 🔵 부류의 이름은 「없어서 0」과 「고장나서 0」이 «같은 픽셀»이라는 것이다.
