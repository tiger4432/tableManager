# 벽이 아니었던 벽 «둘», 그리고 «물어볼 수 없었던» 분모

> **커밋:** `6cfd921a`~`d9e9d704` (09-13 03:10~04:31)
> **일자:** 2026-09-13 (새벽, 셋째 묶음)
> **레인:** 구현자(서버) · 클라
> **측정 상자:** 이 워크스테이션. **운영이 아니다.**

## 무엇이 일어났나

이 묶음의 문장은 **「‘없다’가 두 번 다 ‘아무도 안 써 봤다’였다」**이다 — 보드 로더(C-93)와
`state.js` 벽(C-91 ⓐ). 둘 다 새로 «지을» 물건이 아니었고, 빠진 것은 «기제»가 아니라 «조합»이었다.
서버 쪽 둘은 반대 방향의 같은 병이다 — **자리가 있는데 «아무도 안 지나서» 틀린 채로 조용했고**
(S-205), **수가 나오는데 «모집단이 틀려서» 그럴듯했다**(S-147-b).
착지 커밋 단위로 적는다.

📎 S-150 `3953f695`(03:08)는 지시 목록에 있었으나 **앞 항목**
(`20260913_031000_false_had_to_be_earned_and_a_seed_set_stopped_being_a_list.md` §④)에
이미 적혀 있다 — 실측해서 중복임을 확인했고, 여기서 다시 적지 않는다.

## ① 초안 수명주기가 «문서 하나»로 되접혔다 (S-205 `6cfd921a` · S-208 `df108854`, 판정 325·340)

```
6cfd921a 03:10  체인 초안 어댑터 은퇴 — `ChainRuleDocument` · `ChainRuleIndex` · `CHAIN_EDITABLE_FILE`
df108854 03:29  나머지가 되접힘 — `LedgerDocument` · `DraftContext` · `document=` 인자
남은 것          `OntologyDraftStore` :365 «하나»
```
🔴 **그 자리는 «제품 호출자를 한 번도 못 가진 채» 은퇴했다.** 판정 325 가 체인 규칙의 문을
«체인 탭의 raw 라우트»로 두면서, 어댑터는 자기 시험 밖 호출자가 «0» 인 채로 서 있었다.
🔴 **그리고 아무도 안 보는 곳에서 «틀려» 있었다** — `config_root` 가 온톨로지 루트를 돌려주는데
`chain_rules.json` 은 그 «부모»에 산다. 그 자리로 발효했으면 **어떤 로더도 안 읽는 파일**에
썼을 것이다. 그 메서드를 부른 시험은 «하나도 없었고», 그래서 **쓰인 날부터 틀렸는데 아무 말도
안 했다**. 📌 그것이 「소비자 없는 자리」가 치르게 하는 값이다.
🔵 **시험이 «같은 커밋»에 갔다** — 남겨 두면 유일한 호출자가 서 있어 «수가 살아 보인다»
(상설 「테스트는 자기가 재던 코드와 같은 커밋에서 죽는다」).
🔵 산문 «셋»이 그 클래스를 이름으로 들고 있었고 「은퇴했다」로 고쳐졌다 — `_ledger_predicate_dry_run`
이 함수가 죽고도 몇 주 동안 주석에 «살아 있던» 그 자리이고, 구현자가 그 문장 하나에서
거짓 결론을 다시 유도할 뻔했다고 커밋에 적었다.

### ⚠️ 그리고 제가 이 층을 «좋게» 적어 뒀던 것을 정정했다 (`5a280c26`)
제 D-14 의 `SYSTEM_FLOWS.md` §④-bis 제목이 **「수명주기는 하나, 문서는 «둘»」**이었다.
그때 **제품 문서는 원장 «하나»였고 둘째는 «예정»**이었다 — 즉 제가
**«여럿이 될 거라는 베팅»을 「이미 여럿」으로 옮겨 적었다**. 상설
「무한 케이스인가, 여럿이 될 거라는 베팅인가」가 겨누는 바로 그것이고,
**층은 «둘일 때» 짓지 «둘이 될 것 같을 때» 짓지 않는다.**
🔵 본문은 «그때의 설계»로 남기고 머리에 정정을 얹었다 — 판정 303 에서 서 있는 절반
(「합칠 사람이 없다 — 수명주기는 하나」)은 오늘도 참이고, 접힌 것은 «문서 축»뿐이다.

## ② «물어볼 수 없었던» 대조는 분모에서 나간다 (S-147-b `21440c63`, 판정 340·342)

```
종전   분모가 «타입» -> `quantity` 에 «닿기만» 한 대조가 «한 번도 재지 않은» 항목의 분모에 들었다
       => 0/2 가 «진짜 차이»처럼 보였다
오늘   가르는 것은 「그 대조에게 이 질문이 «판정 가능»했나」 = S-149 가 이미 노드에 접어 둔 verdict
       `unknown` 이 분모를 떠난다 -> 0/0, 그리고 **0/0 은 차이가 아니다**
```
🔴 **읽지, 다시 «도출»하지 않는다.** 판정 340 이 `parents` 를 출처로 이름 댔고 342 가 이 모양을
택했다 — **한 사실을 두 방식으로 세면 그 둘이 «갈릴 자리»가 생긴다**(상설 ④ 「같은 기능에 두 경로」).

### 재는 동안 구현이 «두 번» 바뀌었다 — 그것이 이 항목의 값이다
```
① 후보는 술어 «집합»으로 이어진다     사례는 `observed` 로 결함에 닿고 대조는 «다른 것»으로 닿는다
                                   처음 본 하나를 쓰면 «순회 순서»가 분모를 고른다
② 그 집합은 `parents` 에서 못 나온다   씨앗당 «경로 하나»만 들고 있어, 기록된 술어는 그 씨앗의 걷기에서
                                   «먼저 도착한» 것이다 — `observed` 는 «아예 없었다»
                                   🔴 그리고 그 불완전은 «안 보인다» — 조회가 타입 질문으로 흘러가고
                                      수는 여전히 그럴듯하다. 엣지가 완전히 나르므로 «엣지»가 출처다
```
📌 부류: **「모집단이 틀리면 수가 거짓말한다」** — 그리고 이 파일의 «자기 주석»이 그 결함을 적고 있었다.
🔵 **접는 규칙은 «만들지 않았다»** — 확인자를 선언 안 한 술어는 타입 분모로 그대로 폴백하고,
확인 가능한 술어가 «둘»인 후보도 폴백한다(한 후보에 대한 «다른 두 질문»이라, 여기서 고르면
이 함수가 «답의 저자»가 된다). 접기는 소유자의 것이다(판정 332).
⚠️ **제 픽스처가 먼저 틀렸고 게이트가 그것을 말했다** — 대조에게 결함으로 가는 경로를 안 줘서
두 분모가 «다 0» 이었고, 시험이 «두 0을 비교»하고 있었다. 부류: 「범례가 단언을 공허하게 만든다」.
📐 변이 실측: 분모에 `unknown` 을 남기면 빨강 2 · `parents` 를 읽으면 빨강 2 ·
미선언 술어에서 항목 가지를 타면 빨강 2.

## ③ 클라 넷 — 셋은 「좌석이 선언한다」, 하나는 「벽이 벽이 아니었다」

### ⓐ 접는 자리가 서버로 갔고, 다리가 «예고한 날»에 걷혔다 (C-90 ② `deebe3da`, 판정 331·335·341)
🔴 **종전 코드는 «잘린 걷기를 세기를 거부»했다.** 그래서 증상이 「치우친 답」이 아니라
**«영원히 빈 차트»**였다 — 운영 규모에서 예산이 먼저 자르는데, 자른 순간 그림이 사라진다.
부류: **「실패하는 것이 무해한 것은 아니다」**. 이제 `cut: true` 가 «수 옆»에 선다.
🔵 **`AGGREGATE` 와 `aggregationIsNumericOnly` 가 은퇴했고 계약의 «클라 절반»도 같이 갔다** —
계약은 «갈릴 수 있는 두 쪽»이 있어야 계약이고, 이제 한 쪽이다. C-90 ①(`705a884a`)이 그 표를
**«딱 한 라운드»** export 한 것은 둘이 «먼저 일치함»을 증명하기 위해서였다(벡터 13, 동일).
⏳ **제가 D-19 에 그 export 를 「다리다 — C-90 ② 에서 은퇴 예정」으로 적었고, «하루 만에» 그대로
걷혔다.** 다리를 놓을 때 «언제 걷을지»를 같이 적으면 영구 건물이 되지 않는다.
🔵 그리고 «거절의 자리»가 옮겨 갔다 — 패널의 문장 셋(「건너뜀 N」·「전부 건너뛰었습니다」·
「안 실었습니다」)은 클라가 «이제 만들 수 없는 구별»을 짓고 있던 것이다. 서버가 못 받는 이름을
«이름 대어» 거절하고(`measure_needs_numbers` · `unknown_measure` · `ambiguous_value_name`),
화면은 그 이름을 그리며 «자기 문장을 쓰지 않는다».
⚠️ `skipped` 는 `null` 이지 0 이 아니다 — 0 은 「누가 셌다」고 말한다.
⚠️ **그리고 지시서가 두 번 틀렸고 «실측»이 고쳤다** — 「잘림 좌석 다섯이 MAP 의 `.length` 를
읽으니 그 절반은 영원히 거짓」은 거짓이었다(`subgraphModel` 이 `truncationAxes`, `api.js`:538 에서
이미 «배열»로 만든다). 총괄이 판정 341 에서 그것을 «자기 전제»로 정정했다.

### ⓑ 좌석이 술어와 타입을 «선언»한다 (C-92 `17819603`, 판정 343)
```
맵 좌석 셋   `cells: {type:'die'}` · `marks: {scanned:'inspected', found:'observed'}`
             -> `mapModel(answer, grid, axis, plan)` :1115
웨이퍼 사실   `type:'wafer'` · `kinds:{type:'defect_kind', key:'defect_kind'}`
             -> `waferFactsFromWalk(answer, kind, plan)` :1289
배선         «값 넘기기만» — 좌석이 `plan` 을 옵션으로 내리고 부품이 옵션에서 읽는다(조립식 그대로)
```
🔴 **아무것도 선언하지 않은 좌석은 «셀을 안 그린다»** — 오늘의 이름들이 «숨은 기본값»이 아니다.
🔴 **다섯째 낱말은 막혔고, 벽에 «이름을 붙였다»** — `compositionFromWalk` 의 `keys.mat_id` 두 줄은
선언(`entities[].keys`)이 답해야 하고 그 독자는 `walk/derive.js` 에 사는데, `rnd_board/api.js` 와
`main.js` 가 하니스에서 `data:` URL 로 실려서 **상대 지정자를 못 푼다**. 실측: import 하나가
보드 하니스 «여덟»을 「아무것도 안 잽니다」로 만들었다. 좌석이 키 이름을 «다시 적는» 쪽은
**거절했다** — 그건 선언의 «둘째 저자»이고, 이 라운드가 없애려던 결함 자신이다.

### ⓒ 그 벽은 «더 작고 더 나빴다» — 문이 하나가 됐다 (C-93 `d30e0a1d`, 판정 345)
```
이미 있던 것   `tests/lib/board_modules.mjs` 가 «바깥 지정자» 재작성을 들고 있었다
              (그 파일 «자기 주석»이 이유를 적는다 — 손으로 적은 목록이 하니스를 «두 번» 넘어뜨렸다: C-70 · C-77)
없던 것       `api.js` 의 줄이 그 자리를 «안 지났고», 사설 로더 «여섯»이 같은 세 줄을 각자 들고
              «같은 누락»을 들고 있었다
그래서        `api.js` 가 «첫 바깥 import 를 얻는 날» 보드 하니스 «여덟»이 조용히 아무것도 안 재게 됐다
```
🔵 **문 하나**: 모든 모듈 URL 이 `moduleUrl(src)` :24 를 지나고 `outward()` :39 가 «그 안»에 산다 —
호출 자리가 «잊을 수 없다». 🔴 **그리고 gone-anchor 던지기가 «공용 로더 안»으로 들어갔다** —
사설 로더 전부가 그걸 들고 있었고 공용 로더는 «안» 들고 있었다. 없이 접었으면
**「변이가 아무것도 안 했다」가 「잡혔다」로 바뀌었을 것**이다.
📌 부류: **상설 구성 기준 ④** 「같은 기능에 두 경로가 있어서도 안 됨」의 하니스 판 — 한 자리를
고쳤는데 여섯이 «갈렸고», 갈린 것은 «제품 파일이 모양을 바꾸는 날»까지 안 보인다.
🔵 그리고 다섯째 낱말이 나갔다 — `compositionFromWalk` 이 «선언의 정체성 키»(`declaredKeys`, `tableColumns`
와 «같은 독자»)로 부품 id 를 짓는다. 못 읽으면 노드 자신의 id 로 떨어지지 «이름을 지어내지» 않는다.

### ⓓ `state.js` 벽도 «안 써 본 조합»이었다 (C-91 ⓐ `d9e9d704`, 판정 346)
C-88 이 `state.js` 를 「사본이 «자기 객체»를 들어서 다른 모듈이 안 읽는다」로 적었고, 그래서
대조군 둘이 **여섯 중 «다섯»**만 덮었다. 스파이크로 «돌려» 보니 `probe.mjs` 의 `spec.stubs` 가
그것을 **이미** 할 수 있었다 — 사본을 먼저 싣고 그 `state` 를 나머지 주체에게 넘기면 치환이 «닿는다»
(명시적 export 가 stub 의 `export *` 를 이긴다).
```
stub 목록   «주체의 import 줄»을 파싱해서 얻는다 — 이름이 하나 늘어도 대조군에서 «조용히 빠질 수» 없다
stage      러너가 «자기가 채점하는 번들»의 state 를 앉힌다 — 진짜 싱글턴을 앉히면 변이가
           «아무도 안 연 페이지»에 서 있게 된다
거절       프로브가 「주체가 import 안 한 이름의 stub」을 «이름 대어» 거절한다(`probe.mjs` :220~)
           실측으로 받아 봤다: `grid.js` 는 `isVirtualColumn` 을 import 하지 않는다
```
🔴 **그리고 치환이 «닿았는지»는 대조군으로 못 잰다** — 달아난 대조군은 「배선됐어도」 「조용히
건너뛰었어도」 «같은 값»을 낸다. 그래서 `state.js` 에 «잡힐 변이»를 하나 놓고, 그것을 잡는 자리를
«다른 파일»의 쓰기 깔때기로 뒀다 — **29/29 CAUGHT**(종전 28). 부류: 「초록 대리지표는 초록 주장이
아니다」의 «대조군 판».
📌 **한 밤에 «두 번»** — C-93 의 로더와 이 벽, 둘 다 「기제가 없다」가 아니라 「아무도 그 조합을
안 써 봤다」였다. ⏳ 그리고 `data:` 다리 «자체»는 그대로 선다 — 판정 346 이 보드 아홉의 probe 전환을
**C-94** 로 떼어 냈다(관통 전환을 «나눠» 착지시키지 않기 위해). 제 실측:
`git grep -l "data:text/javascript" -- client2 contracts` 가 «여덟 파일»을 낸다.

## ④ 전 스위트 한 바퀴 — 빨강 «0» (`40ab1ca6`)

```
6,067 passed · 156 skipped · 4 xfailed · FAILED «0» · ERROR «0» · 656.59s
수집 6,227 = 6,067 + 156 + 4      <- 산술 대조: 남거나 빠진 시험 «없음»
env python 절대경로 · 파일로 받음(`| tail` 없음) · `-rf`
```
🔵 **xfail 넷을 «이름 대어» 적었고, 그 자리가 낱말 세기의 함정이다** — `@pytest.mark.xfail(strict=True)`
문법 grep 은 «셋»을 내는데 실제는 «넷»이다. 넷째(`test_trace_fixture`)는 픽스처가 사유를 «붙여»
파라미터로 만든다. 📌 부류: **「같은 낱말, 두 모집단」**.
🔴 **그리고 구현자가 「기존 둘이 어느 둘이었는지 못 가린다」를 «추측으로 안 메웠다»** — 모른다고
적고 넷의 이름을 전부 냈다. 상설 「모르는 것은 반드시 적는다」 그대로.

---
📎 착지 커밋 단위(D-18 판정 02:14 · D-19·D-20 지시). 문장마다 해시를 달았고, 「운영」이라 적은 줄은 «없다».
📎 이 묶음의 문서 착지: `5a280c26`(CODE_MAP · SYSTEM_FLOWS 자기 정정) · `3a6afe88`(CODE_MAP §7) ·
`2195e100`(하니스 가이드 §5-quinquies) · 그리고 이 항목과 함께 가는 §5-sexies.
