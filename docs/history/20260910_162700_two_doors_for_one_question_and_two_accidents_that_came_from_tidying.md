# 한 물음에 문이 둘이었고, 정리하다 사고가 둘 났다 — 둘 다 «운영 화면»에서 났다

> **커밋:** `14cb6818`~`9354f97c` (06:41~16:27)
> | **일자:** 2026-09-10
> **레인:** 구현자(서버) · 클라
> **측정 상자:** 이 워크스테이션. **운영이 아니다.** 다만 아래 사고 둘은 «운영에서 보였다».

## 무엇이 일어났나

「같은 기능인데 두 경로가 있어서도 안 됨」(상설)의 실물이 하루에 셋 나왔고, 그것을 지우다
«정리 자체»가 낸 사고가 둘 났다. 두 사고 다 **게이트는 초록이었고 운영자가 먼저 봤다.**

## ① 한 물음에 문이 둘 — 갈라지면 «조용히» 다른 답이 된다

```
무엇이   같은 파일이 «어느 문으로 들어왔나»에 따라 다른 컬럼 집합으로 적재됐다
왜      파일 워처는 `display_columns` 로 거르고, `crud` 는 `column_types` 로 걸렀다.
        한쪽에만 선언된 컬럼은 API 로는 «착지»하고 워처로는 «조용히 버려졌다»
        판정(09-10 12:20): 적재가 묻는 것은 「이것이 존재하나」 = `column_types`.
        컬럼은 화면에 없어도 «존재하고 쓰일 수» 있지만, 화면은 없는 컬럼을 못 그린다
어디    6170db70 12:34  `display_columns` 는 «보여 주는 축»만 남고 「무엇을 쓸 수 있나」를 안 정한다.
                        발견 경로: 저장소 자기 행 생성기가 API 로 쓰는 `dt_job` 을 워처가 «떨어뜨렸다»
```

## ② 은퇴 — 소비자가 «0» 인 문을 닫는다

```
무엇이   커서를 올리는 문과 `/refusals` 라우트가 은퇴하고, 그 자리의 «진짜 기록자»가 남는다
왜      S-76 이래 라이브 경로는 `advance_cursor=False` 로 흐른다. 그래서 커서 행을 만드는
        유일한 문장이 «제품 호출자 0» 이 됐고, 그 뒤로 선언된 소스는 행이 «영영 안 생겼다»
어디    14cb6818 06:41  센서스 틱이 소스의 레지스트리 행을 만든다(S-113, 판정 221)
        9d10218b 07:10  소스 패널이 「아직 «기록자»가 있는 것」을 읽고, 거절 라우트는 은퇴한다
        240576c9 06:57  번역하는 «그 프로세스»가 거절 사유를 쓴다 — 운영에서 그 수가 0 이 되지 않게
```

## ③ 정리가 낸 사고 «둘» — 구간으로 지웠고, 데코레이터 사이에 끼워 넣었다

```
무엇이   ㉠ 운영이 온톨로지 탐색기를 열자 `loadCensus is not defined`
        ㉡ 운영 배포의 «모든» 표에서 그리드가 비었다(422)
왜      ㉠ «이름»이 아니라 «구간»으로 지웠다 — 주석부터 함수 끝까지 잘라내니 그 안에 살던
           남의 이름이 같이 갔고, :739 의 호출자는 그대로 남았다. 파일 안에서는 아무도 항의하지 않는다
        ㉡ 데코레이터는 «다음 def»에 붙는다. 라우트와 함수 사이에 헬퍼를 끼우니 라우트가 «헬퍼»에
           등록됐고, 그 헬퍼의 기본값 없는 인자가 «필수 쿼리»가 되어 평범한 요청이 전부 422 였다
어디    024b36bd 09:31  센서스만 «이름으로» 복원. 그리고 게이트가 왜 초록이었는지가 같은 커밋에 —
                        `ontology_explorer.js` 가 CSS 를 import 해 node 가 «그 파일을 못 열었다»
        e3744b47 15:08  헬퍼를 데코레이터 «위»로. 두 줄. 게이트는 인접이 아니라 «바인딩»에 건다
                        (`app.routes` 가 그 경로를 «그 함수»로 매핑하는지)
```
🔴 **둘 다 「하니스가 약하다」가 아니라 「하니스가 «못 보는 파일»이었다」다.** 그리고 둘 다
운영이 먼저 봤다 — 어드민 화면 변경은 «열어» 보고 병합한다는 규칙이 그날 상설로 올라갔다.

## ④ 그 밖에 이날 «하나로» 모인 것

```
0ecbf7c2 14:31  날것 카탈로그를 «이름 대어» 거절한다 — 제품 어댑터를 안 거친 모양이 조용히 통과하고
                있었고, 그것이 같은 날 응용의 계측기 오류(S-85 부류)를 «지어낸» 자리다
9354f97c 16:27  정렬이 «색인이 답할 수 있는 순서»를 요청한다. 그리고 그 옆의
                「PostgreSQL 은 이렇다」는 주석이 «틀렸다»고 같은 커밋에 적혔다
1d006fbe 13:25  색인 빌더가 «낱말»로 답한다 — 참/거짓 한 비트가 서로 다른 거짓 두 줄을 내보내고 있었다
```
