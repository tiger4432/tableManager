# 오염된 커넥션 «하나»가 결함 넷과 하루치 가드를 설명했다

> **커밋:** `d68fbb4d`~`fa46e798` (11:28~13:46)
> | **일자:** 2026-09-11 (오후)
> **레인:** 구현자(서버) · 클라 · 응용 · 총괄
> **측정 상자:** 이 워크스테이션 — «운영»이라고 표시한 줄만 소유자가 준 운영 수다.

## 무엇이 일어났나

오전이 「자를 대니 조용한 손실이 보였다」였다면, 오후는 **「그 손실과 그날의 400 둘이 «같은 원인»이었다」**이다.
그리고 그 원인은 아무도 처음에 의심하지 않은 자리에 있었다 — **풀에 돌려준 커넥션 하나**.

## ⓐ S-162 — 쓰기 파이프라인 계약, 그리고 «측정으로» 되돌림

```
무엇이   한 그룹이 대상 표 수만큼 `apply_batch_updates` 를 «따로» 지난다(선인출·ORM·flush·부수 표 ×N).
        그것을 「한 범위 · 한 커밋」으로 묶는 계약(판정 270·272)
왜      S-151 이 남긴 0.66 s 가 그 ×N 에 있었다. 설계 실측은 표 둘에서 0.37 s
어디    8ab1cbb9 12:12  한 그룹이 쓰는 모든 대상에 «한 범위, 한 커밋»
        0004b80f 12:20  «생존 시험» — 한 표의 충돌이 이미 쓴 표들을 데려가면 안 된다
        1b0942b4 13:04  SAVEPOINT 는 «앉을 트랜잭션»이 있어야 한다   <- 여기서 사고가 시작됐다
⚰️ 되돌림  판정 277: **측정해 보니 이득이 «없었고» 「한 커밋」도 아니었다** → (a) 되돌림
```
🔵 **이 되돌림이 이 항목에서 제일 값진 줄이다.** 설계가 옳아 보였고 실측이 반대였으며,
«숫자가 이겼다». 되돌린 뒤에도 생존 시험은 남는다 — 계약이 다시 올 때 그 시험이 먼저 서 있다.

## ⓑ 🔴 결함 «넷»이 한 병이었다 — 0행 SUCCESS → 거절 → 미룸 → 오염 커넥션 → 풀 밖

```
S-158  접힌 사건이 자기 행을 «하나도» 못 읽으면 빈 페이로드로 돌고 SUCCESS   -> 거절로 (판정 265)
S-160  그 창(<100 ms)이 «왜» 열리나 — 전제(사건이 먼저 커밋)는 판정 268 이 반증(같은 xmin)
S-166  참조뷰 0 이 400 `25P01 NoActiveSqlTransaction: SAVEPOINT` — 트랜잭션 밖의 SAVEPOINT
S-167  🔴 **뿌리**: `engine.raw_connection()` 은 «풀» 커넥션을 준다. LISTEN 은 autocommit 이 필요해
       `set_isolation_level(0)` 로 바꾸고, 프록시를 닫으면 «autocommit 인 채로» 풀에 돌아간다.
       실측: 바로 다음 대여가 «같은 커넥션 · autocommit=True»
```
```
그 커넥션을 쥔 세션은 «트랜잭션을 시작하지 않는다»
  => ① 그 위의 모든 `begin_nested()` 가 25P01 (= S-166 의 400)
     ② 행과 사건이 «서로 다른 커밋»에 쓰인다 (= S-160 의 창, = S-158 의 0행)
어디   fa46e798 13:16  LISTEN 이 «풀을 떠난다» — `psycopg2.connect` 로 풀이 본 적 없는 커넥션을 쓰고,
                      `_reset_connection` 의 close 는 «진짜 닫기»이지 반납이 아니다
확인   S-167 뒤 1,000 행 × 6 연속 — LOST 0 · rows_not_visible 0 · 계기 0 (S-160 «측정으로 닫음»)
```
🔴 **그리고 가드 둘은 그 뿌리를 «못 닫는다»** — autocommit 커넥션에서는 `session.begin()` 이
BEGIN 을 «안 보내서» SAVEPOINT 가 여전히 앉을 데가 없다. 그 가드를 이미 단 빌드에서 «112 회» 났다.
```
1b0942b4 13:04 · 2bd34d90 13:10  두 가드는 «한 층 위의 옳은 방어»로 남는다 — 뿌리가 아닐 뿐이다
```
📌 **부류: 증상 넷에 원인 하나.** 넷을 각각 고치는 라운드였다면 넷 다 고쳐지고 «다섯째»가 왔을 것이다.
그리고 격리로 흩어졌던 행 8,000 이 이 줄기에서 복구됐다.

## ⓒ 참조뷰가 «아무 컬럼»이나 바인딩한다 — 반쪽이 «두 번»

```
무엇이   파생 행의 어느 컬럼이든 참조뷰가 바인딩할 수 있다. 판단 키 하나가 아니라
왜      선언이 넓어졌는데 «값»이 안 따라오면 운영자는 적을 수 있는 것을 «못 적는다»
어디    e441de84 12:17  좌석 «전부»에서 — 한 좌석만 되는 것은 좌석마다 답이 다른 것이다
        d5fab9fc 12:30  파생 표가 «선언한 모든 컬럼»을 바인딩(판단 키만이 아니라)  [S-163 서버]
        (클라 C-73 이 같은 시간에 «행»을 보내게 — 서버만 넓히면 클라가 여전히 키만 보낸다)
```
⚠️ **반쪽이 두 번이었다**: S-136 → S-163(서버) 에서 한 번, S-163 → C-73(클라) 에서 또 한 번.
한쪽만 넓히면 «아무 오류 없이» 옛 동작이 남는다.

## ⓓ 운영 인제션 1분/1,000행 — 계기를 «운영으로» 보냈다

```
🔵 운영(소유자)   파일 인제션 1,000 행에 «1분» = 60 ms/행. 이 박스 같은 함수 0.6 ms/행 (100배)
🔵 운영(소유자)   청크 줄: apply 19 s/1k = 선인출 «10 s» · 행 만들기 1 · 부수 표 «7 s»
이 박스          같은 셋이 0.03 · 0.37 · 0.42
=> 선인출이 «300배». 운영에서 인덱스를 못 타거나(문장 모양·부분 인덱스 술어) 인덱스 집합이 다르다
```
```
어디    ceaaf450        워처 청크가 «어디에 썼나»를 말한다 — 체인의 «그 계수기»를 그대로 쓴다
                        `[Ingest] <표> chunk N: R row(s) in T s · STAGES · <스테이지…> · unnamed U s
                         · INSIDE THE WRITE · <스텝…>`  (`parsers/directory_watcher.py`)
        S-165           `cell_sources` 업서트가 «안 바뀐 셀»도 다시 쓴다 → 그 쓰기가 배큠을 먹인다
                        (재전달된 «같은 값»이 새 진술인가 = 소유자 몫, 판정 대기)
        S-169 · S-170   🔴 방향: **제품이 자기 대기와 인덱스를 «스스로» 말한다**(276).
                        소유자가 SQL 을 못 내므로, 운영자 스크립트와 제품 보고가 답이어야 한다
⚠️ 정정  「배큠이 계속 뜬다」의 전제 — 운영 dead 튜플은 «거의 0» 이다. 그러면 그 배큠은
        청소가 아니라 analyze/insert 계기이고, 어느 쪽인지는 «계기가» 말한다
```
🔵 **[13:4x 운영] 풀 수정 뒤 운영이 «빨라졌다»** — 오염된 커넥션의 «문장마다 커밋»이
부수 표 시간의 출렁임을 설명한다. 즉 ⓑ 의 뿌리가 ⓓ 의 일부도 설명했다.

## ⓔ 판정과 그날의 규율

```
258~260  체인 목표 5 s/1k → «배관»의 것(매퍼 로직은 규격 밖) → IO 는 1 s/1k
270~272  남은 0.66 s 는 «쓰기 파이프라인 계약» · 공유 누적 · 그룹당 한 트랜잭션 · 호출자 무변
275      «예산 시험은 천장이다» — 오늘 수에 못 박으면 빠른 기계에서 빨개지고 아무 말도 안 한다
276      제품이 «자기» 대기·인덱스를 말한다. 소유자에게 SQL 을 더 청하지 않는다
277      S-162 (a) 되돌림(이득 없음) · S-160·S-166·S-167 «라이브 수»로 닫음
🔵 IO 목표까지 남은 거리 = 1.3 (배관 2.52 → 1.66 → 그다음은 계약이 아니라 측정이 정한다)
⚠️ 오늘 pull 알림 «셋» — 둘째는 «철회»됐다. 라이브 게이트보다 «먼저» 나갔기 때문이고,
   그 순서를 총괄이 스스로 정정했다. 「준비됐다」는 증거 «뒤»에 오는 말이다
```

---
📎 「운영」이라 표시한 줄만 소유자가 준 운영 수이고, 나머지는 전부 이 박스다.
📎 응용의 보고는 `task/ontology_application_report.md`(09-11), 판정은 큐·보드.
