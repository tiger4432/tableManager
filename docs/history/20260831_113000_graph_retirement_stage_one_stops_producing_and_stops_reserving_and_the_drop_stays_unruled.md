# 그래프 은퇴 1단 — «만들기»와 «자리 잡기»를 멈추고, 44개 표의 컬럼 삭제는 판정 전으로 남긴다

> **커밋:** `53b89277` (09:39) · `523968c3` (09:42) · `9eb30691` (11:30) · `42efb58b` (11:45) ·
> `9078bb4e` (11:55) · `21e9df3d` (12:01)
> | **일자:** 2026-08-31 오전~정오
> **레인:** 클라 레인 → 서버 레인
> **측정 상자:** 이 워크스테이션. **운영이 아니다.**

## 은퇴한 동기화의 «잔해»가 44개 표마다 세 자리를 차지하고 있었다

그래프 동기화는 서버에서 은퇴했다 — `/graph/mapping-summary` 는 **410 Gone** 으로 답한다.
그런데 스키마 라우트가 여전히 `is_graph_synced` · `needs_graph_rollback` · `graph_synced_at`
를 보내서, **모든 표가 세 컬럼을 그렸고 그중 둘에는 이모지가 붙어 있었다.**

## ① 화면에서 먼저 걷어냈다 — 그리고 «멈춰야 할 자리»에서 멈췄다

`53b89277`. `buildColumnDefs` 가 **defs 를 만들기 «전»에** 거른다 — 그렇게 하면
**컬럼 토글 목록에서도 자동으로 빠진다**(그 목록이 같은 집합이기 때문이다).

이름은 클라의 시스템 컬럼 목록들에서도 나왔다. **그 목록들은 그리드에 «있는» id 만 보므로**
(클립보드 깔때기는 `getColumns()` 에서 id 를 가져온다 — 그 파일의 주석이 그렇게 적고 있다),
컬럼이 사라진 뒤 그 항목들은 **도달 불가능**이었다.
🔴 **그중 두 자리는 지시서의 목록에 «없었다»** — 주장은 「클라의 시스템 컬럼 목록들」이었고,
그것은 **한 번의 검색으로 쓴 열거보다 «많은» 자리다.**

🔴 **그리고 `push_columns.js` 에서 «멈췄다» — 지시서를 따르지 않고.**

```
PUSH_SYSTEM_COLUMNS 는 «표시 목록»이 아니다
   서버 자신의 분류를 «거울»처럼 비춘다 (main.py 의 system_cols · crud.py 의 쓰기 스킵 목록,
   그 시점에 «둘 다» 셋을 이름 대고 있었다)
   그리고 «맵 푸시 게이트»가 그것을 읽어 replay_map 푸시가 «파괴할» 컬럼을 정한다
=> 거기서 셋을 지우면, 아직 그 컬럼을 가진 표들에서 그것들이 «평범한 데이터 컬럼»으로
   재분류되고 «게이트의 답이 바뀐다». 하니스가 그 정확한 집합을 못 박고 있다
```
`523968c3` 가 총괄 쪽 기록이다 — **「system columns」라고 라벨된 grep 히트 하나를 근거로**
지우라고 지시했고, **레인이 멈추고 세 자리를 인용했다.** 이름이 맞는다고 같은 목록인 것이 아니다.

거르는 자리를 고른 이유도 기록됐다: 처음에는 `state.currentColumns = data.columns || []`
**한 자리**에 넣었는데, **그 줄이 한 하니스의 변이 대상이고 그 하니스의 기준선 단언 여덟이
그 목록을 읽는다.** 그래서 지시가 지정한 자리로 옮겼다.

읽기 전용 오리진으로 라이브 스키마에 대고 확인: **서버는 여전히 셋을 다 보내고**(컬럼 13),
그리드는 하나도 안 그리며, 이모지 셀이 남지 않고, 컬럼 토글은 11 항목에 그래프 관련이 없다.
붙여넣기 정렬은 **라이브 표에 붙여넣지 않고**(그건 쓰기다) 하니스에 맡겼다 — 66/0, 변이 28.

## ② 소유자 판정이 «되돌릴 수 있는 선»에서 청소를 갈랐다

`9eb30691`. 화면에서 감추는 것은 끝났지만 **서버는 여전히 컬럼을 유지**하고 있었고,
진짜로 없애는 것은 **서버 변경 + 44개 표 마이그레이션**이다.

```
1단 (되돌릴 수 있다)   서버가 값을 «안 만들고», 새 표가 «안 받는다»
2단 (되돌릴 수 «없다»)  기존 44개 표의 컬럼 «삭제» -> 별도 판정
```

## 🔴 ③ 지우기 «전»에 «누가 읽나»를 셌다 — 그것이 이 라운드가 켠 게이트다

`42efb58b`.

```
셋 중 «어느 값에도» 갈래를 트는 «운영 코드가 없다»
graph_sync_worker.py     «존재하지 않는다»
/graph/mapping-summary   «이미 410»
남아 있는 유일한 «대입»   아웃박스 스킵 케이스를 만드는 «시험» 하나
그 밖의 모든 등장         «제외 목록 안의 이름»
```

그래서 서버가 **없는 동기화기를 위해 모든 표의 모든 행에 「미동기화」를 찍는 일**을 멈췄고,
**모든 표의 컬럼에 세 자리를 잡아 두는 일**도 멈췄다. **쓰기 경로 스킵 목록**에서도 빠졌다 —
그 목록은 **쓰기가 «건드리지 않을 것»**인데, 아무도 안 쓰는 이름이 거기 있는 것은
**아무것도 보호하지 않는 규칙**이다.

**클라 계약이 «같은 커밋»에 움직인다** — 그것이 방금 바뀐 서버 목록 둘을 거울처럼 비추기
때문이다. 🔴 **그리고 그것이 무엇을 바꾸는지 쟀다**: `column_types` 에 셋 중 하나라도 선언한
표가 **0** 이라서, 그것들은 **푸시 게이트의 입력에 애초에 도달하지 않고 게이트의 답은 오늘
동일하다.** 하니스 둘을 같이 고쳤다 — 명부 단언은 **개수가 아니라 «멤버 이름»**을 대고,
픽스처의 `SYS_TAIL` 은 그대로 두면 **더 이상 존재하지 않는 서버**를 서술하게 된다.
`push_gate` **34 passed** · `virtual_column_render` **66 passed**, 결함 **28/28** 여전히 잡힘.

## ④ 주석이 «방금 자기를 거짓으로 만든 변경»에 반대하고 있었다

`9078bb4e`. `grid.js` 는 `push_columns.js` 가 세 이름을 **지켜야 한다**고 적고 있었다 —
지우면 푸시 게이트가 그것들을 «푸시가 파괴할 데이터»로 세게 되니까.
**서버가 모든 표에 그것들을 덧붙이던 동안엔 참이었다.** 앞 커밋에서 멈췄으므로
그 문장은 **끝난 세계를 서술**하고, **다음에 읽는 사람은 이름을 도로 넣었을 것이다.**

거르개 자체는 **남는다.** 배포 중에 **새 클라가 옛 서버를 만날 수 있고**, 그때는 컬럼이
여전히 도착한다 — **그 한 순간이 이것을 죽은 코드가 아니라 «마지막 선»으로 만든다.**

번들은 **같은 커밋에** 재빌드됐다 — 빌드 안 된 클라 변경은 쓰는 사람에게는 변경이 아니다.

## ⑤ 공장이 여전히 «죽은 컬럼 셋을 달고» 표를 낳고 있었다

`21e9df3d`. 앞 커밋은 서버가 **채우는 것**을 멈췄지만 **만드는 것**은 그대로였다 —
**새 표마다 죽은 컬럼 셋, 그중 «둘은 인덱스».** 즉 존재하지 않는 동기화기를 위해
**모든 insert 가 인덱스 둘을 유지**하고 있었다.

```python
# server/database/models.py — init_dynamic_models()
- Column("is_graph_synced", Boolean, default=False, nullable=True, index=True),
- Column("needs_graph_rollback", Boolean, default=False, nullable=True, index=True),
- Column("graph_synced_at", DateTime(timezone=True), nullable=True),
+ # RETIRED 2026-08-31: ...
+ # ⚠️ EXISTING TABLES KEEP THEIR COLUMNS. This changes what is CREATED, which is
+ # reversible; dropping 44 tables' columns is not, and is a separate ruling.
+ # Nothing here re-creates an existing table, so no table changes shape today.
```

**S1 을 확인했고 발화하지 않았다** — 기존 표를 다시 만드는 것이 아무것도 없어서
**오늘 모양이 바뀌는 표가 없다.** G1 을 양방향으로: 갓 만든 표에는 셋이 없고
(`row_id` · `business_key_val` · `created_at` · `updated_at` 과 자기 컬럼), 기존 표는 여전히
바인딩되고 읽힌다.

🔴 **스키마 드리프트 시험 넷이 빨개졌는데 죽은 것은 «주제»가 아니라 «픽스처»였다.**
그 스위트는 드리프트 «검출기»를 재는데, 거짓말을 심을 **프레임워크 DateTime 컬럼**으로
`graph_synced_at` 을 «빌려 쓰고» 있었을 뿐이다. 이제 `updated_at` 을 빌린다 —
**모든 표가 «항상» 가질 컬럼은 밑에서 은퇴당할 수 없으므로 더 나은 대출**이다. **44 passed.**

## 아키텍처 영향

- 서버가 세 컬럼에 **값을 만들지 않는다.** 스키마 라우트가 반환하는 컬럼에도 없고,
  쓰기 경로 스킵 목록에도 없다.
- **새 표는 그 컬럼을 받지 않는다.** 인덱스 둘이 신규 표에서 사라졌다.
- **기존 44개 표의 컬럼은 그대로다.** 삭제는 되돌릴 수 없어 별도 판정으로 남았다.
- 클라는 세 이름을 그리지 않고, 시스템 컬럼 목록들에서도 뺐다. **`push_columns.js` 의 명부만
  서버 분류의 거울로 남아 «같은 커밋»에서 함께 움직였다.**
- 그리드의 거르개는 **배포 중 옛 서버를 만나는 순간**을 위해 남는다.

## 그때 남아 있던 것

- **DB 컬럼은 44개 표에 그대로 있다.** 이 시점에 그 삭제는 판정 전이다.
- `column_types` 에 셋 중 하나라도 선언한 표는 **0** 이었다. 그것이 푸시 게이트의 답이
  안 바뀐다는 근거다.
- 셋에 남아 있던 유일한 대입은 **시험 하나**(아웃박스 스킵 케이스 구성)였다.
- 이 항목 작성 시 재측정: `push_gate_harness` **ASSERTIONS 34 0** ·
  `virtual_column_render_harness` **66 0** (exit 0) ·
  `server/tests/test_schema_drift_startup.py` 를 포함한 네 파일 **141 passed**.
