# 「같은 결함 두 번」은 갈래에 대한 주장이고, 본문 없는 커밋엔 보존할 왜가 없다는 교훈이 기억 파일에 실렸다

**날짜:** 2026-08-11 07:34 · **커밋:** `a242c27` · **레인:** 문서(doc-historian, 자기 교훈)
**측정 상자:** 이 워크스테이션. **운영이 아니다.**

---

## 배경

직전 커밋 `beffd89`가 만든 두 관측을 doc-historian 자신의 교훈 파일
(`agent_workspace/memory/doc-historian.md`)에 반영하는 것이 이 커밋의 전부다. 이 파일은
운영 규칙상 에이전트가 **제안만** 하고 총괄 검수 후 반영되는 자리이며, 이 커밋은 그 반영이
실제로 이뤄졌다는 기록이다.

## 무엇이 추가됐나

두 항목, 4줄.

**함정 1** — 「증상이 같다」에서 「같은 결함을 두 번 고쳤다」를 추론한다. 커밋 제목 둘이 같은
증상을 말하면 중복 수리로 읽히기 쉽고, 그 판단이 지시서를 통해 사실처럼 다음 레인에
넘어가기도 한다. 올바른 방법: 적기 전에 **두 hunk 범위를 실제로 대조**한다.
`014b5d3`/`3dc79e6`은 겹치는 줄이 0이었다 — `if anchor_dxy is not None:`과 `else:`라는
서로 배타적인 두 갈래를 각각 고쳤다.

**함정 2** — 커버리지를 「이 해시가 어딘가에 언급됐나」로만 판정한다. 본문이 제목 한 줄뿐인
커밋에는 보존할 **왜**가 커밋 안에 아예 없다. 올바른 방법: **왜가 실제로 착지한 자리를
찾아 해시에 묶는 것**이 그때의 일이다. `a501d6d`·`2ec8e24`·`7097a67`이 사례다 — 근거는
그 커밋들이 스스로 추가한 `docs/history/` 파일에만 있었는데, 그 파일들은 해시를 달고
있지 않았다.

## 검증

`agent_workspace/memory/doc-historian.md` 1개 파일, +4줄. 코드 변경 없음, 이 커밋 대상
스위트 없음.

## 그때 남아 있던 것

- 이 교훈은 **이 세션이 착수 전 필독으로 읽은 바로 그 파일**에 있다 — 이 항목을 쓰는
  세션 자신이 그 교훈을 적용하는 첫 사례다.
- 교훈 파일은 이 시점 기준 doc-historian 전용 섹션에 7개 항목(공통 섹션 별도)을 갖고
  있으며, 이 커밋이 그중 마지막 두 개(같은 결함 두 번 · 커버리지 판정)를 더했다.
