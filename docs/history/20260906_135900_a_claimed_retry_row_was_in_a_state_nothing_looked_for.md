# 선점된 재시도 행이 «아무도 안 찾는 상태»에 있었고, 죽지 «않아도» 그렇게 됐다

> **커밋:** `fe3e9261` (13:59) · `cb76478b` (14:29)
> | **일자:** 2026-09-06
> **레인:** 구현자(서버)
> **측정 상자:** 이 워크스테이션. **운영이 아니다.**

## 무엇이 일어났나

`poll_pending_retries` 는 일을 «시작하기 전»에 선점을 커밋한다(`status = "PENDING"`).
그런데 폴러 «자신의» 질의는 `status == "PENDING_RETRY"` 를 고른다.

```
=> 그 커밋부터 종단 상태에 닿기까지, 그 행은 «아무것도 다시 안 찾는» 상태에 있다
```

## 🔴 그리고 「워처가 «죽으면»」이 아니었다 — 그게 이 라운드가 뒤집은 문장이다

이 줄을 연 측정은 「그 창에서 워처가 «죽으면»」이라고 적었다. 2026-09-06 실측:
**죽을 필요가 없다.** 준비 단계의 «평범한 예외» 하나면 충분했고, 준비 단계는 `try` «밖»에
있었다 — 워크스페이스 해석 · import 둘 · `os.listdir` · 핸들러 생성.

```
그 예외는 루프의 «바깥» 핸들러로 새어 나가 "Error in retry poller loop" 를 찍고
`log.status` 를 «안 건드린다» -> 행은 영원히 PENDING, 그리고 그 로그 줄은 «행 이름도 안 댄다»
🔴 별칭 워크스페이스 조회나 config 디렉터리 권한이 실패하는 자리가 바로 거기다
   -> «정상 운영»에서 난다. 사고일 때만이 아니다
```
`fe3e9261` 이 준비 단계를 `try` «안»으로 들여 그 절반을 닫았다 — 이미 있던 `except` 가
FAILED 로 표시하고 트레이스백을 남기고 통지한다.

## 그 프로세스 «안»에서 못 닫는 절반 — 밖에서 돌려준다

프로세스가 «사라진» 경우는 그 프로세스 안의 어떤 코드도 못 닫는다. `cb76478b` 가 밖에서 닫는다.

```
⚠️ 술어가 «두 사실»이고 둘째가 이유다
   「선점됐고 오래됨」만으로 회수하면 «건강하게 오래 도는» 인제션을 뺏는다
   -> 산 작업을 회수하는 것은 «손실»을 «중복»으로 바꾼다. 그게 더 나쁘다:
      손실은 데이터가 «안 오는» 것이고 중복은 «틀린 데이터가 조용히 오는» 것이다
   => 둘째 사실은 «체크포인트»다. 청크가 계속 앉고 있으면 그 작업은 «살아 있다» — 얼마나 오래 돌았든
🔴 비교는 «청크 진행»에 대고 한다. «총 실행 시간»에 대고 «절대» 하지 않는다
```

## ⛔ 체크포인트가 «없는» 행은 회수하지 않는다 — 건너뛰고, **그 건너뜀을 «센다»**

`_plan_checkpoint` 의 세 갈래가 체크포인트를 안 낸다(서명 없음 · 계획이 던짐 · 이어받기 꺼짐).
셋 다 「파일이 멈췄다」는 뜻이 «아니다». 그래서 「죽었다」로 추측하지 않고 **건너뛴 수를 센다** —
「판정할 수 없는 것」이 「없는 것」으로 안 읽히게.

## 확인한 것 (diff 실측)
```
fe3e9261  준비 단계가 `try:` 안으로 들어감 (+44줄)
cb76478b  `server/ingestion_checkpoint.py` 신설 — 주석이 「«청크 진행»에 대고 재고,
          «총 실행 시간»에 대고 «절대» 안 잰다」를 자기 문장으로 적는다
          시험 `test_a_stranded_claim_is_given_back.py` (159줄)
```
📎 큐 등급 0 «L-1». 이 줄은 「보여 주는 것으로 안 고쳐지는」 손실 칸이었다.
