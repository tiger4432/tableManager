# 죽은 화면 둘, 레거시 라우트 여섯, 그리고 그 뒤에 있던 고아 무리 — 27,000줄이 나갔다

> **커밋:** `57d25d17` (23:45, 08-24) · `f9a8a73c` (00:16) · `67cc2e8a` (00:31)
> · `9cdf224c` (22:50) · `5e3f263f` (22:58)
> | **일자:** 2026-08-24 심야 ~ 2026-08-25 밤
> **레인:** 클라(화면 은퇴) + 서버(라우트 절제)
> **측정 상자:** 이 워크스테이션. **운영이 아니다.**

## 배경 — 살아 있는 입구 일곱에서 «닿지 않는» 화면들

```
57d25d17   삭제 파일 11   삭제 6,562줄   (총 17파일 · +47 / −6,728)
f9a8a73c   삭제 파일 10   삭제 6,762줄   (총 14파일 · +17 / −6,768)
67cc2e8a   삭제 파일  1   1,554줄        server/ledger_journey.py
5e3f263f   삭제 파일 23   삭제 13,742줄  (총 25파일 · +23 / −13,755)
```

클라에서 삭제된 파일은 세 chore 커밋 합쳐 **44**(src 모듈 27 · 하니스 6).

서버 쪽 라우트는 diff 로 정확히 **여섯**이다:

```diff
-@router.get("/trace")
-@router.get("/explore")
-@router.get("/entities")
-@router.get("/journey")
-@router.get("/lots")
-@router.get("/coverage")
```

## 삭제는 «순서»가 있었다 — 소비자를 먼저 0으로 만든다

`57d25d17`이 자기 본문에 적었다: 모듈 넷이 아직 `./surprise_core.js`를 임포트해서
`npm run build`가 막힌다. **16분 뒤 `f9a8a73c`가 그것을 풀었다** — 「나머지 고아 무리, 그리고
일부러 한 층 낮춘 것」.

`9cdf224c`가 원장 탭을 어드민 메뉴에서 빼고 「맵 정렬기」를 그리드 메뉴에 넣자
`src/ledger_map_panel.js`와 모듈 열이 **소비자 0**이 됐고, **8분 뒤 `5e3f263f`가 지웠다.**
`src/ledger_console.css`는 남았다 — `tokens.css:21`이 임포트하고 있어서.

## 🔴 그리고 커밋 하나가 자기 줄 수를 틀렸다

`67cc2e8a` 본문: 「786줄에서 575줄로」. 실측: 파일은 **785 → 571**이고 `--numstat`은
**0 삽입 / 214 삭제**다. 786 − 575 = 211 ≠ 214.
**라우트 수와 삭제 파일 수는 맞고, 줄 수치는 틀리다.**

## 아키텍처 영향

- 원장 라우터에서 **레거시 라우트 여섯**이 나갔다. 남은 것은 walk 과 그 주변이다.
- 클라에 **원장 화면이 없다.** 원장으로 가는 입구는 그리드 메뉴의 「맵 정렬기」와 R&D 보드다.
- `server/ledger_journey.py` 1,554줄이 통째로 사라졌다.

## 그때 남아 있던 것

- `server/ledger_catalog.py`가 **일부러 고아로 남았다**(`67cc2e8a` 자기 기록).
- 빨간 preview translator-version 시험 하나는 **이 변경 이전부터 빨갛다.**
- `67cc2e8a`의 **줄 수치는 틀린 채로 커밋 본문에 남아 있다.**
