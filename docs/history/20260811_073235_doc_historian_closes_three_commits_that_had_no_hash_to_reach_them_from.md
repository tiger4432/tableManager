# 히스토리 기록자가 `34d2518` 이후 11개를 채우고, 본문 없는 커밋 셋의 연결을 만들었다

**날짜:** 2026-08-11 07:32 · **커밋:** `beffd89` · **레인:** 문서(doc-historian)
**측정 상자:** 이 워크스테이션. **운영이 아니다.**

---

## 배경

앞선 doc-historian 세션이 커버리지 경계를 점검했다. 가장 최신 히스토리 파일명만 보면
최신처럼 보였지만, 실제 경계는 `34d2518`까지였다 — `20260809`·`20260810` 디렉터리 안의
파일들은 **구현 레인이 스스로 남긴 노트**이고 커밋 해시를 달고 있지 않았다. 그래서
`a501d6d`·`2ec8e24`·`7097a67` 세 커밋은 해시로는 도달할 방법이 없는 채로 「커버된 것처럼」
보이고 있었다. 이 세 커밋은 모두 제목 한 줄뿐인 본문을 갖고 있어, **왜**가 해시가 도달할
수 있는 어디에도 없었다.

## 이 커밋이 한 일

`34d2518` 이후 11개 커밋에 대해 히스토리 항목 7개를 새로 썼다(파일명 접미사로 미루어
보건대 그중 일부는 여러 커밋을 한 항목에 묶었다). `20260810_075619_...refused_the_chain.md`
가 그 세 무해시 커밋에 대한 연결을 만드는 항목이다.

## 자기 브리핑 정정

같은 커밋이 앞선 doc-historian 자신의 판단 하나를 취소한다: `014b5d3`와 `3dc79e6`을
「같은 결함을 두 번 고쳤다」로 지시서에 적어 건넸는데, 실제로는 hunk가 **겹치지 않았다** —
앵커/탐색 분기의 **서로 배타적인 두 갈래**를 각각 고친 것이고, `014b5d3`은 신고자의 데이터가
들어가지 않은 그 갈래에서 **실제로 동작을 바꿨다.**

## 검증

이 커밋 자체는 문서 추가와 `docs/history/README.md` 갱신(+12행)뿐이다. 코드 변경은 없고
스위트 수치도 이 커밋 대상이 아니다.

## 그때 남아 있던 것

- 이 정정(`014b5d3`/`3dc79e6`이 겹치지 않는다는 사실)은 이 커밋 시점엔 아직 doc-historian의
  교훈 파일에 반영되지 않았다 — 그건 다음 커밋 `a242c27`이 한다.
- 커버리지 경계는 이 커밋으로 `34d2518` → (7개 항목이 다루는 범위)까지 밀렸을 뿐, 그 뒤에도
  같은 종류의 무해시 구현 노트가 또 쌓일 수 있다는 구조적 위험 자체는 이 커밋이 없애지
  않았다.
