# 첫 번째 회귀 — 내가 **지어낸 시나리오**를 수리하는 잔차를 출하했고, 근거표도 내가 심은 픽스처였다

> **커밋:** `4947a65`(2026-08-06 17:49 · 회귀를 낸 쪽) · `88ffc74`(19:00 · 되돌림) · `a28c021`(19:01 · 스펙 기록)
> **일자:** 2026-08-06 저녁 | **담당:** 총괄(오판) · 사용자(이분탐색으로 발견)
> **대상:** `server/map_alignment.py` · `server/tests/test_map_alignment.py` · `docs/spec/MAP_ALIGNMENT_SPEC.md`
> **스위트:** `4947a65` 618 passed / 3 skipped · `88ffc74` 208 passed / 3 skipped

## 1. `4947a65`가 **맞게** 진단한 것

조작자 신고: 「index로는 잘 되는데 **shift를 무조건 0,0으로** 계산함」.

후보 구성이 모든 셀을 `reference_top_left + L·(cell − anchor_cell)`로 놓는다. `i = i_min`에
대해 그 값은 **문자 그대로 `reference_top_left`**다. 그다음 `_anchor_shift`가
`reference_top_left − placed[i_min]`을 계산한다. **뺄 것이 남아 있지 않다.**

그 0은 데이터도 아니고 거절도 아니었다 — **앵커가 이미 해 놓은 배치 위의 산수**였다.
여덟 프레임 × 네 시나리오 × 두 개의 `reference_top_left`((17,1)·(40,16)) 전부에서
`placed[i_min] == reference_top_left`, `anchor_reason` None, `i_min` 0.

index 축이 그 모든 실행에서 266/266을 읽은 것도 같은 사실이다 — 세페인 워크는 **평행이동
불변**이다. **조작자가 본 「좋은 index 점수 옆의 0 시프트」는 애초에 서로 모순이 아니었다.**

## 2. 🔴 그리고 동어반복을 **제거하는 대신 그 위에 층을 얹었다**

`_residual_shift`를 추가해 「작업이 워크 중간에서 시작하는 경우」를 잡게 했다.

**그 경우는 내가 구성한 것이고, 실재를 한 번도 확인하지 않았다.**

근거로 커밋 본문에 실은 표 — 워크 다이 #43에서 시작하면 잔차 (13,2)에 126셀이 뒤집히고,
#101이면 (−9,5)에 117셀, #401이면 (−9,14)에 125셀 — **전부 내가 심은 픽스처에서 나온
수치다.** 운영 데이터에서 나온 수가 아니다.

## 3. 사용자가 이분탐색으로 잡았다

「깃 헤드를 `ec8c0e` 로 옮기니까 잘되는데」, 그리고 그 리비전에서 「**정확히 유효영역다이의
start x,y 만큼** 어긋남」.

운영 데이터에서는 **앵커의 전제가 성립했고 앵커의 자리가 맞았다.** 잔차가 그것을 **치웠다** —
앵커의 자리가 정답인 맵을 `(5,26)`으로 옮겼다. 그 커밋 이전에는 앵커 경로가
`anchor_dxy[frame]`을 그대로 냈고 그 값은 **동일하게 (0,0)**이었으므로, 그 값을 만들 수 있는
것은 그것 말고 없다.

`88ffc74`가 되돌렸다. **분석은 소스에 남기고 이동만 뺐다** — 그 케이스를 다시 벌어야 하는
라운드를 위해.

## 4. 같은 커밋에서 확정 원점 유도 둘이 실측에 의해 폐기됐다 (하나는 내 것)

| 유도 | 성적 |
|---|---|
| 바닥 원점 그대로 | 240셀 중 **240셀이 틀린 다이**, 균일 (4,3) |
| `floor_start + t` (당시 **출하 중**) | 32 프레임 조합 중 **0** |
| `floor_start − t` (제안) | 32 중 **4** — `L`이 퇴화하는 자리에서만 |
| `start_from_placement` (이 함수) | **32/32** |

`floor_start + t`는 **기준의 저장 공간에서 사는 평행이동**을 **기준의 원점**에 더하고 그 합을
**소스의 원점**이라 부른다 — 한 식에 공간이 셋이다.

🔴 **`− t`가 손검산을 통과한 이유는 그 예제가 `L`이 퇴화하는 4에 속했기 때문이다.**

## 5. 🔴 `box.minC`/`box.minR`은 상쇄되지 않는다 — 내가 그 상쇄를 유도했고 틀렸다

두 box.min을 빼는 유혹적인 유도가 있다. **그 둘은 같은 수가 아니다** — 하나는 소스 프레임의
상자, 하나는 기준 프레임의 상자고, **90/270 회전이 그것들이 앉은 축을 맞바꾼다.**

실측: 편집기의 원점 상자를 원(circle) 대신 마스크로 바꾸면 rot270에서 필요한 원점이
**(6,9) → (8,11)**로 두 축 다 움직이고, rot90에서는 **불변**이다.
**한 프레임에서 움직이고 다른 프레임에서 안 움직이는 항은 상쇄되지 않는다.**

`L`은 세 지점에서 `make_frame_transform`에서 **읽어 온다** — 손으로 쓰지 않는다.
`start_for_placement`와 `frame_linear_part`가 이미 지키던 규율이다. **이 파일은 좌표 대수를
전에 두 번 잘못 옮겼고**(QA O3/B1), 위의 폐기된 유도 둘이 **세 번째와 네 번째**다.

## 6. 회귀 불변식

답이 **framed meta 자신의 현재 원점에 의존하면 안 된다.** 그것을 `D`만큼 옮기면
`mft(anchor_src)`가 `−L·D`만큼 움직이고, `L`의 역이 그것을 `−D`로 되돌려 첫 항의 `+D`와
상쇄한다. 바닥 원점 (3,5)와 (0,0)에서 — `anchor_ref`가 (3,5)만큼 다르다 — 반환된 원점은
**두 번 다 (6,9)**였다. `L`의 역을 빼거나 변환기를 만든 것과 다른 meta의 grid를 먹이면
`test_the_written_start_is_where_the_editor_redraws_it`이 빨개진다.

## 7. 그때 남아 있던 것

- **동어반복은 이 시점에도 서 있었다.** `88ffc74`는 잔차를 되돌렸을 뿐 앵커가 배치에 구워지는
  구조는 그대로 두었다. 그것은 21분 뒤 `34b4a93`이 손댄다 — 그리고 그 커밋이 **두 번째 회귀**를
  낸다.
- `a28c021`은 코드 변경 0이고, 위 네 유도의 실측을 `MAP_ALIGNMENT_SPEC.md`에 남긴 것뿐이다.

## 8. 🔴 이 회귀가 왜 났나 — 남길 문장

**사용자는 운영 화면을 보고 말했고, 나는 내가 심은 픽스처를 보고 판정했다. 어긋났을 때 내
쪽을 믿었다.**

같은 날 같은 원인이 한 번 더 나온다(`34b4a93`).
