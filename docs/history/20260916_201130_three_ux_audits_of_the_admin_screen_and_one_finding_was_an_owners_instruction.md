# 어드민 화면 적대 UX 감사 «셋» — 그리고 가장 큰 발견 하나는 «소유자의 지시»였다

> **커밋:** `cee40f30`(19:39 · 되돌리기 공백) · `981cbf85`(19:46 · 상태 요약) ·
>   `6ccadf7a`(19:52 · C-116 런타임) — 전부 **보고서 전용, 소스 변경 «0»**
> **일자:** 2026-09-16 (밤)
> **레인:** 클라 · 디자인 · 응용 (셋이 «같은 화면»을 다른 각도로)
> **측정 상자:** 이 워크스테이션. **운영이 아니다.**
> ⚠️ **이 항목이 적는 것은 「감사가 «무엇을 쟀나»」다.** 감사의 판정을 내 판정으로 승격하지 않는다

## 세 보고서가 «다른 것»을 재고 있었다 — 그래서 셋이다

```
task/admin_ux_audit_20260916.md       708줄  디자인 시스템·CLAUDE.md UI 상설 대조 + 일반 UX
task/admin_ux_audit_gaps_20260916.md  364줄  UX_AUDIT_REFERENCE.md §C 의 «넷»에만 좁힘 —
                                             진행 표시 · 되돌리기 · 전문가 가속기 · 화면 내 도움말
task/admin_runtime_ux_audit.md        241줄  C-116. «브라우저로 열어» 잰 것 (+ 메인 그리드)
task/axis_and_material_report.md       66줄  그 런타임 감사가 같이 실은 것
```
🔵 셋이 «겹치지 않은» 이유가 설계다 — 첫째는 소스와 규칙을, 둘째는 «규칙이 침묵하는 네 축»을,
셋째는 «도는 화면»을 잰다. 소스만 읽어서는 토스트가 클릭을 먹는 것을 못 본다.

## 🔴 그런데 헤드라인 발견 하나는 «사고가 아니라 결정»이었다 — 내가 blob 에서 다시 쟀다

`981cbf85` 의 H-1: 「상태 요약 스트립이 화면에 없고, 그것을 채우는 코드가 죽었다」.
**모양은 정확하다.** 내가 확인한 것:

```
실측 (2026-09-16, 981cbf85 의 blob)
   client2/src/admin.js:577   healthStripEl.style.display = 'none';      <- 무조건, if/else «밖»
   client2/src/admin.js:4842  async function refreshHealthStrip() {      <- 호출자 «0»
   git grep -n refreshHealthStrip 981cbf85 -- client2/  ->  히트 «둘»: :576(주석) · :4842(정의)
```
그런데 **바로 위 세 줄이 «왜»를 들고 있었다:**
```
:574  // 🔴 소유자: 「띄 다 빼」 (2026-09-05). 카드 넷이 하던 일 둘 중
:575  //    «이동»은 탭 바가 이미 하고, «수»는 각 탭의 절이 다시 말합니다(보고 참조).
:576  //    마크업과 `refreshHealthStrip` 은 남깁니다 — 되돌리는 것이 한 줄이어야 하기 때문입니다.
```
🔴 **즉 스트립이 꺼진 것은 소유자 지시(2026-09-05)이고, 마크업이 남은 것은 «되돌림을 한 줄로
   두려는 의도»다.** 감사는 :576 을 「그 식별자의 유일한 다른 히트」로 «세었지만» 그 내용을
   보고서에 옮기지 않았다.

⚠️ 그렇다고 감사가 «틀린» 것은 아니다 — 보고서의 제안 P-2 가 세 갈래를 열어 두었고
(① 지운다 ② 되살린다 ③ 그대로 둔다) ③ 이 정확히 이 결정이다. **틀린 것은 없고, 빠진 것이 있다.**

📌 **부류: 「주석은 «의도»의 증거다」 — 그리고 의도가 «소유자의 것»일 때는 그 증거가 발견의
   «등급»을 바꾼다.** 「잔해」와 「되돌릴 수 있게 둔 것」은 같은 코드 모양이고, 가르는 것은 주석뿐이다.

🔵 그리고 감사가 «덧붙인» 것은 새롭다: 번들러가 그 경로를 도달 불가로 판단해
`체인 파이프라인 정상`·`실패 0건` 문자열을 dist 에서 «떨궜는데», `health-strip` 과 네 클릭 핸들러는
«실린다». 그리고 프로세스 생사를 그리는 유일한 기관(`RuntimePanel` 의 「살았나」 열)은
**Overview 탭에만 마운트된다** — 체인이 안 돈다고 Chain 탭을 여는 운영자는 그 답이 있는
유일한 자리를 «떠난다».

## 🔴 되돌리기는 «응답 본문에 있었고 아무 화면도 안 읽었다» (`cee40f30`)

```
실측 (cee40f30 의 blob)
   server/main.py:6858-6860
      # The copy that can undo this save. A value, so a screen can offer the
      # restore instead of the operator having to know a path on the server.
      "backup": backup_path,
   client2/src/admin.js:4655  async function saveScriptCode(path, code) {   <- 응답 «본문»을 안 읽는다
   설정 패널 둘은 saved.enabled «만» 읽는다
   백업 디렉터리 위의 HTTP 라우트 «없음»
```
🔴 **서버가 「화면이 되돌리기를 제공하라고 이 값을 준다」고 «주석에 적어 두고», 아무도 안 읽었다.**
설정 파일의 경우 유일한 복원 수단이 **이 운영자가 닿을 수 없는 호스트의 CLI** 다.

📌 **부류: 「착지는 배선이 아니다」의 «응답 칸» 판.** 값은 «넷 중 넷»의 쓰기 라우트가 전부 싣는다.
   실린 것과 읽히는 것 사이의 간격이 오류를 안 낸다.

🔵 이 보고서가 «의심 셋을 반증»하고 그것을 발견으로 부풀리지 않은 것을 기록해 둔다 —
   Running 블록의 진행 표시는 «정직하고 신중한 작업»이고, 소급 블록은 «자기 자리의 도움말»을
   이미 들고 있고, 딥링크와 그리드→어드민 스코프 인계는 «진짜 가속기»다.
   남은 것은 「그 패턴들이 «확장되지 않은 곳»」이다.

## 🔴 소유자의 토스트는 실재하고 «잴 수 있었다» — 그런데 더 큰 것은 토스트가 «필요 없었다» (`6ccadf7a`)

```
토스트 (어드민 overview, 900px 뷰포트)
   쌓인 토스트 넷 = 640 x 278 px = 뷰포트의 «31%»
   elementFromPoint(「탭 열기 →」의 중앙)  ->  토스트를 돌려준다
      = 가려진 것이 아니라 클릭이 «먹힌다»
   컨테이너와 토스트 «둘 다» pointer-events: auto  ->  토스트 «사이의 틈»도 클릭을 삼킨다
   실측 확인 (내가 다시 잼)  client2/src/utils.js:51  const TOAST_MAX_VISIBLE = 4;
                            client2/src/admin.js:208 · :3516 · :3531  ttl: 12000   <- 12초
   🔴 CSS 에는 그 상한이 «안 보인다». JS 에 산다

메인 그리드 상단 바 (1440x900)  — 토스트가 «하나도 없어도» 클릭 못 하는 컨트롤 «넷»
   「Loaded 1000 rows in …」 알약 (.log-indicator-area, static, z-index «없음», left 815 w 243)
      -> `Replay chain`(924..993) · `Desktop` · 업로드 버튼을 «완전히» 덮는다
      -> 표 선택의 래퍼가 넷째를 가져간다
   1280 에서는 «아홉 중 여섯» (Refresh 포함)
   1024 에서는 API/WS 상태 배지와 테마 토글이 «오른쪽 밖»(scrollWidth 1413), 바에 가로 스크롤 «없음»
      -> 「아직 연결돼 있나」를 말하는 유일한 자리가 «제일 먼저» 사라진다
   🔴 기제는 z-index 가 «아니라» 폭이다 — 바에 넘침 전략이 없어 static 형제들이 서로 위에 그린다
```
🔴 **못 닿는 그 버튼 중 하나가 이 레인이 C-109..C-114 에서 «지은» 재생 배너의 입구다.**
자기가 지은 기능으로 들어가는 문이 자기 화면에서 클릭이 안 된다 — 그리고 «오류가 안 난다».

📌 **부류: 「기능이 없나, «못 부르나»」.** 여기서는 «있고, 배선됐고, 도달 불가»다. 그리고 이 셋을
   가르는 유일한 방법이 **화면을 «여는 것»**이었다 — 소스 감사 둘이 이것을 못 봤다.

## 🔵 감사가 «자기가 틀린 넷»을 같이 적었다 — 그것이 이 보고서의 값이다

```
① 첫 대비(contrast) 패스가 «알파를 무시»해 비율 1.0 짜리 «가짜 실패 열» 을 보고했다
   제대로 합성하니 다크 테마에서 AA 미만은 «정확히 하나» (범례 점)
② 그리드 뷰포트 아래 행들은 AG-Grid «가상화»이지 결함이 아니다
③ `toastFadeIn 0.3s` 가 첫 두 스크린샷이 «비어 보인» 이유였다
```
🔴 감사가 스스로 적은 이유가 정확하다 — **「거짓말하는 계측기는 틀린 답이 아니라 «문제 없음»을
   준다」.** ① 을 안 잡았으면 열 개의 가짜 실패가 진짜 하나를 «묻었을» 것이다.

⚠️ 그리고 스크린샷을 브라우저 창에서 «파일로 못 썼다» — 그래서 모든 발견이 좌표와 콘솔 한 줄짜리
재현법을 «대신» 싣는다. 증거의 모양이 도구의 한계로 정해진 자리다.

## 디자인 시스템 실측 (`981cbf85`, 세 파일 전수)

```
font-size 선언 116   그중 토큰화 «12» · 서로 다른 리터럴 «25»
여백 수 230          그중 3·4 격자를 벗어난 것 «183» · --space-5/--space-6 «한 번도 안 씀»
border-radius 33     그중 0 이 아닌 것 «23»  («radius 0, everywhere» 에 대해)
인라인 style 속성 156  -> 토큰을 바꿔도 «닿지 못하는» 자리
정의 없이 참조되는 토큰  --text-primary · --text-secondary · --bg
                     -> 흐린 톤이 no-op 이고 chain-graph 노드가 «검게» 찬다
접근성               role= 0 · tabindex 0 · aria-expanded 0 · aria-sort 0 · aria-live 0
```
🔴 **`--text-primary` 류가 「참조되는데 정의가 없다」는 것이 ③ 부류(아무도 안 타는 갈래)가 아니라
   ①(이 줄이 참인가) 부류다** — CSS 는 조용히 상속으로 떨어지고, 결과가 «검은 노드»다.

## 그때 남아 있던 것

```
소스 변경        «0». 셋 다 보고서다. 어떤 발견도 수리되지 않았다
판정            열려 있다. `0181af60`(C-117) · `001ab6aa`(C-118) 가 같은 밤 지시로 올라갔다
제안            981cbf85 에 「지시받은 것 이상」 11건 (짓지 «않았다», 소유자 검토 대기)
모르는 것        981cbf85 에 8건 — «대부분 도는 화면»이 있어야 답해진다.
                cee40f30 의 하중은 「서버가 «단계(phase)»를 내놓기는 하나」 — 그 답이
                「단계 표시 없음」이 «클라 라운드인지»를 정한다
안 한 것         어드민 토큰을 안 쳤고 쓰기 컨트롤을 «안 눌렀다» (6ccadf7a). 그래서 쓰기 경로의
                진행·실패 표면은 «관측 안 됨»이지 「없음」이 아니다
```
📎 「UI 라운드는 지시 이상의 제안 표를 항상 같이 낸다」(소유자 상설 2026-09-13)를 셋 다 지켰다.
