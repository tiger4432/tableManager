# 서열만 적고 동점을 안 적은 문장, 그리고 그 문장의 주인 없는 사본 둘

**날짜:** 2026-08-11 16:24~16:39 · **커밋:** `851d2a6` `32da009` `09b1511` `7034b26`
· **레인:** 문서(SSOT·스펙·코드맵·보드)
**측정 상자:** 대조 기준을 **워킹 트리가 아니라** `git show c4a3159:<path>`로 잡았다.
서버·클라 레인 여럿이 같은 파일을 편집 중이었기 때문이다.

---

## 배경

`347de78`의 결함(동점이 삽입 순서로 갈려 기존 값이 200/200 이겼다)은 코드에서 시작하지
않았다. **문서에서 시작했다.** `SYSTEM_OVERVIEW`의 「우선순위 결정」이 두 층만 적고
멈춰 있었다 — 수동 핀, 그다음 `SOURCE_PRIORITY`. 파일명 유래 소스는 전부 미등재라 99로
동점인데, **서열을 이름 대고 동점 규칙을 이름 대지 않은 문장은 다음 구현자가 규칙을
지어내게 만든다.** `sorted()`의 안정성이 지어냈다.

## 무엇을 했나

### `32da009` — SSOT가 세 번째 층을 적는다

```
수동 핀 > SOURCE_PRIORITY > (같은 우선순위 안에서) ingested_at 내림차순 > source_name 오름차순
```

**2·3층은 계층을 넘지 못한다** — 동점만 가르고 낮은 서열을 승격시키지 않는다. 이 제약이
문장에 같이 들어간 것이 핵심이다. SSOT라 사용자가 문구를 승인한 뒤에야 반영됐고,
같은 날 리빙 문서 라운드가 이 공백을 찾고도 **손대기를 올바르게 거절**했다
(`2fb1c44`의 본문이 그렇게 적고 있다).

코드 쪽 실제 모양은 문장보다 한 칸 더 세밀했다 — 이 커밋 시점의 `_rank`는 **네 성분**을
돌려주고, 2층이 「날짜 있는 층이 없는 층을 이긴다」와 「최신 우선」으로 갈려 있다.

```python
    def _rank(name):
        ts = resolution_ingested_at(sources[name], name, ingested_at_by_source)
        return (
            priority_map.get(name, 99),        # 1. declared ranking
            0 if ts is not None else 1,        # 2a. a dated layer beats an undated one
            -ts if ts is not None else 0.0,    # 2b. newest first
            name,                              # 3. total order, always decides
        )
```

### 총괄의 룰링 하나가 **내린 것보다 넓게** 쓰여 있었다

2026-08-05의 「목록의 `state`·`reason_code`는 안 바뀐다」는 실제로는 **`state`에 대한
룰링**이었다 — 닫힌 3값 집합에 네 번째가 붙지 않는다는 뜻. `reason_code`를 같은 절에
써 넣은 것이 **룰링한 적 없는 어휘를 얼렸고**, `c4a3159`가 이미 다루고 있던 원인에
이름을 붙이기 전에 멈춰 물어야 했다.

**멈춘 것은 옳았고 문장이 틀렸다.** 원래 귀속을 남긴 채 좁혔고, 좁힌 날짜를 적었다.
같은 계급(내린 것보다 넓게 적힌 절대문)을 파일 전체에서 훑었고 두 번째 사례는 없었다.

### 어휘가 문서에 사본 0개였다

새 §9.11이 워크리스트가 실제로 말할 수 있는 것을 열거한다 — 고유 `reason_code` 다섯 +
상세 뷰의 제외 어휘에서 **이름을 바꾸지 않고 빌려 온** 둘. 쓰기 전에 실측했다:
`docs/`에서 `unscorable_reasons`·`map_keys_unavailable`·`reference_absent` 히트 **0**.
독자가 `map_alignment.py`를 읽는 것 말고는 닫힌 집합을 알 방법이 없었다.

감사가 낸 hazard 넷은 **FINDINGS로, 명시적으로 수정이 아니라** 기록됐고 **넷 다
미측정**이다.

### `7034b26` — 「우선순위 맵만 본다」의 마지막 주인 없는 사본

`data_model.md`의 사본은 `347de78`이, 스펙의 사본은 `2fb1c44`가 고쳤다. `CODE_MAP.md`의
사본에는 주인이 없었다. 그리고 **보내진 건 하나였는데 실제로는 둘**이었다 — §2 심볼 행과
no-op-write 각주, 그리고 **각주 쪽은 그 거짓 문장을 자기 정당화로 인용**하고 있었다.

이 커밋이 남긴 판단 두 가지가 기록할 값이 있다.

- **대조 기준을 워킹 트리로 잡지 않았다.** `git show c4a3159:<path>`를 썼고, 그 선택이
  비용 0임을 확인했다 — `git diff c4a3159..HEAD -- server/ client2/`가 비었다.
- `chain_bindings.py:76`이 `ORIGIN_INHERITED`를 선언하고 **한 번도 반환하지 않는다.**
  레인보다 넓게 확인해 작동하는 상수는 `map_overlay.py:1669`(소비자 셋)이고 이것이 소비자
  0인 두 번째 사본임을 알았다. **지우지 않고 기록만 했다 — 심볼 삭제는 변경이고 이
  커밋은 지도다.**

그리고 도구에 대한 사실 하나: `client2/src/map2/main.js`에 **NUL 바이트**가 있어 plain
`grep`이 이 파일을 바이너리로 보고 매치 0을 보고한다. 그렇게 검색한 앞선 패스는
**「매치 없음」을 얻은 게 아니라 질문을 하지 않은 것**이다.

### `851d2a6` / `09b1511` — 로스터, 그리고 그 열이 하중을 받는 이유

여덟 레인이 자기 커밋 없이 동시에 돌고 있었다. 보드가 레인마다 **무엇을 만들고 · 보고서가
어디에 떨어지고 · 어떤 경로를 건드릴 수 있는지**를 싣기 시작했다. 마지막 열이 하중을
받는다 — **오늘 두 번, 경로로 고른 커밋이 다른 레인이 쓰고 있던 파일을 쓸어 담았다.**
`git status`는 무엇이 바뀌었는지 말하고 **누가 바꾸고 있는지는 절대 말하지 않는다.**

`09b1511`은 첫 레인의 착지를 기록하면서 **건드리지 않은 파일**을 같이 적었다
(`PROJECT_STATUS.md`도 `CODE_MAP.md`도 아님) — 로스터 열의 용도가 그것이다.

## 아키텍처 영향

- 표시값 해결 순서(`compute_priority_value`)가 `DOC_OWNERSHIP`에서 **자기 줄**을 얻었다.
  데이터 모델 행 안에 묻혀 있었는데, 이 라운드가 그 함수를 데이터 결함의 발원지로
  만들었다.
- 「한 항목의 결함이 목록 전체를 죽이지 않는다」가 `PRIMITIVES` §7에 재사용 형태로
  올라갔다 — **가드가 둘이라는 부분까지** 포함해서.
- 코드맵이 `chain_bindings.py`를 자기 소절로 얻었다(244줄 모듈인데 이 파일이 한 번도
  나열한 적이 없었다). 두 가지는 **추측하는 대신 싣기를 거절**했다 —
  `dt_map_mapper.py.sample`의 나머지 구조, 그리고 **커밋 메시지에서 가져온 하네스 바닥
  수치**(재채점은 코드맵의 일이 아니다).

## 검증

네 커밋 모두 **코드 변경 0줄**이다 — 스위트 게이트가 없다. 이 라운드의 게이트는 다른
것이었다.

- 대조 기준을 **워킹 트리가 아닌 커밋 트리**로 잡고, 그 선택의 비용이 0임을 `git diff`로
  확인.
- 새 어휘를 쓰기 **전에** `docs/` 전수 grep으로 사본 0을 실측.
- 「같은 계급이 또 있는가」를 파일 전체에서 훑고 **없다**를 명시적으로 기록.

## 그때 남아 있던 것

- `CODE_MAP.md`는 이 라운드에서 **다른 레인이 워킹 트리에서 잡고 있었고**, `32da009`는
  일부러 그 파일을 커밋에 넣지 않았다.
- `ORIGIN_INHERITED`는 이 시점에도 **선언된 채 반환되지 않는다.** 기록만 됐다.
- 감사의 hazard 넷(모든 키가 드롭돼도 `apply_batch_updates`가 INSERT하는 것,
  `dt_standard_map_mapper.py:104-105`의 `replace_map: True` 조합, `dt_inventory.dt_frame`을
  체인은 JSON으로 enrichment는 문자열로 쓰는 것, 자동 경로가 `FrameConfirmation` 행을
  남기지 않아 체인이 확정한 단위도 조작자에게는 `pending`으로 보이는 것)은 **넷 다 이
  시점에 미측정이고 미수정**이다.
- `map2/main.js`의 NUL 바이트 자체는 이 커밋에서 제거되지 않았다 — 검색 방식(`grep -a`)만
  바뀌었다.
</content>
