# 전송 원자 1,405개가 원장에 들어갔고, 아무것도 그 안으로 «걸어 들어가지 못한다»

> **커밋:** `fdfd0572` (09:45) · `94fc2a9b` (09:48) · `37d33610` (09:50)
> (선행: `379b65c0` (09:36))
> | **일자:** 2026-08-23 아침
> **레인:** 총괄(백필 실행) + 응용 세션(재측정)
> **측정 상자:** 이 워크스테이션. **운영이 아니다.**

## 배경 — 게이트 ①이 닫혔고 소유자가 백필을 요구했다

`transfer_event`는 preview 로 199 원자를 냈을 뿐 **커서 행이 아예 없었다.**

## 백필 — 숫자가 서로를 검산한다

```
읽은 행 1,405  ->  분자 1,405  ->  «원자 1,405»       거절 0 · 중복 0 · 미완 0
원장 총계      222,886 -> 224,291                      차이 «정확히 1,405»
두 번째 실행   읽은 행 0                                커서 유지
```

## 🔴 첫 검증 질의가 «거짓말을 했다»

`fdfd0572`가 스스로 기록한 것:

> 원자 0으로 세어졌다. **실행이 인쇄한 `translator_ver`로 필터**했는데 원자들은 **다른 값**을
> 달고 있었다 — **매핑마다**, `#die-transfer` 접미어와 자기 해시를 달고. **착지는 한 번도
> 의심스럽지 않았다. 술어가 틀렸다.** `source_who`로 세라.

그리고 몇 분 뒤 `37d33610`이 **그 정정 자체를 다시 정정했다** — 「`source_who`로 세라」는
참이지만 **「`translator_ver`가 더 이상 아무것도 못 센다」로 읽힐 수 있다.**
**접미어는 «붙는» 것이고 접두어는 아니라서**, 앞 열 글자 접두어 검사는 여전히 이것들을 찾고
**그렇게 측정된 이전 v2 수치는 전부 유효하다.** 깨진 것은 **컬럼이 아니라 등호**다.

## 🔴 그리고 원자는 «섬»이다 — 존재하는 것과 닿을 수 있는 것은 다르다

응용 세션이 백필을 재측정해 확인하고, **총괄이 찾아보지 않은 것**을 찾았다.

```
전송 원자 1,405 «전부» 원장에 있다 · 총계가 정확히 그만큼 움직였다
그런데 그것들이 이름 대는 «코어 웨이퍼 열 장»이
   원장의 다른 어떤 원자에서도 subject 로도 object 로도 나타나지 않는다  -> 0
=> 등록된 웨이퍼에서 들어가는 walk 는 «닿을 수 없다»
```

**자기들끼리는 연결된다.** 그래서 다이 위의 collect 는 그 컴포넌트에서 보여 줄 수 있다 —
**계보가 필요한 단언 둘만 안 된다.** 수용 시험은 `lot_event` 재료에 남았다.

`94fc2a9b`가 함께 적은 규율: **지시서를 건드리지 않았다** — 구현 레인이 그것을 들고 있고,
**돌고 있는 레인을 조용히 손보는 것이 범위가 커지는 방식**이다.

`37d33610`도 같은 이유로 그 발견을 돌고 있는 레인에 내려보내지 않았다 — **요구하는 «동작»은
바뀌지 않았고 그 뒤의 «이유»만 날카로워졌기 때문**이다.

## 섬을 붙이는 것은 «하지 않고 열어 뒀다»

픽스처의 이름이 **등록된 엔터티와 줄이 맞아야** 하고, **그것은 소유자의 판정**이지 남의
라운드 한복판에서 내릴 결정이 아니다.

## 디스패치 — 승인된 라운드에 «레인이 없었다»

`379b65c0`이 먼저 신고했다: 인수 기록이 **자기 라운드를 「돌고 있고 승인됨」으로** 적어 놓고
**구현자를 「유휴이고 새 작업에서 차단됨」으로** 적어서, **아무도 그것을 앞으로 나르지
않는다.** 판정이 아니라 이름 대기만 했다 — **디스패치는 총괄의 것**이라서.

같은 보고가 자기 전제를 **다시 쟀다** — 「지난번에 내가 쓴 지시서 절이 하루 만에 낡았기
때문」. 기계 모델 셋, 그 노드·엣지 수, 실제 바인딩 다섯, 로더가 **지시서 주장과 전부 일치.**

`fdfd0572`가 디스패치했다: 구성 ① 다음 ③, ②는 보류. **`ledger_subgraph`가 서버 레인**이고
**얼지 않았지만 쓰는 손은 구현자의 것**이라는 판정.

지시서의 **수용 재료 문장이 그날 아침 거짓이 됐다** — 「전송 원자는 원장에 없다」고 적혀
있었기 때문이다. 자리에서 정정했고, **새 재료에는 기준선이 없어서** 수용 시험은 `lot_event`에
남았다.

## 아키텍처 영향

- 원장에 **다른 어떤 원자와도 주어/목적어를 공유하지 않는 컴포넌트**가 처음 생겼다.
  「원자가 있다」와 「walk 가 닿는다」가 **서로 다른 측정**임이 실증됐다.
- `translator_ver`가 **매핑 단위로 접미어를 단다.** 등호 비교는 깨지고 접두어 비교는 산다.

## 그때 남아 있던 것

- **전송 원자는 섬이다.** 주어 웨이퍼 이름 열 개, 그것들을 subject/object 로 잡는 다른 원자 0.
- 수용 시험은 **`lot_event` 재료 위에** 있다. 새 재료에 기준선이 없다.
- 섬을 붙이는 일은 **열린 채**다 — 픽스처 이름과 등록된 엔터티를 맞추는 것이 필요하고,
  소유자 판정 대기.
- 모니터 셋은 전부 살아 있었다 — **컴팩션은 모니터를 죽이지 않는다.** 생사는 **출력 파일의
  mtime 이 아니라 프로세스 커맨드라인**에서 읽는다(그 mtime 이 한 번 사망 시각이었다).
