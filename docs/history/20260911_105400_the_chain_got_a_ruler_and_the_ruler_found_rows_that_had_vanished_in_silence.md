# 체인에 «자»를 대자, 조용히 사라지던 행이 드러났다

> **커밋:** `ea137dcb`~`a223dcac` (05:50~10:54)
> | **일자:** 2026-09-11 (오전)
> **레인:** 구현자(서버) · 응용 · 총괄
> **측정 상자:** 이 워크스테이션. **운영이 아니다.** 아래 수는 전부 «이 박스(운영 모양)»의 것이다.

## 무엇이 일어났나

아침의 순서가 그대로 이 항목의 줄거리다: **속도를 «재려고» 자를 댔고 → 자가 정밀해지자
「성공」으로 찍히던 자리가 «사실은 아무것도 안 한 자리»였음이 드러났고 → 그 원인이 고장이 아니라
«가시성의 시차»였다.** 주제 다섯으로 적는다.

## ⓐ 체인 «속도»를 규모별로 쟀다 — 그리고 소유자가 규격을 정했다

```
무엇이   주입 규모 10k · 30k · 100k 를 「마지막 쓰기 → 큐가 다시 빔」으로 재고, 요청 크기를 한 점 더
왜      「천만 행이면 며칠인가」에 답하려면 «곡선의 모양»이 먼저다 — 커질수록 느려지는지 아닌지
어디    응용 보고(`d31e4446` · `3adc5fc8` · `87b1b777` · `ce723752`)
```
```
규모        요청당    배수        행/s    s/1k
10k         1,000      114.1 s    87.6    11.4
30k         1,000      316.3 s    94.8    10.5
100k        1,000    1,129.1 s    88.6    11.3
30k         5,000      220.2 s   136.2   « 7.34 »   <- 코드 0, 트랜잭션 크기만
```
🔵 **규모는 s/1k 를 안 움직이고, «트랜잭션 크기»가 움직인다**(30%). 10⁷ 외삽 ≈ 31.4 h(오늘 모양).
그리고 그룹 줄을 직접 읽으면 배관 2.52 s/1k · 매퍼 8.44 s/1k, 매퍼는 «그룹당 뷰 1,000»(= 행마다 하나)에
`rows_out` 0 이다. py-spy 로 그 안을 보면 `resolve_alignment_view` 가 프로세스의 36.7%, 그중
`stamp_meta_refusal`+`meta_absence_reason` 이 ≈22% — **아무 행도 안 내는 계산의 3분의 1이 「왜 없나」를 적는 일**이다.

```
판정 258  소유자: 체인 목표 = 1,000 행당 «5 s»  (그 행은 컬럼 여덟 꼴)
판정 259  그 5 s 는 «배관»의 것 — 매퍼 «로직»은 규격 밖
판정 260  IO 를 1,000 행당 «1 s» 로 — S-151 이 S-94 ③ «앞»에
```

## ⓑ 자를 정밀하게 — 그룹 한 줄이 세 층을 말하게 됐다 (S-151)

```
무엇이   그룹 줄이 «기계류 · 뷰 안 · 쓰기 안» 세 층을 각각 «이름 붙은 조각 + 남은 시간»으로 말한다
왜      한 수가 세 가지를 뜻하면 어디를 고칠지 못 고른다. 그리고 「남은 시간」을 같이 찍어야
        «이름 안 붙은 것»이 얼마인지가 보인다 — 합이 맞는지 독자가 «검산»할 수 있다
어디    ea137dcb 08:59  그룹 «범위» 안에 이음매 다섯 — 그리고 그 범위가 판정이 가정한 것보다 «좁았다»
        6492d8d1 09:05  줄의 범위를 «커밋»까지 넓히고, 배치 발사는 «자기 줄»을 얻는다
                        (배치당 한 번이라 어느 그룹에 달면 그룹 합이 «검산 안 되는 수»가 된다)
        f6371646 09:21  쓰기 단계를 «한 층 더» 쪼개되 «셋째 dict»에 — 같은 초를 두 번 세지 않기 위해
```
🔵 자의 모양(오늘): `group <id>: view builds N · … · T s · MACHINERY · <stage…> · unnamed U s
· INSIDE THE VIEW · <phase…> · unnamed U s · INSIDE THE WRITE · <step…> · unnamed U s`
그리고 배치마다 `batch: broadcast dispatch T s · groups N`.

```
9648a272 10:00  그 자가 «첫 표적»을 찍었다 — 자동 확정이 커밋 경로에서 1,000 행 그룹당 0.875 s,
                매퍼 밖 전부의 «35%»이고 두 목적 표 쓰기를 합친 것보다 컸다. 커밋 경로에서 내보냈다
                (판정 262·264) — ⚠️ 그리고 이 이동은 같은 날 «되돌려진다»(아래 ⓒ)
```

## ⓒ 🔴 그 자가 드러낸 것 — 「성공」으로 찍히던 «조용한 손실» (S-158 · S-160)

```
무엇이   접힌 outbox 사건은 자기 행을 «이름 대어» 든다. 그 행을 «하나도» 못 읽으면
        맵퍼는 «빈 페이로드»를 받아 아무것도 안 하고, 그룹은 SUCCESS 로 끝났다 —
        사건은 처리됨으로 찍히고 그 행들은 파생 표에 «영영 안 닿았다». 오류도 재시도도 격리도 없이
왜      그리고 그때 남던 WARNING 의 «설명»이 틀렸다: 「쓰기와 체인 실행 사이에 지워졌다」.
        실측하니 그 행들은 «전부 살아 있었다»
어디    6bed2bb4 10:24  「하나도 못 읽은 접힌 사건」은 SUCCESS 가 아니라 «거절»이다 (판정 265)
```
🔴 **그리고 진짜 원인은 삭제가 아니라 «가시성의 시차»였다** — 프로브가 그 순간을 잡았다:
```
사건이 1,000 행을 이름 댔고, 세션은 «0» 을 읽었고, «같은 세션»이 100 ms 뒤 1,000 을 «전부» 봤다
(새 세션도 같음 · id 는 String 컬럼에 str · 격리 수준 read committed)
=> 그룹에 잘못된 것이 없었다. «입력이 아직 도착하지 않았을» 뿐이다
```
```
그래서 무엇이 틀렸나   「시도되지 않은 것」을 «시도»로 셌다. `max_group_attempts` 기본이 1 이라
                    첫 번에 격리됐고, 격리는 청크를 «행 단위로 다시 편다» —
                    재현: 여섯 번 쓰기 한 묶음이 «약 1,700 개»의 행별 사건이 됐다. 1초도 안 되는 창을 기다리려고
어디    a223dcac 10:48  읽을 수 없는 행은 그룹을 «미룬다», 실패시키지 않는다 (S-160, 판정 267 ⓑ)
                       새 이름 `ROWS_NOT_VISIBLE` 하나를 «내는 쪽과 알아보는 쪽»이 같이 읽는다
                       상한 `max_rows_not_visible_defers` 기본 30 (스윕 ≤ 2 s 이므로 «약 1분»)
                       🔴 미룸은 «시도가 아니다» — 재시도 예산에 달면 멀쩡한 청크가 격리된다
                       ⚠️ 그래도 «유한»하다 — 무한히 미루면 이 라운드가 없애려던 그 침묵이
                          원장 대신 «큐 안»에서 그대로 일어난다. 상한에서는 «이름 대어» 거절한다
```
```
되찾은 것   네 묶음. 커밋이 적은 수는 「3,000 행이 파생 표에 못 닿았다」이고,
           넷째 묶음은 그 뒤에 따로 되찾았다(`cef5661c`). 되돌림도 있었다 —
           ⓑ 의 자동 확정 이동이 판정 265 로 «되돌려지고», 잃은 행이 재생됐다
```
📌 **부류: 「0 을 성공으로 읽었다」.** 같은 날 응용의 계기도 같은 병을 앓았다 —
아무것도 안 쓴 실행이 「4,984 행/s」를 찍었다. 그래서 그 계기는 이제 «rc≠0» 과 «큐가 0을 안 벗어남»을
둘 다 거절한다. **수가 나온다고 잰 것이 아니다.**

## ⓓ 기저를 «쟀다» — 표 A 의 구멍과, 체인 계약표의 «신설»

```
무엇이   원장 완전성 표의 ③ 을 HEAD 에 대고 하나씩 다시 재고(D-3c), 체인 쪽을 «처음으로» 같은 모양으로 쟀다(D-4)
왜      「우리 제품 베이시스는 모두 덮어?」에 답하려면 «안 잰 곳»이 어디인지부터 말해야 한다
어디    fff3a7f5 06:4x  표 A: ③ 11 → «4» · ③′ 2 → «0». 여섯이 이미 닫혀 있었는데 표가 안 따라왔다.
                        그리고 «한 사실이 세 행»이었다(value_type) — 한 곳을 고치면 나머지 둘이
                        «같은 문서 안에서» 반대를 주장한다
        8a203e47 09:1x  `CHAIN_CONTRACT_COMPLETENESS.md` «신설» — 축 일곱 · ③ 여섯 · ③′ 0.
                        제일 큰 ③ = 「규칙 하나의 «모양»을 검증하는 자리가 없다」
                        (`load_chain_rules` 는 `json.load` + `.get("rules", [])` 가 전부)
        49432777 09:5x  정정: «키» 오타는 통과하고 «값» 오타는 그 칸만 거절된다(S-140 ④) —
                        없는 것은 「값 검사」가 아니라 «키 집합을 닫는 것»
        d55d8e7a 09:2x  표 C: ③ 여섯 그대로이되 문장 둘이 틀렸고(기저가 이틀 전 판정 168 로 움직였는데
                        표가 안 따라옴) 셋은 «한 부류»(성질이 한 생성자에만)
```
🔵 **대칭이 반대다** — 원장은 「칸 먼저, 읽는 쪽 나중」이라 ③′ 가 생겼고, 체인은 「기제 먼저,
칸은 안 만듦」이라 ③ 이 «전부 칸 없음»이다. 그래서 순서는 「검증기 먼저, 칸은 그다음」이다.

## ⓔ 그 밖에 이름이 붙은 것들

```
c47dd7d5 09:33  S-125-c — 은퇴한 검색 계약을 들고 있던 시험 둘이 «지금 것»을 들게 됐다
                (판정 263: 출하 샘플은 «싼 두 컬럼»을 선언하고, 판정 255 가 내준 여섯은 안 적는다)
S-142           이 박스의 `/health` 가 「감독자 상태가 85,118 초 낡음」을 말한다 — 이름이 붙었다
S-126-b         판정 261 이 그룹 줄의 범위를 넓히며 «마지막 한 층»에 이름을 줬다
S-143~S-159     D-3c·D-4 가 «잰» 구멍들이 큐 행이 됐다 — 소유자 「잰 것 중 기능 없는 것 대기열」 그대로.
                S-152~S-157 이 체인 계약표의 ③ 여섯, S-143 이 소급 전 비용(체인 끝 포함)
```

---
📎 이 항목의 수는 보고서(`task/ontology_application_report.md`, 09-11)의 것을 «그대로» 옮겼다.
전부 이 박스(운영 모양)의 수이고, 운영에 대한 주장이 아니다.
