# 📚 AssyManager Documentation

> **여기서 시작하세요.** 이 파일은 전체 문서의 **유일한 진입점(문서 지도)**입니다.
> 현재 아키텍처의 권위 있는 설명은 **[SYSTEM_OVERVIEW (SSOT)](./overview/SYSTEM_OVERVIEW.md)** 하나뿐입니다.

**Status 범례:** 🟢 Living(최신·검증됨) · 🟠 부분 최신 · ⚪ 참고/스냅샷 · 🗄️ Archived(대체됨)

---

## 🧭 1. 먼저 읽을 것

| 문서 | 내용 |
|---|---|
| 🟢 **[overview/SYSTEM_OVERVIEW.md](./overview/SYSTEM_OVERVIEW.md)** | **SSOT** — 현재 시스템의 전체 아키텍처. 무엇이든 여기서 시작 |
| 🟢 **[architecture/PRIMITIVES.md](./architecture/PRIMITIVES.md)** | **만들기 전에 여기부터** — 이 시스템이 이미 할 줄 아는 연산·패턴 카탈로그. "이건 무엇과 구조적으로 같은가"에 답하지 못하면 아직 설계할 준비가 안 된 것 |
| 🟢 [process/PROJECT_STATUS.md](./process/PROJECT_STATUS.md) | **「지금 무엇이 열려 있나」 단일 보드** — 다음 할 일과 살아 있는 결함. 🔴 **2026-08-01에 679줄에서 쪼갰다**(낡은 한 줄이 총괄의 위험 판정을 반대로 뒤집은 사고가 실제로 났다). 아래 셋이 그 분가다 |
| 🟢 [process/DESIGN_TRACKS.md](./process/DESIGN_TRACKS.md) | **진행 중인 설계와 그 근거** — 모호값 사전화 + 결측 온톨로지 · 쓰기 리디렉션 · 목적별 작은 온톨로지 · `map_editor.js` 리팩토링 계획. 대화에서 확정된 것이라 **다시 걸어오지 말 것** |
| 🟢 [process/INCIDENTS_2026-07.md](./process/INCIDENTS_2026-07.md) | **무슨 일이 있었나** — 프록시·체크포인트 경합·고아 워커·`dist` 오염 등 인시던트 경과와 총괄 정정 이력. 라운드별 상세는 [history/](./history/README.md) |
| 🟢 [process/BACKLOG_ARCHIVE.md](./process/BACKLOG_ARCHIVE.md) | **옛 대기열·현재 초점·완료 롤업·장기 백로그** — 미완 항목의 정본은 보드로 승격되었다 |
| 🟢 [process/CONTRIBUTING.md](./process/CONTRIBUTING.md) | **개발·문서 갱신 규율(docs-as-code)** — 코드 바꾸면 여기 규칙대로. **§2-bis = 이 저장소가 자기를 검증하는 자리**(pytest = 서버 절반 · `npm run build` = 클라 절반 **세 채점자**, 둘 다 돌려야 한다. 목록의 정본은 `package.json`의 `prebuild` 한 줄) |
| 🟢 [guide/CONFIG_GUIDE.md](./guide/CONFIG_GUIDE.md) | **"무엇을 설정해야 하는가" 단일 참조** — config 전수 지도 + 시나리오별 체크리스트(새 테이블/맵/수집기/그래프 온보딩) |

## 🏛️ 2. 아키텍처 (architecture/)

| 문서 | 내용 |
|---|---|
| 🟢 [PRIMITIVES.md](./architecture/PRIMITIVES.md) | **기능→구현 카탈로그** — *무엇을 할 줄 아나*. CODE_MAP이 *어디에 있나*라면 이쪽은 재사용 가능한 연산·패턴과 그 함정 |
| 🟢 [DUPLICATION_LEDGER.md](./architecture/DUPLICATION_LEDGER.md) | **알려진 중복 원장** — *이미 두 번 이상 만들어져 있는 것*. PRIMITIVES의 짝 문서로, 저쪽은 만들기 **전에** 읽고 이쪽은 목록의 연산을 **건드리기 직전에** 읽는다. 항목마다 **몇 곳(실측) · 정본 · 왜 아직 안 합쳤나 · 합칠 때의 함정** — 「왜 안 합쳤나」가 순진한 통합이 무엇을 깨뜨리는지 말한다 |
| 🟢 [ROOT_DEFECTS.md](./architecture/ROOT_DEFECTS.md) | **근본 결함 전담 트랙**(2026-08-11 개설) — *왜 같은 계급이 계속 나오나*. 증상은 보드가 세고 여기는 **뿌리 넷**만 다룬다: **「거절 못 함」**(쓰기 경로에 거절 어휘가 없다) · **「대조 안 함」**(선언이 실재와 맞춰지지 않는다) · **「박제」**(파생 상태를 굳혀 놓고 낡았다고 말할 방법이 없다) · **「표식이 열쇠로」**(생산자가 표식으로 쓴 필드를 소비자가 열쇠로 읽는다). 🔴 **뿌리에 번호를 안 붙인다** — `R1`~`R3`는 이미 `chain_replay`의 수리 연산 이름이라 충돌했다(§0-0). 🔴 **새 결함은 먼저 「어느 뿌리의 그림자인가」를 묻는다** — 어디에도 안 붙으면 그게 다음 뿌리 후보다. §5에 **뿌리가 아닌 것**을 명시해 둔 것이 이 문서를 살린다(전부 근본이면 죽는다) |
| 🟢 [SCHEMA_CANON.md](./architecture/SCHEMA_CANON.md) | **컬럼·키의 정준 형식 — 여덟 규칙 (2026-08-12 신설, 총괄 전담)** — *어떤 모양이어야 하나*. 여기 있는 것은 서술이 아니라 **판정**이고, 규칙마다 **그 규칙이 없어서 일어난 사고**가 붙어 있다: R1 식별자는 절대 수치형이 아니다 · R2 `business_key` 선언은 UNIQUE 인덱스 의무를 만든다 · R3 `map_key_columns` ⊆ `composite_key_source` · R4 기본값 있는 컬럼에 명시 `NULL`을 보내지 않는다 · R5 시각은 `timestamptz`이고 세상 시각은 소스별 선언 · R6 표식은 열쇠가 아니다(승격은 결정이다) · R7 상한 걸린 읽기에는 전순서 · R8 스테이지 접두 `core_*`→`dt_*`→`bond_*`. 🔴 **§2 — 검출기 없는 규칙은 규칙이 아니다**: `server/scripts/audit_schema_canon.py`가 규칙별 위반을 **센다**(읽기 전용). 🔴 **다른 문서에서 스키마 관례를 서술하게 되면 여기를 «가리키고» 규칙을 다시 쓰지 마라** — 판정의 사본이 둘이 되는 순간 드리프트가 시작된다. ⚠️ **doc-keeper·code-mapper·doc-historian은 이 파일을 편집하지 않는다** |
| ⚪ [DEFECT_SCHEMA_PROPOSAL.md](./architecture/DEFECT_SCHEMA_PROPOSAL.md) | **결함 필드 스키마 제안 (총괄 소유)** — 제안이지 현행 서술이 아니다. `ROOT_DEFECTS`/`SCHEMA_CANON`과 같은 트랙의 문서 |
| 🔵 [CANONICAL_LEDGER_DESIGN.md](./architecture/CANONICAL_LEDGER_DESIGN.md) | **정준 원장·온톨로지 설계 (착수 문서, 2026-08-12)** — **설계 문서이고 현행 서술이 아니다.** append-only 원장(원자 7필드) + 언어/기록/투영 3분할 + 어휘 성장 메커니즘(SEMI 차용) + 현행 연결 전략(소비자로 태어남, 쓰기 경로 무변경). 조인의 벽·결측 추정·확정 책임 경감이 동기. 🔴 **[2026-08-13] 종전의 「구현 0」이 거짓이 됐다 — 슬라이스 1의 세 층이 전부 착지했다**(`f896020` L1 · `01452d5` L2 · `d9b98ab` L3). **현행 서술의 정본은 설계가 아니라 리빙 문서 셋**이다: 저장 [data_model §1.1-ter](./architecture/data_model.md) · 라우트 [backend §2](./architecture/backend.md) · 화면 [frontend §6.1](./architecture/frontend.md) |
| 🟠 [PHYSICS_ONTOLOGY_SETUP.md](./architecture/PHYSICS_ONTOLOGY_SETUP.md) | **물리 온톨로지 세팅 — WF 공정·RCP 원장, 불량 모델링→액션 시나리오**(2026-08-13 · 온톨로지 포크 소유). 🔴 **절마다 상태가 다르고 헤더의 착지표가 정본이다** — **§2(공정 `processed_with`)·§3(레시피 `Recipe`+`has_param`)·§6(가상 소스)은 2026-08-14에 착지**했고, **§4 메커니즘 그래프(M4)도 같은 날 착지했다**(`f52628f` — `server/config/mechanism_models.json(.sample)`: 모델 셋 · 방향만 있는 엣지 22개 · `bindings`(필드→물리량, `87374a5`부터 `post_bond_queue_h` 포함). 소비자 둘: 3관문 랭킹의 기전 관문 `mechanism_gate.py` + `/structure` 기전 층 — 현행 서술은 [backend §2](./architecture/backend.md)·[LEDGER_GUIDE §4.6-bis](./guide/LEDGER_GUIDE.md)). **§1의 M1 `BondLine`·M2 물리량 사전 · §5 시나리오 · §5-bis 액션 어휘는 여전히 제안**이다. 🔴 **현행 서술의 정본은 이 문서가 아니다**(어휘 계약 [spec §3.7](./spec/LEDGER_TECHNICAL_SPEC.md) · 절차 [LEDGER_GUIDE](./guide/LEDGER_GUIDE.md) · 저장 [data_model §1.1-ter](./architecture/data_model.md)) — 착지 블록은 **「제안의 무엇이 실제로 무엇이 됐는가」**만 적는다. ⚠️ **`step`의 「닫힌 값 집합」은 착지하지 않았다** — 코드는 아무 문자열이나 받는다 |
| 🟢 [CODE_MAP.md](./architecture/CODE_MAP.md) | **압축 구조 지도** — 파일별 시그니처·라인 앵커·호출 흐름. 소스 전량 읽기 전에 여기부터 |
| 🟢 [backend.md](./architecture/backend.md) | 멀티프로세스 토폴로지(⚠️ **[2026-08-14] 백엔드 자식이 다섯에서 «넷»** — 그래프 싱크 워커 은퇴. **수를 여기 적지 않는다, 정본은 `run_decoupled_app.py`의 `specs`**), API 엔드포인트, outbox 패턴, **§2 ⚰️ 구 그래프 라우트 은퇴 블록**(410 · `no-store` · 구조화 본문 · 부활 경로 셋 봉인 · 저장소 DROP 841 MB), **프로세스 감시·`/health`·진행 박동(§1.3)** · **§1.3-bis 포트 충돌 = 세 번째 실패 계급**(2026-08-04 — 재시도가 원리적으로 못 고치는 실패는 예산을 태우기 전에 이름을 얻는다. 런처가 **아무것도 띄우기 전에** 거절하고 PID를 대며, 감시자는 **동료 규칙보다 먼저** 포트를 프로브해 `port_conflict`로 즉시 영구 실패. 🔴 `SO_REUSEADDR`을 세우면 가드가 조용히 통과하고, 가드 자체는 **열려 실패**해야 한다. 자식 stdout는 **바이트 그대로** 파일로 받는다) · **§1.2에 「`main`은 워커 안에서 안정된 이름이 아니다」**(2026-08-04 `cd80d66` — 수집기·파서가 자기 디렉터리를 `sys.path[0]`에 넣으므로 사용자 `main.py`가 선점한다. **CLI에서는 통과하고 워커에서만 죽는** 계급) · **§1.2에 `server/scripts` 한 방향 문**(2026-07-31 — 런타임이 그쪽을 import하면 **운영에서만** 죽고 트리거는 **200 `queued` 뒤 워커 스레드에서** 죽는다. 「import되는가」는 공유 인터프리터에서 테스트 불가라 판정은 별도 프로세스) · **§2.2 `/schema`의 `virtual_columns`**(가상 컬럼을 **`columns`에 합치지 않고 별도 키로** 알린다 — 그 키를 무시하는 클라는 키가 없던 때와 같게 동작한다. 🔴 읽기 전용을 지키는 것은 여전히 쓰기 깔때기) · **소급 적용 어드민 3라우트**(건수는 `count_kind`를 함께 답한다 · 실행은 아웃박스 한 줄 + 즉시 반환) · **그래프 조회 표에 `GET /graph/chip-trace` 등재**(2026-07-30 — depth 없는 고정 형상 · 다리별 닫힌 어휘 5종 + `scope_unresolved` · 절단은 상태가 아니라 플래그) |
| 🟢 [frontend.md](./architecture/frontend.md) | client2 웹(AG-Grid) + QtWebEngine 데스크톱 셸 · **§4.2 Map Editor 2**(2026-08-05 신설 — 페이지 하나와 `src/map2/*` 17모듈이 **이 문서에 존재하지 않는 채로** 출하돼 있었다. 🔴 **레거시를 대체하지 않고 옆에 선다** · 🔴 **정렬 캔버스에 웨이퍼 테두리는 안 그려진다**(`d4e0fed` — 하드코딩 장식이었고 **모든 맵이 구성상 그것을 넘쳤다**) · 🔴 **좌석과 그리기를 다시 합치지 마라**) · **§2.1 빌드 게이트**(`prebuild`의 **세 채점자** — 클립보드 관례 + `contracts/*` 발견식 스캔 + `client2/tests/*.mjs` 발견식 스캔. **빈 스캔은 실패**이고 목록의 정본은 `package.json` — 🔴 **수를 여기 적지 않는다. 러너가 매 실행마다 `N harnesses ― M gated, K known-red`를 찍으므로 그것을 읽을 것.** 「16」도 「18개 중 13 강제·5 부채」도 적힌 뒤 곧 틀렸고, 두 번째 것은 **「이 수는 인용하지 마라」는 문장 바로 앞에** 적혀 있었다. **단언 플로어(바닥)와 천장(`CEILINGS` — 현재 `map_editor.js`의 모듈 레벨 가변 바인딩 상한)까지가 이 게이트다**) · **§3.4 가상 조인 컬럼 렌더**(2026-07-31 — 🔴 **그리는 순간 그 컬럼은 쓰기 대상이다**: 붙여넣기는 컬럼 목록이 아니라 **그리드 컬럼 id**로 배치를 만든다. 쓰기 문 넷에 가드, **복사는 반대로** 받아들인다. ⏳ CSV 추출 누락 · `미상` 행 검색 불가) · **§3.3 값 제안 셀 에디터**(`Enter` 한 번이 채택이고 확정 · `Escape`는 시계가 아니라 `suggestionsEngaged`가 판정) · **§5 config 해석 한 줄**(F9 — 문장은 서버가 만들고 클라는 그대로 렌더 · **읽기 실패 다섯 갈래는 상태코드가 아니라 `WWW-Authenticate` 헤더로 갈린다**) · **§3.5 / §3.5-bis 기동 순서와 연결 감시견**(2026-08-04 — `init()`의 **첫 문장**이 `initWebSocket()`이고, 응답이 오지 않는 경로는 `onopen`도 `onclose`도 부르지 않으므로 **8초 상한**이 필요하다. 🔴 **배지가 덮고 있던 세 상태가 갈라졌다** — 「소켓을 만든 적 없음」·「협상 중」·「경로가 삼킴」. 첫째는 `index.html`의 **초기값이 손대지 않은 채 남은 것**이고 그 자체가 §3.5 결함의 신호다) |
| 🟢 [data_model.md](./architecture/data_model.md) | ORM 모델 + 동적 테이블 + 레이어링/우선순위. **§2.1이 표시값 해결의 정본** — 등재 우선순위 → `ingested_at` 내림차순 → `source_name` 오름차순의 **전(全)순서**(2026-08-11). 승자는 **박제**되므로 규칙 수리는 앞으로의 쓰기만 고치고, 이미 확정된 칸은 **§2.2-ter R3**가 고친다 |
| 🟠 [event_driven_backend.md](./architecture/event_driven_backend.md) | Outbox 패턴 · 체인 인제션 심화(§1~§3 = Living 정본). ⚰️ **[2026-08-14 `2ec78b9`] §4 온톨로지 그래프 승격(materializer)은 은퇴**해 접혀 있다 — outbox 소비자가 셋에서 **둘**이 됐고, §1 다이어그램·§2의 `GRAPH_BATCH_LIMIT` 서술은 이제 **도는 코드에 대한 서술이 아니다**(축약 설계의 근거로만 읽을 것) |

## 🧩 3. 서브시스템 리빙 가이드

| 서브시스템 | 문서 |
|---|---|
| 파일 인제션 파이프라인 | 🟢 [guide/INGESTION_GUIDE.md](./guide/INGESTION_GUIDE.md) — **§1.8-bis 두 층 원장**(2026-08-13 `ba664c5` — 파일을 옮기지 않고 재처리를 막는다. tier 1 = `(테이블, 경로, mtime, size)`, tier 2 = 내용 해시. 🔴 실패 방향과 마이그레이션 선행 조건을 **켜기 전에** 읽을 것) · **§1.8-ter tier 1을 «어디서» 묻는가**(2026-08-13 `831ab68` — 스윕과 트리 워크가 **이미 든 `stat`으로 500개씩 묶어** 묻는다. 🔴 **느린 것은 질문이 아니라 질문에 도달하는 길이었다** — 파일당 92 ms 중 원장 몫 0. 재스윕 26.4초→0.6초, **콜드 스윕 1.0배가 「아무것도 안 건너뛴다」의 대조군**) · **§1.9 폴더 드롭은 제자리 적재**(2026-07-30 `600b49d` — 파일을 루트로 승격하지 않고 **자기 중첩 경로 그대로** 적재, 상대 POSIX 경로가 파서에 `self.rel_path`로 간다. 접두 개명·`~` 구분자·`__force__` 방어는 함께 소멸. ⚠️ 토글 `flatten_nested_dirs`는 **이름은 그대로 뜻만 바뀌었다** — 개명하면 운영자의 off 스위치가 조용히 무력화된다) · **§1.9-bis `filename_rules` 선언 규격**(폴더 이름 → 컬럼. 허용 키 5개·명명 상태 4종·로드 시점 거절 · 🔴 **우선순위 `filename < header < row`는 사용자 판정**: 파일 안의 값은 그 파일의 주장이고 폴더 이름은 누가 옮기면 바뀌는 외부 맥락이다) |
| 체인 인제션(DB세션 맵퍼) | 🟢 [guide/chain_ingestion_guide.md](./guide/chain_ingestion_guide.md) |
| **DT/Core 프레임 파생 체인** | 🟢 [architecture/DT_CORE_FRAME_CHAINS.md](./architecture/DT_CORE_FRAME_CHAINS.md)(구조·권위·불변식) · 🟢 [guide/DT_CORE_FRAME_CHAINS_GUIDE.md](./guide/DT_CORE_FRAME_CHAINS_GUIDE.md)(운영 설정 순서) · 🟢 [spec/DT_ALIGNMENT_METADATA_CHAIN_SPEC.md](./spec/DT_ALIGNMENT_METADATA_CHAIN_SPEC.md)(메타데이터 홉 계약) — **[2026-08-09~10 `a501d6d`·`2ec8e24`·`7097a67`]** `dt_log` 한 잡이 **좌표 공간 둘**(DT `b_wx/b_wy` · core `c_wx/c_wy`)을 들고 있고, 그 둘을 **각각 한 번 확정해 `dt_inventory`에 적은 뒤** 표준 좌표 맵을 파생한다. 🔴 **정렬기의 확정을 사람 클릭이 아니라 체인이 부른다** — `confirmed_meta_for`가 그 자리다(⚠️ **§2.5의 dt→core 채점 축(`index_group_count`)을 물린 것이 아니다** — 그쪽은 여전히 호출자 0건). 🔴 **[2026-08-13 `4d5198c`] `dt_map`은 이제 취득 단위(`dt_job`)가 아니라 물리 단위 `(dt_lot, dt_slot)`으로 키가 잡힌다** — **여러 잡이 한 맵에 수렴하는 것이 목적**이고, 그래서 제거 전략이 `replace_map`이 아니라 **`retract`(출처 단위 철회)**이다. 🔴 **철회는 향상이 아니라 전제조건**이다(맵 단위 purge는 재실행이 아니라 **첫 파생에서** 형제 잡을 지운다) · 🔴 **셀의 `dt_job`은 last-writer-wins**라 셀을 뺏긴 잡은 자기 stale 기여를 회수할 수 없다(덜 지우는 쪽이라 안전하지만 잔여가 남는다) · **lot/slot이 확정 안 된 잡은 맵이 없는 것이 정상**이다. 🔴 **`dt_map`·`core_usage_map`은 파생물이라 손으로 고치지 않는다** — 프레임이나 원본이 바뀌면 **소유 체인을 다시 돌린다**. 🔴 **기준 부재·`core_wafer` 미해결은 정상적인 무출력**이지 프레임을 지어낼 근거가 아니다. ⚠️ **이 셋은 2026-08-11까지 이 표와 [DOC_OWNERSHIP](./process/DOC_OWNERSHIP.md) 양쪽에 행이 없는 고아였다**(MAP_ALIGNMENT_SPEC·TRACE_FIXTURE_SPEC과 같은 실패, 세 번째다) |
| Auto-Update 스케줄러 | 🟢 [guide/AUTO_UPDATE_GUIDE.md](./guide/AUTO_UPDATE_GUIDE.md) — 주석 기반 크론 · `out` 변수 가로채기 · 수집기별 Active 토글 · **이 데몬이 도는 「수집기가 아닌 일」 3건**: §4-bis 주간 config 스냅샷 · **§4-ter 그래프 고아 노드 스윕**(2026-07-30 — 요점은 삭제가 아니라 **거절**: 선언에 거부가 하나라도 있으면 전체를 거절한다) · **§4-quater 소급 적용 실행**(2026-07-31 — 🔴 앞의 둘과 달리 **전용 스레드**다. 인라인이면 실행 내내 박동이 멈춰 `/health`가 이 데몬을 `wedged`로 보고한다. 동시 1건) |
| 웨이퍼 맵 에디터 | 🟢 [map_editor/](./map_editor/README.md) · [spec/MAP_EDITOR_SPEC.md](./spec/MAP_EDITOR_SPEC.md) — 🔴 **§1의 0-요약) 맵 기준 가치 6줄이 먼저다**(사용자 확정 2026-07-31 · 축은 실제 물리 환경 · 핵심은 **4)와 5)의 구분** — 기하를 바꾸는 것과 방향을 바꾸는 것은 좌표에 대해 다른 일이다. **정본은 [보드](./process/PROJECT_STATUS.md)**). 그 하위가 **§1의 0) 좌표 규약**(화면이 기준 · 표시 = 오리진 + DB 값 · `start_x/y` = 유효 다이 영역의 최소 열·행이자 **운영자의 선언** · **저장 좌표 = 오리진 기준 칸수, mm 아님**). **§1-bis 좌표 공간 이름**(2026-07-31 개명 — `getDbCoords`/`getDieIndex`/`getCanvasCellFrom*` · **웨이퍼 mm 공간이 `cd3e0f4`에 생겼다** — 종전 *「`mm`은 일부러 비어 있다」*는 **거짓이 됐다**. ⚠️ 저장 좌표가 칸수인 것은 안 바뀌었다) · **§1-bis-2 파일 경계**(🔴 **맵 에디터는 더 이상 파일 하나가 아니다** — `map_key.js`·`split_registry_row.js`가 빠져나갔고 **분할은 진행 중**이다. 심볼의 거처는 CODE_MAP grep) · **§1-ter 캔버스 축척**(2026-08-04 — **두 축에 px/mm 하나**이고 그것을 **선언된 웨이퍼 지름**이 정박한다. 타원의 원인은 원이 아니라 **셀**이었다. 피치 X≠Y면 셀은 직사각형이고 그것이 요청된 결과 · **여백은 셀이 아니라 격자선뿐**이고 세어지지도 저장되지도 않는다) · **§1-ter.1 `auto_registered`**(합성 규격 표지 — 🔴 **판정은 값이 아니라 표지**이고 `chip 1×1`을 냄새 맡지 않는다. 그런 기하는 원을 정박하지 않고 **오버레이 정렬을 이름 대며 거절**한다. 실측 668행 중 **320행**). §1~§4 격자 에디터 · **§4-ter 회사 양식 왕복**(거부 5갈래 · 선언 맵 179개 중 27개만 프레임 지문) · **§5 범용 맵 오버레이**(`wafer_map_metadata`가 정렬의 유일한 기준 · 변환은 클라 단일 구현 · **프레임 창이 자기 상자를 실어 나른다**) · **§5.7-bis / §5.7-ter 원점 상자가 움직였을 때의 유일한 반응**(`reseatCellsToStoredCoords` — 기하는 참조를 따라가고 **좌표는 안 움직이며 칸이 좌표를 따라간다**. 선언 없는 맵의 **기하 프리셋·피치 편집도 같은 연산**이고, `9d7d9a4`부터 **격자 `COLS`/`ROWS` 직접 입력까지 같은 반응 하나를 탄다 — 부르는 자리 넷, 함수 하나**) · **§4-ter.2-bis 상단 병합은 맵 격자에서 끝난다**(`9d7d9a4` — 종전에는 인쇄물에서 제목·그룹 띠가 DOE 보조표 위를 지나갔다) · **§6 전사 계획**(zone 모델 · 데이터 보호 게이트 4종은 §6.0-ter) · **§5.9 규격을 모르는 맵을 정렬한다**(2026-08-05 신설 — **규격 부재가 정상이라는 전제**에서 빌림 → ~~담김~~(**[D16] 2026-08-06 은퇴**) → 요청 단위 1회 거절(`basis_undeclared`)이 사슬 하나로 따라 나온다. **판정·실측의 정본은 [spec/MAP_ALIGNMENT_SPEC §9.1·§9.4·§9.5·§9.6](./spec/MAP_ALIGNMENT_SPEC.md)이고 §5.9는 수를 다시 적지 않는다** · 🕐 **OPEN 하나**: 가정 수락이 **선언된** 격자까지 덮어야 하는가 — **오늘은 안 덮는다, 어느 쪽으로도 구현 금지**) · **§4-bis.3-ter 고른 프레임은 골랐다고 기록된다**(`frame_chosen_from` — 🔴 **일곱째 토큰이 아니다**, 선례는 `phys_assumed_from` · 빈 START 칸은 0으로 지어내지 않고 이름을 대며 거절). 🔴 **§5.7-bis는 기본적으로 불신하라** — 이틀 새 네 번 뒤집혔고 제품에 없는 문구를 인용한 코드 블록이 두 번 남았다. **인용 전 소스 grep.** |
| **맵 정렬 (Map Editor 2)** | 🟢 [spec/MAP_ALIGNMENT_SPEC.md](./spec/MAP_ALIGNMENT_SPEC.md) — 🔴 **맵 에디터 재개발에 착수하기 전에 이 문서를 먼저 읽는다.** 5개 렌즈가 하루 동안 독립적으로 측정한 종합이고 **여기 적힌 것 중 여럿이 직관과 반대다**. ⚠️ **2026-08-05까지 이 표와 [DOC_OWNERSHIP](./process/DOC_OWNERSHIP.md) 양쪽에 행이 없는 고아였다**(TRACE_FIXTURE_SPEC과 같은 실패). **§0.1 목적은 사슬이지 기능이 아니다**(좌표계 확정 → 소스 얼라인 → 다이 맵 확정 → **본딩 계획**. 종점은 계획이고, 한때 층 목록에서 그 계획이 빠져 있었다) · **§0.2 층 열 개 — 「안 하는 일」이 층을 지키는 부분이다**(쓰는 층은 ⑧ 확정 하나 · 화면을 아는 층은 조립 하나 · 그리는 층은 ⑩ 하나) · **§1 왜 지금 안 되는가는 정리(theorem)다**(같은 변환이 비교할 두 집합에 함께 걸리면 관계를 못 바꾼다 — **어떤 UI로도 「회전 버튼을 돌려 본다」는 정보를 낼 수 없다**) · 🔴 **§2.4 후보의 두 번째 축은 걸음의 시작 모서리다**(2026-08-08 `db1ee42` **교체** — 후보 = **4회전 × {좌상단, 우상단} 시작**, 면은 전부 `front`이고 **거울은 후보 집합에서 나갔다**. 철자 `rot90_tl`/`rot90_tr`, `parse_frame`이 `front`/`back`을 **레거시로 계속 받는다**. ⚠️ **종전 문장 「거울 반쪽 = 우상단 시작 반쪽」은 두 번 거짓이 됐다** — 그 등가는 **4분의 1 회전에서 틀리고**, 지금은 거울 자체가 후보가 아니다. **어디서 보든 「8후보 = 회전 × 면」이라 적힌 문장은 낡은 것이다**) · **§9.10 앵커는 기준의 좌상단에 고정되지 않는다**(후보가 모서리를 고른다 — 그 전엔 `tl`/`tr`이 **구성상 쌍둥이**였다) · **§9.11 목록(worklist) 사유 코드 전수**(2026-08-11 `c4a3159` — `unscorable_reasons`가 낼 수 있는 다섯 + 상세와 공유하는 둘. 종전엔 이 어휘가 문서 어디에도 없었다) · 🔴 **§9.12 상세도 목록도 맵마다 질의하지 않는다**(2026-08-12 `b510df2` — **게이트는 요청이 아니라 *맵* 단위이고 그대로 둬야 한다**: 되돌릴 수 없는 맵 170/1,443개는 오늘 셀을 0건 읽으므로 배치로 서빙하면 **지금 화면에 없는 셀이 나타난다**. ⚠️ **`dt_map`의 `/view`는 이제 파이썬 바운드라 그 SQL을 다시 공격하지 말 것**) · **§2.5 dt→core 축**(`index_group_count`·`bin_fingerprint_shift`가 **호출자 0건**으로 착지 · 「88/88 대 4/88」은 **반증이 아니다**) · **§3 채점 지표 둘과 그 한계**(🔴 **적합도 퍼센트를 순위 근거로 쓰지 말 것** — 방향이 맞고 한 칸 밀린 후보가 방향이 틀린 셋보다 아래로 갔다. **항상 순위를 내는 도구는 무능을 인정하는 도구보다 나쁘다**) · **§7 절대 다시 만들면 안 되는 것 I1~I11** — 새 시스템의 첫 하네스 목록 · **§9.1 웨이퍼 규격은 빌리고 선언은 만들지 않는다**(순환을 끊은 자리) · **§9.2 어휘를 늘렸을 때 읽는 쪽이 무너지는 법**(소비자 전수표). 화면의 모듈 경계는 [architecture/frontend §4.2](./architecture/frontend.md) |
| DOE 작성 가이드(사용자) | 🟢 [guide/DOE_GUIDE.md](./guide/DOE_GUIDE.md) — 색칠=계획 · STACK 0=상태 표시 · 저장은 ⚡ Push 하나 · 검수는 보고만(막는 건 **게이트 4종**) · **수량은 「저장되는 셀」만 센다**(2026-07-30 — Fill All을 쓴 맵은 숫자가 내려감, §9에 감소폭 표) · 엑셀 계약 밖 칸은 **13단어**(COLOR·칠함·COUNT + 롤업 8단어는 **예비**) · **§4.2 맵 화면 Ctrl+V로 회사 양식 되붙이기**(VALUE·STACK·DESC만 복원, 자재·COLOR는 왕복 안 함, **값을 지우지 않음**) — ⚠️ **§4.2에 거부 안내 신설**: 거부는 시트가 아니라 **맵의 기하** 때문이고 **179개 중 27개에서만 왕복이 성립**한다 · **§6.1 `MAP X`는 `↻ 가용` 없이도 풀린다**(맵 로드·Push 성공·STACK 마커 경계 편집이 자동 재확인 — `✓`는 다시 묻지 않고 `X`/`?`만 다시 묻는다) |
| 유효 다이 맵 가이드(사용자) | 🟢 [guide/VALID_DIE_MAP_GUIDE.md](./guide/VALID_DIE_MAP_GUIDE.md) — **유효 다이도 맵이다**: 어느 셀이 유효한지를 원 기하가 아니라 **다른 맵 하나**가 정하게 한다. 프리셋의 「🧩 템플릿 만들기」로 저작 → ⚡ Push → 쓸 맵의 「🎯 유효 다이 맵」에서 **고르면 곧 적용** → **⚡ Push** 또는 **📐 규격만 저장**. 🔴 **[2026-08-04 `5b15c24`] §4가 하루 만에 다시 개정됐다** — 전날 생겼던 **🎯 APPLY / 💾 SAVE 두 버튼이 삭제**됐고, 컨트롤은 진짜 `<select>`(고르는 즉시 적용)이며 목록이 잘렸거나 지금 키가 목록에 없으면 **입력칸으로 폴백해 `Enter`만** 받는다. 저장 테이블은 `valid_die_ref` 고정, 참조는 1단계, 키를 비우면 원 복귀(**해제도 저장해야 남는다**). 참조가 안 풀리면 **이름을 대고 거절**한다(§4-0). **§4-bis.0 좌표 규약이 먼저다**(🔴 **저장 좌표 = 오리진 기준 칸수, mm 아님**). **§4-bis.1**(2026-07-31 개정): 지정하면 **물리 규격과 격자 크기가 템플릿을 따라가고**(회전·면·START는 그대로) 칠한 셀은 **자기 저장 좌표가 가리키는 칸으로 다시 앉는다** — **저장 좌표는 하나도 안 움직인다.** **§4-bis.1-bis 신설**: 참조가 **없는** 맵에서도 **피치·규격 프리셋만 고쳐도 같은 일**이 일어난다(선언이 없으면 웨이퍼 원 자체가 유효 다이 영역이므로 같은 연산). 알람은 크기가 아니라 **원점**을 보므로 **크기가 같아도 뜬다**. **§4-bis.3 `[유효다이]` 콘솔 7줄** · **§4-bis.4** 거절은 참조 메타 치수가 `1~100` 정수 밖일 때 하나뿐 · **§4-bis.5**(2026-07-31 `9d7d9a4` 정정) ✅ **격자 `COLS`/`ROWS` 직접 입력도 이제 같은 보호를 받는다** — 종전의 *「보호가 없으니 맞춘 뒤 📂 Load로 다시 읽으라」*는 **거짓이 됐고 우회로째 삭제**됐다. ⚠️ 이 절은 이틀 새 네 번 뒤집혔다 — 인용 전 소스 grep. |
| DOE 구간 모델(부분 폐기) | 🟠 [spec/DOE_BAND_MODEL.md](./spec/DOE_BAND_MODEL.md) — 구간(band) 모델 본문은 zone 모델로 대체(🗄️), **§4-bis BIN 축·§6-bis BIN별 분해는 계속 정본** |
| DOE 영역 저장 지도 | 🗄️ [spec/DOE_STORAGE_MAP.md](./spec/DOE_STORAGE_MAP.md) — **본문은 폐기된 3테이블 모델**이며 기존 데이터 해석용으로만 보존합니다. M2.6이 양쪽 다 착지해(`cdcddee`+`0f8d35f`) 지금은 `map_split_registry` **한 테이블**입니다 → 현행 계약은 [MAP_EDITOR_SPEC §6](./spec/MAP_EDITOR_SPEC.md)·[CONFIG_GUIDE §5.8](./guide/CONFIG_GUIDE.md). 🔴 **[2026-08-13 `c0fb735`] `map_doe`·`map_doe_source`는 선언째 은퇴했고 개발 두 DB에서는 물리 DROP까지 실행됐습니다** — 이 문서의 컬럼 표는 이제 **읽을 수 있는 테이블에 대한 서술이 아닙니다**(운영 실행은 [OPERATOR_RUNBOOK §5](./process/OPERATOR_RUNBOOK.md)) |
| **🧱 원장·온톨로지 «셋업»** (2026-08-15 통폐합) | 🟢 **[guide/ONTOLOGY_LEDGER_SETUP.md](./guide/ONTOLOGY_LEDGER_SETUP.md) — 「무엇을 선언해야 도는가」의 단일 정본.** 소유자 지시(「온톨로지 원장 셋업 관련 문서 하나로 통폐합」)로 **네 곳에 흩어져 있던 셋업 절차를 여기 모았다**(종전: `LEDGER_GUIDE §3 ②·§4.1·§4.2` · `CONFIG_GUIDE §1` · `DEPLOY_SETUP 8-septies` · `CONFIG_ROLLOUT_GUIDE`). 🔴 **절차가 두 벌이면 한 벌이 낡는다** — 같은 날 운영자 가이드 셋이 **이미 삭제된 것을 돌리라고** 말하고 있었다(`08e2659`). 담는 것: **선언 표 전수**(파일마다 config인가 코드인가·없으면 무엇이 일어나는가·`.sample` 폴백 여부) · **순서**(시간대·`tzdata` → 선행 테이블 → 마이그레이션 둘 → 소스 선언 → 백필 → 화면 층) · **`ledger_config.json` 키 표 «넷»**(문법 `lineage`·`observation`·`transfer`·**`declared`** — 🔴 **넷째는 베낄 파이썬 번역기가 «없다»**: 행→원자 사상이 `emit` 선언 그 자체라 새 모양의 테이블에 **코드 0줄**이다, 2026-08-15 3차 · §3.4) · 🔴 **§8 실물 대 제안**(PHYSICS 문서의 절별 상태를 **실측으로** 갈라 적는다 — 제안을 단계로 적은 셋업 문서는 없는 것보다 나쁘다) · 🔴 **§9 완성 조건 미달 자리**(⚠️ **2026-08-15에 절반이 닫혔다** — 어휘의 **온톨로지 층 술어**는 이제 선언으로 늘리지만 **개체 타입은 여전히 코드**다. ✅ **[2026-08-15 3차] 「등재한 낱말을 발화할 번역기가 없다」는 닫혔다** — 넷째 문법 **`declared`**가 그 구멍이고(§3.4), ⚠️ **그것은 미구현으로 남은 `derivation`과 «다른 것»이다**(이쪽은 소스 행을 옮기는 2류, 저쪽은 원장을 걸어 추론하는 3류). `finding_kinds`의 모집단 주소 셋도 여전히 코드 상수다) · **§7 `ontology_mapping.json`은 쓰지 마라**(은퇴 — 남은 유일한 독자가 아무도 안 읽는 표지 하나다) |
| **정준 원장 (ledger) — 슬라이스 1** (2026-08-13 착지) | 🔴 **판정의 정본은 [process/LEDGER_RULINGS.md](./process/LEDGER_RULINGS.md)이고, 거기 없는 판정은 «내려진 적이 없는 것»이다**(세션 간 메시지 채널이 하루에 세 건을 조용히 유실한 것이 실증돼 판정이 파일로 옮겨졌다). 착수 지시는 [process/LEDGER_SLICE_1_BRIEF.md](./process/LEDGER_SLICE_1_BRIEF.md), 설계 배경은 [architecture/CANONICAL_LEDGER_DESIGN.md](./architecture/CANONICAL_LEDGER_DESIGN.md). 🔴 **[2026-08-13 신설] 도메인 문서 둘 — 여기부터 읽는다**: **HOW** [guide/LEDGER_GUIDE.md](./guide/LEDGER_GUIDE.md)(모듈 지도 · 쓰기 경로 · **§3 새 소스 붙이는 법 6단계** · 운영자 절 · 경계) · **EXACTLY-WHAT** [spec/LEDGER_TECHNICAL_SPEC.md](./spec/LEDGER_TECHNICAL_SPEC.md)(11컬럼·CHECK·복합 PK·파티션 · **가격표 붙은 인덱스 집합**(제거된 셋 포함 — 되돌리는 것은 «숫자가 붙은 결정») · 쓰기/해결 의미론 · 실패 모드 · 비기능). **분업은 넷이고 서로를 «가리킨다»**: 설계=WHY · 가이드=HOW · 명세=EXACTLY-WHAT · RULINGS=판정. **현행 서술 셋** — 저장·시각 선언 [architecture/data_model §1.1-ter](./architecture/data_model.md)(🔴 **파티션이 첫날부터라 PK가 `(id, occurred_at)`이고, 그래서 「`id`가 유일하니 마지막 층이 결판낸다」를 쓸 수 없다** · 🔴 **선언이 `UTC`이던 동안 모든 원자가 9시간 어긋나 있었고 아무것도 항의하지 않았다** — 어긋난 시각도 well-formed하다 · ⚠️ **`tzdata`가 새 배포 의존성**) · 라우트 [architecture/backend §2](./architecture/backend.md)(🔴 **빈 `hops`는 가능한 답이 아니다** — 끊긴 홉과 이유가 답이다 · **응답 형태는 pin됐고 바꾸는 것은 에스컬레이션**) · 화면 [architecture/frontend §6.1](./architecture/frontend.md)(🔴 **관례/실측 구분이 `reason` 산문에만 살아 소박한 파스가 판정을 «뒤집었다»** → 상설 원칙: **화면이 분기해야 하는 사실은 반드시 구조화 필드**). 재사용 관점은 [PRIMITIVES §3·§5·§6·§7](./architecture/PRIMITIVES.md), 회귀 점검은 [qa/FEATURE_CHECKLIST §1.13](./qa/FEATURE_CHECKLIST.md), 운영 실행은 [process/OPERATOR_RUNBOOK §6](./process/OPERATOR_RUNBOOK.md).<br>🔴 **[2026-08-13 5차 · `4f46c25..f313279`] 세 가지가 바뀌었고 전부 「조용한 것을 소리 나게 만든」 변경이다**: ① **선언이 문다** — `subject_type`(단수, 어느 원자에도 도달 안 함) → **`subject_types`(복수 허용목록)**이고 밖은 `undeclared_subject_type`으로 거절·계수된다. **옛 키는 에러다**(무시도 승계도 아님). 상설 규칙: **선언 필드는 집행 지점을 갖거나 존재하지 않는다** ② **거절이 반쪽을 안 남긴다** — 「원자 하나가 나쁘면 분자 전체」가 **어느 파편에든** 걸리도록 `gate.MoleculeRefused`(예외) + `building_molecule` 스코프로 승격됐다. 🔴 **반환값 신호는 `... or []` 하나에 삼켜졌고 원자 셋이 실제로 착지했다** ③ **거절 사유를 DB에서 읽을 수 있다** — 커서에 `refusal_reasons`(열둘 → **열셋**, **마이그레이션이 둘이 됐다**)이고 `/coverage`가 그것을 서빙한다. 🔴 **`refusals_unaccounted`는 «부호»가 계약**이고 **`> 0`은 배포 이력이지 결함이 아니다**(이 박스 두 행이 지금 `1`이다). ⚠️ **`atoms.estimate`가 `count(*)`보다 낫다는 것은 이 박스에서 시연 불가**(909 원자에서 정확 카운트 0.194 ms — 근거는 구조이지 측정이 아니다).<br>🔴 **[2026-08-14] 어휘가 일곱 → «아홉»이고 DDL은 0줄 바뀌었다** — `processed_with`(설계가 **예약해 뒀던** 낱말의 첫 사용)·`has_param`, 개체 타입 **`Recipe`**(🔴 **`rev`가 subject 키 재료** — 개정은 편집이 아니라 새 등록이고, 속성이면 rev5를 적는 순간 rev4로 돌았던 웨이퍼의 근거가 도달 불가능해진다). 🔴 **어휘는 컬럼이 아니라 코드의 선언이라 스키마 감시로는 절대 안 잡힌다.** 🔴 **`value` 목적어가 처음으로 «검사된다»**(`required`) — ⚠️ **존재 검사이지 진리값이 아니다**(`0`·`False`는 정당한 설정값). 🔴 **「실측이 설정값을 이긴다」에 랭킹 코드가 «한 줄도» 없다** — 계급 경계가 사준 것이고, 그래서 **초록만으로는 아무것도 증명 못 하며 계급-맹 뮤턴트가 «반대» 답을 내야** 한다([spec §3.7·§4.1-bis](./spec/LEDGER_TECHNICAL_SPEC.md) · [LEDGER_GUIDE §3-bis 생성기 소스 · §4.7 롤백](./guide/LEDGER_GUIDE.md) · 설계 대조 [PHYSICS_ONTOLOGY_SETUP](./architecture/PHYSICS_ONTOLOGY_SETUP.md)).<br>🔴 **[2026-08-14 2차] 읽는 쪽이 «두 수준»이 됐다** — `GET /api/ledger/structure`(`server/ledger_structure.py`)가 **유형 수준** 그림을 답한다(`/trace`는 인스턴스 수준이고 이건 그 보완 — 랏·웨이퍼는 한 건도 안 나온다). 🔴 **응답 어디에도 손으로 적은 노드·엣지 목록이 없다**: 선언된 절반은 어휘에서, 관측된 절반은 `GROUP BY` 하나에서 **생성**되고 병합된다 — 술어를 어휘에 더하면 라우트를 **한 줄도 안 고치고** 화면에 뜨고, 어휘에 없는 모양은 **`undeclared`로 뜬다**(버려지지 않는다). 🔴 **`atoms: 0` ≠ `atoms: null`**이고 **건수 0인 선언 엣지는 숨기지 않는다** · 🔴 **창은 «건수»만 좁히고 선언은 안 좁힌다**(크기 방어가 「정직한 빈 축」을 먹으면 안 된다) · **캐시 없음이 판정**. ⚠️ **기전 층(M4)은 `state: "absent"`** — [PHYSICS_ONTOLOGY_SETUP §4](./architecture/PHYSICS_ONTOLOGY_SETUP.md)의 제안을 **데이터로 옮겨 적지 않고** 부재를 이름 대어 말한다([guide §4.6-bis](./guide/LEDGER_GUIDE.md) · [spec §4.7·§5.7](./spec/LEDGER_TECHNICAL_SPEC.md) · [backend §2](./architecture/backend.md)).<br>🔴 **[2026-08-14 3차 · R-2026-08-14-D·E] 결함 관측이 원장에 들어왔고, 걷기 의미론이 «선언»이 됐다.** ① 어휘가 **열하나**(`observed` — subject `Wafer`, `value` 목적어의 `required`에 🔴 **`run_uid`**: 분모 규율이 원장 «안»에서 서는 자리다. ⚠️ **짝인 `measured`는 일부러 미등재** — 발화자가 없다) ② **소스 선언에 `kind`가 생겨 문법이 둘**(`lineage` 기본 · **`observation`**: 한 행 = 한 발화, 🔴 **세상의 시각이 관측 행에 없어 런에서 읽고 못 풀면 거절**, 🔴 **커서가 세계시각이 아니라 «키셋»** — 대량 적재가 `updated_at` 하나를 91,756행에 찍는다) ③ 🔴 **`traversable`·`direction`이 술어 선언의 필드**가 되고 `LINEAGE_PREDICATES`는 **파생물**이 됐다 — **동작 불변이 단언돼 있고**, **`observed`는 `traversable: None`이라 걷기가 아예 인출하지 않는다**(웨이퍼 하나가 관측 수만 건을 인다). ⚠️ **`degree_cap`은 집행 지점이 없어 «미선언»으로 추인**됐다(R-2026-08-14-G). 실측: **102,177 관측 원자 · 거절 0 · 불완전 0**, 원장 84,747 → **186,924 · 파티션 다섯**, 3홉 추적은 여전히 **관측 0건 · 5.2–5.8 ms**([spec §3.7-quinquies·§4.8·§5.5-bis](./spec/LEDGER_TECHNICAL_SPEC.md) · [guide §3 ②-bis·§4.7](./guide/LEDGER_GUIDE.md) · [data_model §1.1-ter·§1.2-bis.1](./architecture/data_model.md))<br>🔴 **[2026-08-14 밤] 콘솔이 «마킹→대조»가 됐고 `/siblings`가 드디어 라우트 표에 등재됐다.** `scope=`가 엔진을 바꾼다(**걷기 대조** — 항목 목록 0줄) · **수치가 원인 후보가 됐다**(주어당 한 값으로 접고 같은 랭킹 사다리 — 둘째 임계 없음) · **`wafer` 마킹 축은 선언 한 항목·파이썬 0줄**(「이 두 장 vs 나머지」가 처음 표현 가능) · **요청된 scope 값은 전수 회계**(에코는 해소가 아니다). 화면은 [frontend **§6.4 신설**](./architecture/frontend.md)(가로 트렌드 표·열 구성=URL·`data-mark-lot` 체크박스·2장=우측 쌍 레일·`wafer` 포커스 파라미터), 라우트는 [backend §2](./architecture/backend.md), 선언 둘(`siblings_axes.json`·`mechanism_models.json`)은 [CONFIG_GUIDE §1](./guide/CONFIG_GUIDE.md). ⏳ **P0-2-1 마킹 승격·P0-3 여정 대조·P0-4 탐색기는 설계 확정·미구현** — 정본 [SCENARIO_CONSOLE_BRIEF](./process/SCENARIO_CONSOLE_BRIEF.md), 기능으로 읽지 말 것 |
| **결함 종류 레지스트리** (2026-08-14 착지) | 🟢 저장·규칙 [architecture/data_model §1.2-bis.1](./architecture/data_model.md) · 선언 [guide/CONFIG_GUIDE §1 `finding_kinds.json`·§5.8-ter](./guide/CONFIG_GUIDE.md) · 점검 [qa/FEATURE_CHECKLIST §1.13 F1](./qa/FEATURE_CHECKLIST.md). **종류(`void`·`delam`)는 코드의 분기가 아니라 «조회»다**(`server/finding_kinds.py`) — 종류가 하나뿐이면 그 파라미터는 아무도 바꿔 본 적이 없고, 숨은 `WHERE finding_kind='void'`는 **두 번째 종류가 도착하는 날에만** 드러난다. 그래서 `delam_obs`(SCAT)가 있다. 🔴 **`kind_clean`은 「스캔됨 MINUS 발견됨」이지 `NOT EXISTS(finding)`가 아니다** — 실측으로 그 철자는 **한 번도 안 본 280,001개**를 「깨끗함」으로 옮긴다. ⚠️ **오늘 그 규칙의 철자는 «둘»이다**(참조 `finding_kinds.population_ctes` + 화면 경로 `ledger_siblings.py`의 자체 조립 — **시간 창** 때문에 갈렸고 분리에는 이유가 있다). **둘 다 규칙에는 동의하지만 갈라지는 날을 탐지하는 것이 없다.** 🔴 **`observed_by`가 분모의 정의이고 «부재 ≠ 빈 목록»**(빈 것은 「분모 없음」이라는 결정, 부재는 로드 시점 거절). ⚠️ **두 종류의 `method`가 달라야** 종류를 바꿨을 때 분모가 움직인다 |
| **void(보이드) 스키마** (2026-08-13 착지 · **2026-08-14부터 종류 둘 중 하나**) | 🟢 저장 [architecture/data_model §1.2-bis](./architecture/data_model.md) · 파서 [guide/INGESTION_GUIDE §1.11](./guide/INGESTION_GUIDE.md) · 선언 [guide/CONFIG_GUIDE §5.8-ter](./guide/CONFIG_GUIDE.md) · 켜는 순서 [process/OPERATOR_RUNBOOK §4](./process/OPERATOR_RUNBOOK.md). 🔴 **분모(`inspection_run`)가 자기 행을 갖는 것이 이 설계의 전부다** — 없으면 「보이드 0」과 「스캔 안 함」이 같은 부재이고 둘 다 깨끗하게 읽힌다. 🔴 **등급은 저장되지 않고 이제 저장될 수도 없으며**(해당 컬럼 0개) 면적도 컬럼이 아니라 **식 인덱스**다. ⚠️ **아직 `bonding_log`와 조인되지 않는다**(웨이퍼 신원 ↔ 카세트 위치) — 2주차를 막고 원장 슬라이스 1은 막지 않는다. ⚠️ **진짜 SAT 파일을 한 번도 보지 못했고** 런 메타 헤더 블록은 지어낸 것이다 |
| HTML 토폴로지 파서 | 🟢 [guide/HTML_TOPOLOGY_PARSER_GUIDE.md](./guide/HTML_TOPOLOGY_PARSER_GUIDE.md) |
| 공정열 비교 알고리즘 | 🟠 [spec/PROCESS_SEQUENCE_COMPARISON_ALGORITHM.md](./spec/PROCESS_SEQUENCE_COMPARISON_ALGORITHM.md) — route master 없이 실제 이벤트에서 순서 변경·반복·기록 없음·미실시·스키마 분기를 구분한다. N-way 압축, 원인 후보, 결측 정보이득 액션과 UI 표시 계약을 정의한다. |
| R&D Surprise 조사 알고리즘 | 🟢 [spec/RND_SURPRISE_INVESTIGATION_ALGORITHM.md](./spec/RND_SURPRISE_INVESTIGATION_ALGORITHM.md) — `Base WF × BONDING LEG` 분석 단위, Source→Ledger→Ontology, Surprise 점수, 물리 관문, Meta Action, 시공간 역마킹의 구현 계약. |
| 배치 업서트 | 🟠 [spec/batch_update_technical_specification.md](./spec/batch_update_technical_specification.md) — **알고리즘의 *모양*을 읽는 문서이고 서술 정본은 [architecture/backend §3](./architecture/backend.md)이다.** 🔴 **[2026-08-12] §3.3의 두 `bulk_upsert_*` 코드 블록은 *보내는 방식*에 대해 낡았다** — PostgreSQL+psycopg2에서는 청크마다 **다중 행 `VALUES` 문장 하나**가 나가고(`_pg_multirow_upsert`), 그 빠른 길은 **닫힌 어휘 넷**으로 **거절**할 수 있으며 거절은 `(테이블, 사유)`당 한 번 로그를 남긴다. 그리고 블록 안의 `deduped` 사전은 이제 **하중을 받는 전제조건**이다(중복 충돌 키 = SQLSTATE 21000, `IntegrityError` 아님 → 아무도 안 잡는다) |
| 실시간 동기화 | 🟢 **현행 정본은 [architecture/frontend §3·§3.1](./architecture/frontend.md)** — 모듈 구조 + **무결성 3문제**(중복 행·늦은 응답 오염·`total` 드리프트). 🗄️ **[2026-08-06 아카이브 완료]** [_archive/DATA_SYNC_SPEC.md](./_archive/DATA_SYNC_SPEC.md) — 폐기된 PySide6 클라 기준이고 §3 문제 서술도 2026-07-27 이관 완료라 **남은 유효 내용 0**. 🔴 **조건은 「SSOT 링크 정리 후」였고 그 조건은 이미 충족돼 있었는데 아무도 안 봤습니다** — 실측 결과 SSOT의 참조는 **0건**이었습니다. **조건부 대기는 조건을 다시 재는 사람이 없으면 영구 대기가 됩니다.** 인용하지 말 것 |
| 실패 관리/재시도 | 🟢 [spec/FAILURE_MANAGEMENT_SPEC.md](./spec/FAILURE_MANAGEMENT_SPEC.md) |
| 비즈니스 로직/레이어링 | 🗄️ **[2026-08-06 아카이브]** [_archive/BUSINESS_LOGIC_SPEC.md](./_archive/BUSINESS_LOGIC_SPEC.md) — 서술의 중심인 `update_cell`·`upsert_row`가 **`server/` 어디에도 없습니다.** 현행 정본은 [architecture/data_model](./architecture/data_model.md)(레이어링)과 [spec/batch_update_technical_specification](./spec/batch_update_technical_specification.md)(쓰기 경로) |
| Enrichment Queue(결손 보정 워크리스트) | 🟢 [spec/ENRICHMENT_QUEUE_SPEC.md](./spec/ENRICHMENT_QUEUE_SPEC.md) |
| 온톨로지 지식그래프(LLM 백본) | 🗄️ **부분 대체** [spec/ONTOLOGY_GRAPH_SPEC.md](./spec/ONTOLOGY_GRAPH_SPEC.md) — 🔴 **[2026-08-14 `2ec78b9` · R-2026-08-14-H] 「G1+뷰어+G2 라이브 가동」은 «거짓이 됐다».** 사본을 만들던 파이프라인이 은퇴했다: 워커가 스택에서 빠지고, 저장소 셋이 **DROP**됐으며, 라우트 일곱이 **410**을 답한다. **구현을 서술하는 절(§2 저장소·§4 엣지 스토어·§5 처리량·§7.5d chip-trace·§7.5e 재동기화/고아 스윕/mapping-summary·§8 단계 G1·G2)은 죽은 서술**이고, **미구현 설계(§0 핵심가치·§7 불량 추론망·§7.5/§7.5b 시공간 위상·§7.5c `node_class`·§7.6·§7.7)만 살아 있다.** 🔴 **원장은 이 문서의 어휘를 «쓰지 않는다»** — 자기 어휘를 [CANONICAL_LEDGER_DESIGN §4.2](./architecture/CANONICAL_LEDGER_DESIGN.md)에 따로 갖고 있고(SEMI E90/E142 정박), `node_class`·`label`·`ontology_mapping.json`을 한 번도 참조하지 않는다. 후계: 인스턴스 혈통 `GET /api/ledger/trace` · 유형 구조 `GET /api/ledger/structure`. **아카이브로 옮기지 않는 이유**는 살아 있는 미구현 설계가 다른 거처가 없기 때문이다.<br>~~아래는 은퇴 전 서술~~: **§3 매핑 예제는 「셀 체인」이 정본**(2026-07-30 `aea4700` — `CoreCell(core_lot,core_slot,cx,cy)`가 두 로그의 행 노드 · `BONDED_TO→BaseCell` · `TRANSFERRED_TO→DtCell` · `FROM_CORE→Core` · **좌표는 엣지 props가 아니라 identity 안에**. 🗄️ 폐기 형태 `Chip`/`log_id`/`BONDED_FROM→Wafer`/`PLACED_ON→Base`) · **§7.5d 칩 추적 API**(`GET /graph/chip-trace` — depth 없는 고정 형상, **다리별 닫힌 어휘로 빈 홉을 금지**: `mapping_unavailable`("읽지 못했다" ≠ "옮겨갔다")·`not_reached`("묻지 않았다")) · **§7.5e 선언 변경의 전파와 잔여물**(재동기화가 `SYSTEM_RELOAD`를 직접 발행 · 고아 스윕은 **깨끗하지 않은 선언 앞에서 전체를 거절** · `mapping-summary`의 `rejected[]`). 승격 흐름 요약: [event_driven_backend §4](./architecture/event_driven_backend.md) |
| 추적 시뮬레이션 픽스처(합성 데이터 규격) | 🟠 [spec/TRACE_FIXTURE_SPEC.md](./spec/TRACE_FIXTURE_SPEC.md) — **작성 중이고 채우는 주체가 사용자**다(총괄 검수 → server-pm 생성). 온톨로지 순환의 **④(발견 → 다시 보강)**을 한 바퀴 완주시키기 위한 데이터로, **정체성(§1)·불완전성(§6)·정답(§7)** 셋이 빠지면 그 증명이 성립하지 않는다 — **완벽한 데이터는 교정할 것이 없고**, 정답 없이 만든 trace 결과는 채점할 방법이 없다. ⚠️ 2026-08-04까지 이 표와 [DOC_OWNERSHIP](./process/DOC_OWNERSHIP.md) **양쪽에 행이 없는 고아**였다.<br>🔴 **[2026-08-14 정정] 「데이터를 만드는 코드는 아직 없다」는 서술은 거짓이다** — `server/scripts/`에 **생성기 여섯 + 정답지 하나**가 이미 산다(전수 목록은 [DOC_OWNERSHIP](./process/DOC_OWNERSHIP.md)의 그 행). 가장 최근이 `seed_syn_world.py`(1,157줄)이고, 그것이 3축 맵의 **코어축을 처음 그릴 수 있게** 만들었다([backend §2 `lot_map`](./architecture/backend.md)). ⏳ **Status가 🟠로 남아 있는 이유는 코드가 아니라 «폼»이다** — 스펙 §1~§8의 기입란이 아직 절반 비어 있고, 🟢 승격은 총괄 판정이다 |
| API 레퍼런스 | 🟠 [spec/api_documentation.md](./spec/api_documentation.md) — **행·셀 쓰기 계약의 상세 레퍼런스**(전체 라우트 지도는 [backend §2](./architecture/backend.md)가 정본). 2026-07-31 추가: **§2.1 가상 조인 컬럼 쓰기 400**(배치 단위 거부라 한 셀이 붙여넣기 전체를 잃게 한다) · **§5.2 `/tables/{t}/schema`**(`virtual_columns`) · **§6 소급 적용 어드민 3라우트**. ⚠️ 본문은 이 파일만 영어다 |

## ✅ 3.5 QA (qa/)

| 문서 | 내용 |
|---|---|
| 🟢 [qa/FEATURE_CHECKLIST.md](./qa/FEATURE_CHECKLIST.md) | **기능 인벤토리 + QA 수동 점검 체크리스트** — 서브시스템별 기능 지도·진입 경로·릴리스 전 회귀 점검 절차(SLO·멱등성 포함). **§2.0 자동 게이트**(pytest + `npm run build` — 손으로 점검하기 전에 통과시킬 것) · **§1.11/§2.15 운영 감시**(감시자·`/health`·격리 환경) 포함. **2026-07-31 신설 2행** — §1.1 **가상 조인 컬럼이 화면에 뜬다**(+ §2.2-bis B파트 · 🔴 **그리는 순간 쓰기 대상**이므로 붙여넣기·delete·일괄채우기가 각자 가드를 갖고 **복사는 반대**) · §1.8 **소급 적용 어드민 API**(+ §2.8-quinquies · ⚠️ **화면은 없다 — `curl` 점검**). **2026-07-30 신설 6행** — §1.1 **값 제안 셀 에디터**(+ §2.1에 F3 점검 11건 · ⚠️ `switchTable`이 `txModeActive`를 되켠다는 절차 경고) · §1.7 유효 다이 맵(M4) · §1.9 **칩 추적**·재동기화 알림·**고아 스윕** · §1.10 **셸의 서버 주소 해석**. 새 기능 병합 시 doc-keeper가 갱신 |

## 💡 3.6 제안·연구 (proposal/) — 미확정, 제품 스펙 아님

🔴 **이 표가 이 라운드(2026-08-11)까지 없었다** — `docs/proposal/`가 이미 7개 문서를 담고 있었는데 이 문서에도 [DOC_OWNERSHIP](./process/DOC_OWNERSHIP.md)에도 등재 행이 없어, 결정되지 않은 설계가 조용히 실행된 것으로 오독될 위험이 있었다. **이 폴더의 문서는 전부 "제안"이지 "정본"이 아니다** — 채택 여부는 총괄이 정하고, 채택되면 해당 서브시스템의 리빙 문서(architecture/guide/spec)로 내용이 옮겨간다. 이 폴더 자체가 SSOT가 되는 일은 없다.

| 문서 | 상태(문서 자체 표기) | 다룬다 |
|---|---|---|
| [proposal/FETCH_AND_AUDIT_HISTORY_PERFORMANCE_PROPOSAL.md](./proposal/FETCH_AND_AUDIT_HISTORY_PERFORMANCE_PROPOSAL.md) | 제안(미확정) | `CellSource` 쓰기 증폭 축소(제안 A/B, **미구현**) + 조회 경로 성능(제안 C). 🔴 **제안 C(행/셀 이력 keyset 페이징 + 전역 감사 화면 bounded scan)는 2026-08-11 `dab9152`+`2630790`+`fde424c`로 이미 구현·착지했다** — 이 문서는 그 실행의 설계 근거로만 남고, A/B는 여전히 미착수 |
| [proposal/UPSERT_THROUGHPUT_NEXT_STEP_PROPOSAL.md](./proposal/UPSERT_THROUGHPUT_NEXT_STEP_PROPOSAL.md) | 제안(미확정) | 대형 파일 인제션 `apply_batch_updates` 처리량 다음 단계. 검증됨: 미구현 |
| [proposal/SCHEMA_AND_ONTOLOGY_TRANSPORT_RESEARCH.md](./proposal/SCHEMA_AND_ONTOLOGY_TRANSPORT_RESEARCH.md) | 연구·제안(미확정) | 환경 간 스키마·온톨로지 정의 이행. 결론: 정의는 이미 사실상 버전 관리되고 있고(8/10 config가 `.sample`과 바이트 동일), 없는 것은 방향(어느 쪽이 상류인가)과 병합 규칙. `server/schema_drift.py`를 리포트에서 기대상태 계산기로 승격 권고 |
| [proposal/DT_ALIGNMENT_METADATA_CHAIN_PROPOSAL.md](./proposal/DT_ALIGNMENT_METADATA_CHAIN_PROPOSAL.md) | S1/S2 구현됨 · S3 제안 | DT 정렬 결과를 metadata로 적재하는 체인의 첫 단계. S1/S2는 [DT/Core 프레임 파생 체인](./architecture/DT_CORE_FRAME_CHAINS.md)으로 착지(DOC_OWNERSHIP 행 참조) |
| [proposal/DT_STANDARD_MAP_CHAIN_PROPOSAL.md](./proposal/DT_STANDARD_MAP_CHAIN_PROPOSAL.md) | 승인된 구현 방향(운영 검증 전까지 proposal 유지) | DT frame → 표준 DT map 체인. 같은 파생 체인 행 참조 |
| [proposal/DT_TRANSFORMATION_CHAIN_PROPOSAL.md](./proposal/DT_TRANSFORMATION_CHAIN_PROPOSAL.md) | 제안 | DT–Core 표준 좌표 변환 체인 — 원본 행을 바꾸는 권한은 이 제안이 주지 않는다는 전제를 못박음 |
| [proposal/CORE_ALIGNMENT_FINGERPRINT_PROPOSAL.md](./proposal/CORE_ALIGNMENT_FINGERPRINT_PROPOSAL.md) | 제안 | Core 정렬 지문 — 이 문서가 프로덕션 core-frame 확정이나 canonical core-map 쓰기를 켜지 않는다고 스스로 명시 |

> **DT 관련 넷(맨 아래 넷)은 [DT/Core 프레임 파생 체인 행](./process/DOC_OWNERSHIP.md)의 설계 배경 문서다** — 구현이 착지한 뒤에도 "왜 이렇게 설계했나"의 기록으로 남기고, 최신 상태 서술은 옮기지 않는다(정본은 architecture/guide/spec 셋).

## 🛠️ 4. 운영 & 셋업 (guide/)

| 문서 | 내용 |
|---|---|
| 🟢 [DEPLOY_SETUP.md](./guide/DEPLOY_SETUP.md) | **"내가 무엇을 채워야 하는가"** — 새 환경 배포 요약(제품 소유 vs 현장 소유, 제품 테이블 설치 스크립트, **격리 개발·검증 환경 §5**, 기동 후 `/health` 확인). 🔴 **§1-4 `ASSY_ADMIN_TOKEN`은 `[Environment]::SetEnvironmentVariable(…, "User")`로 영구 설정**한다 — `$env:`/`set`/`export`는 **셸이 닫히면 사라져** 잠기지 않은 서버를 만든다. 세우기 전에 **기존 값을 두 범위 모두 읽을 것**(추가가 아니라 **교체**다). **§1-5 프록시 함정**: loopback 통지가 사내 프록시로 나가 403이 되던 사고 — 코드는 `trust_env=False`로 막혀 있고, 🔴 **`NO_PROXY` 환경변수는 처방이 아니다**(트리 전체가 프록시를 못 타게 되어 자동 업데이트가 죽는다) |
| 🟢 [ROLLBACK_PROCEDURE.md](./guide/ROLLBACK_PROCEDURE.md) | **배포를 되돌리는 법** — 코드·config·스키마의 반영 시점이 다르다는 전제 위의 절차. 순서(`config → 코드 → 재기동`)·재기동 대상 5개·`/health`의 사각·**남는 물리 스키마**. 2026-07-28 격리 스택 드릴 실측 포함 |
| 🟢 [BACKFILL_GUIDE.md](./guide/BACKFILL_GUIDE.md) | **소급(backfill) 적용 — 규칙을 「이미 쌓인 데이터」에 적용하는 길들**(2026-07-31 신설). 규칙은 **증분(outbox) 구동**이라 자기가 선언된 뒤에 바뀐 행만 본다 — 규칙을 고쳐도 과거는 옛 규칙이 남긴 그대로다. **§0 증상→도구 결정표부터** 보라(도구 이름에서 출발하면 ⓒ/ⓓ를 반드시 헷갈린다: ⓒ `backfill_enrichment`는 **없던 파생 행을 만들고**, ⓓ `confirm`은 **있는 행의 빈 칸을 채운다** — 잘못 고르면 **에러 없이 아무 일도 안 일어난다**). ⓐ R1 재적용 / ⓑ R2 소스 철회 / ⓒ / ⓓ / ⓔ 그래프 고아 스윕 / **ⓕ R3 표시값 재계산**(2026-08-11 신설 · §2.5). 🔴 **R1과 R2를 가르는 문장**: 「룰이 여기서 더는 값을 안 만든다」와 「값이 비었다」는 **다른 진술**이고 앞엣것은 R2만 표현한다 → R1은 **절대 공백을 쓰지 않고** 철회 후보로 보고만 한다. 🔴 **R2와 R3를 가르는 문장**: 「그 층이 **틀렸다**」와 「그 층이 **지고 있다**」도 다른 진술이고, ⓕ는 **아무것도 지우지 않고** 저장된 층들로 승자만 다시 계산한다(층이 2개 미만인 칸은 구조적으로 손대지 않는다). **§4.1 「confirm을 돌렸는데 아무 일도 안 일어났다」의 세 원인**(`auto_confirm` 기본 OFF 포함) · **§6 함정**(`--limit`이 도구마다 뜻이 다르다 · ⓔ만 페이지 커밋이 아니다 · ⓔ의 종료 코드 3 · `reapply_chain.py`는 **삭제됨** · **§6.6 R2 카운트의 인덱스**) · **§7 어드민 API 3라우트**(2026-07-31 — ⚠️ **화면의 버튼은 `77d27d3` 기준 아직 없어 `curl`이다**(작업 진행 중). 건수는 **`count_kind`를 함께** 답하고 넷은 요청 경로에서 정확할 수 없다. 실행은 큐에 넣고 즉시 반환. 🔴 **ⓕ는 이 표면에 없다 — CLI 전용**) |
| 🟢 [process/OPERATOR_RUNBOOK.md](./process/OPERATOR_RUNBOOK.md) | **운영에서 «실제로 돌릴 것»의 단일 목록**(2026-08-13 신설 · **총괄이 쓰고 대표님이 돌린다**). 항목마다 기록 시간 · 목적 · 방법 · 선행 · 되돌리기. 🔴 **끝난 항목은 지우지 않고 「✅ 완료」로 표시한다** — 빈 목록과 완료된 목록이 같아 보이면 안 된다. 🔴 **에이전트는 운영 DB에 닿지 않고, 이 박스의 실측은 운영의 증거가 아니다** — 그래서 마이그레이션들이 **스스로 거절**하도록 만들어져 있다. ⚠️ **`.sql`은 잘못된 «서버»를 거절하지 못한다**(내용만 지킨다) — 돌리기 전 `SELECT current_database(), current_user, inet_server_addr();`. 순서가 걸린 항목은 「선행」에 적혀 있고 **건너뛰면 조용히 틀린다** |
| 🟢 **[ONTOLOGY_LEDGER_SETUP.md](./guide/ONTOLOGY_LEDGER_SETUP.md)** | 🧱 **원장·온톨로지 셋업 — 「무엇을 어느 순서로 선언해야 도는가」의 단일 정본**(2026-08-15 통폐합, 소유자 지시). **§1 선언 표 전수**(파일마다 config인가 코드인가 · 없으면 무엇이 일어나는가 · `.sample` 폴백 유무) · **§2 순서**(시간대·`tzdata` → 선행 테이블 → 마이그레이션 둘 → 소스 선언 → 어휘 → 백필 → 화면 층 → 검증) · **§3 `ledger_config.json` 키 표 «넷»**(문법 `lineage`·`observation`·`transfer`·**`declared`** — 🔴 **넷째는 베낄 파이썬 번역기가 «없다»**: 행→원자 사상이 `emit` 선언 그 자체라 새 모양의 테이블에 **코드 0줄**이다, 2026-08-15 3차 · §3.4) · §4 어휘는 **층에 따라 갈린다**(⚠️ **2026-08-15 2차부터 「어휘는 코드다」는 절반만 참** — 온톨로지 층 술어는 `ledger_vocabulary.json`으로 선언하고, **정준 층과 개체 타입만** 코드 + 판정이다) · §5 결함 종류(코드 기본값 + JSON 덮어쓰기) · §6 화면 층 셋(`mechanism_models`·`siblings_axes`·`ledger_journey`) · 🚫 §7 `ontology_mapping.json`은 쓰지 마라 · 🔴 **§8 실물 대 제안**(제안을 단계로 적은 셋업 문서는 없는 것보다 나쁘다) · 🔴 **§9 완성 조건 미달 자리**. **여기 코드 절차는 없다** — 번역기를 «쓰는» 법은 아래 LEDGER_GUIDE |
| 🟢 [LEDGER_GUIDE.md](./guide/LEDGER_GUIDE.md) | **정준 원장 — 번역기 쓰는 법 · 숫자 읽는 법**(2026-08-13 신설 · ⚠️ **2026-08-15부터 「선언·순서」는 이 문서가 아니라 바로 위 ONTOLOGY_LEDGER_SETUP이 소유한다**). 독자 둘: **번역기를 붙이려는 개발자**와 **백필을 돌리고 숫자를 읽어야 하는 운영자**. 🔴 **§3이 이 문서의 존재 이유**(새 소스 6단계 — 어휘 3문 검사 → 선언 한 장 → 번역기 → **`#<derivation>` 접미와 «예상된 빨강»**(새 파생은 스위트를 빨갛게 만든다, **그것이 기능이다**) → **픽스처는 `envelope.entity_ref`를 «불러» 만든다**(`01452d5` — 모든 테스트가 초록인 채 통합이 끝에서 끝까지 깨져 있었다) → **멱등성은 그물 «둘»을 각각** 증명). §4 운영자 절: **마이그레이션 «둘»**(2026-08-13 `0198e7e`부터 — `add_ledger_events.py` + `add_ledger_refusal_reasons.py`) · 백필 플래그 · **재실행이 0을 쓰는 이유**(⚠️ **선언을 바꾸면 해시가 움직여 재실행이 dedupe가 아니라 «중복 적재»가 된다**) · 커서/박동 숫자 읽는 법(🔴 **`molecules_done`은 거절된 분자를 «포함»한다** · 🔴 **`atoms_lost`는 「얼마나 잃었나」가 아니다** · 🔴 **거절은 커서를 세우지 않는다** · 🔴 **모든 카운터가 「이 커서 행이 사는 동안」의 누적이지 「이번 런」이 아니다**). 🔴 **안 돌아도 아무것도 안 깨진다** — 부팅 시 `server/ledger`를 import하는 프로세스가 없다(**[2026-08-14 2차] 구조 뷰가 어휘를 «지연» import해 이 문장을 글자 그대로 유지한다**). **§4.6-bis 신설 — 구조 뷰 읽는 법**: 상태 낱말 다섯(`flowing`/`declared_only`/`undeclared`/`unmeasured`/`declared_unconsumed`) · 🔴 **`atoms: 0` ≠ `atoms: null`** · 🔴 **창은 건수만 좁힌다** · ~~⚠️ 결함 관측은 이 그래프에 노드가 없다(원장 밖)~~ → **[2026-08-14 3차] 거짓이 됐다**(아래). 🔴 **[2026-08-14 3차] §3이 이제 «문법 둘»을 가르친다** — 소스 선언의 `kind`(`lineage` 기본 \| **`observation`**)가 절차를 가르고, 관측 문법은 **§3 ②-bis**가 소유한다(한 행 = 한 발화 · 런 조인에서 시각·분모를 읽고 **못 풀면 거절** · 커서가 **키셋**). §4.2에 `--source void_obs` / `delam_obs`와 키셋 `--from` 철자, **§4.6-ter 신설**(`/kinds`의 원장 위치 필드 다섯 읽는 법), §4.7에 **관측 롤백 술어 둘**. ⚠️ **순서는 [OPERATOR_RUNBOOK](./process/OPERATOR_RUNBOOK.md)이 소유하고 이 문서는 복제하지 않는다** |
| 🟢 [process/PRODUCTION_READINESS.md](./process/PRODUCTION_READINESS.md) | **프로덕션 게이트** — 무엇이 아직 막고 있는가(차단/조건부/통과 + 근거). 배포 판단은 여기서 |
| 🟢 [CONFIG_GUIDE.md](./guide/CONFIG_GUIDE.md) | **설정 전수 지도** — `server/config/*` 파일별 목적·소유·리로드 방식, 시나리오별 온보딩 체크리스트, 핫리로드/검증 규율, 함정 모음 |
| 🟢 [CONFIG_ROLLOUT_GUIDE.md](./guide/CONFIG_ROLLOUT_GUIDE.md) | **선언 한 벌을 운영에 올리는 순서 런북**(2026-08-02 신설 · 사용자 요청 「운영에서 보고 따라하게」). 저쪽이 **목록**이라면 이쪽은 **순서와 조용히 안 먹는 방식**이다: ① table_config → ② 맵 바인딩 → ③ 체인 → ④ enrichment → ⑤ 가상 조인 → ⑥ 온톨로지, 각 단계의 **최소 선언 · 먹었는지 확인하는 명령 · 안 먹었을 때의 모양**. 🔴 **§4의 셋이 이 문서의 존재 이유**: ① 규칙은 **디스크에서 유효하면서 워커 안에서는 죽어 있을 수 있다**(체인 워커가 자기 `TABLE_CONFIG`보다 먼저 규칙을 읽고 거부한 뒤 **재시도하지 않는다** — 확인은 응답이 아니라 `chain_worker.log`의 `Synthesized N`) ② **`/schema`의 200은 증거가 아니다**(config 싱글턴을 읽는다 — `/tables/<t>/data`와 `information_schema`로 볼 것) ③ **선언을 지우면 그것을 가리키던 다른 선언이 조용히 반쯤 돈다**(그중 `bonding_plan_config`·`transfer_plan_config`는 **검증하는 코드가 아예 없어** 아무 데도 안 뜬다). ⑤는 **선언보다 인덱스가 먼저**다 — 조인 키를 덮는 UNIQUE 인덱스가 없으면 거부되고 거부가 실행할 DDL을 준다. ⏳ **§6에 미완 4절**(`auto_update_control`·`maps`·`bonding_plan_config`·`transfer_plan_config` — **샘플이 실 파일보다 낡아** 옮겨 적을 수 없다. 🔴 `auto_update_control`이 빠진 동안에는 이 문서만으로 「데이터를 계속 만드는 상태」를 재현할 수 없다 — **선언 자체는 아래 `config_reference/`에서 읽을 수 있다**) |
| 🟠 [config_reference/](./guide/config_reference/README.md) | **이 환경의 실 config 사본**(2026-08-02 신설 · 스냅샷 2026-08-01) — `.sample`이 *형태*라면 이쪽은 **실제로 돌아가고 있는 선언 전체**다(자격증명·호스트·절대경로는 복사 전 제외). 🔴 **배포물이 아니다** — 현장마다 이름이 다르므로 복사해 덮으면 그쪽 선언이 사라진다. **절차는 CONFIG_ROLLOUT_GUIDE, 선언은 여기**. 🔴 **[2026-08-04] `transfer_plan_config.json`·`bonding_plan_config.json` 사본은 지금 이 환경에서 *해석되지 않는다*** — 형태가 낡은 것에 더해 존재하지 않는 테이블·틀린 컬럼명을 가리킨다. **이 사본의 일반명(`"x": "x"`)을 옮겨 적은 것이 2026-08-04 라이브 사고의 원인이었다**(그 README 헤더의 「알려진 고장」 · 계획 config는 [config/transfer_plan_config](./guide/config/transfer_plan_config.md)에서 배울 것 — 그쪽 예시는 dry-run으로 수용을 실측했다). 🔴 **사본이라 조용히 낡는다** — 2026-08-04에 **책임자(Lead / Backend)와 갱신 트리거**(`.sample` 변경 · config 추가·폐지 · 키 변경, 그리고 정비 사이클마다 1회)를 그 README 헤더에 못박았다. 갱신은 **다시 복사**하는 것이지 손으로 고치는 것이 아니다 |
| 🟢 [config/](./guide/config/README.md) | **파일별 세팅 절차** — config 파일당 가이드 1개(언제 만지나·세팅 절차·반영 확인·복구·키 사전). 운영 서버에서 실제로 세팅할 때는 여기부터 |
| 🟢 [CONDA_SETUP_GUIDE.md](./guide/CONDA_SETUP_GUIDE.md) | Conda 환경 구성 |
| 🟢 [NATIVE_POSTGRES_SETUP_GUIDE.md](./guide/NATIVE_POSTGRES_SETUP_GUIDE.md) | PostgreSQL 설치 |
| 🟢 [POSTGRES_OPERATIONS_GUIDE.md](./guide/POSTGRES_OPERATIONS_GUIDE.md) | DB 운영 · **§3.1 「읽기 전용 가드」**(2026-08-13 `1260c9b` — 운영 DB를 가리키는 사전점검이 «못 쓴다»는 것을 **PostgreSQL이 되읽어 증명**한다. 집은 `server/db_safety.py` 하나이고 **모드가 둘**이다 — 🔴 스크립트 안에서 다시 철자하지 말 것) · **§3.1 인덱스 계열**(재교정률 · 상호작용 점수 · 값 제안 접두 · **R2 회수 범위 `idx_sources_by_source`** 2026-07-31 · **업무 키 UNIQUE `uq_bk_*` + PK 복제 인덱스 회수** 2026-08-07 D3). 🔴 **`create_all`은 이미 있는 테이블에 인덱스를 추가하지 않는다** — 운영 DB 반영 경로는 **둘**이다: `scripts/setup_db_performance.py`(성능 인덱스)와 `migrations/add_business_key_unique_index.py`(업무 키 UNIQUE · PK 복제 회수) |
| 🟠 [SERVER_STARTUP_GUIDE.md](./guide/SERVER_STARTUP_GUIDE.md) | **성능 튜닝 관점**(인덱스·트라이그램·`work_mem`). 🔴 **기동 절차의 정본이 아니다** — 그쪽은 [SYSTEM_OVERVIEW §7](./overview/SYSTEM_OVERVIEW.md)이다. ⚠️ 2026-08-04까지 **존재하지 않는 명령 셋**을 인쇄하고 있었다(`requirements.txt`·`scripts/setup_db_performance.py`·`uvicorn --port 8000`) — 전부 정정 |
| 🟢 [data_preservation_and_signature_change.md](./guide/data_preservation_and_signature_change.md) | 시그니처 변경·병합 보존 규율(SOP 필독) |
| 🟢 [spec/DEBUGGING_GUIDE.md](./spec/DEBUGGING_GUIDE.md) | **[2026-08-06 분할] 이제 §0만 남습니다** — §1–§4(PySide6 시대)는 [_archive/DEBUGGING_GUIDE_pyside6_era.md](./_archive/DEBUGGING_GUIDE_pyside6_era.md)로. **§0 읽기 전용 진단 도구 셋**(2026-08-04 신설, 2026-08-11 세 번째 추가) — `diagnose_socket.py`(「소켓이 안 된다」의 **다섯 원인**을 가른다. 검사는 전부 **raw 소켓** — 사내 프록시에 속지도 바꾸지도 않는다) · `diagnose_db_health.py`(「예전보다 느려졌다」 = 긴 트랜잭션 → 블로트 → 스캔 비용. 세션에 `transaction_read_only`를 박고, **앱이 해석한 접속 URL**을 쓴다) · `diagnose_slow_after_ingest.py`(「대량 인제션 직후 느려졌다」 = 스레드 토큰/DB 커넥션/쿼리 자체 사다리, 운영 박스가 출력 복사를 금지해 **결론이 한 줄**로 끝난다) |

## 📜 5. 이력 & 개발 체계

| 문서 | 내용 |
|---|---|
| 🟢 [history/README.md](./history/README.md) | **자동 생성** 이력 인덱스. `python docs/history/gen_index.py`로 갱신(직접 편집 금지) |
| 🟢 [process/PRODUCTION_READINESS.md](./process/PRODUCTION_READINESS.md) | 프로덕션 게이트 — 차단/조건부/통과 판정과 근거 |
| 🟢 [process/CONTRIBUTING.md](./process/CONTRIBUTING.md) | 문서 갱신 규율 |
| 🟢 [process/DOC_OWNERSHIP.md](./process/DOC_OWNERSHIP.md) | 서브시스템 ↔ 문서 소유 매핑 |
| 🟢 [process/LEDGER_RULINGS.md](./process/LEDGER_RULINGS.md) | **원장·온톨로지 판정 대장**(2026-08-13 신설 · 온톨로지 포크 소유) — 🔴 **판정의 «배달 경로»다. 여기 없는 판정은 내려진 적이 없는 것으로 친다.** 세션 간 메시지 채널이 하루에 세 건을 조용히 유실한 것이 grep으로 실증돼(보내는 쪽은 성공과 유실을 구별할 수 없다) 판정이 파일로 옮겨졌다 — **커밋은 트리를 공유하므로 잃을 수 없다** |
| ⚪ [process/LEDGER_SLICE_1_BRIEF.md](./process/LEDGER_SLICE_1_BRIEF.md) | 원장 슬라이스 1 착수 지시서 — 착지한 구현의 **근거 문서**이지 현행 서술이 아니다(현행은 위 서브시스템 행의 리빙 문서 셋) |
| 🟢 [process/RELEASE_LOG.md](./process/RELEASE_LOG.md) | 릴리스 요약(Phase 번호 대체) |
| 🟢 [process/agentic_environment.md](./process/agentic_environment.md) | 멀티 에이전트 협업 체계 — 총괄 + 도메인/문서/검수 에이전트. **명단의 정본은 `.claude/agents/`이고 여기에 수를 적지 않는다**(종전 「총괄 + 2 PM」이 넉 달간 낡아 있었다) |
| 🟢 [prompts/starting_prompt.md](./prompts/starting_prompt.md) | 총괄 PM 작업 헌장(SOP) + 조직 구조 |
| 🟢 [prompts/server_pm.md](./prompts/server_pm.md) | Server(백엔드) 도메인 PM 헌장 |
| 🟢 [prompts/client_pm.md](./prompts/client_pm.md) | Client(프론트엔드) 도메인 PM 헌장 |

## 🗄️ 6. 아카이브 (_archive/)

현실과 상충하게 되어 대체된 문서들로, **더 이상 유효하지 않습니다.** 히스토리 추적용으로만 보존됩니다: [_archive/](./_archive/) — 구 PySide6 시대 문서(`ASSY_MANAGER_BIBLE`, `TECHNICAL_GUIDE`, `ARCHITECTURE_ANALYSIS`, `CLIENT_FEATURE_CHECKLIST`) 및 `graph_db_integration_plan`(Kafka/Neo4j 구상 — ONTOLOGY_GRAPH_SPEC + PG materializer가 대체) 등.

---

*문서를 추가·변경할 때는 이 인덱스와 해당 문서의 Status 배지를 함께 갱신하십시오 → [process/CONTRIBUTING.md](./process/CONTRIBUTING.md)*
