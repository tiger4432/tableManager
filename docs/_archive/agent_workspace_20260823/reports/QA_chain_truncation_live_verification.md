# QA 라이브 검증: created_logs 절단 수정 (cc57b64) 재기동 후 실측

- **검증일시**: 2026-07-25 22:08 ~ 22:29 (KST)
- **대상**: 2026-07-25 21:29 인시던트 수정 — 체인 워커 통지 payload 500건 절단 + `total_log_count` (`server/chain_ingestion_worker.py:459-492`)
- **방법**: 코드 수정·재기동 없이 관찰만. 로그 grep + :8080 0.5초 간격 8분 폴링(952 샘플) + 읽기 전용 DB SELECT.

## 판정: **PASS**

인시던트의 3대 증상(notify 누적 지연 3422→21282ms·ok=False, 전 워커 read timeout 연쇄, 매 사이클 :8080 2s+ 스파이크)이 재기동 이후 **전부 소멸**. 단, 인시던트 원 규모(수만 건 로그의 대형 tx) 경로는 재기동 후 자연 발생하지 않아 라이브에서 미실측(하단 한계 참조).

## 수치 증거

### 1. chain_worker.log — notify 지연 정상화 (PASS)
- 마지막 "Failed to send API notification": **21:29:47** (인시던트 구간). 이후 22:29까지 재발 0건.
- 수정 반영 후(21:33~) [Latency] 라인: **ok=True 56건 / ok=False 0건** (테스트 오염 라인 `tx=tx1` 제외).
- 최종 재기동(22:07:20) 이후 notify_ms 분포: **n=33, min=0, p50=16, max=421, mean=38.8ms**. 300ms 초과는 2건(328/421ms)뿐이며 :8080 자체 재기동 창(22:09:39~44)과 겹침. 인시던트 당시 3,422~21,282ms 대비 2자릿수 ms로 복귀.

### 2. watcher.log — 타임아웃 연쇄 소멸 (PASS)
- 마지막 "Failed to send API notification": **21:29:52**. 이후 재발 0건 (로그 말단 22:29 기준).

### 3. :8080 응답성 폴링 — 사이클 스파이크 소멸 (PASS)
- 관찰창: 22:08:59 → 22:16:59, 0.5s 간격 **952 샘플**. auto-update 수집기는 1~2분 주기(auto_update.log)로 관찰창 내 인제션→체인→브로드캐스트 사이클 7회 이상 포함.
- 이상치 4건은 전부 22:09:39~22:10:04에 집중:
  - 22:09:39 / 22:09:41: 2038·2040ms **WinError 10061(연결 거부)** — 지연이 아니라 서버 다운. server.log가 22:09:39 "Client disconnected" → 22:09:44 기동 로그("Decoupled mode active")를 보임. 즉 사용자의 순차 재기동 중 :8080 프로세스 자신의 재기동 순간이며 이벤트 루프 동결 아님.
  - 22:09:43 1264ms / 22:10:04 575ms: 기동 직후 워밍업 + 재기동 후 첫 인제션 브로드캐스트.
- **정상 판정창(22:10:10 → 22:16:59, 819 샘플, 실패 0)**: min 3.3 / p50 6.0 / p95 28.3 / p99 29.5 / **max 283.5ms**. **1.5s 초과 스파이크 0건, 300ms 초과도 0건.** 인시던트 전 "매 사이클 2s+ 스파이크 상시 재발" 증상 소멸 확인.

### 4. 재기동 스윕 대형 tx 경로 (부분 실측)
- 22:07:20 재기동 직후 22:07:23 `Loaded 40 extra events to complete tx 'dc844f1d…' (Total size: 240)` — 재기동 캐치업으로 유입된 240건 tx.
- DB 확인(읽기 전용): 해당 tx 240행 전부 `processed_chain=true, status=SUCCESS`, 전체 outbox `processed_chain=false` 잔여 **0건** — 백로그 없음, 소비 정상.
- **한계**: 인시던트를 유발한 규모(수만 건 created_logs, ~50MB payload)의 tx는 관찰창 내 자연 발생하지 않아 500건 절단이 실제로 발동하는 순간의 라이브 실측은 없음. 절단 코드는 가동 중 빌드에 탑재 확인(`chain_ingestion_worker.py:463-472`, 직렬화 루프 앞 절단 + `total_log_count`). 절단 발동 시 로그 마커가 없어(무음 절단) 로그만으로는 발동 여부 관측 불가.

## 관찰 사항 (판정 외)

1. **테스트의 라이브 로그/인프라 오염**: pytest 실행 흔적(`tx=tx1` 워밍업, `chtrunc_test_target`, `enrich_test_derived`, `autoupd_test_tbl`, "no_such_module_xyz_123", "db down")이 21:43·21:49·21:59·22:05·22:11에 라이브 chain_worker.log/서버로 유입. 본 측정에서는 제외 처리했으나, 라이브 지표 오염 및 운영 로그 판독성 저하 요인. 테스트 로거의 파일 핸들러 분리 권장.
2. **무관 단발 오류**: watcher.log 21:48:04 `[wafer_process] UndefinedColumn: "lot_id"` (eqp_wafer_process_20260725_214801.csv 처리 실패, 1회) — 본 수정과 무관한 스키마 불일치. 별도 추적 권장.
3. 절단 발동 시 관측 가능한 1줄 로그(`truncated N->500`) 추가를 제안 — 무음 절단은 재발 시 진단을 늦춤.

## 원시 데이터
- 폴링 원시: 세션 스크래치패드 `poll_results.jsonl` (952행, 세션 종료 시 소멸 가능 — 핵심 수치는 본 문서에 보존).
