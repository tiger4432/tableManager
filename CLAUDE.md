# 🔴🔴 맨 먼저 — 「**나는 방금 깨어났을 수 있다**」 (모든 세션, 2026-08-30)

**컴팩트는 대화를 이어 보이게 만든다. 그래서 깨어난 세션은 자기가 깨어난 줄 모른다.**
이 절이 여기 있는 이유는 하나다 — **복구 절차를 보드에만 두면, 복구를 안 한 세션은
그 절차를 «영원히 못 본다».** 보드는 열어야 보이고, 이 파일은 «자동으로» 들어온다.

```
이 턴이 이 세션의 «첫 응답»이거나, 요약을 받아 이어가는 중이라면 -- 답하기 전에:
  ① docs/process/PROJECT_STATUS.md 의 «맨 위 절»을 읽는다  (감시 다섯 · 레인 분장 · 재기동)
  ② 감시가 몇 개 살아 있는지 «센다». TaskList 에는 «안 뜬다»
     -> 이벤트가 한 번도 안 온 감시는 죽은 것으로 보고 다시 건다
  ③ 열려 있는 채널을 본다: task/IMPLEMENTER_ORDERS.md · task/DESIGN_ORDERS.md
```
🔴 **일을 «시키는» 방식도 여기서 확인한다 — 서브에이전트가 아니라 «채널»이다.**
레인은 각자 세션으로 돌고, 지시는 위 채널 파일에 «쓰고 커밋»한다.
⚠️ 그리고 **커밋은 초인종이 아니다** — 멈춘 레인은 안 깨어난다. 소유자가 그 창에 한 글자 쳐야 한다.

## 🔴 레인(구현자·클라·응용)이라면 — 답하기 «전에» 셋 (소유자 상설, 2026-08-30)
```
① 자기 채널 파일의 «끝»을 읽는다
   자는 동안 판정이 올라와 있다. 당신이 물은 것의 답이 이미 거기 있을 수 있다
② 자기 세션에 «15분 지속 감시»를 건다
   그래야 채널에 글이 올라올 때 «스스로» 깨어난다. 안 걸면 소유자가 창을 두드려야 한다
③ 판정이 필요하면 «총괄 채널»로 쓴다. 🔴 소유자께 «직접» 묻지 않는다
   소유자는 당신이 무엇을 재 봤는지, 총괄이 무엇을 판정했는지 «맥락»을 안 들고 계신다
   소유자 판단이 필요한 것이면 «총괄이» 올린다
```
**왜 적혔나:** 2026-08-30 밤. 총괄의 판정이 «레인의 턴이 끝난 뒤»에 도착한 것이 «두 번»
(범위 컬럼 · apply). 레인은 답이 없다고 보고 «소유자께 직접» 물었고, 소유자가 그것을
중계하게 됐다. 그날 소유자가 레인 창을 «네 번» 두드렸다.
소유자: 「구현자 자꾸 나한테 이렇게 물어보는데 너랑만 말하라고해」.
🔴 질문 자체는 «옳았다» — 자리만 틀렸다. 그리고 ②를 걸었으면 그 자리도 안 틀렸다.

**왜 적혔나:** 2026-08-30, 컴팩트 직후 저는 기술 실타래만 이어받고 «운영 체계»를 통째로
잊었습니다. 보드 5~20행에 감시 다섯과 레인 분장이 「깨어난 총괄은 이 절만 읽어라」로 적혀
있었는데 **제가 그 절을 안 읽었습니다.** 그 결과 감시 둘이 죽은 채로 돌았고, 구현자 채널이
있는데 서브에이전트를 띄웠습니다. 소유자가 둘 다 잡았습니다 —
「감시 살리고」 · 「에이전트를 왜시킴 구현자 등등 있잖아」 · 「왜 컴팩트 하고나니 다 까먹었지」.

---

# 🔴🔴 역할 — 「**도메인을 아는 «사용자»가 자유롭게 세팅해 쓰는 온톨로지 툴**」 (소유자 2026-08-30)

> 「도메인 지식을 알고 있는 유저가 자유롭게 세팅해서 사용할 수 있는 온톨로지 툴을 만드는게 너의 역할이야」

**도메인 지식은 «내 안»에도 «소유자 머릿속»에도 있지 않다. 도구를 «쓰는 사람»에게 있다.**
내가 만드는 것은 지식이 아니라 **그 사람이 자기 지식을 «적을 수 있는 자리»**다.

## 그래서 아래가 «자동으로» 따라온다 — 따로 외울 규칙이 아니다
```
스키마로 도메인을 «추론»하지 않는다      추론이 필요하다는 것은 «적을 자리가 없다»는 뜻이다
코드에 도메인 낱말이 «없다»            void · defect · wafer 로 갈래를 트는 순간 사용자가 못 바꾼다
뷰를 «짜야» 하면 그것이 결함이다        사용자가 못 적어서 «내가 대신» 적은 것이다
소유자께 도메인을 여쭙는 것도 «최후»다   답이 선언 가능해야 하는 것이면 «그 자리»를 만드는 게 일이다
```

## 🔴 판별식 — 막혔을 때 «한 번만» 묻는다
```
「이 질문의 답을 «사용자가 적을 수» 있나」
   ✅ 예       -> 적을 자리를 만들고, 값은 «비워 둔다». 내가 채우지 않는다
   🔴 아니오  -> 그것이 이번 라운드의 «진짜 일»이다. 우회로(뷰·분기·상수)를 짜지 않는다
```
⚠️ 이 역할이 「완성의 정의」(바로 아래)의 «이유»다. 두 줄로 말해져야 하는 까닭은
   그 두 줄을 «읽을 사람»이 엔지니어가 아니라 **도메인을 아는 운영자**이기 때문이다.

---

# 🔴 판정은 «총괄»이 받는다 — 소유자께 올리는 것은 «원칙이 못 정하는 것»뿐 (소유자 2026-08-30)

> 「설계 원칙은 이제 충분히 쌓인것 같아서 내가 판정할건 없을것 같아」

```
✅ 총괄이 «판정»한다   이 파일의 원칙으로 갈리는 것 전부. 올리지 «않는다»
                    같은 것을 두 번 판정하지 않는다 — 이미 판정된 «부류»를 먼저 찾는다
🔴 올리는 것       우선순위 · 되돌릴 수 없는 것 · 바깥으로 나가는 것 · 소유자만 아는 사실
🔴 도메인 사실도 «먼저» 묻지 않는다
                    위 역할대로 «선언 가능하게 만드는 것»이 답이다.
                    그래도 남으면 그때 올린다 — 「무엇을 알려주면 판정되는지」를 «한 줄»로 적어서
```
**왜 적혔나:** 2026-08-30 하루에 제가 스키마로 도메인을 추론하다 «네 번» 틀렸고 네 번 다
소유자가 한 줄로 잡으셨습니다. 그날 밤 소유자가 두 문장으로 그 왕복을 끝냈습니다 —
판정은 총괄이 받고, 도메인은 «사용자가 적는다».

---

# 🔴 완성의 정의 — 「**임의의 스키마에서 «두 줄»로 선언법을 말할 수 있어야 한다**」 (소유자 2026-08-30)

> 「항상 설계할때 이 박스에서 선언 조작하고 해서 했다고 한게 아니고 운영에서 arbitrary한
> 스키마에서 어떻게 선언하면 되는지 2줄 이내로 설명할 수 있을 정도로 쉬워야 개발한것임」

**이 박스에서 «돌게 만든 것»은 완성이 아니다.** 완성은 다음 문장이 나오는 것이다:

```
「운영에서는 <이 표>에 <이 칸> 을 적으면 됩니다」    <- 두 줄 안. 스키마 이름은 «빈칸»
```

## 판별식 — 설계를 끝냈다고 말하기 «전»에 그 두 줄을 «써 본다»
```
✅ 통과   두 줄이 나오고, 그 안에 이 박스의 «컬럼 이름»이 안 들어간다
❌ 실패   「이 박스에서는 이렇게 하면 됩니다」로만 말할 수 있다
❌ 실패   두 줄이 안 되고 «절차»가 된다 (먼저 A 하고, B 를 만들고, C 를 맞추고 …)
❌ 실패   내가 이 박스 선언을 «손으로 맞춰서» 돌게 만들었다
```

## 🔴 이 규칙이 실제로 걸러 내는 것 — «뷰»가 약점이다
```
선언       두 줄로 말해진다. 스키마가 달라도 «칸 이름만» 바뀐다        ✅
뷰(SQL)    그 스키마에 대고 «짜는» 것이다. 두 줄로 안 말해진다        ⚠️
=> 「뷰를 하나 만들면 됩니다」는 통과처럼 들리지만 통과가 아니다.
   뷰가 필요하면 «왜 선언으로 안 되는지»를 같이 말해야 하고, 그게 설계의 빚이다
```

**왜 적혔나:** 2026-08-30 밤, 원장 여러 건을 「됩니다」로 보고했는데 그중 일부는
**이 박스의 컬럼 이름에 맞춰 제가 선언을 손보고 뷰를 짜서** 돈 것이었다. 같은 밤 소유자가
같은 병을 세 번 잡았다 — 「이 박스 얘기 금지」 · 「범용적으로 고려해라」 · 이 항목.

⚠️ 기존 DoD(「다른 스키마 운영 환경에서 코드 0줄, 선언 교체만으로 발화」, 2026-08-14)의
**조임쇠**다. 코드가 0줄이어도 선언이 서른 줄이면 그건 완성이 아니다.

---

---

# 🔴 시험 상설 — 「**잘라쓰기 하니스 «절대 금지»**」 (소유자 2026-09-02 밤)

> 「하네스 모두 임포트로 하기로 했는데 왜 이따위로 설계했아? 당장 리팩토링 다해」
> 「클로드.md에 박아 잘라쓰기 하네스 절대금지」

```
하니스·계약은 대상 모듈을 «import» 한다.  그것뿐이다
⛔ 파일을 «텍스트로 읽어» 함수 본문을 잘라내고 vm 에 넣는 것 — 금지
⛔ 정규식으로 상수·함수를 «뽑아» 쓰는 것 — 금지
```

## 왜 — 잘라쓰기는 «동작»이 아니라 «글자 모양»을 잽니다
```
import 를 하나 더하면        잘라낸 조각이 그 함수를 못 찾아 «던집니다»
const 를 하나 더하면         vm 에서 const 는 컨텍스트의 «속성»이 안 됩니다 -> 밖에선 undefined
함수가 헬퍼를 부르게 바뀌면    그 헬퍼가 「뽑을 목록」에 없어 «던집니다»
=> 전부 «코드가 맞는데» 빨개집니다. 그리고 반대도 참입니다 — 틀렸는데 초록일 수 있습니다
```
🔴 **그래서 이건 「하니스가 약하다」가 아니라 「하니스가 다른 것을 재고 있다」입니다.**

## 대상이 import 가 «안 되면» — 그것이 고쳐야 할 결함이다
```
❌ 하니스를 잘라쓰기로 우회한다        <- 금지. 결함을 시험 쪽으로 옮기는 것이다
✅ 재려는 로직을 «import 되는 모듈»로 뺀다
   그 모듈은 DOM 도 CSS 도 «모듈 최상단»에서 건드리지 않는다
```
실측 2026-09-02: `map_editor.js` 가 import 안 되는 이유는 «줄 수»가 아니라 `import './tokens.css'` 였다.
node 가 CSS 를 못 읽어 «import 문 자체»가 죽는다. 잘라쓰기 34건 중 «27건»이 이 파일 하나 때문이었다.

## 새 부품은 «처음부터» 이 규율로
오늘 뽑은 `truncation.js` · `match_count.js` · `dropdown.js` 가 그 예다 —
로직이 자기 모듈에 살고, 하니스가 그것을 import 한다. 그게 정본이다.

## ⚠️ 「덧붙이기」는 잘라쓰기가 «아니다» — 다만 «다리»다 (총괄 판정 2026-09-02 밤)
하니스가 대상의 «모듈 변수를 심어» 재야 할 때가 있다. `export` 로는 안 된다 —
ESM 네임스페이스는 봉인돼 있고 `export let` 은 밖에서 «읽기 전용»이다.
```
✅ 덧붙이기   원본 «전문» + 뒤에 접근자 블록 -> 그 사본을 import
              잘라내는 양이 «0» 이라 금지의 사유가 하나도 남지 않는다:
              import 도 const 도 헬퍼도 «파일 안에 그대로» 있다
⛔ 잘라쓰기   금지 그대로다. 사유는 위와 같다
```
🔴 **퇴화 방지 장치가 «하나» 있어야 한다 — 사본이 «원본 바이트로 시작»하는지 단언한다.**
그 단언이 없으면 덧붙이기는 조용히 잘라쓰기로 돌아간다. 그리고 사본 만들기는 «헬퍼 하나»가
하고 하니스는 «필요한 이름»만 선언한다 — 24군데가 각자 만들면 병이 이름만 바뀐 것이다.

⚠️ **도착지가 아니다.** 도착지는 위의 「재려는 로직을 import 되는 모듈로 뺀다」이고,
덧붙이기는 그 대공사를 오늘 안 하기 위한 다리다. 다리를 영구 건물로 읽지 말 것.

## ⚠️ 「텍스트가 «대상»인 하니스」는 이 규칙 밖이다 (총괄 판정 2026-09-03)
```
금지가 겨냥한 것   «동작»을 재려고 텍스트를 «대리»로 쓰는 것
                 -> 파일 모양이 바뀌면 «옳은 코드가 빨개진다». 그것이 병이다
예외             두 리비전의 «텍스트가 달라졌나»를 재는 드리프트 오라클
                 -> 여기서 「모양이 바뀌면 빨개진다」는 «병이 아니라 기능»이다
```
🔴 판별식: **텍스트가 «주어»인가, «대리»인가.** 주어면 밖이고, 대리면 금지다.
🔴 그리고 이 판별식은 **«단언» 단위다. «파일» 단위가 아니다.**
   한 파일이 「순서를 읽는 단언」과 「잘라내어 돌리는 단언」을 «같이» 가질 수 있다.
   파일을 통째로 예외로 읽으면 잘라쓰기가 그 안에 그대로 남는다 (2026-09-03 실측: valid_die_authoring).
   과거 리비전은 import 될 수 없다는 것은 «부수적»이다 — 되게 만들어도 이 예외는 그대로다.
   그리고 그런 하니스는 «양쪽을 같은 기제»로 둬야 한다. 한쪽만 import 로 바꾸면
   두 쪽이 «다른 질문»에 답하기 시작한다.

# 🔴 착수 전 관문 — 예외 없음

**소유자 상설 지시 (2026-08-19). 무엇을 하기 «전에» 아래 셋을 검토하고 진행한다.**

> 「모든 개발은 최소 수정, 핵심 기능만 개발, 보안 등 부수적인건 차근차근」
> 「그냥 뭐든 하기 전에 최소 수정, 단순 로직 이 두항목 검토하고 진행해」
> 「무분별한 기능추가 절대 금지」

### ① 최소 수정 — *Minimal edit*

바뀌는 층만 바꾼다. 주변 인터페이스·호출자·이름은 그대로 둔다.
막힌 자리 하나를 리팩터링·재설계 라운드로 키우지 않는다.

### ② 단순 로직 — *Simple logic*

가장 단순하게 도는 것을 고른다. 지금 필요 없는 일반화·추상·설정 축을 만들지 않는다.

### ③ 무분별한 기능 추가 절대 금지 — *Never add what was not asked for*

**지시받지 않은 것은 만들지 않는다.** 가드·테스트·불변식·헬퍼·「나중을 위한」 훅 전부 포함.
필요해 보이면 **만들지 말고 말하고 기다린다.**

### 🔴 세 항목은 «코드» 관점이다 — 요구사항을 자르는 근거가 아니다

**소유자 정정 (2026-08-19): 「최소수정 및 단순화는 코드 관점이야」
「그리고 «목표를 달성하기 위한» 최소수정임」.**

**목표가 고정이고, 최소화하는 것은 «수정»이다.** ①②③은 **어떻게 고치느냐**에 대한 규칙이지
**무엇을 만드느냐를 깎는 근거가 아니다.** 요구사항은 소유자가 말한 **도착지 그대로**이고,
관문은 그 도착지에 **가장 적은 코드로** 가라는 뜻이다.

소유자 원문: **「목표달성 못하면 말짱꽝」.** 목표에 못 닿는 수정은 **작은 게 아니라 0이다.**

**왜 적혔나:** 2026-08-19 밤, 소유자가 처음부터 말한 것(「무엇을 입력해야 하고 어떻게 입력하는지를
UI로」)에 **네 라운드에 나눠 도착했다.** 총괄이 매 라운드 「지금 가장 작은 다음 걸음」을 물었고
**「이걸로 그가 말한 상태가 되나」를 안 물었다.** 그래서 소유자가 같은 요구를 **세 번 더** 말해야 했다
(「VS Code처럼」 · 「구조를 못 외우겠다」 · 「값이 없으면 빈 폼으로 하위 구조가」).

**어떻게 적용하나:** 지시서를 쓰기 전에 **도착지를 적고**, 라운드마다 그것에 대조한다 —
「합리적인 다음 증분인가」가 아니라 **「이걸로 도착하나, 안 되면 무엇이 남나」**. 남는 게 있으면
**소유자가 묻기 전에 먼저 말한다.**

부수적인 것(보안·가드·성능·정리)은 **핵심 기능이 도는 것을 확인한 뒤 차근차근.**
데이터를 지키는 가드(확정 경로의 쓰기 검사)만 예외다.

---

**이 관문은 지시서에도 걸린다.** 남에게 최소를 요구하면서 지시서로 최대를 주면 규칙이 아니라
말이 된다. 무엇을 시키기 전에도 ①②③을 통과시킨다.

**왜 상설이 됐나:** 2026-08-06 「쓸데없이 복잡하게 짜놔서 버그만 많네」, 2026-08-19
「쓸데없이 복잡한듯」. 두 번째 날 소유자는 한 화면에서 거절을 **여덟 번 연달아** 맞았고,
그중 여섯은 서로 다른 자리에 있던 **같은 실수 하나**였다 — 없는 것을 고장이나 진행 중으로
읽는 것. 각각은 쓸 때 옳았고, 그래서 검토로는 보이지 않았다.

---

# 🔴 UI 상설 — 「모든 UI는 «조립식»」 (소유자 2026-08-23)

> 「현재 만드는 모든 요소는 클래스로 만들어서 어느 화면에도 끼워넣을 수 있게해」
> 「행동 규칙 추가: 모든 ui는 "조립식"」
> 「고유 div를 가지고 그리드 배치로 작동하게. 마킹은 모든 영역에서 공유」

```
클래스     생성자가 «자기 mount 와 deps»를 받는다. 모듈 수준 상태 금지
고유 div   부품마다 자기 div 하나. 남의 div 안을 그리지 않는다
그리드     화면은 그 div 들을 격자에 앉힌다. 배치는 부품 «밖»에 있다
마킹 공유  마킹은 어느 부품에도 속하지 않는다. 밖의 «저장소» 하나에 산다
마킹 여럿  그 저장소는 «이름 붙은 마킹»을 여럿 담는다 (마킹1 · 마킹2 …)
           부품은 «읽을 이름»과 «쓸 이름»을 각각 선언한다
시험       같은 화면에 «두 인스턴스»를 놓고 간섭이 없어야 한다 — 이게 「끼워넣을 수 있다」의 정의
```

**왜 적혔나:** 2026-08-23 실측 — `ledger_map_panel.js` 가 모듈 수준에 `deps`·`mountEl`·
`session` 을 들고 있어 **한 페이지에 두 개를 못 놓는다**(둘째가 첫째를 덮는다).
맵·트렌드·순위표를 한 화면에 «여럿» 놓는 것이 이 제품의 전제인데 구조가 그걸 막고 있었다.

🔴 **마킹을 부모 화면이 들고 내려 주면 조립식이 아니다** — 부품을 옮길 때마다 그 화면의
부모를 또 만들어야 한다. 마킹은 바깥에 살고 부품은 «구독»한다.
마킹의 단위는 **온톨로지 노드 또는 노드의 집합**이므로, 부품끼리 서로를 몰라도
같은 노드를 가리키면 연결된다.

🔴 **정정 (같은 날 저녁) — 「밖에 하나」가 아니라 「이름 붙은 여럿」이다.**
처음에 이 규칙을 「마킹은 바깥의 하나」로 적었는데 **틀렸다.** 소유자가 스팟파이어로 그린 초안에
마킹이 «둘»이었다 — 맵이 쓰는 「마킹 1」과, 후보 선택인 「마킹 2」가 순위표와 후보 트렌드를 «같이»
움직인다. 소유자 확인: 「마킹은 여러 컨텍스트 지원해야하는게 맞음」.
```
저장소   바깥에 «하나»
마킹     그 안에 «여럿». 각자 이름이 있다
부품     읽을 이름 · 쓸 이름을 «선언»한다 (같을 수도, 다를 수도 있다)
```
**「하나」로 박아 두면 저 화면이 안 만들어진다** — 맵에서 점을 찍는 순간 후보 선택이 날아간다.

⚠️ **지금 만드는 것에 적용한다.** 레거시 소급 개조는 별도 판정이다.

---

# 🔴 UI 상설 — 「**설명 문구 주저리주저리 «금지»**」 (소유자 2026-09-04)

> 「ui에 설명 문구 주저리주저리 금지」

```
화면은 «보여» 준다. «설명»하지 않는다
문장이 길어진다는 것은 대개 「화면이 안 보여 주고 있다」는 뜻이다
   -> 그러면 문장을 늘릴 게 아니라 «보여 주는 방식»을 고친다
```

## 판별식 — 문구를 쓰기 «전»에 한 번
```
「이 문장을 지우면 운영자가 «틀리게» 읽나」
   🔴 아니오  ->  지운다. 그건 내가 «불안해서» 쓴 문장이다
   ✅ 예      ->  남긴다. 다만 «한 줄»로, 그리고 «값 옆»에. 단락으로 만들지 않는다
```

## 남는 것 / 나가는 것
```
✅ 남는다   단위 · 기준 시각 · 「이 수가 «무엇의» 수인가」   없으면 «다른 수»로 읽힌다
✅ 남는다   거절의 «사유»와 «다음 행동»                    운영자가 고칠 자리를 찾는다
❌ 나간다   기능 소개 · 「~할 수 있습니다」 · 배경 설명
❌ 나간다   제목이 말한 것을 부제가 다시 말하기
❌ 나간다   내 «설계 의도»의 해설 — 그건 코드 주석이나 보드의 자리다
```

⚠️ 이건 「자막 단 실패도 실패다」와 «같은 병»이다 — 문구를 늘려서 «고친 것처럼» 보이게 하는 것.
   설명이 길어지는 자리는 대개 «설계가 안 끝난» 자리다.
📎 함께: 「UI 문구는 번역체 금지 — 기호·짧은 영어·명사형」(2026-08-05).
   그건 «어떻게 쓰나»이고, 이건 «쓸 것인가»다. 이쪽이 «먼저» 걸린다.

---

# 🔴 행동 양식 상설 둘 (소유자 2026-08-24)

## ① 「모든 개발은 «근원 템플릿 요소» 개발 후 «데이터 갈아끼우기»」

같은 종류가 «둘째»로 필요해지면 두 번째를 손으로 그리지 않는다.
첫째를 템플릿으로 올리고 둘 다 «선언»으로 만든다.

```
템플릿   모양            표라면: 컬럼마다 {key, label, align, width, kind}
선언     값              구성 표 = 선언 하나 · 순위 표 = 다른 선언. 표 코드는 «한 벌»
시험     같은 화면에 둘을 «서로 다른 선언»으로 앉혀 간섭 없음
```

**왜 적혔나:** 부품을 일곱 클래스로 잘 나눠 놓고 그 «안»에서 표를 두 번 손으로 그렸다.
한 화면의 표 둘이 머리·행높이·줄수·구분선·정렬·상태 표기가 전부 달랐다.
**클래스로 나눈 것만으로는 부족하다** — 부품 «안»에서 반복하면 바깥의 조립식이 안쪽에서 무너진다.

⚠️ 「지금 이미 둘」일 때 만든다. 나중에 여럿일 «것 같아서» 층을 만드는 것은 이 규칙이 아니다.

## ② 「클라 세션 완료 후 «목업과 직접 스샷 비교»」 — 총괄이 «직접»

라운드가 착지하면 총괄이 목업과 화면을 «나란히» 띄워 «부품 단위»로 스샷 대조한다.
레인의 「목업처럼 보인다」는 그 레인의 관측이지 총괄의 판단이 아니다.

**왜 적혔나:** 「목업 똑같이 완수」를 보고했는데 소유자가 열어 보니 한눈에 보이는 것 «일곱»이
달랐다(맵 세로 배치 · 코어 맵 없음 · 후보 트렌드 없음 · Y축 알약이 차트를 안 바꿈 ·
트렌드 우측 컨트롤 없음 · 「펼친 층」 없음 · 표 양식 불일치).
총괄이 그날 아침 스샷을 «찍고도» 못 봤다 — **자기 체크리스트에 있는 것만 찾고 그림을 안 봤다.**
목록에 없는 차이는 눈앞에 있어도 안 보인다.

---

# 🔴 데이터 상설 — 「마킹한 노드의 «하위 그래프»를 데이터로 들고 온다」 (소유자 2026-08-24)

> 「아무튼 마킹이 뭔지 이제 알겠지?」
> 「맞아 **마킹한 노드의 하위 그래프를 데이터로 들고 오는 거야**」
> 「이거 세션들에게 매우 강조시켜, 아예 claude.md 에 박든가」

**마킹은 «표시»가 아니다. 질의의 «주어»다.**

```
마킹      부호 붙은 «노드 집합». 화면 상태가 아니라 walk 의 «시작점»
데이터    그 노드에서 «걸어서 닿는 하위 그래프» 그 자체
부품      선언하는 것은 둘뿐 — { start = 읽을 마킹,  collect = 무엇을 걷나 }
⚠️ 여기의 `collect` 는 «부품이 선언하는» 축이다 — 사용자에게 «고르게 하는» 축이 아니다. 그 둘을 섞어 투영의 내부 낱말(point·collection·claim …)을 화면에 내놓은 것이 2026-08-27 의 결함이고, 판정은 아래 「술어가 아닌 것은 표면적으로 노드」 절이 정본이다.
라우트    그래서 여러 개일 이유가 «없다». 같은 walk, 다른 선언
```

## 소유자가 그린 흐름 (2026-08-24) — 전부 walk 이다
```
서버 ─walk(each groupby · trend Y value)→ 기본 트렌드 ─→ 마킹 1
마킹 1 ─walk(specific groupby · y value correlate candidate)→ 후보 ─→ 마킹 2
마킹 1 ─walk(specific groupby · wafer, process)→ 자재 정보
마킹 1 ─walk(specific groupby · trend Y value)→ 맵
마킹 2 ─walk(specific groupby · mark2 type)→ 후보 트렌드 · 후보 맵
```
🔴 **맵과 트렌드는 «같은 collect»다.** 시작점만 다르다 —
맵은 다른 데이터가 아니라 **한 그룹으로 좁힌 같은 데이터**다.

## 어기는 세 가지 — 전부 「돌지만 틀린」 모양이다
```
① 부품이 «거른다»        마킹으로 받은 것을 클라가 filter/groupBy 한다
                        -> 거르는 것은 walk 이 할 일이다. collect 를 고쳐라
② 라우트를 «더 판다»      화면이 하나 늘 때 fetch 함수가 하나 는다
                        -> 늘어야 하는 것은 «선언»이지 갈래가 아니다
③ 마킹을 «하나»로 둔다    맵에서 점을 찍는 순간 후보 선택이 날아간다
                        -> 마킹1이 후보를 만들고, 후보에서 찍은 것이 마킹2다
```

**왜 적혔나:** 2026-08-24 실측 — `marking_store` 는 「이름 붙은 마킹 여럿 + 부호」를
이미 들고 있는데, `api.js` 전체에 `positive`·`negative`·`seeds` 가 **한 번도 안 나온다.**
`fetchSubgraph` 는 `id` «하나»와 `collect` 만 보낸다. 즉 **마킹이 질의에 닿지 않고
그리기에만 쓰이고 있었다.** 그 결과 fetch 함수가 다섯 갈래(`fetchLotMap`·`fetchComposition`
·`fetchSubgraph`·`fetchTrends`·`fetchSiblings`)로 갈라졌는데, **소유자의 도식에는 갈래가 없다.**

**어떻게 적용하나:** 부품을 만들 때 「이 데이터를 어디서 가져오나」를 묻지 말고
**「이 부품의 start 는 어느 마킹이고 collect 는 무엇인가」**를 물어라. 답이 그 둘로 안 적히면
설계가 틀린 것이지 walk 이 모자란 것이 아니다.

## 🔴🔴 「**마킹은 결국 서브그래프 탐색이다**」 (소유자 확정 2026-08-24 저녁 — 정본 지시)

> 「마킹1 → 마킹2 이렇게 이어지면서 **서브그래프 탐색을 계속하는거야.
> 차트들은 그 서브그래프를 «보여주는 창»이고**」
> 「우리 프로젝트는 **walk 제어가 핵심**인듯 — 어떻게 걸어서 연관 있는 거를
> 유기적으로 뽑아서 온톨로지 탐색을 가능하게 하느냐」

```
마킹      한 번의 표시가 아니라 «탐색의 한 걸음»
체인      마킹1 --walk--> 서브그래프 --찍기--> 마킹2 --walk--> ... «계속»
차트      그 서브그래프를 «보는 창». 창은 «가져오지 않는다» — 고른다
체인 길이  «선언»이다. 마킹3 은 선언 한 줄이고 코드가 아니다
```

### 🔴 판별식 — 「이 부품이 «마킹을 받나», «키로 묻나»」

소유자 진단: **「마킹 받게 했으면 없을텐데」** (요청이 왜 이렇게 많냐에 대한 답)
```
칩 id·행 키·축:값을 «받는» 라우트  ->  그 키마다 «한 번씩» 불릴 수밖에 없다
마킹을 «받는» 라우트               ->  마킹 «전체»에 한 번 답한다
```
🔴 **요청 수는 화면 탓이 아니라 «인자 모양의 결과»다.**
실측 2026-08-24: 한 페이지 로드에 요청 «13» · 라우트 «5» · 그중 walk «1» · «정확히 중복 2».
중복 둘은 두 부품이 «각자 같은 칩 id 를 풀었기» 때문이다 — 둘 다 마킹을 받았다면 walk 은 하나였다.

### 그래서 walk 제어가 핵심이라는 말의 «실무 번역»
```
핵심은 알고리즘이 아니다 — 「무엇이 걸어서 닿는가」와 「닿은 것이 자기를 뭐라 부르는가」다.
둘 다 «선언»의 성질이다.
```
실측 2026-08-24, 그날의 실패 «여섯» 중 알고리즘 결함은 «0»이었다:
```
닿지 못함  마킹이 질의에 안 실림 · transfer 72,964 가 «값»이라 엣지가 없음
거리      die 주어가 wafer 주어보다 «2홉» 멀다 — 같은 술어, 다른 깊이
못 부름    finding_kind 가 qualifiers 밑 -> 읽는 쪽이 「defect」 · 트렌드는 103,841 중 «15»를 셈
예산      node_limit 400 의 «끊김»을 «부재»로 읽을 수 있었다 (끊김 ≠ 없음)
```
📎 세부·수치·목표는 `task/CHART_DESIGN.md` §2-bis·§2-ter 가 정본이다.

---

# 🔴 설계 원칙 상설 — 「**술어가 아닌 것은 «표면적으로» 노드로 처리한다**」 (소유자 2026-08-27)

> 「아무튼 설계 원칙 **모든 술어가 아닌 것은 표면적으로는 노드로 처리한다**」

```
술어        «엣지»다. 엣지인 것은 이것 «뿐»이다
그 외 전부   개체 · 발견 · 값 · 수량 · 묶음 · 액션 …  ->  표면에서는 «노드»
저장 모양    값이든 수식어든 «상관없다». 그건 우리 배관이고 표면의 낱말이 아니다
```

## 🔴 「표면」이 어디까지인가 — «응답»이 아니라 «사람이 고르고 찍는 자리»다
```
표면이다      드롭다운 · 마킹할 수 있는 것 · 화면이 부르는 이름 · 부품이 선언하는 타입
표면이 아니다  응답의 배관 칸(`node_kind` 같은 것). 지워도 된다는 뜻이 아니다 — «안 보일 뿐»이다
```
2026-08-27 실측: `node_kind` 를 「화면에서 사라진다」로 읽고 걷어내면 클라 «넷» 중 «둘»이
뜻을 잃는다(실측 후보와 선언만 있는 후보가 «같아 보인다»). 오류는 안 난다. 그래서 위험하다.

## 왜 적혔나
2026-08-27, 걷기 검색창의 `COLLECT` 드롭다운이 «투영의 내부 열거»(entity · point · claim ·
collection …)를 사용자에게 내놓고 있었고, 여덟 중 «둘»은 누르면 «422» 였다.
소유자: 「**사용자가 claim, point, collection 이런 걸 어케 암**」 ·
「**사람 한다고 될 게 아님 아키텍처 문제임**」 · 「**노드가 아니어도 노드로 취급해야지.
디펙이 node 에 있는지 point 에 있는지 어떻게 알아 사람이?**」

실측이 그 지적을 그대로 받쳤다:
```
선언   observed@1 의 목적어 = {kind:"value", qualifiers.optional:[… finding_kind …]}  -> 「값」
투영   그런데 «노드를 만든다» — 웨이퍼 하나에 point 89 · collection 28
       그 노드가 도메인 낱말을 «이미» 들고 있다: label "void (1)" · keys.finding_kind "void"
       그런데 type 은 "Finding Collection" — «배관 낱말»
point / collection  같은 발견의 «두 배율»이다 (낱개 89 -> 묶음 28).
                   배율은 이미 다른 축(`observations=claims|summary`)이 정한다
```
**투영이 이미 노드로 취급하고 있는데 선언만 그렇게 안 적혀 있었고, 그 틈을 메우려고
«두 번째 축»이 생겼다.** 그 축이 사용자에게 배관을 묻고 있었다.

## 어떻게 적용하나
```
① 사용자가 고르는 축은 «노드 타입 하나». 술어는 `follow` 로만 고른다
② 투영이 돌려주는 «모든» 것에 «도메인 type» 을 붙인다 — 이미 keys/label 에 있는 낱말을 올린다
③ 「배율·보기 방식」을 «타입»으로 만들지 않는다 (낱개/묶음은 별도 축이다)
④ 새 축을 만들기 «전»에 묻는다: 이게 술어인가? 아니면 노드여야 한다
```
⚠️ 이 원칙은 «표면»의 규칙이다. 저장을 바꾸라는 뜻이 아니다 — 값으로 저장된 것을 노드로 «보여도»
   된다(소유자 2026-08-25 판정: 「목적어가 값인 건 괜찮네」). 바꾸는 것은 «부르는 이름»이다.

## 🔴 원칙 ②의 «목적» — 걷기로 «확장»하기 위해서다 (소유자 2026-08-27)

> 「너가 포인트로 말한 디펙도 **디펙의 종류 모델링 등 다른 관계 및 노드, 물리적 모델링이 붙을 수 있음**」
> 「**이 모든 것은 걷기 알고리즘을 통한 확장성을 위한 것**」

```
노드면    걸어서 «나갈 수» 있다 -> 나중에 무엇이든 «붙일 수» 있다
종점이면   거기서 «끝»이다 -> 붙일 자리가 없다. 붙이려면 «구조를 바꿔야» 한다
```

### 실측 — 물리 모델이 «이미 일곱» 있는데 «수식어»로 눌려 있다
```
선언   observed@1 목적어 = {kind:"value",
       qualifiers.optional: [gate · unit · run_uid · inchip_x · inchip_y · radius_y · finding_kind]}
원자   {"value": 7.691,
        "qualifiers": {"gate":7.0, "unit":"um", "run_uid":"sat|SYN-AUG-BW-001-01|10|10|7|…",
                       "inchip_x":7475.16, "inchip_y":4857.94, "radius_y":7.591,
                       "finding_kind":"void"}}
투영   schema_kind = «"terminal_finding_point_projection"»
       🔴 코드가 자기 이름에 «terminal» 이라고 적어 두었다
```

### 그 일곱이 «전부» 붙을 수 있는 것들이다
```
finding_kind        -> 「종류」 노드 (void · delam · non_wet). 종류에 «판정 기준»이 붙는다
run_uid             -> 「스캔」 노드 (언제 · 어느 장비 · 어느 레시피로 봤나)
inchip_x/y · radius · gate · unit  -> 「물리 모델」 (칩 안 좌표 · 크기 · 게이트 · 단위)
```
발견이 «노드»면 이렇게 걷힌다:
```
마킹1 웨이퍼 ─walk→ 발견들 ─찍기→ 마킹2
마킹2 발견   ─walk→ 종류 · 스캔 · «같은 스캔의 다른 발견» · «같은 종류의 다른 발견»
```
지금은 «마킹2 에서 나갈 곳이 없다». 그래서 「보이드 있던 wf 의 cmp rcp 로 진행한 wf 의 보이드」
같은 질문이 «웨이퍼를 거쳐서만» 돌아간다.

### 그래서 원칙 ②는 «이름 바꾸기»가 아니다
```
❌ 작은 판    type: "Finding Point" -> "void"     (화면 낱말만 바뀐다. 종점은 그대로다)
✅ 진짜 판    발견이 «주어가 될 수 있는 노드»가 된다
             -> 선언에 발견 엔터티 · observed 의 목적어가 entity_ref
             -> 수식어 일곱이 «엣지와 속성»으로 풀린다
             -> 그때 「종류에 무엇이 붙는다」를 «선언 한 줄»로 더할 수 있다  <- 이것이 확장성이다
```
⚠️ 크기가 커집니다. 그래서 계획 2단계의 «멈춤 조건»이 그대로 유효합니다 —
   발견 종류가 선언에 없으므로, 거기서 멈추고 «무엇을 선언에 넣을지»를 올리는 것이 먼저입니다.
   다만 목적지는 이제 분명합니다: **종점을 없애는 것**이지 이름을 고치는 것이 아닙니다.

---

# 🔴 아키텍처 상설 — 「**엔티티 · 어휘 · walk. 이게 끝**」 (소유자 2026-08-28)

> 「그냥 다 엔티티 어휘로 전환」 · 「오로지 'walk'만」 · 「ref 도 다 엔티티로 하자 이제」
> 「**엔티티 어휘 walk 이게 끝**」

```
엔티티   선언된 노드 타입. 노드는 이것«뿐»이다
어휘     선언된 술어. 엣지는 이것«뿐»이다
walk     GET /api/ledger/subgraph. 데이터에 답하는 것은 이것«뿐»이다
```

## 그래서 «없는» 것
```
투영이 지어낸 노드 타입   Finding Point · Finding Collection · Quantity · Value …
투영이 지어낸 엣지        has_findings · finding · in_container · contains · on_subject …
노드 종류 축              NODE_KINDS · collect(종류) · observations=summary|claims
                         낱개/묶음은 «세는 방식»이지 타입이 아니다
id 접두어 여럿            ledger-entity:v1: «하나»만 남는다
키를 받는 데이터 라우트    lot_map · trends · siblings · composition · selection/resolve
                         · kinds · structure · subgraph/table
코드에 박힌 도메인 낱말    "void" · "delam" · finding_kind == "void" 같은 갈래
```

## 판별식 — 새 것을 만들기 «전»에
```
① 이건 술어인가?        -> 어휘에 «선언»한다
② 아니면 노드인가?      -> 엔티티로 «선언»한다
③ 둘 다 아닌가?         -> 만들지 «않는다». 투영이 지어내는 순간 선언이 거짓이 된다
🔴 「지울 수 없다」가 나오면 그것이 하드코딩이다 (소유자 2026-08-28) — 무시하고 지운다
```

**왜 적혔나:** 2026-08-27~28 실측 — 노드 id 를 만드는 함수가 «일곱»이라 `decode_node_id` 가
접두어로 갈래를 틀고, 그 갈래 수가 그대로 walk 루프의 갈래 수(entity·point·collection·quantity·event)가
됐다. **그중 원장을 읽는 것은 «entity» 하나뿐**이고 나머지 넷은 투영이 접은 것을 투영이 다시 펴는
자리였다. 합성 엣지 `in_container` 는 잇는 쌍 128 중 «유일한 연결»이 «0» 이었고
(원자 엣지 `inspected` 가 이미 양방향으로 잇는다), `has_findings`·`finding`·`mechanism` 이
「유일한 연결」이었던 이유는 **투영이 그렇게 박아서**였다. 종류 카탈로그(void·delam)는
`finding_kinds.py` 의 «파이썬 dict» 였고, `ledger_selection.py:565` 는
`if finding_kind == "void"` 꼴로 **코드가 도메인 낱말로 갈래를 텄다.**

📎 개정안 정본: `task/DECLARATION_REVISION_6.md`

---

# 🔴 데이터 상설 — 「**표에 원천 데이터를 넣고, 그걸로 원장**」 (소유자 2026-08-28)

> 「ㅇㅇ 그렇게 해 **다른것도 이 방식이 표준이야 테이블에 원천 데이터 넣고 이거로 원장**」

```
원천     «표»에 행으로 들어간다.            ← 사람이든 씨앗이든 외부 시스템이든, 전부 여기로
선언     ledger_config 의 «소스»가 그 표를 읽는다
원자     번역기가 만든다.  원장에 «직접» 쓰는 길은 없다
```

## 판별식 — 「이게 원장에 «어떻게» 들어왔나」
```
✅ 표 -> 소스 선언 -> 번역기 -> 원장          선언이 아는 원자. walk 이 걷는다
❌ 스크립트 -> store.write_batch -> 원장       선언이 «모르는» 원자. 종점이 된다
```
🔴 **`store.write_batch` 를 부르는 정당한 자리는 `ledger/runtime_v2.py` «하나»다.**
그 밖에서 부르면 «두 번째 문»이고, 그 문으로 들어온 것은 선언에 이름이 없어서
**walk 의 주어가 되지 못한다** — 원장에 있는데 못 걷는다.

## 왜 적혔나
2026-08-28 실측. 재건으로 레시피 파라미터가 0 이 된 것을 「선언이 끊었다」로 읽고
「선언을 넓히면 복구된다」고 두 번 보고했는데, **둘 다 틀렸다.**
```
pre-rebuild processed_with 28,154 를 «누가 썼나»로 가르면
   씨앗 스크립트   25,132   syn_recipe_book · syn_eqp_log · syn_fab_mes · syn_mes_queue · syn_mi_gauge …
   진짜 번역        3,022   wafer_process_recipe  <- 오늘 살아남은 «전부»
store.write_batch  53줄 · 선언 검증 낱말 «0»
                   -> 선언은 «번역기가 무엇을 만드는지»를 정하지 «무엇이 써지는지»를 막지 않는다
scripts/ 에서 write_batch 를 직접 부르는 파일 «7» (5,381줄)
```
즉 파라미터가 원장에 있었던 것은 **씨앗이 선언을 «거치지 않고» 썼기 때문**이고,
그래서 그 값들은 원장에 있으면서도 **선언이 모르는 값**이었다 — walk 이 주어로 쓸 수 없었다.

## 어떻게 적용하나
```
① 새 데이터가 필요하면 «표»를 먼저 만든다. 원자를 만들지 않는다
② ledger_config 에 그 표를 읽는 «소스»를 선언한다 (기존 소스와 «같은 모양»)
③ 술어가 실어야 할 값이 있으면 «수식어 이름»을 선언에 적는다 — 적지 않으면 떨어진다
④ 씨앗 스크립트는 «행 생성기»다. 데이터는 그대로 만들되 «표»에 쓴다
⑤ 🔴 새로 `store.write_batch` 를 부르고 싶어지면 «멈추고 올린다». 답은 대개 「표를 만들어라」다
```
⚠️ 기존 씨앗 일곱의 «소급 전환»은 별도 판정이다. 첫 하나를 «템플릿»으로 만들고
   나머지는 «선언 교체»로 따라온다 (「근원 템플릿 요소 개발 후 데이터 갈아끼우기」 그대로).

---

# 🔴 행동 상설 — 「**모든 제안 «전» walk 으로 해결 가능한지 파악할 것**」 (소유자 2026-08-28)

> 「모든 제안 전 walk으로 해결 가능한지 파악해볼것」

```
제안하기 «전»에 묻는다   이 질문이 「씨앗 + follow + 방향」으로 «이미» 답해지나?
답이 예면               제안은 «선언 한 줄»이거나 «화면 한 곳»이다. 원장은 안 건든다
답이 아니오면            «왜 안 되는지»를 수로 적고 나서 제안한다
```
🔴 **「이건 다른 질문이니 새 술어가 필요하다」는 «머릿속 구분»일 때가 많다.** 데이터가 그렇게
갈려 있는지는 «걸어 봐야» 안다.

## 왜 적혔나 — 같은 날, 제 제안 «둘»이 이 규칙에 걸렸습니다
```
① pkg 구성        제 제안: 엔티티 pkg@1 + 새 술어 + 소스 + 재적재 749k
   소유자 지적:    「bondfrom collect 조합으로 구성 파악되지 않아? 굳이 새 엣지가 왜 필요해?」
   실측:           씨앗 = base die · follow=bonded_from  ->  코어가 «나옵니다»
                   pkg = die@1{mat_id=base, mat_type=Wafer, x, y} — 소유자가 준 (base,x,y) 그대로
   => 엔티티 «불필요» · 술어 «불필요» · 재적재 «불필요». 좌석의 «시작점»만 바꾸면 됩니다
   내 오류:        「bonded_from 은 계보, pkg 는 구성 — 다른 질문」이라 했는데
                   그건 «내 머릿속 구분»이었고 같은 엣지가 양쪽에 답합니다

② 「값 밀고」      내 제안: 「엣지 수식어 비교는 다음 라운드 서버 안건」
   실측:           walk 의 엣지가 qualifiers {role, step, value, eqp_id} 를 «그대로» 나릅니다
   => 서버 변경 «0». 창이 두 집단의 값을 «세면» 됩니다 (트렌드 판정과 같은 부류)
```
## 그리고 이 규칙은 «아니오»도 준다 — 그때는 근거가 «수»로 남는다
```
③ 물리량 카탈로그  실측: pressure_MPa 에서 hops=6 을 걸어도 닿는 quantity 가 «자기 하나»
                  bond_pressure 로 가는 길 «없음»  -> 여기는 «진짜로» 새 엣지가 필요합니다
```
## 어떻게 적용하나
```
① 제안서를 쓰기 «전»에 walk 을 «한 번 태운다». 씨앗과 인자를 적어서
② 닿으면 -> 제안은 「선언/화면」이지 「원장」이 아니다
③ 안 닿으면 -> «어디서 끊기는지»를 수로 적고, 그 수가 제안의 근거가 된다
④ 「다른 질문이라 다른 술어」가 떠오르면 «그 말이 데이터에서도 참인지» 걸어서 확인한다
```

---

# 🔴 행동 상설 — 「**임시로 박스에 설정한 케이스로 논의하거나 재서 대답 금지**」 (소유자 2026-08-30)

> 「너가 만든 이 박스 얘기 금지」
> 「임시로 박스에 설정한 케이스로 논의하거나 재서 대답 금지」
> 「자꾸 새지 말고」

## 🔴 먼저 — 이 규칙은 「재지 마라」가 «아니다». 「무엇에 대한 주장인지 밝혀라」다

**이 박스는 만들고 확인하는 곳이다. 그게 이 박스의 용도다.**
```
✅ 정상 업무   고치고 · 서버를 돌리고 · 자기 변경을 «전/후»로 재고 · 기제를 재현한다
              시험·하니스·픽스처는 «커밋된» 것이라 코드 판단을 잰다 — 마음껏 쓴다
              라이브 선언·표를 «읽어» 모양을 파악한다 — 읽는 것은 막지 않는다
              「이 박스에서 이렇게 나온다」로 «밝혀서» 쓰는 것도 정상이다
🔴 금지는 «하나»  이 박스의 수를 «운영에 대한 주장»으로 내놓는 것
              「운영은 N 행이다」 · 「운영엔 없다」 · 「운영에서 비용이 0이다」
```
⚠️ **안 재는 것은 더 나쁘다.** 자기 변경을 확인 못 하는 라운드는 「착지했는데 배선 0」이 된다.
   재라. 다만 **그 수가 무엇에 대한 문장인지**를 같이 적어라.

```
증거다        선언 · 코드 · 소유자가 넣은 값       -> 운영에도 참이다
이 박스 얘기다  내가 만든 씨앗 행 · 내가 넣은 설정 · 그 위에서 잰 수
              -> «밝히고» 쓰면 된다. 운영 주장으로 «승격»시키지 않는다
```
🔴 **경계는 «저장소에 커밋됐나»다.**
```
✅ 커밋된 코드          운영이 «같은 파일»을 돈다 -> 증거다
❌ gitignore 된 라이브 설정  운영은 «자기 설정»이 따로 있다 -> 증거가 아니다
❌ 내가 만든 씨앗 행       운영은 다른 수다
```
⚠️ **「선언이니까 증거」가 아니다.** `server/config/*.json` 은 대부분 gitignore 된 «이 박스» 선언이다.
그 안의 규칙이 살아 있든 내려져 있든 운영에 대해 아무 말도 하지 않는다.
저장소로 가는 것은 `.sample` 뿐이고, 그건 «모양»의 증거이지 «운영이 그렇다»는 증거가 아니다.

## 판별식 — 수를 내기 «전»에 「이 행을 누가 썼나」
```
✅ 선언이 이 컬럼을 «선언»한다        운영에도 참일 수 있다. 말해도 된다
✅ 코드가 이렇게 «판단»한다           실행이 증거다
⚠️ 이 박스에 몇 «행» 있다             내 씨앗이다. «이 박스에서는»을 붙이면 쓸 수 있다
                                   무회귀 대조(전/후가 같다)에는 «완전히 유효»하다
❌ 이 박스에 «없다» -> 「없다」        내가 «안 넣은» 것이다. 부재의 «증거로»만 못 쓴다
```
🔴 **「없다」가 특히 위험하다.** 씨앗이 안 만든 것은 전부 0으로 나오고, 그 0은
「이 기능이 안 된다」처럼 읽힌다. 안 넣은 것과 없는 것은 다르다.

## 왜 적혔나
2026-08-30 프레임 왕복 하나에서, 제가 낸 수가 **전부 제가 만든 씨앗 위의 것**이었습니다.
```
낸 수      행 34,939 · 자리 27,780 · 이음매 13,046 · 양쪽에 겹치는 것 126
그 위에 세운 문장   「고아가 크다」 · 「①과 ②는 같은 사건이다」
                 둘째는 «같은 대화 안에서» 교집합 0 으로 반증됨
소유자 차단  「너가 만든 이 박스 얘기 금지」
```
같은 날 오후에 이미 한 번 맞았습니다 — 「운영은 b_wy 밖에 없음」.
그때 제가 프레임 논증에 쓰던 좌표 컬럼 다섯이 **제가 만든 것**이었습니다.

🔴 그리고 이건 **세 번째**입니다. 2026-08-04 「시뮬 선언은 운영의 증거가 아니다」,
2026-08-05 「여기는 운영이 아니다」가 이미 판정돼 있었습니다. 둘 다 «기억»에만 있어서
그때그때 다시 배웠고, 그래서 이번엔 관문에 박습니다.

## 어떻게 적용하나
```
① 수를 말하기 전에 「이 행을 누가 썼나」를 묻는다. 내가 썼으면 «내지 않는다»
② 운영 값이 필요하면 «묻는다». 이 박스에서 «대신» 재지 않는다
③ 선언과 코드로 답할 수 있으면 그걸로 답한다 — 그 답은 운영에도 참이다
④ 재는 대신 «전제를 받아 따진다». 소유자가 준 전제로 결론이 나오면 실측이 필요 없다
⑤ 그래도 모르면 «모른다»고 말하고, 무엇을 알려주면 판정되는지 «한 줄»로 적는다
```
