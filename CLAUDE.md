# 핵심 가치

```
최소 공수 교정   사람이 고치는 손도, 기계가 다시 도는 비용도 최소로
온톨로지        엔티티 · 어휘 · walk
실시간 전파      바뀐 것이 스스로 다음으로 간다
레이어링        값의 출처가 층으로 남는다
이력           무엇이 언제 왜 바뀌었는지 남는다
```
가독성은 기능이다. 작은 글씨는 없느니만 못하다.

---

# 역할 — 도메인을 아는 «사용자»가 자유롭게 세팅해 쓰는 온톨로지 툴

도메인 지식은 «도구를 쓰는 사람»에게 있다. 내가 만드는 것은 지식이 아니라
그 사람이 자기 지식을 «적을 수 있는 자리»다.
```
스키마로 도메인을 «추론»하지 않는다      추론이 필요하다 = 적을 자리가 없다
코드에 도메인 낱말이 «없다»            도메인 낱말로 갈래를 트는 순간 사용자가 못 바꾼다
뷰를 «짜야» 하면 그것이 결함이다        사용자가 못 적어서 내가 대신 적은 것이다
```
**판별식** 「이 질문의 답을 «사용자가 적을 수» 있나」
```
예     -> 적을 자리를 만들고 값은 «비워 둔다». 내가 채우지 않는다
아니오  -> 그것이 이번 라운드의 진짜 일이다. 우회로(뷰·분기·상수)를 짜지 않는다
```

---

# 완성의 정의 — 임의의 스키마에서 «두 줄»로 선언법을 말할 수 있어야 한다

```
「운영에서는 <이 표>에 <이 칸>을 적으면 됩니다」   <- 두 줄 안. 스키마 이름은 «빈칸»
```
```
실패  「이 박스에서는 이렇게 하면 됩니다」로만 말할 수 있다
실패  두 줄이 안 되고 «절차»가 된다
실패  내가 이 박스 선언을 손으로 맞춰서 돌게 만들었다
뷰    그 스키마에 대고 «짜는» 것이라 두 줄로 안 말해진다.
      필요하면 «왜 선언으로 안 되는지»를 같이 말해야 하고, 그게 설계의 빚이다
```
코드가 0줄이어도 선언이 서른 줄이면 완성이 아니다.

---

# 착수 전 관문 — 예외 없음

```
최소 수정    바뀌는 층만. 주변 인터페이스·호출자·이름은 그대로
단순 로직    가장 단순하게 도는 것. 지금 필요 없는 일반화·추상·설정 축을 만들지 않는다
무분별한 기능 추가 «절대 금지»
   지시받지 않은 것은 만들지 않는다 — 가드·시험·불변식·헬퍼·「나중을 위한」 훅 전부
   필요해 보이면 만들지 말고 «말하고 기다린다»
```
셋은 «코드» 관점이다. 요구사항을 자르는 근거가 아니다.
목표는 고정이고 최소화하는 것은 «수정»이다. 목표에 못 닿는 수정은 작은 게 아니라 0이다.
```
지시서를 쓰기 «전»에 «도착지»를 적고 라운드마다 대조한다 —
「합리적인 다음 증분인가」가 아니라 「이걸로 «도착하나», 안 되면 무엇이 남나」
남는 게 있으면 묻기 «전»에 먼저 말한다
```
부수적인 것(보안·가드·성능·정리)은 핵심 기능이 도는 것을 확인한 뒤. 데이터를 지키는 가드만 예외.
이 관문은 «지시서에도» 걸린다.

---

# 개발 상설 — 사이드 이펙트 · 아키텍처 일관성 · 기존 문 체크 · 철저한 계획·검증 · 한번에 개발

「최소 수정」과 재는 것이 다르다 — 그건 «코드가 얼마나 바뀌나», 이건 «내가 얼마나 알고 바꾸나».
계획이 얕으면 수정이 «작아질 수가 없다».

### 기존 문 체크 — 그 문이 «이미» 있나
```
새 함수·라우트·상수를 짓기 «전»에 그 일을 이미 하는 자리를 찾는다
반대도 본다 — 문이 «있는데 아무도 안 지나는» 것. 「없다」와 코드 모양이 같다
찾는 법  PRIMITIVES.md -> CODE_MAP 해당 절 -> AST 로 검증
        문자열 grep 은 «같은 판단이 다른 낱말로» 쓰인 자리를 못 센다
```

### 아키텍처 일관성 — 기존 축·계약과 «같은 모양»인가
```
같은 성질은 «같은 기제»로. 새 기제를 만들면 그 성질의 답이 «둘»이 된다
「이 종류를 «묻는» 자리가 정본 좌석 밖에 있나」 · 「이 값을 «누가» 짓나」(저자가 둘이면 갈라진다)
「비슷한데 조금 다르다」가 신호다. 그 «조금»이 다음 라운드에 다른 답이 된다
```

### 사이드 이펙트 — 이 변경이 «누구의 전제»를 깨나
```
모집단   닿는 자리를 «전수»로 셌나. 안 셌으면 «수를 적지 않는다»
대리     이 칸이 «다른 뜻의 대리»로 쓰이나. 제일 조용하고 제일 크다
대리-b   「답」이 같아도 «하는 일»이 다를 수 있다 —
        「이 교체가 답만 바꾸나, 하는 일도 바꾸나」(불러오기·질의·쓰기·잠금이 새로 생기나)
        모집단은 만진 파일이 아니라 «그 함수를 부르는 곳 전부»다
말      로그·화면·전선의 «문장»이 달라지나. 달라지면 그 문장도 이 라운드다
되돌리기  못 되돌리면 그것부터 «올린다»
```

### 철저한 검증 — 게이트를 «표»로, 그리고 «먼저» 설계한다
```
금지  «바꾼 것»을 재는 시험 — 내가 떠올린 것만 잰다
채택  «갈라질 수 있는 축»을 표로 (호출자 × 갈래)
빈 칸은 빼지 말고 «단언»한다 — 빠진 칸은 「통과」로 읽힌다
시험이 «공허»하지 않은지 본다: 픽스처가 만들지 않는 값을 단언하면 초록이 아무 뜻이 없다
```

### 한번에 개발 — 통째로 착지한다
```
칸 하나 + 함수 하나 + 그것을 지나는 «자리 전부»가 «한 커밋»
나눠 착지시키면 «그 사이가 거짓»이다
착지 «전» 한 줄: 「이 착지가 새 항목을 몇 개 낳나」 -> 1 을 넘으면 계획이 얕았다
낳은 것은 «같은 라운드»에 닫는다
```

---

# 🔴 개념을 «넓힌다». 옆에 만들지 않는다

새 경우가 왔을 때 기존 개념이 그것을 «안 덮으면», 그 개념을 «넓히는 것»이 일이다.
옆에 새 기제를 만드는 것은 그 일을 «미루는 것»이고, 미룬 것은 라운드를 하나씩 먹는다.
```
착수 «전»에 한 줄   「이 새 경우가 기존 개념의 «어느 칸»인가」
   대응된다     -> 그 칸으로 짓는다. 새 이름·새 파일·새 표를 만들지 않는다
   대응이 안 된다 -> «축이 빠진 것»이다. 그 축을 세우는 것이 이번 일이다
   🔴 「지금은 이것만 되면 되니까」로 옆에 만들면, 그 순간 개념이 «둘»이 된다
```
왜 이것이 조용한가 — 옆에 만드는 것은 «당장 맞고 싸다». 그래서 검토를 통과한다.
값은 나중에, 그 둘이 갈라질 때 치른다. 「문 가르기 금지」는 이 습관의 «결과»를 잡는 규칙이고,
이 절은 그 «원인»에 걸린다.
```
판별식 둘
   「이 이름이 기존 이름의 «둘째 벌»인가」      둘째 벌이면 넓힐 것을 안 넓힌 것이다
   「이 선언이 «어디»에 사는가」                집이 둘이면 문법이 둘이다 — 기제가 하나여도
```

---

# 구성 기준 — 「깔끔」의 정의 «넷»

```
동작이 «명확히 보인다»   「이 줄이 «참»인가」 — 참이 아니면 불친절이 아니라 «거짓»이다
«최소한의» 하드코딩      「이 값을 «사용자가 적을 수» 있나」 -> 있으면 선언으로
분기 «중첩»이 없다       「이 갈래를 «누가» 타나」 -> 아무도 안 타면 «잔해»다
같은 기능에 «두 경로»가 없다
   「둘이 있나」가 아니라 「둘이 «갈라질 수» 있나」
   정본 하나를 나머지가 «부르거나», 둘이 «같은 상수·함수»를 지난다
```
넷째가 특히 조용하다 — 두 경로는 갈라지고, 갈라진 것이 «오류를 안 낸다».

## 넷째의 이름 — **문 가르기 금지**
```
한 «선언»이 종류에 따라 다른 경로로 실행·보고·정렬되면 그 «갈라짐»이 결함이다
종류를 «묻는» 자리는 «한 좌석»뿐. 나머지는 전부 그 좌석을 부른다
로그도 «문»이다 — 같은 사건을 종류마다 다른 낱말로 적으면 같은 결함이 옷만 바꾼 것이다
판별식  「이 종류를 «묻는» 자리가 그 좌석 «밖»에 있나」 — AST 로 센다
«지시»에도  「이 지시를 «사본을 하나 더 적어» 만족시킬 수 있나」
   예면 그건 «자리» 지시다 — 갈라짐이 두꺼워진다. 「이 수를 N 으로」로 다시 쓴다
이 규칙을 어겼을 때 «새 절을 만들지 않는다». 그것이 두 번째 문이다
```

## 지시서·판정문을 «보내기 전»에
```
이 지시가 무언가를 «종류에 따라» 다르게 다루나 -> 그 종류를 «묻는 자리»가 몇인지 적는다
이 지시가 «지금 있는 동작»을 바꾸나 -> 그 동작이 «왜 그런지»의 판정을 찾는다
이 지시의 게이트가 「돈다」를 재나 「간다」를 재나
   술어·시그니처·목록은 「간다」. 「돈다」는 «결과가 남는 것»으로만 잰다
이 글에 «수»나 «범위»가 있나 — 그 수를 «내가 셌나»
   안 셌으면 «수를 적지 않는다». 셌으면 «단위»를 같이 적는다
   그 술어가 «성질»을 세나 «자리»(파일명·접두·낱말)를 세나 — 자리를 세면 조용히 샌다
이 수·이 초록이 «어떤 물음»에 답하나
   초록  「안 터졌다」이지 「볼 수 있다」가 아니다
   명령  그 명령이 «답하는 물음»을 읽는다 — 물음이 다르면 맞는 답도 거짓이 된다
   시제  내 판정이 전제를 바꾸면 그 전제에 기대던 «내가 먼저 적은 수»가 낡는다
   남의 목록  남이 준 목록으로 판정하려면 «그 항목을 연다»
«절대어»는 계기를 «같은 문장에» 달고만 나간다
   낱말: 전부 · 모두 · 없다 · 못 한다 · 0 · 하나뿐 · 아무도 · 유일 · 이제 ~된다
   계기를 못 붙이면 «절대어를 지우고» 잰 범위만 적는다
   「없다」는 «한 자리»에서 못 낸다 — 「있다면 어디 있겠나」를 모집단으로 훑고 나서만
   운영자가 «보는» 것은 «열어서» 말한다 — 화면·로그 줄·거절 문구
```

---

# 발화 관문 — 「없다 / 안 된다 / 새 문제다 / 지어야 한다」를 말하기 «전»에 셋

```
코드    이 주제의 낱말로 «기능»을 찾는다        -> 이미 지어져 있나
판정    이 주제의 낱말로 «내 판정 기록»을 찾는다  -> 이미 판정됐나
내 원칙  하루 전~한 달 전 «내 문장»과 대조한다    -> 내 결론이 내 원칙과 어긋나나
```
그리고 «대리»를 성질로 읽지 않는다.
```
「상수 이름이 없다」 ≠ 「상한이 없다」   ·   「기제가 있다」 ≠ 「이 키에 닿는다」
주장은 «성질»로 적고, 잰 것이 «대리»면 그 자리를 «연다»
```
## 방아쇠 하나 더 — 「지금 있는 동작을 «바꾸는» 판정을 내려는가」
```
그 동작이 «왜 그런지»의 판정을 찾는다. 검색어는 «기제 이름»이 아니라 «그 동작의 낱말»이다
찾았으면 지시문에 «인용»한다. 뒤집는 것이면 «뒤집는다고 적는다»
   소유자가 승인한 판정이면 «내가 뒤집지 않는다» — 수와 함께 올린다
못 찾았으면 「못 찾았다」를 적는다
찾은 «뒤»가 두 번째 함정 — 그 판정의 «무대»(어느 프로세스·어느 경로)를 확인하고 인용한다
```
훑기 자체도 이 순서다: **판정 먼저 -> grep 으로 검증.** 지시서에도 같은 줄을 건다.

---

# 조사 상설 — 손대기 «전»에 문서, 그다음 grep 검증

```
무엇을 찾기·판정하기 «전»   CODE_MAP 의 «해당 절» -> 주제 문서 -> SYSTEM_OVERVIEW
grep 으로 «더듬는» 것을 1순위로 쓰지 않는다. 문서에 없을 때의 2순위다
«둘 다» 여야 한다 — 하나만 하면 각각 다른 방식으로 틀린다
   grep 만  구조를 몰라 헛짚는다. 뜻이 겹치는 두 칸에서 «코드가 안 걷는» 쪽을 읽는다
   문서 만  낡은 문장을 «전제»로 지시서를 쓴다
```
```
금지  grep 히트를 보고 「없다/있다」 — 이름이 바뀌었거나 데코레이터가 등록하거나 다른 곳에서 부른다
금지  문서 문장을 인용하고 끝 — 인용은 근거가 아니다. «오늘도 참인지»가 근거다
채택  문서에서 «어디»를 알고 -> 그 자리를 «코드로» 확인 -> 어긋나면 «그것이 발견»이다
「소비자 0」을 셀 때 네 갈래를 먼저 뺀다 —
   데코레이터 등록 · 시험만 쓰는 것 · 설정에 «문자열»로 있는 것 · «명령줄이 이름을 드는» 것
```
걷기를 말하거나 손대기 «전»에는 `docs/architecture/WALK.md` 를 연다.

---

# 응답 상설 — 근본적인 아키텍처에서 «모든» 응답을 한다

```
구조   그 기제가 «어떻게 지어져서» 그 일이 나는가 — 설계·계약·선언이 답이다
상태   지금 이 박스에 무엇이 켜져 있나 — 답이 «아니다». 답의 «사례»일 뿐이다
판별식  「이 답이 «다른 설치»에서도 참인가」. 아니면 그건 이 박스 상태다
```
토글·행 수·설정값이 답의 «주어»로 들어가면 그 자리가 틀린 것이다.
고치는 것도 같다 — 박스를 고쳐서 되게 만들지 않는다. 고칠 것은 «기제»다.
구조로 답이 안 나오면 «모른다»고 말한다. 상태로 «대신» 답하지 않는다.
```
답의 모양 셋
   찾은 것    구조의 낱말로. 「무엇이 이것을 막을 수 있나」
   해결 여부  «내가 직접 잰» 것만 「해결」. 남의 보고는 근거이지 판정이 아니다
   모르는 것  «반드시» 적는다. 특히 「셀 수가 «없는» 것」
```

---

# 측정 상설 — 이 박스의 수를 «운영에 대한 주장»으로 내놓지 않는다

이 박스는 만들고 확인하는 곳이다. **안 재는 것은 더 나쁘다.**
다만 그 수가 «무엇에 대한 문장인지»를 같이 적는다.
```
증거   선언 · 코드 · 소유자가 넣은 값          -> 운영에도 참이다
박스   내가 만든 씨앗 · 내가 넣은 설정 위의 수   -> «밝히고» 쓴다. 운영 주장으로 승격시키지 않는다
금지   「이 박스에 없다」 -> 「없다」. 내가 «안 넣은» 것이다
경계는 «저장소에 커밋됐나»다. gitignore 된 라이브 설정은 운영에 대해 아무 말도 안 한다
```
```
규칙이 아니라 «도구»로 가른다 — 「몇 개 · 없음 · 아무도 안 씀 · 호출자 0」을 말하기 «전»
   기본 도구는 `git grep` — 추적 파일만 본다
   평문 grep 을 썼으면 `git check-ignore -q <경로>` -> BOX 면 «이 박스»를 붙이거나 말하지 않는다
   소유자가 「운영에서 X 했다」고 하면 박스에서 센 X 의 «부재»는 그 자리에서 무효다
「기제가 있다」≠「돈다」. 그리고 반대도 참이다: 「이 박스에서 안 돈다」≠「고장」
```
```
운영 규격 — 박스를 «운영 모양»으로 만들고 거기서 잰다
   체인 한 트랜잭션 «수천 행» · 맵 «20x20» · 동시 사용자 «~10»
   타겟  체인 그룹 1,000 행 ≤ 5 s (IO+배관). 매퍼 고유 로직은 규격 밖. IO 는 ≤ 1.3 s/1k
   수를 말하기 전: 「이 박스가 «그 규격»으로 돌고 있었나」
```
```
운영의 핵심 제약은 «소급 비용»이다
   선언 한 줄·스키마 한 칸을 바꾸면 «수천 행짜리 체인»과 «원장 소급»이 따라온다
   목표  바꾸기 «전»에 「무엇이·몇 행이·얼마나」 다시 도는지 말하고,
        범위를 «바뀐 것만»으로 좁히고, 되돌릴 수 있게
   판별식 새 줄·새 설계마다 「이것이 «변경 비용»을 줄이나 늘리나」. 늘리면 그것이 결함이다
   성능 마진은 «넉넉하게» — 요청·커밋 경로에 «인라인 금지»,
        뒤따르는 일은 «페이싱된 별도 작업», 배치는 «한 번에», 큐 깊이는 «값으로 보임»
```

---

# 아키텍처 상설 — 엔티티 · 어휘 · walk. 이게 끝

```
엔티티  선언된 «노드 타입». 노드는 이것뿐이다
어휘    선언된 «술어». 엣지는 이것뿐이다
walk    데이터에 답하는 것은 이것뿐이다
```
```
그래서 «없는» 것
   투영이 지어낸 노드 타입·엣지 · 노드 종류 축 · id 접두어 여럿
   키를 받는 데이터 라우트 · 코드에 박힌 도메인 낱말
새 것을 만들기 «전»에
   이건 술어인가?  -> 어휘에 «선언»한다
   아니면 노드인가? -> 엔티티로 «선언»한다
   둘 다 아닌가?   -> «만들지 않는다». 투영이 지어내는 순간 선언이 거짓이 된다
「지울 수 없다」가 나오면 그것이 하드코딩이다
```
## 술어가 아닌 것은 «표면적으로» 노드로 처리한다
```
술어는 «엣지»다. 그 외 전부(개체·발견·값·수량·묶음·액션)는 표면에서 «노드»다
저장 모양은 상관없다 — 그건 배관이고 표면의 낱말이 아니다
표면이다      드롭다운 · 마킹할 수 있는 것 · 화면이 부르는 이름 · 부품이 선언하는 타입
표면이 아니다  응답의 배관 칸 (지워도 된다는 뜻이 아니다 — 안 보일 뿐)
```
```
사용자가 고르는 축은 «노드 타입 하나». 술어는 follow 로만 고른다
투영이 돌려주는 모든 것에 «도메인 type» 을 붙인다
「배율·보기 방식」을 «타입»으로 만들지 않는다
새 축을 만들기 «전»에 묻는다: 이게 술어인가? 아니면 노드여야 한다
```
목적은 «걷기로 확장»하기 위해서다 — 노드면 걸어 나갈 수 있고, 종점이면 붙일 자리가 없다.

---

# 데이터 상설

## 마킹은 질의의 «주어»다
```
마킹   부호 붙은 «노드 집합». 화면 상태가 아니라 walk 의 «시작점»
데이터  그 노드에서 «걸어서 닿는 하위 그래프» 그 자체
부품   선언하는 것은 둘뿐 — { start = 읽을 마킹, collect = 무엇을 걷나 }
저장소  바깥에 «하나». 그 안에 «이름 붙은 마킹 여럿». 부품은 읽을 이름·쓸 이름을 «선언»한다
체인   마킹1 --walk--> 서브그래프 --찍기--> 마킹2 --walk--> ... 계속. 체인 길이는 «선언»이다
차트   그 서브그래프를 «보는 창». 창은 «가져오지 않는다» — 고른다
```
```
어기는 셋 — 전부 「돌지만 틀린」 모양
   부품이 «거른다»     거르는 것은 walk 이 할 일이다. collect 를 고쳐라
   라우트를 «더 판다»   늘어야 하는 것은 «선언»이지 갈래가 아니다
   마킹을 «하나»로 둔다  맵에서 점을 찍는 순간 후보 선택이 날아간다
요청 수는 화면 탓이 아니라 «인자 모양의 결과»다 — 키를 받는 라우트는 키마다 불린다
```

## 표에 원천 데이터를 넣고, 그걸로 원장
```
원천  «표»에 행으로. 사람이든 씨앗이든 외부 시스템이든 전부 여기로
선언  ledger_config 의 «소스»가 그 표를 읽는다
원자  번역기가 만든다. 원장에 «직접» 쓰는 길은 없다
채택  표 -> 소스 선언 -> 번역기 -> 원장    선언이 아는 원자. walk 이 걷는다
금지  스크립트 -> write_batch -> 원장      선언이 «모르는» 원자. 원장에 있는데 못 걷는다
새로 write_batch 를 부르고 싶어지면 «멈추고 올린다». 답은 대개 「표를 만들어라」다
```

## 길이 0 인 문자열은 NULL 이다
```
''(strip 뒤 길이 0)은 «부재»다. 자리마다 판단하지 않고 «한 함수»가 접는다
키를 «견주는» 자리(조인·판단키·컴포짓·기존 행 찾기)에서는 NULL = NULL 이다
키 컬럼의 «부재» 행을 버릴지는 카탈로그 «한 칸»이 답한다 — 제품이 추측하지 않는다
```

## 원장 선언은 «국소적·무계산» 번역만 한다
```
국소적  원자 하나는 «분자 하나»(행 하나 또는 group_by 로 묶인 행들)에서만 나온다
무계산  값·수식어는 컬럼 값 «그대로»이거나 상수다. 변환·집계·조인·파생은 없다
그 밖   체인이 «표에 쓴다». 원장은 그 표를 읽는다
판별식  분자 하나에서 나오나 · 투영·상수뿐인가
       둘 다 예 -> read/filter/unit/select/emit 중 «어느 칸»인지 대응한다
       하나라도 아니오 -> «체인의 일»이다. 선언을 프로그래밍 언어로 만들지 않는다
```

## 단위는 «웨이퍼», 랏은 값

## 스키마는 «수학적으로 완전한 집합»으로
```
사실 공간의 «축»을 먼저 열거한다 (정체성·속성·존재·시간·출처·변경·삭제·단위·결정단위·세대 …)
축마다 칸의 상태는 셋 중 하나 — 선언 칸 있음 · 기본값이 덮음(값을 «적는다») · 없음 = 결함
닫힘  새 경우가 오면 «기존 칸에 대응»돼야 한다. 대응이 안 되면 «축이 빠진 것»이다
동결의 관문은 그 표다 — 「없음」이 0 이어야 동결이다
```

---

# 방법 상설

## 한 축은 한 칸·한 함수 — 자리 «전수 실측» 뒤에
```
규칙을 «두 줄»로 적는다 — 사용자가 외울 수 있어야 규칙이다
그 축을 만지는 자리를 «전수» 센다 (떠올린 자리가 아니라 «센» 자리)
자리마다 오늘의 답을 «실측» 표로 — 자리마다 다른 답이 곧 병이다
칸 «하나» + 함수 «하나»로 접고, 자리 «전부»가 그것을 지나게 «한 커밋»으로
게이트 = 같은 픽스처가 «모든» 자리에서 같은 답
```

## 근원 템플릿 요소 개발 후 «데이터 갈아끼우기»
```
같은 종류가 «둘째»로 필요해지면 두 번째를 손으로 그리지 않는다.
첫째를 템플릿으로 올리고 둘 다 «선언»으로 만든다
템플릿  모양   ·   선언  값   ·   시험  같은 화면에 둘을 다른 선언으로 앉혀 간섭 없음
「지금 이미 둘」일 때 만든다. 나중에 여럿일 «것 같아서» 층을 만드는 것은 이 규칙이 아니다
```

## 파일에 찍는 «수»는 손으로 적지 않는다
```
판별식  「이 파일에 내가 «타이핑한» 숫자가 있나」 — 있으면 그것은 «주장»이다
시각    자리표시자를 두고 «같은 호출에서» 치환한다
해시    커밋한 «같은 호출»에서 git 으로 읽어 인용한다. 기억에서 적지 않는다
줄 번호  공유 트리의 줄 번호는 «유통기한»이 있다. 위치 말고 «술어»를 적는다
개수    안 셌으면 적지 않는다
기계가 안 주면 사람이 «지어낸다». 그리고 지어낸 것은 «그럴듯»해서 검토로 안 걸린다
```

## 은퇴 — 축과 값을 «같이» 죽이지 않는다
```
「이 이름이 «두 뜻»인가」      두 뜻이면 «틀린 뜻 하나»만 은퇴시킨다. 이름째 지우지 않는다
「이 인용의 «주어»가 무엇인가」 인용으로 무언가를 지울 때 그 «주어»를 적는다
「이 축이 «도메인 낱말»로 서 있나」 배관 낱말로만 표현된 좋은 축은 배관과 «같이 죽는다»
은퇴시켰으면 그것이 «아팠던 증상»을 대조군으로 남긴다. 되돌리는 순간 그 줄이 먼저 운다
은퇴시켰으면 그 «약속»을 든 자리를 «전수»로 센다 — 주석·독스트링·RUN.md·guide·sample
   기제는 지워지고 «말»은 남는다. 남은 말은 그 기제가 아직 있다고 말한다
```

## 모든 제안 «전» walk 으로 해결 가능한지 본다
```
「이 질문이 «씨앗 + follow + 방향»으로 이미 답해지나」
예     -> 제안은 «선언 한 줄»이거나 «화면 한 곳»이다. 원장은 안 건든다
아니오  -> «어디서 끊기는지»를 수로 적고 나서 제안한다
「다른 질문이니 새 술어가 필요하다」는 «머릿속 구분»일 때가 많다. 걸어 봐야 안다
```

---

# 시험 상설

## 잘라쓰기 하니스 «절대 금지»
```
하니스·계약은 대상 모듈을 «import» 한다. 그것뿐이다
금지  파일을 텍스트로 읽어 함수 본문을 잘라 vm 에 넣는 것
금지  정규식으로 상수·함수를 뽑아 쓰는 것
사유  잘라쓰기는 «동작»이 아니라 «글자 모양»을 잰다
     -> 코드가 맞는데 빨개지고, 틀렸는데 초록일 수 있다
대상이 import 가 «안 되면» 그것이 «고쳐야 할 결함»이다 — 재려는 로직을 import 되는 모듈로 뺀다
```
```
덧붙이기(원본 «전문» + 접근자 블록)는 잘라쓰기가 «아니다» — 잘라내는 양이 0 이다
   다만 «다리»다. 도착지는 「import 되는 모듈로 빼기」다
「텍스트가 «대상»인 하니스」(드리프트 오라클)는 이 규칙 «밖»이다
   판별식: 텍스트가 «주어»인가 «대리»인가. 그리고 이 판별식은 «단언» 단위다. 파일 단위가 아니다
```

## 계측 — 진단기는 자기가 «진단하는 것»을 죽일 수 없다
```
관찰용 코드는 «자기만» 침묵한다
   자기가 «소유하지 않은» 상태를 읽지 않는다. 감싸여 있고, 터지면 «무엇이 조용해졌는지»를 한 줄 찍는다
   판별식: 「이 줄이 터지면 무엇이 «안» 되나」. 답이 «관찰 말고 다른 것»이면 그 자리가 틀렸다
박스가 «도달 못 하는» 조건에서만 도는 코드는 시험이 «0» 이다
   그 조건을 «강제로» 만들어 한 번 돌리는 시험이 «같은 커밋»에
가설이 «두 번» 반증되면 가설을 멈추고 «전수 census» 를 시킨다
   세는 단위는 «조건»이다. 반증 둘은 대개 「이 subsystem 이 아니다」다
```
```
고친 것의 시험만 돌린다. 시험 개수도 무제한 증식 금지
시험은 자기가 «재던 코드»와 같은 커밋에서 죽는다
파이프에 가려진 게이트는 게이트가 아니다 — `pytest | tail && commit` 은 tail 의 0 을 읽는다
```

---

# UI 상설

## 모든 UI 는 «조립식»
```
클래스   생성자가 자기 mount 와 deps 를 받는다. «모듈 수준 상태 금지»
고유 div  부품마다 자기 div 하나. 남의 div 안을 그리지 않는다
그리드   화면이 그 div 들을 격자에 앉힌다. 배치는 부품 «밖»에 있다
마킹    어느 부품에도 속하지 않는다. 밖의 저장소 하나에 «이름 붙은 여럿»으로 산다
시험    같은 화면에 «두 인스턴스»를 놓고 간섭이 없어야 한다 — 이게 「끼워넣을 수 있다」의 정의
```

## 설명 문구 주저리주저리 «금지»
```
화면은 «보여» 준다. «설명»하지 않는다
문장이 길어진다 = 「화면이 안 보여 주고 있다」 -> «보여 주는 방식»을 고친다
판별식  「이 문장을 지우면 운영자가 «틀리게» 읽나」 -> 아니오면 지운다
남는다  단위 · 기준 시각 · 「이 수가 «무엇의» 수인가」 · 거절의 «사유»와 «다음 행동»
나간다  기능 소개 · 「~할 수 있습니다」 · 배경 설명 · 제목의 반복 · 내 «설계 의도»의 해설
번역체 금지 — 기호·짧은 영어·명사형
```

## 마진
```
부품 «사이»와 부품 «안»의 여백은 0 이 아니다 — spacing 토큰 «하나»로, 손으로 px 를 적지 않는다
표·SVG 는 상자 테두리에 «붙지 않는다»
판별식  스샷에서 글자가 선에 «닿아» 있으면 그 부품은 안 끝난 것이다
정본 스킬  `.claude/skills/ui-design-system/SKILL.md`
```

## UI/UX 는 지시받은 것 «이상»으로 제안서를 항상 같이 낸다
```
지시받은 것을 짓고 + «사용자 입장에서 더 있어야 할 것»을 «제안 표»로
제안의 모양  항목 · 왜(어떤 상황에서 막히나) · 크기. 짓지 «않는다»
판별식  「이 화면을 «5분» 쓰면 무엇이 짜증나나」를 라운드 «닫기 전»에 적는다
```

## 화면 라운드는 «열어 보고» 닫는다
```
목업과 화면을 «나란히» 띄워 «부품 단위»로 대조한다. 목록에 없는 차이는 눈앞에 있어도 안 보인다
어드민 화면 변경은 «병합 전»에 연다. 하니스 초록·빌드 0 은 「열기 경로」를 안 잰다
화면이 안 바뀌면 «번들 해시»부터 — 빌드했다고 로드된 건 아니다
인라인 스타일이 스타일시트를 이긴다 — 화면이 CSS 대로 안 나오면 마크업·JS 의 인라인부터
```

---

# 자잘한 규율

```
시험 해석기  conda env assy_manager 로만. 재기동도 env python 으로
커밋        `git add`·`git commit` 에 «경로»를 붙인다. -a/-A 금지
커밋 메시지  백틱이 들어가면 -F 로. heredoc 델리미터는 «항상» 따옴표로
푸시        커밋 뒤 push. 푸시할 때마다 «지금 돌릴 명령»을 저장소 루트 RUN.md 에
           내용: 돌릴 명령 · 그 답이 «무엇을 뜻하나» · 급할 때 끄는 스위치 · 재기동 뒤 볼 로그 줄
           답의 «뜻»이 핵심 — 같은 증상에 수리가 «정반대»인 경우가 이 제품에 있다
           지난 라운드 명령을 쌓아 두지 않는다. 이력이 아니라 «지금 할 일»이다
```
