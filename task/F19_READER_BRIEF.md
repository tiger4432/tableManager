# 지시서 — F-19 「같은 맵을 두 화면이 다르게 부른다」의 «읽는 쪽»

> 총괄 판정 29(변수 가름) · 32(나르개 ㉡) 이행. **작성: 응용 레인. 코드 0줄.**
> ⛔ 토큰(`GEOMETRY_CONFIRMED`)을 «가르지 않습니다» — 판정 29 가 막았습니다
> (`map_alignment.py:516·4751` 이 그것을 «신뢰 토큰»으로 읽어, 건드리면 정렬 동작이 움직입니다).

## 닫으려는 «한 문장»
> **같은 맵을 오버레이는 「확정됨」이라 그리는데 확정 워크리스트는 「pending」이라 그립니다.**
> 표지를 «찍은 것»과 확정 «행»을 만드는 것이 다른 경로이기 때문입니다. 오류는 «안 납니다».

## 이미 서 있는 것 (다시 짓지 마십시오)
```
서버   map_overlay.orientation_declaration 의 축 dict 가 `confirmed_by_person` 을 «답니다»
       (83e86115). 표지 안에 `confirmation_uid`(확정 «행»의 열쇠)가 있으면 참입니다.
       사람 경로만 그것을 싣고(frame_confirmation.py:638), 체인 맵퍼 둘은 «안 싣습니다»
       (dt_alignment_metadata_mapper.py:203 · core_alignment_mapper.py:221)
🔴 그런데 그 칸이 «클라에 안 닿습니다» — 아래 ①이 그 구멍입니다
```

## ① 나르개 — `maps[]` 에 «불리언 한 칸»  (서버)
```
어디    map_alignment.py:1060  axes = {"rotation": rot["source"], "side": side["source"]}
        -> 여기서 `confirmed_by_person` 이 «떨어집니다». 축 dict 에서 source 만 골라 담습니다
무엇    맵별 응답(`maps[]`, 지금 `declared_frame_source` 를 싣는 그 행)에 «불리언 하나».
        이름이 「사람이 확정했나」로 읽히게 (예: `frame_confirmed_by_person`)
🔴 «한 칸»입니다   dict 아님 · 사유 문자열 아님 · 사람 이름 아님
```
🔵 **`map_alignment.py:6057` 의 주석이 이 모양을 «지시»합니다** — 그 주석이 막는 것은
   「맵마다 «축 dict»」이고(40맵에서 6.5KB -> 11.2KB, +72%), 마지막 줄이 「어느 맵인지는
   `maps[].declared_frame_source` 가 이미 말한다」고 «맵별 사실의 자리»를 지목합니다.
⛔ `axis_sources` 집계는 «그대로» 두십시오. 그건 단위 수준이고 F-19 는 «맵 단위»라 안 닫힙니다.

## ② 읽는 쪽 — `client2/src/map2/**`  (클라·맵)
```
자리    map2/decode.js:386   `declaredFrameSource` 를 푸는 그 줄 «옆»
        map2/main.js:2249    confirmed_candidate_id = declaredFrameSource === CONFIRMED ? … : null
무엇    「확정」 배지를 «사람이 확정한 맵»에만 답니다.
        체인 표지 맵은 배지를 «안 답니다» — 프레임 «값»은 그대로 그립니다(값은 참입니다)
```
🔵 **그릴 자리가 «이미 있습니다»** — `main.js:2241-2247` 주석이 「its own field, read only by
   the label」이라 적어 두었습니다. 새 칸도, 새 화면도 필요 없습니다.
⛔ `stored_candidate_id` 로 접지 마십시오 — 같은 주석이 「그러면 그리는 자리가 옮겨간다」고 적습니다.

## 게이트
```
① 가름     사람 표지 맵 -> 「확정」 배지 «있음» · 체인 표지 맵 -> «없음»
           (두 표본이 «다른 갈래로만» 잡히게 — 같은 답을 내는 표본은 판별식이 아닙니다)
🔴 ② 무회귀  `GEOMETRY_CONFIRMED` 가 «오늘과 같은 맵»에 붙는다 —
           🔴 사람 표지 맵 «과» 체인 표지 맵 «둘 다»로 확인. 한쪽만 보면 못 봅니다
🔴 ③ 페이로드 그 칸이 «불리언»이고 «맵당 하나»다. dict 로 자라면 :6057 이 막는 모양이 됩니다
④ 읽는 쪽   «수»를 보고에 적으십시오 (오늘: `confirmed_by_person` 독자 «0»)
```
🔴 **멈춤** — 화면이 배지를 그리는 자리가 «생각과 다르면» 거기서 멈추고 올리십시오.
   그건 「읽기」가 아니라 「만들기」이고 크기가 다릅니다.

## 안 하는 것
```
토큰 가르기 · axis_sources 확장 · 체인 맵퍼 손대기 · 표지 개명 · 설명 문구
```

---

# 지시서 ②(같은 배) — S-19 「이 원점이 «무엇 위에» 섰나」

> 🔴 **번호 정정 (총괄, 07:1x).** 이 절은 «S-19» 입니다. 제가 처음에 붙인 «S-16» 은
> `task/UNCALLED_ROUTES.md:292` 에 «이미» 있던 번호입니다(큐 패널의 「반영됐나 · 언제 것인가」).
> 제 실수이고, 이 줄을 «내용» 안 건드리고 머리글만 고칩니다 — 따라가면 «다른 줄»에 도착합니다.

> 총괄 판정 36(등급 2) · 37(새 뜻이면 새 토큰) 이행. **작성: 응용 레인. 코드 0줄.**
> 🔴 **위 F-19 칸과 «한 커밋»입니다** — 같은 `maps[]`, 같은 게이트.

## 닫으려는 것
```
지금   `[OriginBox]` 폴백이 «조용합니다» — 유효 다이 마스크에 격자 안 셀이 «0» 이면 원점 상자가
      웨이퍼 원으로 물러나고(map_overlay.py:1219), 좌표는 «그대로», 응답에는 «아무 말도 없습니다»
그리고 `box_aware_origin` 이 거짓이면 확정 기록에 `valid_die_ref` 를 «안 찍습니다»(map_alignment.py:802)
      🔴 그 자리 주석: 「안 쓰였다는 사실은 «로그»가 말한다」 -> 빠뜨림이 아니라 «결정»입니다
=> 그러므로 이 작업은 「고치기」가 아니라 «그 결정을 뒤집는 것»입니다. 지시서에 그렇게 적습니다
등급   «2 (안 들린다)». 화면이 «거짓을 말하는» 게 아니라 «말할 칸이 없습니다»
      🔴 뒤집는 조건: 어느 화면이 그 원점을 「마스크 기준」이라 «이름 대면» 그날 등급 1 이 됩니다
```

## ① 서버 — «새 토큰» 하나 + `maps[]` 한 칸
```
자리   map_alignment.py:744·753 (`confirmed_meta_for` 안 -> «맵 단위») -> 나르개는 `maps[]`
값    최소 둘: 유효 다이 마스크 위에 섰나 · 웨이퍼 원으로 물러났나
🔴 «맵당 한 칸»입니다   dict 로 자라면 `:6057` 이 막는 모양이 됩니다
```
### 🔴 상수 «옆»에 반드시 두 줄 — 없으면 다음 사람이 합칩니다
```
· `GEOMETRY_*` 가 «아닌» 이유   그건 「기하의 «출처»」이고 이건 「원점 상자의 «근거»」다
· `ALIGN_ORIGIN_*` 도 «아닌» 이유  그건 「«어떻게» 얻었나(방법)」이고 이건 「«무엇 위에» 섰나(기준)」다
   -> 같은 값에 «둘 다 참»일 수 있으므로 다른 뜻이다
⚠️ `ALIGN_ORIGIN_UNRESOLVABLE` 을 «재사용하지 마십시오». 더 이상 발화하지 않아 빈 자리처럼
   보이지만(map_overlay.py:84-85), 채우면 «죽은 낱말에 새 뜻»을 얹는 것이라 더 나쁩니다 —
   grep 에는 «살아 보입니다». 그것이 이 줄이 여기 적힌 이유입니다
```

## ② 화면 — `map2/**` (남의 파일이라 «읽기»만 지시)
```
원점이 «물러난» 맵에 그 사실을 그립니다. 좌표는 «안 건드립니다» — 값은 오늘과 같습니다
⛔ 「마스크 기준」이라고 «이름 대지» 마십시오. 그게 이 줄을 등급 1 로 올리는 그 문장입니다
```

## 게이트 (두 칸 «모두»에)
```
① 가름     폴백한 맵과 «안 한» 맵이 다른 답을 낸다 (같은 답을 내는 표본은 판별식이 아닙니다)
🔴 ② 무회귀  좌표·프레임·원점 «값»이 오늘과 같다. 이 라운드는 «말하기»이지 «바꾸기»가 아닙니다
🔴 ③ 모양    «맵당 한 칸». dict 가 되면 `:6057` 이 막는 모양입니다 — `confirmed_by_person` 도 «같이»
④ 읽는 쪽   «수» + «도달 가능»(번들에서 «축약에 안 죽는 문자열»로)
```
🔴 **멈춤** — 폴백이 «셋째»로 나오면 세고 멈추십시오. 제가 잰 것은 둘입니다
(`map_overlay.py:1219` · `map_alignment.py:802`). `map_alignment` 전수는 «안 훑었습니다».

---

# 지시서 ③(같은 배) — S-19 «화면 절반» 「원점이 «물러난» 맵을 그린다」

> 총괄 판정 36(등급 2) · 48(값 셋) 이행. **작성: 응용 레인. 코드 0줄.**
> 🔒 예약: 줄 **S-19 화면 절반** · 파일 `client2/src/map2/**` · 술어 「`origin_basis` 를 읽는 자리 «0»」
> 🔵 서버 절반은 «닫혔습니다** (`02f51b92`, 총괄 직접 13 passed).

## 서버가 «이미» 내는 것 — 다시 짓지 마십시오
```
자리   map_alignment.py:6056  maps[].origin_basis   <- 맵당 «문자열 하나»
값 넷  map_overlay.py:1217-1224 가 정본입니다
   mask           유효 다이 마스크가 상자를 정했다              (의도대로)
   circle         마스크가 «없어» 웨이퍼 원이 정했다            (🔵 정상이고 «흔하다»)
   mask_off_grid  🔴 마스크는 «있는데» 이 격자에 한 칸도 안 앉았다 -> 원으로 «물러났다»
   absent         격자가 없어 «상자 자체»가 없다
🔵 그 상수들이 «한 자리»에 사는 이유가 그 파일 주석에 적혀 있습니다 — 저장 경로(S-22)도 같은 것을
   씁니다. 클라에서 값을 «다시 철자하지» 마십시오
```

## ① 무엇을 그리나 — **`mask_off_grid` «하나»만** 그립니다
```
판별식(UI 상설)  「이 문장을 지우면 운영자가 «틀리게» 읽나」
   mask     지운다 — 의도대로 선 것이다. 말할 것이 없다
   circle   지운다 — «정상이고 흔하다». 그리면 대부분의 맵에 잡음이 붙는다
   absent   지운다 — 격자가 없다는 사실은 그 맵의 «다른 자리»가 이미 말한다.
            원점은 그 맵의 이야기가 아니다
   🔴 mask_off_grid  «남긴다» — 마스크가 있는데 «안 쓰였다». 운영자는 마스크 기준인 줄 안다
```
⛔ **「마스크 기준」이라고 «이름 대지» 마십시오** — 물러난 맵에 그 말을 붙이면 화면이 «거짓»을
   말하고 이 줄이 등급 1 로 올라갑니다 (판정 36).
✅ 말할 것은 「이 격자에 마스크가 «안 앉아» 원으로 섰다」 «한 줄»입니다. 단락으로 만들지 마십시오.

## ② «없음»의 뜻은 «하나» — 「옛 서버」
```
서버가 «모든» 맵에 싣기로 했으므로(판정 48 + absent 넷째), 칸이 «없으면» 옛 서버입니다
=> 그때는 «안 그립니다». 「0」도 「모름」도 «지어내지 않습니다»
   (S-13 의 `limits` 독자가 그렇게 지었습니다 — 같은 규율)
```

## ③ 어디에 붙나
```
디코드   map2/decode.js:386 근처 — `declared_frame_source` 를 푸는 «그 줄 옆». 맵당 한 칸입니다
그리기   그 맵의 행/배지 자리. 🔴 좌표·프레임·픽셀은 «한 글자도» 안 건드립니다 —
        이 라운드는 «말하기»이지 «바꾸기»가 아닙니다
```

## 게이트
```
① 갈림     `mask` 인 맵과 `mask_off_grid` 인 맵이 «다르게» 그려진다
           🔴 판별식 표본: 「기준이 풀렸는데 원점은 «원»에 선 맵」 — 두 값이 «같은 답을 내면»
              그 표본은 판별식이 아닙니다 (총괄이 서버 절반에서 쓴 그 표본입니다)
② 무회귀   `mask` · `circle` · `absent` 맵의 픽셀이 «오늘과 동일». 좌표 수치도 동일
③ 없음     칸이 없는 응답에서 «아무것도 안 그린다» (옛 서버)
④ 읽는 쪽  «수» + «도달» — 🔵 이번엔 «클라 값»이라 «번들»이 맞는 표면입니다.
           축약에 안 죽는 문자열로 재십시오 (`origin_basis` · `mask_off_grid` 같은 «서버 낱말»)
```
🔴 **멈춤** — 값이 «다섯째»로 나오면 세고 멈추십시오. 정본은 `map_overlay.py:1217-1224` 넷입니다.

---

# 지시서 ④ — S-9b 「계획이 «왜 이 프레임인가»를 못 보여 준다」

> 총괄 판정(C-3 라운드 뒤) 이행. **작성: 응용 레인. 코드 0줄.**
> 🔒 예약: 줄 S-9b · 파일 `server/transfer_plan.py` · `client2/src/transfer_plan.js`

## 🔴 먼저 — 이름이 닮았지만 S-19 와 «다른 물음»입니다 (합치지 마십시오)
```
S-19 `origin_basis`   「이 «원점 상자»가 무엇 위에 섰나」   mask · circle · mask_off_grid · absent
S-9b `frame_basis`    「이 «기준 프레임»을 무엇으로 골랐나」  confirmation · role_order
=> 둘 다 낱말이 basis 입니다. 재 보니 «다른 사실»입니다 — 한 칸에 담지 마십시오
```

## 실측 — 같은 함수, 두 호출자, «한쪽만» 나릅니다
```
만드는 곳   bonding_plan.canonical_basis(:261) -> `(meta|None, basis)`
           basis = {kind, roles, reason, …}  · 어휘는 «이미 있습니다»:
             BASIS_CONFIRMATION(:72) 「확정 기록이 기준을 지목했다」
             BASIS_ROLE_ORDER(:73)   「확정이 없어 역할 순서로 골랐다 (퇴화형)」
             STATUS_NOT_DECLARED     (기존 상수 재사용)
M1 (본딩)   bonding_plan.py:1086  `result["frame_basis"] = frame_basis`   <- «싣습니다»
🔴 M2 (전사) transfer_plan.py:1480  `meta, _basis = …`                      <- «버립니다»
호출자 전수  «둘»뿐입니다 (`canonical_basis(` 전수) — 버리는 자리는 :1480 «하나»
```
🔴 **그리고 클라는 `frame_basis` 를 «아예 안 읽습니다» — M1 것도 «0» 입니다** (client2/src 전수).
   그러므로 서버만 고치면 「발신 둘 · 독자 0」이 됩니다 — 오늘 여섯 번 닫은 그 부류입니다.
   ✅ 이 라운드는 «나르개 + 독자»를 «같이» 갑니다.

## ① 나르개 — 서버 «한 칸»
```
transfer_plan.py:1480 이 `_basis` 를 버리지 말고 M2 응답에 «M1 과 같은 철자»로 싣습니다: `frame_basis`
⛔ 새 키 이름 금지 — 같은 사실이 두 응답에서 다른 이름이면 그게 기준 ④ 입니다
```

## 🔵 [조정 09:2x · 판정 67] 알갱이는 «코어별»입니다
```
사실   M2 `_summarize_inline` 은 «테이프» 단위이고, `involved_cores` 를 돌며 코어마다 근거를 얻습니다
=> 철자는 `frame_basis` «그대로», 자리는 `by_core` «옆에» «코어별» (판정 48 「맵당 한 칸」과 같은 원칙)
⛔ 테이프에 «하나»로 접지 마십시오 — 코어마다 다른 길로 왔을 수 있고, 접으면 그 차이가 사라집니다
```

## ② 독자 — 클라 «한 줄», 자리는 «이미 있습니다»
```
자리   계획 사이드바의 «각주» 채널 — `transfer_plan.js:547-548`
      「기호는 한 글자다 … 기호가 같은 화면 안의 각주(`.tp-foot-note`)를 가리킨다」
      `inactive_subtractions` 가 이미 그 모양으로 «이름을 그대로» 인쇄합니다
무엇을 그리나   🔴 `role_order` «인 코어»만 그립니다 — 각주에 «코어 이름 + 서버의 reason·roles 그대로»
      confirmation 코어는 «조용합니다»
      🔵 코어 «전부»가 role_order 면 «나열이 아니라 「전부」 한 줄» — 각주가 목록이 되면 그것이 「주저리주저리」입니다
      판별식(UI 상설): 「이 문장을 지우면 운영자가 «틀리게» 읽나」
        confirmation -> 지운다. 의도대로 골린 것이고 말할 것이 없습니다
        role_order   -> 남긴다. «확정이 없어» 순서로 고른 것이고, 운영자는 그것을 모릅니다
⛔ 문장을 «짓지» 마십시오 — 서버의 `reason`·`roles` 를 «그대로» 인쇄합니다 (판정 33 과 같은 규율)
```

## 게이트
```
① 갈림     🔵 «같은 응답 안»에서 갈립니다 — `confirmation` 코어와 `role_order` 코어가 «다르게» 그려진다
           (계획 둘을 비교하는 것이 아니라 «한 계획의 코어들»입니다 — 판정 67)
           (두 값이 «같은 답»을 내면 그 표본은 판별식이 아닙니다)
🔴 ② 무회귀  계획의 «수»와 픽셀이 오늘과 동일 — 이 라운드는 «말하기»이지 «바꾸기»가 아닙니다
🔴 ③ 독자   «수 + 도달»을 «M1·M2 둘 다»에서. M1 의 독자도 오늘 «0» 이라 이 라운드가 그것도 닫습니다
           도달은 «번들»에서, 축약에 안 죽는 문자열로 (`frame_basis` · `role_order`)
```
🔴 **멈춤** — `canonical_basis` 가 셋째 `kind` 를 내면 세고 멈추십시오. 오늘 어휘는 «둘»입니다(:72-73).

---

# 지시서 ⑤ — S-5 「잘렸다」의 철자를 «하나»로, 그리고 «듣는 화면»을 붙인다

> 총괄 판정 66 이행. **작성: 응용 레인. 코드 0줄.**
> 🔒 예약: 줄 S-5 · 파일 서버 다섯 + `client2/src/truncation.js` 독자들

## 정본 모양 — «인용»입니다. 새로 짓지 마십시오
```
client2/src/truncation.js:72-75  `saysTruncated(said)`
   불리언이면 그대로 · 객체면 `Boolean(said.reason)` · 그 밖은 `null`(「말한 적 없음」)
=> 서버가 낼 것: `truncated` — 불리언 «또는» `{reason, …}`. 이 함수가 «둘 다» 이미 읽습니다
```

## 서버 절 — 철자별 처분 (자리 · 파일:줄)
```
① `*_omitted` -> 정본 «안»으로   `truncated: {reason, omitted: N}`  «지우지 않고 옮깁니다»
   chain_key_gate.py:181·183·201·210   ·   database/crud.py:214·238·272·275   ·   event_constants.py:169
   🔵 근거: 「몇 개 뺐나」는 「잘렸다」의 «수식어»이지 «다른 사실»이 아닙니다
② `changes_truncated` -> 정본 키로 «접힘»    chain_replay.py:103 (그 줄이 「`changes_truncated` says when it bit」)
③ `total_log_count`  -> «그대로»            chain_ingestion_worker.py:1147
   🔴 «절단 표지가 아닙니다»(분모) · 그리고 «독자가 하나 있습니다» — 건드리면 그것이 깨집니다
④ history 나르개 -> «같은 라운드 끝»에      bonding_plan.py:1027 (`q.limit(HISTORY_LIMIT)`) · :1072 (`"history": history`)
   🔵 이건 «진짜» 자리입니다 — 「50건이 전부」와 「최근 50건」이 오늘 «같은 그림»입니다
⏸ samples -> «별도 판정»                    chain_replay.py:379·392
   이름이 «samples» 라 「전부」를 주장하지 않습니다. 잘렸다고 말하게 할지는 «다른 물음»입니다
```
🔴 **반경 — 응답 «모양»이 바뀝니다.** 클라 독자가 0 이라고 안전한 것이 «아닙니다»:
   오늘 아침 이름 «하나»를 바꿔 CLI 둘과 시험 하나가 깨졌습니다. 게이트에 «표면 전수»를 답니다 —
   `server/tests` · `server/scripts` · `contracts/` · 하니스. 그리고 훑기는 «커밋 «전»».

## 클라 절 — 독자는 «하나»를 부릅니다
```
✅ 부를 것   `truncation.js` 의 `saysTruncated` — 새 독자를 «만들지 마십시오»
낱말        `walk/main.js:483` 의 「절단됨 · …」 과 «같게». S-13 화면이 이미 그 낱말을 씁니다
🔴 제약     `cutBudgets` 는 `walk/derive.js:79` 에 «삽니다» — 걷기 상자 «밖» 화면은 못 부릅니다
           ⛔ 복사하지 마십시오(사본 둘) · 옮기는 것은 «이 라운드 밖»입니다. 밖 화면은 `saysTruncated` 만 씁니다
```

## 게이트
```
① 철자 «하나»   서버 전수에 최상위 `*_omitted` «0» · `changes_truncated` «0»
② 그림         잘린 목록에 「잘렸다」가 그려진다 · 🔴 «안 잘린» 목록은 오늘과 «동일»
③ 독자         «수 + 도달»(번들 · 축약에 안 죽는 문자열) — 화면 «각각»에
🔴 ④ 값        정본은 «성공에도 옵니다» -> 「칸이 있나」가 아니라 «값»을 단언하십시오
               (`saysTruncated` 가 `null` 을 「말한 적 없음」으로 돌려주는 이유가 그것입니다)
```
🔴 **멈춤** — 다섯째 철자가 나오거나, 이 목록에 «없는» 라우트가 자르는 것을 그리는 화면이 있으면 세고 멈추십시오.

---

# 지시서 ⑥ — S-7 「왜 느린가」를 «운영자가» 볼 수 있게

> 총괄 지시(S-7) 이행. **작성: 응용 레인. 코드 0줄.** 줄이 짧아 «인용»으로 두고 실측했습니다.

## 실측 — 서버는 «문장까지» 만들어 두었고, 읽는 화면이 «0» 입니다
```
사유를 «만드는» 곳   value_suggest.py:842·854  `result["slow_reason"] = _slow_reason(...)`
   문장            :861 `f"응답이 {elapsed_ms}ms 걸렸습니다 (예산 {warn_ms}ms)"` (+ `— {advice}`)
   🔵 그 파일이 «왜»까지 적어 두었습니다: :97 「`slow_reason` is what makes it actionable」
   => 「사유 + 다음 행동」이 «이미» 있습니다 (「자막 단 실패도 실패다」의 요구를 서버가 «충족»)
🔴 독자             client2/src 전수 «0» · 번들 «0»
시간만 내는 곳       dt_map_derivation.py:705·812 · map_alignment.py:3950  -> `elapsed_ms` «만», 사유 «없음»
   그 독자          map2/decode.js:511 · main.js:2297 · view_model.js:783  -> «수»를 읽습니다 (번들 도달 ✅)
   ⚠️ 그 화면이 그 수를 «무슨 낱말»로 그리는지는 안 쟀습니다
```

## 갈래 — 라우트별로 «셋» (지시대로 갈라 적습니다)
```
① 발신 있고 독자 «0»   value_suggest      등급 «2 (안 들린다)» — 문장이 다 만들어져 화면 앞에 «놓여» 있습니다
🔴 ② 발신이 «절반»     dt_map_derivation · map_alignment
                     「얼마나」는 말하고 「왜」는 «안 말합니다». 수만 보면 운영자가 «행동할 수 없습니다»
                     등급은 ①과 «다릅니다» — 이건 「안 들린다」가 아니라 «안 실린다»입니다
③ 둘 다 있음          «없습니다» — 사유를 그리는 화면이 «하나도» 없습니다
⏸ rnd_board 자리      미룸 (해당 없음 — 위 셋에 rnd_board 는 «없습니다»)
```

## ① 의 수리 — 클라 «한 줄». 서버는 «안 건드립니다»
```
읽을 것   `slow_reason` — «있으면» 그대로 그립니다. 서버가 만든 문장이라 화면이 «짓지 않습니다» (판정 33 과 같은 규율)
언제      «성공에도» 옵니다(:141 「Above this measured cost a SUCCESSFUL answer still reports」)
          -> 「실패했나」가 아니라 «값이 있나»로 갈라야 합니다
⛔ 안 함   문구 새로 짓기 · `elapsed_ms` 를 화면이 «판단»해서 느리다고 말하기(그건 두 번째 저자)
```

## ② 의 수리 — 서버 «한 칸». 그리고 «같은 철자»로
```
`dt_map_derivation` · `map_alignment` 이 시간을 낼 때 «같은 이름»으로 사유를 답니다: `slow_reason`
⛔ 새 이름 금지 — 오늘 S-5 에서 「잘렸다」의 철자 넷을 접었습니다. 「느리다」를 둘로 만들지 마십시오
🔵 임계값은 그 자리의 «선언»입니다 — value_suggest 가 `warn_ms` 를 쓰는 그 모양을 «인용»하십시오(:141 근처)
```

## 게이트
```
① 갈림     느린 응답과 안 느린 응답이 «다르게» 그려진다 (안 느린 쪽은 오늘과 «동일»)
② 문장     화면이 서버 문장을 «그대로» 낸다 — 클라에 문구 리터럴 «0»
🔴 ③ 값     `slow_reason` 이 «성공에도» 오므로 「있나」가 아니라 «값»을 단언
④ 독자     «수 + 도달»(번들 · 축약에 안 죽는 문자열 — `slow_reason` 은 서버 키라 삽니다)
🔴 ⑤ 철자   저장소 전수에 「느리다」의 철자가 «하나» (`slow_reason`)
```
🔴 **멈춤** — 시간을 재는데 사유를 «못 붙이는» 자리가 나오면(임계값을 정할 선언이 없는 곳) 세고 멈추십시오.
