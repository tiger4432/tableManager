# 📋 디자인 세션 지시 — 지금 할 것 (총괄 → 디자인 세션, 단일 정본)

> 채널: **파일과 커밋**입니다. 당신은 `design` 브랜치의 워크트리에서 일합니다.
> ```
> 총괄 → 당신    task/DESIGN_ORDERS.md          «main» 에 올립니다
>                받는 법:  git fetch origin && git merge origin/main
> 당신 → 총괄    task/design_session_report.md   «design» 에 커밋 + push
> ```
> 인수 지시문: `task/CLIENT_SESSION_BRIEF.md` (`faf8d8bd`) — **§1 워크트리부터** 읽으십시오.

---

# 🔴🔴 소유자 판정 «셋» 도착 (18:4x) — 각 레인의 다음 라운드입니다

> 소유자 원문: **「키는 노드 아이디와 노드 타입, 2번 b, 3번 예」**

```
① mark_key = «노드 id + 노드 타입»      (내 추천에 «타입»을 더하셨습니다)
② transfer 엣지 = ⓑ                     풀린 웨이퍼/다이로 «바로» 엣지. 그릇은 개체로 «안» 세움
③ processed_with 선언 = «예»
```
🔴 **①에 「노드 타입」이 붙은 것이 중요합니다** — 키가 (id, type) 이면 소비자가 «무엇을 찍었는지»를
   풀어 보지 않고 압니다. 그리고 두 타입이 같은 id 문자열을 가져도 «안 부딪힙니다».
   지금처럼 「experiment-unit:v1:…」 하나로 뭉뚱그리는 것과 정반대 방향입니다.

## 배정 — 구현자가 정지 중이라 이렇게 가릅니다

### 응용 (이음새) — ① 마킹 키. **가장 큽니다**
```
왜 당신    서버(트렌드가 mark_key 를 냅니다)와 클라(패널이 그걸 비교합니다) «양쪽»입니다
           그리고 `task/MARKING_CONTRACT.md` 가 당신 문서입니다
할 것      키를 (노드 id, 노드 타입) 으로. 계약 문서 먼저 고치고 그다음 구현
🔴 게이트   «수»가 아니라 «여정»입니다. 총괄이 직접 누릅니다:
              트렌드 점 클릭 -> 머리·맵 «둘»이 그 씨앗으로 따라오는가
              후보 행 클릭  -> 마킹2 · 순위 강조 · 후보 트렌드
           둘 중 하나라도 끊기면 «착지 금지». 오늘 소유자 원문: 「마킹 한번 꼬이면 답없다」
⚠️ 착수 전  지금 키를 «읽는 자리»를 전수하십시오. 오늘 제 훑기가 오탐 둘·누락 하나를 냈습니다 —
           낱말이 아니라 「마킹 키를 만들거나 비교하는 자리」로 세십시오
```

### 총괄 (나) — ②③의 «선언». 지금 씁니다
```
② dt_log 4,669  transfer 엣지를 ⓑ 모양으로. `dt_job` 이 이미 그 표를 읽고 있으므로 선언으로 닿습니다
   ⚠️ 정본(dt_transfer_log)의 바인딩을 «복사하면 0행»입니다 — 그 표엔 core_wafer_id·c_wx 가 없습니다
      dt_log 에 «있는 것»: dt_job 100% · core_x·core_y 100% · core_wafer 81%
   🔴 그리고 core_wafer 가 없는 19% 는 «엣지를 안 만듭니다». 추정으로 채우지 않습니다
③ processed_with 술어 선언. value 노드가 여기서 1:1 로 나옵니다
```

### 클라 (디자인) — ①의 «클라 쪽». ①이 계약을 고친 «뒤»
```
지금 할 것  없습니다. ① 계약이 확정될 때까지 마킹 관련 코드를 «건드리지 마십시오»
            (양쪽이 동시에 키 모양을 바꾸면 오늘 하루가 헛돕니다)
그 사이     「예산에서 끊김」 배너 두 줄 (api.js:487 · 빈 뷰모델 :433 · 패널의 `!m.complete`)
            -> 데이터 오기 «전»에 뜹니다. 응용이 진단만 넘겼습니다
```

## 순서 — 겹치지 않게
```
1  총괄이 ②③ 선언을 씁니다 (지금)          -> lc.load() 통과가 착지 조건
2  응용이 ① 계약을 고칩니다 (지금, 병렬)     -> 파일이 안 겹칩니다
3  ②③ 백필은 «선언이 선 뒤». 구현자가 살아나면 그쪽, 아니면 응용
4  클라는 ① 확정 뒤
```
📌 그리고 구현자가 살아나면 «서버 파일 반납»부터입니다 — 응용이 목록을 적어 뒀습니다.

# ⚖️ 판정 — **후보는 «있습니다». 화면이 비는 것은 «예산»입니다.** 오늘 저녁에 살릴 수 있습니다 (총괄 17:0x)

응용이 자기 증거를 무너뜨린 것(「내 측정이 «내가 방금 올린 수정을 안 실은 서버»를 지났다」),
그게 이 답을 열었습니다. 제가 지금 쟀습니다:
```
SYN-CX-BW-001   limit=400    trunc=«nodes, claims»   ranked «0»    <- 화면 기본값이 여기입니다
                limit=1000   trunc=None              ranked «21»   ← 1위 「void · void_formation」
SYN-BW-103-11   limit=400    trunc=None              ranked «25»   ← 1위 「delam · delam_formation」
```
🔴 **후보 21개가 «이미 있습니다».** 오늘 오후 제가 「재료가 없다」고 보고한 것이 두 번째로 틀렸고,
두 번 다 «끊긴 걷기»를 «부재»로 읽은 것입니다.

## 할 것 — 그 두 패널의 «예산을 선언»하십시오
```
어디    후보 목록 · 순위 패널의 선언 (경계는 이미 node_limit·hops 를 나릅니다 — 9fbf1f09)
무엇    이 걷기가 400 을 넘습니다. 구성이 스텝 267 개를 물고 오기 때문입니다
        -> 그 패널의 선언에 «넉넉한 node_limit» 을 적으십시오
🔴 컨트롤이 «아닙니다»   버튼·슬라이더·자동 재시도 금지 (오늘 이미 그은 선입니다).
        «선언된 값» 하나입니다
게이트  후보 «21» · 순위 21행 · 1위가 「void · void_formation」
        그리고 trunc=None 이면 「예산에서 끊김」 문구가 «안 떠야» 합니다
```

## 📎 그리고 그 「끊김」 문구 — 응용이 두 줄로 좁혔습니다. «클라 파일»입니다
```
서버    complete = not(depth|node|edge|claim|action cut) — «정직합니다». 고칠 것 없음
클라    api.js:487  없는 prop.complete 를 «false» 로 접음
        빈 뷰모델 :433 이 null 로 시작 · 패널이 `!m.complete` 로 시험
        -> 🔴 «데이터가 오기 전에» 배너가 뜹니다. 「없음」을 「고장」으로 읽는 오늘의 그 부류
```
응용이 「클라가 그 디렉터리에서 일하는 중이라 경쟁하지 않고 진단만 넘긴다」고 한 것,
**맞습니다.** 클라 레인이 그 두 줄을 고치십시오 — 예산 선언과 «같은 라운드»에.

📌 오늘 이 자리에서 «셋»이 같은 실수를 했습니다 — 저(두 번) · 응용(한 번).
   전부 「끊긴 답」 또는 「안 실린 수정」을 «사실»로 읽은 것입니다.
   **재기 전에 「무엇이 도는가」와 「끊겼는가」를 먼저 본다** — 오늘의 규칙입니다.

# ⚖️ 판정 — **제 ③ 철회. 그리고 ⓐ·ⓑ 둘 다 «아직» 아닙니다 — 울타리가 지키는 것이 «틀린 것»입니다** (총괄 16:1x)

라우트에 «직접» 물어 반박한 것, 그게 맞습니다. 제가 같은 두 호출로 재확인했습니다:
```
① context_fields 를 빼도  ->  422 expected ["wafer", «"bonding_leg"»]
                              (즉 leg 요구는 클라의 context_fields 가 아니라 «집계 단위»에서 옵니다)
② aggregation_unit 변경   ->  422 「아직 고정이다 (마킹 계약)」 fenced_to «void_by_experiment_unit»
③ ①②만 넣은 선언         ->  200 · 점 72 · 0인 점 «72/72»   (당신 실측, 그대로입니다)
```
**제 ③(「클라가 컨텍스트 요구를 뺀다」)은 클라 레인에서 닫히지 않습니다. 철회합니다.**
그리고 **no-op 커밋을 남기지 않은 판단이 맞습니다** — 답이 안 바뀌는 변경은 「고쳤다」로 읽힙니다.

## 🔴 그런데 ⓐ·ⓑ 둘 다 아직 아닙니다. 울타리를 «왜» 세웠는지가 문제입니다
```
mark_key 실측   "experiment-unit:v1:" + base64(["bonding_experiment_unit", …])
                -> 🔴 마킹 키가 «집계 단위»에서 파생됩니다
그래서 울타리    단위를 바꾸면 mark_key 의 이름공간이 바뀌고, 트렌드가 찍은 것을
                맵이 «못 알아봅니다». 울타리는 그걸 막고 있습니다 — 목적은 «옳습니다»
```
```
ⓑ 단위를 하나 더    -> mark_key 이름공간이 «둘»이 됩니다.
                      소유자 「마킹 한번 꼬이면 답없다」의 정확한 자리입니다. 지금은 안 됩니다
ⓐ 울타리를 연다     -> 같은 이유로 더 위험합니다
```

## 🔴 그래서 진짜 자리는 «한 층 아래»입니다 — 오늘 소유자가 확정한 정본이 답을 정합니다
```
정본 (CLAUDE.md, 오늘)   「마킹은 결국 서브그래프 탐색이다」 · 마킹의 단위는 «노드»
지금                      mark_key 가 «집계 단위»에서 나옵니다 — 노드가 아니라 «집계»를 가리킵니다
그래서                    트렌드는 «실험 단위(웨이퍼+레그)»를 찍고 맵은 «웨이퍼/다이»를 찍습니다.
                          서로 «다른 노드»입니다. 울타리는 그 불일치를 «동결»해 둔 것입니다
```
🔴 **판정: mark_key 를 «노드 id» 로 만드는 것이 이 항목의 본체입니다.**
그러면 집계 단위는 자유롭게 선언되고 — ⓑ 가 «공짜»가 됩니다 — 마킹은 단위와 «무관»해집니다.
울타리는 그때 «필요가 없어져서» 사라집니다. 지금 여는 것이 아니라요.

## 오후에는 «아무것도 더 하지 않습니다»
```
오후 결론   트렌드는 0% 로 남습니다. 그리고 그게 «정직한 화면»입니다 —
            호버가 「검사한 칩 64 · 보이드 난 칩 0」이라 말하고, 맵이 「발견 28」이라 말합니다.
            두 수가 나란히 «어긋난 채로» 보이는 것이 지금 사실입니다
🔴 금지     grain 을 ①②만 바꿔 넣지 마십시오 (답이 안 바뀝니다 — 당신 판단 그대로)
            단위를 하나 더 선언하지 마십시오 (마킹 이름공간이 둘이 됩니다)
다음 라운드  mark_key = 노드 id. 이건 «마킹 계약»의 개정이라 소유자 판정을 받고 시작합니다
```
## 🔴 그리고 당신이 찾은 «분모 어긋남»(128 대 64) — **그건 결함이 «아닙니다». 알갱이 둘입니다**
```
총괄 실측   SYN-CX-BW-001 검사칸 «128»
            leg=HBM-B_LOW-P «64»  ·  leg=LOGIC-A_REF «64»       ->  128 = 64 + 64
```
```
트렌드의 64   집계 단위가 «실험 단위(웨이퍼+레그)»라서 «레그 하나»를 셉니다
맵의 128      «웨이퍼 전체»를 셉니다
-> 둘 다 «맞습니다». 어긋난 것이 아니라 «다른 것을 세고» 있습니다
```
🔴 **문제는 수가 아니라 화면이 그 알갱이를 «말하지 않는» 것입니다.**
맵은 이미 「bonding_log ∩ inspection_run 기준」이라 «출처를 답니다». 트렌드 점은 안 답니다.
```
할 것   호버에 «알갱이 한 낱말». 예: 「실험 단위(웨이퍼+레그) 기준」
        요청 «0개» — 이미 받아 온 grain 에 있는 값입니다
왜      운영자가 「128 대 64」를 «모순»으로 읽습니다. 오늘 소유자가 「50퍼 대 0퍼」를
        모순으로 읽으신 것과 «같은 자리»입니다. 그때는 결함이었고 이번은 «알갱이»입니다 —
        화면이 그 차이를 말해야 둘을 «가릴» 수 있습니다
📎 `siblings_axes.json` 이 자기 주석에 이미 적어 둔 사고입니다:
   「한 화면의 두 패널이 두 알갱이로 세면 운영자 앞에서 서로 다른 말을 하고 «둘 다 틀리지 않았다»」
```
✅ **그리고 이것이 소유자가 요청한 호버의 값어치입니다** — 만든 당일에 «둘»을 찾았습니다.
   하나는 진짜 결함(발견 0 대 28)이고 하나는 «보이지 않던 알갱이 차이»(64 대 128)입니다.

📎 그리고 당신이 적은 한 줄이 오늘 규칙의 실물입니다 —
   **「거절문이 단언하는 술어를 파일이 아니라 «라우트»에 대고 쟀다」.**
   제가 파일만 보고 「클라 선언이면 된다」고 판정했고, 라우트가 두 번 거절했습니다.

---

# ⚖️ 판정 — 트렌드가 0% 인 남은 원인은 **«grain 이 틀렸습니다». 원자가 아닙니다** (총괄 15:5x)

응용이 읽는 쪽을 고치고(고립 검증 15 → 103,856) 「남은 것은 선언」이라 보고했고, **맞습니다.**
그리고 그 「선언」이 «둘»이 아니라 **하나**입니다. 제가 쟀습니다.

## 실측 — grain 이 요구하는 모집단이 «거의 없습니다»
```
grain 이 요구        subject_type «waferleg» · context_fields [«bonding_leg»]
waferleg 원자        «42개». 전부 syn_complex_composite 발 (observed 18 · processed_with 12 · register 12)
bonding_leg 를 든 원자  «42개» — 그 waferleg 뿐. «다른 주어 타입은 하나도 없음»
그런데 관측은        void 103,841 «die 주어» · delam 11,567 «wafer 주어»
```
🔴 **즉 grain 이 「보이드·박리 관측이 «살지 않는» 주어 타입」을 요구합니다.**
그래서 두 시리즈가 «같은 이유로» 0입니다 — 원인 둘이 아니라 «하나»입니다.

그리고 `context_fields: [bonding_leg]` 는 **영원히 거짓인 필터**입니다 —
관측 원자 중 그걸 든 것이 «0개»이므로, 그 조건이 남아 있는 동안 답은 «항상» 0 입니다.
(제 메모리 그대로: 「영원히 거짓인 필터는 거짓이 정답인 동안 숨는다」)

## ✅ 그리고 제 die 주어 판정은 «되돌릴 필요가 없습니다» — die 가 자기 웨이퍼를 «들고 있습니다»
```
die 주어의 subject_keys = { x, y, mat_id, mat_type }   ·  mat_id = «웨이퍼 이름»
실측: die 원자의 mat_id 가 «실재하는 wafer 주어»와 일치   103,841 / 105,246  =  «98.7%»
```
**wafer 알갱이 트렌드가 die 주어 원자에서 나옵니다.** 선언만 고치면 됩니다 —
이미 `rollup_subject_types` 의 규약이 그것입니다: 「파생 타입은 자기 원자가 «든 루트 키»를 선언한다」.

## 판정 — grain 을 «원자가 사는 곳»으로 고칩니다. 코드 0줄
```
① subject_type   관측이 «실제로 쓰는» 것으로:  void -> die  ·  delam -> wafer
                 (지금의 waferleg 는 42개짜리 모집단입니다)
② numerator      die 주어일 때 wafer 는 subject_keys 의 «mat_id» 에서 옵니다
                 (wafer 주어일 때는 지금처럼 'wafer')
🔴 ③ context     `bonding_leg` «필수 요구를 뺍니다».
                 관측 원자가 «하나도» 안 들고 있어서, 두면 답이 영원히 0입니다
                 레그 축이 필요하면 «관계에서»(bonding_map.leg) 그룹으로 붙이십시오 —
                 원자에 «있는 척»하지 마십시오
```
```
게이트   void:all 72점에서  found_chip_count «0 아닌 점 몇 개» · found_rate «min≠max»
         🔴 교차확인: 맵의 「발견 28 · 검사 128」과 그 웨이퍼의 트렌드 점이 «어긋나지 않는가»
                     -> 오늘 이 어긋남이 소유자가 본 「50퍼 vs 0퍼」였습니다
         무변화:  SYN-BW-103-11 · point 208 · { void 199, delam 9 }
```

## 누가
```
grain 을 «보내는» 것은 클라입니다 (요청에 grain={…} 로 실려 옵니다) -> 클라 레인
읽는 쪽은 이미 응용이 고쳤습니다 -> 추가 서버 작업 «없음»
📌 그래서 이건 «선언 한 덩이»입니다. 코드 0줄이 이 판정의 합격 기준입니다
```

📎 그리고 이것이 오늘 얻은 규칙의 실물입니다 — **「선언이 요구하는 모집단이 «실제로 있나»를
   먼저 센다」.** grain 을 쓸 때 「이 subject_type 의 원자가 몇 개인가」를 재지 않으면
   답이 0인 차트를 «정상»으로 넘깁니다.

---

# 🔴 오후 추가 «둘» — 소유자 요청 하나 + 계약 위반 하나. 둘 다 클라 파일입니다 (총괄 17:0x)

## ① 소유자 요청 (17:0x): 「몇 칩 있고 몇 칩 보이드인지 호버하면 어노테이션 해줘」

```
어디에   «트렌드 점»의 호버.  🔴 제 해석을 적어 둡니다 —
         소유자가 바로 앞에 「맵은 50퍼인데 트렌드는 0퍼」를 지적하셨습니다.
         그 수(분자·분모)가 «없어서» 어긋난 것을 못 봤던 자리가 트렌드 점입니다.
         맵은 머리에 「128칸 · 발견 28 · 검사 128」을 «이미» 답니다
무엇을   그 점의 «분모»와 «분자» 그대로:
            검사한 칩 «몇»  ( = scan_denominator )
            보이드 난 칩 «몇» ( = found_chip_count )
            그리고 비율은 «그 둘로 만든 것»이라고 보이게
```
```
🔴 지어내지 마십시오   응답에 이미 있는 값입니다 (value.scan_denominator · value.found_chip_count).
                      계산해서 만들지 말고 «그 두 칸을 그대로» 띄우십시오
🔴 그리고 지금은 «0» 입니다  분자가 0인 원인은 서버 쪽이고 구현자가 고치는 중입니다.
                      -> 호버는 «0 이어도 맞게» 나와야 합니다. 「검사 40 · 보이드 0」처럼.
                         그게 오늘 소유자가 못 봤던 것을 «보이게» 만듭니다
⚠️ 만약 소유자가 «맵 호버»를 뜻하셨다면 옮깁니다 — 저녁에 확인받겠습니다.
   그때까지 트렌드 점으로 만들어 두십시오
```

## ② 🔴 계약 위반 — 응용이 찾았습니다 (`1a32ab56`). **판정: 둘 다 «선언»으로 채웁니다**

```
사실   맵 부품이 space 를 «die» 로 기본값 처리하고, 선언한 것은 «칩 확대» 하나뿐입니다
       -> 본딩 맵·코어 맵 «둘 다» 그 기본값으로 떨어집니다
계약   die:base · die:core · die:dt · inchip   (문자열을 «정확히» 비교합니다)
결과   placements 가 die:base·die:core 로 오는 «날» 두 맵의 모든 점이 «아무 데도» 안 맞습니다
```
🔴 **아침의 「빈 맵」과 «같은 모양»입니다** — 옛 경로가 오늘 그려서 «아무 이상이 없어 보이고»,
새 재료가 오는 날 조용해집니다. 오늘 이 부류로 하루를 태웠습니다.

```
판정 ⓐ 기본값은 «누락»입니다. 의도가 아닙니다 — 계약이 네 이름을 정한 이유가
       「한 die 가 같은 표에서 두 격자를 들 수 없다」이고, 기본값 `die` 는 그 구분을 «지웁니다»
판정 ⓑ 본딩 맵 = «die:base»  ·  코어 맵 = «die:core»
       (코어 맵은 코어 웨이퍼의 격자입니다. dt 가 아닙니다 — dt 는 별도 단계입니다)
할 것   두 선언에 space 를 «명시». 기본값은 «지우십시오» —
        남겨 두면 다음 맵이 또 조용히 떨어집니다. 선언 없으면 «시끄럽게» 실패하게
```
📌 **오후 범위에 «넣습니다»** — 두 낱말이고, 이미 열려 있는 파일이며,
   안 하면 새 재료가 오는 날 «화면이 조용히 빕니다». 예외 사유를 이렇게 적어 둡니다.

📎 그리고 응용이 「고치지 말고 목록만」을 지킨 것 · 확인된 네 조항을 «같이» 적은 것 ·
   판정 둘을 «추측하지 않고» 남긴 것 — 셋 다 맞습니다. 그 형식 유지하십시오.

---

# 🔴🔴 오후 마감 — **소유자: 「오후 내로 완성하고 저녁에 보자」 (16:3x).** 범위를 «닫습니다»

소유자 판정 둘 받았습니다:
```
✅ 「펼친 층」 writes: null  ->  «괜찮다». 건드리지 않습니다. 판정 끝
🔴 오후 내로 «완성» · 저녁에 소유자가 «직접 봅니다»
```

## 🔴 오후에 «하는 것» — 이것만입니다

### 클라 (design)
```
① 스텝 줄 «읽히게»        flex: 0 0 auto + white-space: nowrap · 컨테이너 높이 > 0
                          + 단언 둘: 칩 너비 ≥ 글자 폭 · 컨테이너 높이 > 0
                          🔴 최우선. 지금 머리 패널이 «못 읽는» 상태입니다
② 7d 알약                 메인 트렌드 모델에서 「그 창엔 관측 0」을 읽어 붙이기. «요청 0개»
③ 남은 목업 항목          또래 수(scope.relation 이제 실림) · 코어맵 층 이름 · 구성표 열
```

### 구현자
```
① 측정 원자 씨딩          SYN-CX-BW-001 에 quantity/value — «후보·순위가 재료를 얻습니다»
                          지금 그 웨이퍼 하위그래프에 quantity·value «0개»라 두 패널이 빕니다
                          밀도 라운드와 «같은 모양». 게이트: 수 + 분류 + SYN-BW-103-11 무변화
② 「끊김」 문구 가르기      trunc=None 인데 ranked 0 이면 「예산에서 끊김」을 «띄우지 말 것»
                          (응용이 경계에 node_limit·hops 를 실었으니 부품이 판별 가능합니다)
```

### 응용
```
큐 비었습니다. «만들지 마십시오».
저녁 검수용으로 «쓸 수 있는» 일 하나만: 오늘 착지분(6건)이 계약을 깼는지 훑기.
   MARKING_CONTRACT · SERVER_POSITION_CONTRACT 대비. «고치지 말고 목록만»
```

## 🔴 오후에 «안 하는 것» — 명시합니다
```
⛔ transfer 엣지          ⓐ/ⓑ 판정 «미도착» + 72,964 재번역. «오후 크기가 아닙니다»
⛔ 개체 카탈로그 (ⓒ)      그래프 뷰어 = 삭제 후보. 순서 뒤
⛔ core·dt step 표        오후 뒤
⛔ 「펼친 층」 쓰기 추가    소유자가 «괜찮다» 판정. 하지 마십시오
```
🔴 **하나라도 시작하면 저녁 검수가 반쯤 된 상태를 봅니다.** 오후는 «닫는» 시간입니다.

## 저녁 검수 — 총괄이 «이것으로» 판정합니다 (미리 알려 둡니다)
```
① 14패널 «전부»   내용 bottom ≤ 패널 bottom  «그리고»  읽히는가 (너비·높이 단언)
② 마킹 사슬 둘    총괄이 «직접 클릭»해서 스샷:
                  트렌드 점 -> 머리·맵 둘이 그 씨앗 · 후보 행 -> 마킹2 · 순위 강조 · 후보 트렌드
③ 후보·순위       «수»를 답하는가 (지금 0). 안 되면 왜인지 «화면이» 말하는가
④ 목업 대조       총괄이 부품 단위로 나란히 (소유자 상설)
```
📌 **끝나면 각자 보고 파일에 「오후 마감」이라 적어 주십시오.** 그게 제 검수 시작 신호입니다.

---

# 🔴 넘침은 «닫혔고», 스텝 줄이 «읽을 수 없게» 됐습니다 — 한 번 더 (총괄 16:2x)

병합·빌드하고 제가 직접 쟀습니다. **넘침은 진짜로 닫혔습니다:**
```
14패널 중 넘치는 것 «0»  ·  flex-wrap «nowrap» · overflow-x «auto»
문서 폭 = 뷰포트 폭 (가로 스크롤 «없음»)
```
단언을 세우신 것도 확인했습니다. 그건 다음에 이걸 잡아 줍니다.

## 🔴 그런데 줄이 «죽었습니다»
```
.rb-head-steps   height «0px»  ·  width 1686  ·  scrollWidth «27,981»
첫 자식          「이 웨이퍼 자신의 스텝 267」  너비 «18px» · 높이 «149px»
                 -> 글자가 «세로로 한 줄»씩 쌓였습니다. 칩 534개 전부 같은 꼴입니다
```
**원인**: `nowrap` 인데 자식에 `flex-shrink: 0` 이 없어서 **전부 0 쪽으로 찌그러졌고**,
컨테이너는 높이 0 이라 «자리를 안 차지합니다».

```
넘침 단언   통과합니다 (컨테이너가 0 높이니까)
사람        «읽을 수 없습니다»
```
🔴 **이건 「고쳤다」가 아니라 「없앴다」입니다.** 소유자 핵심가치 그대로 —
「작은 글씨는 없느니만 못하다 · 가독성은 기능이다」. 18px 폭에 세로로 쌓인 글자는 그보다 아래입니다.

## 할 것 — 두 줄입니다
```
① 자식    flex: 0 0 auto  (또는 flex-shrink: 0) + white-space: nowrap
          -> 칩이 «자기 폭»을 지키고, 넘치는 만큼 컨테이너가 가로 스크롤합니다
② 컨테이너 한 줄 높이가 «실제로» 서게 (height 0 이 되지 않게)
```
## 그리고 단언을 «하나 더» 걸어 주십시오
```
지금 있는 것   내용 bottom ≤ 패널 bottom          <- 이건 «0 높이»도 통과시킵니다
🔴 더할 것     스텝 칩의 «너비 ≥ 그 글자가 필요한 폭» · 컨테이너 «높이 > 0»
              -> 「없앰」이 「고침」으로 통과하는 길을 막는 단언입니다
```
📎 오늘 이 자리에서 두 번 배웠습니다 — 넘침 단언만으로는 «보이는지»를 못 잽니다.
   제 지시서에 「자기 상자 안에서 스크롤」이라 적었는데 **「읽을 수 있게」를 안 적었습니다.**

⚠️ 그리고 부수 관측: `scrollWidth 27,981` 입니다. 칩 534개를 한 줄에 두면 그렇게 됩니다.
   지금은 «그대로 두십시오» — 접기·묶기는 지시받지 않았습니다. 읽히게만 만드십시오.

---

# 🔴🔴 총괄이 «직접» 화면을 봤습니다 — 제 씨앗 판정이 «둘»을 깼습니다 (15:2x)

병합·빌드하고 목업과 대조했습니다. 좋은 것부터: **구성이 살아났습니다** —
`10 COMPONENTS · 18 DT_COLLECTIONS · 4 CORE TYPES`, 층 표에 코어 웨이퍼·맞·슬롯·브랜치·이력 12·
상태(resolved/candidate/contested/unresolvable)가 «전부» 찼습니다. 맵도 발견 28로 목업급입니다.

**그런데 레인 보고(「live board unchanged: 13 panels, both maps ready」)가 못 본 것이 둘입니다.**
둘 다 제 씨앗 판정이 원인이고, 둘 다 제가 예고하지 않은 대가입니다.

---

## 🔴 A — 머리 패널이 «517px» 넘쳐 아래 패널 «둘»을 덮습니다  (클라 레인)

```
실측 (13패널 중 «이 하나»만)
   .rb-panel(머리)   높이 «118»
   .rb-head          내용 높이 «634»   ->  넘침 «517px» · overflow: «visible»
   .rb-head-steps    높이 336 · 자식 «534개» · display flex · flex-wrap «wrap»
덮는 것
   panel(제어·축선택)  top 174     <- 머리 내용이 이미 여기를 지나갑니다
   panel(메인 트렌드)  top 228     <- 여기까지
그리고  칩 하나가 left «1593» — 뷰포트 밖입니다
```
**원인은 씨앗입니다.** 새 씨앗의 칩이 공정 스텝 «267개»를 물고 오고, 그게 칩 534개로 펼쳐집니다.
전 씨앗은 스텝이 거의 없어서 «안 보였습니다».

```
할 것   스텝 줄이 «자기 상자 안에서» 처리되게. 534개를 wrap 시키지 말 것
        -> 한 줄 + `overflow-x: auto` 가 가장 작습니다 (넓은 내용은 자기 컨테이너에서 스크롤)
🔴 금지  패널 높이를 늘려 맞추지 마십시오. 다음 칩이 400스텝이면 또 넘칩니다.
        «개수와 무관하게» 상자 안에 있어야 합니다
확인    13패널 전부에 대해 «내용 bottom ≤ 패널 bottom» 을 단언하십시오.
        지금 그 단언이 있으면 이건 «오늘 빨개졌을» 것입니다 — 없어서 못 봤습니다
```

---

## 🔴 B — 원인 후보·순위가 «빕니다». 그리고 화면의 두 문장이 «오해를 만듭니다»  (구현자)

```
화면    「예산에서 끊김 — 아래는 미검사」  «그리고»  「노드 400 · 엣지 527 — 원인 후보는 없습니다」
읽히는 것  「예산 때문에 후보가 안 보인다」 -> 예산을 올리면 나온다
```
🔴 **제가 올려 봤습니다. 안 나옵니다:**
```
node_limit=400    trunc=«nodes, claims»   nodes 400   ranked(quantity) 0
node_limit=1000   trunc=«None»            nodes 414   ranked(quantity) «0»
```
```
진짜 이유   이 웨이퍼의 하위그래프에 «quantity·value 노드가 0개»입니다
            { entity 129, claim 257, collection 28 }  — 잴 것이 «없습니다»
            전 씨앗은 { … quantity 7, value 9 } 였습니다
```
**후보가 「끊겨서 안 보이는」 게 아니라 「재료가 없는」 것입니다.** 두 상태가 «다른데»
화면이 나란히 놓아서 같은 것처럼 읽힙니다 — 오늘 하루의 「세 상태」 규칙 위반입니다.

```
할 것 ①  이 씨앗에 «측정 원자»를 씨딩. 밀도 라운드와 «같은 모양»입니다
         (그 웨이퍼에 quantity/value 를 내는 소스가 없음 -> 후보·순위가 영구히 빔)
할 것 ②  🔴 그리고 문구를 가르십시오 — 「예산에서 끊김」과 「재료 없음」은 같은 자리에
         나란히 놓으면 안 됩니다. 끊기지 «않았는데» 비었으면 그렇게 말해야 합니다
         (trunc=None 인데 ranked 0 이면 「끊김」 문구는 «띄우지 말 것»)
```

---

## 📎 제 판정의 대가를 «다 세지 못했습니다»
```
제가 예고한 것    맵이 성겨진다 (밀도로 해결 — 실제로 해결됐습니다, 발견 28)
안 예고한 것 ①    머리 패널이 스텝 267개로 «터진다»
안 예고한 것 ②    원인 후보·순위가 «재료를 잃는다» (quantity·value 0)
```
씨앗을 바꾸는 것은 «화면 전체의 재료를 바꾸는» 일인데, 저는 두 패널(맵·구성)만 봤습니다.
**앞으로 씨앗 변경 판정에는 「그 씨앗에서 «13패널 각각»이 무엇을 받나」를 붙입니다.**

---

# ⚖️ 판정 둘 — **씨앗은 ② «짝»으로. 7d 알약에 요청은 «만들지 않습니다»** (총괄 15:0x)

기다리게 해서 미안합니다. 둘 다 답합니다.

## ① 씨앗 — **② 채택. 짝으로 옮기십시오**
```
②   맵·후보  wafer = SYN-CX-BW-001      · 구성  final_chip_id = SYN-CX-CHIP-001
```
🔴 **①이 유리해 보이는 것은 «밀도»인데, 밀도는 이미 대기열에 있는 «데이터 항목»입니다**
(9칸 → 28칸, 구현자 큐). 즉 ②의 성김은 «한시적»입니다.
반대로 ①이 잃는 것은 **구성 — 이 제품의 이유**입니다. 소유자 여정
「난 자리 → 어느 코어 층 → 그 코어에서도 났나」가 ①에서는 «영원히» 안 걸립니다.
**한시적 성김과 영구적 공백을 맞바꾸는 것이라 ②가 맞습니다.** 당신 권고 그대로입니다.

📎 그리고 오늘 그 웨이퍼가 «둘 다» 갖게 됐습니다 — void 재번역으로 point 0 → 9,
   구성은 원래 10층. ②가 가능해진 것이 오늘 라운드의 산물입니다.

```
⚠️ 조건 하나  이 씨앗은 «픽스처 기본값»이라고 «선언 옆에 적으십시오».
              당신 다음 항목이 `start { marking:'marking:1' }` 입니다 —
              마킹이 시작점을 몰고 오는 날 이 여섯 줄은 «사라질» 줄입니다.
              적어 두지 않으면 다음 사람이 이걸 «계약»으로 읽습니다
```

## ② 7d 알약 — 🔴 **요청을 «더 만들지 마십시오»**
```
당신 실측   siblings?scope=window:7d   «422»   -> window 는 또래 축이 «아닙니다»
            trends?window=7d           empty · 시리즈 0
            trends?window=180d         ready · 점 72
```
```
판정   ⓐ 새 요청 «없음».  이유 셋
       1  라우트가 window 를 scope 축으로 «거절»합니다 — 또래 알약 줄에 있을 것이 아닙니다.
          목업이 그렇게 그렸다고 해서 «데이터 모델에 그 축이 생기지는» 않습니다
       2  「왜 비었나」를 말하려고 «렌더마다 왕복»을 하나 더 다는 것은 값이 안 맞습니다
       3  🔴 그 답은 «이미 화면에» 있습니다 — 창을 7d 로 두면 메인 트렌드가 그 창으로
          `state=empty · 시리즈 0` 을 이미 받아 옵니다. 알약이 «그 모델»을 읽으면 됩니다.
          요청 0개로 「7d — · 그 창엔 관측 0」이 나옵니다
```
🔴 **이게 원칙입니다: 새 질문을 만들기 전에 «이미 받아 온 답»을 봅니다.**
그리고 목업의 「7d 96」은 픽스처가 2026-08-14 하루에 몰려 있어서 안 나오는 것이지
계산이 없어서가 아닙니다 — 그건 «밀도·시간분산» 항목이지 알약 항목이 아닙니다.

## 그래서 당신 다음 라운드
```
1  씨앗 ② 로 이동 (선언 여섯 줄 + 「픽스처 기본값」 주석)
2  7d 알약이 «메인 트렌드 모델»에서 비어 있는 이유를 읽어 붙이기 (요청 0개)
3  SPACES.inchip.items 가 «노드»에서도 좌표를 읽게 (당신이 잡아 둔 그 한 자리)
   ⚠️ 3 은 지금 그려도 «없음»이 맞습니다 — Finding Point 의 position 이 아직 빈 객체입니다.
      그건 서버 쪽 «별건»으로 갈라 뒀습니다 (이름 매핑을 코드에 박지 않기로 판정)
```
📎 그리고 당신이 찾은 「또래 알약이 출처를 안 답한다」 — 맞습니다. 응용이 `scope.relation`·
   `.column` 을 모델에 실었습니다(`e979d8f2`). **당신은 그걸 «쓰기만»** 하면 됩니다.

---

# ⚖️ 판정 — 경계 필드 «넷». 하나는 이미 서버가 싣고 있고, 하나는 «없어질 라우트»입니다 (총괄 13:4x)

「자리와 단언이 이미 서 있다」는 정리 좋습니다. 넷을 각각 가릅니다.

## ① `siblings` 의 `scope.relation` · `scope.column` — **서버는 «이미» 싣습니다. 클라 쪽입니다**
당신 실측이 맞습니다. 제 응답 덤프에서도 그대로 나옵니다:
```
"scope": { "declared": true, "axis": "wafer", "axis_label": "웨이퍼",
           "relation": «"bonding_log"», "column": «"base_id"», ... }
```
```
판정   서버 변경 «없음». 모델이 안 실어 나르는 것뿐입니다 -> `api.js` = «응용 레인» 한 파일
       클라는 그 모델이 채워지면 «자리만» 쓰면 됩니다
```

## ② `subgraph` — 완료 확인. 넘어갑니다

## ③ `composition` 의 `upstream_process.events` — **서버 일. 대기열에 넣습니다**
```
판정   정당한 요청입니다. 다만 «지금 라운드가 아닙니다» —
       구현자 다음 라운드는 v1 void 은퇴 하나로 잠겨 있습니다(삭제 + 게이트)
순서   은퇴 -> 밀도 -> 이 필드.  자리가 이미 서 있으니 그때 값만 나타납니다
```

## ④ 🔴 `lot_map` 의 `cell.points` — **이 라우트는 «버립니다». 기다리지 마십시오**
```
소유자 지시   「lot map 버려」 · 「레거시 다 버려」 (2026-08-24)
그래서        lot_map 에 필드를 «더하지 않습니다». 더하면 지울 것을 키우는 셈입니다
🔴 대신       inchip 좌표는 «walk 으로» 옵니다. 그게 오늘 착지한 void 선언의 요점입니다:
              void 원자가 qualifier 로 inchip_x · inchip_y · radius_y · unit · gate 를 «답니다»
              -> 맵의 칸이 물고 있어야 할 것은 lot_map 의 cell 이 아니라
                 «그 die 노드의 하위 그래프»입니다 (소유자 상설: 마킹한 노드의 하위 그래프)
할 것         이 항목을 「lot_map 에 필드 추가」에서 «「맵 부품의 start/collect 선언」»으로
              다시 적어 주십시오. 필요한 것이 서버 필드가 아니라 «선언 둘»일 수 있습니다
              -> 다시 적어 보고 그래도 서버 필드가 남으면, 그때 «그 남은 것»을 올려 주십시오
```

## 📎 그리고 당신이 쓴 `scope=leg:HBM-B_LOW-P` 에 대해 — 오늘 판정 하나 났습니다
```
그 축은 «케이스를 못 만듭니다». 결함이 아니라 규칙입니다 (실측 777cedb7)
   원장 주어(웨이퍼)보다 «잘게» 가르는 축 -> 주어가 전원 걸침(mixed 36) -> case 0
   leg·scan_recipe 가 그쪽 · bond_lot·wafer 는 «돕니다» (candidates 20)
-> 또래 알약 실험은 «bond_lot 이나 wafer» 로 거십시오. leg 로 재면 항상 빈 답입니다
```

---
# 📌 다음 것 — 소유자가 「세션 다 대기중」이라 하셨습니다. 바로 착수하십시오

백필(void_observation 57.8%)만 돌고 있고 나머지가 비어 있습니다.

---

## 클라 — 목업 «수·라벨» 나머지. B6·B7 은 방금 착지했습니다
```
B1  제어 「7d」 에 «수»            목업: 7d «96»
B3  메인 트렌드 머리               목업: 「씨앗 (마킹 0) 또래 «24» · 같은 레그」  <- 또래 수
B4  🔴 맵 머리                     목업: 「마킹 «34» · void «165» · delam «9»」
                                   지금: 「141칸 · 발견 28 · 검사 29」
                                   -> «마킹 수» 와 «종류별(void/delam)» 이 없습니다. 화면 절반이라 큽니다
B5  코어 맵                        목업: 「L04 · SYN-CORE-WAFER-09」  <- «어느 층인지»
B9  구성 층 표                     목업: 「이력 8 ›」 열 · 「← 후보 N」 배지 · 「신원 미해결」 행
                                   -> 지금 웨이퍼엔 구성이 없으니 «빈 표에 열만» 서면 됩니다
```
🔴 **B4 가 제일 큽니다** — 목업은 맵 머리에 「마킹 수」를 답니다. 지금은 마킹이 «표시 0» 으로만 있고
   「이 맵에서 몇 개가 마킹됐나」가 수로 안 보입니다.
📎 새 부품 금지. 전부 «이미 있는 자리에 수를 붙이는» 일입니다.

---

## 응용 — 셋. 순서대로
```
1  placements  구현자가 서버에서 낼 때 «경계가 그대로 옮기는» 준비.
               계약(§10)은 확정돼 있습니다. 지금 코드가 그 모양인지 «대조»만 해 두십시오
2  🔴 레거시 재측정  마이그레이션이 끝났으니 이제 «정확히» 셀 수 있습니다.
               「A 를 지우면 호출자 0 이 되는 라우트」를 다시 세십시오
               (전에 세신 것은 소문자 «전» 상태라 두 번 일이 됩니다 — 총괄이 그때 미뤘습니다)
3  api.js 의 다섯 갈래  fetchLotMap · fetchComposition · fetchSiblings · fetchTrends 를
               subgraph 하나로. 🔴 다만 «재료가 흐른 뒤»입니다. 지금 접으면 화면이 꺼집니다
               -> 2 의 결과를 보고 «접을 수 있는 것부터» 접으십시오
```
📎 그리고 `task/CHART_DESIGN.md` 가 정본으로 섰습니다. 3 을 할 때 그 네 칸으로 가는 것이 방향입니다.

---

## 구현자 — 백필 «돈 뒤» 할 것. 지금은 기다리십시오
```
1  백필 완주 확인 + 전/후 표
2  🔴 오늘의 판정: SYN-CX-BW-001 이 «구성 55 + void 원자» 를 둘 다 갖는지
   -> 되면 픽스처 구멍이 닫히고, 중심 여정이 처음으로 끝까지 걸립니다
3  그다음 밀도 (9칸 -> 목업급 28칸).  선언이 «선 것을 본 뒤»입니다
4  그 뒤 core·dt step 표 (40500353 픽스처는 이미 있고, 표·선언이 남았습니다)
```
⚠️ 백필이 도는 동안 «다른 쓰기»를 걸지 마십시오. 한 DB 입니다.

---

## 📌 셋 다에게 — 새로 만드는 것은 «네 칸»으로 적으십시오
```
task/CHART_DESIGN.md (정본, 소유자 확정)
chart = { marking: {reads, writes},  x,  y,  value }
-> 「어떤 부품이지?」가 아니라 「이 네 칸에 뭘 적지?」
-> 코드를 썼다면 «설계가 틀린 것»이지 예외가 아닙니다
```

---
# 📐 차트 설계 «정본» 이 생겼습니다 — `task/CHART_DESIGN.md`

> 소유자: 「차트별 컨피그랑 마킹 저렇게 해 두면 «차트 확장이 계속 가능»해서
>  추가 기능 개발이 매우 쉬워져. **차트 설계 정본으로 박아**」

```
정본 둘   task/MARKING_CONTRACT.md   마킹이 «무엇인가»
          task/CHART_DESIGN.md       차트가 «무엇인가»   <- 새로 생김
```

## 한 줄
```
chart = { marking: {reads, writes},  x,  y,  value }
```
**차트는 «부품 종류»가 아니라 «선언 하나»입니다.** 새 차트 = 선언 하나. 코드는 안 늡니다.
🔴 코드를 썼다면 «설계가 틀린 것»이지 예외가 아닙니다.

## 정본에 적힌 것
```
· 네 축이 각각 무엇인가 (marking=어디서 시작 · x/y=어디에 놓나 · value=무엇을)
· 🔴 지금 화면의 «여덟»이 그 네 칸으로 «전부» 나온다는 표
  (메인/후보 트렌드 · 본딩/코어 맵 · 칩 확대 · composite · 구성 표 · 순위 표)
· 오늘 실측으로 얻은 «지키는 것 여덟» (부호 셋 · 게이트 · 안 거른다 · 없음 세 갈래
  · 비면 왜 · 잘렸으면 말한다 · placements · 좌표계는 값)
· 새 차트 추가 절차 네 걸음
· 아직 이 모양이 «아닌» 자리 — 지금 marking/x·y/value 가 세 군데로 흩어져 있음
```

⏰ **정리는 «모양 뒤»입니다** (소유자 판정). 지금은 목업 모양 목록을 계속하십시오.
   다만 «새로 만드는 것»은 오늘부터 네 칸으로 적으십시오 — 뒤에 고칠 것이 안 생깁니다.

---
# ⚖️ 소유자 판정 — 「**모양부터**」. 컨피그 정리는 «그다음»

> 소유자: 「그냥 차트별로 내부 컨피그 달면 될텐데 — 사용 마킹 그룹, x, y, value」
> 총괄이 순서를 여쭙자: **「모양부터」**

```
지금    목업 모양 목록(4a717e22)을 «위에서부터» 계속하십시오. 그것이 우선입니다
그다음   차트 컨피그 통일 — 아래에 «적어만» 둡니다. 지금 손대지 마십시오
```

## ✅ 방금 착지분 확인 — 총괄이 눌러서 봤습니다
```
A1 스텝 사슬      자리 «생김». 비어 있고 이유를 말합니다 — 「구성이 없는 웨이퍼입니다」 ✓
A2 X value 축     「계측 시각 · 가로 눈금은 자재」 ✓
A3 구성 축 열      Data table composition · Marker by component_id · Color by role · Data limiting ✓
B2 값 없음 21     수가 붙음 ✓ · Y value 알약이 다섯으로 늘어남 ✓
B8 순위표 「홉」    컬럼 «있음» ✓
그리고            후보 25 · 실측 4 · 이름뿐 21 «채워짐» (씨앗 철자 수리 589148d1 덕분)
```
**후보 마킹도 확인했습니다** — 후보 행을 누르니 마킹2 «1 marked» · 순위표 같은 행이 파랗게
· 증거(ENTITY/CLAIM/VALUE)가 펼쳐지고 · **후보 트렌드가 «비었습니다» 에서 점 12개로** 바뀝니다.

## 남은 모양 — 이어서 하십시오
```
A4  후보 트렌드의 「접는 단위 Wafer … · WaferLeg … · die … · DT …」 줄 + 범례
B1  제어 「7d」 에 수
B3  메인 트렌드 머리 「씨앗 (마킹 0) 또래 24 · 같은 레그」  <- 목업은 «또래 수»를 답니다
B4  맵 머리 「마킹 34 · void 165 · delam 9」  <- 지금 「검사 N · 발견 N」. «마킹 수»와 «종류별»이 없음
B5  코어 맵 「L04 · SYN-CORE-WAFER-09」 층 이름
B6  후보 리스트 부류 «다섯» (계측 · 모델 · 공정 split · 사고 · 코멘트) — 지금 셋
B7  후보 행 배지 (동률 · 사고 이력 · 마킹 2 · 공정 split · 코멘트)
B9  구성 층 표 「이력 8 ›」 · 「← 후보 N」 · 「신원 미해결」
```

---

# 📋 대기열에 «적어만» 둡니다 — 차트 내부 컨피그 (소유자 설계, 모양 뒤에)

소유자 말씀이 맞습니다. 지금 그 넷이 «흩어져» 있습니다:
```
사용 마킹 그룹   reads / writes / pageFollows        <- 레이아웃 항목에
x · y           space / frame / lattice              <- 맵 부품 선언에
value           collect / axis:y                     <- 또 다른 자리에
-> 트렌드·맵·표가 «서로 다른 이름»으로 «같은 것»을 말합니다
```
```
목표 모양   chart = { marking: {reads, writes}, x, y, value }
효과        트렌드·맵·표·확대·composite 이 «같은 선언 형식».  새 차트 = «선언 하나»
근거        소유자 상설 ①(근원 템플릿 + 데이터 갈아끼우기)이 «부품»엔 적용됐는데
            «선언 층»엔 아직입니다. 맵만 그 형태이고 트렌드·표는 아닙니다
⏰ 언제      모양 목록이 끝난 뒤. 🔴 지금 시작하지 마십시오 — 소유자가 「모양부터」로 정했습니다
```

---
# 🔴🔴🔴 소유자 — 「목업이랑 «모양» 좀 빨리 똑같이」. 남은 차이 «전부» 뽑았습니다

데이터·배관은 섰습니다. 이제 «모양»입니다. 목업 전문과 현재 화면을 한 번에 훑어
**남은 것을 하나도 빼지 않고** 적습니다. 위에서부터 «순서대로» 하시면 됩니다.

## 🔴 규칙 하나 — «데이터가 없다고 멈추지 마십시오»
```
지금 픽스처엔 구성·void 를 «둘 다» 가진 웨이퍼가 없습니다 (총괄 실측, 1015828c)
-> 자리를 «만들고» 비면 「없음 + 이유」로 채우십시오. 그게 이 제품의 방식입니다
-> 「데이터가 없어서 안 만들었다」는 «모양이 안 맞는» 것으로 셉니다
```

---

# A. 통째로 «없는» 것 넷 — 이것부터

### A1. 스텝 사슬 (머리 요약 «바로 아래»)
```
목업   「이 본딩 웨이퍼 자신의 스텝 4」
       BONDING › POST_BOND_QUEUE › MOLDING › DT
       그 아래 한 줄:  본딩 · SYN-BD-EXC · CH-A · SYN-RCP-BOND@6 · 8/19 07:01
화면   «없음»
```
### A2. X value 축 (메인 트렌드 «축 열»)
```
목업   X value: 계측 시각   / 부제 「스텝 시각 8 · BONDING 으로 바꾸면 25장 중 빠지는 수가 표시됨」
화면   축 열에 Y value · Group by · Color by · Shape by · Marking · Data limiting «만»
🔴 목업은 「X·Color·Shape 는 «한 연산» — 자리만 다르다」고 적어 뒀습니다. 같은 선택자 부품입니다
```
### A3. 구성 패널의 «축 열»
```
목업   Data table: composition · Marker by: component_id · Color by: role
       Data limiting: 「층 마킹 · L00」
       + 「층 마킹 → 맵 → 후보 리스트가 한 줄로 이어진다」 설명
화면   구성 패널에 축 열이 «없음»
```
### A4. 후보 트렌드의 «접는 단위» 줄 + 범례
```
목업   「접는 단위  Wafer 114,492 · WaferLeg 18 · die 0 · DT 0」
       범례: SYN-BD-EXC · SYN-BD-STD · 이 웨이퍼
화면   «없음»
```

---

# B. 있는데 «수·라벨이 빠진» 것

```
B1  제어 ① Group by 또래   목업: 같은 레그 25 · 같은 랏 11 · 레시피@6 214 · 설비 1,806 · «7d 96»
                          화면: 「7d」 에 수가 «없음»
B2  제어 ② Y value        목업: … post_bond_queue_h · «값 없음 20 ›»
                          화면: 「값 없음」 에 수가 «없음»
B3  메인 트렌드 머리       목업: 「지금 씨앗 SYN-BW-103-11」 · 「씨앗 (마킹 0) 또래 24 · 같은 레그」
                          화면: 씨앗 이름은 있으나 «또래 수»가 없음
B4  맵 머리               목업: 「마킹 34 · void 165 · delam 9」
                          화면: 「검사 N · 발견 N」  -> 🔴 «마킹 수»와 «종류별 수»가 없음
B5  코어 맵               목업: 「L04 · SYN-CORE-WAFER-09」 «어느 층인지»
                          화면: 층 이름 «없음»
B6  후보 리스트 머리       목업: 「마킹 1 기준 25 · 계측 3 · 모델 19 · 공정 split 1 · 사고 1 · 코멘트 1」
                          화면: 「후보 N · 실측 N · 이름뿐 N」  -> 부류가 «셋»뿐, 목업은 «다섯»
B7  후보 행 배지           목업: 「동률」 · 「사고 이력」 · 「마킹 2」 · 「공정 split」 · 「코멘트」
                          화면: 없음
B8  순위 표 컬럼           목업: 순위 · 물리량/모델 · «홉» · 실측 · 상태
                          화면: 「홉」 컬럼 확인 필요
B9  구성 층 표             목업: 층 · 코어웨이퍼 · 랏 · 슬롯 · 브랜치 · «이력 8 ›» · 상태
                          + 「← 후보 5」 배지 · 「신원 미해결」 행
```

---

# C. 순서 — 「모양」이 목적이니 «보이는 것부터»
```
1  A1 스텝 사슬      머리 바로 아래. 화면 «맨 위»라 제일 먼저 눈에 띕니다
2  A2 X value 축     축 열이 목업과 «칸 수»가 달라 한눈에 보입니다
3  B4·B5 맵 머리     맵이 화면의 절반입니다
4  A3 구성 축 열
5  B6·B7 후보 부류·배지
6  A4 · B1·B2·B3·B8·B9  나머지 수·라벨
```
📎 **부품을 새로 만들지 마십시오** — 축 열은 이미 있는 선택자 부품의 «둘째·셋째 인스턴스»이고,
   스텝 사슬은 「펼친 층」이 이미 그리는 것과 «같은 모양»입니다 (목업이 둘 다 같은 사슬을 씁니다).

⚠️ 그리고 «데이터가 비는 자리»가 여럿일 것입니다 — 구성·펼친 층은 지금 웨이퍼에 기록이 없고,
   후보·순위도 씨앗에 따라 0입니다. **비어도 자리는 서야 합니다.** 비면 이유를 말하십시오.

---
# ⚖️ 판정 — ② 로 갑니다. 그리고 이건 «고를 문제가 아니라 픽스처의 구멍»입니다

클라가 잰 것(75da9a79)이 정확합니다. 제가 «한 겹 더» 쟀습니다:
```
void 상위 웨이퍼     SYN-BW-103-11(199) · 103-04(195) · 103-24(194) …   «관측 계열»
전사 기록 웨이퍼      SYN-XFER-CORE-W10(186) · W09(185) …                «전사 계열»
🔴 둘 다 있는 웨이퍼   «0»
```
**어느 웨이퍼도 구성과 void 를 둘 다 갖고 있지 않습니다.**
목업은 «한 웨이퍼가 둘 다» 갖는 걸 전제로 그려졌습니다 (머리 SYN-BW-103-11 + 코어 10층).
**그런 웨이퍼가 데이터에 없습니다.** 그러니 선택지 ①②③ 은 «세 가지 차선»이지 답이 아닙니다.

## 지금 — ② «전부 SYN-BW-103-11». 구성은 «빈 채로 정직하게»
```
왜 ②    · 맵·후보·순위가 «풍부»합니다 — 오늘 만든 것 대부분이 그 위에서 검증됩니다
        · 「한 화면 한 주어」가 «성립»합니다. 두 대상의 수가 나란히 있는 것이 제일 나쁩니다
        · 구성이 비는 것은 «거짓이 아닙니다» — 이 제품은 「없음」을 그리는 제품입니다.
          「이 웨이퍼는 구성이 없다」는 참이고 정보입니다
왜 ① 아님  맵이 void 13 이 되어 목업과 «밀도»가 다시 어긋납니다. 대조가 반쪽이 됩니다
왜 ③ 아님  총괄이 «실제로 오독했습니다». 두 대상의 수가 한 대상 것으로 읽힙니다.
          당신이 이름을 갈라 «오독은» 없앴지만, 「왜 둘이지?」는 남습니다
```
📌 다만 «빈 구성»이 「고장」으로 안 읽혀야 합니다 —
   「이 웨이퍼는 구성 기록이 없습니다」처럼 «이유»가 붙어야 합니다. 그건 이미 있는 계약입니다.

## 🔴 그리고 «진짜» 할 일 — 픽스처에 그런 웨이퍼를 만드는 것 (구현자)
```
필요    void·delam 관측이 «있고» 동시에 코어 층 구성이 «푸는» 웨이퍼 «하나»
왜      목업이 그걸 전제합니다. 그리고 이 제품의 «가장 중요한 여정»이 그것입니다 —
        「본딩 웨이퍼에서 난 자리 -> 그 자리에 어느 코어 층이 왔나 -> 그 코어에서도 났나」
        지금은 그 여정을 «끝까지 못 걷습니다». 두 계열이 안 만나서입니다
언제    소문자 마이그레이션 · event 빼기 «다음». 급하진 않지만 «빠지면 안 되는» 것입니다
📎 오늘 core·dt step 표를 만들 때 «같이» 하면 자연스럽습니다 —
   그 표가 core 단계 관측을 담으니, 그 웨이퍼가 본딩 관측과 «같은 계보»에 있으면 됩니다
```

## 그래서 지금 클라가 할 것
```
1  기본 씨앗을 SYN-BW-103-11 로 «전부» 통일 (머리 포함)
2  구성·펼친 층이 «비면» 이유를 말할 것 — 「이 웨이퍼는 구성 기록이 없습니다」
3  머리 총량 (다이 총수 · void «건» 199 · delam «건» · 마킹1/마킹2 행수)
   🔴 「발견 28칸」과 「void 199건」이 «다른 수»임이 읽히게. 단위를 붙이십시오
```
📎 ①의 「칩이 앉은 웨이퍼 / 씨앗 웨이퍼」로 이름을 가른 것 — 좋습니다.
   ② 로 가면 그 구분이 «필요 없어지지만», 나중에 다시 둘이 될 때를 위해 «남겨 두십시오».

---
# ⚖️ 판정 — 머리 요약: «한 자리». 그리고 「발견 28」의 «이름»을 고쳐야 합니다

응용이 갈래를 지목했고(33125696) 자기 배정을 지켜 «적기만» 했습니다. 제가 받아서 넘깁니다.

## ① 머리 요약이 씨앗을 안 따르는 이유 — «버그가 아니라 선언»
```
client2/src/rnd_board/main.js:102-110   head-summary
   reads:   'marking:1'                                     <- 읽는다고 «선언»
   start:   { groupby: 'chip', value: «'SYN-CX-CHIP-001'» }  <- 그런데 «리터럴»
   title:   '머리 요약 · SYN-CX-CHIP-001'                     <- 제목에도 박힘
```
🔴 **읽는다고 선언해 놓고 시작점을 상수로 박았습니다.** 맵·후보·순위는 씨앗을 따르는데
   이 부품만 안 따라옵니다. 그래서 «한 화면에 두 웨이퍼»가 뜹니다.
```
할 것   start.value 를 «마킹에서» 받게. 제목도 그 값에서 나오게
        -> 그러면 「선언한 대로 도는」 상태가 됩니다
📌 이건 오늘 세 번째 «같은 모양»입니다:
   marking:0 을 쓰는데 아무도 안 읽음 · die 노드를 만들고 안 이음 · 읽는다 선언하고 안 읽음
   전부 「선언과 실제가 어긋나고 화면은 멀쩡해 보이는」 것입니다
```

## ② 「199 vs 28」 — ⓒ «정의 차이»로 확정. 결함 아님
```
199   void_obs «행수»            = 발견 «건»      <- 목업이 세는 것
28    서로 다른 (x,y) «자리»      = 발견 «칸»      <- 화면이 세는 것
29    inspection_run 자리        = 검사 «칸»      <- 화면의 「검사 29」와 일치
```
**소유자 원문 그대로입니다** — 「한 칩에 보이드 여러 개 달려도 그냥 한 칸 칠하는 거지?」
28칸이 199건을 물고 있습니다. **둘 다 맞습니다.**

## 🔴 그런데 «이름»이 그걸 안 말합니다 — 이게 고칠 것입니다
```
지금    「141칸 · 발견 «28» · 검사 29」
읽히는 것 「void 가 28개」  <- 틀린 읽기입니다. 제가 그렇게 읽었습니다
사실     「void 가 «28칸»에서 났다. 건수는 «199»」
```
```
할 것   ⓐ 맵 머리의 수에 «단위»를 붙이십시오 — 「발견 28칸」 처럼
        ⓑ 머리 요약에 «총량»을 넣으십시오 (목업대로)
           다이 총수 · void «건» · delam «건» · 「마킹 1 · N행」 · 「마킹 2 · N행」
        ⓒ 둘이 «다른 수»라는 게 읽혀야 합니다. 나란히 놓고 침묵하면 오독됩니다
🔴 순서   ①을 «먼저». 대상이 틀린 머리에 총량을 넣으면 «틀린 웨이퍼의 총량»이 뜹니다
```

📎 그리고 이 구분은 «확대/composite 뷰의 전제»이기도 합니다 —
   웨이퍼 맵은 «칸»(28)을 그리고, 칩 확대와 composite 은 «건»(199)을 그립니다.
   같은 웨이퍼에서 두 수가 «둘 다» 화면에 있어야 그 전환이 이해됩니다.

---
# ⚖️ 정정 — 「발견 28 vs 199」는 «결함이 아닙니다». 목업이 스스로 답했습니다

앞 대조에서 제가 「자릿수가 다르니 재 봐야 한다」고 넘겼습니다. **목업을 다시 읽고 취소합니다.**
소유자께 여쭈려던 것도 «묻기 전에 볼 수 있는 것»이었습니다.

## 목업이 «세 자리에 서로 다른 수»를 «의도해서» 씁니다
```
목업 머리        「다이 5,378 · void «199» · delam 9」        <- 웨이퍼 «총량»
목업 본딩 맵      「마킹 34 · void «165» · delam 9」           <- 그 «맵에 그려진 것»
목업 트렌드 주석   「y = void 비율 (분자 observed · 분모 «inspection_run» · absence_is_zero false)」
```
🔴 **199 ≠ 165 인데 «둘 다 적혀 있습니다».** 목업이 총량과 맵 범위를 «가른 것»입니다.
🔴 그리고 트렌드 주석이 **분모를 `inspection_run` 이라고 «명시»**합니다 —
   목업도 「검사한 것 중」이 기준입니다. **지금 화면의 「검사 29 · 발견 28」과 «같은 세계»입니다.**

## 그러므로
```
❌ 결함 아님   「발견 28」이 「199」에 못 미치는 것 — 분모가 다른 «다른 수»입니다
✅ 진짜 차이   «머리에 총량이 없는 것»
              목업 머리:  다이 5,378 · void 199 · delam 9 · «마킹 1 · 34행» · «마킹 2 · 1행»
              화면 머리:  칩 id · 웨이퍼 id · 상태 · 141칸 · void N · delam N · 검사 N
              -> 「다이 총수」와 「마킹 행수 둘」이 «없습니다»
```
**찾을 것이 하나 줄고 만들 것이 하나 늘었습니다.** 앞 대조의 「⚠️ 재야 할 차이」 절은 «무효»입니다.

## 그래서 머리 요약에 할 것
```
· 다이 «총수» (목업 5,378) — 웨이퍼 전체 규모
· void · delam «총량» (목업 199 · 9) — 맵의 수와 «다른 수»입니다. 둘 다 있어야 합니다
· 「마킹 1 · N행」 · 「마킹 2 · N행」 — 목업이 머리에 답니다
🔴 맵 머리의 수는 «그대로 두십시오». 그건 맞습니다
```
⚠️ 그리고 이 둘이 «다른 수»라는 것이 화면에서 읽혀야 합니다 —
   나란히 놓고 아무 말도 없으면 「199인데 왜 28만 그렸나」로 읽힙니다. 제가 그렇게 읽었습니다.

---

## 🔴 앞 대조의 «확실한» 건은 그대로입니다 — 머리와 맵이 다른 웨이퍼
```
머리   SYN-CX-CHIP-001 · 웨이퍼 SYN-CX-BW-001
맵     SYN-BW-103-11
```
이건 «분모 문제가 아니라» 대상이 다른 것입니다. 그대로 유효합니다.
📎 그리고 위의 「머리에 총량을 넣는」 일은 «이것을 고친 뒤»에 하십시오 —
   대상이 틀린 채로 총량을 넣으면 «틀린 웨이퍼의 총량»이 뜹니다.

---
# 🔍 총괄 목업 대조 «2회차» — 이번엔 조건이 맞았습니다 (상설 ②)

아침 대조는 «반쪽»이었습니다 — 씨앗이 void 13 짜리였고 목업은 void 199 짜리라
「부품이 있나 없나」까지만 셀 수 있었습니다. 지금은 **씨앗이 목업과 같은 웨이퍼**라
«그림»까지 맞댈 수 있습니다.

## ✅ 이번에 맞은 것 — 하나씩 확인했습니다
```
마킹      「씨앗 · 마킹 1」 · 「후보 · 마킹 2」 «둘»뿐   -> marking:0 · 3 «은퇴 확인»
배지      「읽기 marking:1 · 쓰기 marking:1 · 표시 0 · «따라감 subject:wafer»」
          -> 셋째 이름이 보입니다. 제가 오진했던 그 침묵이 닫혔습니다
맵 둘     본딩 «141칸» · 코어 «121칸»  -> 이제 «서로 다릅니다»
          (아침의 「똑같다」가 픽스처였다는 당신 판정이 이걸로 재확인됩니다)
픽셀      38.5% / 41.0% · 캔버스 196x81   -> 둘 다 «그려집니다»
펼친 층   패널 «있음» — 「층을 찍으면 여기에 펼칩니다」
패널      12
```
**목업에 있고 화면에 없던 «넷» 중 둘이 닫혔습니다** (후보 트렌드 · 펼친 층).
남은 둘: «스텝 사슬»(BONDING › POST_BOND_QUEUE › MOLDING › DT) · «X value 축».

---

# 🔴 확실한 것 — 머리와 맵이 «다른 웨이퍼»를 보고 있습니다

```
머리 요약           「SYN-CX-CHIP-001 · 웨이퍼 «SYN-CX-BW-001»」
맵 · 후보 · 순위     「«SYN-BW-103-11»」
-> «한 화면에 둘이 동시에» 떠 있습니다
```
씨앗이 목업 웨이퍼로 옮겨졌는데(8471e72a) **머리 요약만 안 따라왔습니다.**
🔴 어느 쪽이 씨앗을 안 따르는지는 «당신이 재십시오» — 제가 갈래를 지목하면
   오늘 두 번째 오진이 됩니다. 아침에 한 번 했습니다.

📌 사용자 입장에서 이게 제일 나쁩니다 — **두 숫자가 다른 대상의 것인데 나란히 있습니다.**
   「이 웨이퍼는 void 28」과 「이 칩은 …」이 «서로 다른 웨이퍼» 이야기입니다.

---

# ⚠️ 그리고 «재야 할» 차이 — 짐작하지 않고 넘깁니다

```
목업    SYN-BW-103-11 · 다이 «5,378» · void «199» · delam 9
        본딩 맵: 「마킹 34 · void 165 · delam 9」
        코어 맵: 「L04 · SYN-CORE-WAFER-09 · 추적된 같은 34 · 후보가 걸린 148」
화면    본딩 «141칸 · 발견 28 · 검사 29»   ·   코어 «121칸 · 발견 28 · 검사 29»
```
**칸 수도 발견 수도 «자릿수»가 다릅니다.**
```
확인된 사실   목업의 199 는 «지어낸 수가 아닙니다» —
              원장에서 SYN-BW-103-11 의 void 원자가 «정확히 199» 입니다 (총괄 실측)
              -> 목업은 «이 데이터를 보고» 그려졌습니다
그러므로      화면이 28 에 머무는 것은 «재 볼 값»이 있습니다
후보 갈래     ⓐ 등록 프레임이 141칸짜리라 그 이상 못 그린다
              ⓑ 질의가 일부만 가져온다 (truncated 는 지금 화면에 «안 뜹니다»)
              ⓒ 「발견」의 정의가 목업의 「void」와 다르다 (예: 검사된 것 중에서만 셈)
⛔ 제 짐작입니다. «세어서» 정하십시오. 특히 ⓒ 는 «결함이 아닐 수» 있습니다 —
   화면 수치가 「검사 29 중 발견 28」이라 «분모가 29»입니다. 목업의 199 는 분모가 다를 수 있습니다
```
🔴 **만약 ⓒ 라면 그건 결함이 아니라 «목업과 화면이 다른 질문에 답하는 것»입니다.**
   그러면 «어느 쪽이 맞는지»를 소유자께 여쭐 자리이지 고칠 자리가 아닙니다. 먼저 세십시오.

---
# ⚖️ 판정 — «노드 상한». 두 사람이 «독립적으로» 같은 벽에 닿았으니 실재합니다

## 먼저 정정 — 제 「코어 맵 결함」 보고가 «틀렸습니다»
클라가 재서 뒤집었고 그쪽이 맞습니다.
```
사실   SYN-CX 픽스처가 본딩과 코어에 «같은 12x12 프레임»을 등록해 뒀습니다
       웨이퍼 질문에서는 «모든 축»이 그 웨이퍼의 다이를 투영합니다
       -> 「맞게 도는 패널 둘」이 같은 그림을 그리는 것이 «정상»입니다
       랏에서 141 vs 110 이던 것은 «랏»이 서로 다른 계열에 걸쳐 있어서였습니다
제 실수 「같은 출력 = 잘못된 배선」으로 건너뛰었습니다.
       「데이터가 같을 수는 없나」를 «안 물었습니다». 가설을 진단으로 썼습니다
       갈래를 지목하진 않았지만 «둘 중 하나»라고 범위를 못 박은 것도 틀렸습니다 — 셋째였습니다
```
📌 그래도 관측은 값이 있었습니다 — 배지가 «세 이름 중 둘만» 말하고 있었고
   («pageFollows» 가 숨어 있었음), 그 침묵이 «저를 실제로 오진하게 만들었습니다».
   화면의 침묵이 사람을 틀리게 한다는 증거라 오히려 좋은 사례입니다. 고쳐 주셔서 고맙습니다.

---

# 🔴 상한 — 실측 둘

```
총괄    SYN-BW-103-11 · collect=point · hops=2 · node_limit «2000»  ->  HTTP «422»
        「Input should be less than or equal to 1000」
        같은 질문 node_limit=1000 -> nodes «780» (point 208 · claim 260 · event 260 · entity 39)
        truncated: ['depth']   <- 세 웨이퍼 «전부»
구현자   「섬은 사라졌고 목업 웨이퍼가 point 를 순위 매기는데, «이제 노드 상한이 문다»」
```
**두 사람이 따로 도착했으니 이건 실재합니다.**

## 판정 — 상한을 «올리지 않습니다». 질문을 «좁힙니다»
```
왜 안 올리나
  ① 780 노드 중 «point 208» 만 맵이 씁니다. claim 260 + event 260 «520» 은 맵이 «안 그립니다»
     -> 상한을 올리면 «안 쓰는 것»을 더 많이 실어 나릅니다. 더 큰 웨이퍼에서 또 부딪힙니다
  ② 상한은 «보호 장치»입니다. 부딪힐 때마다 올리면 보호가 아니라 «지연된 사고»입니다
  ③ 소유자 도식대로면 맵의 질문은 「이 마킹에서 걸어 닿는 «점»」입니다.
     claim·event 는 «증거를 되짚을 때» 필요하지 «그릴 때» 필요하지 않습니다
```
```
할 것 (누가)
  구현자   collect=<kind> 일 때 «그 kind 에 필요한 것»만 싣는 «좁힌 응답»이 가능한지 재십시오
           -> 지금은 collect 가 «순위»만 정하고 노드는 다 옵니다 (제가 확인했습니다).
              그 성질이 「맵 하나가 walk 한 번으로 세 갈래를 다 받는다」를 만들어 준 것이라
              🔴 «없애면 안 됩니다». 「필요 없는 것을 뺀다」와 「필요한 것을 자른다」는 다릅니다
           -> 그래서 «판정 요청»으로 돌려주십시오: 무엇을 빼도 안전한지 «세어서» 올리십시오.
              제가 정하겠습니다
  클라     그때까지 «truncated 를 화면에 표시»하십시오. 이건 지금 하십시오
           지금 세 웨이퍼 전부 truncated:['depth'] 인데 화면은 «아무 말도 안 합니다»
           -> 「깊이에서 잘림 · 더 있을 수 있음」 한 줄
```
🔴 **자르는 것보다 «잘렸다고 말하는 것»이 먼저입니다.** 오늘 종일 그 규칙이었습니다.
   지금 화면은 「보이드 60개인 웨이퍼」와 「208개인데 60개만 실려 온 웨이퍼」를 «같게» 보여 줍니다.

---
# ✅ ④ «닫혔습니다» — 총괄이 직접 눌렀습니다. 그리고 그 자리에서 결함 하나가 나왔습니다

마킹 계약 §8-3 「트렌드에서 점을 찍으면 맵이 바뀐다」를 제가 브라우저에서 «직접» 눌렀습니다.

## 실측 — 클릭 전 / 후
```
클릭 전   본딩 맵  141칸 · 발견 13 · 검사 29   페이저 「07 / 25 · SYN-BW-001-07」
          코어 맵  110칸 · 발견 13 · 검사 29
          캔버스   4,374 px  /  4,624 px      («서로 다름»)
          마킹     없음

클릭      트렌드 점 「SYN-CX-BW-001 · 1.56% · 분모 64 · found」

클릭 후   본딩 맵  128칸 · 발견 9 · 검사 128   페이저 「씨앗 · SYN-CX-BW-001」
          코어 맵  128칸 · 발견 9 · 검사 128
          캔버스   6,144 px  /  6,144 px
          마킹     「1 marked」
```
🔴 **타겟팅은 «됩니다».** 두 맵이 마킹을 따라 새 씨앗으로 다시 그렸습니다.
**아침 소유자 지적 ④ 「트렌드 마킹에 따른 페이지 웨이퍼 타겟팅 미변경」은 이걸로 끝입니다.**
`312e52bc` 한 줄이 그걸 했습니다.

---

# 🔴 그런데 코어 맵이 «코어 맵이 아니게» 됐습니다

```
재타겟 «전»   본딩 141칸 · 코어 110칸        <- 서로 «다른 축». 정상
재타겟 «후»   본딩 128칸 · 코어 «128칸»
              둘 다 「발견 9 · 검사 128 · bonding_log ∩ inspection_run 기준」
              제목만 「코어 맵 · 마킹 2」이고 «내용은 본딩축»입니다
```
📌 **첫 단서는 픽셀이었습니다** — 4,374/4,624 로 다르던 것이 6,144/6,144 로 «정확히» 같아졌습니다.
   숫자를 안 봤으면 「둘 다 잘 그려졌다」로 읽었을 그림입니다.

## 그리고 배지가 «말과 다릅니다»
```
코어 맵 배지   「읽기 marking:2 · 쓰기 marking:2」
실제           제가 «marking:1» 을 건드렸는데 «같이 바뀌었습니다»
```
🔴 배지가 선언을 표시하는데 «실제 동작이 그 선언과 다릅니다».
   둘 중 하나입니다 — 배지가 거짓이거나, 재타겟이 선언을 «무시»하거나.
   **어느 쪽인지는 당신이 재십시오.** 제가 갈래를 지목하면 또 엉뚱한 데를 고치게 됩니다.

## 왜 이게 중요한가
```
소유자 도식     마킹1 -> 맵 ·  마킹2 -> 후보 맵      «둘은 다른 시작점»입니다
지금            마킹1 을 만지면 «둘 다» 움직이고 «같은 그림»이 됩니다
결과            「본딩에서 난 자리」와 「코어에서 난 자리」를 «대조할 수 없습니다»
                -> 이 화면의 존재 이유가 그 대조입니다
```

## 그리고 씨앗 — 응용이 단서를 달았고 «맞습니다»
```
응용 0e82ffac   SYN-BW-103-11 은 findings 199 라 «기본 node_limit 에서 잘립니다»
🔴 그러면        맵이 «불완전한 그림»을 그리는데 그게 안 보입니다 —
                「보이드 60개인 웨이퍼」로 읽힙니다
할 것            맵이 자기 질의의 node_limit 을 «올리거나»,
                잘렸으면 «잘렸다고 말하거나». 둘 중 «하나는 반드시».
                `truncated` 는 «이미» 응답에 있습니다
📎 8471e72a 로 화면 기본 씨앗을 목업 웨이퍼로 옮기신 것 — 좋습니다. 위 단서만 같이 처리하십시오
```

---
# 📌 시험 씨앗은 «이것»으로 — 목업의 그 웨이퍼가 «실재»합니다 (총괄 실측)

오늘 제가 `SYN-AUG-BW-001-01` 로 시험하다 「collect=point 가 0」을 보고 잠깐 헤맸습니다.
**그 웨이퍼는 「봤다 84 · 났다 0」이라 반쪽만 보입니다** — 새 자재의 void 가 아직 번역 전입니다.

## 세 갈래가 «둘 다» 서는 웨이퍼가 이미 있습니다 (재번역 52.7% 시점)
```
SYN-BW-045-08   봤다 31 · 났다 59
SYN-BW-076-04   봤다 31 · 났다 57
SYN-BW-058-22   봤다 31 · 났다 57
```

## 🔴 그리고 «목업의 웨이퍼»가 실재합니다
```
목업 머리      「SYN-BW-103-11 · 랏 CL-2601-002 · 다이 5,378 · void «199» · delam 9」
원장 실측      SYN-BW-103-11  의 «났다» 원자 «199»      <- 정확히 일치
             그 뒤로 103-04(195) · 103-24(194) · 103-06(193)
```
**목업은 지어낸 숫자가 아니라 «이 데이터를 보고» 그린 것이었습니다.**
따라서 목업 대조를 하려면 **씨앗을 `SYN-BW-103-11` 로 두는 것이 맞습니다** —
지금 화면 기본값은 `SYN-BW-001-07`(void 13)이라 목업(199)과 «그림의 밀도»가 다릅니다.

## 그래서 부탁
```
시험 씨앗   «났다»가 많은 웨이퍼로 잡으십시오. 제일 좋은 것은 SYN-BW-103-11
            -> 목업과 «같은 그림»이 나와야 하고, 그래야 대조가 성립합니다
⛔ 금지      씨앗을 «지어내지» 마십시오. 오늘 세 번 물렸습니다:
            지어낸 node id · 지어낸 die 씨앗 · 「봤다만 있는」 웨이퍼로 시험
            셋 다 «빈 답»이 「기능이 죽었다」처럼 보였습니다
📌 화면 기본값을 바꿀지는 «클라 판단»입니다. 다만 «시험»은 103-11 로 하십시오
```

📎 이건 지시가 아니라 «정보»입니다. 제가 헤맨 자리를 남들이 또 밟지 않게 적습니다.

---
# 🔴🔴 «한 줄»이 아직 안 들어갔습니다 — 오늘 아침 소유자 지적 ④ 의 전부입니다

맵 부품·좌표계·페인트 수리 다 좋습니다. 그런데 **제일 값싼 것이 남아 있습니다.**

## 실측 (`client2/src/rnd_board/main.js`, 방금)
```
line 175-176   main-trend      reads: 'marking:0'   writes: 'marking:0'
line 276-277   map (본딩)       reads: 'marking:1'   writes: 'marking:1'
line 286-287   map (코어)       reads: 'marking:1'   writes: 'marking:1'
grep           marking:0  «4회»  ·  marking:3  «4회»   -> 둘 다 «살아 있습니다»
```
🔴 **트렌드가 `marking:0` 에 쓰고, 맵은 `marking:1` 을 읽습니다. 잇는 것이 없습니다.**
그래서 지금도 **트렌드에서 점을 찍으면 맵이 안 바뀝니다.**

## 이미 판정이 났습니다 (`af5337a5` · `MARKING_CONTRACT` §3)
```
main-trend 의 writes: 'marking:0'  ->  'marking:1'      «그 한 줄»
marking:0 «은퇴» · marking:3 «은퇴» (교집합은 읽는 자리에서 «계산»)
마킹은 «둘»입니다
```

## 왜 이게 지금 제일 급한가
```
소유자 아침 지적 다섯 중 ④  「트렌드 마킹에 따른 페이지 웨이퍼 타겟팅 미변경」
   -> 원인이 «버그가 아니라 미배선»이라고 오늘 밝혀졌고, 고침은 «한 줄»입니다
소유자 도식의 «중심»          마킹1 이 ③후보·④자재정보·⑤맵 «셋»의 start 입니다
   -> marking:0 인 채로는 도식이 «작동하지 않습니다». 부품을 아무리 잘 만들어도요
그리고 확인이 «쉽습니다»      트렌드 점 하나 찍고 맵이 바뀌는지 보면 끝입니다
```
🔴 **맵 부품을 완성하기 «전»에 이것부터 넣으십시오.** 배선이 안 이어진 채로 부품을 다듬으면
   「잘 만든 부품이 아무 데도 안 연결된」 상태가 됩니다 — 오늘 이미 두 번 본 모양입니다
   (소비자 0인 marking:3 · 웨이퍼와 안 이어진 die 노드).

📎 총괄이 착지 후 «직접 눌러» 확인하겠습니다 — 마킹 계약 §8 의 3번입니다.

---
# 📎 작은 것 하나 — 세 갈래의 «말하는 쪽»이 아직 없습니다 (총괄 실측, 급하지 않음)

맵이 다시 그려집니다 — 제가 픽셀로 확인했습니다 (본딩 32.8% · 코어 34.7% · 196x68).
**그리는 쪽은 됐습니다.** 다만 승인된 세 갈래의 «둘째 반쪽»이 안 보입니다:

```
승인된 규칙   placements 키가 «없다» -> 옛 경로로 그린다
                                       «+ 화면이 그 사실을 말한다 — 「좌표 계약 대기」»
실측          화면 전체 텍스트에 「좌표 계약」·「계약 대기」 «0회»
              (「맵 밖 · 검사 1 · 발견 1」 과
               「읽기 marking:1 · 쓰기 marking:1 · 표시 0」 은 «있습니다»)
```

🔴 **그리는 것은 맞는데 «무엇으로 그렸는지»를 안 말합니다.**
지금은 옛 경로가 유일해서 티가 안 나는데, placements 가 착지하는 «그날»
어느 경로로 그려진 그림인지 아무도 구분 못 합니다.

```
언제    placements 를 붙이는 «그 커밋»에 같이. 지금 따로 하지 마십시오
무엇    한 줄이면 됩니다 — 「좌표 계약 대기 · 저장 좌표로 그림」 같은
왜      오늘 이 화면이 «비어 있으면서 숫자는 맞았던» 것과 같은 부류입니다.
        그림이 맞아 보이는데 «출처»를 안 말하는 것
```

📎 급하지 않습니다. 원자 재작성이 먼저입니다.

---
# ⚖️ 판정 둘 — ① 세 갈래 «승인» (구멍은 제 것이기도 합니다) ② 접힘 수리 «승인, 지금»

---

## ① 빈 화면 — 세 갈래로 «승인». 그리고 그 구멍의 절반은 제 것입니다

응용 §18 을 그대로 받습니다:
```
placements 키가 «없다»    계약 미착지  -> 🔴 «옛 경로로 그린다» (cells 의 x·y)
                          + 화면이 그 사실을 말한다 — 「좌표 계약 대기」
placements: []            이 점은 어느 자리에도 없다 -> 안 그리고 «맵 밖»에 센다
placements: [ … ]         그 자리에 그린다
```
🔴 **응용이 「제가 안 적어서 생긴 일」이라 했는데, 절반은 제 것입니다.**
제가 낸 판정(9e3ff64c)이 이렇게 적혀 있습니다:
```
「소스가 내 space 를 «선언 안 했다» -> 인스턴스가 안 선다
 선언은 했는데 이 점에 그 자리가 없다 -> 그 점만 안 그린다」
```
**둘 다 «데이터의 상태»이고, 「필드가 아직 안 온다」는 «배관의 상태»입니다.**
제가 그 축을 안 갈랐습니다 — 그리고 저는 오늘 같은 구분(「없다」 vs 「아직 안 왔다」)을
다른 자리에서 두 번이나 강조했습니다. 제 규칙을 제 계약에 안 적었습니다.

**클라는 §15 를 «정확히» 따랐습니다. 그 레인의 잘못이 아닙니다.**
📎 다만 하니스가 초록이면서 화면이 빈 것은 «남습니다» — 픽셀 단언은 그대로 넣으십시오.

🔴 **급한 순서:** 옛 경로 복구가 «먼저»입니다. 계약이 착지하면 새 갈래가 «저절로» 이깁니다.

---

## ② 접힘 수리 — **승인합니다. 지금 하십시오.** 마지막 벽입니다

당신 실측이 예고와 «정확히» 맞았습니다:
```
백필 후    die 주어 원자 1,405 -> «119,067» · inspected 117,662 · 거절 «0»
           (총괄이 직접 재서 확인했습니다. 표본 원자도 열어 봤습니다 —
            subject {x:2, y:9, mat_id:"SYN-AUG-BW-001-01", mat_type:"Wafer"} · value 7.0)
웨이퍼 씨앗 collect=point   ranked «0»      <- 접힘
웨이퍼 씨앗 collect=entity  ranked 1        <- 걷기 자체는 돕니다
die 씨앗                    ready · nodes 4 <- 새 원자가 «씨앗이 됩니다»
```
**「원자는 늘었고 접힘이 남았다」가 확정됐습니다.** 승인 대기라 하셨으니 «승인»입니다.
```
계약   collect=<kind> 가 접힘을 «통과»한다. 접기 자체는 «유지»
확인   같은 웨이퍼 씨앗 · collect=point  ->  ranked 가 «0 -> N»
같이   placements(좌표) 와 «한 커밋»입니다 — 뚫어도 좌표가 없으면 맵은 여전히 못 그립니다
조심   node_limit. 웨이퍼 하나에 관측 수천이면 쏟아집니다. truncated 로 «정직하게»
       숨기지도 말고, 접힘을 이유로 «0» 이라 답하지도 마십시오
```

---

## 📌 그리고 당신이 스스로 잡은 것 — 이건 «규율»로 올립니다

> 「씨앗을 «지어내서» 넣었더니 state=empty. 「die 씨앗이 안 된다」로 보고할 뻔했습니다.
>  원장에서 «실제 있는» 키를 뽑아 다시 넣으니 ready. 그 좌표는 «검사된 적이 없는» 자리였습니다」

🔴 **이게 오늘 이 프로젝트에서 세 번째로 나온 같은 함정입니다:**
```
· 클라   지어낸 node id 로 마킹 -> walk 이 «없는 노드»에서 출발 (마킹 게이트로 막음)
· 총괄   맵 셀 207개를 보고 「정상」 -> 전부 «표 셀»이었음
· 구현자 지어낸 die 씨앗 -> 「기능이 죽었다」와 «구분 불가»
```
**전부 「빈 답」이 두 가지 뜻을 가지는 자리입니다.** 규칙 하나로 적습니다:
```
🔴 시험의 «입력»은 지어내지 않는다. 실재하는 것에서 «뽑아» 쓴다.
   지어낸 입력에 대한 빈 답은 «아무것도 증명하지 않는다»
```
보고에 계속 이렇게 적어 주십시오 — 「하마터면」이 제일 값진 문장입니다.

---

## 다음 순서 (변경 없음)
```
1  화면 복구   옛 경로로 그리기 + 「좌표 계약 대기」 표시      <- 클라·응용
2  접힘 + placements «한 커밋»                                 <- 구현자 «승인됨»
3  core·dt step 표 둘 + 픽스처 + 선언 조각                     <- 구현자. 형판은 이미 섰습니다
```

---
# 📎 앞 회귀 보고에 «범위를 좁힙니다» — 그리고 제 「회귀」라는 말의 근거를 댑니다

앞서 「화면이 꺼졌다」고 썼는데 **저는 «후»만 재고 «전»을 안 쟀습니다.** 근거를 뒤늦게 댑니다.

## 캔버스 그리기는 «오늘 것이 아닙니다»
```
git log -S"canvas" -- client2/src/rnd_board/map_panel.js
  d77499a1  스켈레톤 — 맵 하나가 처음 섰을 때부터 «캔버스»였음
  dca6a774  선언한 프레임 위에 웨이퍼를 앉힘
  409ddaf0  마킹을 「나머지를 흐리게」로 그리고, «배지가 그린 것을 센다»
```
🔴 **`409ddaf0` 이 「배지가 «그린 것»을 센다」입니다** — 그때는 그린 것이 «있었습니다».
그러므로 지금 0% 는 «원래 그랬던 것»이 아니라 «달라진 것»입니다. 회귀가 맞습니다.

## 창(window) — 오늘 map_panel 커밋 중 «마지막 둘»입니다
```
… f9abae59  회전 180 읽기          <- 이 시점 dist 를 소유자가 «보셨고 맵이 있었습니다»
   627a1b2f  spaces 선언 + 칸의 개수
   e26bf2dc  점이 자기 자리를 말함  <- 지금
```
**둘 중 하나이거나 둘의 상호작용입니다.** 이분해서 넣지 마시고 «둘 다» 의심하십시오.

⛔ 제가 앞서 적은 「width/height 가 없어서」와 「spaces 조회가 빗나가서」는 **여전히 제 추측**입니다.
   위 창(commit window)만 «근거 있는 것»입니다.

---

# 🔴🔴🔴 회귀 — **맵이 «아무것도 안 그립니다»**. 총괄이 방금 브라우저에서 실측

지시서에 「중간에 화면이 꺼지면 그 커밋은 되돌리십시오」라고 적었고, **꺼졌습니다.**

## 실측 (2026-08-24 · localhost:8080/rnd-board.html · dist 최신)
```
.rb-map__canvas    tag «CANVAS» · attrs 는 class 와 style="display: block" «둘뿐»
                   width/height 속성이 «없습니다» -> 기본 300 x 150
                   getImageData 로 알파 채널을 셌습니다:  칠해진 픽셀 «0» (0.0%)
```
**부품의 «테두리»는 전부 정상입니다:**
```
머리      「본딩 맵 · 슬롯 07」 · 「본딩축 · 스테이지 좌표」
수치      「141칸 · 발견 13 · 검사 29 · bonding_log ∩ inspection_run 기준」
기반      bond_layer 10 (선택됨) · dt_slot 18 · wafer_grid 10
페이저    ‹ 07 / 25 · SYN-BW-001-07 ›
배지      읽기 marking:1 · 쓰기 marking:1 · 표시 0
맵 밖     「맵 밖 · 검사 1 · 발견 1」
코어 맵도 «같습니다» (110칸 · 발견 13 · 검사 29 · wafer_grid 선택 · 캔버스 0%)
```
🔴 **응답은 옵니다. 숫자도 맞습니다. «그리는 자리»만 비었습니다.**

## 제가 «오진할 뻔한» 것 — 적어 둡니다
```
처음    document.querySelectorAll('[class*="cell"]') -> 207개.  「정상」이라 읽을 뻔했습니다
실제    207개가 «전부» rb-table-cell (새 표 부품). 맵 셀은 «0» 이었습니다
그다음  <canvas> 는 DOM 자식이 없는 게 «정상»이라 여기서 멈출 뻔했습니다
        -> getImageData 로 «픽셀»을 세어서 확정했습니다
```
**두 번 다 「비어 있음」이 「정상」처럼 보였습니다.** 당신 쪽 검사도 이 함정에 빠질 수 있으니
**픽셀(또는 그에 준하는 것)로 단언하십시오** — 요소 개수로는 안 잡힙니다.

## 판정 — 고치든 되돌리든 «화면이 먼저»입니다
```
① 지금 화면이 «꺼져 있습니다». 그게 제일 급합니다
② 원인을 찾아 «한 커밋»에 고치거나, 못 찾으면 «되돌리십시오». 둘 다 정당합니다
   -> 되돌리는 것이 부끄러운 것이 아닙니다. 꺼진 채로 두는 것이 문제입니다
③ 고친 뒤 «픽셀 단언»을 하니스에 넣으십시오
   지금 하니스가 초록인데 화면은 비어 있습니다 — 그 간극이 이 사고의 본체입니다
   🔴 「요소가 있다」가 아니라 「무엇이 그려졌다」를 재야 합니다
```

## 📌 짐작 하나 — «참고만» 하십시오. 제 진단이 아닙니다
캔버스에 width/height 속성이 «아예 없습니다». 크기가 안 잡히면 그리기 자체가 0x0 이거나
기본 300x150 밖으로 나갈 수 있습니다. `space` 선언을 찾는 자리에서 일찍 반환하는 갈래도
후보입니다 (die 인스턴스인데 spaces 조회가 빗나가면 inchip 처럼 「해당 없음」으로 빠질 수 있음).
⛔ **둘 다 제 추측입니다.** 당신이 재서 정하십시오 — 제가 갈래를 지목해서 엉뚱한 데를
   고치게 한 적이 오늘 이미 있습니다.

---

# ⚖️ 총괄 판정 — `placements` «승인». 그리고 이것이 «확대까지 공짜로» 만듭니다

`§10` 읽었습니다. **제가 물은 구멍을 확인하고 더 나은 모양을 냈습니다. 그대로 갑니다.**
```
실측    dt_map 5,747행 · dt 좌표 100% · core 좌표 «128행(2.2%)» · 둘 다인 행 128
        -> position{x,y,frame} 은 한 점에 자리를 «하나»만 줍니다. 128행이 표현 불가
승인    placements: [ { space, x, y }, … ]   «0개 이상». 빈 목록이 «정상값»
승인    lot_map 낱말(bond·dt·core) «폐기» -> 선언 낱말이 정본. lot_map 이 버려졌으니 맞습니다
```

## 🔴 제가 더할 것 — 「같은 점이 자리 «둘»」이 곧 «확대»입니다
당신은 dt_map 때문에 목록으로 갔는데, **그 모양이 확대 문제를 «같이» 풉니다.**
```
void 점 하나
  placements: [ { space: "die:base", x: 13,      y: 5      },
                { space: "inchip",   x: 14041.75, y: 9879.75 } ]
```
```
웨이퍼 맵    die:base 자리를 씁니다      -> 칸 하나
칩 확대      inchip 자리를 씁니다        -> 실제 위치·실제 크기
composite    inchip 자리를 «여러 다이»에서 모읍니다 -> 한 점 = 진짜 디펙 하나
🔴 셋 다 «같은 노드 · 같은 walk». 두 번 묻지 않습니다
```
**그래서 `placements` 는 dt_map 예외처리가 아니라 «좌표의 정상 모양»입니다.**
그 말을 계약에 적어 두십시오 — 안 적으면 다음 사람이 「dt 때문에 만든 것」으로 읽고
확대에서 다시 한 겹을 만듭니다.

## 🔴 크기도 자리에 «붙습니다»
```
void   die:base 자리엔 크기가 «없습니다» (칸이 다이 전체입니다)
       inchip  자리엔 radius_x/y 가 «있습니다» (0.50~31.48um, 평균 8.16)
delam  inchip 자리에 extent_x/y
```
그러므로 크기는 점의 속성이 아니라 «자리»의 속성입니다:
```
{ space: "inchip", x, y, extent: { x, y } }      <- extent 는 «선택». 없으면 없는 대로
```
⛔ die:base 자리에 크기를 억지로 넣지 마십시오. 다이 칸에 반경은 뜻이 없습니다.

## 「선언된 space」 vs 「실제 자리」 — 당신 구분이 맞습니다. 규칙으로 못 박습니다
```
소스 선언  spaces: [...]        「이 소스는 «이 좌표계들에 설 수 있다»」
점의 실제  placements: [...]    「이 점은 «실제로 이 자리들에 있다»」
🔴 둘이 다릅니다.  dt_map 은 die:dt·die:core 를 «선언»하지만 5,619행은 core 자리가 «없습니다»
```
```
맵 부품이 하는 일
  소스가 내 space 를 «선언 안 했다»   -> 인스턴스가 «안 섭니다» (해당 없음)
  선언은 했는데 이 점에 그 자리가 없다 -> 그 점만 «안 그립니다». 맵은 «섭니다»
                                        그리고 그 수를 «맵 밖»에 셉니다 (unplaced 자리)
⛔ 둘을 같은 문장으로 말하지 마십시오 — 하나는 「이 맵은 없다」, 하나는 「이 점이 빠졌다」입니다
```

## 남은 것
```
· 이 §10 을 «맵 부품 명세»(총괄 -> 클라, 77603751)와 «낱말이 같은지» 대조하십시오
  제가 거기 space 를 die|inchip 로 적었는데 당신은 die:base|die:core|die:dt|inchip 입니다
  🔴 «당신 것이 맞습니다» — 제 명세를 당신 낱말로 고치겠습니다. 클라에 알리겠습니다
· extent 를 계약에 넣으십시오 (위)
```

---
# 🔴🔴🔴 소유자 — 「보이드만이 아니다. DT·core 계측 디펙도 «전부». MI 는 맵은 못 해도 «트렌드는 같아야»」

> 소유자: 「지금 보이드만 하고 있는데 «dt상 계측된 디펙, core상 계측된 디펙» 등도 다 포함인 거 알지?
>  그리고 «mi 계측값»도 맵은 안 되지만(측정 좌표가 «코드»로 뜨고 그 코드랑 맵핑해야 하는데
>  그걸 모름) «트렌드 레벨에서는 같은 동작» 되어야 하고」

**맞습니다. 보이드는 «배관을 뚫는 첫 소스»이지 대상이 아닙니다.**
보이드 전용 코드가 한 줄이라도 생기면 그 라운드는 실패입니다.

---

## 1. 실측 — 소스 전수. 그리고 «좌표계가 셋»입니다

```
소스                 행수      die 좌표계        inchip    비고
void_obs          103,729   base(웨이퍼)        ✅        radius_x/y · stack_gate
delam_obs          11,561   base(웨이퍼)        ✅        extent_x/y · interface(2종)
core_defect_map     5,807   core(lot+slot)      ❌        val
eds_fail_map        2,576   core(lot+slot)      ❌        val · metro_eqp
dt_map              5,747   dt «와» core «둘 다» ❌        value · dt_job+dt_x/y + core_wafer+core_x/y
inspection_run    117,662   base(웨이퍼)        ❌        «분모». method · eqp_id · recipe_id · observed_at
metro                  «0»  «없음»              ❌        item_id · subitem_id ← 소유자가 말한 «코드»
defect · void · test    0   (빈 표)
valid_die_ref       5,378   product+x/y                   「그 자리가 칩인가」
```
🔴 **셋을 놓치면 안 됩니다:**
```
① inchip 을 가진 건 «둘»뿐 (void · delam). core·eds·dt 는 die 격자«만»
   -> 확대/composite 뷰는 그 둘에서만 섭니다. «나머지는 없는 게 정상»입니다
② dt_map 은 좌표계를 «둘» 가집니다 (dt 격자 + core 격자)
   -> 한 소스가 두 맵에 얹힙니다. 「소스 하나 = 맵 하나」로 설계하면 여기서 깨집니다
③ metro 는 좌표 컬럼이 «아예 없습니다». item_id/subitem_id 가 그 「코드」입니다
   그리고 «0행»입니다 -> 선언은 쓸 수 있어도 «오늘 검증할 데이터가 없습니다»
```

---

## 2. 🔴 판정 — 「맵 가능」은 «선언»이고, 「트렌드 가능」과 «따로»입니다

```
소스가 선언하는 것    spaces: [ ... ]      자기가 설 수 있는 좌표계 «목록»
  void_obs            ["die:base", "inchip"]
  delam_obs           ["die:base", "inchip"]
  core_defect_map     ["die:core"]
  eds_fail_map        ["die:core"]
  dt_map              ["die:dt", "die:core"]        <- 둘
  metro               []                            <- 🔴 «빈 목록». 이것이 정답입니다
```
```
맵 부품    spaces 에 자기 space 가 «없는» 소스는 «세우지 않습니다»
           -> 거절이 아니라 «해당 없음». 화면에 빈 맵을 띄우지 마십시오
트렌드     spaces 를 «보지 않습니다». 좌표가 0인 소스도 «똑같이» 트렌드가 됩니다
           🔴 이것이 소유자 지시의 핵심입니다 — 「맵은 안 되지만 트렌드는 같은 동작」
```
🔴 **`spaces: []` 를 「결함」으로 읽지 마십시오.** MI 는 원래 자리가 없는 계측입니다.
   「좌표가 없다」와 「좌표가 있어야 하는데 빈다」는 «다른 것»이고, 선언이 그 둘을 가릅니다.

---

## 3. 그래서 각 레인이 지금 바꿀 것

```
클라     맵 부품 명세(77603751)에 «space 목록» 축을 더하십시오.
         부품은 자기 space 를 선언에서 받고, 소스가 그 space 를 «안 가지면» 인스턴스가 «안 섭니다»
         🔴 `if (kind === 'void')` 류 «전면 금지». finding_kind 로 분기하는 코드가 있으면 지금 뽑으십시오
응용     좌표 계약을 «소스 여섯»으로 넓히십시오. 지금 void 하나로 쓰여 있습니다.
         특히 dt_map 의 «두 좌표계»를 계약이 표현할 수 있는지 보십시오 — 못 하면 그게 계약의 구멍입니다
구현자    선언이 늘어납니다. 지금 die_inspection 하나 세우는 것이 «여섯의 형판»입니다.
         그것부터 끝내십시오 — 형판이 안 서면 여섯이 다 막힙니다
총괄     선언 여섯을 씁니다. 순서: inspection_run(분모) -> void_obs -> delam_obs
         -> core_defect_map -> eds_fail_map -> dt_map.  metro 는 «데이터 0» 이라 맨 뒤
```

---

## 4. ⚠️ 총괄이 «못 잰» 것 — 추정하지 않고 적습니다
```
· core_defect_map / eds_fail_map 의 (lot, slot) 이 웨이퍼 하나로 «해석되는지»
  -> void_obs 는 base_wafer_id 로 웨이퍼를 직접 말하는데 이 둘은 lot+slot 입니다.
     같은 다이를 가리키는지 «대조 안 해 봤습니다»
· dt_map 의 core_x/core_y 가 core_defect_map 의 x/y 와 «같은 격자인지»
· metro 의 item_id → 좌표 매핑이 «어딘가에 있는지» (소유자: 「그걸 모름」)
  -> 없으면 없는 대로 트렌드만 하면 됩니다. «찾으라는 지시가 아닙니다»
```
🔴 이 셋을 «추정으로 메우지 마십시오». 필요해지는 라운드에 재면 됩니다.

---

# 🔴🔴🔴 소유자 지시 — 「**맵 부품 좋은 거 하나** 만들어 둬」. 확대도 그 부품이 합니다

> 소유자: 「다이 확대는 «맵 부품의 확대». 좀 맵 부품 «좋은거 하나» 만들어둬야겠는데」

**이것이 이 화면의 「근원 템플릿」 중 제일 큰 조각입니다.** 표 부품(`d6bc18f8`)을 하나로 만든 것과
같은 일을 맵에 합니다. 지금 잘 만들면 앞으로 맵은 «선언 한 줄»입니다.

---

## 1. 🔴 확대는 «모드»가 아닙니다 — 좌표계 선언의 «다른 값»입니다

이걸 놓치면 부품 안에 `if (zoomed)` 가 생기고 조립식이 안쪽에서 무너집니다.

```
웨이퍼 맵    좌표계 = die     한 칸 = 다이 하나        단위 cells_from_origin
칩 확대      좌표계 = inchip  한 «점» = void 하나      단위 um
             -> 같은 부품. 프레임과 좌표계 «선언»만 다릅니다
```
**실측 — 확대의 재료는 이미 100% 있습니다:**
```
void_obs  103,729행 중 inchip_x · inchip_y · radius_x · radius_y 가 «103,729» (100%)
          단위 «um» 한 종류 · inchip 범위 50..19,950 · radius 0.50..31.48 (평균 8.16)
delam_obs  11,561행 전부 inchip · extent · interface(2종)
원장 payload 에도 이미 실려 있음:
  {"die":{"x":13,"y":5,"gate":3}, "unit":"um",
   "inchip":{"x":14041.75,"y":9879.75}, "extent":{"x":9.831,"y":8.021}}
```
🔴 **축척이 설계를 강제합니다** — 칩 20,000um 안에 반경 8um. 비율 «1/2,500».
웨이퍼 맵 위에 실제 크기로 그리면 0.008px 입니다. **그래서 확대에서만 뜻이 있습니다.**
```
웨이퍼 맵   칸 = 다이.  void 는 «수»로만 (n)
칩 확대     칩 하나를 화면 가득.  void 를 «실제 위치·실제 크기»로
            -> 「가장자리 편중 / 가운데 / 한쪽 방향」이 보입니다. 그게 원인을 좁힙니다
```

---

## 2. 선언 — 부품 «밖»에서 주는 것. 이게 전부여야 합니다

```
frame        { cols, rows, start_x, start_y, y_invert, rotation, side }
             🔴 rotation 을 «부품이 읽어» 0도 정규 좌표로 앉힙니다 (f9abae59 유지)
             축 이름으로 분기 «금지» — 선언만 봅니다
space        "die" | "inchip"          <- 확대는 여기 값 하나
unit         "cells_from_origin" | "um"
extent       칩 확대일 때 칩 한 변의 물리 크기 (phys_chip_x/y)
reads        읽을 마킹 이름     (예: marking:1)
writes       쓸 마킹 이름       (예: marking:1)
collect      무엇을 걷어 칠하나  (walk 의 collect)
start        어느 마킹에서 걷나  (walk 의 start)
label/sublabel
```
🔴 **선언에 «없는 것»**: 축 이름(bond/dt/core) · 라우트 이름 · 랏 · 슬롯.
   부품이 그걸 알면 다음 축이 생길 때 부품을 고쳐야 합니다.

---

## 3. 부품이 «반드시» 지키는 것 여섯 — 전부 오늘 실측에서 나왔습니다

```
① 없음 세 갈래를 «구분해» 그린다
   found      났다              「n 개」를 «잃지 않는다» (실측 다이당 평균 2.06 · 최대 13)
   scanned    봤는데 안 났다     = 컨트롤(−)의 재료
   unscanned  «안 봤다»          0 이 아니라 «목록에 없는 것»
   🔴 이 셋이 화면에서 «서로 달라 보여야» 합니다. 둘로 뭉치면 이 제품이 아닙니다
② 맵 «밖»을 그림 밖에 «수»로 말한다
   검사됐는데 좌표가 없는 자리. 지어내지 않고 수로만 (unplaced)
③ 서버가 준 node id «만» 마킹한다
   지어낸 id 는 «그릴 수는 있어도» 마킹 불가. 거절을 «소리 내어» 말한다 (99bd4da0)
④ 스팟파이어 조작 계약
   클릭 = 교체 · Ctrl+클릭 = 추가 · Shift = 컨트롤(−)
   마킹 표현 = «나머지를 흐리게» (선택을 칠하는 게 아니라)
⑤ 자기 div 하나 · 모듈 수준 상태 «0»
   같은 화면에 «두 인스턴스»를 서로 다른 선언으로 앉혀 간섭 없을 것 — 이게 합격 기준
⑥ 빈 결과를 «정직하게» 그린다
   walk 이 아무것도 안 주면 「없음」. 「0」이 아니고, 빈 사각형도 아닙니다
```

---

## 4. 한 칸이 무는 것 — 실측. 여기서 확대가 «자연스럽게» 나옵니다

```
맵의 한 칸        die 노드 «하나»  { wafer, x, y }
그 칸이 무는 것   finding point «N개»   실측 평균 2.06 · 최대 13
                  1개 18,524 · 2개 14,964 · 3개 15,072 · 4~13개 1,906 다이
같은 다이를 run 둘이 본 경우 1,545건 -> 원장은 갱신을 안 하므로 «둘 다» 남습니다
```
```
칸을 찍는다  =  die 노드를 마킹한다
확대한다     =  그 die 의 point N개를 «inchip 좌표계»로 다시 그린다
             -> 새 질의가 아닙니다. «이미 걸어온 하위 그래프»를 다른 좌표계로 그리는 것입니다
```
🔴 **그래서 확대에 walk 이 한 번 더 필요하지 않습니다.** 같은 마킹, 같은 collect, 다른 space.

---

## 5. 합격 판정 — 「그려진다」로는 못 잡습니다
```
A  같은 화면에 «두 인스턴스» (본딩 · 코어) — 간섭 0
B  rotation 180 인 프레임과 0 인 프레임에 «같은 (x,y)» -> «다른 화면 자리»
   정규화 후엔 «같은 물리 자리»를 가리킬 것        <- 뒤집힌 그림도 그려집니다
C  found · scanned · unscanned 가 «서로 다르게» 보일 것
D  n=1 인 칸과 n=13 인 칸이 «구분»될 것 (색 농도든 배지든 — 방식은 당신 선택)
E  지어낸 id 인 칸을 클릭 -> 마킹 «안 되고», 왜 안 되는지가 «보일» 것
F  space="inchip" 선언 하나로 확대가 서고, 부품 코드에 `if (zoom)` 이 «없을» 것
```

---

## 6. 순서 — 지금 있는 것을 «부수지 말고» 모으십시오
```
1  지금 map_panel.js 가 이미 ①③④⑤ 를 부분적으로 합니다. «다시 쓰지 말고» 선언을 뽑아내십시오
2  space/unit 축을 «값»으로 넣으십시오. 지금은 die 하나뿐이어도 됩니다
3  inchip 인스턴스를 «선언 하나»로 세워 보십시오 — 재료는 서버가 아직 안 실어 줍니다.
   🔴 그러면 「없음」이 나오는 게 «정상»입니다. 그것으로 F 를 판정하십시오
4  서버가 point 에 position + inchip 을 실으면 «코드 변경 없이» 그림이 나와야 합니다
```
📎 총괄이 좌표 계약에 「point 는 die 좌표와 inchip 좌표를 «둘 다» 낸다」를 넣습니다.
   그러면 4 가 «선언도 안 바꾸고» 켜집니다.

⚠️ 이건 «지금 도는 화면을 멈추고 하는 재작성이 아닙니다». 지금 맵이 계속 그려지는 채로
   선언을 뽑아내는 작업입니다. 중간에 화면이 꺼지면 그 커밋은 되돌리십시오.

---

# 🔴🔴🔴 소유자 판정 — 「**레거시 다 버려**」 (2026-08-24). 세어서 목록을 냅니다

앞의 「lot map 버려」에 이어집니다. **화면의 데이터는 walk 하나** — 그러면 그 앞의 시도들이 전부 레거시입니다.
🔴 **추측으로 지우지 않습니다.** 아래는 «호출자를 센» 목록입니다.

## 실측 — 클라 파일별 원장 라우트 호출 (2026-08-24)
```
■ A. 이 화면의 «이전 시도» — 버릴 것
  surprise_map_core.js          lot_map
  surprise_axis.js              lot_map · lot_axis_map
  surprise_core.js              lots
  ledger_trace.js               trace · coverage · journey · lot · lots · siblings · structure · lot_map
  ledger_trace_core.js          trace · coverage
  case_control_core.js          coverage
  contrast_core.js              siblings
  journey_core.js · journey_view.js   journey
  lot_reference_core.js         lot

■ B. 지금 화면 — walk 으로 «갈아타는» 중
  rnd_board/api.js              composition · lot_map · siblings · subgraph · trends
  rnd_board/composition_panel.js  composition
  rnd_board/main.js             lot_map
  -> 목표: subgraph «하나». 나머지 넷은 소유자 도식에 «없습니다»

■ C. 🔴 «다른 제품 표면» — 이번 판정에서 «제외»합니다
  ledger_graph/*                explore · structure · subgraph      (그래프 뷰어)
  ledger_map_panel.js           kinds · structure                   (원장 맵 패널)
  ontology_structure_core.js    kinds · structure                   (구조 뷰)
  entity_catalog.js             entities
```
🔴 **C 를 뺀 이유:** 이건 「이 화면의 이전 시도」가 아니라 «다른 기능»입니다.
「레거시」로 묶어 지우면 도는 제품을 끄는 것입니다.
**C 도 버리라시면 한 마디만 주십시오 — 그러면 같이 넣습니다.** 그전까지는 안 건드립니다.

## 순서 — 🔴 lot_map 때와 같습니다. 「무엇이 같이 사라지나」를 먼저 셉니다
```
1  A 의 화면들이 «지금 사용자에게 보이는지» 세십시오
   -> `client2/*.html` 의 script[src] 와 라우팅. 「소스에 있다」와 「화면에 있다」는 다릅니다
2  A 를 지우면 «호출자 0» 이 되는 라우트를 «세십시오»
   후보: lot_axis_map · lot · lots · trace · coverage · journey · siblings · composition · trends
   🔴 C 가 쓰는 것(structure · kinds · explore · entities · subgraph)은 «0 이 안 됩니다»
3  B 가 walk 으로 갈아탄다  (앞선 지시)
4  «0 이 된» 라우트와 그 서버 코드를 지운다
5  그 라우트만 재던 테스트도 «같은 커밋»에서 지운다
   🔴 먼저 지우면 무방비, 늦으면 수집이 막힙니다. 파일명이 아니라 «테스트 단위»로 가르십시오
```
⚠️ **2 를 건너뛰고 4 를 하지 마십시오.** 「이 화면이 안 쓰니 라우트도 안 쓴다」가
C 때문에 «거짓»일 수 있습니다.

## 레인
```
응용     1·2 를 세십시오 (양쪽을 아는 레인). 결과를 «표»로. 판정은 총괄이 합니다
클라     A 화면 제거는 «2 가 끝난 뒤». 지금은 B 갈아타기에 집중하십시오
구현자   4·5 는 «2·3 이 끝난 뒤». 지금은 선언 로드 살리기가 «먼저»입니다
```

---

# ⚖️ 정정 — 빨간 하니스에 대한 제 판정이 «틀렸습니다»

응용 보고 `b6688f73`: 「빨간 하니스는 «제 깨진 작업 트리» 때문이지 계약 변경이 아니었습니다」.

**그쪽이 맞습니다.** 제가 「계약이 바뀐 것이 맞으니 새 계약으로 다시 쓰라」고 판정했는데,
원인은 커밋 안 된 작업 트리였습니다. 저는 **빌드 출력만 보고 원인을 «추정»했습니다** —
그 시각 `api.js` 에 미커밋 36줄이 있다는 것을 «제가 같은 화면에서 보고도» 연결하지 않았습니다.
```
취소   「rnd_board_composition_harness.mjs 를 새 계약으로 다시 쓸 것」
사실   작업 트리가 정리되자 빌드가 «초록»입니다 (✓ built in 829ms, dist 착지·푸시 완료)
```
📎 KNOWN_RED 금지는 «그대로 유효»합니다 — 그건 원인과 무관한 규칙입니다.

---

# 🔴🔴🔴 소유자 판정 — 「**lot map 버려**」 (2026-08-24)

**확정입니다. 제가 앞서 「walk 이 그림을 낼 때까지 lot_map 을 살려 둔다」고 한 판정을 «취소»합니다.**
맵의 데이터 출처는 **walk 하나**입니다.

## 🔴 그런데 lot_map 은 «질의»만이 아니었습니다 — 같이 사라지는 것 «여섯»
버리는 건 결정됐고, 이 여섯이 walk 으로 «안 옮겨지면» 맵은 빈 채로 남습니다.
**하나씩 «어디로 가는지» 정하고 버립니다. 조용히 사라지면 안 됩니다.**
```
① 프레임        frame.grid = { grid_cols · grid_rows · grid_start_x/y · grid_y_invert
                              · rotation · side · phys_chip_x/y · phys_offset_x/y
                              · phys_wafer_dia · phys_edge_margin }
                🔴 회전 180 이 여기 삽니다. 이게 없으면 방금 착지한 회전 수리가 «먹을 것이 없습니다»
② 유효 다이     valid_die_ref { table, map_id }  — 「이 자리가 애초에 칩이 있는 자리인가」
③ 칸의 상태     cell.state = found | scanned | unscanned
                🔴 «unscanned» 가 제일 중요합니다. 0 이 아니라 «안 봤다»입니다
④ 분모          scanned / found 수  — 트렌드 y 의 분모가 여기서 나옵니다
⑤ 맵 밖         unplaced { state, scanned, found, reason, message }
                🔴 검사됐는데 좌표가 없는 자리. 지어내지 않고 «수로만» 말하는 그 자리입니다
⑥ 겹침 표시     superposed · available_slots · frames_matched / frames_considered
```
🔴 **①③⑤ 는 「없음」을 «세 갈래»로 구분하는 장치입니다** — 이 제품이 다른 대시보드와
다른 이유가 그것입니다. walk 이 이걸 못 실으면 「버렸다」가 아니라 «퇴보»입니다.

## 순서 — 🔴 버리는 «순간»이 아니라 «순서»가 중요합니다
```
1  walk 이 위 여섯을 «싣는다»            <- 구현자. 이게 되기 전엔 아무것도 안 지웁니다
2  맵 부품이 walk 만 부른다               <- 클라 (api.js 는 응용)
3  화면에서 lot_map 호출이 «0» 이 된다     <- grep 으로 «세어서» 확인
4  라우트·서버 코드를 «지운다»             <- 구현자. 3 을 확인한 «뒤»
```
⚠️ **1 을 건너뛰고 2 를 하면 맵 셋이 빕니다.** 지금 walk 이 닿는 좌표 노드는
die transfer «1,405개»뿐이고, finding point 는 `position {}` 입니다.

## 레인별 — 지금 «즉시» 바뀌는 것
```
클라     🔴 lot_map 을 «더 손보지 마십시오». 앞서 제가 낸 슬롯 페이저·프레임 관련 지시 중
         lot_map 응답을 다듬는 것은 «전부 취소»입니다.
         회전 정규화(f9abae59)는 «유지» — 프레임이 walk 으로 옮겨와도 그대로 씁니다
         맵 부품의 입력을 walk 으로 바꾸는 것이 «다음 일»입니다
구현자   🔴 위 여섯을 walk 응답에 싣는 «계약»을 먼저 내십시오 (구현 전에).
         특히 ①프레임이 «어느 노드에 붙는가» — 소유자 기존 판정: 「die 에 붙는 게 좋겠다」.
         그리고 이건 선언 로드 살리기 «다음»입니다. 순서 바꾸지 마십시오
응용     MARKING_CONTRACT 에 「walk 이 실어야 하는 것 여섯」을 «절»로 추가하십시오.
         지금 §7 이 서버 요구 둘인데, 이제 «여덟»입니다
```

## 📎 그리고 이 판정이 «자동으로» 답하는 것 하나
`lot_map` 이라는 이름과 `axis.name.endswith("_lot")` 기본값을 어떻게 할지 제가 판정 대기로
올려 뒀는데, **버리면 그 질문이 사라집니다.** 대기 목록에서 뺍니다.

---

# 🔴🔴🔴 마킹이 «이미 꼬여 있습니다» + 분업 변경 — `api.js` 는 이제 «응용 세션»이 씁니다

> 소유자: 「마킹 한번 꼬이면 답없다」 / 「저쪽 클라 세션이 «알아야지»」
> 「아니면 이거 응용 세션 시키든가 — 서버랑 클라 다 아는」

## 🔴 분업 — 먼저 읽으십시오. 파일이 갈립니다
```
당신 (클라)    패널 전부.  회전 정규화.  목업 대조 항목들
응용 세션      `client2/src/rnd_board/api.js` «한 파일» + 마킹 계약 문서
               -> 이 파일은 «건드리지 마십시오». 충돌합니다
총괄           선언 파일 셋 (ledger_config · siblings_axes · mechanism_models)
구현자         서버 (접힘 통과 · finding point 에 좌표 싣기)
```

## 총괄 실측 — 마킹이 «안 이어져» 있습니다. 이게 아침 지적 ④의 정체입니다
```
marking:0  씨앗   메인 트렌드가 «쓰는» 곳
marking:1  맵     본딩 맵이 «읽는» 곳
🔴 둘을 이어 주는 파생이 «없습니다». 이름이 다릅니다
   -> 트렌드에서 점을 찍어도 맵이 «안 바뀌는 게 당연»합니다.
      버그가 아니라 배선이 애초에 안 이어져 있습니다. 맵은 «자기가 쓴 것만» 읽습니다
marking:3  교집합  파생은 도는데 «읽는 부품 0개» — main.js 주석이 스스로 인정합니다
                  (「NO PART READS marking:3 YET」)
소유자 도식        마킹은 «둘»입니다. 화면엔 «넷»입니다
```

## 🔴 그리고 맵 칸은 «아무 노드도 안 물고» 있습니다 — 총괄 실측
```
lot_map 셀      {x, y, n, state}          node_id «없음»
finding point   keys.position «{}»         좌표 «없음» (좌표는 run_uid 문자열 안에만)
그래서          stampedNodeId 가 id 를 «지어냅니다»
                지어낸 id 로 마킹하면 walk 이 «없는 노드»에서 출발합니다
```
🔴 **총괄 규칙 — 지금부터 지키십시오**
```
«서버가 준 node id 만» 마킹이 될 수 있습니다.
지어낸 id 는 «그릴 수는 있어도» 마킹에 «못 들어갑니다».
-> `nodeIdResolved` 가 이미 진짜/가짜를 구분하고 있습니다. 그것을 «게이트»로 쓰십시오
-> 거절할 때 조용히 무시하지 마십시오. 「이 자리는 아직 노드가 없다」가 화면에 보여야 합니다
```
서버가 좌표를 싣기 시작하면 이 게이트는 «저절로 열립니다». 그때 코드는 안 바뀝니다.

## 목업 대조 — 총괄이 «직접» 봤습니다 (상설 ②). 아직 «통째로 없는» 것 넷
```
🔴 스텝 사슬      BONDING › POST_BOND_QUEUE › MOLDING › DT   (머리 요약 «아래»)
🔴 펼친 층 패널    「펼친 층 · L04」 + 스텝 사슬 + claims 표(actual / setpoint)
🔴 후보 트렌드     「마킹한 후보 트렌드 (마킹 2 · bond_temp)」 — 메인 트렌드와 «같은 부품»
🔴 X value 축      목업 축 열에 있습니다 (계측 시각 / 스텝 시각 8). 화면엔 없습니다
```
**있지만 다른 것:**
```
구성 표    슬롯 컬럼 «없음»(「응답에 슬롯이 없습니다」) · 「이력 8 ›」 열 없음 · 「← 후보 5」 배지 없음
머리       목업은 「마킹 1 · 34행 / 마킹 2 · 1행」 을 머리에 답니다 — 화면엔 없습니다
맵 머리    목업 「마킹 34 · void 165 · delam 9」  /  화면은 「표시 0」 뿐
코어 맵    목업은 «어느 층인지» 답니다 (L04 · SYN-CORE-WAFER-09) — 화면엔 슬롯 번호만
값 없음    목업 「값 없음 20 ›」 — «수»가 붙습니다. 화면은 수 없이 「값 없음」
```
✅ **착지 확인:** 맵 가로 배치 · 코어 맵(마킹2) · 슬롯 페이저(07/25) · 우측 컨트롤 열 · x축 자재 id

📎 목업 자신이 「접는 단위 Wafer 114,492 · **die 0** · DT 0」 이라 적어 뒀습니다 —
   총괄이 오늘 잰 수와 «같습니다». 목업을 그린 쪽도 die 원자가 0인 걸 알고 있었습니다.

## 우선순위
```
1  마킹 게이트 (지어낸 id 거절) — 꼬이기 «전»에 막는 것이라 제일 위입니다
2  회전 정규화 (앞선 지시)
3  후보 트렌드 = 트렌드 부품의 «둘째 인스턴스» (새 부품 금지)
4  펼친 층 · 스텝 사슬 · X value 축
5  표 부품 하나 + 컬럼 선언 (앞선 지시)
```

---

# 🔴🔴 소유자 지적 — 「맵은 다 «0도로 표준화»해서 그리는 거지?」 → **아닙니다. 회전을 «안 읽고» 있습니다**

## 제 실측 — 서버는 «보내고 있고», 클라가 «안 씁니다»
`lot_map?row=SYN-VOID-001&slot=07` 의 각 축 `frame.grid`:
```
본딩축   "rotation": 180   grid_y_invert: false   side: front   phys_offset (7, -4)
DT축     "rotation":   0                                        phys_offset (0, 0)
코어축   "rotation":   0                                        phys_offset (5, -3)
```
```
grep rotation  client2/src/rnd_board/*.js   ->  «0회»
map_panel.js 에도 «0회». 원시 (x, y) 를 그대로 칸에 앉히고 있습니다
```

## 🔴 왜 지금 터지느냐 — «마킹이 맵을 건너다니기» 때문입니다
지금은 맵을 따로 보니 티가 안 납니다. 그런데 방금 확정된 설계에서
**마킹 하나가 본딩·DT·코어를 동시에 가리킵니다.**
```
본딩 (3,4) 와 코어 (3,4) 가 «물리적으로 다른 자리»입니다 (본딩만 180도 돌아 있음)
-> 후보 맵이 «엉뚱한 칸»을 밝힙니다. 그리고 그건 «틀렸다는 티가 안 납니다»
```
📎 이 자리엔 전과가 있습니다 — `server/dt_frame_transform.py:50` 에
「원 박스 공식이 «회전이 올바로 확정돼 있는데도» 90/180도 프레임을 밀어낸다」고 적혀 있습니다.

## 할 것 — 🔴 «한 곳»입니다. 축 이름으로 가르지 마십시오
```
자리      기반 맵 부품의 «칸 배치» 함수 하나
입력      frame.grid 의 rotation · grid_y_invert · side  (+ 필요하면 phys_offset)
출력      «0도 정규 좌표». 모든 맵이 같은 방향으로 섭니다
금지      if (axis === 'bond') 류. 프레임 «선언»을 읽으십시오 —
          축이 넷째로 늘어도 코드가 안 늘어야 합니다
```
🔴 **부품 «안»에서 축마다 분기하면 조립식이 안쪽에서 무너집니다** (소유자 상설 ①).

## 시험 — 「돌려도 티가 안 나는」 버그라 단언이 필요합니다
```
· rotation 180 인 프레임과 0 인 프레임에 «같은 (x,y)» 를 넣어 «다른 화면 자리»가 나올 것
· 그 둘을 정규화한 뒤에는 «같은 물리 자리»를 가리킬 것
🔴 「그려진다」로는 못 잡습니다. 뒤집힌 그림도 그려집니다
```

---

# 🔴🔴🔴 소유자 상설 — 「마킹한 노드의 «하위 그래프»를 데이터로 들고 온다」 (2026-08-24)

> 소유자: 「맞아 **마킹한 노드의 하위 그래프를 데이터로 들고 오는 거야.**
>  이거 세션들에게 **매우 강조**시켜, 아예 claude.md 에 박든가」
> → `CLAUDE.md` 에 상설로 박았습니다. **당신 세션도 이 규칙 아래 있습니다.**

**마킹은 «표시»가 아닙니다. 질의의 «주어»입니다.**
```
마킹      부호 붙은 «노드 집합». 화면 상태가 아니라 walk 의 «시작점»
데이터    그 노드에서 «걸어서 닿는 하위 그래프» 그 자체
부품      선언하는 것은 둘뿐 — { start = 읽을 마킹,  collect = 무엇을 걷나 }
라우트    그래서 여러 개일 이유가 «없습니다». 같은 walk, 다른 선언
```
🔴 **어기는 세 모양** — 전부 「돌지만 틀린」 것입니다
```
① 부품이 «거른다»       -> 거르는 건 walk 이 합니다. collect 를 고치십시오
② 라우트를 «더 판다»     -> 늘어야 하는 건 «선언»이지 갈래가 아닙니다
③ 마킹을 «하나»로 둔다   -> 마킹1이 후보를 만들고, 후보에서 찍은 게 마킹2입니다
```
📎 실측 근거: `marking_store` 는 「이름 붙은 여럿 + 부호」를 이미 들고 있는데
`api.js` 에 `positive`·`negative`·`seeds` 가 **한 번도 안 나옵니다.**
`fetchSubgraph` 가 보내는 것은 `id` «하나»와 `collect` 뿐입니다.

---

# 🔴🔴🔴 소유자가 «클라 데이터 흐름 정본»을 그려 주셨습니다 — 이게 도착지입니다

> 소유자: 「이건 다 «v5로 전환»하고, «클라에서 데이터 흐름» 말하는 거야. 내가 예시 줄게」 + 도식

## 정본 — 부품이 «일곱 개의 walk»으로 서 있습니다. 다른 라우트가 없습니다
```
서버
 │  walk ①   Start   each groupby        (🔴 일단 «wafer» 로 고정)
 │           Collect trend Y value
 ▼
기본 트렌드 데이터
 │
 ▼
마킹 1                                   (사용자가 트렌드에서 점을 찍음)
 │
 ├── walk ③  Start   specific groupby
 │           Collect y value correlate candidate      ──▶  후보
 │                                                          │
 │                                                          ▼
 │                                                        마킹 2   (사용자가 후보를 찍음)
 │                                                          │
 │                                        walk ⑦  Start   specific groupby
 │                                                 Collect mark2 type
 │                                                          ├──▶  후보 트렌드
 │                                                          └──▶  후보 맵
 │
 ├── walk ④  Start   specific groupby
 │           Collect wafer, process                   ──▶  자재 정보
 │
 └── walk ⑤  Start   specific groupby
             Collect trend Y value                    ──▶  맵
```

## 🔴 이 그림이 «말하고 있는 것» 셋 — 이걸 놓치면 그려도 틀립니다

### ⓐ 맵과 트렌드는 «같은 collect» 입니다. 시작점만 다릅니다
```
① 기본 트렌드   Start each groupby     · Collect trend Y value
⑤ 맵           Start specific groupby · Collect trend Y value   <- «똑같습니다»
```
**맵은 다른 데이터가 아니라 «한 그룹으로 좁힌 같은 데이터»입니다.**
그래서 부품마다 데이터를 다르게 가져오면 안 됩니다 — 🔴 **walk 함수는 «한 벌»입니다.**
소유자 상설 ①(근원 템플릿 + 데이터 갈아끼우기)이 여기 그대로 적용됩니다:
```
템플릿   walk 호출 «하나»            walk({ start, collect })
선언     부품마다 { start, collect } «값»만 다름
시험     같은 화면에 트렌드와 맵을 «서로 다른 선언»으로 앉혀 간섭 없음
```

### ⓑ 마킹이 «둘» 이고 각자 다음 walk의 «시작점»입니다
```
마킹 1   기본 트렌드에서 찍음  ->  ③④⑤ 의 start
마킹 2   후보에서 찍음        ->  ⑦   의 start
```
이미 만드신 marking_store 의 「이름 붙은 마킹 여럿」이 «이걸 위한» 구조입니다. 그대로 씁니다.

### ⓒ 「일단 wafer 로 고정」 — 🔴 groupby 를 «설정 축»으로 만들지 마십시오
소유자가 「일단」이라 쓰셨습니다. **지금은 wafer 하나입니다.**
나중에 여럿일 «것 같아서» 축을 만드는 건 이 지시가 아닙니다 (관문 ②③).
값 하나를 그냥 `'wafer'` 로 두고, 바꿀 일이 생기면 그때 «둘»이 됩니다.

## 할 것 — 순서대로
```
1  walk 함수 «한 벌»을 세우십시오. 입력은 { start, collect } 둘뿐입니다
2  기존 부품들의 데이터 취득을 그 함수 «호출 하나»로 바꾸십시오
   -> 지금 api.js 는 fetchLotMap · fetchComposition · fetchSubgraph · fetchTrends · fetchSiblings
      «다섯 갈래»입니다. 도식에는 그런 갈래가 «없습니다»
3  부품마다 { start, collect } 선언을 «레이아웃 항목 옆»에 두십시오 (지금 reads/writes 옆)
4  ⑦ 이 «둘»(후보 트렌드 · 후보 맵)을 먹입니다 — 같은 walk 결과를 «두 부품»이 씁니다.
   두 번 부르지 마십시오
```

## 🔴 지우지 마십시오 — 앞선 판정 그대로입니다
```
lot_map 경로는 «살아 있어야» 합니다. collect 가 그림을 «낼 때» 옛 경로를 뺍니다
지금 원장에 좌표를 «주어로» 가진 원자는 die transfer «1,405개»뿐입니다
보이드·본딩은 총괄이 «v5 선언»으로 여는 중입니다 — 열리면 알리겠습니다
```
즉 **당신은 배선을 먼저 깔고, 재료는 제가 넣습니다.** 재료가 없다고 배선을 미루지 마십시오 —
walk 이 «빈 결과»를 주면 부품은 「없음」을 정직하게 그리면 됩니다. 그게 이미 있는 계약입니다.

## 완료의 정의
```
· api.js 에서 부품이 부르는 함수가 «하나»입니다
· 트렌드와 맵이 «같은 collect 선언»으로 서고, 시작점만 다릅니다
· 마킹 1 을 바꾸면 ③④⑤ 가 «같이» 바뀝니다
· 마킹 2 를 바꾸면 후보 트렌드·후보 맵이 «같이» 바뀝니다
```

---

# 🔴🔴🔴 오늘 아침 제 「슬롯 페이저」 지시를 «대체합니다» — 소유자가 설계를 확정했습니다

> 소유자 원문 둘:
> 「그냥 walk collect 쓰면 되지 굳이 lot_map 을 왜 씀?」
> 「이미 «내가 가지고 있는» 마킹 연동된 데이터 행들로 맵을 그리는 거잖아
>  «그 안에서 웨이퍼 단위로» 그리는 거고」

## 설계 — 맵은 «질의»가 아니라 «마킹의 그림»입니다
```
지금 (틀림)   맵이 lot_map 에 «랏을 물어봅니다»  ->  25슬롯이 한 장에 겹침
              ->  그래서 「프레임 여러 개」 거절이 났고, 제가 «페이저»로 때우라고 했습니다
확정 (맞음)   행 집합은 «마킹»이 정합니다 (walk collect 가 걸어서 모아 온 노드들)
              그 집합 «안에서» 웨이퍼 단위로 한 장씩 그립니다
```
🔴 **랏을 묻지 않으면 프레임이 겹칠 일이 «없습니다».** 겹침은 데이터의 성질이 아니라
**제가 랏으로 물어봐서 생긴 것**이었습니다. 페이저는 그 증상에 붙이는 반창고였습니다.

🔴 **그래서 「페이지」의 뜻이 바뀝니다**
```
아침 지시   available_slots 를 넘긴다        (lot_map 의 슬롯 목록)
확정        «마킹된 집합의 웨이퍼»를 넘긴다   (1 웨이퍼 = 1 장)
```
페이저 «UI 자체는 살립니다» — 넘기는 대상만 슬롯에서 «웨이퍼»로 바뀝니다.
이미 만드셨으면 버리지 마시고 소스만 바꾸십시오.

## 제 실측 — 원장은 «이미 좌표를 들고 있습니다». 다만 한 갈래만
```
die 노드     subject_keys = {x, y, mat_id, mat_type}     <- 웨이퍼 단위 좌표 그 자체입니다
             predicate `transfer` 로 코어die -> DT die 가 이어져 있습니다
             🔴 «1,405개». dt_transfer_log 행 수와 «정확히 같습니다»
Wafer 관측   observed 114,492 · transferred 72,964       <- 전부 «웨이퍼 단위». x/y 가 «없습니다»
```
```
그래서 오늘  walk collect 로 «DT/코어 전사 맵»은 그릴 수 있습니다 (die 1,405개)
            보이드·본딩 맵은 «못 그립니다» — 그 관측이 die 원자로 번역돼 있지 않습니다
```
**이건 클라가 풀 문제가 아닙니다** — 구현자 레인의 자재 작업이 die 원자까지 내려가야 합니다.
제가 그쪽에 걸었습니다.

## 그래서 당신이 «지금» 할 것
```
① 맵 부품의 입력을 «마킹된 노드 집합»으로 바꿉니다
   subgraph 응답의 노드 중 좌표를 «가진» 것(die)을 mat_id 로 «묶고», 묶음 하나 = 한 장
② 페이저가 넘기는 것은 «그 묶음»입니다.  ‹ SYN-XFER-CORE-W04  3/12 ›
③ 좌표 없는 노드는 «맵 밖»으로 셉니다 — 이미 있는 「맵 밖 · 검사 N · 발견 N」 자리가 그것입니다
④ lot_map 경로는 «지우지 마십시오». 지금 화면이 그걸로 돌고 있고, 보이드/본딩 원자가
   생기기 전엔 대체가 «불가능»합니다. 두 경로가 «공존»하는 라운드입니다
```
🔴 **④를 어기지 마십시오.** 「walk 로 가자」가 곧 「lot_map 을 뜯자」가 아닙니다.
   그리면 화면 절반이 꺼집니다. 새 경로가 «그림을 낼 때» 옛 경로를 뺍니다.

## 완료의 정의
```
· DT/코어 전사 맵이 «마킹에서» 그려집니다 (랏을 묻지 않고)
· 페이저가 «웨이퍼»를 넘기고 넘길 때마다 그림이 바뀝니다
· 같은 화면에서 lot_map 으로 그리던 맵이 «그대로 살아 있습니다»
```

---

# 🔴🔴 소유자 판정 — 「프레임이 여러 개면 «거절»이 아니라 «페이지»입니다」 (총괄, 오전)

> 소유자 원문: 「이 행이 프레임 여러 개에 걸쳐 있다 … slot을 지정할 것. -> 맵에 이딴 말 뜨는데
> **그냥 한장한장 페이지네이션 걸어서 그리면 되는데 대체 뭐가 문제임?**」

**맞는 말씀입니다. 이건 거절할 자리가 아닙니다.** 이 판정을 그대로 실행하십시오.

## 제가 방금 잰 것 — 서버는 «이미 페이지를 넘길 재료를 실어 보내고 있습니다»
`GET /api/ledger/lot_map?row=SYN-VOID-001&kind=void` (slot 없이)
```
본딩축   state=no_frame  reason=frame_ambiguous_across_slots   cells «156개가 이미 옵니다»
         frames_considered 25 · frames_matched 25 · grid «None»
         frame.available_slots = ['01','02',…,'25']   ← 🔴 «페이저의 재료가 여기 있습니다»
         frame.superposed = true
DT축     state=ready   grid 15x10   (25/25 일치)      → 그립니다
코어축   state=ready   grid 23x23   (29/29 일치)      → 그립니다
```
그리고 `&slot=07` 을 붙이면 **본딩축이 `state=ready`, grid 15x15 로 «그려집니다».**
즉 **서버 변경 없이 지금 됩니다.** 클라가 슬롯을 안 고르고 있을 뿐입니다.

🔴 **거절문 자체는 «거짓이 아닙니다»** — 본딩은 25장이 다 매칭됐는데 격자가 서로 달라
`grid=None` 입니다. 겹쳐 그리면 정말로 어긋납니다. **틀린 것은 「그래서 안 그린다」입니다.**

## 할 것 — 기반 맵 부품 «한 곳». 축 이름을 검사하지 마십시오
```
받았을 때   projection.state == no_frame 이고
            frame.available_slots 가 «비어 있지 않다»
그러면      거절문을 «찍지 말고» 슬롯 페이저를 세웁니다
            ‹  slot 07 / 25  ›     (씨앗 페이저와 «같은 모양»으로)
넘기면      같은 질문에 slot 을 붙여 다시 물어 그 «한 장»을 그립니다
남길 것     「25장 중 1장」이라는 사실은 «지워지면 안 됩니다» — 페이저 자체가 그 표시입니다
```
🔴 **부류로 고치십시오** — 축(bond/dt/core)을 이름으로 가르지 마십시오. 조건은
`state == no_frame && available_slots.length > 0` 하나이고, 그러면 세 축 어디서 나든 똑같이 페이지가 섭니다.

🔴 **여전히 거절할 자리는 남습니다** — `available_slots` 가 «비었을» 때입니다
(`frame_unkeyed` · `frames_unregistered`). 넘길 페이지가 없으면 페이저가 아니라 문장입니다.
이 둘은 «그대로 두십시오».

📎 지금 화면의 「기반 bond_layer 10 / dt_slot 18 / wafer_grid 10」 알약 옆에 페이저가 서면
   같은 줄에서 「어느 장을 보고 있는가」가 읽힙니다.

## 완료의 정의
```
· 본딩 맵이 «그려집니다» (지금은 문장만 있습니다)
· ‹ › 로 다음 슬롯이 넘어가고 그릴 때마다 칸 수/발견 수가 «바뀝니다»
· 슬롯이 없는 축에서는 «문장이 그대로» 나옵니다 (거절을 지운 게 아님을 보이십시오)
```

---


# 🔴 표가 «부품이 아닙니다» — 하나 만들어서 데이터만 갈아끼우십시오 (소유자 지적, 08:3x)

> 소유자: 「표 양식도 좀 «하나 템플릿»해서 데이터만 갈아끼우면 되지 왜 표마다 양식이 다르니
> (구성 표, 순위 표)」

## 제가 둘을 나란히 보고 잰 차이
```
구성(층) 표   머리 «옅은 회색» · «한 줄» 행 · 구분선 «없음» · 상태가 «영문 대문자»(RESOLVED…)
순위 표       머리 «회색 띠»   · «두 줄» 행(물리량/모델) · 행마다 «구분선» · 상태가 «한글»(최상위·동률)
              순위 숫자만 굵게 · 행 높이가 «더 큼»
```
**같은 화면에서 머리·행높이·줄수·구분선·정렬·상태 표기가 전부 다릅니다.**

## 🔴 이건 «소유자 상설 규칙»의 위반입니다
```
「현재 만드는 모든 요소는 «클래스»로 만들어서 어느 화면에도 끼워넣을 수 있게」
부품은 일곱 개 클래스로 만들었는데, 그 «안»에서 표를 두 번 손으로 그렸습니다
```

## 할 것 — 표 «부품 하나». 나머지는 «선언»입니다
```
Table 부품    다른 부품들과 같은 계약(자기 mount·deps·모듈 상태 0)
선언          컬럼마다 { key, label, align, width, kind }
              kind 예: text · mono · number · two_line(주/부) · badge(상태) · rank
쓰는 쪽       구성 = 그 선언 하나 · 순위 = 다른 선언 하나. «표 코드는 한 벌»
```
🔴 **상태 표기도 한 곳으로** — RESOLVED / 최상위 / 동률 이 지금 서로 다른 규칙입니다.
   badge kind 가 «서버가 준 값»을 그대로 받아 같은 모양으로 찍게 하십시오.
🔴 **시험**: 같은 화면에 표 «둘»을 서로 다른 선언으로 앉혀서 «간섭 없음» — 부품 규칙 그대로입니다.
📎 앞으로 표가 더 생깁니다(후보 트렌드 옆 표 등). 지금 한 벌로 만들면 그때는 «선언 한 줄»입니다.

# 🔴 총괄이 «직접 눌러» 확인 + 소유자 추가 둘 + 제가 찾은 하나 (08:2x)

## 소유자 지적 다섯 — 제가 화면에서 확인했습니다
```
③ Y축 반응 없음   «확인». 「보이드 비율」을 눌렀더니 알약은 파랗게 찼는데
                  트렌드 점의 «위치가 그대로»입니다. 고르는 것만 되고 그리는 게 안 바뀝니다
⑤ 우측 컨트롤     «확인». 트렌드가 «전폭»이고 오른쪽이 비어 있습니다
① 후보 트렌드     «확인». 패널 목록에 아예 없습니다
② 구성 부실       «확인». 「펼친 층」 패널이 «통째로» 없고, 층 표에 「← 후보 N」 배지도 없습니다
④ 마킹->맵 타겟팅  정적으로는 못 잽니다. 소유자 관측을 그대로 받습니다
```
## 🔴 소유자 추가 둘 — 이것이 제일 눈에 띄는 차이입니다
```
맵 배치   목업은 «가로»(본딩 맵 | 코어 맵 나란히).  우리는 «세로»로 쌓여 있습니다
후보 맵   목업의 둘째 맵은 «코어 맵 (마킹 2 · 후보가 걸린 점)» 입니다
          우리 둘째는 «또 다른 본딩 맵(슬롯 03)» 입니다 — «후보 맵이 아닙니다»
할 것     그리드를 가로로. 그리고 둘째 맵을 «마킹 2 를 읽는 코어 맵»으로 바꾸십시오
          🔴 기반 선택자가 이미 있으니 «wafer_grid» 로 서는 인스턴스면 됩니다.
             부품을 새로 만들지 마십시오 — 레이아웃 항목의 기반과 읽는 마킹만 다릅니다
```
## 📎 제가 찾은 것 하나
```
Y value 알약에 «bond_pressure 가 두 개» 있는데 글자가 같아 «구별이 안 됩니다»
(모델이 둘이라 둘인 것이 맞습니다 — 「합치지 마라」는 규칙대로입니다.
 다만 화면에서 «어느 모델인지»가 보여야 합니다)
```
🔴 우선순위: **맵 배치·후보 맵 → ③ Y축 → ④ 타겟팅 → ① 후보 트렌드 → ⑤ 컨트롤 → ② 구성**
   앞의 둘은 «한눈에 보이는» 차이라 그것부터입니다.

# 🔴🔴 소유자 지적 «다섯» — 아침에 직접 보시고 주신 것입니다 (총괄 08:0x)

> 「1) 후보에 대한 트렌드 없음. 2) 구성 영역 목업 대비 부실. 3) 메인 트렌드 y축 «뿌릴것»에 따른
> 반응 없음. 4) 메인 트렌드 마킹에 따른 페이지 웨이퍼 타겟팅 미변경. 5) 트렌드 차트 우측 컨트롤 패널 없음」

**순서를 이렇게 잡으십시오 — 3·4 가 먼저입니다. 「컨트롤이 컨트롤을 안 하는 것」이 제일 나쁩니다.**

## ③ Y축 「뿌릴 것」이 트렌드를 «안 바꿉니다»  ← 최우선
```
목업   ⊙ Y value 뿌릴 것  [void 비율][delam 비율][bond_temp][bond_pressure][post_bond_queue_h]
지금   알약은 있는데 «누르면 트렌드가 안 바뀝니다» -> 죽은 컨트롤입니다
할 것  고른 축이 메인 트렌드의 y 가 되게. 고른 것은 파랗게 «찹니다»
       🔴 값이 없는 축(bond_temp 등)을 고르면 «빈 채로 두고 왜인지 말하십시오». 0 금지
```
## ④ 트렌드에서 마킹해도 «맵 페이지가 안 따라옵니다»  ← 두 번째
```
목업   메인 트렌드의 점 하나 = 웨이퍼 하나. 찍으면 «그것이 씨앗»
지금   찍어도 맵이 그 웨이퍼로 «안 넘어갑니다»
할 것  트렌드가 쓰는 마킹을 맵이 읽고, 그 웨이퍼로 «페이지를 옮깁니다»
       🔴 페이지 이동은 «마킹을 지우지 않습니다» (이미 규칙으로 있습니다)
```
## ① 「마킹한 후보 트렌드」 패널이 «통째로 없습니다»
```
목업   패널 여덟 중 하나가 «마킹 2» 로 도는 후보 트렌드입니다 (메인 트렌드와 별개)
할 것  트렌드 부품을 «두 번째 인스턴스»로 앉히십시오 — 읽는 마킹 이름만 다릅니다
       (조립식 규칙대로면 «레이아웃 항목 하나»여야 합니다. 부품을 새로 만들지 마십시오)
```
## ⑤ 트렌드 «우측 컨트롤 패널»이 없습니다
```
스팟파이어  차트마다 오른쪽에 Data limiting · Marker by · Color by · Shape by · Size by
지금        그 블록이 없습니다 — 차트가 «무엇에 반응하는지»를 화면이 말하지 않습니다
할 것       트렌드 오른쪽에 그 선언 블록. 값은 «지어내지 말고» 그 부품이 실제로 쓰는 것을 씁니다
```
## ② 구성 영역이 목업 대비 «부실»합니다
```
목업   최종 대상 + 「어떻게 정해졌나」(state·basis·candidates) + «큰 숫자 타일 둘»
       + 층 표 + «펼친 층» 패널(스텝 알약 · 「후보 N 이 이 칩을 가리킨다」 상자 · 값 목록)
지금   표와 상자는 있는데 «타일»과 «펼친 층» 패널이 얇거나 없습니다
할 것  목업을 열어 «나란히» 놓고 그 차이를 지우십시오
```
🔴 셋(①③④⑤)은 «작동»입니다. ②는 밀도입니다. **작동 먼저.**
📎 목업: `http://localhost:8123/웨이퍼 진단 화면.dc.html` (살아 있습니다)

# 🔴 마지막 하나 — **또래 알약 넷 중 둘이 «빈 답» 위에 수를 찍고 있습니다** (총괄 07:2x)

응용이 배선을 확인했습니다. **부르긴 부릅니다. 수도 참입니다. 그런데 «자리»가 틀렸습니다.**
```
알약        화면   state    case주어  excluded        뜻
같은 레그     6    empty      0      mixed «6»       6이 «전부 빠졌고» 대조는 0
레시피      124    empty      0      mixed «124»     같음
같은 랏      25    ready     25        —             진짜 25로 대조됨
설비         75    ready     75        —             진짜 75로 대조됨
```
🔴 **「같은 레그 6」이 「6으로 대조할 수 있다」로 읽힙니다. 실제는 「6이 어느 쪽도 아니라 빠졌다」입니다.**
   사용자가 정확히 «반대로» 읽습니다. 이게 이 화면이 없애려는 바로 그 오독입니다.

## 서버가 이미 «한국어 사유 둘»을 줍니다 — 지어내지 마십시오
```
reason   "empty_case_side"
message  「케이스 쪽 주어 0 — 마킹한 값이 이 모집단에 없음」
버킷      excluded[].bucket = "mixed" + 그 버킷의 사유 문장
```
## 할 것 — 문구 한 줄
```
state == "ready"   지금처럼 수를 씁니다
state == "empty"   그 수를 «대조 가능한 수»처럼 쓰지 마십시오
                   -> 「대조 0 · 걸침 6」 같은 식으로 «둘을 갈라» 쓰거나, 서버 문장을 그대로 답니다
🔴 부재가 «넷»입니다  안 골랐다 / 그 종류가 없다 / 대조를 안 했다 / «걸쳐 있어 어느 쪽도 아니다»
                    넷째가 오늘 새로 나온 것입니다. 앞의 셋과 같은 문구로 뭉치지 마십시오
```
📎 이게 도착지의 마지막 조각입니다. 이것만 닫히면 「보인다 · 돈다 · 참말한다」 셋이 다 참입니다.

# ⚖️ 판정 — **주어는 «칩»입니다. 웨이퍼는 «옆에» 붙입니다** (총괄 06:3x)

## 왜 칩인가 — 목업의 그림이 아니라 «이 화면의 질문»으로 갈랐습니다
```
이 화면의 질문   「이 불량이 왜 났나」
구성 패널        10층 · 18 DT 컬렉션 — 이건 «본딩된 칩»에만 있습니다. 웨이퍼엔 구성이 없습니다
후보·순위        그 칩의 층들을 걸어서 나옵니다
```
**주어를 웨이퍼로 바꾸면 인과 패널 절반이 «설 자리»를 잃습니다.** 목업 ①의 신원 줄은
소유자가 「저런 느낌으로」 그리신 스케치이고, 도착지의 «주어»를 정한 문장이 아닙니다.
📎 그리고 레그 칸은 «비우는 게» 맞습니다 — 소유자가 오늘 밤 WaferLeg 를 «뺐습니다».
   빈 칸을 만들려고 주어를 바꾸는 것은 방향이 거꾸로입니다.

## 그래도 목업이 «주는 정보»는 가져옵니다 — 주어를 안 바꾸고
```
칩 (주어)      SYN-CX-CHIP-001 · 구성 10층 · 18 DT   <- 지금 그대로
그 아래 한 줄   이 칩이 앉은 «웨이퍼»와 그 수
               웨이퍼 SYN-BW-001-07 · 랏 SYN-VOID-001 · 141칸 · void 13 · 검사 29
```
✅ **더하는 것이지 옮기는 것이 아닙니다.** 화면 절반이 따라 움직이지 않습니다.
✅ delam 은 `kind=delam` 으로 «한 번 더» 불러 나란히 놓으십시오 — 목업이 그렇게 씁니다
   (⚠️ 두 번째 요청이라 값이 늦게 오면 «빈 칸»으로 두고 나중에 채우십시오. 0 금지)

## 그리고 「둘을 한 모양으로 맞출지」(보고 242행)도 같이 답합니다
**맞추지 마십시오.** 지금 둘 다 받게 고쳐 두셨고 주석에 적어 두셨습니다 — 그걸로 충분합니다.
모양을 통일하는 것은 «서버가 한 모양으로 낼 때» 할 일이고, 그때는 클라가 할 일이 없습니다.

# ✅ A 가 섰습니다 — **두 번째 요청을 «걷으십시오»** (총괄 05:5x)

제가 재기동하고 라이브에서 확인했습니다. 화면이 부르는 그대로(슬롯 지정 · by 없음):
```
unplaced  { state: "measured", scanned: 1, found: 1, reason: "no_row_in_process_relation" }
bond      found 13 · scanned 29
합         14 · 30   ✅ 소스와 일치
```
**서버가 이제 «측정»으로 답합니다.** 거절 사유도 참인 문장으로 바뀌었습니다 —
「랏·설비·슬롯이 없다」(거짓) -> 「공정 관계에 행이 없다」(참).

```
할 것   웨이퍼 축으로 «한 번 더» 묻던 그 호출을 지우십시오 (B). 이제 첫 요청이 답합니다
🔴 확인  지우고 나서 화면의 수가 «그대로»여야 합니다 — 14 · 30. 달라지면 지우지 말고 보고
📎 남기기 진짜 귀속 불가(state unknown) 경로는 «남아 있습니다». 지금 0건이지만 그때 필요합니다
```

# ⚖️ A 와 B 가 «둘 다» 났습니다 — 충돌 아닙니다. 순서를 정합니다 (총괄 05:3x)

```
클라   B 를 이미 했습니다 (44d0b9a0) — 웨이퍼 축으로 «한 번 더» 물어서 옆에 붙임
총괄   A 를 판정했습니다 — 슬롯이 지정되면 서버가 «측정으로» 답한다
```
**둘은 배타적이지 않습니다.** B 는 «지금» 화면을 맞게 만들고, A 는 «서버가 거짓을 말하는 것»을 없앱니다.
```
지금    B 유지 — 화면이 이미 맞는 수를 말합니다. 되돌리지 마십시오
A 착지 후  클라가 «두 번째 요청을 지웁니다». 그때 한 줄 삭제입니다
🔴 그때까지 서버는 여전히 「귀속 불가」라고 «틀린 말»을 합니다 — A 를 취소하지 않습니다
```
📌 **누구도 남의 것을 되돌리지 마십시오.** 클라는 B 를 지키고, 구현자는 A 를 하고,
   A 가 서면 제가 클라에 「B 를 걷어라」를 냅니다.

# 🔴 새 필드 `unplaced` — **그릴 수 없는 것을 «수»로 말하십시오** (총괄 04:2x)

구현자가 응용의 발견을 끝까지 파서 «맵 밖의 수»를 냈습니다. **한 자리가 아니었습니다:**
```
검사됐는데 공정 행이 없는 자리   «2,527»  ·  그중 void 기록된 것 «2,525»
좌표        bond_x · dt_x · cx 가 «전부 없음» -> 어느 축의 칸으로도 못 그립니다
            (base 좌표는 있지만 «다른 좌표계»라 옮겨 찍지 않았습니다 — 맞는 판단입니다)
```
## 응답에 이렇게 옵니다 (최상위 · 축마다가 아니라 «행에 대한 사실»)
```
unplaced { state, scanned, found, reason, message }
  state "measured"  -> scanned·found 가 «수»입니다.  예: 맵 29 + unplaced 1 = 소스 30
  state "unknown"   -> 그 행 축으로는 귀속 «불가». 🔴 0 이 아니라 «모른다»입니다
                       message 에 이유가 한국어로 옵니다 (그대로 쓰십시오)
```
## 할 것
```
맵 배지 옆(또는 아래)에 «맵 밖» 수를 답니다 — 「맵 밖 검사 1 · 발견 1」 같은 식
🔴 state "unknown" 이면 «0 으로 그리지 마십시오». 「귀속 불가」로 말하고 message 를 보여 주십시오
🔴 그리고 이건 «맵이 못 그린 것»이지 «없는 것»이 아닙니다 — 그 차이가 화면에 보여야 합니다
```
📎 이게 오늘 밤 이 화면이 존재하는 이유에 가장 가까운 자리입니다.
   2,525건이 어느 그림에도 없이 원장에만 있었고, 이제 화면이 «있다고는» 말합니다.

# 🔴🔴 화면 대조 결과 — **맵이 «진짜 결함 하나»를 못 그리고 있습니다** (응용 실측, 총괄 03:5x)

응용이 화면의 수를 라우트에, 다시 원장·소스표에 대조했습니다. **대부분 맞고, 맵이 틀립니다.**
```
✅ 맞음   머리 요약(10 · 18 · 4) · 층 표(resolved 5 · candidate 2 · contested 2 · unresolvable 1)
          후보 순위(25개 · 2위 다섯 동률) · 걷기가 «잘리지 않았다»고 스스로 보고하는 것
🔴 틀림   맵    화면 「검사 29 · 발견 13」   소스 「30 · 14」
          빠진 한 자리에 «void 가 기록돼 있습니다» -> 진짜 결함이 그림에 «영영 안 나타납니다»
```
```
원인    맵이 bonding_log 에서 셉니다. «검사는 됐는데 본딩 행이 없는» 자리가 조용히 탈락합니다
검증    창을 30·180·365일로 바꿔도 전부 30·14  (창 문제 아님)
        그 랏의 «25 슬롯 전부» 각각 최소 한 자리씩 잃습니다  (이 화면만의 일이 아님)
이름    라벨이 「검사 29」인데 실제 뜻은 「검사«되고 본딩된» 29」. 화면 어디에도 그 말이 없습니다
```

## 🔴 구현자 — 이 조인이 «부재를 만들어» 냅니다
```
할 것   검사된 자리가 «본딩 행이 없다는 이유로» 빠지지 않게 하십시오.
        어느 조인을 어떻게 바꿀지는 «당신이 읽고 정하십시오» — 제가 컬럼을 대면 틀립니다(3/3)
🔴 다만  「본딩 안 된 검사 자리」를 «어떻게 그릴지»는 설계 문제입니다.
        본딩 맵의 격자에 그 자리가 «없을» 수도 있습니다. 그러면 수는 맞추되
        그 자리를 «맵 밖의 수»로 따로 말해야 합니다 — 조용히 더하지 마십시오
멈춤    격자에 자리가 없으면 «멈추고 보고». 없는 칸에 억지로 그리지 마십시오
```
## 🔴 클라 — 라벨이 «자기가 세는 것»을 말해야 합니다
```
「검사 29」  -> 그 수가 «무엇의» 29 인지. 서버가 뜻을 바꾸면 라벨도 같이 바뀌어야 합니다
📎 그리고 응용이 「또래 패널이 «한 번도 안 불린다»」고 했습니다 — 확인해서 부르거나,
   안 부르는 것이 맞으면 «왜»를 적어 주십시오
```

# ✅ grain «복사해 붙일 것» 도착 — 그리고 제 진단이 «틀렸습니다» (총괄 03:3x)

## 붙이십시오 — `grain` 은 쿼리 파라미터, 값은 «URL 인코딩된 JSON»
```
GET /api/ledger/trends?window=180d&grain=<JSON을 URL 인코딩>
```
```json
{"subject_type":"WaferLeg","identity_fields":["wafer"],"aggregation_unit":"void_by_experiment_unit","context_fields":["bonding_leg"],"context_role":"planned_bonding_experiment_unit","marking":"identity.mark_key","axes":[{"name":"wafer","denominator":{"relation":"inspection_run","column":"base_wafer_id"},"numerator":{"from":"subject_keys","key":"wafer"}},{"name":"bonding_leg","denominator":{"relation":"bonding_map","column":"leg"},"numerator":{"from":"subject_keys","key":"bonding_leg"}}]}
```
기본값과 다른 곳은 «둘»뿐입니다: `subject_type` Wafer→WaferLeg · `axes[1].numerator.from` object_payload→subject_keys

## 🔴 제 진단이 틀렸습니다 — 「값 없는 점」이 아니라 «값이 0» 이었습니다
```
제가 본 것   metric.value / metric.state 를 읽어서 「값 있는 점 0개」
실제 필드    event_count · found_chip_count · scan_denominator · found_rate · state
             -> 24개 «전부» 값이 붙어 있습니다. 제가 «없는 필드»를 읽었습니다
```
**그러니 화면이 «비지 않습니다».** 지금도 그려집니다 — 다만 **전부 0 으로** 그려집니다.
```
기본 grain   24점 · 전부 state "scanned_clean" · found_rate 0.0     -> 「봤는데 없음」
정정 grain   24점 · 12 found + 12 scanned_clean · sum(found) «12»   -> 진짜 값
```
🔴 **그래서 고칠 이유는 그대로입니다 — 다만 「빈 화면」이 아니라 «12건을 0으로 보여주는 것»입니다.**
   그게 더 나쁩니다. 빈 화면은 의심이라도 사는데, 0 은 «없다»로 읽힙니다.

⛔ 제가 「빈 화면이니 그 이유를 말해 두라」고 한 지시는 «취소»합니다. 빈 게 아닙니다.

# 🔴🔴 «지금 제일 급한 것» — 메인 트렌드가 «값 없는 점»만 받고 있습니다 (총괄, 실측)

제가 화면이 부르는 그대로 불러 봤습니다:
```
GET /api/ledger/trends?window=180d          ← 지금 클라가 «이렇게만» 부릅니다 (grain 없음)
결과   series 둘 · 점 24개 · «값 있는 점 0개»   (metric.state 도 전부 null)
```
🔴 **아침에 소유자가 여실 화면에서 메인 트렌드가 «빈 채로» 서 있게 됩니다.**
그리고 클라 하니스가 「값 없는 점을 0 으로 찍지 마라」를 «맞게» 막고 있어서
0 으로도 안 그려지고 «아무것도 안 그려집니다».

## 원인은 이미 알던 것입니다 — 제가 못 박아 두고 «배선을 확인 안 했습니다»
```
제 지시(23:3x)   「트렌드 부품은 grain 을 «반드시» 넘긴다. 기본값에 기대지 말 것」
실제             client2/src/rnd_board/api.js  fetchTrends 가 kinds · window «둘만» 보냅니다
기본 grain       numerator 가 object_payload 를 가리키는데 이 원장엔 «0행» -> found 0
정정 grain       구현자가 found «24» 를 냈습니다 (라운드 2 수락 근거)
```

## 🔴 구현자 — **그 «정정 grain» 의 실제 payload 를 그대로 주십시오**
```
낼 것   found 24 를 내는 «그 요청»의 전체 형태 — 쿼리스트링이든 JSON이든 «복사해 붙일 수 있게»
        (제가 필드 이름을 옮기다 오늘 세 번 틀렸습니다. 당신이 «그대로» 적어 주십시오)
확인    그 요청으로 series 의 점에 «값이 실제로 붙는지»까지 보고 주십시오 — 24 라는 수만이 아니라
```
## 🔴 클라 — 그게 오면 `fetchTrends` 에 실으십시오. 그 전까지는
```
트렌드가 비어 있는 것은 «알려진 상태»입니다. 다른 것 하시고, 오면 «한 줄» 바꾸면 됩니다
그리고 빈 이유를 화면에 «말해» 두십시오 — 「측정값 없음」이 아니라
「이 조회 조건에서 값이 없음」쪽입니다. 셋 중 어느 부재인지 구분하는 그 규칙 그대로입니다

# ✅ 막힌 둘 «풀렸습니다» — 필드 이름과 실측값을 그대로 드립니다 (총괄 03:1x)

구현자가 라이브에서 뽑아 왔습니다. **넷 다 나옵니다. 안 되는 것 없습니다.**

## ① 또래 개수 — 읽을 필드가 «다른 곳»이었습니다
```
GET /api/ledger/siblings?scope=<축>:<값>&window=<창>
읽을 것   scope.value_accounting[0].subjects   (주어 수)
          scope.value_accounting[0].units      (유닛 = 패키지 수)
          같은 항목의 state == "resolved" 면 값이 있는 것
❌ 아님   case.subjects  — 주어 «안»을 가르는 축(레그·레시피·설비)에서 0 이나 딴 수가 나옵니다
```
실측 (window=180d):
```
같은 레그   leg:HBM-B_LOW-P          units  384 · subjects   6
같은 랏     bond_lot:SYN-K1-201             725 ·           25
            core_lot:SYN-CL-006             813 ·           50
레시피      scan_recipe:SYN_VOID_R1        4028 ·          124
설비        bond_eqp:SYN-BD-02             2173 ·           75
            scan_eqp:SYN-SAT-01            3697 ·          117
```
📎 「랏」·「설비」는 축이 여럿(랏 셋 · 설비 둘)입니다 — **어느 것을 알약에 쓸지는 화면이 정합니다.**
   서버는 다 답합니다. 개수 단위(주어 vs 패키지)도 «같은 응답에 나란히» 오니 골라 쓰십시오.

## ② 트렌드 «퍼짐» — 집계 값은 «없습니다». 그런데 목업도 그걸 안 씁니다
```
없음   percentile · quantile · stddev · median · mean · band · iqr · min/max   «전부 0개»
있음   흩뿌릴 «점» 자체는 옵니다
```
🔴 **목업을 다시 보십시오** — 그 트렌드는 «띠(band)» 가 아니라 **점들의 흩어짐 + 씨앗 값에 점선**입니다.
   그러니 집계 퍼짐이 없어도 목업은 그려집니다. **점을 그리고, 씨앗 값에 가로 점선을 놓으십시오.**
   범례 한 줄도 목업 그대로: 「점 하나 = 웨이퍼 하나 · y = … (분자 … · 분모 …)」
⛔ 없는 통계를 «계산해서 그리지» 마십시오. 서버가 안 주는 것은 화면이 지어내지 않습니다.

## 이제 클라 쪽 막힌 것은 «페이지네이션 하나»입니다
그것도 재료는 `/composition` 응답의 노드 keys 에 있습니다(앞 블록). 진행하십시오.

# ✅ 상호작용 점검 «수락» + 🔴 페이지네이션은 «막힌 게 아닙니다» (총괄 02:3x)

## 점검이 이 라운드의 값입니다
```
화면을 «직접 몰아» 보고 — 스크롤·행·카드·알약·점·캔버스·수식어 조합 전부
스팟파이어 계약이 맵에서도 성립: 클릭 replace · Ctrl 누적 · Ctrl+Shift 컨트롤 · 둘째 맵 0 유지
찾은 결함 하나: 층을 마킹하니 «아무것도 안 켜진» 웨이퍼 맵이 통째로 흐려짐
   -> 감쇠가 «공유 마킹의 크기»를 봤지 «이 패널이 그리는 것»을 안 봤다
   -> 배지에서 났던 그 결함이 «칠하는 경로»에 한 겹 더 있었던 것. 정확한 진단입니다
```

## 🔴 「페이지 넘길 질의가 없다」 — **있습니다. 이미 받고 있는 응답 안에**
제가 `/composition` 을 열어 봤습니다. **각 노드가 «자재의 키»를 들고 있습니다:**
```
wafer_grid  10개   keys.wafer     = SYN-CX-CW-HBM-B-03 · SYN-CX-CW-SENSOR-B-02 · …
dt_slot     18개   keys.dt_lot    = SYN-CX-DT-02   keys.dt_slot = 02 · 03 · …
bond_layer  10개   keys.base_wafer_id · bond_wafer · bonding_leg · final_chip_id · layer
```
**알약의 개수를 세던 그 목록이 곧 «페이지 목록»입니다.** 새 질의가 필요 없습니다 —
개수만 세고 «키를 버리고» 있었던 것입니다.
```
dt   페이지   keys.dt_lot + keys.dt_slot  -> /lot_map?row=&slot= 에 «그대로» 들어갑니다
core 페이지   keys.wafer 는 있는데 lot·slot 이 그 노드에 있는지 «제가 확인 못 했습니다»
              -> 넣어 보고, 안 되면 «그것만» 막힌 것으로 보고하십시오
```
⚠️ 제가 이 도메인에서 컬럼을 추론하다 세 번 틀렸습니다. 그래서 **「이 키로 된다」가 아니라
   「이 키가 응답에 있다」까지만** 말합니다. 되는지는 «불러 보고» 판단하십시오.

📎 나머지 둘(트렌드 spread · 또래 개수 넷)은 서버 쪽입니다. 구현자에게 넘기겠습니다 —
   당신은 그 자리를 «em dash 로 비워 두고» 다른 것을 계속하십시오.

# ⚖️ 빌드 빨강 판정 — **계약은 «안» 바뀌었습니다. 앵커만 낡았습니다** (총괄 02:1x)

빌드가 이렇게 서 있습니다 (제가 백그라운드로 돌려서 잡았습니다):
```
INERT  M3 「제공 안 된 또래 개수를 0 으로 그린다」 -- mutation anchor is GONE: control_bar_panel.js
19 passed 0 failed · 7/8 defects caught · 1 escaped   -> 빌드 종료코드 1 · dist «안 나감»
```
🔴 **당신이 만든 그 가드가 «제대로 물었습니다»** — 변이가 0번 적용되고 초록으로 지나가던 것을
막으라고 만든 것이고, 지금 그 상황이 실제로 났습니다. 기계가 옳습니다.

## 판정
```
계약    «그대로»입니다 — 제공 안 된 개수는 «em dash», 0 이 아닙니다. 이 단언은 유지합니다
원인    당신이 기반 선택자 작업으로 control_bar_panel.js 를 고치면서 «앵커 문자열»이 사라졌습니다
할 것   앵커를 «지금 코드»에 다시 맞추십시오. 그리고 변이가 실제로 잡히는지 확인
⛔ 금지  KNOWN_RED 에 넣지 마십시오. 빌드 스크립트가 그 이유까지 적어 뒀습니다
```
🔴 **그리고 앵커를 «이름»에 매지 마십시오** — 이 저장소는 「대상의 이름을 고정 참조한 하니스가
   개명과 함께 죽는」 사고를 이미 겪었습니다. 리팩터링 한 번에 또 죽을 앵커면
   «술어»(그 동작을 하는 자리)로 잡으십시오.

## ⚠️ 그동안 dist 가 «안 나갑니다»
빌드가 종료코드 1 이라 제가 dist 를 못 굽습니다. **8080 은 그 전 상태입니다.**
소유자가 아침에 보실 화면이라 이게 제일 급합니다 — 다른 부품보다 «먼저» 고치십시오.

# 🎨 CSS 계측 결과 인계 — **응용 세션 → 디자인 세션** (소유자 지시로 전달, 01:5x)

> 소유자: 「할거 없으면 css라도 빡세게 만져」 → 잰 뒤 「클라한테 넘겨」
> ⚠️ **총괄 지시가 아닙니다.** 응용 세션이 «계측»만 해서 넘기는 것입니다.
> 파일은 **안 건드렸습니다** — 당신이 `board.css` 를 90분에 네 번 커밋 중이어서입니다.
> 정본: `task/APPLICATION_CSS_TOKEN_AUDIT.md` (값까지 붙여 넣게 뽑아 뒀습니다)

목업 `:8123` 과 라이브 `:8080/rnd-board.html` 을 **둘 다 1280px 로 열어 computed style 로** 쟀습니다.
눈으로 하는 감사가 못 잡는 것만 적습니다.

## 🔴 1순위 — 한글 폰트. **한 줄이고, 이게 제일 큽니다**
```
board.css:20   Outfit, system-ui, sans-serif
목업           Outfit, "Noto Sans KR", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif
```
`Outfit` 에 **한글이 없습니다.** 한글은 전부 다음 폰트로 떨어지는데 그게 서로 다릅니다.
```
같은 한글 문자열 20px 폭    목업 223.2px · 라이브 240.8px  ->  «7.9% 더 넓다»
```
줄바꿈·칸 폭·잘림이 전부 달라집니다. **이걸 안 고치면 나머지를 맞춰도 「같아 보인다」가 안 됩니다.**
📎 `Noto Sans KR` 은 이 박스에 «있습니다» (`document.fonts.check` = true, 목업·라이브 양쪽).

## 2~6순위
```
2  패널 모서리    목업 8px(66개) / 안쪽 카드 10px   ->  라이브 .rb-panel «0px» (직각)
3  본문 크기      목업 최빈 12.5px                  ->  라이브 최빈 11.5·11px  (상설 「작은 글씨」)
4  알약           목업 radius 20px «33개»           ->  라이브 «0개»
                  중립  20px · #FFF            · rgb(31,39,51)   · 12.5px · 2px 8px
                  파랑  20px · rgb(232,240,252) · rgb(26,102,208) · 12px  · 2px 8px
                  보라  20px · rgb(240,233,251) · rgb(109,40,217) · 11px  · 2px 7px
                  선형  20px · transparent      · rgb(71,83,107)  · 11px  · 2px 7px
5  굵기 축        목업 700:110 · 600:190 · 500:30   ->  라이브 700:«3» · 600:42 · 500:«0»
6  색면·빨강      목업 232,240,252(27)·227,243,247(10)·240,233,251(8)·194,47,47(13) -> 라이브 «전부 0»
```

## ✅ 통과 — **여기엔 밤을 쓰지 마십시오**
```
타입 하한 11px 미만 «0개» · 대비 AA 미달 «0개» · 가로 넘침 없음
인라인 스타일 11개는 «전부» grid-area/min-width — 조립식 규칙대로 배치가 부품 «밖»에 있음
```

## 제가 «못 잰» 것
```
부품별 위치·크기 대조     브라우저 창이 안 떠서 스크린샷을 못 찍었습니다. 그건 당신 몫입니다
상호작용(클릭·Ctrl·감쇠)   이번 계측은 «정지 화면»만입니다
```

📎 값이 더 필요하면 «무엇을 재 달라»고 이 파일에 적어 두십시오. 15분마다 봅니다.

---

# 🔴 맵의 «기반(basis)» 은 라벨이 아니라 **선택자**입니다 (소유자 지시, 01:3x)

> 소유자: 「목업 맵 하단에 «맵 타입» 볼 수 있게 되어 있는데, 이거 «타입 바꾸면 해당 타입으로
> 추적해서 맵 그려주는» 거 알지?」
```
목업 하단   기반  [bond_layer 10]  [dt_slot 18]  [wafer_grid 10]
뜻          누르면 «그 타입으로 추적해서» 그 맵을 그린다. 지금 우리는 «알약만» 붙였습니다
```

## 🔴 재료가 «전부 이미 있습니다» — 제가 방금 불러서 확인했습니다
```
개수   GET /api/ledger/composition?final_chip_id=…   -> graph.nodes 38
       dt_slot «18» · wafer_grid «10» · bond_layer «10»
       -> 목업의 세 숫자와 «정확히 같습니다». 지어낼 것 없습니다
맵     GET /api/ledger/lot_map…  응답 하나에 «투영 셋»이 이미 들어 있습니다
       bond(cells 141 · frame 15x15) · dt(cells 11 · grid 15x10) · core(cells 110 · grid 23x23)
```
**새 API 가 필요 없습니다.** 지금 맵 부품이 `axis:'bond'` 를 layout 에서 «고정»으로 받고 있는데,
그것을 «상태»로 만들고 알약이 그걸 바꾸면 됩니다.

## 하는 것
```
1  기반 알약 셋에 «개수»를 붙입니다 — /composition 의 graph.nodes 타입별 수
2  누르면 맵이 «그 투영»으로 다시 그려집니다 — /lot_map 응답의 dt · core 투영
3  프레임도 같이 바뀝니다 (dt 15x10 · core 23x23) — 프레임 기반 배치는 이미 하셨습니다
4  🔴 «지금 고른 것»이 보여야 합니다 (목업은 선택된 알약을 파랗게 칠합니다)
```
⚠️ 부품 «둘»이 서로 다른 기반일 수 있어야 합니다 — 맵 A 는 bond, 맵 B 는 dt.
   기반은 «그 부품의 상태»이지 화면 전역이 아닙니다. (조립식 규칙 그대로)
📎 dt·core 투영은 `frame.state=no_frame` 이지만 `grid` 는 «합의로» 나옵니다(25/25 · 27/27).
   그래서 테두리는 그릴 수 있습니다 — 「프레임 없음」으로 읽고 «안 그리지» 마십시오.


## 🔴 추가 — **여럿이면 «페이지네이션»** (소유자, 01:4x)
> 소유자: 「만약 추적되서 wf 같은게 «여러개» 뜨면 «페이지네이션» 걸어서 여러개 넘기면서 볼 수 있게해」
```
왜   wafer_grid «10» · dt_slot «18» 입니다. 맵은 «한 장»만 그립니다.
     알약의 숫자가 곧 «페이지 수»입니다 — 10 이면 열 장을 넘겨 볼 수 있어야 합니다
낼 것 맵 머리(또는 발치)에 «‹ 3/10 ›» 같은 자리. 누르면 그 자재의 맵으로 다시 그립니다
      지금 어느 자재인지 «이름»이 보여야 합니다 (목업이 맵 위에 자재 이름을 답니다)
```
⚠️ **페이지가 «그 부품의 상태»입니다.** 맵 A 가 3/10, 맵 B 가 7/18 이어도 서로 간섭 없어야 합니다.
⚠️ 마킹은 페이지를 넘겨도 «유지»됩니다 — 마킹은 노드 id 의 집합이지 화면 상태가 아닙니다.
   3페이지에서 찍고 7페이지 갔다 돌아오면 그 표시가 «그대로» 있어야 합니다.
# 🔴🔴🔴 소유자 도착지 — **내일 아침까지 «목업과 똑같이 + 작동»** (2026-08-24 01:1x)

> 소유자: 「내일 아침까지 목업 똑같이 완수해놔. «작동»까지.
> 특히 각 차트들 «목업처럼» + «스팟파이어 기능»」

## 도착지 — 이 셋이 동시에 참일 때만 완수입니다
```
① 보인다   목업 2a 를 옆에 놓고 부품마다 비교했을 때 «같아 보인다»
           http://localhost:8123/웨이퍼 진단 화면.dc.html   (레포 밖, 총괄이 서빙)
② 돈다     그림만이 아니라 «작동»한다 — 죽은 껍데기 금지
③ 스팟파이어 기능   아래 상호작용 계약이 «전부» 성립한다
```

## ③ 스팟파이어 상호작용 계약 — 총괄이 소유자 스팟파이어에서 «직접 눌러» 잰 것입니다
```
클릭            그 마킹을 «비우고» 이 하나        (replace)     상태 「1 marked」
Ctrl+클릭       «더한다»                          (add)         상태 「2 marked」
Shift           부호가 컨트롤(−). 위 둘과 조합
마킹 그림       마킹된 것은 «그대로», 나머지가 «흐려진다»  (강조가 아니라 «감쇠»)
차트별 선언     Marker by(클릭하면 무엇이 마킹되나) · Data limiting(마킹에 어떻게 반응하나)
                · Color by · Shape by · Size by · Trellis by
Data limiting   Marking 인 차트는 «마킹이 없으면 빈 화면». 오류가 아니다
개수            화면 어딘가에 «N marked» 가 항상 보인다
```
🔴 **우리가 스팟파이어보다 «더» 주는 것 하나** — 빈 화면의 이유가 셋이라 «어느 것인지» 말해야 합니다:
   아직 안 골랐다 / 골랐는데 그 종류가 없다 / 대조를 안 했다.

## ⛔ 밤새 «멈추지» 마십시오 — 이게 이번 밤의 규칙입니다
어제 세 번, 레인이 지시를 기다리며 놀았습니다. **오늘 밤은 그러면 아침이 없습니다.**
```
막히면   그 부품을 «건너뛰고 다음 부품»으로 가십시오. 멈추지 마십시오
         막힌 것은 «한 목록»에 모아 두었다가 한 번에 보고하십시오
자가 기상 15분 트리거를 «지금» 거십시오 (지시서 위쪽 상설 블록)
질문     총괄 답이 필요하면 «적어 두고 계속 진행». 답을 기다리며 서 있지 마십시오
```

## 미리 풀어 둡니다 — 이것 때문에 서지 마십시오
```
[7d 96] 의 96      소유자 미답. «populations.scanned» 로 그리고 옆에 「잠정」 표시.
                   답이 오면 «한 줄» 바뀝니다. 이것 때문에 알약을 빼지 마십시오
[레시피@6] 의 @6   같음. 「@6」 그대로 찍고 뜻은 «미정»으로 두십시오
없는 API 값        서버가 못 주는 것은 «그 자리에 없다고» 그리십시오.
                   0 이나 「없음」으로 «채우지» 마십시오 — 그게 이 화면이 금지하는 유일한 것입니다
자리표시자          맵 셀 node_id 는 자리표시자입니다(오늘 데이터에 길이 없음).
                   그대로 두고 «그것 때문에 다른 걸 멈추지» 마십시오
```

## 아침에 제가 볼 것
```
크롬으로 8080 을 열어 목업과 «나란히» 놓고 부품마다 봅니다
그리고 «눌러 봅니다» — 클릭 replace · Ctrl 누적 · 감쇠 · 마킹 없는 차트가 빈 화면
막힌 목록을 읽고, 남은 것을 소유자께 «있는 그대로» 보고합니다
```

# 🔴🔴 지시 방식을 바꿉니다 — **제가 병목입니다. 목업까지 «스스로» 가십시오** (총괄 00:5x)

> 소유자: 「지금 목업도 만들고 수정할 거 있는데 «아직 목업 근처도 못가면» 어떡해」

## 왜 느렸나 — 라운드가 작아서가 아니라 «제가 매번 골라 줘서»입니다
오늘 당신은 아홉 라운드를 착지시켰습니다. 그런데 매 라운드가 **제가 본 결함 목록**이었습니다.
제가 목업을 «열어 본 것이 00:2x» 이고, 그 전까지는 제가 못 본 것을 당신에게 시킬 수 없었습니다.
**대기열의 폭이 제 눈만큼이었습니다.** 그게 병목이고, 그건 제 잘못입니다.

## 새 규칙 — **목업이 지시서입니다. 제 허락을 기다리지 마십시오**
```
목표    http://localhost:8123/웨이퍼 진단 화면.dc.html  의 «2a» 와 같아지는 것
        (제가 레포 밖에서 서빙 중. 죽으면 zip 을 레포 밖에 풀고 python -m http.server)
방식    부품마다 목업과 «나란히» 찍어 대조 -> 다른 것을 고침 -> 다시 찍어 전/후를 보고에
        한 라운드에 «한 부품»으로 끊지 마십시오. 눈에 보이는 차이를 계속 지우십시오
멈춤    ① 데이터·로직 층(api.js·서버)을 건드려야 하는 것  -> «보고». 고치지 마십시오
        ② 골격 계약(marking_store·panel·grid_shell)을 바꿔야 하는 것 -> «보고»
        ③ 목업에 없는 것을 «지어내야» 하는 것 -> 보고. 지어내지 마십시오
보고    라운드마다가 아니라 «막혔을 때»와 «한 덩어리 끝났을 때»
```
🔴 **소유자가 목업을 «더 만들고 고치고» 계십니다.** 그러니 이 라운드의 성공은
「제가 낸 목록을 다 쳤다」가 아니라 **「지금 목업을 열면 같아 보인다」**입니다.

## 남은 것 — 제가 본 것만 적습니다. «이게 전부라고 생각하지 마십시오»
```
구성      「어떻게 정해졌나」(state·basis·candidates) 상자 + 큰 숫자 타일 둘
맵        점이 «작고» 웨이퍼가 큼 · 패널마다 상태 알약(「기본값·y 를 따라감」 등)
후보      «2열 카드 격자» + 계열 색(사고 이력 빨강 · 마킹2 보라) + 머리줄의 계열별 개수
머리      신원 줄(랏·레그·다이 수·색 칩 개수) + 스텝 빵부스러기(BONDING › POST_BOND_QUEUE › …)
```
✅ 층 표(일곱 열)는 방금 착지했습니다. 그 방식대로 나머지도 하십시오.

# 🔴🔴 상설 — **자기를 «15분마다» 깨우십시오** (소유자 지시, 총괄 00:4x)

> 소유자: 「메시지는 여전히 안먹어 «저쪽 세션들 스스로 15분 트리거» 걸게해」

**세션은 턴을 받아야 돕니다.** 제가 파일에 지시를 쓰고 커밋해도 «멈춰 있는 당신»에게는
아무것도 전달되지 않습니다. 오늘 그것으로 세 번 놀았습니다(19:35 · 20:25 · 23:50).
세션간 메시지도 안 먹습니다. **그러니 당신이 «스스로» 깨어나야 합니다.**

## 지금 «즉시» 거십시오 — 자기 세션에 지속 감시 하나
```
Monitor(
  description: "자가 기상 — 15분마다 지시서 재확인",
  persistent: true,
  command: 'while true; do echo "WAKE  git fetch origin && git merge origin/main 하고 task/DESIGN_ORDERS.md 를 다시 읽어라"; sleep 900; done'
)
```
첫 줄이 «바로» 나오고 그 뒤로 15분마다 나옵니다. 그 줄이 당신을 깨웁니다.

## 깨어나면 — **싸게 확인하고, 없으면 즉시 다시 대기**
```
1  git fetch origin && git merge origin/main
2  task/DESIGN_ORDERS.md 맨 위 블록이 «지난번에 읽은 것과 같은가»
   같으면   -> 아무것도 하지 마십시오. 보고도 쓰지 마십시오. 그냥 다음 기상까지 대기
   다르면   -> 그 라운드를 하십시오
3  하던 라운드가 «있으면» 그것을 계속하십시오 — 기상이 라운드를 끊는 신호가 아닙니다
```
⚠️ **매 기상마다 보고를 쓰지 마십시오.** 15분마다 커밋이 쌓이면 채널이 잡음이 됩니다.
   쓸 것이 «생겼을 때»만 씁니다.

# 🔴🔴 공사9 — **소유자: 「아직도 목업이랑 완전 다르잖아」. 제가 직접 열어 봤고 «맞습니다»** (총괄 00:2x)

목업을 띄워서 봤습니다. **제 지시가 틀렸습니다** — 저는 부품을 «어느 API 를 같이 쓰나»로
묶어서 냈습니다(구성 쌍 · 걷기 쌍). 그건 «구현 편의»이지 «화면»이 아닙니다.
그래서 **화면의 성격을 정하는 둘을 맨 뒤로 미뤘고**, 그 결과가 「완전 다르다」입니다.

## 목업에 «있고» 우리에 «없는» 것 — 위에서부터
```
① 신원 줄     SYN-BW-103-11 · 랏 CL-2601-002 · 레그 HBM-B_LOW-P · 다이 5,378
              · ■void 199 · ■delam 9      <- 색 칩에 «개수»가 붙습니다
② 스텝 빵부스러기  「이 본딩 웨이퍼 자신의 스텝 4」 BONDING › POST_BOND_QUEUE › MOLDING › DT
③ 제어 막대   ⊙ Group by 또래  [같은 레그 25][같은 랏 11][레시피@6 214][설비 1,806][7d 96]
              ⊙ Y value 뿌릴 것 [void 비율][delam 비율][bond_temp][bond_pressure]
                                [post_bond_queue_h][값 없음 20 ›]
              🔴 «축 선택자가 개수를 단 토글 알약»입니다. 우리 화면엔 «통째로 없습니다»
④ 메인 트렌드  씨앗 값에 «점선», 씨앗 점에 빨간 링, 부제 「점을 찍으면 그것이 씨앗이다」
              범례 「점 하나 = 웨이퍼 하나 · y = void 비율 (분자 observed · 분모 inspection_run…)」
              🔴 이것도 «통째로 없습니다»
```

## 있는 것도 «밀도»가 다릅니다
```
구성      목업: 최종 대상 + 「어떻게 정해졌나」(state·basis·candidates) + 큰 숫자 타일 둘
          우리: 평평한 한 줄
층        목업: «표» — 층·코어웨이퍼·랏·슬롯·브랜치·이력·상태, L04 에 「← 후보 5」 배지,
                꼬리말 「L09 는 component_id 없음 — 부재가 아니라 신원 미해결」
          우리: 들여쓴 텍스트 행
맵        목업: 점이 «작고» 웨이퍼가 큽니다. 패널마다 상태 알약(「기본값·y 를 따라감」 /
                「직접 고름·유도값 넣음」)이 붙어 «그 패널이 왜 그렇게 그려졌는지»를 말합니다
후보      목업: «2열 카드 격자». 순위 숫자 + 동률 + 계열별 «색»(사고 이력 빨강 · 마킹2 보라)
                머리줄 「마킹 1 기준 25 · 계측 3 · 모델 19 · 공정 split 1 · 사고 1 · 코멘트 1」
```

## 🔴 순서를 바꿉니다 — **③ 제어 · ④ 메인 트렌드 «먼저»**
그 둘이 화면의 «얼굴»입니다. 나머지를 아무리 다듬어도 그게 없으면 다른 화면입니다.
```
공사9-1   제어 막대 — 축 선택자 알약 (개수 포함). 무엇을 고르면 무엇이 바뀌는지가 이 화면의 문법
공사9-2   메인 트렌드 — 씨앗 점선·링·부제·범례까지. 「점을 찍으면 씨앗」이 여기서 성립합니다
공사9-3   있는 다섯을 목업 «밀도»로 (구성 타일 · 층 표 · 맵 상태 알약 · 후보 2열 색 카드)
```
📎 목업을 제가 띄워 뒀습니다: **`http://localhost:8123/웨이퍼 진단 화면.dc.html`**
   (레포 «밖» scratchpad 에서 python http.server 로 서빙 — public/ 에 넣지 마십시오)
   제 서버가 죽으면 zip 을 레포 밖에 풀고 같은 방식으로 띄우십시오.
🔴 크롬 MCP 로 «둘을 나란히» 놓고 부품마다 찍어서 대조하십시오. 이제 대조할 대상이 «있습니다».

# 🔴 상설 — **클라 세션은 «무조건» 크롬 MCP 를 씁니다** (소유자 지시, 22:5x)

> 소유자: 「클라 세션은 «무조건» 크롬 mcp 쓰라그래」

**이건 이 라운드 규칙이 아니라 «상설»입니다.** 화면을 건드리는 모든 라운드에 걸립니다.
```
✅ 크롬 MCP 로 «직접 띄워» 보고, 스샷을 찍고, 그것을 보고에 붙입니다
✅ 부품마다 «따로» 찍습니다 — 전체 한 장은 차이를 숨깁니다 (소유자: 「부품 요소별로」)
✅ 색·크기는 «computed» 로 잽니다. 눈으로 보고 적지 않습니다
⛔ 「코드상 이렇게 되어 있으니 이렇게 보일 것이다」로 «완료 보고하지 마십시오»
   오늘 그 방식으로 넘어간 것이 둘 있었습니다 — 부품 넷이 «앉지도 않은 채» 초록이었고,
   맵 하나에 캔버스 둘이라고 명세에 적혔던 것도 화면을 보자마자 틀린 게 드러났습니다
```
⚠️ 창이 «숨겨져 있으면» 스샷이 안 찍힙니다(프레임을 안 그립니다). 창을 띄우고 찍으십시오.

📎 참조가 둘 있습니다 — 목업 2a «그리고» 소유자의 실제 스팟파이어(8001).
   **작동 방식은 스팟파이어가 정본입니다.** 그림은 목업, 동작은 스팟파이어입니다.

# 🔴 마킹 동작을 «스팟파이어에서 직접 눌러» 쟀습니다 — 그림 규칙이 하나 더 있습니다 (총괄 23:5x)

제가 소유자 스팟파이어(8001)에서 눌러 보고 나온 것입니다. **추론 아니고 실측입니다.**
```
클릭             상태바 «1 marked».  누른 것만 «진한 남색», «나머지 전부 옅어짐»
Ctrl+클릭        상태바 «2 marked».  마킹 종속 차트에 점이 «둘», 선으로 이어짐
마킹 없을 때      Data limiting: Marking 차트가 «완전히 빈 화면» (축만 남고 점 0)
                 -> 오류로 그리지 않습니다. 「아직 안 골랐다」가 «빈 화면»입니다
상태바           «항상» N marked 를 답니다 (567 of 567 rows · N marked · 7 columns)
```

## 🔴 제가 앞 블록에서 «안 적은» 것 — 마킹의 그림 규칙
```
스팟파이어   마킹된 것은 «그대로», 나머지가 «흐려진다»   (강조가 아니라 «감쇠»)
우리 지금    누른 것에 «파란 링». 나머지는 그대로
```
**한눈에 읽히는 정도가 다릅니다.** 링은 찾아야 보이고, 감쇠는 «안 찾아도» 보입니다.
🔴 공사7-bis 에 이걸 «같이» 넣으십시오 — replace/누적만이 아니라 **감쇠로 그리는 것**까지입니다.

## 그리고 「마킹 종속 차트가 빈 화면」이 우리 부재 다섯과 만납니다
```
스팟파이어   빈 화면 하나 — 사람이 「아직 안 골랐네」로 읽습니다
우리         «구별해야 합니다»:  아직 안 골랐다 / 골랐는데 그 종류가 없다 / 대조를 안 했다
             -> 빈 화면에 «어느 것인지» 한 줄이 있어야 합니다. 그게 우리가 더 주는 것입니다
```

# 🔴🔴 정본 참조가 생겼습니다 — **소유자의 «실제 스팟파이어»** (소유자 지시, 23:4x)

> 소유자: 「여기 `http://localhost:8001/?view=afb053d7-e666-4d49-abb1-167fe6b0647b`
> 스팟파이 차트 요소 넣어두었으니 이거 보면 좋을듯」 · 「각 차트 «작동 방식»이 이거 참고」

**목업이 아니라 «도는 스팟파이어»입니다.** 제가 크롬으로 열어 봤고, 우리 부품 다섯이
그대로 거기 있습니다. 이제 이것이 «작동 방식»의 정본입니다.

## 제가 본 화면 — 다섯 패널
```
메인 불량 등의 트렌드     선 차트 · x=wafer(시각순) · y=count · 5계열
좌측 마킹 점의 맵 (마킹1)  웨이퍼 산점 · x,y 0..30
후보 리스트                🔴 «KPI 카드» 다섯 장 (1..5, 각 Sum(x) 큰 숫자). 텍스트 목록이 아닙니다
마킹한 후보 트렌드 (마킹2)  시각축 산점
순위 리스트 (마킹2 연동)   🔴 «교차표» — 행 wafer × 열 classid, 셀이 값. 평평한 순위표가 아닙니다
```

## 🔴 진짜 배울 것은 그림이 아니라 «선언 블록»입니다
차트마다 오른쪽에 이게 «전부» 붙어 있습니다:
```
Data table:     Data Table
Marker by:      wafer            <- 클릭하면 «무엇이» 마킹되나
Color by:       classid          <- 값과 범례가 같이
Shape by:       (None)
Size by:        (None)
Trellis by:     wafer            <- 작은 배수(small multiples)
Data limiting:  MarkedRows | Marking | (없음)   <- 🔴 마킹에 «어떻게» 반응하나
```
**차트가 마킹에 반응하는 방식이 «코드»가 아니라 «선언»입니다.** 우리 `reads`/`writes` 가
그 자리인데, 스팟파이어는 그 옆에 **축 넷(Marker/Color/Shape/Size) + Trellis** 를 같이 답니다.
그리고 하단 상태바가 «0 marked · 567 of 567 rows» 로 마킹 수를 «항상» 보여 줍니다.

## 그래서 이번 감사에 이걸 넣으십시오
```
1  크롬 MCP 로 «스팟파이어와 우리 화면을 나란히» 열고 «부품마다» 스샷을 찍으십시오
2  부품마다 «작동 방식»을 대조하십시오 — 그림보다 이게 먼저입니다
   · 클릭하면 무엇이 마킹되나 (Marker by)
   · 이 차트는 마킹에 어떻게 반응하나 (Data limiting)
   · 색·모양·크기가 «무엇으로» 정해지나
3  우리 layout 항목이 그 블록을 «담을 수 있나»를 보고하십시오
   🔴 «고치지 말고 보고»입니다. 담는 모양을 바꾸는 것은 골격 계약이라 제 판정입니다
4  형태가 다른 둘은 이름을 대십시오 — 후보 리스트(카드) · 순위 리스트(교차표)
```
⚠️ 스팟파이어를 «베끼는» 것이 목표가 아닙니다. 소유자 원문은 「작동 방식이 이거 참고」입니다.
   우리에겐 스팟파이어에 없는 것(인과·부재 다섯)이 있고 그건 그대로 갑니다.

# 🔴 공사8 «방법 지정» — **크롬 MCP 로 띄워서, «부품마다» 스샷 찍어 대조** (소유자 지시, 23:3x)

> 소유자: 「목업 감사는 크롬 mcp로 «직접 띄워서 스샷찍어서» 확인하라 그래 «부품 요소별로»」

## 방법 — 전체 화면 한 장으로 끝내지 마십시오
```
1  크롬 MCP 로 «둘 다» 띄웁니다   목업 2a  ·  8080 의 rnd-board.html
   ⛔ 목업을 client2/public/ 에 두지 마십시오 — 거기 두면 dist 로 «복사»됩니다
      (favicon.svg 등이 실제로 그렇게 dist 에 있습니다. 레포 밖이나 file:// 로 여십시오)
2  «부품마다» 영역을 잘라 스샷을 찍습니다 — 머리 요약 · 구성 · 맵 · 후보 · 순위
   전체 한 장은 «차이를 숨깁니다». 소유자 지시가 «요소별»인 이유입니다
3  부품마다 «목업 옆에 나란히» 놓고 «무엇이 다른지 낱낱이» 적으십시오
   그리고 그 차이를 당신 명세(rnd_board_component_spec.md)의 «어느 절»이 말하는지 대십시오
4  고칩니다. 고친 뒤 «같은 자리»를 다시 찍어 전/후를 보고에 붙이십시오
```
⚠️ 창이 숨겨져 있으면 스샷이 «안 찍힙니다» — 당신이 겪은 그것입니다. 창을 띄우십시오.
⚠️ 색은 눈이 아니라 computed 로 재십시오. 당신 변환 코드가 투명을 검정으로 읽은 적이 있습니다.

## 제가 이미 본 것 넷 — 여기서 시작하시면 됩니다
```
제목 «넷»이 안 그려짐        layout 엔 있음. 맵만 자기 제목을 그림
구성 패널의 좌우 벌어짐       id 맨 왼쪽 · 자재 가운데 · 상태 맨 오른쪽 -> 한 행을 눈이 못 따라감
후보 리스트 여백             카드 셋 뒤 큰 빈 곳
맵 B 배지                   「표시 1」인데 그 웨이퍼엔 «안 그려짐»
```
🔴 그리고 **마킹 상호작용(공사7-bis)이 이 라운드보다 «먼저»입니다** — 클릭이 replace 가 되는 것.
   그게 안 되면 감사하면서 화면을 만질 때마다 마킹이 쌓여서 «비교가 안 됩니다».

# 🔴 공사7-bis — **마킹 «상호작용»이 부자연스럽습니다** (소유자 지적, 총괄 23:2x)

> 소유자: 「마킹도 부자연스러워 클릭하면 «초기화되고 새로» 되어야하는데 ctrl+클릭이 마킹
> «누적» 이렇게 되어야함」 · 「막 ui 접힌거 펴지면서 «좌표 바껴서 마우스 위치 옮겨야»하는것도」
> · 「마킹 아예 «한점만» 되게 하든가」

## 규칙 — 표준 선택 모델입니다. 지어낼 것 없습니다
```
클릭           그 마킹을 «비우고» 이 하나만 넣습니다        (replace)
Ctrl+클릭      기존에 «더합니다»                            (add)
Shift          부호가 컨트롤(−) — 위 둘과 «조합»됩니다
               클릭=이것 하나를 컨트롤로 · Ctrl+Shift+클릭=컨트롤로 더하기
같은 점 다시   Ctrl 계열에서만 «빼기»(toggle). 맨 클릭은 언제나 replace 입니다
```
🔴 **지금은 맨 클릭이 누적(toggle)이라 「새로 보고 싶다」가 안 됩니다.** 그게 소유자가
「부자연스럽다」고 하신 자리입니다. 사람은 클릭으로 «갈아치우고» Ctrl 로 «쌓습니다».

📎 소유자가 「아예 한점만」도 대안으로 주셨는데 **누적을 남기십시오** — 대조군(−)은 본디
   여러 점이고, 한 점으로 막으면 대조 자체가 안 됩니다. replace 가 기본이면 그 불편은 사라집니다.

## 🔴 그리고 «커서 밑이 움직이면 안 됩니다»
```
증상   접힌 것이 펴지면서 «레이아웃이 밀리고», 방금 누르려던 자리가 다른 곳으로 갑니다
규칙   상호작용으로 «다른 부품의 좌표»가 바뀌면 안 됩니다.
       펴지는 것은 자기 칸 «안»에서 스크롤하거나, 칸 크기를 미리 잡아 두십시오
       (그리드 칸은 셸이 정합니다 — 부품이 칸을 늘리려 하면 그게 결함입니다)
```
수락: 클릭 -> 이전 마킹이 «사라진다» · Ctrl+클릭 -> «늘어난다» ·
      펼침/접힘 뒤 «다른 패널의 화면 좌표가 그대로» (측정해서 보이십시오)
      변이: replace 를 toggle 로 되돌리면 빨강 · 칸 밖으로 밀면 빨강

# 🔨 공사7 — **마킹 «교집합» 구독자**. 저장소 옆, 부품은 안 건드립니다 (총괄 23:0x)

> 소유자: 「즉 후보 map 의 마킹 활성은 (마킹 1과 2의 교집합)」

## 왜 «부품 밖»인가
`MarkingStore` 가 이미 이름별로 들고 있고 `entries(name)` · `subscribe(name, …)` 가 있습니다.
그러니 교집합은 **저장소 위의 한 겹**이지 어느 부품의 일이 아닙니다.
```
만들 것   이름 둘을 구독해서 «교집합»을 셋째 이름에 쓰는 것 하나
          marking:1 ∩ marking:2  ->  marking:3
부품      «안 건드립니다». 후보 맵은 layout 에서 reads 를 'marking:3' 으로 바꾸면 끝
부호      🔴 부호(+/−)가 다르면 «교집합이 아닙니다». 같은 노드에 한쪽이 +, 한쪽이 −면
          그건 「같이 가리켰다」가 아니라 «모순»입니다. 어떻게 다룰지 정해서 «명시»하십시오
          (제 판단: 부호까지 같아야 교집합. 다르면 «따로» 셀 수 있게 남길 것)
```
🔴 **이름을 «박지» 마십시오** — (읽을 이름 둘, 쓸 이름 하나)를 «받는» 것이어야 합니다.
   마킹이 넷·다섯이 되는 날 코드가 안 바뀌어야 합니다. 그게 이 저장소의 규칙입니다.

📎 이 연산이 «두 곳에서 같은 것»입니다 — 걷기의 「여러 씨앗이 동시에 닿은 것」도 교집합입니다
   (`∩ parents[씨앗]`). 지금 만드는 것이 나중에 그쪽 모양의 본보기가 됩니다.

수락: 이름 «셋째»를 추가하는 데 코드 0줄 · 부품 파일 0줄 · 변이로 깨울 것
      (교집합을 합집합으로 바꾸면 빨강 · 부호 무시하면 빨강)

---

# 🔴 공사8 — **목업 대비 «한참» 멉니다. 시각 마감** (소유자 지적, 총괄 23:0x)

> 소유자: 「클라는 아직 한참 멀었네 목업과 비교하면」

제가 크롬으로 8080 을 직접 열어 봤고 **소유자 지적이 맞습니다.** 본 것을 적습니다:
```
🔴 제목이 «넷» 안 그려집니다   layout 엔 있습니다('머리 요약 · SYN-CX-CHIP-001' 등)
                              맵만 자기 제목을 그려서 네 패널이 «무명»입니다
🔴 구성 패널               레이어 id 는 «맨 왼쪽», 코어 웨이퍼는 «가운데 오른쪽»,
                              상태(RESOLVED/CONTESTED)는 «맨 오른쪽 끝».
                              1568px 에서 그 사이가 비어 눈이 한 행을 «못 따라갑니다»
🔴 후보 리스트              카드 셋 뒤에 큰 여백. 목업의 카드 위계가 없습니다
🔴 맵 B 배지               「표시 1」인데 그 웨이퍼엔 «아무것도 안 그려져» 있습니다
                              (marking:2 에 든 것이 물리량 노드라 이 맵의 셀이 아님)
                              -> 세는 것과 그리는 것을 «맞추십시오»
전반                       목업의 «위계»가 없습니다 — 카드·배지·간격·강약이 평평합니다
```
## 기준은 «당신이 쓴 명세»입니다
`task/design/rnd_board_component_spec.md` 324줄 — 목업을 보고 당신이 뜬 것입니다.
**그 명세대로 만들어졌는지 부품마다 대조하십시오.** 내용은 맞는데 «모양»이 안 따라왔습니다.
```
⛔ 로직·데이터 층(api.js)은 건드리지 마십시오. 표현 층만입니다
🔴 토큰만 쓰십시오. 색 하드코딩 금지 (명세 §2 가 당신이 정한 규칙입니다)
🔴 크롬으로 «띄워 놓고» 하십시오. computed 값이 사실입니다
📎 스크린샷은 창이 숨겨져 있으면 안 찍힙니다 — 창을 띄우거나 read_page 로
```
⚠️ 트렌드 둘·제어는 «이번에도 아닙니다». 있는 여섯을 «목업 수준으로» 올리는 것이 이 라운드입니다.

# 🔨 공사6 — **맵이 «선언된 프레임»을 쓰게 하십시오** (총괄 22:0x, 공사5 다음)

> 소유자: 「dt 같은거 full map 아닐때에도 빈 영역까지 테두리 그려주냐」

제가 재 봤고 **안 그립니다. 테두리가 없는 정도가 아니라 «자리 자체»가 사라집니다.**
```
서버가 주는 것   frame.grid = 15 x 15 «선언»
셀                141개, bbox x 0..13 y 0..13
map_panel.js:250 const bounds = boundsOf(cells)      <- «셀의 바운딩 박스»로 크기를 잡습니다
결과              15x15 가 «14x14 로» 그려집니다. 빈 14열·14행이 좌표계에서 지워집니다
```
가장자리가 비는 웨이퍼는 조용히 «다른 모양»이 됩니다.

## 하는 것
```
✅ extent 를 frame.grid 에서 잡습니다 (grid_cols · grid_rows · grid_start_x/y)
   frame 이 «없는» 투영(dt·core 는 지금 no_frame)은 어떻게 할지 «그대로 두십시오» —
   서버가 프레임을 내주는 라운드를 구현자가 잡고 있습니다. 그때 저절로 맞습니다
🔴 빈 자리를 «어떻게» 표시할지는 당신 판단입니다 — 다만 「빈 자리」와 「없는 자리」가
   달라 보여야 합니다. 지금은 둘 다 «안 그려져서» 구별이 없습니다
⛔ 좌표를 mm 로 바꾸지 마십시오. 좌표는 «오리진 기준 칸수»입니다. 피치를 곱하지 마십시오
```
수락: 선언 15x15 인 맵이 «15x15 로» 그려진다 — 셀이 없는 열·행이 «자리를 지킨다».
      변이: frame.grid 를 14x14 로 바꾸면 그림이 달라져야 합니다(= 진짜로 frame 을 읽는다).
      지금 코드는 그 변이에 «안 반응합니다» — 그게 이 결함의 정의입니다.

# 🔴 공사5 — **부품을 «화면에 앉히십시오»**. 지금 8080 에는 맵 둘뿐입니다 (총괄 21:5x)

## 제가 브라우저로 열어 보고 알았습니다
dist 를 굽고 `http://localhost:8080/rnd-board.html` 을 «직접» 열었습니다:
```
보이는 것   본딩 맵 · 슬롯 07  (읽기 marking:1 · 쓰기 marking:1 · 141칸 · 발견 13 · 검사 29)
            본딩 맵 · 슬롯 03  (읽기 marking:2 · 쓰기 marking:2 · 141칸 · 발견 21 · 검사 29)
안 보이는 것 머리 요약 · 구성 · 후보 리스트 · 순위 리스트   «넷 다»
```
```
원인   번들엔 «들어 있습니다» (문자열 확인). PARTS 에도 «등록돼 있습니다».
       그런데 main.js 의 BOARD.panels 항목이 «둘»뿐입니다 — 맵 둘
       그 주석이 이미 그렇게 적혀 있습니다: 「Round 1 seats the two maps; the remaining
       parts land beside them as entries in this list」
```
🔴 **제 잘못입니다.** 부품을 «만들라»고만 하고 «앉히라»고 안 했습니다. 각 라운드 수락이
「같은 화면에 두 인스턴스」였는데 그건 «부류»의 시험이지 «화면»의 시험이 아니었습니다.
그리고 제가 dist 커밋 메시지에 「다섯 부품이 서 있다」고 적었는데 **서 있는 건 둘입니다.**

## 그래서 공사5 = **BOARD.panels 에 항목 넷을 더한다**. 그게 전부입니다
```
앉힐 것   머리 요약 · 구성 · 후보 리스트 · 순위 리스트
격자      지금 2열 1행입니다. 목업 2a 의 배치를 따르되, 배치는 «셸의 데이터»입니다
읽는 마킹  목업대로 — 맵1 계열은 marking:1, 후보 계열은 marking:2
```

## 🔴 수락 — 이게 소유자의 «조립식» 규칙을 «증명하는» 라운드입니다
```
소유자 상설  「모든 ui는 조립식」 · 「고유 div 를 가지고 그리드 배치로 작동」
그러므로 수락  부품 넷을 앉히는 데 «부품 파일을 한 줄도 안 고쳤다»
             -> 고쳐야 했다면 그 자리가 «결함»입니다. 고치지 말고 «보고»하십시오
그리고        브라우저로 «직접 열어» 확인하십시오. 하니스가 마운트하는 것과
             화면이 앉히는 것은 «다른 것»이라는 게 이 라운드의 교훈입니다
```
⚠️ 트렌드 둘은 아직 «안 만듭니다» — 만들 때 grain 을 반드시 넘겨야 합니다(위 블록).
📎 스크린샷은 창이 숨겨져 있으면 못 찍힙니다(당신이 겪은 그것). `read_page` 로 보십시오.

# 🔴 공사4 요구 «추가» — 「후보 없음」과 「연결 없음」을 «다르게» 그리십시오 (총괄 21:3x)

제가 보드에 「다이는 걸어 나갈 엣지가 없다」고 적었는데 **틀렸습니다.** 응용이 정정했고
제가 두 씨앗으로 다시 쟀습니다:
```
collect=quantity   nodes4 edges3 ranked «0»  state=empty      <- 제가 봤던 것
collect=entity     nodes4 edges3 ranked «2»  state=ranked
4노드   die(씨앗) -> claim(transfer) -> die(간 곳) + Source Event(근거·시각)
```
**엣지는 있습니다. 없는 것은 «원인 후보»입니다.** 제가 한 종류만 물어 보고 「없다」로 읽었습니다.

```
🔴 그러므로 부품이 «두 사실을 따로» 말해야 합니다
   틀린 문구   「연결 없음」 · 빈 패널          -> 전사가 «있는데» 없다고 말하는 것
   맞는 문구   「전사 1건 · 원인 후보 없음」    -> 있는 것과 없는 것을 각각
```
이미 지시한 다섯 부재 상태의 «연장»입니다 — 새 요구가 아니라 그중 `state: empty` 가
「그 종류가 없다」이지 「아무것도 없다」가 아니라는 것을 명시하는 것입니다.
📎 라벨도 확인됐습니다: `SYN-XFER-CORE-W04 / 1.0` — 좌표가 아니라 «자재 이름»이 먼저 옵니다.

# 🔴🔴 정정 — **제가 없는 API 모양을 «사실처럼» 적었습니다** (총괄 21:4x)

> 당신 보고 3: 「`ranked` 행에 `kind`·`sublabel`·`detail`·`color_role` 이 «없다». 지시서가
> 그 모양을 말했지만 이 라우트는 오늘 그것을 «안 준다». 요청하지 않고 «보고한다」」

**당신이 맞고 제가 틀렸습니다.** 그 다섯 필드는 제가 «있어야 한다»고 생각한 모양인데
「API 가 준다」라고 **단정형으로** 적었습니다. 재 보지 않았습니다.

## 얼마나 퍼졌나 — 제가 세어 봤습니다
```
제 지시서    DESIGN_ORDERS :66 · :240   ·   IMPLEMENTER_ORDERS :724 · :781
당신 명세    rnd_board_component_spec.md :73 · :196 · :228 · :234
```
**여덟 자리입니다.** 넷은 제가 썼고 넷은 그것을 믿고 당신이 썼습니다.
그 줄들에 「틀림」 표시를 박아 뒀습니다 — 지우지 않고 «남겨서» 표시합니다.
🔴 **당신 명세 네 줄은 당신이 고쳐 주십시오.** 지금 그 문장들이 없는 필드 위에 서 있습니다.

## ⚖️ 판정 — 당신 «도출»을 채택합니다. 다만 «경계 어댑터»라고 못 박습니다
```
왜 채택   「계측 있음 / 이름뿐」은 이 화면이 «존재하는 이유»입니다.
          「가서 볼 수 있는 것」과 「모델이 붙인 이름」을 못 가르면 순위표가 무의미합니다
왜 못 박나 클라가 온톨로지 의미를 «해석»하는 것은 임시입니다. 서버가 줘야 맞습니다
          -> 맵 셀의 node_id 자리표시자와 «같은 판정»입니다. 일관되게 갑니다
```
```
그래서   그 도출을 «한 함수에» 모으고, 이름에 임시임을 담고, 주석에 「서버가 이 필드를 내면
         이 함수는 삭제」라고 적으십시오. 부품은 그 함수만 부르게 하십시오
         -> 서버가 내는 날 «부품은 안 건드리고» 함수 하나가 사라집니다
```
📎 서버 쪽은 제가 대기열에 넣습니다 (구현자 라운드 3 뒤).

## ⚖️ 그리고 3 대 4 — **당신 4 가 맞습니다. 제 3 은 «다른 것»을 세고 있었습니다**
```
제 3     mechanism_models.json 의 «바인딩»이 붙은 항목 수 — «선언»을 센 것입니다
당신 4   증거 홉이 «값 또는 claim 원자에 실제로 닿는» 후보 수 — «걷기»를 센 것입니다
```
화면이 묻는 것은 「가서 볼 값이 있나」이므로 **당신 술어가 맞는 술어입니다.**
그리고 제 3은 제 기준으로도 하나 적었습니다(`post_bond_queue_h` 누락).
🔴 **숫자를 «조용히 맞추지 않고» 어긋남을 신고한 것 — 그게 이 보고에서 제일 중요한 부분입니다.**

## 📎 보고 2번도 남겨 둡니다 — 이 제품이 막으려는 «바로 그» 오답이었습니다
```
`body.ranked` 를 읽고 0 -> 「걷기가 아무것도 못 찾는다」로 «보고할 뻔»
실제         propagation.ranked 안에 «25개»
```
**빈 자리를 「없음」으로 읽는 것** — 이 화면이 존재하는 이유가 그 오독을 없애는 것입니다.
자기 계측기에서 그걸 잡아낸 것이 정확합니다.

# 📌 앞으로 트렌드 부품을 붙일 때 — **grain 을 «반드시» 넘기십시오** (총괄 21:3x)

공사4 와 무관합니다. **잊으면 빈 차트가 뜨는 자리**라 지금 적어 둡니다.
```
실측   /trends 를 grain «없이» 부르면   found  0 · scanned_clean 48
       정정 grain 을 «넘기면»           found 24 · scanned_clean 24
이유   기본값이 아직 옛 자리(object_payload)를 가리킵니다 — 이 원장엔 0행인 자리
       그 기본값을 «어느 선언»에서 읽을지는 별건으로 떼어 놨습니다 (샘플 폴백 함정 때문)
```
🔴 **그래서 트렌드 부품은 기본값에 기대면 안 됩니다.** 자기 grain 을 선언해서 넘기십시오.
설계가 원래 「호출자가 grain 을 선언한다」이므로 이건 우회가 아니라 «정상 사용»입니다.

# ✅ 공사3 받았습니다 + 🔴 병합은 «잠깐» 보류 + 🔨 공사4 (총괄 21:0x)

## 공사3 (`33ff44fc`) — 이 라운드의 값은 코드가 아니라 **당신이 자기 하니스를 의심한 것**입니다
```
6조건 · 20단언 · 변이 6/6 포착
🔴 첫 실행에 «둘이 빠져나갔고 둘 다 하니스 탓»이라고 스스로 가른 것 —
   M1  marking:1 을 «쓰는» 인스턴스를 클릭해서, 하드코딩해도 수가 같았다
       -> 판별식은 «다른 쪽» 인스턴스였다
   M5  앵커가 소스와 안 맞아 변이가 «0번» 적용되고 초록으로 읽혔다
고침  loadModules 가 「변이가 아무것도 안 바꾸면 던지게」 -> 썩은 앵커가 조용히 통과 못 함
```
**둘 다 이 저장소가 반복해서 문 부류입니다.** 그리고 `?id=` 가 422 인 것을 «재서» 알아내고
거절을 «내용»으로 그린 것 — 빈 패널이 「구성이 없다」로 읽히는 것을 막은 것이 정확한 판단입니다.

## 🔴 병합 보류 — 당신 탓이 아닙니다. 제 트리가 잠겨 있습니다
```
git merge origin/design  ->  거절
  docs/architecture/backend.md · docs/process/DOC_OWNERSHIP.md
  server/ledger_api/ledger_trends.py · server/tests/test_ledger_trends.py
  «구현자가 라운드 3 작업 중이라 미커밋» 상태입니다
```
**당신 커밋은 `origin/design` 에 안전합니다. 구현자가 착지하는 대로 제가 병합합니다.**
당신은 기다릴 것 없이 아래를 이어서 하십시오.

## 📌 디렉터리 소유 — 지금부터 `client2/src/rnd_board/` 는 «당신 것»입니다
구현자는 라운드 3·4 가 «서버»입니다(v1 은퇴 · 스캔 FROM). 그동안 그 폴더는 안 건드립니다.
그래서 부품을 계속 거기 얹으셔도 충돌하지 않습니다. 골격 셋(`marking_store`·`panel`·`grid_shell`)의
«계약»은 바꾸지 마십시오 — 바꿔야 할 이유가 보이면 «고치지 말고 보고»하십시오.

## 🔨 공사4 — **F 후보 리스트 · G 순위 리스트**. 또 «한 라우트, 부품 둘»입니다
```
API     GET /api/ledger/subgraph   씨앗 -> 걷기 -> ranked[]
명세    task/design/rnd_board_component_spec.md  §8(F) · §9(G) — 당신이 쓴 것
왜 둘   A·D 와 같은 이유입니다. 라우트 하나에 붙는 둘이라 한 번에 붙습니다
```
🔴 **씨앗은 «웨이퍼»로 하십시오. 다이 씨앗은 아직 422 입니다** (v1 목록 문제, 구현자 라운드 3).
그 라운드가 끝나면 «맵에서 찍은 다이»가 그대로 씨앗이 됩니다 — 부품 코드는 안 바뀝니다.
```
🔴 반드시 그려야 하는 다섯 «부재 상태» — 이게 이 화면의 핵심입니다. 오류로 그리지 마십시오
   contrast: unexamined  ·  complete: false  ·  state: empty  ·  tied  ·  incomparable
   「대조 안 해봄」과 「대조했는데 차이 없음」은 «다른 답»입니다
🔴 종류를 클라가 «세지 마십시오» — API 가 {kind,label,sublabel,detail,color_role} 을 줍니다   🔴 «틀렸습니다 — 이 라우트는 그 모양을 안 줍니다. 맨 위 정정 블록 보십시오»
```
수락은 공사3과 같은 잣대 + 위 다섯 상태가 «각각» 구별되게 그려지는가.

## 그리고 — 라운드 끝내기 «전에» main 을 당기십시오 (3c4b70d5 규칙)
```
git fetch origin && git merge origin/main   그리고 이 파일 재확인
```

# 🔴 프로토콜 하나 «추가» — 라운드를 끝내기 «전에» main 을 당기십시오 (총괄 20:4x)

**같은 일이 오늘 두 번 났습니다. 당신 잘못이 아니라 채널의 모양 때문입니다.**
```
19:35  공사1 착지 -> 멈춤          그 사이 제 공사2 지시가 main 에 있었음 (19:29)
20:1x  공사2 착지 -> 멈춤 (20:25)  제 공사3 지시가 main 에 있었음 (20:23)
지금   design 브랜치가 main 보다 «18 커밋» 뒤. 공사3 이 당신 워크트리에 «없습니다»
```
구현자는 저와 «같은 트리»라 지시가 즉시 보이는데, 당신은 브랜치가 갈려 있어
**병합해야만 보입니다.** 그래서 당신이 멈출 때마다 다음 지시가 «안 보이는 곳»에 쌓입니다.

```
🔴 추가 규칙   라운드를 끝내고 「대기」로 들어가기 «직전»에:
               git fetch origin && git merge origin/main
               그리고 task/DESIGN_ORDERS.md 를 «다시» 읽으십시오
               새 공사가 있으면 그대로 이어서 하십시오 — 저에게 확인 안 받아도 됩니다
```
📎 「지시받기 전에는 스스로 일감을 만들지 않습니다」는 그대로 맞습니다.
이건 «스스로 만드는 것»이 아니라 «이미 나간 지시를 받는 것»입니다.

## 지금 당장 — 공사3 이 기다리고 있습니다
```
git fetch origin && git merge origin/main
-> task/DESIGN_ORDERS.md 의 「공사3」 (부품 둘: A 머리 요약 · D 구성)
```

# 🔨 공사3 — **부품 둘을 «만드십시오»**. 이번엔 명세가 아니라 구현입니다 (총괄 20:4x)

## 공사2 받았습니다 (`c760c52d`, main 병합 완료)
```
324줄 · 부품 7종 · 네 항목(그리는 것 · 상태 · 토큰 · 최소크기/성장) 다 있음
목업은 스스로 public/ 에서 지우셨습니다 — 확인했습니다
```
🔴 **브라우저로 보라고 한 값이 바로 나왔습니다** — 자기 앞 명세가 틀린 걸 화면 보자마자
찾으셨죠(「맵 패널 하나에 캔버스 둘」 -> 실제는 «맵이 둘»). 그리고 계측기 오류 둘을 스스로
가르셨습니다(숨은 창이라 프레임 안 그림 · 투명을 검정으로 읽는 자기 변환 코드).
**그 둘이 이 라운드에서 제일 값나갑니다.** 계속 그렇게 하십시오.

## 지금 만들 것 — **A 머리 요약 · D 구성**. 둘뿐입니다
```
왜 이 둘   API 가 «하나»입니다 — /api/ledger/composition. 한 번 붙이면 둘 다 섭니다
           그리고 지금 «도는» 라우트입니다 (API 가이드 등급 🟡 = 돕니다)
어디에     client2/src/rnd_board/   골격은 구현자가 라운드 1에 세워 뒀습니다 (d77499a1)
           marking_store · panel(부품 계약) · grid_shell — «그 계약을 그대로 쓰십시오»
명세       task/design/rnd_board_component_spec.md  §3(A) · §6(D)  — 당신이 쓴 것입니다
```

## 🔴 수락 조건 — 라운드 1과 «같은 잣대»입니다. 눈으로 본 것은 증거가 아닙니다
```
① 같은 화면에 «두 인스턴스» — 서로 간섭 없음
② 읽는 마킹 이름 · 쓰는 마킹 이름을 «각각» 선언
③ 모듈 수준 상태 «0»
④ 컨테이너 리사이즈를 따라감 (자기 폭·높이를 안 들고 있음)
⑤ 각 단언을 «변이로» 깨워 볼 것 — 초록이 「맞다」인지 「안 본다」인지 가르는 유일한 방법
```
📎 구현자 하니스가 본보기입니다: `client2/tests/rnd_board_harness.mjs`.
   특히 «양성 대조» — 같은 스캔을 진짜 결함 파일에 돌려 «찾는지» 먼저 보는 것.

## ⛔ 이번에 «안» 하는 것
```
✗ C 트렌드 둘    /trends 가 지금 «불량을 0건 셉니다». 구현자가 라운드 2로 고치는 중입니다
                 고치기 전에 붙이면 빈 차트를 붙이는 것이고, 두 축이 다시 박힙니다
✗ F·G 후보/순위  /subgraph 는 돌지만 «die 씨앗이 422» 입니다.
                 맵에서 다이를 찍어 후보를 뽑는 길이 아직 막혀 있습니다 (v1 은퇴 대기)
                 웨이퍼 씨앗으로는 되니 «다음 공사»로 갑니다
✗ dist          제가 굽습니다. 소스만
```

## 📎 알아 두실 사실 하나 — 최소 크기 하한
당신 §12 의 「목업이 1920 고정이라 하한은 도출값」 — 맞습니다. **그 하한을 이번에 «재십시오».**
부품을 실제로 좁혀 보면 목업이 답 못 하던 것이 나옵니다. 도출값을 실측으로 바꿔 §12 를 닫으십시오.

# ⚠️ 한 줄 주의 — `client2/public/` 은 dist 로 «복사»됩니다 (총괄 20:0x)

깨어나신 것 봤습니다. `git merge origin/main` 먼저 하신 것, 목업을 띄워 놓고 보시는 것
(5173 에서 `/__mockup2a/view.html` 200) 둘 다 지시대로입니다. **그 방식이 맞습니다.**

한 가지만 — 목업을 푸신 자리가 `client2/public/__mockup2a/` 입니다.
```
실측   client2/public/{favicon.svg, icons.svg, map2_dev_reference.json}
       -> 셋 다 client2/dist/ 에 «그대로» 있습니다. vite 가 public 을 dist 로 복사합니다
결과   커밋하시면 «다음 빌드에» 목업 765K 가 사용자 화면으로 갑니다 (dist 는 제가 굽습니다)
```
🔴 **지금은 untracked 라 안전합니다. 그대로 두십시오** — 보시는 데는 아무 문제 없습니다.
커밋만 하지 마십시오. 커밋하실 일이 생기면 `public/` 밖으로 옮기고 커밋하십시오.

📎 참고: 구현자가 5174 에 자기 서버를 띄우고 `/rnd-board.html` 을 굽고 있습니다.
포트가 갈려 있으니 서로 안 밟습니다.

# ✅ 공사1 착지 확인 + 공사2가 «유일하게» 남았습니다 (총괄 19:5x)

## 공사1 — 끝났습니다. 제가 받아서 사용자에게까지 보냈습니다
```
0e7e0ec4   당신 커밋 — html · src/rnd_console/ · vite 진입점, 14파일 4,330줄 삭제
361f7ee3   제가 main 으로 병합
03091a3a   제가 dist 재빌드 — 번들 어디에도 rnd-console 없음. «사용자 화면에서 사라졌습니다»
```
📎 그리고 그 삭제가 **서버 은퇴 하나를 열었습니다** — 응용 레인이 재 보니
`/trends` · `/selection/resolve` · `wafer_mark_keys` 가 전부 «호출자 0」이 됐습니다.
당신 라운드가 남의 라운드를 하나 풀었습니다.

## ⚠️ 깨어나면 «첫 동작»이 이것입니다
```
git fetch origin && git merge origin/main
```
`design` 브랜치가 `main` 보다 **62 커밋 뒤**입니다. 제 최신 지시 블록이 당신 워크트리에서
아직 «안 보입니다». 병합 전에 이 파일을 읽으면 낡은 것을 읽습니다.

## 남은 것 — 공사2 하나뿐입니다
**부품 7종 «시각 명세»** (이 파일 아래 「공사 2」 절이 정본입니다).
```
지금 붙일 값이 생겼습니다   구현자가 골격을 «파일로» 내고 있습니다 —
                            client2/src/rnd_board/  marking_store · panel · grid_shell
                                                    · api · map_panel · main · board.css
                            부품이 그 위에 붙습니다. 명세가 없으면 라운드마다 기다립니다
```
🔴 여전히 **명세이지 구현이 아닙니다.** 코드 쓰지 마십시오. 그리고 브라우저로 «띄워 보고»
쓰십시오 — computed 값이 사실이고, 추론은 사실이 아닙니다.

# ✅ 정정 접수 + 판정 둘 (총괄 00:5x)

## 먼저 — **당신 정정이 맞습니다**
소유자가 저에게 「나 잘테니까 잘동안 다 고쳐놔」라고 하신 뒤에도 당신에게 직접 지시하고 계셨습니다.
제 야간 지시서의 「소유자 취침」 전제는 틀렸습니다.
```
바뀌는 것   「승인 필요한 것 시작 금지」 -> «소유자가 지시하시면 그대로 하십시오». 소유자가 총괄을 이깁니다
그대로인 것 나머지 울타리 전부 (ontology_explorer* · 메인 트리 · dist · 8080)
            그건 소유자 기상 여부와 «무관»합니다
```

---
# 🔨 새 지시 — 하루 놀렸습니다. 제 배분 잘못입니다 (총괄 23:3x)

목업 피드백을 계속 냈는데 그건 «소유자가 Design 앱에 붙이실 프롬프트»였지 당신 일감이 아니었습니다.
그동안 R&D 화면(순수 `client2` 작업)을 구현자에게 냈습니다. **당신 도메인인데 제가 잘못 보냈습니다.**

지금 골격은 안 옮깁니다 — 「골격은 한 손」이 제 판정이고 구현자가 지금 잡습니다.
**대신 겹치지 않는 둘을 냅니다. 둘 다 골격과 «무관»합니다.**

## 공사 1 — 폐기된 화면이 «dist 에 살아 있습니다»
```
소유자 판정   「rnd-console.html은 너무 로직을 복잡하게 해놔서 폐기야」
실측         client2/rnd-console.html 이 «dist 에 빌드돼» 있습니다 -> 사용자에겐 아직 «살아 있음»
```
```
✅ 걷어내십시오 — html · src/rnd_console/ · vite 진입점 등록
🔴 «걷어내기 전에» 그 안에서 다른 화면이 쓰는 것이 있는지 «세십시오».
   rnd_console/trend_workbench.js 등이 밖에서 임포트되면 그건 «옮길 것»이지 지울 것이 아닙니다
   (오늘 서버 쪽에서 같은 부류를 세다가 「죽은 47개 중 38이 손댈 게 아니었다」가 나왔습니다)
⛔ dist 는 «건드리지 마십시오». 소스만. 빌드는 총괄이 합니다
📎 곁가지: rnd_console 넷이 「고정 마킹 키」를 쓰고 있어 은퇴 목록에 이미 올라 있습니다.
   이 공사가 그중 클라 몫을 «먼저» 닫습니다
```

## 공사 2 — 부품 «시각 명세»를 목업에서 떠 주십시오
골격이 통과하면 부품 일곱을 «병렬»로 붙입니다. 그때 각 라운드가 명세를 기다리면 안 됩니다.
```
재료   spotfire-style-ontology-rnd-view.zip (repo 루트) 의 «2a»
       패널 8개: 머리 요약 · 제어 · 메인 트렌드 · 구성 · 맵 · 마킹 후보 트렌드 · 후보 리스트 · 순위 리스트
낼 것   부품 «7종»마다 한 절씩 (인스턴스 9 — 맵과 트렌드가 «두 번씩»)
        · 그 부품이 그리는 것 · 상태(비었을 때·마킹 없을 때·데이터 없을 때)
        · tokens.css 의 «어느 토큰»을 쓰는지 (색을 하드코딩하지 마십시오)
        · 최소 크기 / 늘어날 때 무엇이 늘어나는지  <- 리사이즈 계약에 필요합니다
```
🔴 **명세이지 구현이 아닙니다.** 코드 쓰지 마십시오.
🔴 그리고 «클라는 후보의 종류를 세지 않습니다» — API 가 `{kind, label, sublabel, detail, color_role}`   🔴 «틀림 — 정정됨(맨 위)»
   을 줍니다. 명세에 종류 목록을 «박지 마십시오». 색도 서버가 역할로 줍니다.

## ⛔ 울타리
```
골격 금지        마킹 저장소 · 부품 계약 · 그리드 셸은 구현자 몫입니다. 손대지 마십시오
dist 금지        소스만. 빌드는 총괄
main 트리 금지    당신은 design 워크트리에서 일합니다
서버 금지        필요해 보이면 «짓지 말고 보고»하십시오
```

## 지금 당신 대기열
```
1  폐기 화면 걷어내기 (세고 나서)
2  부품 7종 시각 명세
3  그다음은 총괄이 부품별 구현 라운드로 냅니다
```
보고는 `task/design_session_report.md`, 판정 요청은 그 파일 «맨 위»에.

---

# 🎨 새 지시 — 「이 웨이퍼 왜 이런가」 화면 (총괄 13:5x)

## 도착지 — 여기부터 읽고, 라운드마다 여기에 대조하십시오
> R&D 엔지니어가 **자기가 이미 보고 있는 자리**에서 웨이퍼 하나를 지목해 「왜 이런가」를 묻고,
> **원인 후보 순위**와 **거기까지 온 경로**를 받는다. 그리고 **어느 후보에 실측이 붙어 있고
> 어느 것이 이름뿐인지 한눈에 갈린다.**

## 왜 지금 이게 1순위인가
소유자 상설 (2026-08-23): 「반도체 rnd 관점에서 어떻게 써먹을지 항상 본질을 기억할것」.
**엔진은 «돕니다».** 오늘 착지했고 제가 실측했습니다. 안 쓰이는 이유는 **화면이 없어서**입니다.
엔지니어가 curl 로 JSON 을 받지는 않습니다.

## 서버는 «안 건드립니다» — API 가 이미 있고 제가 실측했습니다
```
GET /api/ledger/subgraph?id=<노드id>&collect=quantity
```
실측 (씨앗 `SYN-BW-103-11` · 2026-08-23 13:3x · 서버 PID 48960):
```
HTTP 200 · state ranked · 후보 «25» · rank 1~9 · complete true · contrast unexamined
근거 경로   25/25 «전 순위»에 붙어 있음 · 홉 3~6
label 모양  「물리량 · 모델명」      예: bond_pressure · void_formation
동률        rank 2 에 «둘» (tied)
```
📎 **샘플 응답을 남겼습니다: `task/sample_walk_response.json`** — 실제 응답에서 3행 추린 것입니다.
lorem 으로 시안 만들지 마시고 이걸로 하십시오.

🔴 **모델명이 label 에 붙는 것은 «의도»입니다.** 같은 물리량이 두 모델에 나오고, 합치면
아무도 하지 않은 세 번째 주장이 생깁니다. **두 줄로 보여야 합니다** (위 rank 4 에 실제로 그렇습니다).

---

## 🔴 이 화면의 «성패»를 가르는 요구 — 하나뿐입니다
```
후보 25개 중 «실측이 붙은» 것   «3»개    bond_temp · bond_pressure(모델 둘)   🔴 «이 3 은 선언 바인딩 세기였습니다. 라이브 실측은 4 — 맨 위 정정»
나머지                          «22»개   선언에서만 나온 «이름»
```
**갈리는 법 (응답에서 그려집니다 — 제가 확인했습니다):**
```
실측 있음   근거 홉에 node_kind 가 "value" 또는 "claim" 인 것이 «있다»
            그 홉의 ref 가 실물을 가리킨다   예: recipe_book:SYN-RCP-BOND@6:SYN
이름뿐      홉이 전부 node_kind "quantity" 이고 ref 가 전부 mechanism_models.json
```
🔴 **이 둘이 같아 보이면 엔지니어가 `adhesive_residue` 를 쫓아갑니다 — 가 보면 볼 게 없습니다.**
그러면 이 화면은 «한 번 쓰이고 버려집니다». 다른 어떤 것보다 이게 먼저입니다.

---

## 🔴 오해되면 안 되는 상태 «다섯» — 전부 «없음»인데 «고장»이 아닙니다
```
contrast: unexamined   「안 쟀다」 ≠ 「깨끗했다」    ← 지금 «항상» 이 값입니다
                       (원장에 「검사했는데 결함 0」인 웨이퍼가 «0장»)
complete: false        예산에 끊김 -> 끊긴 후보는 «부재»가 아니라 «미검사»
state: empty           걷기가 물리량에 «안 닿음» ≠ 「원인 없음」
tied: true             동률. 순서가 «있는 척» 하면 안 됩니다
incomparable: true     우열이 아니라 «종류가 다름» (한쪽은 표시에서, 한쪽은 대조군에서 앞섬)
```
🔴 **이 저장소는 이 부류로 하루에 여덟 번 거절당한 적이 있습니다** — 「없는 것을 고장이나
진행 중으로 읽는 것」. 다섯이 **서로 다른 말**로 보여야 하고, 어느 것도 «에러처럼» 보이면 안 됩니다.

## 순위는 «판정이 아닙니다»
도달량(숫자)은 응답에 **안 나갑니다** — 오늘 제가 뺐습니다. 나가는 것은 순위·최상위 집합·경로뿐이고
**판정은 소유자가 합니다.** 「원인은 X 입니다」로 쓰지 마십시오. 화면이 결론을 내면 안 됩니다.

---

## ⛔ 만들지 말 것
```
새 화면·새 모드·새 모달       소유자 상설. 엔지니어가 «이미 있는 자리»에서 들어갑니다
                             어디서 들어갈지는 «당신 판단»이고, 그 판단이 이 라운드의 절반입니다
대조군 씨앗 고르는 UI          지금 원장에 진짜 대조군이 «0장» -> 소비자 0인 컨트롤입니다
                             상태만 정직하게 보이고, 고르는 UI 는 데이터가 생기는 날 만듭니다
collect=entity (혈통) 화면     같은 API 의 다른 «값»이지만 «이번 아닙니다». 하나를 끝까지
서버 변경 요청                필요 없습니다. 필요해 보이면 «짓지 말고 저한테 말하십시오»
25개 전부 경로 펼치기          홉이 3~6 입니다. 기본은 접혀 있어야 합니다
```

## 산출물
시안(HTML/CSS) **+ 「어디서 들어가는가」에 대한 당신 판단**. 로직은 이 라운드가 아닙니다.
막히거나 위 울타리와 부딪히면 **짓지 말고** `task/design_session_report.md` 에 적으십시오.

---


## 🔴 판정 ① Copy Header — **둘 다 두십시오. 새 기계장치는 만들지 마십시오**

당신 실측이 결정적이었습니다. 다만 대가가 «생각보다 큽니다» — 총괄이 재 봤습니다:
```
clipboard.js:256 · :655   이 토글은 «메인 그리드»의 복사를 결정합니다. 참조 패널 전용이 아닙니다
그러므로   규칙 없는 테이블(대부분)에서 «전역 설정이 손에 안 닿습니다».
           값은 계속 적용되는데 바꿀 수가 없습니다 — 그건 「기능이 숨은 것」입니다
```
🔴 **그런데 이 저장소엔 이미 «같은 문제의 답»이 있습니다.**
```
map_editor.js:480 · :700-712
   맵 화면은 «자기 체크박스»(map-copy-header-toggle)를 따로 두고
   같은 localStorage['copyHeader'] 에 붙였습니다.
   그 주석이 판정을 들고 있습니다 — 「새 저장 기계장치를 만들지 않는다 …
   이건 그 프리미티브의 맵 화면 사본이다」
```
```
✔  하단 단축키 줄의 토글 «유지» (목업 그대로)
✔  Options 드롭다운의 토글 «복구» — 그리드 화면 어디서나 닿아야 합니다
✔  둘 다 localStorage['copyHeader'] 에 붙입니다. map_editor.js:706-712 «그대로» 베끼십시오
✖  새 저장 키 · 새 동기화 장치 · 이벤트 브리지 금지
⚠️ id 는 하나만 가질 수 있으니 새 쪽에 다른 id 를 주고, dom.js 게터를 «둘»로 둡니다
```
**「두 컨트롤 = 두 어휘」가 아닙니다.** 값이 하나이고 «보는 자리»가 둘인 것이며, 그건 이미 이 저장소의 관례입니다.

---

## 🔴 판정 ② 열 폭 — **당신 제안 그대로 채택합니다. 목업 폭은 «최솟값»입니다**
```
✔  규칙 하나:  폭 = max(목업 폭, 라벨이 안 잘리는 폭)
✖  낱개 특례(dt_slot 만 넓히기) 금지 — 그건 다음 컬럼이 오는 날 또 틀립니다
```
**사유:** 소유자 상설이 「**작은 글씨는 없느니만 못하다 · 가독성이 기능**」입니다.
잘린 컬럼 이름은 «좁은 것»이 아니라 «없는 것»입니다. 그리고 목업은 자기 라벨(`Slot`)로 폭을 쟀고
소유자는 이름을 실제 스키마(`dt_slot`)로 쓰라 하셨으니, **폭이 이름에 양보하는 것**이 두 지시를
동시에 지키는 유일한 방향입니다.
```
확인   적용 후 «잘리는 헤더 0개»를 실측으로 적으십시오 (오늘 셋 중 둘이 잘렸습니다)
```

---

## ③ LOT_EVENT 근거 표 — 목업의 「아래 쌓기」 vs 지금의 「탭」
이건 **시각 결정**이라 소유자 몫입니다. 지시하시면 그대로 하시고, 없으면 지금 형태를 유지하십시오.

---

# 🌙 야간 지시 — 소유자 취침 (총괄 00:4x). **승인이 필요한 것은 «시작하지 마십시오»**

> 소유자가 주무십니다. 당신의 절차는 「목업 → 소유자 승인 → 구현」인데,
> **오늘 밤은 그 2단계가 없습니다.**

## 그러니 밤에 할 것 / 안 할 것
```
✔  이미 승인된 것의 «마무리»     Phase 4.1~4.3 (하네스 · 폴백 채점 · 문서)
✔  ⑨ 불가 경로 «보행»           아래 §를 보십시오 — 총괄이 막힌 걸 풀었습니다
✔  당신이 스스로 잡은 결함의 수리  근거를 보고에 적으면 됩니다
✖  «새 시각 결정» 전부           목업 새로 만들기 · 색/간격/배치의 새 판단 · 「이왕 하는 김에」
    -> 만들어만 두고 «착지하지 마십시오». 아침에 소유자가 보십니다
```

## ✅ ⑨ 풀렸습니다 — 이제 «불가»를 걸을 수 있습니다
소유자 승인을 받아 `dt_job_attribution` 을 카탈로그에 등록했습니다.
(픽스처 편의가 아니라, «선언 둘이 기대던 테이블이 라이브 카탈로그에서 빠져 있던» 것이었습니다.)
```
dt_inventory   virtual_columns = ['dt_lot_confirmed', 'dt_slot_confirmed']   ← 실측, 서버 재기동 완료
               참조 패널도 «같은 테이블»에 있습니다 -> 두 픽스처의 «분리»가 없어졌습니다
가상조인       accepted 2 · refused 0
```
🔴 **그러니 붙여넣기 대상 범위가 가상 컬럼을 건드리는 «불가» 판정을 실제로 만들어 보실 수 있습니다.**
「NOT WALKED」로 적으라던 앞 지시는 «취소»합니다 — 걸어 보고, 서버가 실제로 거절하는 것까지 보십시오.
안 되면 «안 된 그대로» 적으십시오. 되는 척은 하지 마십시오.

## 야간 울타리
```
✖  client2/src/ontology_explorer*.{js,css}   구현자가 «지금» 씁니다.
    그리고 아침에 소유자가 그 화면으로 원장 셋업을 하십니다 — 오늘 밤 가장 건드리면 안 되는 파일입니다
✖  메인 트리에서 작업·빌드   당신은 워크트리입니다
✖  dist 굽기 · 커밋          총괄 몫입니다. 소스만 올리십시오
✖  8080 재기동               총괄이 관리합니다
✔  자주 git fetch origin && git merge origin/main   총괄이 밤새 병합·빌드합니다
```

## 총괄은 깨어 있습니다
```
push 하면 총괄이 감시로 받아서 «병합 + dist 굽기»까지 합니다. 판정 필요하면 보고에 적으십시오 —
기다리지 말고 다음 것을 하십시오.
```

---

# ✅ 판정 ⑫ — **㉮ 지금 고칩니다.** 소유자가 그 규칙을 «왜» 쓰셨는지가 답입니다

자기 커밋을 새 기준으로 다시 재고 «후하게 적혀 있었다»고 먼저 말한 것, 그게 맞는 순서입니다.

## 판정
```
㉮  지금 고친다     <- 채택
㉯  다음 라운드로   <- 기각
```
**사유:** 소유자가 Phase 0 을 «새로 붙이신» 것은 지금 화면에 그 문제가 보이기 때문입니다.
병합돼 있다는 것은 미루는 이유가 아니라 **서두르는 이유**입니다 — 갈라진 배너가 하루 더 살면
그게 다음 사람의 «본»이 됩니다.
```
✔  .grid-filter-bar · .reference-alignment  ->  .tx-filter-banner 의 «마크업·클래스» 복제,
   색만 변종.  banner-icon · banner-text · clear-banner-btn 구조 그대로
✔  .filter-chip-x  ->  .clear-banner-btn  (당신이 스스로 짚은 것, 맞습니다)
✔  .72rem -> .76rem 은 이미 고치셨다니 그대로
```

## 🔴 조건 하나 — «색만» 달라야 합니다
```
멈춤 조건   구조를 갈아끼웠는데 «레이아웃이 눈에 띄게» 달라지면 착지하지 말고 보고하십시오.
            소유자가 보고 계신 화면입니다. 규칙을 지키려다 화면을 바꾸면 그건 다른 라운드입니다
확인        전/후 스크린샷 한 쌍.  같은 자리·같은 높이·같은 정렬이어야 합니다
```

## 유지 — 당신 판단이 맞습니다
```
.reference-view-fill · .reference-view-gutter · .filter-chip
   상태가 아니라 «역할» 표시이고, 칩은 재사용할 선행 클래스가 «없었습니다».
   Phase 0 이 금지한 것은 「참조 그리드 셀의 새 «상태» 클래스」입니다. 그대로 두십시오
```

## 그리고
```
소유자가 당신에게 «직접» 지시하시는 것은 프로토콜대로 정상입니다. 총괄이 다시 판정하지 않습니다.
받으면 그대로 하시고, 총괄 지시와 «충돌할 때만» 물어보십시오 — 그때는 소유자가 이깁니다
dist    당신은 짓지 않습니다. 소스만 착지시키면 총괄이 한 번 굽습니다 (방금 그렇게 했습니다)
```

---

# ⚠️ ⑨ 정정 — 규칙은 통과했는데 «화면에는 안 뜹니다». 원인 찾았고, 지금은 «미보행»으로 둡니다

총괄이 병합·빌드·재기동까지 마치고 실측했습니다:
```
verification_report      accepted 2 · refused 0        <- 규칙 자체는 «맞습니다»
GET /tables/dt_inventory/schema
     virtual_columns     []                            <- 그런데 «비어 있습니다»
     (dt_log 쪽은 여섯 개 다 정상)
원인   dt_job_attribution 이 «table_config.json 에 없습니다» (선언 테이블 26개 중 없음)
       유일성은 통과했지만 스키마 층이 «카탈로그가 모르는 테이블»의 컬럼을 안 내보냅니다
```
🔴 **제가 「이제 걸을 수 있습니다」라고 적은 것은 «틀렸습니다».** 정정합니다.

⚠️ **그리고 그 선언은 «치웠습니다».** 서버가 `/tables/dt_inventory/schema` 를 «부를 때마다»
`declaration rejected: right_table 'dt_job_attribution' is not registered in table_config.json`
경고를 찍고 있었습니다 — 아무 일도 안 하면서 로그만 채우는 선언은 남겨 두지 않습니다.
지금 가상조인은 `dt_log_frame_from_inventory` «하나»이고 accepted 1 · refused 0 입니다.

## 지금 판정 — «불가»는 미보행으로 남깁니다
```
✔  최종 보고에 「불가 경로 NOT WALKED — 픽스처가 만들어지지 않아서」라고 «그대로» 적으십시오
✔  당신이 제안한 그 문장이 맞습니다. 검증된 것처럼 읽히게 두지 마십시오
```
길은 둘인데 지금은 «둘 다 안 갑니다»:
```
갑  dt_job_attribution 을 table_config 에 선언   -> 앱 테이블 목록에 «새 테이블»이 뜬다.  지시 밖
을  dt_log 에 enrichment 규칙을 하나 더          -> 참조 패널이 dt_log 에 생기고, 거기엔 가상 컬럼이 «이미» 있다
```
**「을」이 작습니다.** 다만 지금은 소유자 우선순위가 «원장 셋업»이라 총괄이 손을 뗍니다.
원장 라운드가 닫히면 「을」을 만들어 드리고, 그때 걸으십시오.

## 그동안
```
4.1~4.3 계속.  Phase 1–2 는 «병합 끝났습니다» — main 이 origin/design 을 «전부» 담고 있고
                dist 도 총괄이 한 번 구웠습니다 (게이트 초록).  8080 이 이제 당신 코드를 서빙합니다
⑪  당신 확인과 총괄 측정이 «일치»했습니다.  메인 트리 4파일 정리 완료.
    유일하게 design 에 없던 건 «주석 4줄»(floatingFilter 설명)이었고 따로 보관해 뒀습니다
```

---

# 🔴 판정 넷 — 열려 있던 것 전부 (총괄 23:4x)

## ⑦ 깔때기 버튼 — **그대로 두십시오.** 지시서에서 그 줄을 «뺍니다»
당신 근거가 맞습니다: 조인 컬럼의 «자기 툴팁»이 「필터를 Equals 로 두고」라고 시키는데
그 Equals 로 가는 길이 깔때기 하나뿐입니다. 없애면 **화면이 시킨 일을 화면에서 못 하게** 됩니다.
```
✔  inert 한 줄은 «지우십시오» (남겨두면 다음 사람이 「왜 안 먹지」로 하루를 씁니다)
✖  operator 선택을 다른 데로 옮기는 것 — 새 컨트롤이고, 지시 밖입니다
```

## ⑨ 가상 컬럼 — **총괄이 만들었습니다.** 이제 «불가» 를 걸을 수 있습니다
```
dt_inventory_confirmed_from_attribution      (server/config/virtual_join_rules.json)
   left  dt_inventory     right  dt_job_attribution     on dt_job
   expose  dt_lot_confirmed · dt_slot_confirmed
   유일성  uq_vjoin_dt_job_attribution_dt_job   (252/252 distinct)
   검증    accepted 2 · refused 0 · invalid 0    <- verification_report 로 실측
   붙는 행  dt_inventory 401 중 251
```
🔴 **이제 참조 패널이 있는 바로 그 테이블에 가상 컬럼이 있습니다.** 두 픽스처의 분리가 없어졌습니다.
⚠️ **서버가 config 를 다시 읽어야 화면에 뜹니다. 그 리로드는 «총괄이» 합니다** —
소유자가 지금 그 서버로 원장 설정을 쓰고 계셔서 손이 빌 때 누릅니다. 뜬 뒤에 걸으십시오.

## ⑩ Phase 1–2 병합 — **지금은 «보류»입니다. 사유를 적습니다**
```
막는 것   8080 이 «메인 트리»를 서빙하고, 소유자가 «지금» 그 화면으로 원장 설정을 쓰고 계십니다
          병합은 grid.js · dom.js · index.html 을 바꾸고 dist 재빌드를 부릅니다
언제      원장 셋업 라운드가 착지하면 «총괄이» 병합 + dist 한 번. 그날 안입니다
그동안    당신 워크트리 5173 에서 계속 걸으십시오 — 그건 이미 되고 있습니다
```
**당신이 잘못한 게 아니라 «타이밍»입니다.** 3.x 를 계속 쌓으셔도 됩니다.

## ⑪ 메인 트리에 남은 `grid.js` — **한 줄만 확인해 주십시오**
총괄이 쟀습니다:
```
메인 트리 미착지 diff   228줄
origin/design 의 grid.js  filterChipText · offscreenColumnCount «둘 다 있음» (4곳)
```
👉 **「메인 트리의 그 변경은 design 브랜치에 이미 들어간 것의 사본이다」가 맞습니까?**
```
맞다   -> 총괄이 «그 한 경로만» 되돌립니다. 빌드 게이트가 저절로 초록이 됩니다
아니다 -> 어느 부분이 design 에 «없는지» 적어 주십시오. 그것만 살립니다
```
🔴 당신이 되돌리지 마십시오. 공유 트리라 총괄이 합니다.

## 다음
```
4.1~4.3  진행하십시오 (dist 는 짓지 않는다 — 당신 판단이 맞습니다)
4.4      빌드·dist 커밋은 «총괄 몫». 지시서의 그 항목은 당신에게서 «뺍니다»
```

---

# 🔴 먼저 — **총괄이 당신에게 «틀린 것»을 보냈습니다. 정정합니다**

제가 메시지로 「당신이 `npm run build` 를 돌려 dist 를 다시 구웠다」고 보냈습니다.
**재 보니 아닙니다.** 당신 보고가 맞습니다.

```
20:55:12   총괄이 «서버만» 재기동 (빌드 안 함)
20:55:52   dist «전체»가 다시 구워짐   <- 총괄 것이 아니다
검증       index.html 이 가리키는 main-DlUVbgcq.js · style-BJgac6KN.css 안에
           grid-filter-bar / offscreen-cols / history-tabs--wide 가 «각각 1건»
```
🔴 **즉 당신이 실어 보낸 게 아니라, 남의 빌드가 당신의 미완성 작업을 «싣고 갔습니다».**
제가 삭제된 dist 자산만 보고 「이 세션이 구웠다」로 «추측»했습니다.
**mtime 으로 재서 반박한 것이 맞고, 아무것도 되돌리지 않은 것도 맞습니다.**

⚠️ 그리고 이건 오늘 제가 «이름을 추측해서» 틀린 두 번째입니다(앞엔 `die_transfer`/`die-transfer`).
앞으로 당신 것/남의 것을 지목할 땐 **재고 나서** 적겠습니다.

---

# ✅ 판정 ① — `dist` 는 «총괄만» 만진다. 당신 작업은 «되돌리지 않는다**

```
✔  레인은 «소스만» 커밋한다.  dist 는 커밋하지 않는다
✔  재빌드는 «모든 라운드가 착지한 뒤 총괄이 한 번만»
🔴 지금 서빙 중인 번들은 «검수되지 않은 것»이다 — 세 세션의 미착지 작업이 섞여 있다
   -> 화면에서 본 것을 「착지한 상태」로 읽지 말 것. 당신도, 다른 레인도
```

**당신 작업을 되돌리지 않습니다.** 「끝내고 검증하겠다」는 당신 선호가 맞습니다.
다만 **그 검증은 «워크트리에서»** 합니다:

```
총괄이 워크트리를 «팠습니다»:   C:/Users/kk980/Developments/assyManager-design   (브랜치 design)
```
```
1  거기로 옮겨서 일하십시오.  main 트리에는 더 쓰지 마십시오
2  npm install 이 «필요합니다» — node_modules 는 git 에 없습니다
      cd C:/Users/kk980/Developments/assyManager-design/client2 && npm install   (백그라운드로)
3  main 트리에 남긴 당신 변경(grid.js · dom.js · style.css · index.html)은
      🔴 «되돌리지 말고» 그대로 두십시오.  총괄이 워크트리로 옮겨 드립니다
      옮긴 뒤 main 트리 쪽을 정리하는 것도 총괄이 합니다
```
⚠️ **`ontology_explorer*` 는 당신 것이 아닙니다.** 구현자가 지금 씁니다. 워크트리에서도 손대지 마십시오.

---

# ✅ 판정 ② — 소유자의 `MIGRATION_2b.md` 는 «유효한 지시»입니다. 계속하십시오

소유자는 양쪽 세션에 **직접 지시할 수 있습니다.** 그것이 총괄을 우회한 게 아니라
**총괄이 몰랐던 것**이고, 알려 준 것이 맞는 처신입니다.

```
✔  Phase 1–2 계속.  단, «워크트리에서»
✔  그 지시서를 design 브랜치에 «커밋»해 주십시오 — 총괄이 읽을 수 있게
      (소유자의 Claude Design 프로젝트에서 가져온 것이라 저장소에 없습니다)
⚠️  Phase 를 넘어갈 때마다 보고 파일에 «한 줄». 총괄이 순서를 조정할 수 있어야 합니다
```

---

# ✅ 판정 ③ — **`candidate_for` 입니다. `fill_targets` 만들지 마십시오**

당신 분석이 맞고, 결론도 맞습니다.

```
fill_targets      서버 두 곳 + 소유자의 gitignore 설정 변경이 필요
                  -> 이주 지시서 «자신의 전제»(「서버 계약 변경 0」)를 깬다
candidate_for     이미 있다 · 이미 선언돼 있다 · 이미 정규화된다 · 이미 클라로 나간다
                  {target_field: view_result_column} — «어느 참조 컬럼이 어느 대상을 채우나»
                  fill_targets 보다 «더 많이» 말하고, 추측이 아니라 «선언»이다
```
🔴 **이건 이 프로젝트의 상설 규칙 그대로입니다: 「만들기 전에 기존 시스템에 구조적으로
같은 연산이 있는지 먼저 본다」.** 당신이 그것을 «코드를 쓰기 전에» 했습니다.

⚠️ 다만 하나 재고 진행하십시오: **`candidate_for` 의 키 순서에 기대는 것이 안전한가.**
당신이 「선언 순서」라고 적었는데, 그것이 **JSON 로더를 거쳐도 보존되는지**
(파이썬 dict 는 보존하지만 정규화 과정에서 정렬될 수 있습니다) 한 번 실측하십시오.
순서가 뜻을 가지면 그건 **깨져도 조용한** 종류의 의존입니다.

---

# ▶▶ 지금 할 것 — 순서대로

```
1  워크트리로 이동 + npm install (백그라운드)
2  MIGRATION_2b.md 를 design 브랜치에 커밋
3  Phase 1–2 를 워크트리에서 계속.  Phase 3 는 candidate_for 로
4  Phase 경계마다 보고 파일에 한 줄 + push
```

## ⛔ 울타리
```
✖  main 트리에 쓰기                     워크트리가 있습니다
✖  npm run build 를 main 트리에서        남의 미검수 소스를 굽습니다
✖  checkout -- · stash · reset · clean   트리 전체를 건드립니다 (오늘 두 번 사고)
✖  server/ 수정                          로직은 당신 것이 아닙니다. 필요하면 «말하고 기다립니다»
✖  ontology_explorer*                    구현자가 씁니다
```

## 🔴 그리고 오늘 이 프로젝트가 비싸게 배운 것 둘
```
「없다」고 적기 전에 «다 펼쳐 보고» 적는다        접힌 컨트롤은 없는 컨트롤이 아니다
파일을 «프로그램으로» 자르면 «바뀐 줄 수»를 찍고 예상과 대조한다
   총괄이 78줄 교체를 하려다 643줄을 지웠고, git status 는 「M 하나」로 조용했다
```

---

# 🔴 정정 — **총괄 판정 ③ 이 «틀렸습니다». 당신이 스스로 뒤집은 것이 맞습니다** (21:3x)

제가 `candidate_for` 로 가라고 판정했고, 당신이 **라이브 선언을 재서** 그것을 철회했습니다.
```
dt_job_lot_slot_attribution
   view[3]  candidate_for = {'dt_lot_confirmed':  'dt_lot'}
   view[4]  candidate_for = {'dt_slot_confirmed': 'dt_slot'}
-> 두 대상이 «서로 다른 뷰»에 하나씩. 「인접·순서·한 그리드」를 표현할 수 없다
```
🔴 **제가 «정규화기»를 읽은 당신 분석 위에 판정했고, 저도 라이브를 안 봤습니다.**
심지어 저는 「키 «순서»에 기대는 게 안전한가」를 조심하라고 붙였는데,
**실제 문제는 더 앞이었습니다 — 크기 1짜리 dict 에는 순서가 «없습니다».**

**오늘 제가 같은 실수를 세 번째 했습니다** — 검증기·라이브 선언에 «먹여 보지 않고» 형태를 골랐습니다.
당신이 「코드를 쓰기 전에」 잡아서 잃은 것이 0입니다.

## 그래서 Phase 3.1 은 «판정 대기»로 되돌립니다
```
후보 다시    ㉮ fill_targets + 서버 통과로 (서버 세션 라운드가 필요 — 소유자 판정 사안)
             ㉯ Phase 3.1 을 «다른 설계»로
```
**당신이 세 번째 안을 지금 내지 않은 것도 맞습니다.** 총괄이 소유자에게 올립니다.

## ④ Phase 3 가 이 환경에서 «걸 화면이 없다» — 받습니다
```
virtual_join_rules   활성 규칙 «없음» (둘 다 _retired_ 접두)
enrichment_rules     reference_views 를 가진 유일한 규칙의 derived_table 이
                     table_config 에 «없어서» 그리드 드롭박스에 안 뜬다 (26표 중 부재, 실측)
```
「실패가 아니라 한계」로 적은 것이 정확합니다. **Phase 3 은 여기서 못 겁니다.**
그 사실 자체가 값이고, 그걸 재서 올린 것이 이 라운드의 산출입니다.

# ✅ 검수 — Phase 1–2 는 «받습니다»
```
파일 8개 전부 client2.  server/ 0.  ontology_explorer* 0   <- 겹침 없음
하네스   virtual_column_render_harness.mjs 2줄 = «앵커 갱신»뿐
         변이는 여전히 1회 적용되고 같은 결함을 지킵니다. 낮춘 것이 아닙니다
Chrome   당신이 «직접» 걸었습니다
```
🔴 **다만 `main` 에 «아직 병합하지 않습니다».** 소유자가 화면으로 먼저 보셔야 하고,
병합·재빌드는 구현자 가족이 끝난 뒤 총괄이 «한 번에» 합니다. 그때 알려 드리겠습니다.

## ▶ 지금 할 것
```
대기.  Phase 3 은 판정 대기이고 Phase 1–2 는 병합 대기입니다
필요하면  `design` 브랜치에서 다듬되, 새 Phase 를 «시작하지 마십시오»
```

---

# ✅ Phase 3 의 «걸 화면»을 만들어 드렸습니다 (소유자 지시, 21:4x)

당신이 「Phase 3 는 이 환경에서 걸 화면이 없다」를 **실측으로** 올렸고, 소유자가
「클라용 enrich 는 몇 개 만들자, 가상조인이랑 시나리오 짜서」로 답했습니다. 총괄이 만들었습니다.

## 이제 «있는» 것 — 전부 8080 API 에 실려 있습니다

### ① 참조 뷰가 달린 enrichment 규칙 «둘» (전에는 0)
```
dt_frame_confrimation    source dt_log        derived dt_inventory   views 3
core_frame_review        source dt_core_view  derived dt_inventory   views 3
```
🔴 **`dt_inventory` 는 table_config 에 «등록돼 있습니다»** — 그리드 표 드롭박스에 «뜹니다».
당신이 「선택 불가라 못 연다」고 재서 올린 그 벽이 이것으로 사라집니다.

각 규칙의 참조 뷰 셋:
```
1  이 job 의 원본 행            dt_log / dt_core_view 에서 그 job 의 행 전부
2  관측된 좌표 범위             x_min·x_max·y_min·y_max·cells   <- 프레임이 «덮어야» 하는 것
3  같은 장비의 다른 job / 쓴 core 웨이퍼
```
⚠️ 뜻을 «지어내지 않았습니다**. `dt_frame`·`core_frame` 은 평범한 값이 아니라
`{"frame_confirmed_from": …}` JSON 이력이라, 뷰는 「이 결정의 원본 행과 그 좌표 범위」까지만 보여줍니다.
그 이상은 좌표 도메인 판정이라 총괄이 만들지 않았습니다.

### ② 가상조인 «하나» (전에는 활성 0)
```
dt_log_frame_from_inventory
   dt_log  ⋈  dt_inventory   on dt_job   (cardinality one)
   expose  dt_x_base · dt_x_sign · dt_x_offset · dt_y_base · dt_y_sign · dt_y_offset
```
**`dt_log` 그리드에 좌표 프레임 6열이 «가상 컬럼»으로 붙습니다.** 126/401 job 에 값이 있습니다
(나머지는 위 enrichment 로 확정되면서 찹니다 — 두 기능이 «서로를 먹입니다»).

## 총괄이 이걸 만들려고 «고친» 것 — 알아 두십시오
```
table_config.dt_inventory   column_types 에 dt_job 추가        (DB 엔 있는데 선언에 없었다)
                            composite_key_source: ["dt_job"] 추가 (upsert 키 계약)
DB                          CREATE UNIQUE INDEX uq_dt_inventory_dt_job  (401/401 distinct 확인 후)
```
🔴 **`server/config/*` 는 gitignore 입니다 — «당신 워크트리에는 없습니다».**
당신이 워크트리에서 서버를 띄우면 이 규칙들이 «없습니다». 8080(메인 트리)으로 보십시오.

## ⚠️ 아직 «안 되는» 것 — 정직하게
```
dt_job_lot_slot_attribution   여전히 skip.  derived_table 'dt_job_attribution' 이 «미등록»
   그 규칙의 참조 뷰 5개가 제일 풍부한데 «못 씁니다»
   되살리려면 target_fields(dt_lot_confirmed·dt_slot_confirmed)를 dt_inventory 컬럼으로 바꿔야 하고
   auto_confirm: true 라 «400행에 자동으로 씁니다» -> 소유자 판정 사안. 총괄이 손대지 않았습니다
```

## ▶ 그러니 지금 할 수 있는 것
```
1  git fetch origin && git merge origin/main   (이 지시를 받는 법)
2  8080 에서 dt_inventory 를 표로 골라 참조 패널을 «연다»  -> Phase 3 를 실제로 걸어 본다
3  dt_log 그리드에서 가상 컬럼 6열이 뜨는지 «본다»
4  Phase 3.1 은 여전히 «판정 대기» — fill_targets 냐 다른 설계냐. 시작하지 마십시오
```
**걸어 보고 «무엇이 안 되는지»를 재서 올려 주십시오.** 그게 다음 라운드의 재료입니다.

---

# ✅ 판정 ⑤⑥ — 둘 다 «처리했습니다». Phase 3.1 착수하십시오 (22:0x)

> 소유자: 「알아서 써, 적당한 시나리오로」 — 총괄이 선언을 썼습니다.

## ⑥ 🔴 **제 잘못이었습니다. 고쳤습니다**
```
전   dt_inventory.display_columns = [dt_job_id, dt_eqp, dt_lot, ...]
     dt_job 은 401/401 채워져 있는데 «그리드에 없다» -> 앞 네 칸이 전부 빈 401행
후   display_columns[0] = "dt_job"     정체를 «맨 앞»에
```
`column_types` 에만 넣고 `display_columns` 에는 「최소 수정」으로 «일부러» 안 넣었습니다.
**그 최소가 틀렸습니다** — 행을 구별할 수단을 안 준 것이고, 당신이 재서 잡았습니다.
`dt_job_id` 가 401/401 비어 있다는 것도 그대로 보입니다. 그건 별개 문제로 남깁니다.

## ⑤ **`candidate_for` 로 갑니다. 반대 근거가 «제 뷰에는 성립하지 않습니다»**

당신이 철회한 이유는 정확했습니다 — «기존» 규칙은 두 대상이 서로 다른 뷰에 하나씩이라
순서를 말할 수 없었습니다. **그건 그 선언의 사실이지 `candidate_for` 의 한계가 아닙니다.**
새 뷰는 제가 모양을 정하므로 **두 대상을 한 뷰에 선언 순서로** 담았습니다.

```
새 규칙   dt_lot_slot_from_log
   source dt_log · derived dt_inventory · decision_key ["dt_job"]
   target_fields  ["dt_lot", "dt_slot"]          <- 순서가 곧 열 순서
   auto_confirm   false                           <- 사람이 확인한다
   view[0]  "관측된 dt_lot / dt_slot"
            candidate_for = {"dt_lot": "dt_lot", "dt_slot": "dt_slot"}
   view[1]  "이 job 의 원본 행 (근거)"
```
**총괄이 클라 투영까지 실측했습니다:**
```
to_public_rule(...)  ->  target_fields ['dt_lot','dt_slot']
                         view[0] candidate_for = {"dt_lot":"dt_lot","dt_slot":"dt_slot"}
                         view[1] EMPTY   (근거 뷰는 후보가 아니다 — 의도한 것)
[Enrichment] Synthesized 3 dedup chain rule(s)      <- 규칙 2 -> 3
```
🔴 **서버 코드 0줄입니다.** 이주 지시서의 전제(「서버 계약 변경 0」)가 지켜집니다.
`fill_targets` 는 «만들지 마십시오».

### 왜 이 시나리오인가 — 지어낸 게 아닙니다
```
dt_inventory.dt_lot / dt_slot   401 중 «400 이 빈칸»       <- 진짜 일거리
dt_log 는 같은 job 의 그 쌍을 «이미» 들고 있다              <- 표본 job: DT-2601-001 / 01 (72셀)
396 / 401 job 이 dt_log 에 행을 가진다
후보가 대개 «1개»  ->  선언 주석 그대로: 「후보가 1개면 판단이 아니라 확인」
```

## ▶ 이제 Phase 3.1 을 착수하십시오
```
읽을 것   view[0].candidate_for 의 «키 순서» = 열 순서
          (선언 순서가 곧 target_fields 순서이고, 둘이 같은 뷰에 있습니다)
⚠️ 다만  당신이 앞서 걱정한 「순서가 로더를 거쳐도 보존되나」는 «여전히 유효한 질문»입니다.
          위 실측에서 순서가 보존됐지만, 그건 «표본 하나»입니다.
          Phase 3.1 이 순서에 «기대기 전에» 그 가정을 코드 주석에 적어 두십시오 —
          깨져도 조용한 종류입니다
```

## ⚠️ 그리고 이건 «남깁니다»
```
dt_job_lot_slot_attribution   여전히 skip (미등록 표) · auto_confirm true · 400행 자동 기록
   -> 소유자 판정 사안. 새 규칙이 그 자리를 대신하므로 «급하지 않습니다»
```

---

# ✅ 「브랜치를 못 띄운다」 — **길이 이미 코드에 있습니다** (22:1x)

`0fcabbb9` 의 막힘(자기 브랜치를 서빙 못 해서 3.1 을 못 걷는다)에 대한 답입니다.
**병합도, 설정 복사도, 두 번째 서버도 필요 없습니다.**

```
client2/src/config.js:1-3
   const isDevServer = window.location.port === '5173';
   API_BASE = isDevServer ? 'http://127.0.0.1:8080' : window.location.origin;
   WS_URL   = 같은 판정
```
```
main.py:157-159   allow_origins = ["http://localhost:5173", "http://127.0.0.1:5173"]
총괄 실측         두 Origin 모두 access-control-allow-origin 이 «되돌아옵니다» (curl 로 확인)
```

## 그래서 이렇게 하십시오
```
cd C:/Users/kk980/Developments/assyManager-design/client2
npm install        (아직 안 했으면 — node_modules 는 git 에 없습니다)
npm run dev        -> :5173
```
```
클라 코드   당신 워크트리의 것          <- 당신이 고친 3.1 이 그대로 돕니다
API·설정    메인 트리의 8080            <- enrichment 규칙 3개 · 가상조인 · dt_job 표시열
```
🔴 **포트가 5173 이어야 합니다.** 5173 이 막혀 있으면 vite 가 5174 로 올라가고
`isDevServer` 가 «거짓»이 되어 API 를 자기 자신에게 겁니다. 그러면 전부 404 입니다.
`npm run dev -- --port 5173 --strictPort` 로 «강제»하고, 못 잡으면 그 사실을 보고하십시오.

⚠️ **여전히 `npm run build` 는 하지 마십시오** — dev 서버는 dist 를 안 만듭니다.
빌드는 착지 후 총괄이 메인 트리에서 한 번만 합니다.

## 그리고 방금 바뀐 것 둘 — dev 로 띄우면 바로 보입니다
```
dt_inventory 그리드   맨 앞이 dt_job (⑥ 수정)
enrichment 규칙       3개 — 새 dt_lot_slot_from_log 의 view[0] 에 candidate_for 가 «둘 다»
```

---

# ✅ 판정 ⑧ — **선언하는 규칙을 «먼저» 고르십시오. 당신 렌더러는 안 틀렸습니다** (22:3x)

```
dt_inventory 에 걸린 규칙 셋 · 패널은 «첫 번째»에 붙는다
   1 dt_frame_confrimation   declares [] [] []
   2 core_frame_review       declares [] [] []
   3 dt_lot_slot_from_log    declares ['dt_lot','dt_slot']    <- 도달 불가
```

🔴 **한 표에 규칙이 하나라는 가정을 «제가» 셋째 규칙으로 깼습니다.**
당신 fallback 은 옳습니다 — 아무것도 선언 안 하는 규칙은 fallback 해야 하고 그렇게 했습니다.

## 형태 — `syncReferenceViewRule` 의 «고르는 기준»만
```
지금   rules.find(r => r.derived_table === currentTable && views.length)
       -> 첫 번째. 「views 가 있다」가 유일한 기준
되어야  «선언하는» 규칙이 있으면 그것을 먼저.  없으면 지금처럼 첫 번째
       기준: view 중 하나라도 candidate_for 가 «비어 있지 않다»
```
**더 «구체적인» 규칙이 이깁니다** — 그게 이 화면이 하려는 일(사람이 값을 고른다)에 더 가깝고,
정보 표시만 하는 규칙은 그것이 없을 때의 답입니다.

⛔ **규칙 고르는 컨트롤을 «만들지 마십시오».** 새 영역·새 모드 금지가 상설이고,
지금 필요한 것은 「도달 가능하게」이지 「고를 수 있게」가 아닙니다.

## ⚠️ 그리고 이건 «임시방편»이라는 걸 적어 두십시오
```
한 표에 규칙이 N개일 때 패널이 «하나만» 보여준다는 것은 그대로 남습니다
지금은 선언하는 규칙이 하나뿐이라 이 기준이 유일해를 냅니다
둘이 되는 날 «다시 첫 번째»가 됩니다 — 그 날의 답은 총괄·소유자 몫입니다
```
🔴 **그 문장을 코드 주석에 적으십시오.** 오늘 하루가, 「그 날이 오기 전까지 조용한」 것들로
가득했습니다. 조건이 유일해를 내는 이유를 안 적으면 다음 사람은 그게 규칙인 줄 압니다.

## 확인
```
고른 뒤   dt_lot_slot_from_log 의 view[0] 이 열리고
          candidate_for 가 dt_lot -> dt_slot «순서로» 열을 만드는지 (Phase 3.1 의 ①②)
그리고    dt_frame_confrimation 이 «여전히 열리는지» — 다른 표에서, 혹은 그 규칙만 있을 때
          (fallback 을 부수지 않았는지)
```

---

# ▶▶ 지금 할 것 — 이주 지시서의 «남은 넷» (소유자 23:2x)

> **소유자: 「클라 세션 일 시켜」**

Phase 1 · 2 · 3.1 은 착지했고 당신이 «직접 걸어» 확인했습니다. 남은 것:
```
3.2  렌더러 교체
3.3  복사 — clipboard.js 를 import 하지 않는다
3.4  정렬 띠 (붙여넣기 전 «유일한» 관문)
4    게이트·문서
```

## 순서와 이유
```
3.2 -> 3.3 -> 3.4 -> 4      «지시서 순서 그대로». 3.4 는 3.3 이 없으면 지킬 것이 없습니다
```
🔴 **Phase 4 를 «빼지 마십시오».** 지시서가 스스로 그 이유를 적어 뒀습니다 —
「이 단계를 빼면 위 셋은 다음 라운드에 조용히 죽는다」.
**오늘 하루가 그 문장의 실례로 가득했습니다.** 게이트 없이 착지한 것은 다음 사람이 지웁니다.

## ⛔ 울타리 — 오늘 것 그대로 유효합니다
```
✖  main 트리에 쓰기 · main 에서 npm run build      워크트리에서 dev 로 걸으십시오
✖  server/ 수정                                     필요하면 «말하고 기다립니다»
✖  ontology_explorer*                               구현자 소관
✖  checkout -- · stash · reset · clean
🔴 새로 추가:  ledger_trace* · ledger_admin · ledger/config.py 는 «은퇴 대상»입니다
              그 화면(ledger.html · trace.html · rnd-console.html · ledger-graph.html)에
              새 일을 «얹지 마십시오». 버그를 봐도 고치지 말고 «적으십시오»
              (소유자 판정: 은퇴는 기반 원장 셋업이 끝나고 응용을 시작할 때)
```

## 3.3 에 대해 미리 한 마디
「`clipboard.js` 를 import 하지 않는다」는 **기존 것을 재사용하지 말라는 뜻이 아닙니다** —
지시서를 다시 읽고, 그 제약이 «무엇을 막으려는지»를 보고서에 한 줄로 적은 뒤 시작하십시오.
오늘 총괄이 제약의 «이유»를 안 읽고 형태를 골라 세 번 틀렸습니다.

## 보고
```
Phase 경계마다 한 줄 + push.  하나 = 커밋 하나
착지하면 «직접 걸어» 보고 — dev 서버(:5173, --strictPort)로
그리고 3.4 는 «막는 것»이 일이므로, «막히는 것»을 보여 주십시오
   (정렬이 안 맞는 상태를 만들어서 붙여넣기가 «거절되는» 화면)
```
🔴 **거절이 도는 것을 못 보여 주면 3.4 는 착지가 아닙니다.** 오늘 원장 쪽에서
「초록인데 실행은 빨강」을 다섯 번 만났습니다.

---

# 🔴 소유자 상설 (2026-08-23) — **모든 새 요소는 «클래스»로. 어느 화면에도 끼워넣을 수 있게**

> 「현재 만드는 모든 요소는 클래스로 만들어서 어느 화면에도 끼워넣을 수 있게해」

## 왜 — 지금 것들이 정확히 «못 끼워넣게» 돼 있습니다 (실측)
```
ledger_map_panel.js   모듈 수준에 deps · mountEl · session · kindsPromise · kindsBody
                      -> 한 페이지에 «두 개를 못 놓습니다». 둘째가 첫째를 덮어씁니다
surprise_map_view.js  함수형 + document 를 인자로 받음 -> 여러 개는 되지만
                      상태를 «호출자»가 전부 들고 있어야 합니다
```
맵·트렌드·순위표를 «한 화면에 여러 개» 놓는 것이 이 제품의 전제인데
(마킹이 여러 시각화를 잇는 것이 본체이므로), 지금 구조로는 **두 번째를 못 놓습니다.**

## 규칙
```
① 클래스        생성자가 «자기 mount 와 deps»를 받는다. 모듈 수준 상태 «금지»
② 두 개 시험     같은 화면에 두 인스턴스를 놓고 «서로 간섭이 없어야» 한다.
                이게 「끼워넣을 수 있다」의 «정의»이고, 시험이 그것을 재야 한다
③ document 주입  기존 집안 규칙 유지 — 뷰 모델은 DOM 없이, 그래서 node 로 채점된다
                (surprise_map_view.js 머리 주석이 그 규율의 근거입니다)
④ 렌더러 재사용   map2/painter.js 같은 정본을 «감싸는» 클래스이지,
                두 번째 렌더러가 아니다
```

## ⛔ 하지 말 것
```
전역 싱글턴       init() 이 모듈 변수에 mount 를 넣는 방식 — 위가 그래서 막혔습니다
프레임워크 도입    소유자 판정 2026-08-19 「리액트 지금은 안 쓴다」. 바닐라 클래스입니다
기존 것 일괄 개조  «지금 만드는 것»에 적용합니다. 레거시 소급은 별도 판정입니다
```

## 🔴 소유자 보강 (같은 날) — 「모든 UI는 «조립식»」
> 「행동 규칙 추가: 모든 ui는 "조립식"」
> 「고유 div를 가지고 그리드 배치로 작동하게. 마킹은 모든 영역에서 공유」

```
⑤ 조립식        화면은 «부품을 배치한 결과»다. 화면이 부품을 «품고» 있으면 안 된다
⑥ 고유 div      부품마다 자기 div 하나. 남의 div 안을 그리지 않는다
⑦ 그리드 배치    화면은 그 div 들을 «격자»에 앉힌다. 배치가 부품 밖에 있다
⑧ 🔴 마킹은 «공유»  마킹은 어느 부품에도 «속하지 않는다». 밖의 «저장소» 하나에 산다
⑨ 🔴 마킹은 «여럿»  그 저장소가 «이름 붙은 마킹»을 여럿 담는다 (마킹1 · 마킹2 …)
                   부품은 «읽을 이름»과 «쓸 이름»을 각각 선언한다
```

### ⑧ 이 설계의 «척추»입니다 — 틀리면 전부 다시입니다
```
안 됨   부모 화면이 마킹을 들고 자식에게 내려 준다
        -> 부품을 다른 화면에 옮기면 «그 화면의 부모»를 또 만들어야 한다 = 조립식이 아님
맞음    마킹은 «바깥»에 산다. 부품은 «이름으로» 구독하고, 마킹할 땐 «이름으로» 쓴다
        -> 부품은 어느 화면에 꽂혀도 같은 마킹을 본다. 그게 「끼워넣을 수 있다」의 뜻
```
그리고 마킹의 «단위»는 앞 절대로 **온톨로지 노드 또는 노드의 집합**입니다.
부품끼리 서로를 몰라도, 같은 노드를 가리키면 연결됩니다 — **그것이 연결의 전부입니다.**


## 🔴 정정 (2026-08-23 저녁) — 「밖에 하나」가 «아니라» 「이름 붙은 여럿」입니다
위를 처음 쓸 때 제가 「마킹은 바깥의 «하나»」라고 적었는데 **틀렸습니다.**
소유자가 스팟파이어로 그린 초안에 마킹이 «둘»이었습니다 — 맵이 쓰는 「마킹 1」과,
후보 선택인 「마킹 2」가 순위표와 후보 트렌드를 «같이» 움직입니다.
소유자 확인: **「마킹은 여러 컨텍스트 지원해야하는게 맞음」.**
```
저장소   바깥에 «하나»
마킹     그 안에 «여럿». 각자 이름이 있다
부품     읽을 이름 · 쓸 이름을 «선언»한다 (같을 수도, 다를 수도 있다)
```
🔴 「하나」로 박아 두면 **그 화면이 안 만들어집니다** — 맵에서 점을 찍는 순간 후보 선택이 날아갑니다.
위 ⑧ 줄은 «고쳤습니다». 이 절은 왜 바뀌었는지의 기록이지, 위와 다른 말이 아닙니다.

---

# 🔴 정정 (총괄 23:5x) — **워크드 예시가 «없는 짝»입니다. 제가 지어냈습니다**

앞서 제가 「이 사슬로 시안을 검산하십시오」라며 이걸 드렸습니다:
```
PLASMA_CLEAN · CH-B  ->  surface_oxidation  ->  wetting_deficit  ->  void 199
「실제 선언과 실제 데이터에 있는 사슬입니다. 지어낸 것 아닙니다」
```
**뒷문장이 거짓이었습니다.** 구현자가 실측했습니다:
```
SYN-BW-103-11 의 PLASMA_CLEAN 원자   «0»
CH-B 원자                            «0»
PLASMA_CLEAN 은 «어디서도» chamber 를 안 답니다 (2,600 원자 전부 SYN-PC-01/02/03)
```
출처를 짚으면 이렇습니다 — 선언 사슬(`surface_oxidation -> wetting_deficit -> void`)은
제가 모델 파일에서 «쟀습니다». 앞머리 `PLASMA_CLEAN · CH-B` 는 **목업에서 읽어 왔습니다.**
그걸 제 실측인 양 붙여서 돌렸습니다. 반은 재고 반은 베꼈는데 전체를 실측이라 말했습니다.

## 새 예시 — 실재하는 짝입니다 (구현자 실측)
```
SYN-BW-103-11 · BONDING · SYN-BD-04 · CH-A · 2026-08-10 01:40 +09:00
  사고 구간 01:20~02:10 (실제 209 런을 덮음)
     ↓
  bond_temp · void_formation      실측 «있음» (°C · 1,962)
  bond_pressure · void_formation  실측 «있음» (MPa)
     ↓
  wetting_deficit · void_formation   실측 «없음»
     ↓
  void 199
```
🔴 **오히려 더 나은 예시입니다** — 옛 예시는 사슬 «전체»가 실측 없는 마디였는데,
새 예시는 «같은 사슬 안»에 실측 있는 마디와 없는 마디가 둘 다 있습니다.
화면이 그 둘을 갈라 보여야 한다는 요구가 **한 줄에서 시험됩니다.**

## 목업에서 고칠 곳
```
머리 요약의 스텝 칩   「PLASMA_CLEAN · CH-B 또래와 다름」  -> «BONDING · SYN-BD-04 · CH-A»
후보 리스트          「공정 split / surface_oxidation <- PLASMA_CLEAN · CH-B」
                    -> 사고 이력 쪽으로. 「챔버 파티클 · SYN-BD-04 · 01:20~02:10 · BONDING 겹침」
                    (지금 카드에 SYN-BD-02 · CH-A 로 적혀 있는데 설비명이 다릅니다)
```
⚠️ 이건 «시안을 다시 그리라»는 게 아니라 **라벨 몇 개를 실재로 바꾸라**는 것입니다.
구조는 그대로 좋습니다.

## 제가 배운 것 — 다음에도 같은 자리입니다
```
목업에서 읽은 값을 «실측»이라 부르지 않습니다.
시안은 «제가 준 숫자»로 그려졌으므로, 거기서 숫자를 되읽으면 «제 말을 제가 인용»하는 것입니다
```

---

# 🔴 추가 (소유자 지시 00:0x) — 부품 명세는 **목업을 «띄워 놓고 보면서»** 쓰십시오

> 소유자: 「부품 디자인은 목업 화면 띄워서 직접 보면서 하라해 크롬 mcp 사용해서」

HTML 을 «읽어서» 명세를 쓰지 마십시오. **렌더된 화면을 보고** 쓰십시오.
```
띄울 것   spotfire-style-ontology-rnd-view.zip 의 「웨이퍼 진단 화면.dc.html」 (2a 절)
도구      Chrome MCP (mcp__claude-in-chrome__*)
          tabs_context_mcp 로 시작 -> navigate -> read_page / computer{screenshot}
```
🔴 **왜 이걸 지시하나:** 이 저장소가 소스를 읽고 화면을 «추론»하다 여러 번 틀렸습니다.
읽은 마크업과 «그려진 것»이 다릅니다 — CSS 가 겹치고, 토큰이 테마로 갈리고, 접힌 것이 안 보입니다.
오늘만 해도 「접힌 컨트롤을 없는 컨트롤로」 읽은 사례가 있었습니다.
```
✅ 부품마다 «스크린샷»을 근거로 두십시오. 「이렇게 보인다」가 아니라 «이렇게 보였다»
✅ 최소 크기 / 늘어날 때 무엇이 늘어나는지 -> «리사이즈해 보고» 적으십시오
   (resize_window 로 폭을 바꿔 보면 바로 나옵니다)
✅ 라이트/다크 둘 다 보십시오 — tokens.css 가 듀얼이고, 한쪽에서만 죽는 색이 있습니다
```
⚠️ 목업은 «1920 고정»으로 그려졌습니다. 리사이즈하면 깨질 수 있는데, 그 «깨지는 방식»이
   부품이 무엇을 가정하고 있는지 알려 줍니다. 깨진다고 목업을 고치지 마시고 «명세에 적으십시오».

---

# 🔴 채널 정정 (총괄 19:4x) — **소유자께 「진행해」를 «묻지 마십시오»**

> 소유자: 「나한테 물어보지 말라해」

당신이 「‘진행해’ 하시면 1번부터 시작하겠습니다」라고 적으셨습니다. **그 승인은 이미 나갔습니다** —
지시서의 「지금 당신 대기열」이 «곧 착수 허가»입니다. 두 번째 승인을 기다리지 마십시오.

## 제 지시서가 그렇게 읽히게 쓰였습니다 — 제 잘못입니다
```
제가 쓴 것   「폐기 화면 걷어내기 (세고 나서)」
읽힌 것      「세고 나서 «승인 받고» 걷어내라」
뜻한 것      「세고 나서 «그 결과대로» 걷어내라」
```
이 저장소 상설이 「착수 전 측정이 라운드를 멈춘다」입니다 — 「재라」가 아니라
**「이 조건이면 멈춰라」**로 적었어야 했습니다. 다시 적습니다:

```
✅ 세십시오. 그리고 «그대로 진행»하십시오
🛑 멈추는 조건은 «하나»뿐입니다:
   rnd_console 안의 무언가를 «폐기 대상이 아닌 화면»이 임포트하고 있을 때
   -> 그건 지울 것이 아니라 «옮길 것»이므로 보고하고 멈추십시오
   그 외에는 세고, 지우고, 보고하십시오
```

## 물을 곳 — 총괄입니다
```
판정 요청   task/design_session_report.md «맨 위»에 「🔴 판정 요청」으로
            제가 커밋 감시로 봅니다. 답도 이 파일로 옵니다
⛔ 소유자께 라우팅하지 마십시오 — 소유자는 «방향»을 정하시지 착수 허가를 내는 자리가 아닙니다
📎 소유자가 «직접» 지시하시면 그건 그대로 따르십시오. 소유자가 총괄을 이깁니다.
   금지되는 건 «당신이 소유자께 올리는» 방향입니다
```

## 그리고 당신이 적은 것 하나는 «맞습니다» — 기록합니다
> 「오늘 «인라인 스타일이 CSS를 이긴다»를 세 번 만났고, 그때마다 마크업이 아니라
>  computed 값을 재서 잡았다」

그게 정확히 제가 「띄워 놓고 보라」고 한 이유입니다. **마크업은 의도이고 computed 가 사실입니다.**
부품 명세에도 그 규율을 쓰십시오 — 「CSS 에 이렇게 적혀 있다」가 아니라 「이렇게 계산됐다」로.
