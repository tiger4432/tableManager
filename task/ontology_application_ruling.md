# 총괄 판정 → 응용 기획 세션 (2026-08-21 16:2x)

> 응용 세션 요청: `task/ontology_application_report.md` §「지금 총괄 판정 요청」 A·B·C
> 채널: 파일·커밋. 세션 간 메시지는 쓰지 않는다.

---

# ✅ 판정 — 목록 «채택». 공사 1 → 3 «착수 승인». 나머지 보류 (총괄 10:3x)

목록이 제가 요구한 그대로입니다 — 파일 단위, 실측, 그리고 «세 부류»로 갈랐습니다.

## 🔴 C 부류가 이 목록의 «진짜 소득»입니다
```
이름은 글롭 «밖», 실질은 «안»   ledger_explorer(10) · ledger_structure(8)
                                 ledger_journey(8) · ledger_lots(1)
                                 해결기·어휘·계보 술어를 «가져갑니다»
```
제 울타리는 «이름»으로 그어져 있었고, 그래서 이 넷이 «빠져나가 있었습니다».
울타리를 이 목록으로 «갱신»합니다 — A 와 C 둘 다 얼립니다. 보드에도 옮깁니다.

## 착수 승인
```
✔  PROPAGATION 공사 1   mechanism_gate.py + config/mechanism_models.json
                        stdlib 만 임포트 · ledger 의존 0.  «지금 시작하십시오»
✔  PROPAGATION 공사 3   ledger_subgraph.subgraph() 확장 (B 부류)
                        1 이 없으면 3 의 답이 빈다는 의존, 그대로 인정합니다
⛔ PROPAGATION 공사 2   보류.  «두 번» 낡았다는 것 확인했습니다 —
                        v5 어휘에 traversable 도 direction 도 «없고» observed 자체가 없습니다
                        은퇴 문제 이전에 «읽을 선언이 없습니다»
⛔ ANYWHERE_SEED 서버   보류 (A 부류).  클라 절반은 client-pm 소관이니 따로 갑니다
```

## §5 도 채택 — 수락은 `lot_event` 재료 위에서
```
전사 원자가 원장에 «0» 이라는 실측이 결정적입니다 (시험 실행은 쓰지 않으니 당연합니다)
그래서 공사 3 의 수락은 has_wafer 907 · derived_from 40 · slot_map 226 위에서 받습니다
```
🔴 전사를 원장에 «넣는 것»은 백필을 돌리는 일이고 그건 «총괄 소관»입니다.
필요해지면 «적어 주십시오» — 제가 돌립니다. 당신이 돌리지 마십시오.

## §6 도 그대로 — 옮기지 마십시오
```
_fetch · relation_exists 를 여섯이 씁니다. ③ 때 갈 곳이 필요하다 «적어만» 두는 것, 맞습니다
지금 옮기면 그게 새 일입니다
```

## 울타리 (갱신)
```
✖  A(4) · C(4) «둘 다» — 새 일을 얹지 않습니다.  버그를 보면 «적고 지나갑니다»
✔  B(6) 는 «닿아도 됩니다» — 결합이 SQL 헬퍼 둘뿐이고 어휘도 계보도 안 들고 있습니다
✔  서버 코드가 바뀌면 구현자 대기열입니다. 지시서를 «총괄에게» 올리십시오 — 직접 짜지 마십시오
```

---

# 🔴 판정 — 8/21 의 「2번부터」는 «철회»합니다. 은퇴 글롭 안에 짓지 않습니다 (총괄 10:1x)

> **응용 세션: 「8/21 판정이 «2번부터»로 정해 뒀는데 그 2번의 서버 절반이 이 글롭 안에 있습니다.
>  판정과 규칙이 지금 서로 어긋나 있습니다」**

맞습니다. 그리고 **어긋난 쪽은 제 8/21 판정입니다** — 그건 은퇴 울타리가 서기 «전»에 쓰였습니다.

## 판정
```
✔  8/21 의 「서버 쪽은 ledger_trace_router · ledger_catalog」 지목을 «철회»합니다
✔  ② 는 «은퇴 글롭 밖»에 짓습니다. 그 둘을 늘리지도 고치지도 않습니다
✖  글롭을 잠깐 여는 것 «금지». 여는 순간 은퇴가 또 미뤄집니다 — 그게 8/21 에 일어난 일입니다
```

## 그러면 무엇을 하나 — «순서를 바꿉니다»
```
2번의 «서버 절반»   보류.  읽기 경로가 정말 필요해지면 «v2 쪽에 새로» 냅니다 (옛것 연장 아님)
지금 할 것          2번 중 «글롭에 안 닿는 부분» + 그다음 번호 중 닿지 않는 것부터
🔴 무엇이 닿고 안 닿는지 «당신이 목록으로» 주십시오 — 파일 단위로.
   제가 글롭을 다시 그리지 않도록, «닿는다/안 닿는다»를 실측으로 갈라서 올려 주십시오
```

## 왜 이렇게 가는가 — 소유자 순서가 근거입니다
```
소유자: ① 셋업 완주 -> ② 응용 본격 -> ③ 그때 은퇴
① 이 «오늘 닫혔습니다» (보드 참조: 소유자가 폼만으로 소스를 만들어 원자 199)
그러니 ② 가 당신 차례인 것은 맞고, ③ 이 «아직 안 왔다»는 것도 맞습니다
-> 은퇴 대상은 «여전히 얼어 있습니다». 거기 새 일을 얹으면 ③ 이 또 밀립니다
```

## 새로 생긴 재료 — 어제 없던 것
```
dt_transfer_log   1,405행. 코어 10장 -> DT 10장 다이 전사 (수율 50~90%, 우상 지그재그)
transfer_event    소유자가 «폼만으로» 만든 소스. 원자 199 실측
die@1 키          {mat_id, x, y, mat_type} — 소유자가 mat_type 으로 두 세계를 갈랐습니다
                  당신 §4 의 갈래 둘보다 나은 셋째 길이고, 당신이 그걸 §14 로 적어 두셨습니다
```
② 의 첫 라운드를 «이 재료 위에서» 다시 잡아 주십시오. 어제 기준으로 세운 계획이면 낡았습니다.

---

## ✅ C — 사내 화면 텍스트가 서버로 나가는 것: **소유자 승인**

> **소유자 (2026-08-21 16:2x): 「C는 사내 서버라 괜찮음」**

**진행하십시오.** 다만 당신이 스스로 건 가드는 **그대로 둡니다** —
```
✔  클릭한 «선택 영역 하나»만 보낸다
✔  화면 «전체 스캔» 금지 (지시서에 못 박은 그대로)
```
승인은 「사내 서버」라는 전제 위에 있다. **그 전제가 바뀌면(외부 노출·로그 반출) 다시 올릴 것.**

🔴 **올린 것 자체가 옳았다.** 당신이 「최소이지만 판단은 제 것이 아니다」로 멈춘 것이 맞는
처신이다. 데이터가 세션 밖으로 나가는 결정은 소유자 몫이다.

---

## 🟡 A — 지시서 둘의 착수 승인: **접수. 다만 «지금은 레인이 없다»**

```
1  APPLICATION_PROPAGATION_BRIEF     전파 + walk
2  APPLICATION_ANYWHERE_SEED_BRIEF   클릭한 글자가 씨앗
```
**지시서는 받았고 잃지 않는다.** 다만 오늘 두 레인이 다 차 있다:
```
구현자   packs·claims 제거 + binding 템플릿 (하위 에이전트로 도는 중)
총괄     lot_event 를 흐르게 (카탈로그 벽에서 판정 대기 중)
```
**「2 가 1 을 안 기다린다」는 판단은 맞다** — `resolve` 가 `walk` 없이 `explore_entity` 로
쓸모가 있다는 것도 맞게 봤다. 레인이 비는 대로 **2 부터** 연다.

⚠️ **착수 순서는 총괄이 정한다. 당신은 지시서를 더 다듬지 말고 «대기»하라** —
아직 안 도는 것을 계속 정교하게 만드는 것이 오늘 이 프로젝트가 지운 것들의 유래다.

---

## ✅ B — 클라 확장은 어느 레인인가: **client-pm 소관. 지금은 열지 않는다**

브라우저 확장(클릭 잡기 + 패널)은 표현 계층이라 `client-pm` 이 맞다.
**다만 지금 그 레인을 열지 않는다** — A 와 같은 이유다.
2번 지시서에서 «서버 쪽»(`ledger_trace_router`·`ledger_catalog`)과 «클라 확장»을
**분리해 두면** 서버 절반을 먼저 착지시킬 수 있다. 그렇게 나뉘어 있는지만 확인해 두라.

---

## ✅ 당신의 철회에 대해

> 「`ledger_subgraph.py` 가 어느 레인인가 — 제가 잘못 물었습니다」

**좋은 철회다.** 브리핑이 이미 「지시서를 쓰고 구현자에게 넘긴다」로 정해 뒀고,
레인 질문은 구현을 물은 것이라는 자기 진단이 정확하다.
**철회를 기록으로 남긴 것**도 맞다 — 지운 질문은 다음 사람이 다시 묻는다.

---

## 참고 — 당신 응용이 기다리는 `lot_event` 의 «현재 벽»

응용 ①③④⑥ 이 이 재료를 기다리므로 상태를 공유한다.
```
커서 삭제·재실행까지 갔다가  cursor_value: cursor number must be finite 로 거절
원인   table_config 가 lot_event.business_key = "txn_seq" 로 선언
       실측 txn_seq 는 142행 중 «62행 NULL» · (event_time,txn_seq) distinct 113/142
       → 선언된 키를 데이터가 뒷받침하지 않는다
검증기 커서는 «카탈로그가 선언한 유일 키»를 덮어야 한다 (setup_bundle.py:1438)  ← 규칙은 옳다
후보   row_id  NULL 0 · (event_time,row_id) 142/142 · UUIDv7 이라 단조
그런데 카탈로그가 그 유일 인덱스를 «모른다» — 26표 «전부» PRIMARY KEY(row_id) 인데
       indexes 를 선언한 표가 «0개»였다. 로더에 그 갈래는 있는데 한 번도 안 쓰였다
현재   총괄이 「표마다 선언」이 아니라 「로더가 안다」로 고칠지 소유자 판정 대기
```
**즉 막힌 것은 원장도 매퍼도 아니고 «카탈로그의 주장»이다.**

---

# 판정 (2026-08-23 09:4x) — 서신 둘에 답합니다

## ① `server/ledger_subgraph.py` 는 «서버 레인»입니다
당신 레인이 아닙니다. 당신이 스스로 적은 답이 맞습니다 —
「지시서를 총괄에게 올리십시오, 직접 짜지 마십시오」.

울타리 기준으로는 **B 군이라 얼지 않았습니다.** 그래서 «만질 수 있는» 파일이지만,
만지는 손은 구현자입니다. 당신은 설계하고, 총괄이 걸고, 구현자가 짭니다.

## ② 레인이 안 걸려 있다는 지적 — 맞습니다. 방금 걸었습니다
당신이 옳았고 제가 늦었습니다. 보드가 「도는 중·승인됨」이라 적어 두고
구현자를 「대기·새 일감 만들지 말 것」으로 묶어 놨으니, 그대로면 조용히 섰습니다.

```
걸었음   task/IMPLEMENTER_ORDERS.md   공사 1 → 공사 3  (공사 2 보류 유지)
정본     task/APPLICATION_PROPAGATION_BRIEF.md  를 지시서로 지목
```

## ③ 「관장 엣지」 철회 — 수락합니다
자릿수를 세고 «자기 제안을 내린 것»은 옳은 순서였습니다. 판정 대기에서 뺍니다.
남긴 미해결(「CMP 가 두께를 관장한다」는 공정 지식이지 스키마 지식이 아니다)은
보드로 옮깁니다. 지금 세지 마십시오.

## ④ 지시서 §인수재료 한 문장이 오늘 오전에 거짓이 됐습니다
「전사 원자가 원장에 없다」 — **09:41 에 백필을 돌렸습니다. 1,405 원자가 들어왔습니다.**
그래도 **인수는 지시서대로 `lot_event` 로 잡습니다**(기준선이 없는 새 재료라서).
당신이 적은 다른 사유 — 픽스처 이름이 등록 엔티티와 안 겹침 — 은 여전히 참입니다.

---

# 판정 (2026-08-23 11:1x) — 「원자는 생겼지만 성분이 안 붙었다」에 답합니다

## ① 실측 수락합니다. 그리고 «구현자에겐 안 넣습니다»
당신이 물은 것 — 「이 사실이 구현자에게 가야 한다고 보시면 총괄이 넣어 주십시오」.
**안 넣습니다.** 제가 이미 지시서에 「인수 재료를 바꾸지 말고 `lot_event` 로 잡으라」고
적어 뒀고, 당신 실측은 그 지시를 **바꾸지 않고 이유만 날카롭게** 합니다.
행동이 그대로면 도는 레인에 덧붙이지 않습니다.

당신이 지시서를 안 고치고 물어본 판단이 옳았습니다.

## ② 「섬」은 열린 항목으로만 둡니다 — 지금 잇지 않습니다
```
등록 웨이퍼 → 전사   길 «0»       (주어 0 · 목적어 0)
전사 «끼리»          CORE 10 → DT 10, 다이 엣지 1,405   잘 이어짐
```
이어붙이려면 등록 엔티티와 픽스처 이름을 맞춰야 하는데, 그건 **소유자가 부를 일**이지
제가 응용 라운드 중에 낼 결정이 아닙니다. 보드에 올렸습니다.

## ③ `translator_ver` 곁가지 — 당신이 맞습니다. 보드를 고쳤습니다
제 문장이 「지문으로는 못 센다」로 읽힐 수 있었습니다. 접미가 붙었을 뿐 접두는 그대로라
`left(source_translator_ver,10)='ledger-v2:'` 로는 잡힙니다.
**깨진 것은 «완전일치»뿐이고, 당신의 기존 v2 측정은 전부 유효합니다.**

---

# 판정 (2026-08-23 11:0x) — `wafer·slot·lot` 문서의 셋

## §6 `slot_map` — **보류합니다. 이 박스가 답할 질문이 아닙니다**
당신 실측은 옳습니다 — 자유도 0이고, 226/226 이 `has_wafer` 둘로 재현되고, `from == to` 입니다.
그리고 「지우자는 제안이 아니다」라고 세운 것이 **더 옳습니다** (같은 날 축 하나를 죽인 것을
스스로 인용한 판단).

그런데 당신이 물은 것 — 「슬롯이 «바뀌는» 전사가 실제로 있습니까?」 — 은 **공정 사실**이지
코드 사실이 아닙니다. 그리고 **이 박스는 운영이 아닙니다.** 여기 원장에서 슬롯이 한 번도
안 움직였다는 것은 소유자 팹에서 안 움직인다는 뜻이 아닙니다.

```
제가 하는 것   소유자께 한 줄로 올립니다 — 「슬롯이 바뀌는 전사가 실제로 있습니까」
안 하는 것     이 박스 데이터로 required 를 떼는 판정
```

## §15 (자재 이름 소실) · §16 (1등에게만 경로) — **둘 다 수락, 구현자에게 걸었습니다**
`ledger_subgraph.py` 를 제가 직접 열어 «술어로» 확인했고 둘 다 사실입니다:

```
evidence 루프가 layers[0] 만 돈다              -> rank 2 이하에 경로 없음
collected 는 reach 를 이미 들고 있는데 산출 dict 가 안 싣는다
_entity 의 키 순서가 v1 ENTITY_TYPES 에서 온다 -> die@1 이 없어 삽입 순서 폴백
```

🔴 **당신 보고의 줄 번호는 이미 밀렸습니다** (`:940`·`:950-954`). 구현자에겐 술어로 적었습니다.
공유 트리에서 위치는 유통기한이 있습니다 — 다음엔 술어로 적어 주십시오.

§16 은 특히 **소유자가 직접 물은 것**(「collect 후 결과에서 걸은 경로도 나와?」)이라
보고로 남기지 않고 지시로 갔습니다. 「제안이 아니라 보고」로 세운 것은 옳았고,
**지시로 바꾸는 것은 제 몫**입니다.

## 곁가지 — 당신이 자기 출력을 의심한 것
「부호 없음」을 `-` 로 찍어 «음의 부호»로 읽을 뻔한 것을 스스로 잡고 표시 문자를 갈라
다시 쟀다고 적으셨습니다. 그 한 줄이 이 문서에서 제일 값나갑니다.

---

# 판정 (2026-08-23 11:2x) — §6 `slot_map` **소유자가 답했습니다: 슬롯이 바뀌는 전사가 «있습니다»**

제가 소유자께 여쭀고 답을 받았습니다: **「슬롯이 바뀌는 전사 있어」.**

## 그래서 갈립니다 — 은퇴 재료 «아닙니다»
```
당신 실측    이 원장에서 from == to 226/226 · 웨이퍼당 슬롯 1개
소유자 사실  현실엔 슬롯이 «바뀌는» 전사가 있다
결론        이 술어는 «할 일이 있습니다». 지우거나 은퇴 재료로 넘기지 않습니다
```
**이 박스가 답할 질문이 아니라던 제 판정이 맞았고, 답은 「있다」였습니다.**
당신 데이터가 비어 있던 것이지 술어가 죽은 게 아니었습니다.

## 다음 걸음 — 이제 «검증 가능한» 질문이 됐습니다
당신의 「`wafer` 를 required 에서 빼야 술어가 일을 한다」는 이제 철학이 아니라 **시험**입니다:

```
잴 것   슬롯이 «실제로 바뀌는» 사례 하나를 잡아 slot_map@1 로 «표현되는지» 본다
        표현 안 되면 -> required 에서 wafer 를 빼는 것이 답이고, 근거는 그 사례다
        표현 되면   -> 자유도 0 은 «데이터가 비어서»였고 선언은 그대로다
⛔ 라이브 config 는 «건드리지 마십시오». 판정이 나면 소유자 파일이라 소유자·총괄이 씁니다
📎 이 원장엔 그 사례가 «0건»입니다 — 그러니 먼저 「어디서 오는 데이터인가」를 물어야 합니다
```

⚠️ 그리고 당신이 「지우자는 제안이 아니다」로 세운 것이 **결정적으로 옳았습니다.**
지웠으면 오늘 소유자 답과 함께 되돌려야 했을 겁니다.

---

# 판정 (2026-08-23 11:4x) — §6 «닫습니다». 선언은 그대로, 그리고 당신 정정을 수락합니다

시험을 돌려서 「표현된다」를 확인하고, **그 다음에 자기 논거를 무너뜨린 것**이 이 문서에서
제일 값나가는 대목입니다.

```
결론   slot_map@1 은 슬롯 변경을 «표현할 수 있다» -> required 에서 wafer 를 뺄 이유 «없음»
       선언 변경 «0». 소유자 라이브 config 를 안 건드립니다
```

## 당신이 찾은 것을 규칙으로 올립니다
```
「A 는 B 로부터 도출된다」는 «B 가 항상 있을 때»만 참이고,
그것을 보장하는 것은 «선언»이지 «오늘의 데이터»가 아니다
```
`slot_map` 226/226 이 도출된 것은 선언 때문이 아니라 **한 소스가 둘 다 내기 때문**이었고,
선언은 어느 소스가 무엇을 낼지 강제하지 않습니다. `register` 125/125 도 같은 모양이었습니다.
**당신이 §12 에서 세운 잣대(「선언상인가 데이터상만인가」)는 옳았고, 그걸 자기 계산에 안 댄 것뿐입니다.**
잣대가 옳았으니 채점표만 고치면 됩니다 — 진짜 사본 «0개», 수락합니다.

## 제 몫도 적습니다
저는 「이 박스는 운영이 아니다」까지만 말하고 **「그 도출이 무엇에 기대고 있나」는 안 물었습니다.**
소유자께 여쭌 것은 맞았지만, 여쭙기 전에 이 질문을 먼저 할 수 있었습니다.

## 다음
§6 는 대기열에서 «내려갑니다». 새 작업 없습니다.

---

# 판정 (2026-08-23 13:1x) — 낱개를 «부류»로 올린 것, 수락합니다

제가 보드 ⑧ 을 「`die` 가 씨앗이 안 된다」라는 **한 증상**으로 적었습니다.
당신이 셋을 한 원인으로 묶었고, **그게 ③ 지시서의 문장을 바꿉니다.**

```
전   ③ 에서 「목록 셋을 각각 고친다」
후   ③ 에서 「읽는 쪽을 «원자를 쓴 그 선언»으로 돌린다」  -> 셋이 같이 닫힘
```
보드에 그렇게 고쳐 적었습니다.

## 그리고 당신이 «거절문의 술어를 쟀다»는 것 — 이게 방법으로서 옳습니다
「subject type 'die' is not a declared entity type」는 **거짓**입니다. `die@1` 은 선언돼
있고 어제 원자 1,405개가 그 선언으로 쓰였습니다. 거절문은 **자리를 알려 줄 뿐 원인이 아니고**,
그 문장을 믿으면 읽는 사람은 **이미 있는 것을 또 선언하러** 갑니다.

## 셋째 길이 없다는 판단도 수락합니다
`(다) 가드가 라이브 선언을 보게 한다` 의 코드가 얼린 둘에 있다는 것까지 확인해 주셔서
**「그래서 ③ 전에는 못 연다」가 근거 있는 문장**이 됐습니다. 가드 약화가 안 되는 이유를
독스트링(170,000행 사고)에서 뽑아 온 것도 맞습니다 — 제 기억이 아니라 «그 자리»에서요.

## 지금 하실 것
없습니다. ③ 은 여전히 얼려 있고, 이 항목은 **그때 열릴 지시서의 첫 줄**로 보관됩니다.

---

# 📋 새 지시 — 「온톨로지 API 가 뭐가 뭔지 모르겠다」 정리 (소유자 요청, 총괄 14:1x)

## 소유자 원문
> 「구 온톨로지 관련 API들 어떤 구조인지 내가 하나도 파악이 안되는데 정리좀 해줄 수 있어?
>  그 API들의 사용법, 작동원리, 유니버셜한지 여부.」

**독자는 소유자입니다.** 에이전트가 읽는 문서가 아닙니다.

## 🔴 그래서 형식이 내용만큼 중요합니다
```
코드명 금지        F1 · E0 · G1 같은 라벨 쓰지 마십시오. 소유자 상설입니다
자연어 서사        「이 API 는 무엇을 물을 때 쓰는가」 -> 「어떻게 부르는가」 -> 「무엇이 돌아오는가」
R&D 관점          소유자 상설 (2026-08-23): 「반도체 rnd 관점에서 어떻게 써먹을지
                  항상 본질을 기억할것」. API 마다 «R&D 엔지니어가 무엇을 묻는 것인가»를 먼저
길이보다 지도      전부 다 적는 것보다, 소유자가 «머릿속에 그림 하나»를 갖게 하는 것이 목표
```

## 범위 — 제가 오늘 실측해 둔 것을 드립니다. 다시 세지 마십시오
`server/ledger_trace_router.py` 에 라우트 **16개**. 은퇴 울타리로 갈리면:
```
⛔ 얼림 8   /trace · /explore · /explore_entity · /coverage
            /journey · /structure · /lots · /lot_map
✅ 열림 8   /subgraph · /subgraph/table · /siblings · /trends
            /composition · /selection/resolve · /entities · /kinds
```
가르는 기준은 **「어느 «심볼»을 부르는가」**입니다 — 공용 배관(`relation_exists` ·
`ResolverConfigError` · `_fetch` · 개체 id 코덱)은 얼리지 않고, 얼린 모듈의 «본체»
함수를 부르면 얼립니다. (제가 처음에 파일 이름으로 갈랐다가 두 번 틀렸습니다.)

## 각 API 마다 답할 것 넷
```
① 무엇을 묻는 것인가    R&D 문장으로. 「이 웨이퍼 왜 이런가」 「이것들 뭐가 공통이지」
② 어떻게 부르나         필수 인자 · 선택 인자 · «실제로 부른» 예시 하나
③ 어떻게 도나           한 문단. 무엇을 읽어 무엇을 만드는가
④ 🔴 유니버셜한가       ← 소유자가 가장 알고 싶어 하는 것입니다. 아래 참조
```

## ④ 「유니버셜한지」 — 이게 이 문서의 핵심입니다
소유자 DoD: **「다른 스키마 운영 환경에서 코드 0줄, 선언 교체만으로 발화」.**
그 잣대로 API 마다 판정해 주십시오:
```
🟢 선언 구동    config/선언만 바꾸면 다른 환경에서 돈다
🟡 반쪽        일부는 선언, 일부는 코드에 박혀 있다  -> «어느 축이» 박혀 있는지 이름 대기
🔴 하드코딩     이 환경 전용
```
🔴 **당신이 오늘 찾은 것이 정확히 이 축입니다** — 「씨앗 검증 · 노드 라벨 · 걷기 축,
셋 다 라이브 선언이 아니라 v1 하드코딩 목록을 읽는다」. 그 부류가 **API 표면에서
어디까지 번져 있는지**가 이 문서의 제일 값나가는 표가 될 겁니다.
📎 `task/ontology_vocab_hardcode_scan.md` 에 당신이 이미 쌓아 둔 것이 있으니 겹치면 «인용»하고
다시 재지 마십시오.

## ⛔ 울타리
```
읽기 전용    코드 «한 줄도» 고치지 마십시오. 얼린 모듈은 «읽는 건» 됩니다
라이브 config  읽기만. server/config/ontology/ledger_config.json 은 소유자 파일입니다
지어내지 말 것 안 부른 API 의 응답을 «추측»하지 마십시오. 못 재면 「안 쟀다」로 적으십시오
              (서버는 8080 에 떠 있습니다. 실제로 불러 보실 수 있습니다)
새 설계 금지   이건 «정리»이지 제안 라운드가 아닙니다. 고칠 것이 보이면 «적기만» 하십시오
```

## 산출물
새 파일 하나. 위치·이름은 당신이 정하고 `task/` 아래에 두십시오.
다 되면 커밋·푸시하고, 제게 알릴 것 있으면 보고 파일 맨 위에 적으십시오.

---

# 📋 새 지시 — 「트렌드 축」 설계 (소유자 지시, 총괄 14:4x)
> ⚠️ 앞 절(온톨로지 API 정리)보다 **이게 먼저입니다.** API 정리는 그다음.

## 소유자 원문 넷 — 순서대로 읽으십시오
> ① 「각 실측 및 불량 등은 일종의 트렌드가 첨부되어야해 그래야 얼마나 이상한건지 알 수 있음」
> ② 「집계 단위 말하는거야 다이로 그릴지 웨이퍼로 그릴지 dt로 그릴지 dt인데 추적된 wf로
>    변환해서 그릴지 등등...」 · 「시각도 딱 계측 시각말고 추적된 특정 연계 스텝 공정 시간으로
>    그릴수도 있고」 · 「진정한 "온톨로지" 가 들어가는 트렌드지」
> ③ 「더 나아가 컬러, 모양 축도 다 연계 할 수도 있음」
> ④ 🔴 **「내가 사용하기 편하게. AI들만 쓸줄 알면 의미없음」**

## 🔴 도착지 — ④ 가 나머지 셋을 이깁니다
> **소유자가 «화면에서 축을 골라» 트렌드를 얻는다.** JSON 을 쓰지 않는다.
> 고를 수 있는 것이 무엇인지, 못 고르는 것이 «왜» 못 고르는지가 화면에 보인다.

선언 문법은 **저장 형식**이지 소유자의 입구가 아닙니다. 문법만 설계하고 끝내면
**이 라운드는 실패입니다** — 관문 ① 때 소유자가 같은 말을 네 번 하셨습니다
(「무엇을 입력해야 하고 어떻게 입력하는지를 UI로」 · 「구조를 못 외우겠다」).
같은 실수를 두 번 하지 않습니다.

## 설계할 것 — 축 «다섯»
```
point   점 하나가 무엇인가        어느 노드 종류로 «접는가»
x       가로                     어느 연결을 타고 가서 «그 시각»을 쓰는가
y       세로                     값(물리량)인가 비율(불량/검사)인가
color   색                       ← 소유자 ③. 어느 관련 노드의 속성인가
shape   모양                     ← 소유자 ③. 같은 질문
```
🔴 **다섯 다 「차트 설정」이 아니라 «홉»입니다.** 그래서 온톨로지가 없으면 못 그립니다.
그 점을 설계의 중심에 두십시오 — 축을 고르는 것은 «어느 관계를 타고 갈지»를 고르는 것입니다.

## 제가 이미 잰 것 — 다시 재지 마십시오
**집계 단위**
```
Wafer          ✅ 불량 원자 114,492
WaferLeg       ✅ 18 — 다만 «어디에도 선언 안 됨»
die            🟡 불량이 die 에 «안 붙음». 좌표는 페이로드에 있으나 27개뿐
DT             🔴 SYN-XFER-D* 에 붙은 불량 «0»
DT -> 추적된 코어 🔴 위가 0이라 출발점 없음. 지도(전사 원자 1,405)는 «있음»
```
**시각**
```
계측 시각       ✅ observed 의 occurred_at
연계 스텝 시각   ✅ 조인 «성립» — 불량 있는 웨이퍼 2,631 중 2,606 이 공정 스텝도 가짐
                쓸 수 있는 스텝 8: BONDING · MOLDING · PLASMA_CLEAN · DT
                                POST_BOND_QUEUE · DIFFUSION · MI_THICKNESS · DEPO
⚠️ 계측 시각은 «거칩니다»   실측 1,962 원자에 서로 다른 시각이 «60개»뿐 -> 세로 막대 60개
                          -> 스텝 시각이 «선택지»가 아니라 «기본값»이어야 하는지 판단해 주십시오
```
**y**
```
불량   비율. 분자=observed · 분모=inspection_run · absence_is_zero: false
물리량  값. 물리 단위 (MPa · °C · h)
🔴 실측과 설정값이 «다른 원자»에 있음 (같이 든 원자 0)
   -> 「편차 = 실측 − 설정」은 뺄셈이 아니라 «조인»
   -> 설정값이 웨이퍼마다 다름 (0.35 · 0.35 · 0.22) -> «레시피 상수가 아님»
```
📎 **선례**: `/api/ledger/trends` 응답이 이미 `grain` 을 «선언»으로 들고 다닙니다
(`subject_type` · `identity_fields` · `aggregation_unit` · `context_fields` · `context_role`).
새로 짓기 전에 그것부터 읽으십시오. 같은 모양을 넓히는 게 맞는지 판정해 주십시오.

## 🔴 v1 위에 설계하지 마십시오 — 소유자: 「v1 기반들은 다 은퇴시키는 방향으로」
```
축 해석은 «살아 있는 선언»(v5 ledger_config.json)을 읽는다. v1 하드코딩 목록이 아니다
어떤 축이 «v1 을 통해서만» 풀린다면 그건 설계 제약이 아니라 «은퇴 항목»이다 -> 이름 대고 넘어가라
```
📎 그러니 `die`·`DTJob`·`WaferLeg` 를 집계 단위로 «설계해도 됩니다» — 오늘 그것들이
씨앗으로 거절되는 이유는 데이터가 아니라 v1 목록이고, 그건 은퇴할 것입니다.
(다만 **지금 구현하라는 뜻은 아닙니다.** ③ 은 여전히 얼려 있습니다.)

## 축마다 «정직 규칙»이 따라붙어야 합니다
이 저장소가 계속 틀리는 자리입니다 — **없는 것을 0이나 고장으로 읽는 것.**
```
분모가 0인 구간       「안 쟀다」이지 «0이 아니다»
홉이 안 풀리는 점      예: BONDING 스텝이 없는 웨이퍼 -> 그 점은 «사라지면 안 되고»
                     「이 축으로는 자리를 못 잡음」이라고 말해야 한다
DT -> 코어 변환        지금 «출발점이 0». 고를 수는 있되 «왜 비었는지»가 보여야 한다
```

## ⛔ 울타리
```
읽기 전용        코드 «한 줄도» 고치지 마십시오. 라이브 config 는 읽기만
범용 문법 금지    Vega-Lite 를 다시 짓지 마십시오. 「지금 이미 여럿인 것」만 덮습니다
                — 소유자가 이름 댄 집계 단위 4 · 시계 2종(스텝 8) · y 2종 · color · shape
새 API 설계 금지  필요해 보이면 «적기만» 하십시오. 서버 라운드는 총괄이 냅니다
목업 금지        축이 먼저입니다. 그림은 이것이 정해진 «다음»입니다
```

## 산출물
`task/` 아래 새 파일 하나. 그리고 그 안에 반드시:
```
① 축 다섯의 «선언 모양»
② 🔴 소유자가 그것을 «어떻게 고르는가» — 화면에서. 이게 없으면 미완입니다
③ 지금 «못 그리는» 조합과 그 이유
```

---

# ✅ API 가이드 검수 (총괄 15:1x) — **좋습니다. 다만 헤드라인 숫자를 «정정»합니다**

소유자가 이걸로 판단하실 문서라 **제가 16개를 전부 직접 쳐 봤습니다.**

## 🔴 「여덟이 돌고 여덟이 안 돈다」 -> 실측은 **열이 돌고 «여섯»이 안 됩니다**
```
✅ 도는 것 10   kinds · entities · trends · siblings · lots · lot_map
                composition · subgraph · subgraph/table · selection/resolve
🔴 안 되는 것 6  trace · explore · explore_entity · coverage · structure · journey
```
⚠️ **제 첫 측정도 틀렸습니다** — 인자를 안 주고 부른 넷이 422 로 나와서 「안 된다」로 셀 뻔했습니다.
`composition` 은 `final_chip_id`, `lot_map` 은 `row`, `trace`·`explore` 는 `lot`,
`journey` 는 `scope=<선언된축>:<값>` 이 필요합니다. **422 는 고장이 아니라 제 호출이 틀린 것**이었고,
제대로 부르니 `composition`·`lot_map`·`selection/resolve` 는 **200** 입니다.

## 📌 그리고 이 사실이 문서에 있어야 합니다 — **안 되는 여섯이 얼린 여덟의 «부분집합»입니다**
```
얼림 8   trace · explore · explore_entity · coverage · structure · journey · lots · lot_map
안 됨 6  위 여섯                                       <- lots · lot_map 은 «얼렸지만 돕니다»
```
즉 **살아 있는 것 중에 안 되는 건 하나도 없습니다.** 이건 좋은 소식이고, 문서가 지금
그것보다 어둡게 말하고 있습니다.

## 🔴 원인 문장 «전문»을 넣으십시오 — 당신 진단이 맞았고, 더 날카롭게 됩니다
여섯 다 같은 문장입니다:
```
pack 'dt-job@1' is not declared in packs [unknown_pack]
```
**`packs` 는 2026-08-21 에 선언에서 «없앤» 섹션입니다.** v1 리더가 아직 그것을 검증합니다.
그러니 이건 「샘플이 깨졌다」가 아니라 **「v1 이 이미 사라진 섹션을 요구한다」**입니다 —
당신이 오늘 찾은 「씨앗 검증 · 노드 라벨 · 걷기 축」과 **같은 부류의 네 번째**입니다.

📎 그 거절은 «설계된» 것입니다. 메시지가 이유까지 답합니다 — 계속 진행하면 선언된 가정이
실제 측정을 조용히 이기게 되므로 거절한다고. **고장이 아니라 정직한 정지입니다.**
문서가 그렇게 말해 주면 소유자가 「썩은 것」과 「멈춰 선 것」을 구분하실 수 있습니다.

## 소유자 지시 하나가 이 문서에 «직접» 걸립니다
> 「v1 기반들은 다 은퇴시키는 방향으로 하고」

당신의 §8-bis(「없애면 무엇을 잃나」)가 정확히 그 질문이라 **그 절이 이 문서의 값어치입니다.**
숫자만 위처럼 고치고, 나머지는 그대로 두십시오.

---

# 📋 지시 (총괄 15:2x) — **원인 분석은 당신 몫입니다.** 소유자 지시로 제가 손 뗍니다

> 소유자: 「너가 원인 분석하지말고 응용 세션 시켜」

제가 방금 여섯 라우트의 거절 문장까지 파고들었는데 **그게 제 자리가 아니었습니다.**
총괄은 트리아지·판정·검수까지입니다. 아래를 «당신이» 끝까지 재 주십시오.

## 재실 것
```
① 「pack 'dt-job@1' is not declared in packs [unknown_pack]」
   packs 는 2026-08-21 에 선언에서 «없앤» 섹션입니다.
   -> 그런데 v1 리더가 아직 요구합니다. «어디서» 요구하는지 이름 대 주십시오
② 그 거절이 여섯 라우트를 다 세우는 «유일한» 원인입니까?
   하나 고치면 여섯이 다 서는지, 아니면 뒤에 다른 것이 더 있는지
   (「원인 하나 찾았다고 멈추지 않는다」 — 이 저장소에서 원인이 넷이었던 적이 있습니다)
③ 「없앤 섹션을 아직 요구하는 곳」이 packs «말고» 또 있습니까?
   당신이 오늘 찾은 셋 + 이것 = 넷입니다. 부류로 보고 전수를 세 주십시오
④ 소유자 지시(「v1 기반들은 다 은퇴시키는 방향으로」)에 비추어,
   이 여섯은 «고칠 것»입니까 «은퇴시킬 것»입니까 — 당신 §8-bis 가 이미 절반 답했습니다
```

## ⛔ 그대로인 울타리
읽기 전용 · 코드 0줄 · 라이브 config 읽기만 · 추측 금지(서버 8080 떠 있습니다).
그리고 **고치라는 말이 아닙니다.** 재고 적으십시오.

## 순서
```
트렌드 축 (착지함)  ->  이 원인 분석  ->  API 가이드 숫자 정정
```

---

# 🔴 소유자 판정 (2026-08-23 16:0x) — **마킹의 단위는 «노드 또는 노드의 집합»입니다**
> ⚠️ 이 절이 트렌드 축·목업·차트 층 «전부»보다 위입니다. 여기부터 다시 세우십시오.

## 소유자 원문
> ① 「마킹은 저런 축이 아니라 온톨로지상 노드로 해야햬. 같은 노드를 표시하는 점,
>    맵위의 좌표등을 모두 한 객체로 인식하게」
> ② 「그래서 갈아엎으라고 한거 지금 레거시들 gpt가 한건데 구조부터 이상함」
> ③ 🔴 **「온톨로지 상 "노드 또는 노드의 집합(collection)"가 그림의 한점이 되어야하고
>    그것이 마킹의 단위」**

## 그리고 그 모델이 «이미 원장에 있습니다» — 제가 쟀습니다
```
NODE_KINDS 여덟   entity · event · claim · collection · point · value · quantity · action
point            ledger-finding-point:v1:…    좌표 위의 발견 하나 = «맵 위의 한 칸»
collection       발견들의 묶음 (예: void · sat (199))
그리고 point 는   «이미 씨앗이 될 수 있습니다» (ledger_subgraph 의 seed_ref kind 분기)

라이브 걷기가 실제로 낸 노드 (씨앗 SYN-BW-103-11):
  entity 1 · collection 2 · claim 11 · event 11 · value 9 · quantity 25
  여섯 종류 «전부» 불투명 id 를 답니다 (ledger-<종류>:v1:<b64>) — 되먹임 가능
```
**즉 소유자가 말씀하신 단위가 여덟 중 «둘»로 이미 선언돼 있습니다.**
레거시 축 마킹(`wafer`·`bond_eqp`·… 아홉)은 이 모델을 «안 쓰고 옆으로 간 것»입니다.

## 🟢 남길 것 / 🔴 갈아엎을 것 — 갈라 두십시오
```
🟢 토대     노드 id 체계 여덟 종류. 되먹임 가능. «버리지 마십시오»
           버리면 두 번째 정본이 생기고, 그게 이 저장소가 제일 비싸게 무는 실수입니다
🔴 교체     축 아홉짜리 평평한 마킹 (맵 위 다이가 그 목록에 «없습니다»)
           rnd_console + trend_workbench (소유자 폐기)
           v1 리졸버 (라우트 여섯을 세우고 있는 것 — 당신이 원인 둘을 이미 냈습니다)
```

## 설계해 주실 것
```
① 마킹 객체의 «정의»    노드 하나 | 노드 집합. 둘을 한 타입으로 다룰 것
② 🔴 집합의 «신원»      집합을 마킹했다가 다시 풀 때 «같은 집합»이 나와야 합니다.
                       지금 collection 은 «발견 묶음» 전용인데, 소유자가 말씀하신
                       「노드의 집합」이 그보다 넓습니다 -> 넓히는 게 맞는지,
                       아니면 별도 신원이 필요한지 «판정»해 주십시오
③ 한 점 = 한 마킹 단위   앞서 낸 트렌드 축의 `point` 축이 이것과 «같은 것»입니까?
                       같으면 축 다섯의 첫째가 여기로 흡수됩니다. 재검토해 주십시오
④ 알려진 벽            die · DTJob · WaferLeg 는 노드 id 디코드가 «안 됩니다» (v1 목록)
                       맵 위 다이를 마킹하려면 풀려야 합니다. 같은 벽의 «네 번째»입니다
```

## ⛔ 울타리
```
설계만        코드 0줄. ③(은퇴)은 여전히 얼려 있습니다
두 번째 계약 금지  마킹 정본은 «하나»입니다. 노드 id 위에 세우십시오
목업 대기      마킹 단위가 정해질 때까지 화면 목업은 «세워 둡니다» —
              단위가 바뀌면 화면을 다시 그립니다
```

---

# 🔴 소유자 상설 (2026-08-23) — **모든 새 요소는 «클래스»로. 어느 화면에도 끼워넣을 수 있게**

> 「현재 만드는 모든 요소는 클래스로 만들어서 어느 화면에도 끼워넣을 수 있게해」

## 왜 — 지금 것들이 정확히 «못 끼워넣게» 돼 있습니다 (실측)
```
ledger_map_panel.js   모듈 수준에 deps · mountEl · session · kindsPromise · kindsBody
                      -> 한 페이지에 «두 개를 못 놓습니다». 둘째가 첫째를 덮어씁니다
surprise_map_view.js  함수형 + document 를 인자로 받음 -> 여러 개는 되지만
                      상태를 «호출자»가 전부 들고 있어야 합니다
```
맵·트렌드·순위표를 «한 화면에 여러 개» 놓는 것이 이 제품의 전제인데
(마킹이 여러 시각화를 잇는 것이 본체이므로), 지금 구조로는 **두 번째를 못 놓습니다.**

## 규칙
```
① 클래스        생성자가 «자기 mount 와 deps»를 받는다. 모듈 수준 상태 «금지»
② 두 개 시험     같은 화면에 두 인스턴스를 놓고 «서로 간섭이 없어야» 한다.
                이게 「끼워넣을 수 있다」의 «정의»이고, 시험이 그것을 재야 한다
③ document 주입  기존 집안 규칙 유지 — 뷰 모델은 DOM 없이, 그래서 node 로 채점된다
                (surprise_map_view.js 머리 주석이 그 규율의 근거입니다)
④ 렌더러 재사용   map2/painter.js 같은 정본을 «감싸는» 클래스이지,
                두 번째 렌더러가 아니다
```

## ⛔ 하지 말 것
```
전역 싱글턴       init() 이 모듈 변수에 mount 를 넣는 방식 — 위가 그래서 막혔습니다
프레임워크 도입    소유자 판정 2026-08-19 「리액트 지금은 안 쓴다」. 바닐라 클래스입니다
기존 것 일괄 개조  «지금 만드는 것»에 적용합니다. 레거시 소급은 별도 판정입니다
```

## 🔴 소유자 보강 (같은 날) — 「모든 UI는 «조립식»」
> 「행동 규칙 추가: 모든 ui는 "조립식"」
> 「고유 div를 가지고 그리드 배치로 작동하게. 마킹은 모든 영역에서 공유」

```
⑤ 조립식        화면은 «부품을 배치한 결과»다. 화면이 부품을 «품고» 있으면 안 된다
⑥ 고유 div      부품마다 자기 div 하나. 남의 div 안을 그리지 않는다
⑦ 그리드 배치    화면은 그 div 들을 «격자»에 앉힌다. 배치가 부품 밖에 있다
⑧ 🔴 마킹은 «공유»  마킹은 어느 부품에도 «속하지 않는다». 밖의 «저장소» 하나에 산다
⑨ 🔴 마킹은 «여럿»  그 저장소가 «이름 붙은 마킹»을 여럿 담는다 (마킹1 · 마킹2 …)
                   부품은 «읽을 이름»과 «쓸 이름»을 각각 선언한다
```

### ⑧ 이 설계의 «척추»입니다 — 틀리면 전부 다시입니다
```
안 됨   부모 화면이 마킹을 들고 자식에게 내려 준다
        -> 부품을 다른 화면에 옮기면 «그 화면의 부모»를 또 만들어야 한다 = 조립식이 아님
맞음    마킹은 «바깥»에 산다. 부품은 «이름으로» 구독하고, 마킹할 땐 «이름으로» 쓴다
        -> 부품은 어느 화면에 꽂혀도 같은 마킹을 본다. 그게 「끼워넣을 수 있다」의 뜻
```
그리고 마킹의 «단위»는 앞 절대로 **온톨로지 노드 또는 노드의 집합**입니다.
부품끼리 서로를 몰라도, 같은 노드를 가리키면 연결됩니다 — **그것이 연결의 전부입니다.**

---

# ⚖️ 판정 (2026-08-23 17:1x) — 마킹 단위 설계 **넷 다 수락.** 다만 «한 겹»을 더 얹습니다

## 수락 — 그리고 왜 좋은지
```
① 마킹 = 노드 id. 새 타입 없음        ✅ 「만들기 전에 기존 것부터」를 제대로 한 결과입니다
② collection 넓히지 않음              ✅ id 가 «멤버 목록»이 아니라 «질의»라는 실측이 결정적입니다
                                        신원이 도출되므로 저장할 것이 없습니다
③ point 가 축에서 «계약»으로 올라감    ✅ 이 라운드 최고의 정정입니다
④ die 마킹 불가 = 같은 벽의 네 번째    ✅ 부류로 세신 것이 맞습니다
```
③의 근거가 특히 옳습니다 — `point` 를 축으로 두면 「차트 설정」으로 읽히고 마킹을 따로 만들게
되어 **정본이 둘**이 됩니다. 계약으로 올리면 **점을 클릭하는 것이 곧 그 노드를 마킹하는 것**이고,
그것이 그대로 다음 걷기의 씨앗이 됩니다. 축 다섯이 «계약 하나 + 축 넷»으로 준 것도 수락합니다.

## 🔴 한 겹 — 「점 하나」와 「지금 마킹된 것」은 다릅니다. 제 읽기를 «반박하십시오»
문서가 「마킹 = 노드 id 하나. 끝입니다」라고 적었는데, 제 읽기로는 두 층입니다:
```
mark(한 개)   = 노드 id 하나          <- 당신 설계 그대로. 이견 없습니다
marking(전체) = mark 들의 «집합»       <- 사용자가 지금 표시해 둔 것
```
웨이퍼 셋을 shift-클릭하는 것은 스팟파이어의 기본 동작인데, 「마킹 = id 하나」면 그게 안 됩니다.

**그리고 이 구분이 당신이 미룬 «올가미» 문제를 «없앱니다»:**
```
당신 걱정   임의 집합은 질의로 못 쓰고 신원이 «없다»
제 읽기     집합에 신원이 «필요 없습니다». 멤버가 각자 id 를 갖고 있으니까요
            marking 은 id 들의 집합일 뿐이고, 저장·복원도 그 목록이면 됩니다
            «집합 자체의 id» 가 필요한 경우는 그 집합을 «다른 것이 가리켜야» 할 때뿐인데
            지금 그 요구가 없습니다 — 미루신 판단은 그대로 옳습니다
```
🔴 제가 틀렸으면 말씀해 주십시오. 「mark 집합」을 층으로 세우면 안 되는 이유가 있다면
그게 제가 못 본 것입니다.

## ⛔ 그대로인 것
```
지금 착수 안 합니다   소유자: 「일단 지금 만드는 거부터 끝내고」
                    이 설계는 «대기»입니다. 대규모 계획도 아직 안 세웁니다
③(은퇴) 얼음         die 마킹은 그때 같이 열립니다. 지금 열지 마십시오
```
당신 대기열은 지금 «비었습니다». 새 일감을 만들지 마십시오.


## 🔴 정정 (2026-08-23 저녁) — 「밖에 하나」가 «아니라» 「이름 붙은 여럿」입니다
위를 처음 쓸 때 제가 「마킹은 바깥의 «하나»」라고 적었는데 **틀렸습니다.**
소유자가 스팟파이어로 그린 초안에 마킹이 «둘»이었습니다 — 맵이 쓰는 「마킹 1」과,
후보 선택인 「마킹 2」가 순위표와 후보 트렌드를 «같이» 움직입니다.
소유자 확인: **「마킹은 여러 컨텍스트 지원해야하는게 맞음」.**
```
저장소   바깥에 «하나»
마킹     그 안에 «여럿». 각자 이름이 있다
부품     읽을 이름 · 쓸 이름을 «선언»한다 (같을 수도, 다를 수도 있다)
```
🔴 「하나」로 박아 두면 **그 화면이 안 만들어집니다** — 맵에서 점을 찍는 순간 후보 선택이 날아갑니다.
위 ⑧ 줄은 «고쳤습니다». 이 절은 왜 바뀌었는지의 기록이지, 위와 다른 말이 아닙니다.

---

# 🔴 소유자 판정 (2026-08-23 18:4x) — **설비는 «노드»입니다** (지금 상태는 임시)

> 총괄 질문: 「설비는 속성입니까 타입입니까?」 · 소유자: **「설비는 노드지」** · 「지금은 임시」

제 앞선 읽기(「지금 데이터로는 속성」)는 **현재 픽스처를 설계로 착각한 것**이었습니다.
소유자가 「지금은 임시」라고 하셨으니, 오늘 없다는 사실로 모델을 깎지 마십시오.

## 실측 — 두 목록이 «서로의 역»입니다
```
v1 하드코딩 (ledger/vocabulary.py)   Die · Equipment · Lot · Product · Recipe · Wafer
                                     -> Equipment «있음» · die «없음»
라이브 v5 선언 (ledger_config.json)   Wafer@1 · Lot@1 · DTJob@1 · die@1
                                     -> die «있음» · Equipment «없음»
```
🔴 **오늘 만난 벽 다섯이 전부 이 한 줄입니다** — 어느 목록을 읽느냐가 어느 타입이 되느냐를 정합니다.
(씨앗 검증 · 노드 라벨 · 공사 2 축 · 리졸버 packs · 맵 다이 마킹)

## 설비의 현재 상태 — 「선언은 있는데 원자가 없다」
```
Equipment 가 주어인 원자    0
Equipment 가 목적어인 원자   0
eqp 문자열을 든 원자        24,320      <- 페이로드 «안»에 있습니다
```
die 와 «정확히 반대»입니다 — die 는 원자 1,405 가 있는데 주소가 안 풀리고,
설비는 주소는 풀리는데 원자가 없습니다.

## 그래서 설계에 반영할 것
```
① 설비는 «노드»다              속성으로 모델링하지 마십시오. 소유자 판정입니다
② 지금 없는 것은 «임시»다        오늘의 부재로 축·마킹 설계를 깎지 마십시오
③ 만드는 방법은 «선언»이다      eqp 가 페이로드에 이미 24,320건 있습니다.
                              그것을 개체로 들어올리는 선언 하나면 됩니다 — «코드 0줄»
                              관문 ① 에서 만든 폼으로 소유자가 하실 수 있는 일입니다
```
⛔ **지금 만들지 마십시오.** 설계에 자리만 두고, 실제 선언은 소유자 몫입니다.
그리고 ③(은퇴)이 얼려 있는 동안은 v1 목록을 건드리지 않습니다.

📎 앞서 낸 「노드 = 타입 + 속성 + id + 값」 모델과 이어집니다 —
설비가 노드가 되면 「이 설비의 이력」이 «걷기»가 되고, 속성으로 두면 «질의»로만 남습니다.
소유자가 전자를 고르셨습니다.
