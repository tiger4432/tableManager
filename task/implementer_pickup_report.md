# Implementer → Lead. 6b T1 + 3열 본체 착지, 소유자 실화면 검수 완료

`main` = `3259243`, 푸시 완료. 제 미커밋 0.

## 착지한 것

- `c697245` **선언 자신이 루트 행.** 총괄 판정대로 「깊이 1은 접지 않는다」 예외를 안 썼습니다.
  접기는 «자식»만 숨기고 노드는 안 숨깁니다. 자식 들여쓰기도 한 단계 내려갔습니다.
- 같은 커밋 **본체 3열** (240 / 트리 / 330). 우측은 자리만 — Integrity를 옮겼고, T3 내용은 손 안 댔습니다.
- `Set` → 「선언됨」. **패널 하네스의 닫힌 어휘를 같은 커밋에서 갱신**했습니다(빌드가 빨갛게 잡아 줬습니다).
- `3259243` **Integrity 중복 제거** — 아래 참조.

## 소유자 실화면(8080) 실측 — 격리환경 아님

소유자 지시: 「브라우저에서 안되면 격리환경에서 되어도 끝임」 · 「크롬 mcp써」.

**소스 `dt_job`** (총괄이 1행을 본 그 선언):

```
전 : 1행
후 : dt_job(RECORD) / relation(선언됨) / profile_id(선언됨) / driver(RECORD · 접힘 · 9)
```

**전부 펼친 뒤 — 41행, 깊이 0~5**
```
값 칸 x   571  × 41행   (한 자리)
상태 칸 x 1210  × 41행   (한 자리)
```

**프로필 `lot-event@1` — 278행, 깊이 0~7**
```
값 칸 x   571  × 278행  (한 자리)
상태 칸 x 1210  × 278행  (한 자리)
```

**두 모드** — 41행/41행 동일, 대비 정상(light `#f8fafb`/`#1c232b`, dark `#20252c`/`#eef2f6`),
`.oe-main` = `240px 1623.64px 330px` 양쪽 동일.

측정은 `:scope > .oe-node-value`로 **각 행의 자기 칸**만 집었습니다. 중첩 오측 우려가 있어
먼저 확인했고 — 값 칸 안에 중첩된 행은 **0개**, `querySelector`와 `:scope >`가 같은 요소였습니다.

## 🔴 눈으로 보고 잡은 결함 하나 (수치로는 안 잡혔습니다)

**Integrity가 두 번 떠 있었습니다** — 제가 옮긴 우측 330px 칸과, 그대로 남아 있던 workspace 안.
같은 패널이 나란히 두 번, 같은 문장. 디프로는 안 보입니다(호출 두 곳 다 «맞는» 코드고 한쪽이
방금 잉여가 된 것). `3259243`에서 workspace 쪽을 지웠고, 재확인 결과 **패널 1개**.

이게 소유자가 「크롬으로 직접 보라」고 하신 이유의 실례입니다. 숫자 검사는 전부 초록이었습니다.

## 열려 있는 것

- **어드민 셸 폭** — 잘림이 `#ontology-explorer-root` 바깥(상단바 버튼도 같이 잘림). 별건, 대기열 판정 요청.
- **T2/T3/T4** — 미착수. 지시 대기.

## 채널

소유자 지시로 **총괄과만 소통**합니다. 그리고 **에이전트는 자기 탭을 새로 만들게** 하겠습니다
(공유 탭에서 저와 에이전트가 같이 몰다가 서로 걸음을 망쳤습니다).
