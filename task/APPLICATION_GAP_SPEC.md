> 📎 **옮겼습니다 → [`docs/spec/APPLICATION_GAP_SPEC.md`](../docs/spec/APPLICATION_GAP_SPEC.md)** (2026-08-31, 총괄 판정)
> 런타임 계약이라 채널 파일에 두지 않습니다 — `GET /api/ledger/gaps` 가 이 표와 어긋나면 «거절»합니다.
> 소유는 «응용» 그대로입니다. 이 자리는 포인터일 뿐이고, 내용은 저쪽이 정본입니다.
