# 🔎 같은 로직, 다른 구현 — 감사 (2026-09-02)

> 소유자 지시: 「동일 로직인데 다른 영역에서 다르게 구현하고 있는것들 찾아 클라 서버 모두」
> 「계속 파도 다 기록해둬 리팩토링하자」

⚠️ **이 문서는 «발견 목록»이지 지시서가 아닙니다.** 각 항목은 「같은 질문에 답하는 코드가
   몇 곳인가」와 「그 답이 이미 갈렸나, 아직 안 갈렸나」를 적습니다.
   아직 안 갈린 것도 적습니다 — 갈리는 날 «오류가 안 나기» 때문입니다.

---

## 🔴 S1. 복합 업무키 조립 — «넷» (서버)  ← 가장 위험

행의 «정체성»을 만드는 코드가 넷이고, 재료도 빈 값 정책도 다릅니다.

```
① crud.py:2297  assemble_composite_business_key
                재료: payload 의 `updates`
                빈 값: `_unfilled_composite_parts` -> «is_blank_value»
                (이 함수의 주석이 「그 둘은 같은 질문이어야 한다」고 이미 경고합니다)
② crud.py:2914  행 동기화 — 소스 컬럼이 «바뀌었을 때»
                재료: «행 객체» getattr
                빈 값: `all(v != "")`  «손으로» · 비면 «폴백»
③ crud.py:4439  셀 «하나» 바뀌었을 때
                재료: «행 객체» getattr
                빈 값: `all(v != "")`  «손으로» · 비면 «None»
④ enrichment_mapper.py:158  파생 표 키
                재료: config 를 «자기가» 읽음
```

**왜 위험한가**
```
구분자·컬럼 목록이 바뀌는 날 «넷 중 몇»만 따라갑니다 -> 같은 행이 두 키를 갖습니다
오류 «안 납니다». 행도 들어갑니다. 나중에 «중복»으로만 보입니다
2026-09-02 소유자가 dt_log 에서 겪은 것이 이 부류입니다
```
⚠️ **총괄이 그날 「조합은 assemble_composite_business_key 가 정본」이라고 안내했는데,
   정본은 «쓰기 경로 하나»에 대해서만 정본입니다.** 그 안내를 여기서 정정합니다.

**해체 방향(제안, 미승인)**: 「재료를 받아 키를 낸다」를 «한 함수»로. 재료 셋(updates / row / config)은
호출자가 dict 로 맞춰 넘기고, 빈 값 판정은 `is_blank_value` «하나».

---

## 🔴 S2. 빈 값 판정 — 정본 «10» · 손으로 «25» (서버)

```
is_blank_value(...) 호출        10
strip() == "" 류를 손으로        25   ← raw grep. «그중 몇이 같은 질문인지는 아직 안 갈랐습니다»
```
⚠️ 25 는 «수»일 뿐 판정이 아닙니다. 파싱처럼 정당하게 다른 것이 섞여 있습니다.
다음 라운드에서 «부류로» 갈라야 합니다 — 아래 셋으로:
```
ⓐ 같은 질문인데 손으로 쓴 것        -> 정본으로 바꾼다
ⓑ 다른 질문 (예: 파서의 입력 검증)   -> 그대로 둔다
ⓒ 판단 불가                       -> 그 자리를 이름으로 남긴다
```

---

## 🟡 C1. 절단 판정 — «세 철자» (클라, 한 파일 안)

`map_editor.js` 안에서 「응답이 잘렸나」를 세 가지로 씁니다.
```
:4210  typeof result.total === 'number' && result.total > result.data.length   -> «던짐»
:5339  같은 식                                                                 -> «경고»
:10066 Number(result && result.total)                                          -> 「N개 중 M개」
```
🔴 그리고 이 셋이 `defer_total` 을 «못 붙이는» 이유였습니다 (2026-09-02 클라 판정).
   `typeof null === 'number'` 는 거짓, `Number(null)` 은 0 이라 «조용히» 꺼집니다.
**해체하면 그 셋이 한 번에 `defer_total` 을 받을 수 있게 됩니다.**

---

## 🟡 C2. fetch 봉투 모양 — «두 가지» (클라)

```
{ok:…} 로 감싸는 것        77
body 를 «그냥» 주는 것      2    map_editor2.js:63 · rnd_board/api.js:74
```
한 경계에 모양이 둘이면 읽는 쪽이 «오류 없이 빈 값»을 받습니다 — 이 저장소가 이미 맞은 부류입니다.
⚠️ 다만 그 둘이 «의도된 저수준 헬퍼»일 수 있습니다. 해체 전에 호출자를 세야 합니다.

---

## 🟡 C3. 필터 조립 — «두 벌» (클라)  ← 이미 큐에 있음

```
api.js:252  narrowingParams()          정본 (2026-09-02 신설)
timeline.js:1076~1092                  같은 q·filters 를 «손으로»
```
지금은 안 갈립니다. 셋째가 생기는 날 갈리고, 그때 «그리드가 보여주는 행»과 «footer 의 수»가
서로 다른 질문의 답이 됩니다.

---

## 🟢 C4. 수 표기 — 흩어져 있음 (클라, 낮은 위험)

`toLocaleString()` 이 admin.js · retroactive_view.js · graph_viewer.js 등에 «각자» 있고,
`graph_viewer.js:116` 만 `fmtInt` 라는 이름을 갖습니다. admin.js:363 에 「타임스탬프 단일 포맷」
주석이 있는 것으로 보아 «시각»은 한 번 통일한 적이 있고, «수»는 아직입니다.

---

## ✅ 안 갈린 것 — 확인해서 적어 둡니다 (다시 안 뒤지게)

```
서버 필터 파싱      apply_column_filters «하나»뿐 (main.py:1457)
서버 카운트/캐시 키   narrowed_table_query 가 질의와 캐시 키를 «같이» 냅니다 (2026-09-02 신설)
클라 드롭다운 닫힘   dropdown.js «한 벌» (2026-09-02 통합)
클라 Matches 표기   match_count.js «한 곳» (2026-09-02 통합, 여섯 -> 하나)
클라 토큰 키        admin_token.js «한 곳» (2026-09-02 통합)
서버 페이싱         pacing.py / pacing.json «한 표», 소비자 셋
```

---

## ⏭ 아직 «안 본» 자리
```
서버   방송(broadcast) 페이로드 조립 · 아웃박스 봉투 · 시각/타임존 처리 · 정렬 키 조립
클라   오류 토스트 · 표 그리기(부품 안에서 반복) · 좌표 변환 · 색 규칙
```

---

## ❌ S3. 아웃박스 봉투 — «취소». 전제가 무너졌습니다 (2026-09-02 재측정)

> 🔴 지시서를 쓰기 전에 다시 쟀더니 아래 두 근거가 «둘 다» 틀렸습니다.
> `processed_chain` 은 모델에 `default=False` 가 있어 안 넣어도 정상 편입되고,
> `source_name` 이 없어도 그 일곱이 쓰는 table_name 에는 «룰이 없어» 아무 룰도 안 깨웁니다.
> 조건부 위험만 남습니다 — 「룰이 있는 진짜 표 이름으로 직접 쓰는 코드가 생기는 날」.
> 아래는 «기록으로» 남깁니다.

### (취소된 근거)

이벤트를 아웃박스에 넣는 자리가 아홉인데, «봉투를 만드는 함수»를 지나는 것은 둘뿐입니다.

```
_outbox_envelope() 를 지나는 것   database.py:232 · database.py:271
   -> tx_id · user · source · ts 를 «한 곳»에서 뽑습니다
지나지 «않는» 것 (일곱)
   internal_event_client.py:135   event_uuid · table_name · event_type · payload · processed_chain
   main.py:5467                   «같은 다섯»
   main.py:5827                   (reload)
   retroactive.py:1031            «같은 다섯»
   outbox_expand.py:273           🔴 processed_chain «없음»
   system_reload.py:132           🔴 processed_chain «없음» · transaction_id 는 «있음»
```

### 왜 위험한가
```
① processed_chain 이 빠지는 자리가 «둘»
   그 컬럼엔 부분 인덱스가 걸려 있습니다 — idx_outbox_unprocessed (WHERE processed_chain = false)
   기본값에 따라 그 이벤트가 「미처리」 목록에 «들어가느냐»가 갈립니다
   그리고 그 차이는 «조용합니다» — 안 뜨면 아무도 안 찾습니다
② source_name 을 «아무도 안 싣습니다»
   그건 순환 필터의 «채널»입니다 (chain_ingestion_guide 가 「CHANNEL, not provenance」라고 못 박음)
   봉투를 안 지나는 일곱은 그 값을 «컨텍스트 변수»에 의존합니다
   -> 그 변수를 안 세운 경로에서 이벤트가 «사람 편집»으로 읽히고, 모든 룰을 깨웁니다
   (chain_ingestion_guide 가 그 사고를 ffb23d6 · 53f9187 두 건으로 기록해 두었습니다)
```

### 해체 방향 (제안, 미승인)
```
「이벤트 하나를 아웃박스에 넣는다」를 한 함수로. 봉투(tx_id·user·source·ts·processed_chain)는
그 함수가 채우고, 호출자는 «무슨 일이 있었나»(table·type·payload)만 줍니다
⚠️ S1 과 같은 성질: «채우는 것»은 합치되 «정책»(예: 어떤 이벤트가 체인을 깨우나)은 호출자 몫입니다
```

---

## 🟡 C5. 사용자에게 «말하는» 방법이 둘 (클라)

```
showToast(...)   정의 «하나» (utils.js:133) · 호출 «163»
alert(...)       «26»  — map_editor.js · clipboard.js · ui.js 등
```
`alert` 는 «페이지를 멈춥니다». 그리고 토스트와 달리 종류(info/error)도 없고 스타일도 다릅니다.
소유자 상설 「UI 문구는 번역체 금지 · 새 영역·모드·모달 금지」의 «모달»이 사실상 이것입니다.
⚠️ 다만 일부는 «막아야 하는» 자리일 수 있습니다(저장 실패 등). 부류로 갈라야 합니다:
```
ⓐ 알림이면 충분      -> showToast
ⓑ 사람을 «세워야» 함  -> 그러면 alert 이 아니라 «카드 안 확인»입니다
                     (2026-08-31 소유자가 네이티브 confirm 을 이미 물렸습니다 — 같은 판정)
```

---

## 🟡 C6. 표 그리기 — 템플릿이 «하나 있는데» 나머지가 안 씁니다 (클라)

```
정본     rnd_board/table_part.js   rb-table / -head / -row / -cell
        (2026-08-24 소유자 상설 「근원 템플릿 요소 개발 후 데이터 갈아끼우기」의 결과)
안 쓰는 것
   enrichment_reference_view.js:366   createElement('table') · reference-view-table
   graph_viewer.js:319 · :865         conn-table  (같은 파일에 «두 번»)
   graph_viewer.js:935 · :943         props-table (같은 파일에 «두 번»)
   map_editor.js:7196                 «인라인 스타일»로 직접 <table …>
```
🔴 `graph_viewer.js` 는 «한 파일 안에서» 같은 표를 두 벌씩 그립니다. 상설이 막는 그 모양이
   부품 «밖»이 아니라 «안»에서 나고 있습니다 — 2026-08-24 보드가 적은 그대로입니다.
⚠️ table_part 는 rnd_board 를 위해 만들어진 것이라 «그대로» 쓸 수 있는지는 확인이 필요합니다.
   할 수 없으면 그건 「템플릿이 아직 일반적이지 않다」는 뜻이고, 그 자체가 결과입니다.

---

## 📋 지금까지 — 우선순위 (총괄 잠정)

```
🔴 S1  복합 업무키 조립 «넷»          지시서 나감 (74928b79). 데이터 정합성
🔴 S3  아웃박스 봉투 «아홉 중 둘»      S1 과 같은 성질 · 같은 파일 근처라 «순차»로
🔴 S2  빈 값 판정 10 vs 25           먼저 «부류로 갈라야» 합니다. 수는 판정이 아닙니다
🟡 C1  절단 판정 «세 철자»            합치면 defer_total 을 그 셋이 받게 됩니다
🟡 C3  필터 조립 «두 벌»              지금은 안 갈림. 셋째가 생기는 날 갈림
🟡 C2  fetch 봉투 «두 모양»           그 둘이 «저수준 헬퍼»인지 먼저 확인
🟡 C5  alert 26                     부류로 갈라야 (알림 / 세워야 함)
🟡 C6  표 그리기                      template 이 «일반적인지»부터
🟢 C4  수 표기
```
⚠️ **전부를 한 라운드에 넣지 마십시오.** S1·S3 는 «데이터»이고 나머지는 «표현»입니다.
   섞으면 무회귀 게이트가 뒤섞여서 무엇이 깨졌는지 못 가립니다.
