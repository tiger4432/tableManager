# -*- coding: utf-8 -*-
"""The chain's operator door. See `python -m chain`.

⛔ NOT THE CHAIN'S CODE. The worker, the replay and the key gate live where they lived
(`chain_ingestion_worker.py`, `chain_replay.py`, `scripts/chain_replay_cli.py`); this package
exists so an operator can reach them by one name they can guess. Moving them here would be a
rename with no reader asking for it.
"""
